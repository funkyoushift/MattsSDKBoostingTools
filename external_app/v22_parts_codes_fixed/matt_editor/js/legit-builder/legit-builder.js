        // ===== LEGIT ITEM BUILDER INLINE LOADER =====
        let data = null;


        function createLegitBuilderInstance(index, tabId, idPrefix, relaxedPicker) {
            return {
                index: index,
                tabId: tabId,
                idPrefix: idPrefix || "",
                relaxedPicker: !!relaxedPicker,
                selectedParts: new Map(),
                /** Modded tab: rootKey -> [{ tableName, partKey, qty }] in UI order (compact lines + qty). */
                miPartLines: new Map(),
                innerTab: "roots",
                partClickAttached: false,
                mount: null,
                /** Modded compact slots: key `${rootKey}\0${normalizeDepTableName(table)}` -> all-manufacturers browse list */
                miSlotAllManufacturersUnlock: new Map(),
                /** Modded: rootKey -> part type names user added (global catalog) beyond this root’s default slots */
                miModdedExtraPartSlots: new Map()
            };
        }
        window.legitBuilderInstances = [];
        window.__lbCtx = null;
        window.legitBuilderSetCtx = function (idx) {
            var list = window.legitBuilderInstances;
            if (list && list[idx]) window.__lbCtx = list[idx];
        };
        var legitBuilderSetCtx = window.legitBuilderSetCtx;
        /** Align Legit Builder instance with the visible main tab (Modded vs Legit). Default page has Modded tab active but __lbCtx used to stay on instance 0, so decode read the wrong #serialInput. */
        window.syncLegitBuilderCtxToActiveMainTab = function syncLegitBuilderCtxToActiveMainTab() {
            var mi = document.getElementById('item-editor-tab');
            var leg = document.getElementById('legit-builder-tab');
            if (mi && mi.classList.contains('active')) {
                window.legitBuilderSetCtx(1);
            } else if (leg && leg.classList.contains('active')) {
                window.legitBuilderSetCtx(0);
            }
        };
        function lbEl(id) {
            var ctx = window.__lbCtx;
            var p = ctx && ctx.idPrefix ? ctx.idPrefix : "";
            return document.getElementById(p + id);
        }
        /** Modded tab: paste/import textarea. Legit tab: #serialInput. */
        function lbSerialInputEl() {
            if (window.__lbCtx && window.__lbCtx.relaxedPicker) {
                return document.getElementById('mi_parseSerialInput');
            }
            return lbEl('serialInput');
        }
        /** Modded tab: generated human serial. Legit tab: #finalOutputString. */
        function lbGeneratedHumanEl() {
            return lbEl('finalOutputString');
        }
        function _sp() {
            return window.__lbCtx.selectedParts;
        }
        function forEachLegitInstance(fn) {
            var list = window.legitBuilderInstances;
            if (!list) return;
            var prev = window.__lbCtx;
            try {
                for (var i = 0; i < list.length; i++) {
                    window.__lbCtx = list[i];
                    fn(list[i], i);
                }
            } finally {
                window.__lbCtx = prev;
            }
        }
        window.forEachLegitInstance = forEachLegitInstance;
        function initLegitBuilderInstances() {
            var m0 = document.querySelector("#legit-builder-tab .legit-inline-app");
            var m1 = document.querySelector("#item-editor-tab .legit-inline-app");
            window.legitBuilderInstances = [
                createLegitBuilderInstance(0, "legit-builder-tab", "", false),
                createLegitBuilderInstance(1, "item-editor-tab", "mi_", true)
            ];
            if (m0) window.legitBuilderInstances[0].mount = m0;
            if (m1) window.legitBuilderInstances[1].mount = m1;
            if (!window.__lbCtx) window.__lbCtx = window.legitBuilderInstances[0];
        }

        window.getActiveLegitBuilderBase85 = function getActiveLegitBuilderBase85() {
            function trimVal(el) {
                if (!el || el.value == null) return '';
                var t = String(el.value).trim();
                if (!t || t === 'Serializing...' || t.indexOf('Error:') === 0) return '';
                return t;
            }
            var mod = document.getElementById('item-editor-tab');
            var leg = document.getElementById('legit-builder-tab');
            if (mod && mod.classList.contains('active')) {
                var m = trimVal(document.getElementById('mi_finalOutputBase85'));
                if (m) return m;
            }
            if (leg && leg.classList.contains('active')) {
                var l = trimVal(document.getElementById('finalOutputBase85'));
                if (l) return l;
            }
            return trimVal(document.getElementById('mi_finalOutputBase85')) || trimVal(document.getElementById('finalOutputBase85')) || '';
        };

        let loadedFileNames = [];

        /** Cosmetic unlock ids (Unlockable_*.Entry) merged from Nexus-Data-Resident*.json during each mergeMultipleFiles run. */
        const nexusCosmeticUnlockIds = new Set();

        /** inv_custom token -> { invCustom, label, sortOrder } from Nexus-Data-inv_custom*.json (Modded skin dropdown). */
        const nexusInvCustomSkinByToken = new Map();

        /** SharedProgress_* unlock ids merged from Nexus-Data-challenge*.json during each mergeMultipleFiles run. */
        const nexusSharedProgressUnlockIds = new Set();
        /** normalized id -> { normalized, sectionKey, rawNcs } */
        const nexusChallengeUnlockableById = new Map();
        /** DLMD_* discovery tokens from gbx_discovery_location_meta_data*.json */
        const nexusDiscoveryDlmdTokens = new Set();
        /** token -> { token, locationType, displayRef, categoryKey, categoryLabel } */
        const nexusDiscoveryDlmdEntryByToken = new Map();
        /** Distinct world map names from discovery meta (for pc_shared level sync). */
        const nexusDiscoveryWorldMapNames = new Set();

        /** Seen any Nexus-Data-progress_graph*.json during merge (for profile progression_shared manifest). */
        let nexusProgressGraphFilesSeen = false;
        /** graph name -> { name, group_def_name, nodes } from latest shard overlay */
        const nexusProgressionSharedGraphsByName = new Map();

        /** missionset_key -> { missions: { mission_key: meta }, dependencies: string[] } — built from Nexus-Data-Mission*.json + missionset*.json */
        let nexusMissionManifestBySet = Object.create(null);

        /** challenge display name -> { challenge, associatedstat, challengetype, goalValue } from Nexus-Data-challenge*.json */
        let nexusChallengeDefByName = Object.create(null);
        let nexusChallengeDefFilesSeen = false;
        /** Challenge ids from challenge'X' refs in Nexus-Data-challenge_list4/6.json only */
        const nexusChallengeListCompletionIds = new Set();
        /** Subset: challengelist_achievements_cowbell / _cello entries only */
        const nexusChallengeAchievementListIds = new Set();
        let nexusChallengeListFilesSeen = false;
        let nexusMissionManifestFilesSeen = false;

        const NEXUS_COSMETIC_TAG_PREFIX = 'Reward.Item.Cosmetic.';

        const SHARED_PROGRESS_UNLOCK_RE = /unlockable'(SharedProgress_[^']+)'/g;
        const CHALLENGE_UNLOCKABLE_RE = /unlockable'([^']+)'/g;

        function normalizeSharedProgressUnlockId(ncsId) {
            if (!ncsId || typeof ncsId !== 'string') return '';
            const dot = ncsId.indexOf('.');
            if (dot === -1) return ncsId;
            return ncsId.slice(0, dot + 1) + ncsId.slice(dot + 1).toLowerCase();
        }

        function normalizeChallengeUnlockableId(ncsId) {
            if (!ncsId || typeof ncsId !== 'string') return '';
            if (ncsId.startsWith('SharedProgress_')) {
                return normalizeSharedProgressUnlockId(ncsId);
            }
            return ncsId.toLowerCase();
        }

        function challengeUnlockableSectionKey(normalizedId) {
            if (!normalizedId || typeof normalizedId !== 'string') return null;
            if (normalizedId.startsWith('SharedProgress_')) {
                const m = /^SharedProgress_([^.]+)\./.exec(normalizedId);
                return m ? 'sharedprogress_' + m[1].toLowerCase() : null;
            }
            const dot = normalizedId.indexOf('.');
            if (dot === -1) return null;
            return normalizedId.slice(0, dot);
        }

        function shouldIncludeChallengeUnlockableRaw(raw) {
            if (!raw || typeof raw !== 'string' || raw.indexOf('.') === -1) return false;
            if (raw.startsWith('Unlockable_')) return false;
            if (raw.startsWith('SharedProgress_')) return true;
            const pre = raw.slice(0, raw.indexOf('.')).toLowerCase();
            return pre.endsWith('_challenges');
        }

        function extractSharedProgressUnlockIdsFromChallenge(rawData, targetSet) {
            if (!rawData || !rawData.challenge || !Array.isArray(rawData.challenge.records)) return;
            let m;
            const blob = JSON.stringify(rawData);
            SHARED_PROGRESS_UNLOCK_RE.lastIndex = 0;
            while ((m = SHARED_PROGRESS_UNLOCK_RE.exec(blob)) !== null) {
                const normalized = normalizeSharedProgressUnlockId(m[1]);
                if (normalized) targetSet.add(normalized);
            }
        }

        function extractChallengeUnlockablesFromChallenge(rawData) {
            if (!rawData || !rawData.challenge || !Array.isArray(rawData.challenge.records)) return;
            const blob = JSON.stringify(rawData);
            let m;
            CHALLENGE_UNLOCKABLE_RE.lastIndex = 0;
            while ((m = CHALLENGE_UNLOCKABLE_RE.exec(blob)) !== null) {
                const raw = m[1];
                if (!shouldIncludeChallengeUnlockableRaw(raw)) continue;
                const normalized = normalizeChallengeUnlockableId(raw);
                if (!normalized) continue;
                const sectionKey = challengeUnlockableSectionKey(normalized);
                if (!sectionKey) continue;
                nexusChallengeUnlockableById.set(normalized, {
                    normalized,
                    sectionKey,
                    rawNcs: raw
                });
            }
        }

        function parseDiscoveryLocationTypeShort(raw) {
            if (!raw || typeof raw !== 'string') return '';
            const m = raw.match(/gbx_discovery_location_type_def'([^']+)'/i);
            return m && m[1] ? m[1] : '';
        }

        function parseDiscoveryDisplayDataRef(raw) {
            if (!raw || typeof raw !== 'string') return '';
            const m = raw.match(/display_data'([^']+)'/i);
            return m && m[1] ? m[1] : '';
        }

        function discoveryDlmdCategoryRank(categoryKey) {
            if (categoryKey === 'other') return 0;
            if (categoryKey === 'nothing') return 1;
            return 2;
        }

        function discoveryCategoryFromLocationType(typeShort) {
            const t = typeShort || '';
            if (!t || t === 'DiscoveryNothing') return { key: 'nothing', label: 'Map scaffolding' };
            if (t.indexOf('LevelTravel') !== -1) return { key: 'level_travel', label: 'Level travel' };
            if (t.indexOf('FastTravel') !== -1) return { key: 'fast_travel', label: 'Fast travel' };
            if (
                t.indexOf('ResupplyVendor') !== -1 ||
                t.indexOf('HealthVendor') !== -1 ||
                t.indexOf('MunitionsVendor') !== -1 ||
                t.indexOf('BlackMarketVendor') !== -1
            ) {
                return { key: 'vendors', label: 'Vendors & machines' };
            }
            if (t.indexOf('PointOfInterest') !== -1) return { key: 'poi', label: 'Points of interest' };
            if (t.indexOf('AugerMine') !== -1) return { key: 'auger_mines', label: 'Auger mines' };
            if (t.indexOf('EchoLog') !== -1) return { key: 'echo_logs', label: 'Echo logs' };
            if (t.indexOf('VaultSymbol') !== -1) return { key: 'vault_symbols', label: 'Vault symbols' };
            if (t.indexOf('Collectible_Cache') !== -1) return { key: 'caches', label: 'Survivalist caches' };
            if (t.indexOf('Secondary_Vault') !== -1) return { key: 'vaults_secondary', label: 'Secondary vaults' };
            if (t.indexOf('BossFight') !== -1) return { key: 'boss_fights', label: 'Boss fights' };
            if (t.indexOf('DecryptStation') !== -1) return { key: 'decrypt_stations', label: 'Decrypt stations' };
            return { key: 'other', label: 'Other' };
        }

        function upsertDiscoveryDlmdEntryFromRow(row) {
            if (!row || typeof row !== 'object') return;
            const v = row.gbx_discovery_location_meta_data;
            if (typeof v !== 'string' || v.indexOf('DLMD_') !== 0) return;
            const typeShort = parseDiscoveryLocationTypeShort(row.locationtype);
            const displayRef = parseDiscoveryDisplayDataRef(row.locationdisplaydata);
            const cat = discoveryCategoryFromLocationType(typeShort);
            const prev = nexusDiscoveryDlmdEntryByToken.get(v);
            const next = {
                token: v,
                locationType: typeShort,
                displayRef,
                categoryKey: cat.key,
                categoryLabel: cat.label
            };
            if (!prev || discoveryDlmdCategoryRank(cat.key) > discoveryDlmdCategoryRank(prev.categoryKey)) {
                nexusDiscoveryDlmdEntryByToken.set(v, next);
            }
            nexusDiscoveryDlmdTokens.add(v);
        }

        function extractDiscoveryDlmdTokensFromMeta(rawData) {
            const meta = rawData && rawData.gbx_discovery_location_meta_data;
            if (!meta || !Array.isArray(meta.records)) return;
            for (const rec of meta.records) {
                const entries = rec && rec.entries;
                if (!Array.isArray(entries)) continue;
                for (const entryObj of entries) {
                    if (!entryObj || typeof entryObj !== 'object') continue;
                    for (const key of Object.keys(entryObj)) {
                        const row = entryObj[key];
                        if (!row || typeof row !== 'object') continue;
                        upsertDiscoveryDlmdEntryFromRow(row);
                        const wm = row.worldmapname;
                        if (typeof wm === 'string' && wm.trim()) {
                            nexusDiscoveryWorldMapNames.add(wm.trim());
                        }
                        const chain = row.owningmapchain;
                        if (Array.isArray(chain)) {
                            for (let ci = 0; ci < chain.length; ci++) {
                                const m = chain[ci] && chain[ci].mapname;
                                if (typeof m === 'string' && m.trim()) {
                                    nexusDiscoveryWorldMapNames.add(m.trim());
                                }
                            }
                        }
                    }
                }
            }
        }

        function parseProgressGraphMaxPoints(val) {
            if (val == null || val === '') return 0;
            const n = Number(val);
            return Number.isFinite(n) ? n : 0;
        }

        function ingestProgressGraphNexusFile(rawData) {
            const pg = rawData && rawData.progress_graph;
            if (!pg || !Array.isArray(pg.records)) return;
            nexusProgressGraphFilesSeen = true;
            for (let ri = 0; ri < pg.records.length; ri++) {
                const record = pg.records[ri];
                const entries = record && record.entries;
                if (!Array.isArray(entries)) continue;
                for (let ei = 0; ei < entries.length; ei++) {
                    const entryObj = entries[ei];
                    if (!entryObj || typeof entryObj !== 'object') continue;
                    const keys = Object.keys(entryObj);
                    for (let ki = 0; ki < keys.length; ki++) {
                        const g = entryObj[keys[ki]];
                        if (!g || typeof g !== 'object') continue;
                        const graphName = g.progress_graph;
                        if (graphName === 'sdu_upgrades') {
                            const nodes = [];
                            const pairs = g.nodes && g.nodes.pairs;
                            if (pairs && typeof pairs === 'object') {
                                const pkeys = Object.keys(pairs);
                                for (let pi = 0; pi < pkeys.length; pi++) {
                                    const p = pairs[pkeys[pi]];
                                    if (!p || typeof p !== 'object') continue;
                                    const nodeKey = p.key;
                                    if (typeof nodeKey !== 'string' || !nodeKey) continue;
                                    const v = p.value || {};
                                    nodes.push({
                                        name: nodeKey,
                                        points_spent: parseProgressGraphMaxPoints(v.maxprogresspoints)
                                    });
                                }
                            }
                            nexusProgressionSharedGraphsByName.set('sdu_upgrades', {
                                name: 'sdu_upgrades',
                                group_def_name: 'Oak2_GlobalProgressGraph_Group',
                                nodes
                            });
                        } else if (graphName === 'vaultpower_vaultreward_upgrades') {
                            const nodes = [];
                            const pairs = g.nodes && g.nodes.pairs;
                            if (pairs && typeof pairs === 'object') {
                                const pkeys = Object.keys(pairs);
                                for (let pi = 0; pi < pkeys.length; pi++) {
                                    const p = pairs[pkeys[pi]];
                                    if (!p || typeof p !== 'object') continue;
                                    const v = p.value || {};
                                    const alias = v.alias;
                                    if (typeof alias !== 'string' || !alias) continue;
                                    nodes.push({ name: alias, is_activated: true });
                                }
                            }
                            nexusProgressionSharedGraphsByName.set('vaultpower_vaultreward_upgrades', {
                                name: 'vaultpower_vaultreward_upgrades',
                                group_def_name: 'ProgressGroup_DarkSiren',
                                nodes
                            });
                        }
                    }
                }
            }
        }

        function finalizeNexusProgressionSharedManifest() {
            if (!nexusProgressGraphFilesSeen) {
                window.NEXUS_PROGRESSION_SHARED_MANIFEST = null;
                try {
                    window.dispatchEvent(
                        new CustomEvent('nexus-progression-shared-manifest-updated', { detail: { graphCount: 0 } })
                    );
                } catch (_) {
                    /* ignore */
                }
                return;
            }
            let embedded = null;
            if (typeof window.profileDataGetProgressionSharedManifestEmbedded === 'function') {
                try {
                    embedded = window.profileDataGetProgressionSharedManifestEmbedded();
                } catch (_) {
                    embedded = null;
                }
            }
            const embGraphs = embedded && Array.isArray(embedded.graphs) ? embedded.graphs : [];
            const byName = new Map();
            for (let i = 0; i < embGraphs.length; i++) {
                const g = embGraphs[i];
                if (g && g.name) {
                    try {
                        byName.set(g.name, JSON.parse(JSON.stringify(g)));
                    } catch (_) {
                        byName.set(g.name, g);
                    }
                }
            }
            const v = nexusProgressionSharedGraphsByName.get('vaultpower_vaultreward_upgrades');
            const s = nexusProgressionSharedGraphsByName.get('sdu_upgrades');
            if (v && v.nodes && v.nodes.length) byName.set('vaultpower_vaultreward_upgrades', v);
            if (s && s.nodes && s.nodes.length) byName.set('sdu_upgrades', s);
            const order = ['vaultpower_vaultreward_upgrades', 'sdu_upgrades'];
            const graphs = [];
            for (let oi = 0; oi < order.length; oi++) {
                const gg = byName.get(order[oi]);
                if (gg) graphs.push(gg);
            }
            const defEcho =
                embedded && embedded.defaultEchoTokenProgressPoints != null
                    ? embedded.defaultEchoTokenProgressPoints
                    : 3790;
            window.NEXUS_PROGRESSION_SHARED_MANIFEST = {
                graphs,
                defaultEchoTokenProgressPoints: defEcho,
                source: 'nexus-progress_graph'
            };
            try {
                window.dispatchEvent(
                    new CustomEvent('nexus-progression-shared-manifest-updated', {
                        detail: { graphCount: graphs.length }
                    })
                );
            } catch (_) {
                /* ignore */
            }
        }

        function missionsetSaveKeyFromNexusInner(inner) {
            if (!inner || typeof inner !== 'string') return '';
            if (inner.indexOf('missionset_') === 0) return inner;
            if (inner.indexOf('MissionSet_') === 0) {
                return 'missionset_' + inner.slice('MissionSet_'.length).toLowerCase();
            }
            return '';
        }

        function missionsetSaveKeyFromRef(ref) {
            if (!ref || typeof ref !== 'string') return '';
            const m = ref.match(/missionset'([^']+)'/i);
            return m ? missionsetSaveKeyFromNexusInner(m[1]) : '';
        }

        function collectMissionsetCompletedDeps(obj, outArr) {
            if (!obj || typeof obj !== 'object') return;
            if (typeof obj.completed === 'string' && obj.completed.indexOf("missionset'") !== -1) {
                const k = missionsetSaveKeyFromRef(obj.completed);
                if (k && outArr.indexOf(k) === -1) outArr.push(k);
            }
            const vals = Object.values(obj);
            for (let i = 0; i < vals.length; i++) {
                const v = vals[i];
                if (v && typeof v === 'object') collectMissionsetCompletedDeps(v, outArr);
            }
        }

        function stripMissionUxTitle(text) {
            if (text == null || typeof text !== 'string') return '';
            const t = text.replace(/^NexusSerialized,\s*[0-9A-Fa-f]{32},\s*/i, '').trim();
            return t || text.trim();
        }

        /**
         * Build save-shaped mission.final entries for objectives marked remember-endstate in Nexus.
         * Keys: stem + '_endstate' normally; if bcustomrememberendstatefactname + rememberendstatealiasname, use alias lowercased (no _endstate).
         */
        function extractRememberedEndstateFinalsFromMissionDef(def) {
            const out = Object.create(null);
            if (!def || typeof def !== 'object') return out;
            const entries = def.__dep_entries;
            if (!Array.isArray(entries)) return out;
            const metaKeys = new Set(['depTableName', 'depTableId', 'depIndex']);
            for (let i = 0; i < entries.length; i++) {
                const row = entries[i];
                if (!row || typeof row !== 'object' || row.depTableName !== 'objective') continue;
                const keys = Object.keys(row).filter(function (k) {
                    return !metaKeys.has(k);
                });
                if (keys.length < 1) continue;
                const stem = keys[0];
                const payload = row[stem];
                if (!payload || typeof payload !== 'object') continue;
                const remember =
                    row.brememberendstateinalias === 'true' ||
                    row.brememberendstateforever === 'true' ||
                    payload.brememberendstateinalias === 'true' ||
                    payload.brememberendstateforever === 'true';
                if (!remember) continue;
                let finalKey;
                if (
                    payload.bcustomrememberendstatefactname === 'true' &&
                    payload.rememberendstatealiasname &&
                    typeof payload.rememberendstatealiasname === 'string'
                ) {
                    finalKey = payload.rememberendstatealiasname.toLowerCase();
                } else {
                    finalKey = stem + '_endstate';
                }
                out[finalKey] = 'completed';
            }
            return out;
        }

        function ensureNexusMissionSetNode(setKey) {
            if (!setKey) return null;
            if (!nexusMissionManifestBySet[setKey]) {
                nexusMissionManifestBySet[setKey] = { missions: Object.create(null), dependencies: [] };
            }
            return nexusMissionManifestBySet[setKey];
        }

        function ingestMissionNexusSidecar(rawData) {
            const m = rawData && rawData.mission;
            if (!m || !Array.isArray(m.records)) return;
            nexusMissionManifestFilesSeen = true;
            for (let ri = 0; ri < m.records.length; ri++) {
                const record = m.records[ri];
                const entries = record && record.entries;
                if (!Array.isArray(entries)) continue;
                for (let ei = 0; ei < entries.length; ei++) {
                    const entryObj = entries[ei];
                    if (!entryObj || typeof entryObj !== 'object') continue;
                    const entryDepEntries = Array.isArray(entryObj.__dep_entries)
                        ? entryObj.__dep_entries
                        : null;
                    const ekeys = Object.keys(entryObj);
                    for (let ki = 0; ki < ekeys.length; ki++) {
                        const missionKey = ekeys[ki];
                        if (missionKey === '__dep_entries') continue;
                        const def = entryObj[missionKey];
                        if (!def || typeof def !== 'object') continue;
                        const setKey = missionsetSaveKeyFromRef(def.missionset);
                        if (!setKey) continue;
                        const node = ensureNexusMissionSetNode(setKey);
                        if (!node) continue;
                        const prev = node.missions[missionKey];
                        const prevObj = prev && typeof prev === 'object' ? prev : null;
                        const title = stripMissionUxTitle(
                            def.ux_display && typeof def.ux_display.text === 'string' ? def.ux_display.text : ''
                        );
                        const meta = prevObj ? Object.assign({}, prevObj) : {};
                        if (title) meta.title = title;
                        if (def.missiontype != null) meta.missiontype = String(def.missiontype);
                        if (def.worldregion != null) meta.worldregion = String(def.worldregion);
                        if (def.missiondifficulty != null) meta.missiondifficulty = String(def.missiondifficulty);
                        if (def.platformactivityid != null) meta.platformactivityid = String(def.platformactivityid);
                        const defForFinals =
                            Array.isArray(def.__dep_entries)
                                ? def
                                : entryDepEntries
                                  ? Object.assign({}, def, { __dep_entries: entryDepEntries })
                                  : def;
                        const finals = extractRememberedEndstateFinalsFromMissionDef(defForFinals);
                        if (Object.keys(finals).length > 0) {
                            meta.rememberedFinals = Object.assign({}, prevObj && prevObj.rememberedFinals, finals);
                        } else if (prevObj && prevObj.rememberedFinals) {
                            meta.rememberedFinals = prevObj.rememberedFinals;
                        }
                        node.missions[missionKey] = meta;
                    }
                }
            }
        }

        function ingestMissionsetNexusSidecar(rawData) {
            const ms = rawData && rawData.missionset;
            if (!ms || !Array.isArray(ms.records)) return;
            nexusMissionManifestFilesSeen = true;
            for (let ri = 0; ri < ms.records.length; ri++) {
                const record = ms.records[ri];
                const entries = record && record.entries;
                if (!Array.isArray(entries)) continue;
                for (let ei = 0; ei < entries.length; ei++) {
                    const entryObj = entries[ei];
                    if (!entryObj || typeof entryObj !== 'object') continue;
                    const ekeys = Object.keys(entryObj);
                    for (let ki = 0; ki < ekeys.length; ki++) {
                        const setKey = ekeys[ki];
                        if (typeof setKey !== 'string' || setKey.indexOf('missionset_') !== 0) continue;
                        const def = entryObj[setKey];
                        if (!def || typeof def !== 'object') continue;
                        const node = ensureNexusMissionSetNode(setKey);
                        if (!node) continue;
                        const deps = [];
                        if (def.generateddependencies) {
                            collectMissionsetCompletedDeps(def.generateddependencies, deps);
                        }
                        node.dependencies = deps;
                    }
                }
            }
        }

        function finalizeNexusMissionManifestForSaveEditor() {
            const keys = Object.keys(nexusMissionManifestBySet);
            if (!nexusMissionManifestFilesSeen || keys.length === 0) {
                window.NEXUS_MISSION_MANIFEST = null;
                try {
                    window.dispatchEvent(
                        new CustomEvent('nexus-mission-manifest-updated', { detail: { missionsetCount: 0 } })
                    );
                } catch (_) {
                    /* ignore */
                }
                return;
            }
            const out = Object.create(null);
            for (let i = 0; i < keys.length; i++) {
                const setKey = keys[i];
                const src = nexusMissionManifestBySet[setKey];
                const missions = Object.create(null);
                const mk = Object.keys(src.missions || {});
                for (let j = 0; j < mk.length; j++) {
                    missions[mk[j]] = Object.assign({}, src.missions[mk[j]]);
                }
                out[setKey] = {
                    missions,
                    dependencies: Array.isArray(src.dependencies) ? src.dependencies.slice() : []
                };
            }
            window.NEXUS_MISSION_MANIFEST = out;
            try {
                window.dispatchEvent(
                    new CustomEvent('nexus-mission-manifest-updated', {
                        detail: { missionsetCount: keys.length }
                    })
                );
            } catch (_) {
                /* ignore */
            }
        }

        function goalValueFromChallengeTiers(tiers) {
            if (!Array.isArray(tiers) || tiers.length === 0) return null;
            let max = 0;
            let any = false;
            for (let i = 0; i < tiers.length; i++) {
                const row = tiers[i];
                const gv = row && row.goalvalue;
                if (gv == null) continue;
                const n = parseFloat(String(gv));
                if (!Number.isNaN(n)) {
                    any = true;
                    if (n > max) max = n;
                }
            }
            return any ? max : null;
        }

        function mergeNexusChallengeDefEntry(name, def) {
            if (!name || typeof name !== 'string' || !def || typeof def !== 'object') return;
            const stat = def.associatedstat != null ? String(def.associatedstat).trim() : '';
            const ctype = def.challengetype != null ? String(def.challengetype) : '';
            const gv = goalValueFromChallengeTiers(def.challengetiers);
            const prev = nexusChallengeDefByName[name];
            const out = {
                challenge: name,
                associatedstat: stat || (prev ? prev.associatedstat : ''),
                challengetype: ctype || (prev ? prev.challengetype : ''),
                goalValue: gv != null ? gv : prev && prev.goalValue != null ? prev.goalValue : null
            };
            if (prev) {
                if (!out.associatedstat && prev.associatedstat) out.associatedstat = prev.associatedstat;
                if (!out.challengetype && prev.challengetype) out.challengetype = prev.challengetype;
                if (out.goalValue == null && prev.goalValue != null) out.goalValue = prev.goalValue;
                if (prev.goalValue != null && out.goalValue != null) {
                    out.goalValue = Math.max(prev.goalValue, out.goalValue);
                }
            }
            nexusChallengeDefByName[name] = out;
            const clotM = name.match(/^challenge_clot_(\d+)$/i);
            if (clotM) {
                const n = parseInt(clotM[1], 10);
                if (!Number.isNaN(n)) {
                    const pascal = 'Challenge_Clot_' + String(n).padStart(2, '0');
                    const lower = 'challenge_clot_' + n;
                    for (let ai = 0; ai < 2; ai++) {
                        const alt = ai === 0 ? pascal : lower;
                        if (alt === name) continue;
                        if (nexusChallengeDefByName[alt]) continue;
                        nexusChallengeDefByName[alt] = Object.assign({}, out, { challenge: alt });
                    }
                }
            }
        }

        function ingestChallengeDefsForSaveEditor(rawData) {
            const ch = rawData && rawData.challenge;
            if (!ch || !Array.isArray(ch.records)) return;
            nexusChallengeDefFilesSeen = true;
            for (let ri = 0; ri < ch.records.length; ri++) {
                const record = ch.records[ri];
                const entries = record && record.entries;
                if (!Array.isArray(entries)) continue;
                for (let ei = 0; ei < entries.length; ei++) {
                    const entryObj = entries[ei];
                    if (!entryObj || typeof entryObj !== 'object') continue;
                    const ekeys = Object.keys(entryObj);
                    for (let ki = 0; ki < ekeys.length; ki++) {
                        const rowKey = ekeys[ki];
                        const def = entryObj[rowKey];
                        if (!def || typeof def !== 'object') continue;
                        const cname = def.challenge != null ? String(def.challenge) : '';
                        if (!cname) continue;
                        mergeNexusChallengeDefEntry(cname, def);
                    }
                }
            }
        }

        const NEXUS_CHALLENGE_LIST_PRESET_FILENAMES = [
            'nexus-data-challenge_list4.json',
            'nexus-data-challenge_list6.json'
        ];

        function ingestChallengeListForSaveEditor(filename, rawData) {
            const base = String(filename || '')
                .split('/')
                .pop()
                .toLowerCase();
            if (NEXUS_CHALLENGE_LIST_PRESET_FILENAMES.indexOf(base) === -1) return;
            const cl = rawData && rawData.challenge_list;
            if (!cl || !Array.isArray(cl.records)) return;
            nexusChallengeListFilesSeen = true;
            const achCow = 'challengelist_achievements_cowbell';
            const achCello = 'challengelist_achievements_cello';
            for (let ri = 0; ri < cl.records.length; ri++) {
                const record = cl.records[ri];
                const entries = record && record.entries;
                if (!Array.isArray(entries)) continue;
                for (let ei = 0; ei < entries.length; ei++) {
                    const entryObj = entries[ei];
                    if (!entryObj || typeof entryObj !== 'object') continue;
                    const lkeys = Object.keys(entryObj);
                    for (let li = 0; li < lkeys.length; li++) {
                        const listKey = lkeys[li];
                        const listDef = entryObj[listKey];
                        if (!listDef || typeof listDef !== 'object') continue;
                        const challenges = listDef.challenges;
                        if (!Array.isArray(challenges)) continue;
                        const isAchList = listKey === achCow || listKey === achCello;
                        for (let ci = 0; ci < challenges.length; ci++) {
                            const ref = challenges[ci];
                            if (typeof ref !== 'string') continue;
                            const m = ref.match(/challenge'([^']+)'/i);
                            if (!m || !m[1]) continue;
                            const inner = m[1];
                            nexusChallengeListCompletionIds.add(inner);
                            if (isAchList) nexusChallengeAchievementListIds.add(inner);
                        }
                    }
                }
            }
        }

        function finalizeNexusChallengeManifestForSaveEditor() {
            if (nexusChallengeDefFilesSeen && Object.keys(nexusChallengeDefByName).length > 0) {
                const out = Object.create(null);
                const names = Object.keys(nexusChallengeDefByName).sort();
                for (let i = 0; i < names.length; i++) {
                    const k = names[i];
                    out[k] = Object.assign({}, nexusChallengeDefByName[k]);
                }
                window.NEXUS_CHALLENGE_BY_NAME = out;
            } else {
                window.NEXUS_CHALLENGE_BY_NAME = null;
            }
            window.NEXUS_CHALLENGE_LIST_COMPLETION_IDS = nexusChallengeListFilesSeen
                ? Array.from(nexusChallengeListCompletionIds).sort()
                : [];
            window.NEXUS_CHALLENGE_ACHIEVEMENT_LIST_IDS = nexusChallengeListFilesSeen
                ? Array.from(nexusChallengeAchievementListIds).sort()
                : [];
        }

        function finalizeNexusSharedProgressForProfileEditor() {
            const sorted = Array.from(nexusSharedProgressUnlockIds).sort();
            window.NEXUS_ALL_SHARED_PROGRESS = sorted;
            const challengeList = Array.from(nexusChallengeUnlockableById.values()).sort((a, b) =>
                a.normalized.localeCompare(b.normalized)
            );
            window.NEXUS_ALL_CHALLENGE_UNLOCKABLES = challengeList;
            const dlmdSorted = Array.from(nexusDiscoveryDlmdTokens).sort();
            window.NEXUS_ALL_DISCOVERY_DLMD = dlmdSorted;
            const dlmdEntries = dlmdSorted.map(t => {
                const e = nexusDiscoveryDlmdEntryByToken.get(t);
                return (
                    e || {
                        token: t,
                        locationType: '',
                        displayRef: '',
                        categoryKey: 'other',
                        categoryLabel: 'Other'
                    }
                );
            });
            window.NEXUS_DISCOVERY_DLMD_ENTRIES = dlmdEntries;
            const categoryOrder = [
                'level_travel',
                'fast_travel',
                'vendors',
                'poi',
                'auger_mines',
                'echo_logs',
                'vault_symbols',
                'caches',
                'vaults_secondary',
                'boss_fights',
                'decrypt_stations',
                'nothing',
                'other'
            ];
            const byCat = new Map();
            for (let i = 0; i < dlmdEntries.length; i++) {
                const e = dlmdEntries[i];
                const k = e.categoryKey || 'other';
                if (!byCat.has(k)) {
                    byCat.set(k, {
                        key: k,
                        label: e.categoryLabel || 'Other',
                        tokens: []
                    });
                }
                byCat.get(k).tokens.push(e.token);
            }
            const groups = [];
            for (let oi = 0; oi < categoryOrder.length; oi++) {
                const ck = categoryOrder[oi];
                if (byCat.has(ck)) {
                    const g = byCat.get(ck);
                    g.tokens.sort();
                    groups.push(g);
                    byCat.delete(ck);
                }
            }
            byCat.forEach(g => {
                g.tokens.sort();
                groups.push(g);
            });
            window.NEXUS_DISCOVERY_DLMD_GROUPS = groups;
            const worldSorted = Array.from(nexusDiscoveryWorldMapNames).sort();
            window.NEXUS_DISCOVERY_WORLD_MAP_NAMES = worldSorted;
            try {
                window.dispatchEvent(
                    new CustomEvent('nexus-shared-progress-updated', { detail: { count: sorted.length } })
                );
                window.dispatchEvent(
                    new CustomEvent('nexus-challenge-unlockables-updated', {
                        detail: { count: challengeList.length }
                    })
                );
                window.dispatchEvent(
                    new CustomEvent('nexus-discovery-dlmd-updated', { detail: { count: dlmdSorted.length } })
                );
                window.dispatchEvent(
                    new CustomEvent('nexus-discovery-worlds-updated', { detail: { count: worldSorted.length } })
                );
            } catch (_) {
                /* ignore */
            }
        }

        function mapUnlockMethodTokenToProfilePrefix(token) {
            if (!token || typeof token !== 'string') return '';
            let t = token.trim();
            if (t === 'Unlockable_WeaponSkin') return 'Unlockable_Weapons';
            if (t === 'vehicle_unlockables') return 'Unlockable_Vehicles';
            // NCS uses e.g. Unlockable_DarkSiren_Cosmetics in unlockmethod while profile YAML uses Unlockable_DarkSiren.* 
            if (t.endsWith('_Cosmetics')) {
                t = t.replace(/_Cosmetics$/, '');
            }
            return t;
        }

        function firstUnlockMethodToken(unlockmethod) {
            if (!unlockmethod || typeof unlockmethod !== 'string') return '';
            const i = unlockmethod.indexOf(',');
            const raw = (i === -1 ? unlockmethod : unlockmethod.slice(0, i)).trim();
            return mapUnlockMethodTokenToProfilePrefix(raw);
        }

        function extractCosmeticUnlockIdsFromResident(rawData, targetSet) {
            if (!rawData || !rawData.unlockable || !Array.isArray(rawData.unlockable.records)) return;
            rawData.unlockable.records.forEach(record => {
                (record.entries || []).forEach(entryObj => {
                    if (!entryObj || typeof entryObj !== 'object') return;
                    let parentPrefix = '';
                    for (const val of Object.values(entryObj)) {
                        if (
                            val &&
                            typeof val === 'object' &&
                            typeof val.unlockable === 'string' &&
                            val.unlockable.indexOf('Unlockable_') === 0
                        ) {
                            parentPrefix = mapUnlockMethodTokenToProfilePrefix(val.unlockable);
                            break;
                        }
                    }
                    const depList = entryObj.__dep_entries;
                    if (!Array.isArray(depList)) return;
                    depList.forEach(dep => {
                        if (!dep || typeof dep !== 'object') return;
                        Object.keys(dep).forEach(k => {
                            if (k === 'depTableName' || k === 'depTableId' || k === 'depIndex') return;
                            const inner = dep[k];
                            if (!inner || typeof inner !== 'object') return;
                            const tag = inner.typetag && inner.typetag.tagname;
                            if (typeof tag !== 'string' || tag.indexOf(NEXUS_COSMETIC_TAG_PREFIX) !== 0) return;
                            const entryName = inner.entry;
                            if (typeof entryName !== 'string' || !entryName) return;
                            let prefix = parentPrefix;
                            if (!prefix) prefix = firstUnlockMethodToken(inner.unlockmethod);
                            if (!prefix || prefix.indexOf('Unlockable_') !== 0) return;
                            targetSet.add(prefix + '.' + entryName);
                        });
                    });
                });
            });
        }

        /** Nexus uiname/displayname: `Cosmetics_Weapon, <guid>, <display>` — display may contain commas (e.g. "Halt, Citizen"). */
        function extractInvCustomSkinDisplayLabelFromNexusField(formatted) {
            if (formatted == null || typeof formatted !== 'string') {
                return '';
            }
            const parts = formatted.split(', ');
            if (parts.length === 0) {
                return '';
            }
            if (parts.length >= 3) {
                return parts.slice(2).join(', ').trim();
            }
            if (parts.length === 2) {
                return parts[1].trim();
            }
            return parts[0].trim();
        }

        function ingestInvCustomRecordsIntoMap(rawData, targetMap) {
            const records =
                rawData && rawData.inv_custom && Array.isArray(rawData.inv_custom.records)
                    ? rawData.inv_custom.records
                    : [];
            records.forEach(record => {
                (record.entries || []).forEach(entryObj => {
                    if (!entryObj || typeof entryObj !== 'object') {
                        return;
                    }
                    Object.values(entryObj).forEach(def => {
                        if (!def || typeof def !== 'object') {
                            return;
                        }
                        const invRaw = def.inv_custom;
                        if (typeof invRaw !== 'string' || !invRaw.trim()) {
                            return;
                        }
                        const invCustom = invRaw.trim();
                        const labelRaw =
                            extractInvCustomSkinDisplayLabelFromNexusField(def.uiname) ||
                            extractInvCustomSkinDisplayLabelFromNexusField(def.displayname);
                        const label = labelRaw || invCustom;
                        let sortOrder = 100000;
                        if (def.sortorder != null && def.sortorder !== '') {
                            const n = parseInt(String(def.sortorder), 10);
                            if (!Number.isNaN(n)) {
                                sortOrder = n;
                            }
                        }
                        let serialIndex = null;
                        if (def.serialindex && def.serialindex.index != null && def.serialindex.index !== '') {
                            const idx = parseInt(String(def.serialindex.index), 10);
                            if (!Number.isNaN(idx)) {
                                serialIndex = idx;
                            }
                        }
                        targetMap.set(invCustom, {
                            invCustom: invCustom,
                            label: label,
                            sortOrder: sortOrder,
                            serialIndex: serialIndex
                        });
                    });
                });
            });
        }

        function finalizeNexusWeaponSkinsForModdedDropdown() {
            const arr = Array.from(nexusInvCustomSkinByToken.values()).sort((a, b) => {
                if (a.sortOrder !== b.sortOrder) {
                    return a.sortOrder - b.sortOrder;
                }
                return String(a.label).localeCompare(String(b.label), undefined, { sensitivity: 'base' });
            });
            window.NEXUS_WEAPON_SKIN_OPTIONS = arr;
        }

        function finalizeNexusCosmeticsForProfileEditor() {
            const sorted = Array.from(nexusCosmeticUnlockIds).sort();
            window.NEXUS_ALL_COSMETICS = sorted;
            try {
                window.dispatchEvent(
                    new CustomEvent('nexus-cosmetics-updated', { detail: { count: sorted.length } })
                );
            } catch (_) {
                /* ignore */
            }
            try {
                if (typeof window.refreshSkinCustomizationDropdown === 'function') {
                    window.refreshSkinCustomizationDropdown();
                } else if (typeof window.refreshMiSkinCustomizationDropdown === 'function') {
                    window.refreshMiSkinCustomizationDropdown();
                }
            } catch (_) {
                /* ignore */
            }
        }

        /** Preprocessed Nexus sources for incremental Pack4 merge (no second JSON.parse of core inv files). */
        let nexusPreprocessedChunks = [];
        let pack4NexusLoaded = false;
        /** Resolved proxy base URL after auto-load probe (for on-demand Pack4 fetch). */
        let nexusProxyBaseUrlResolved = null;

        /** Filenames deferred from initial auto-load (pakchunk4 / DLC sidecars). */
        const NEXUS_PACK4_FILENAMES = new Set([
            'nexus-data-inv4.json',
            'nexus-data-inv_name_part4.json',
            'nexus-data-inv_stat4.json',
            'nexus-data-ui_stat4.json',
            'nexus-data-attribute4.json',
            'nexus-data-gbx_ue_data_table4.json',
            'nexus-data-itempool4.json',
            'nexus-data-itempoollist4.json',
            'nexus-data-skilltrees_data4.json',
            'nexus-data-uitooltipdata4.json'
        ]);

        function isNexusPack4Filename(name) {
            const base = String(name || '').split('/').pop().toLowerCase();
            return NEXUS_PACK4_FILENAMES.has(base);
        }

        /** Merge order for Nexus JSON filenames (must match mergeMultipleFiles semantics). */
        function getNexusMergeOrderPriority(name) {
            const normalized = String(name || '').split('/').pop().toLowerCase();
            if (normalized === 'nexus-data-inv0.json') return 10;
            if (normalized === 'nexus-data-inv4.json') return 20;
            if (normalized === 'nexus-data-inv4-core.json') return 20;
            if (normalized === 'nexus-data-inv4-dlc.json') return 21;
            if (NEXUS_PACK4_FILENAMES.has(normalized)) return 25;
            if (normalized === 'nexus-data-inv6.json') return 30;
            if (normalized === 'nexus-data-inv_custom0.json') return 26;
            if (normalized === 'nexus-data-inv_custom4.json') return 27;
            if (normalized === 'nexus-data-inv.json') return 40;
            if (normalized === 'nexus-data-progress_graph_group0.json') return 45;
            if (normalized === 'nexus-data-progress_graph_group4.json') return 46;
            if (normalized === 'nexus-data-progress_graph0.json') return 50;
            if (normalized === 'nexus-data-progress_graph4.json') return 51;
            if (normalized === 'nexus-data-progress_graph6.json') return 52;
            if (normalized === 'nexus-data-mission0.json') return 53;
            if (normalized === 'nexus-data-mission4.json') return 54;
            if (normalized === 'nexus-data-mission6.json') return 55;
            if (normalized === 'nexus-data-missionset0.json') return 56;
            if (normalized === 'nexus-data-missionset4.json') return 57;
            if (normalized === 'nexus-data-missionset6.json') return 58;
            if (normalized === 'nexus-data-challenge0.json') return 59;
            if (normalized === 'nexus-data-challenge4.json') return 60;
            if (normalized === 'nexus-data-challenge6.json') return 61;
            if (normalized === 'nexus-data-challenge_list4.json') return 62;
            if (normalized === 'nexus-data-challenge_list6.json') return 63;
            return 100;
        }

        function rootKeyNeedsPack4Nexus(rootKey) {
            const k = String(rootKey || '').toLowerCase();
            return k.indexOf('robodealer') !== -1 || k.indexOf('robo_dealer') !== -1;
        }

        function rootDefNeedsPack4Nexus(root) {
            if (!root) return false;
            if (rootKeyNeedsPack4Nexus(root.key)) return true;
            const si = root.serialIndex;
            return si === 404 || si === '404';
        }

        function textMightReferencePack4ClassMod(text) {
            const s = String(text || '');
            if (/classmod_robodealer/i.test(s)) return true;
            if (/Char_RoboDealer/i.test(s)) return true;
            if (/\b404\s*:/.test(s)) return true;
            if (/robo_dealer/i.test(s) && /classmod/i.test(s)) return true;
            return false;
        }

        window.getLegitBuilderDatasetStatus = function () {
            if (!data) {
                return { loaded: false, fileCount: 0, roots: null, pack4NexusLoaded: false };
            }
            var roots = data.statistics && data.statistics.totalRootDefinitions;
            return {
                loaded: true,
                fileCount: loadedFileNames.length,
                roots: roots != null ? roots : null,
                pack4NexusLoaded: !!pack4NexusLoaded
            };
        };

        initLegitBuilderInstances();
        if (typeof window.syncLegitBuilderCtxToActiveMainTab === 'function') {
            window.syncLegitBuilderCtxToActiveMainTab();
        }

        // Skill mapping: serial ID (e.g., "255:194") -> skill info
        let skillMapping = new Map();
        
        // Character name mapping: serial root -> character name
        const characterNameMap = {
            '255': 'Amon',
            '254': 'Siren', // Vex
            '253': 'Rafa',
            '252': 'Harlowe',
            '404': 'Robodealer'
        };

        /** Parse JSON in a worker when possible to avoid blocking the main thread; fallback to main-thread parse. */
        function parseJsonOffMainThread(text) {
            return new Promise(function (resolve, reject) {
                if (typeof Worker === 'undefined') {
                    try { resolve(JSON.parse(text)); } catch (e) { reject(e); }
                    return;
                }
                try {
                    const workerUrl = new URL('js/parse-worker.js', document.baseURI || window.location.href).href;
                    const worker = new Worker(workerUrl);
                    worker.onmessage = function (e) {
                        worker.terminate();
                        if (e.data.error) reject(new Error(e.data.error));
                        else resolve(e.data.data);
                    };
                    worker.onerror = function () {
                        worker.terminate();
                        try { resolve(JSON.parse(text)); } catch (err) { reject(err); }
                    };
                    worker.postMessage({ text: text });
                } catch (err) {
                    try { resolve(JSON.parse(text)); } catch (parseErr) { reject(parseErr); }
                }
            });
        }

        // Shared function to load JSON sources
        async function loadJsonSources(sources) {
            if (!sources || sources.length === 0) return;

            // Update file list - use fullPath if available, otherwise filename
            loadedFileNames = sources.map(s => s.fullPath || s.filename || s.name || s.url || 'Unknown');
            updateFileList();

            if (typeof window.updateDataStatusIndicator === 'function') {
                window.updateDataStatusIndicator('Loading Legit Builder / Nexus JSON…', 'loading');
            }

            // Show loading message on all builder panes
            forEachLegitInstance(function () {
                const c = lbEl('rootsContent');
                if (c) c.innerHTML = '<div class="loading">Loading ' + sources.length + ' file(s)...</div>';
            });

            // Load all files
            const filePromises = sources.map(source => {
                return new Promise(async (resolve, reject) => {
                    try {
                        let text;
                        let filename;
                        let fullPath;

                        if (source instanceof File) {
                            // File input or folder fallback
                            const reader = new FileReader();
                            text = await new Promise((res, rej) => {
                                reader.onload = e => res(e.target.result);
                                reader.onerror = () => rej(new Error('Failed to read file'));
                                reader.readAsText(source);
                            });
                            filename = source.name;
                            fullPath = source.webkitRelativePath || source.name;
                        } else if (source.url) {
                            // URL source (from PHP proxy) - parse in worker then unwrap if needed
                            try {
                                const response = await fetch(source.url);
                                if (!response.ok) {
                                    throw new Error(`HTTP ${response.status}: ${response.statusText}`);
                                }
                                const rawText = await response.text();
                                let rawData = await parseJsonOffMainThread(rawText);
                                if (rawData && typeof rawData === 'object') {
                                    if (rawData.data !== undefined) rawData = rawData.data;
                                    else if (rawData.content !== undefined) rawData = rawData.content;
                                    else if (rawData.json !== undefined) rawData = rawData.json;
                                }
                                filename = source.filename || source.url.split('/').pop() || 'Unknown';
                                fullPath = source.url;
                                resolve({ filename, data: rawData, fullPath });
                                return;
                            } catch (error) {
                                reject({ filename: source.filename || source.url || 'Unknown', error: error.message });
                                return;
                            }
                        } else if (source.file && typeof source.file.getFile === 'function') {
                            // File System Access API FileSystemFileHandle
                            const file = await source.file.getFile();
                            text = await file.text();
                            filename = file.name;
                            fullPath = source.path || file.name;
                        } else if (source.data) {
                            // Pre-parsed data (from folder picker)
                            resolve({ filename: source.filename, data: source.data, fullPath: source.fullPath });
                            return;
                        } else {
                            reject({ filename: source.filename || 'Unknown', error: 'Invalid source format' });
                            return;
                        }

                        const rawData = await parseJsonOffMainThread(text);
                        resolve({ filename, data: rawData, fullPath });
                    } catch (error) {
                        reject({ filename: source.filename || source.name || source.url || 'Unknown', error: error.message });
                    }
                });
            });

            try {
                const hasUrls = sources.some(s => s && s.url);
                let results;
                if (hasUrls) {
                    // URL load: use allSettled so missing/unsupported files (e.g. gbx_ue_data_table) don't abort the whole load
                    const settled = await Promise.allSettled(filePromises);
                    results = settled
                        .filter(p => p.status === 'fulfilled' && p.value)
                        .map(p => p.value);
                    const failed = settled.filter(p => p.status === 'rejected');
                    if (failed.length > 0 && results.length === 0) {
                        throw failed[0].reason;
                    }
                    if (failed.length > 0) {
                        console.warn('Some files failed to load (datatable/attribute may show as refs):', failed.map(f => f.reason?.filename || f.reason));
                    }
                } else {
                    results = await Promise.all(filePromises);
                }
                // Merge all data (merge is self-contained: no refs into results, so we can release file parse data)
                data = mergeMultipleFiles(results);
                results = null; // Allow GC to reclaim parsed file data
                // Defer loadData to next frame so the main thread can process any pending input
                // (reduces INP / input delay when user taps header or tab during/after load)
                requestAnimationFrame(() => {
                    loadData();
                    if (typeof window.refreshCombinedDataStatus === 'function') {
                        window.refreshCombinedDataStatus();
                    }
                    if (typeof window.updateDataStatusIndicator === 'function') {
                        window.updateDataStatusIndicator('Legit Builder / Nexus JSON ready', 'success');
                    }
                    if (typeof setApplicationMode === 'function') {
                        setApplicationMode('simple');
                    }
                });
            } catch (error) {
                showError('Error loading files: ' + (error.filename ? error.filename + ': ' + error.error : error));
                if (typeof window.refreshCombinedDataStatus === 'function') {
                    if (!window.refreshCombinedDataStatus()) {
                        if (typeof window.updateDataStatusIndicator === 'function') {
                            window.updateDataStatusIndicator('Legit Builder JSON failed to load', 'error');
                        }
                    }
                } else if (typeof window.updateDataStatusIndicator === 'function') {
                    window.updateDataStatusIndicator('Legit Builder JSON failed to load', 'error');
                }
            }
        }

        // File / folder handlers (both Legit and Modded panes)
        window.legitBuilderInstances.forEach(function (inst) {
            const prefix = inst.idPrefix || '';
            const fileInput = document.getElementById(prefix + 'fileInput');
            if (fileInput) {
                fileInput.addEventListener('change', function (e) {
                    window.__lbCtx = inst;
                    const files = Array.from(e.target.files);
                    if (files.length === 0) return;
                    loadJsonSources(files);
                });
            }
        });

        window.legitBuilderInstances.forEach(function (inst) {
            const prefix = inst.idPrefix || '';
            const folderButton = document.getElementById(prefix + 'folderButton');
            if (!folderButton) return;
            folderButton.addEventListener('click', async function() {
                window.__lbCtx = inst;
            try {
                // Try File System Access API first
                if ('showDirectoryPicker' in window) {
                    const dirHandle = await window.showDirectoryPicker();
                    const jsonFiles = [];
                    const errors = [];

                    // Recursively traverse directory
                    async function traverseDirectory(handle, path = '') {
                        for await (const entry of handle.values()) {
                            const entryPath = path ? `${path}/${entry.name}` : entry.name;
                            if (entry.kind === 'directory') {
                                await traverseDirectory(entry, entryPath);
                            } else if (entry.kind === 'file' && entry.name.toLowerCase().endsWith('.json')) {
                                try {
                                    const file = await entry.getFile();
                                    const text = await file.text();
                                    const data = JSON.parse(text);
                                    jsonFiles.push({
                                        filename: entry.name,
                                        data: data,
                                        fullPath: entryPath
                                    });
                                } catch (error) {
                                    errors.push({ filename: entry.name, error: error.message });
                                }
                            }
                        }
                    }

                    await traverseDirectory(dirHandle);

                    if (jsonFiles.length === 0) {
                        showError('No JSON files found in selected folder');
                        return;
                    }

                    if (errors.length > 0) {
                        console.warn('Some files failed to load:', errors);
                    }

                    loadedFileNames = jsonFiles.map(f => f.fullPath || f.filename);
                    updateFileList();

                    forEachLegitInstance(function () {
                        const c = lbEl('rootsContent');
                        if (c) c.innerHTML = '<div class="loading">Loading ' + jsonFiles.length + ' file(s) from folder...</div>';
                    });

                    // Process all files (merge is self-contained; clear so GC can reclaim parsed file data)
                    data = mergeMultipleFiles(jsonFiles);
                    jsonFiles.length = 0;
                    loadData();
                } else {
                    // Fallback: use webkitdirectory input
                    document.getElementById(prefix + 'folderInput').click();
                }
            } catch (error) {
                if (error.name !== 'AbortError') {
                    showError('Error selecting folder: ' + error.message);
                }
            }
            });
        });

        window.legitBuilderInstances.forEach(function (inst) {
            const prefix = inst.idPrefix || '';
            const folderInput = document.getElementById(prefix + 'folderInput');
            if (!folderInput) return;
            folderInput.addEventListener('change', function (e) {
                window.__lbCtx = inst;
            const files = Array.from(e.target.files).filter(f => f.name.toLowerCase().endsWith('.json'));
            if (files.length === 0) {
                showError('No JSON files found in selected folder');
                return;
            }
            loadJsonSources(files);
            });
        });

        // Load from json folder: fetch value files from same-origin json/ and merge into data (http(s) only)
        const loadFromJsonFolderBtn = lbEl('loadFromJsonFolderBtn');
        if (loadFromJsonFolderBtn) {
            loadFromJsonFolderBtn.addEventListener('click', async function() {
                if (window.location.protocol === 'file:') {
                    showError('Load from json folder requires a web server (http or https). Use "Load Folder" and select LegitItems/json instead.');
                    return;
                }
                const path = window.location.pathname;
                const basePath = path.endsWith('/') ? path : path.replace(/\/[^/]*$/, '/');
                const jsonBase = window.location.origin + basePath + 'json/';
                try {
                    const fetchResults = await Promise.allSettled(
                        VALUE_FILES.map(name => fetch(jsonBase + name, { cache: 'default' }))
                    );
                    let merged = 0;
                    for (let i = 0; i < fetchResults.length; i++) {
                        const r = fetchResults[i];
                        if (r.status !== 'fulfilled' || !r.value.ok) continue;
                        try {
                            const parsed = await r.value.json();
                            if (parsed && typeof parsed === 'object') {
                                mergeValueFilesIntoData(data, parsed);
                                merged++;
                            }
                        } catch (_) { /* ignore */ }
                    }
                    requestAnimationFrame(() => loadData());
                    if (merged > 0) console.log('Loaded values from ' + merged + ' file(s) in json/');
                } catch (err) {
                    showError('Load from json folder failed: ' + (err && err.message ? err.message : String(err)));
                }
            });
        }

        function updateFileList() {
            forEachLegitInstance(function () {
                const fileListDiv = lbEl('loadedFiles');
                const fileListSpan = lbEl('fileList');
                if (!fileListDiv || !fileListSpan) return;
                if (loadedFileNames.length > 0) {
                    fileListSpan.textContent = loadedFileNames.join(', ');
                    fileListDiv.style.display = 'block';
                } else {
                    fileListDiv.style.display = 'none';
                }
            });
        }

        // Normalize multiplier struct property keys - unified function for all stat key normalization
        function normalizeStatKey(raw) {
            if (!raw || typeof raw !== 'string') return raw;
            
            // Strip trailing _<number>_<hexhash> if present
            // Pattern: X_Value_NN_HASH or X_Scale_NN_HASH or X_Add_NN_HASH
            let normalized = raw.replace(/_\d+_[A-F0-9]{32}$/i, '');
            
            // If remaining ends with _Value, remove _Value
            // But keep _Scale and _Add suffixes
            if (normalized.endsWith('_Value')) {
                normalized = normalized.substring(0, normalized.length - 6);
            }
            
            return normalized;
        }

        // Legacy alias for backward compatibility
        function normalizeStructKey(key) {
            return normalizeStatKey(key);
        }

        // --- Datatable value mapping: config and contract ---
        // Where value data lives: load these files and merge via mergeValueFilesIntoData.
        // Merge contract: gbx_ue_data_table uses entryVal.gbx_ue_data_table -> table name,
        // row.row_name -> row name, row.row_value keys lowercased -> column keys.
        // Part refs use datatable (strip gbx_ue_data_table'...'), rowname, optional columnname.
        const VALUE_FILES = [
            'Nexus-Data-gbx_ue_data_table0.json',
            'Nexus-Data-gbx_ue_data_table4.json',
            'Nexus-Data-gbx_ue_data_table6.json',
            'Nexus-Data-attribute0.json',
            'Nexus-Data-attribute4.json',
            'Nexus-Data-attribute6.json'
        ];
        const VALUE_PAYLOAD_KEYS = ['gbx_ue_data_table', 'attribute'];

        /** When a part ref has no columnname, show these columns for the table (if row exists). Extend to add more tables. */
        const DATATABLE_DISPLAY_DEFAULTS = {
            'Weapon_SG_Magazine_Init': ['reloadtime_value', 'maxloadedammo_value'],
            'Weapon_AR_Magazine_Init': ['reloadtime_value', 'maxloadedammo_value']
        };

        /** Human-readable labels for datatable column keys (so "2.4, 6" becomes "Reload: 2.4, Max ammo: 6"). Extend as needed. */
        const DATATABLE_COLUMN_LABELS = {
            'reloadtime_value': 'Reload',
            'maxloadedammo_value': 'Max ammo',
            'heatimpulse_value': 'Heat impulse',
            'thrownreloadtime_value': 'Thrown reload',
            'recoil_scale': 'Recoil',
            'spread_scale': 'Spread',
            'accuracy_scale': 'Accuracy',
            'firerate_scale': 'Fire rate',
            'damage_scale': 'Damage'
        };
        function labelForDatatableColumn(colKey) {
            if (colKey == null) return '';
            const k = String(colKey).toLowerCase();
            if (DATATABLE_COLUMN_LABELS[k]) return DATATABLE_COLUMN_LABELS[k];
            return k.replace(/_value$/, '').replace(/_scale$/, '').replace(/_/g, ' ');
        }

        // Extract struct name from datatable reference
        function datatableToStructName(dtName) {
            if (!dtName || typeof dtName !== 'string') return null;
            
            // Extract plain asset name from gbx_ue_data_table'Weapon_SG_Underbarrel_Init' => Weapon_SG_Underbarrel_Init
            let assetName = dtName.replace(/gbx_ue_data_table'|'/g, '');
            
            // If starts with Weapon_, map to Struct_Weapon_...
            if (assetName.startsWith('Weapon_')) {
                return 'Struct_' + assetName;
            }
            
            // Could add more mappings here if needed
            // For now, return null if pattern doesn't match
            return null;
        }

        /** Normalize datatable reference to table name (strip gbx_ue_data_table' and quotes). */
        function normalizeDatatableName(dtRef) {
            if (!dtRef || typeof dtRef !== 'string') return '';
            return dtRef.replace(/^gbx_ue_data_table'|'$/g, '').trim();
        }

        /** Get a cell from data.datatableValues with case-insensitive row name match (e.g. DAD_MAG_05 → DAD_Mag_05). */
        function getDatatableCell(data, tableName, rowName, columnKey) {
            const tbl = data?.datatableValues?.[tableName];
            if (!tbl || !columnKey) return undefined;
            const ck = String(columnKey).toLowerCase();
            let row = tbl[rowName];
            if (row == null && rowName && typeof rowName === 'string') {
                const want = String(rowName).toLowerCase();
                const key = Object.keys(tbl).find(function (k) { return String(k).toLowerCase() === want; });
                if (key) row = tbl[key];
            }
            return row == null ? undefined : row[ck];
        }

        /** Get full row from data.datatableValues with case-insensitive row name match. Returns undefined if table/row missing. */
        function getDatatableRow(data, tableName, rowName) {
            const tbl = data?.datatableValues?.[tableName];
            if (!tbl) return undefined;
            let row = tbl[rowName];
            if (row == null && rowName && typeof rowName === 'string') {
                const want = String(rowName).toLowerCase();
                const key = Object.keys(tbl).find(function (k) { return String(k).toLowerCase() === want; });
                if (key) row = tbl[key];
            }
            return row == null ? undefined : row;
        }

        /** Resolve a datatable ref to a single value (if columnname set) or full row object (if columnname missing). */
        function resolveDatatableRef(data, ref) {
            if (!data || !ref) return undefined;
            const tableName = normalizeDatatableName(ref.datatable || '');
            if (!tableName) return undefined;
            const rowName = ref.rowname;
            if (ref.columnname) {
                return getDatatableCell(data, tableName, rowName, ref.columnname);
            }
            return getDatatableRow(data, tableName, rowName);
        }

        // Extract datatablevalue occurrences from aspects
        function extractDatatableValues(aspects) {
            const datatableValues = [];
            
            if (!aspects || !Array.isArray(aspects)) return datatableValues;
            
            aspects.forEach(aspect => {
                if (aspect && typeof aspect === 'object') {
                    // Check for modifiers array
                    if (aspect.modifiers && Array.isArray(aspect.modifiers)) {
                        aspect.modifiers.forEach(modifier => {
                            if (modifier && modifier.datatablevalue) {
                                const dtv = modifier.datatablevalue;
                                datatableValues.push({
                                    datatable: dtv.datatable || null,
                                    rowname: dtv.rowname || null,
                                    columnname: dtv.columnname || null
                                });
                            }
                        });
                    }
                    
                    // Also check for direct datatablevalue property
                    if (aspect.datatablevalue) {
                        const dtv = aspect.datatablevalue;
                        datatableValues.push({
                            datatable: dtv.datatable || null,
                            rowname: dtv.rowname || null,
                            columnname: dtv.columnname || null
                        });
                    }
                }
            });
            
            return datatableValues;
        }

        // Parse multiplier struct JSON files
        function parseMultiplierStruct(filename, rawData) {
            // Check if it's a UserDefinedStruct
            if (!Array.isArray(rawData) || rawData.length === 0) return null;
            
            const struct = rawData[0];
            if (struct.Type !== 'UserDefinedStruct' || !struct.Name || !struct.DefaultProperties) {
                return null;
            }

            const structName = struct.Name;
            const rawDefaultProperties = struct.DefaultProperties;
            const normalized = {};

            // Normalize all properties
            Object.entries(rawDefaultProperties).forEach(([key, value]) => {
                const normalizedKey = normalizeStructKey(key);
                normalized[normalizedKey] = value;
            });

            return {
                name: structName,
                rawDefaultProperties: rawDefaultProperties,
                normalized: normalized
            };
        }

        function parseQualifiedInvHandle(handleValue) {
            if (!handleValue || typeof handleValue !== 'string') return null;
            const trimmed = handleValue.trim();
            const match = trimmed.match(/inv'([^'.]+)\.([^']+)'/i);
            if (!match || !match[1] || !match[2]) return null;
            const rootKey = match[1].trim().toLowerCase();
            const partKey = match[2].trim().toLowerCase();
            if (!partKey.startsWith('comp_')) return null;
            return {
                rootKey,
                compKey: partKey,
                qualifiedKey: `${rootKey}.${partKey}`
            };
        }

        function collectHandleValuesFromNode(node, handles) {
            if (!node || !handles) return;

            if (Array.isArray(node)) {
                node.forEach(item => collectHandleValuesFromNode(item, handles));
                return;
            }

            if (typeof node !== 'object') return;

            Object.entries(node).forEach(([key, value]) => {
                if (key === 'handle') {
                    const handleValue = typeof value === 'string' ? value : value?.value;
                    if (typeof handleValue === 'string') {
                        handles.push(handleValue);
                    }
                } else {
                    collectHandleValuesFromNode(value, handles);
                }
            });
        }

        function parseItemPoolListIndex(filename, rawData) {
            const records = rawData?.itempoollist?.records;
            if (!Array.isArray(records)) return null;

            const qualifiedToPools = new Map();
            const poolNames = new Set();

            records.forEach(record => {
                if (!record || !Array.isArray(record.entries)) return;

                record.entries.forEach(entry => {
                    if (!entry || typeof entry !== 'object') return;

                    Object.entries(entry).forEach(([entryKey, entryValue]) => {
                        if (entryKey === '__op') return;
                        if (!entryValue || typeof entryValue !== 'object') return;

                        const container = entryValue.value || entryValue;
                        const poolName = container?.itempoollist?.value || entryKey;
                        if (poolName) poolNames.add(poolName);

                        const handles = [];
                        collectHandleValuesFromNode(container?.itempools?.value || container, handles);
                        handles.forEach(handle => {
                            const parsedHandle = parseQualifiedInvHandle(handle);
                            if (!parsedHandle) return;

                            if (!qualifiedToPools.has(parsedHandle.qualifiedKey)) {
                                qualifiedToPools.set(parsedHandle.qualifiedKey, new Set());
                            }
                            if (poolName) {
                                qualifiedToPools.get(parsedHandle.qualifiedKey).add(poolName);
                            }
                        });
                    });
                });
            });

            if (qualifiedToPools.size === 0) return null;

            return {
                sourceFile: filename,
                poolCount: poolNames.size,
                qualifiedToPools
            };
        }

        function parseItemPoolIndex(filename, rawData) {
            const records = rawData?.itempool?.records;
            if (!Array.isArray(records)) return null;

            const qualifiedToPools = new Map();
            const poolNames = new Set();

            records.forEach(record => {
                if (!record || !Array.isArray(record.entries)) return;

                record.entries.forEach(entry => {
                    if (!entry || typeof entry !== 'object') return;

                    Object.entries(entry).forEach(([entryKey, entryValue]) => {
                        if (entryKey === '__op') return;
                        if (!entryValue || typeof entryValue !== 'object') return;

                        const container = entryValue.value || entryValue;
                        const poolName = container?.itempool?.value || entryKey;
                        if (poolName) poolNames.add(poolName);

                        const handles = [];
                        collectHandleValuesFromNode(container?.items?.value || container, handles);
                        handles.forEach(handle => {
                            const parsedHandle = parseQualifiedInvHandle(handle);
                            if (!parsedHandle) return;

                            if (!qualifiedToPools.has(parsedHandle.qualifiedKey)) {
                                qualifiedToPools.set(parsedHandle.qualifiedKey, new Set());
                            }
                            if (poolName) {
                                qualifiedToPools.get(parsedHandle.qualifiedKey).add(poolName);
                            }
                        });
                    });
                });
            });

            if (qualifiedToPools.size === 0) return null;

            return {
                sourceFile: filename,
                poolCount: poolNames.size,
                qualifiedToPools
            };
        }

        /** Clone object so merged data does not retain references to raw file parse (enables GC of file data). */
        function cloneForMerge(obj) {
            if (obj === null || typeof obj !== 'object') return obj;
            try {
                return structuredClone(obj);
            } catch (e) {
                return JSON.parse(JSON.stringify(obj));
            }
        }

        /** Parse ui_stat StringDisplayValue argsmap pairs into placeholder key -> { attributedef, bdisplayaspercentage }. */
        function parseUiStatPrimaryArgSpecs(def) {
            const pairs = def?.statvalue?.argsmap?.pairs;
            if (!pairs || typeof pairs !== 'object') return undefined;
            const out = Object.create(null);
            Object.values(pairs).forEach((p) => {
                if (!p || typeof p !== 'object') return;
                const pk = p.key;
                const pv = p.value;
                if (pk == null || !pv || typeof pv !== 'object') return;
                const ad = pv.attributedef;
                if (!ad || typeof ad !== 'string') return;
                out[String(pk)] = {
                    attributedef: ad,
                    bdisplayaspercentage: pv.bdisplayaspercentage === 'true' || pv.bdisplayaspercentage === true,
                    bdisplayplussign: pv.bdisplayplussign === 'true' || pv.bdisplayplussign === true
                };
            });
            return Object.keys(out).length ? out : undefined;
        }

        function mergeFromOrderedNexusChunks(orderedFileResults) {
            const __m0 = typeof performance !== 'undefined' ? performance.now() : Date.now();
            const __mChunks = orderedFileResults ? orderedFileResults.length : 0;
            const allRootDefinitions = [];
            const allSubItems = [];
            const allDependencyTables = new Map();
            let totalSubItems = 0;
            const dependencyTablesObj = {};
            const rootDefinitionsByType = {};
            const multiplierStructs = {};
            const compositionSummary = { total_compositions: 0, total_inv_types: 0, compositions: {} };
            const itemPoolIndex = new Map();
            /** @type {Map<string, object>} attribute id (lowercase) -> attribute entry (for damage/accuracy/spread resolution) */
            const attributeByKey = new Map();
            /** @type {Map<string, object>} inv_stat id (lowercase) -> inv_stat entry (optional labels) */
            const invStatByKey = new Map();
            /** @type {Object.<string, Object.<string, Object.<string, number>>>} tableName -> rowName -> columnKey(lowercase) -> numeric value */
            const datatableValues = {};
            /** @type {Object.<string, string>} Map: inv_name_part id (lowercase, e.g. np_rowan) -> display name (text after last comma in partname) */
            let invNamePartDisplayNames = {};
            /** @type {Map<string, { descriptionText?: string, redtext?: string, primaryText?: string, primaryArgSpecs?: Object.<string, { attributedef: string, bdisplayaspercentage: boolean }> }>} ui_stat id (lowercase) -> description/red/primary text */
            const uiStatByKey = new Map();
            /** @type {Map<string, { tooltipRef?: string, character: string, iconAsset?: string }>} normalized "progressgraph|nodename" -> skilltrees node metadata */
            const skilltreesNodeByGraphAndName = new Map();
            /** @type {Map<string, object>} uitooltipdata key (lowercase) -> { header, body, stats } */
            const uitooltipByKey = new Map();

            nexusCosmeticUnlockIds.clear();
            nexusInvCustomSkinByToken.clear();
            try {
                window.NEXUS_WEAPON_SKIN_OPTIONS = [];
            } catch (_) {
                /* ignore */
            }
            nexusSharedProgressUnlockIds.clear();
            nexusChallengeUnlockableById.clear();
            nexusDiscoveryDlmdTokens.clear();
            nexusDiscoveryDlmdEntryByToken.clear();
            nexusDiscoveryWorldMapNames.clear();
            nexusProgressGraphFilesSeen = false;
            nexusProgressionSharedGraphsByName.clear();
            nexusMissionManifestBySet = Object.create(null);
            nexusMissionManifestFilesSeen = false;
            nexusChallengeDefByName = Object.create(null);
            nexusChallengeDefFilesSeen = false;
            nexusChallengeListCompletionIds.clear();
            nexusChallengeAchievementListIds.clear();
            nexusChallengeListFilesSeen = false;

            function mergeExtractedInventoryFile(filename, fileData) {
                if (!fileData) return;
                // Merge root definitions (handle duplicates by tracking all source files)
                if (fileData.rootDefinitions) {
                    fileData.rootDefinitions.forEach(root => {
                        const existing = allRootDefinitions.find(r => r.key === root.key);
                        if (existing) {
                            const existingHasSerial = existing.serialIndex !== null && existing.serialIndex !== undefined;
                            const incomingHasSerial = root.serialIndex !== null && root.serialIndex !== undefined;
                            if (!existingHasSerial && incomingHasSerial) {
                                existing.serialIndex = root.serialIndex;
                            }
                            if ((!existing.serialindex || existing.serialindex.index === undefined) && root.serialindex) {
                                existing.serialindex = root.serialindex;
                            }
                            const incomingParttypes = Array.isArray(root.parttypes) ? root.parttypes : (Array.isArray(root.data?.parttypes) ? root.data.parttypes : []);
                            const existingParttypes = Array.isArray(existing.parttypes) ? existing.parttypes : (Array.isArray(existing.data?.parttypes) ? existing.data.parttypes : []);
                            if (incomingParttypes.length > 0 && existingParttypes.length === 0) {
                                existing.parttypes = Array.isArray(root.parttypes) ? [...root.parttypes] : (Array.isArray(root.data?.parttypes) ? [...root.data.parttypes] : []);
                                if (existing.data) existing.data.parttypes = existing.parttypes;
                            }
                            if (root.depEntries && root.depEntries.length > 0) {
                                if (!existing.depEntries) {
                                    existing.depEntries = [];
                                }
                                root.depEntries.forEach(dep => {
                                    const exactDuplicate = existing.depEntries.find(e =>
                                        e.key === dep.key &&
                                        e.depTableName === dep.depTableName &&
                                        e.depIndex === dep.depIndex
                                    );
                                    if (exactDuplicate) {
                                        const newHasSerial = dep.serialIndex !== null && dep.serialIndex !== undefined;
                                        const oldHasSerial = exactDuplicate.serialIndex !== null && exactDuplicate.serialIndex !== undefined;
                                        if (newHasSerial && !oldHasSerial) {
                                            exactDuplicate.serialIndex = dep.serialIndex;
                                            exactDuplicate.serialindex = dep.serialindex || exactDuplicate.serialindex;
                                        }
                                        return;
                                    }
                                    const sameKeyEntry = existing.depEntries.find(e =>
                                        e.key === dep.key &&
                                        e.depTableName === dep.depTableName
                                    );
                                    if (sameKeyEntry) {
                                        const newHasSerial = dep.serialIndex !== null && dep.serialIndex !== undefined;
                                        const oldHasSerial = sameKeyEntry.serialIndex !== null && sameKeyEntry.serialIndex !== undefined;
                                        if (newHasSerial && !oldHasSerial) {
                                            const index = existing.depEntries.indexOf(sameKeyEntry);
                                            if (index >= 0) {
                                                existing.depEntries[index] = cloneForMerge(dep);
                                            }
                                        } else if (!newHasSerial && oldHasSerial) {
                                            return;
                                        } else {
                                            existing.depEntries.push(cloneForMerge(dep));
                                        }
                                    } else {
                                        existing.depEntries.push(cloneForMerge(dep));
                                    }
                                });
                            }
                            if (!existing.sourceFiles) {
                                existing.sourceFiles = [existing.sourceFile || filename];
                            }
                            if (!existing.sourceFiles.includes(filename)) {
                                existing.sourceFiles.push(filename);
                            }
                        } else {
                            const rootCopy = cloneForMerge(root);
                            rootCopy.sourceFile = filename;
                            rootCopy.sourceFiles = [filename];
                            allRootDefinitions.push(rootCopy);
                        }
                    });
                }
                if (fileData.statistics && fileData.statistics.totalSubItems) {
                    totalSubItems += fileData.statistics.totalSubItems;
                }
                if (fileData.dependencyTablesObj) {
                    Object.entries(fileData.dependencyTablesObj).forEach(([name, info]) => {
                        if (!dependencyTablesObj[name]) {
                            dependencyTablesObj[name] = {
                                id: info.id,
                                count: 0
                            };
                        }
                        dependencyTablesObj[name].count += info.count || 0;
                    });
                }
                if (fileData.statistics && fileData.statistics.dependencyTables) {
                    Object.entries(fileData.statistics.dependencyTables).forEach(([name, info]) => {
                        if (!dependencyTablesObj[name]) {
                            dependencyTablesObj[name] = {
                                id: info.id,
                                count: 0
                            };
                        }
                        dependencyTablesObj[name].count += (typeof info === 'object' ? info.count : info) || 0;
                    });
                }
                if (fileData.statistics && fileData.statistics.rootDefinitionsByType) {
                    Object.entries(fileData.statistics.rootDefinitionsByType).forEach(([type, count]) => {
                        rootDefinitionsByType[type] = (rootDefinitionsByType[type] || 0) + count;
                    });
                }
                if (fileData.subItemsByTable) {
                    Object.entries(fileData.subItemsByTable).forEach(([tableName, table]) => {
                        if (!allDependencyTables.has(tableName)) {
                            allDependencyTables.set(tableName, {
                                name: tableName,
                                id: table.depTableId,
                                items: []
                            });
                        }
                        if (table.items) {
                            table.items.forEach(item => {
                                const exists = allDependencyTables.get(tableName).items.some(i =>
                                    i.key === item.key &&
                                    i.rootKey === item.rootKey &&
                                    i.depIndex === item.depIndex
                                );
                                if (!exists) {
                                    allDependencyTables.get(tableName).items.push(cloneForMerge(item));
                                }
                            });
                        }
                    });
                }
            }

            orderedFileResults.forEach((chunk) => {
                const filename = chunk.filename;
                if (chunk.inventoryFileData) {
                    mergeExtractedInventoryFile(filename, chunk.inventoryFileData);
                    return;
                }
                const rawData = chunk.data;
                if (!rawData) return;

                // First check if this is a multiplier struct file
                const structData = parseMultiplierStruct(filename, rawData);
                if (structData) {
                    // Store multiplier struct (copy so we don't retain refs to rawData)
                    multiplierStructs[structData.name] = cloneForMerge({
                        rawDefaultProperties: structData.rawDefaultProperties,
                        normalized: structData.normalized
                    });
                    return; // Skip inventory processing for struct files
                }

                // Check if this is a composition_summary.json file (optional hints)
                if (rawData.compositions && rawData.total_compositions !== undefined) {
                    // This is composition_summary.json - store as hints only (copy so we don't retain refs to rawData)
                    compositionSummary.total_compositions = rawData.total_compositions || 0;
                    compositionSummary.total_inv_types = rawData.total_inv_types || 0;
                    const comps = rawData.compositions || {};
                    Object.keys(comps).forEach(k => { compositionSummary.compositions[k] = cloneForMerge(comps[k]); });
                    console.log(`Loaded composition_summary.json as hints: ${Object.keys(rawData.compositions || {}).length} compositions`);
                    return; // Skip inventory processing for summary files
                }

                // Check if this is an itempoollist source file (for dedicated loot pool indexing)
                const itemPoolListData = parseItemPoolListIndex(filename, rawData);
                if (itemPoolListData) {
                    itemPoolListData.qualifiedToPools.forEach((poolSet, qualifiedKey) => {
                        if (!itemPoolIndex.has(qualifiedKey)) {
                            itemPoolIndex.set(qualifiedKey, new Set());
                        }
                        poolSet.forEach(poolName => itemPoolIndex.get(qualifiedKey).add(poolName));
                    });
                    return; // Item pool list files are informational sidecar data only
                }

                // Check if this is an itempool source file (additional dedicated loot pool indexing)
                const itemPoolData = parseItemPoolIndex(filename, rawData);
                if (itemPoolData) {
                    itemPoolData.qualifiedToPools.forEach((poolSet, qualifiedKey) => {
                        if (!itemPoolIndex.has(qualifiedKey)) {
                            itemPoolIndex.set(qualifiedKey, new Set());
                        }
                        poolSet.forEach(poolName => itemPoolIndex.get(qualifiedKey).add(poolName));
                    });
                    return; // Item pool files are informational sidecar data only
                }

                // Check if this is value payload data (attribute and/or gbx_ue_data_table) - single merge path
                const hasValuePayload = VALUE_PAYLOAD_KEYS.some(key => rawData[key] && Array.isArray(rawData[key].records));
                if (hasValuePayload) {
                    const valueData = { datatableValues, attributeByKey };
                    VALUE_PAYLOAD_KEYS.forEach(key => {
                        if (rawData[key] && Array.isArray(rawData[key].records)) {
                            mergeOnePayloadKey(valueData, rawData, key);
                        }
                    });
                    return;
                }

                // Check if this is inv_stat table data (optional stat labels) - copy so we don't retain refs to rawData
                if (rawData.inv_stat && Array.isArray(rawData.inv_stat.records)) {
                    rawData.inv_stat.records.forEach(record => {
                        (record.entries || []).forEach(entryObj => {
                            if (!entryObj || typeof entryObj !== 'object') return;
                            Object.entries(entryObj).forEach(([key, val]) => {
                                if (!val || typeof val !== 'object') return;
                                const mapKey = String(key).toLowerCase();
                                invStatByKey.set(mapKey, cloneForMerge(val));
                            });
                        });
                    });
                    return;
                }

                // Check if this is ui_stat data (descriptions and red text for parts)
                if (rawData.ui_stat && Array.isArray(rawData.ui_stat.records)) {
                    const extractDisplayText = (formattext) => {
                        if (formattext == null || typeof formattext !== 'string') return '';
                        const parts = formattext.split(', ');
                        return parts.length >= 3 ? parts.slice(2).join(', ').trim() : (parts.length === 1 ? formattext.trim() : '');
                    };
                    rawData.ui_stat.records.forEach(record => {
                        (record.entries || []).forEach(entryObj => {
                            if (!entryObj || typeof entryObj !== 'object') return;
                            Object.entries(entryObj).forEach(([entryKey, def]) => {
                                if (!def || typeof def !== 'object') return;
                                const key = String(entryKey).toLowerCase();
                                const formattext = def.statvalue?.formattext || def.statlabel?.formattext;
                                const displayText = extractDisplayText(formattext);
                                const dg = String(def.displaygroup || '');
                                const prev = uiStatByKey.get(key);
                                const value = {
                                    descriptionText: prev?.descriptionText,
                                    redtext: prev?.redtext,
                                    primaryText: prev?.primaryText,
                                    primaryArgSpecs: prev?.primaryArgSpecs
                                };
                                if (dg.indexOf('Secondary') !== -1) value.descriptionText = displayText;
                                if (dg.indexOf('RedText') !== -1) value.redtext = displayText;
                                // Nexus uses ui_stat_group'Text' for main effect lines; ui_stat_group'Primary' also exists.
                                const isPrimaryLikeTextGroup =
                                    dg.indexOf("'Text'") !== -1 &&
                                    dg.indexOf('RedText') === -1 &&
                                    dg.indexOf('Secondary') === -1;
                                if (dg.indexOf('Primary') !== -1 || isPrimaryLikeTextGroup) {
                                    value.primaryText = displayText;
                                    const specs = parseUiStatPrimaryArgSpecs(def);
                                    if (specs) value.primaryArgSpecs = specs;
                                }
                                uiStatByKey.set(key, value);
                            });
                        });
                    });
                    return;
                }

                // Check if this is skilltrees_data (passive node -> tooltip ref + character + optional icon asset)
                if (rawData.skilltrees_data && Array.isArray(rawData.skilltrees_data.records)) {
                    const treeNameToCharacter = {
                        dark_siren_skill_trees: 'Siren',
                        exo_skill_trees: 'Rafa',
                        gravitar_skill_trees: 'Harlowe',
                        paladin_skill_trees: 'Paladin',
                        robodealer_skill_trees: 'Robodealer'
                    };
                    rawData.skilltrees_data.records.forEach(record => {
                        (record.entries || []).forEach(entryObj => {
                            if (!entryObj || typeof entryObj !== 'object') return;
                            Object.entries(entryObj).forEach(([treeName, treeValue]) => {
                                if (!treeValue || typeof treeValue !== 'object') return;
                                const character = treeNameToCharacter[treeName] || treeName;
                                const skilltrees = treeValue.skilltrees;
                                if (!Array.isArray(skilltrees)) return;
                                skilltrees.forEach(st => {
                                    const segments = st.segments;
                                    if (!Array.isArray(segments)) return;
                                    segments.forEach(seg => {
                                        const progressgraph = seg.progressgraph != null ? String(seg.progressgraph) : '';
                                        const tiers = seg.tiers;
                                        if (!Array.isArray(tiers)) return;
                                        tiers.forEach(tier => {
                                            const nodes = tier.nodes;
                                            if (!Array.isArray(nodes)) return;
                                            nodes.forEach(node => {
                                                const name = node.name;
                                                if (!name) return;
                                                const tooltip = node.tooltip;
                                                const iconRaw = node.icon;
                                                const hasTooltip = tooltip && typeof tooltip === 'string';
                                                const hasIcon = iconRaw && typeof iconRaw === 'string';
                                                if (!hasTooltip && !hasIcon) return;
                                                const key = progressgraph + '|' + name;
                                                const prev = skilltreesNodeByGraphAndName.get(key) || {};
                                                const merged = { ...prev, character: character || prev.character };
                                                if (hasTooltip) merged.tooltipRef = tooltip;
                                                if (hasIcon) merged.iconAsset = iconRaw;
                                                skilltreesNodeByGraphAndName.set(key, merged);
                                            });
                                        });
                                    });
                                });
                            });
                        });
                    });
                    return;
                }

                // Check if this is uitooltipdata (tooltip key -> header, body, stats) - copy so we don't retain refs to rawData
                if (rawData.uitooltipdata && Array.isArray(rawData.uitooltipdata.records)) {
                    rawData.uitooltipdata.records.forEach(record => {
                        (record.entries || []).forEach(entryObj => {
                            if (!entryObj || typeof entryObj !== 'object') return;
                            Object.entries(entryObj).forEach(([key, val]) => {
                                if (val && typeof val === 'object') {
                                    const mapKey = String(key).toLowerCase();
                                    uitooltipByKey.set(mapKey, cloneForMerge(val));
                                }
                            });
                        });
                    });
                    return;
                }

                // Check if this is inv_name_part data (display names for np_XXX parts)
                if (rawData.inv_name_part && Array.isArray(rawData.inv_name_part.records)) {
                    rawData.inv_name_part.records.forEach(record => {
                        (record.entries || []).forEach(entryObj => {
                            if (!entryObj || typeof entryObj !== 'object') return;
                            Object.entries(entryObj).forEach(([key, val]) => {
                                if (val && val.partname && typeof val.partname === 'string') {
                                    const parts = val.partname.split(',');
                                    const displayName = parts.length > 0 ? parts[parts.length - 1].trim() : '';
                                    if (displayName) {
                                        const mapKey = (val.inv_name_part || key).toLowerCase();
                                        invNamePartDisplayNames[mapKey] = displayName;
                                    }
                                }
                            });
                        });
                    });
                    return; // Don't process as inventory
                }

                if (rawData.inv_custom && Array.isArray(rawData.inv_custom.records)) {
                    ingestInvCustomRecordsIntoMap(rawData, nexusInvCustomSkinByToken);
                    return;
                }

                // Nexus unlock table (Resident): cosmetic ids for profile editor
                if (rawData.unlockable && Array.isArray(rawData.unlockable.records)) {
                    extractCosmeticUnlockIdsFromResident(rawData, nexusCosmeticUnlockIds);
                    return;
                }

                // Challenge list shards (Cowbell/Cello preset ids) — must run before generic challenge return
                if (rawData.challenge_list && Array.isArray(rawData.challenge_list.records)) {
                    ingestChallengeListForSaveEditor(filename, rawData);
                    return;
                }

                // Challenge tables: SharedProgress_* + echo/vault challenge unlockables for profile editor
                if (rawData.challenge && Array.isArray(rawData.challenge.records)) {
                    ingestChallengeDefsForSaveEditor(rawData);
                    extractSharedProgressUnlockIdsFromChallenge(rawData, nexusSharedProgressUnlockIds);
                    extractChallengeUnlockablesFromChallenge(rawData);
                    return;
                }

                if (rawData.gbx_discovery_location_meta_data && Array.isArray(rawData.gbx_discovery_location_meta_data.records)) {
                    extractDiscoveryDlmdTokensFromMeta(rawData);
                    return;
                }

                if (rawData.progress_graph_group && Array.isArray(rawData.progress_graph_group.records)) {
                    return;
                }

                if (rawData.progress_graph && Array.isArray(rawData.progress_graph.records)) {
                    ingestProgressGraphNexusFile(rawData);
                    return;
                }

                if (rawData.mission && Array.isArray(rawData.mission.records)) {
                    ingestMissionNexusSidecar(rawData);
                    return;
                }

                if (rawData.missionset && Array.isArray(rawData.missionset.records)) {
                    ingestMissionsetNexusSidecar(rawData);
                    return;
                }

                // GbxActorPart: sidecar data only (no inventory merge)
                if (rawData.GbxActorPart && Array.isArray(rawData.GbxActorPart.records)) {
                    return;
                }

                let fileData;
                
                // Check if it's an exported format (has metadata wrapper)
                if (rawData.metadata && rawData.rootDefinitions) {
                    // This is an exported/parsed data file - use it directly
                    fileData = {
                        rootDefinitions: rawData.rootDefinitions || [],
                        dependencyTablesObj: rawData.dependencyTables || {},
                        subItemsByTable: rawData.subItemsByTable || {},
                        multiplierStructs: rawData.multiplierStructs || {}
                    };
                    // Merge multiplier structs from exported file
                    if (rawData.multiplierStructs) {
                        Object.assign(multiplierStructs, rawData.multiplierStructs);
                    }
                } else if (rawData.rootDefinitions) {
                    // Already organized format (from previous extract)
                    fileData = rawData;
                } else if (rawData.inv && rawData.inv.records) {
                    // Original NCS parser format - need to extract
                    fileData = extractFromOriginalFormat(rawData);
                } else {
                    // Log the structure we received for debugging
                    console.warn(`Unknown JSON structure in ${filename}. Skipping.`, {
                        hasMetadata: !!rawData.metadata,
                        hasRootDefinitions: !!rawData.rootDefinitions,
                        hasInv: !!rawData.inv,
                        hasInvRecords: !!(rawData.inv && rawData.inv.records),
                        topLevelKeys: Object.keys(rawData || {}).slice(0, 10),
                        sampleData: JSON.stringify(rawData).substring(0, 200)
                    });
                    return;
                }

                mergeExtractedInventoryFile(filename, fileData);
            });

            // Re-extract sub-items from merged root definitions for accurate count
            allSubItems.length = 0;
            allRootDefinitions.forEach(root => {
                if (root.depEntries) {
                    root.depEntries.forEach(dep => {
                        allSubItems.push(dep);
                    });
                }
            });
            totalSubItems = allSubItems.length;

            // Build final subItemsByTable from merged data
            const subItemsByTable = {};
            allDependencyTables.forEach((table, name) => {
                subItemsByTable[name] = {
                    depTableId: table.id,
                    items: table.items
                };
            });

            // Also build dependencyTablesObj from allDependencyTables for statistics
            // This ensures the count is accurate even when loading exported files
            Object.keys(subItemsByTable).forEach(tableName => {
                const table = subItemsByTable[tableName];
                if (!dependencyTablesObj[tableName]) {
                    dependencyTablesObj[tableName] = {
                        id: table.depTableId,
                        count: table.items ? table.items.length : 0
                    };
                } else {
                    // Update count from actual items
                    dependencyTablesObj[tableName].count = table.items ? table.items.length : dependencyTablesObj[tableName].count;
                }
            });

            // Integrate multipliers with subItems based on aspects - robust correlation system
            const knownStructNames = Object.keys(multiplierStructs);
            
            // Helper function to process a single item's multipliers
            function processItemMultipliers(item) {
                if (!item.aspects || !Array.isArray(item.aspects)) return;
                
                // Extract datatablevalue occurrences from aspects
                const dtValues = extractDatatableValues(item.aspects);
                
                if (dtValues.length === 0) return;
                
                // Create multiplierLinks array
                item.multiplierLinks = [];
                
                // Process each datatablevalue
                dtValues.forEach(dtv => {
                    const datatable = dtv.datatable;
                    if (!datatable) return;
                    
                    // Get struct candidate from datatable name
                    const structCandidate = datatableToStructName(datatable);
                    
                    // Normalize stat key if columnname exists
                    const statKey = dtv.columnname ? normalizeStatKey(dtv.columnname) : null;
                    
                    // Create link object
                    item.multiplierLinks.push({
                        datatable: datatable,
                        rowname: dtv.rowname || null,
                        columnRaw: dtv.columnname || null,
                        statKey: statKey,
                        structCandidate: structCandidate
                    });
                });
                
                // Attach multipliers from structs
                item.multipliers = { Value: {}, Scale: {}, Add: {} };
                
                item.multiplierLinks.forEach(link => {
                    const structCandidate = link.structCandidate;
                    if (!structCandidate) return;
                    
                    const struct = multiplierStructs[structCandidate];
                    if (!struct || !struct.normalized) return;
                    
                    const normalized = struct.normalized;
                    
                    // If statKey is provided, look up that specific key
                    if (link.statKey) {
                        // Check if this key exists in normalized
                        if (normalized.hasOwnProperty(link.statKey)) {
                            const value = normalized[link.statKey];
                            // Determine bucket by suffix
                            if (link.statKey.endsWith('_Scale')) {
                                if (!item.multipliers.Scale[link.statKey]) {
                                    item.multipliers.Scale[link.statKey] = [];
                                }
                                item.multipliers.Scale[link.statKey].push({
                                    struct: structCandidate,
                                    value: value,
                                    rowname: link.rowname
                                });
                            } else if (link.statKey.endsWith('_Add')) {
                                if (!item.multipliers.Add[link.statKey]) {
                                    item.multipliers.Add[link.statKey] = [];
                                }
                                item.multipliers.Add[link.statKey].push({
                                    struct: structCandidate,
                                    value: value,
                                    rowname: link.rowname
                                });
                            } else {
                                // Value bucket
                                if (!item.multipliers.Value[link.statKey]) {
                                    item.multipliers.Value[link.statKey] = [];
                                }
                                item.multipliers.Value[link.statKey].push({
                                    struct: structCandidate,
                                    value: value,
                                    rowname: link.rowname
                                });
                            }
                        }
                    } else {
                        // No specific statKey, attach all normalized values from the struct
                        // This is a fallback for cases where columnname isn't specified
                        Object.entries(normalized).forEach(([key, value]) => {
                            // Determine bucket by suffix
                            if (key.endsWith('_Scale')) {
                                if (!item.multipliers.Scale[key]) {
                                    item.multipliers.Scale[key] = [];
                                }
                                item.multipliers.Scale[key].push({
                                    struct: structCandidate,
                                    value: value,
                                    rowname: link.rowname
                                });
                            } else if (key.endsWith('_Add')) {
                                if (!item.multipliers.Add[key]) {
                                    item.multipliers.Add[key] = [];
                                }
                                item.multipliers.Add[key].push({
                                    struct: structCandidate,
                                    value: value,
                                    rowname: link.rowname
                                });
                            } else {
                                // Value bucket
                                if (!item.multipliers.Value[key]) {
                                    item.multipliers.Value[key] = [];
                                }
                                item.multipliers.Value[key].push({
                                    struct: structCandidate,
                                    value: value,
                                    rowname: link.rowname
                                });
                            }
                        });
                    }
                });
                
                // Clean up empty buckets
                Object.keys(item.multipliers).forEach(bucket => {
                    if (Object.keys(item.multipliers[bucket]).length === 0) {
                        delete item.multipliers[bucket];
                    }
                });
                
                // If all buckets are empty, set multipliers to undefined
                if (Object.keys(item.multipliers).length === 0) {
                    item.multipliers = undefined;
                }
            }
            
            // Process all subItems
            if (knownStructNames.length > 0) {
                allSubItems.forEach(subItem => {
                    processItemMultipliers(subItem);
                });

                // Also update items in dependency tables
                allDependencyTables.forEach((table, tableName) => {
                    if (table.items) {
                        table.items.forEach(item => {
                            processItemMultipliers(item);
                        });
                    }
                });
            }


            finalizeNexusWeaponSkinsForModdedDropdown();
            finalizeNexusCosmeticsForProfileEditor();
            finalizeNexusSharedProgressForProfileEditor();
            finalizeNexusProgressionSharedManifest();
            finalizeNexusMissionManifestForSaveEditor();
            finalizeNexusChallengeManifestForSaveEditor();

            buildUniqueCompQuickAccessCatalog({
                rootDefinitions: allRootDefinitions,
                invNamePartDisplayNames,
                subItemsByTable
            });

            var __mMs = Math.round((typeof performance !== 'undefined' ? performance.now() : Date.now()) - __m0);
            return {
                rootDefinitions: allRootDefinitions,
                statistics: {
                    totalRootDefinitions: allRootDefinitions.length,
                    totalSubItems: totalSubItems,
                    dependencyTables: dependencyTablesObj,
                    rootDefinitionsByType: rootDefinitionsByType,
                    multiplierStructs: Object.keys(multiplierStructs).length,
                    dedicatedLootPoolComponents: itemPoolIndex.size
                },
                subItemsByTable: subItemsByTable,
                multiplierStructs: multiplierStructs,
                compositionSummary: Object.keys(compositionSummary.compositions).length > 0 ? compositionSummary : null, // Optional hints from composition_summary.json
                itemPoolIndex: itemPoolIndex,
                invNamePartDisplayNames: invNamePartDisplayNames,
                attributeByKey: attributeByKey,
                invStatByKey: invStatByKey,
                uiStatByKey: uiStatByKey,
                skilltreesNodeByGraphAndName: skilltreesNodeByGraphAndName,
                uitooltipByKey: uitooltipByKey,
                datatableValues: datatableValues
            };
        }

        function preprocessNexusFileResultsToChunks(fileResults) {
            return [...fileResults].map(fr => {
                const raw = fr.data;
                if (raw && raw.inv && Array.isArray(raw.inv.records)) {
                    return {
                        filename: fr.filename,
                        data: null,
                        inventoryFileData: extractFromOriginalFormat(raw)
                    };
                }
                return { filename: fr.filename, data: raw, inventoryFileData: null };
            }).sort((a, b) => getNexusMergeOrderPriority(a.filename) - getNexusMergeOrderPriority(b.filename));
        }

        function mergeMultipleFiles(fileResults) {
            const ordered = preprocessNexusFileResultsToChunks(fileResults);
            nexusPreprocessedChunks = ordered;
            pack4NexusLoaded = fileResults.some(f => isNexusPack4Filename(f.filename));
            return mergeFromOrderedNexusChunks(ordered);
        }

        let pack4NexusLoadPromise = null;

        function getNexusPack4UrlDescriptors(proxyBaseUrl) {
            const b = proxyBaseUrl.replace(/\/?$/, '');
            return [
                { url: `${b}?file=inv4`, filename: 'Nexus-Data-inv4.json' },
                { url: `${b}?file=inv_name_part4`, filename: 'Nexus-Data-inv_name_part4.json' },
                { url: `${b}?file=inv_stat4`, filename: 'Nexus-Data-inv_stat4.json' },
                { url: `${b}?file=ui_stat4`, filename: 'Nexus-Data-ui_stat4.json' },
                { url: `${b}?file=attribute4`, filename: 'Nexus-Data-attribute4.json' },
                { url: `${b}?file=gbx_ue_data_table4`, filename: 'Nexus-Data-gbx_ue_data_table4.json' },
                { url: `${b}?file=itempool4`, filename: 'Nexus-Data-itempool4.json' },
                { url: `${b}?file=itempoollist4`, filename: 'Nexus-Data-ItemPoolList4.json' },
                { url: `${b}?file=skilltrees_data4`, filename: 'Nexus-Data-skilltrees_data4.json' },
                { url: `${b}?file=uitooltipdata4`, filename: 'Nexus-Data-uitooltipdata4.json' }
            ];
        }

        async function ensurePack4NexusLoaded() {
            if (pack4NexusLoaded) return true;
            if (pack4NexusLoadPromise) return pack4NexusLoadPromise;
            const base = nexusProxyBaseUrlResolved;
            if (!base) {
                console.warn('Pack4 Nexus load skipped: proxy base URL not set (load core Nexus from network first).');
                return false;
            }
            pack4NexusLoadPromise = (async () => {
                try {
                    if (typeof window.updateDataStatusIndicator === 'function') {
                        window.updateDataStatusIndicator('Loading DLC (pak 4) Nexus data…', 'loading');
                    }
                    const sources = getNexusPack4UrlDescriptors(base);
                    const settled = await Promise.allSettled(
                        sources.map(source => new Promise(async (resolve, reject) => {
                            try {
                                const response = await fetch(source.url, { cache: 'default' });
                                if (!response.ok) {
                                    throw new Error(`HTTP ${response.status}: ${response.statusText}`);
                                }
                                const rawText = await response.text();
                                let rawData = await parseJsonOffMainThread(rawText);
                                if (rawData && typeof rawData === 'object') {
                                    if (rawData.data !== undefined) rawData = rawData.data;
                                    else if (rawData.content !== undefined) rawData = rawData.content;
                                    else if (rawData.json !== undefined) rawData = rawData.json;
                                }
                                resolve({ filename: source.filename, data: rawData, fullPath: source.url });
                            } catch (error) {
                                reject({ filename: source.filename, error: error && error.message ? error.message : String(error) });
                            }
                        }))
                    );
                    const results = settled
                        .filter(p => p.status === 'fulfilled' && p.value)
                        .map(p => p.value);
                    const failed = settled.filter(p => p.status === 'rejected');
                    if (failed.length > 0) {
                        console.warn('Some Pack4 Nexus files failed to load:', failed.map(f => f.reason));
                    }
                    if (results.length === 0) {
                        if (typeof window.updateDataStatusIndicator === 'function') {
                            window.updateDataStatusIndicator('Pack4 Nexus load failed', 'error');
                        }
                        return false;
                    }
                    const newChunks = preprocessNexusFileResultsToChunks(
                        results.map(r => ({ filename: r.filename, data: r.data }))
                    );
                    const merged = [...nexusPreprocessedChunks, ...newChunks].sort(
                        (a, b) => getNexusMergeOrderPriority(a.filename) - getNexusMergeOrderPriority(b.filename)
                    );
                    nexusPreprocessedChunks = merged;
                    data = mergeFromOrderedNexusChunks(merged);
                    pack4NexusLoaded = true;
                    const names = [];
                    merged.forEach(c => {
                        if (names.indexOf(c.filename) === -1) names.push(c.filename);
                    });
                    loadedFileNames = names;
                    updateFileList();
                    const st = document.getElementById('lbPack4Status');
                    if (st) st.textContent = 'DLC (pak 4) Nexus data loaded.';
                    if (typeof window.updateDataStatusIndicator === 'function') {
                        window.updateDataStatusIndicator('Legit Builder / Nexus JSON ready', 'success');
                    }
                    requestAnimationFrame(() => {
                        loadData();
                        if (typeof window.refreshCombinedDataStatus === 'function') {
                            window.refreshCombinedDataStatus();
                        }
                    });
                    return true;
                } catch (err) {
                    console.error('ensurePack4NexusLoaded:', err);
                    return false;
                } finally {
                    pack4NexusLoadPromise = null;
                }
            })();
            return pack4NexusLoadPromise;
        }
        window.ensurePack4NexusLoaded = ensurePack4NexusLoaded;

        /** Merge one payload key (gbx_ue_data_table or attribute) from raw payload into data. Single place for merge contract. */
        function mergeOnePayloadKey(data, payload, key) {
            if (!data || !payload || !key) return;
            const records = payload[key] && Array.isArray(payload[key].records) ? payload[key].records : [];
            if (records.length === 0) return;
            if (key === 'gbx_ue_data_table') {
                if (!data.datatableValues) data.datatableValues = {};
                const datatableValues = data.datatableValues;
                records.forEach(record => {
                    (record.entries || []).forEach(entryObj => {
                        if (!entryObj || typeof entryObj !== 'object') return;
                        Object.entries(entryObj).forEach(([entryKey, entryVal]) => {
                            if (!entryVal || typeof entryVal !== 'object') return;
                            const tableName = entryVal.gbx_ue_data_table != null ? String(entryVal.gbx_ue_data_table) : '';
                            if (!tableName) return;
                            if (!datatableValues[tableName]) datatableValues[tableName] = {};
                            const rows = entryVal.data;
                            if (!Array.isArray(rows)) return;
                            rows.forEach(row => {
                                const rowName = row.row_name != null ? String(row.row_name) : '';
                                if (!rowName) return;
                                if (!datatableValues[tableName][rowName]) datatableValues[tableName][rowName] = {};
                                const rv = row.row_value;
                                if (!rv || typeof rv !== 'object') return;
                                Object.entries(rv).forEach(([colKey, colVal]) => {
                                    const str = String(colVal);
                                    if (str.startsWith('NexusSerialized') || str.length > 50) return;
                                    const num = parseFloat(str);
                                    if (!Number.isNaN(num)) datatableValues[tableName][rowName][colKey.toLowerCase()] = num;
                                });
                            });
                        });
                    });
                });
                return;
            }
            if (key === 'attribute') {
                if (!data.attributeByKey) data.attributeByKey = new Map();
                const attributeByKey = data.attributeByKey;
                records.forEach(record => {
                    (record.entries || []).forEach(entryObj => {
                        if (!entryObj || typeof entryObj !== 'object') return;
                        Object.entries(entryObj).forEach(([k, val]) => {
                            if (!val || typeof val !== 'object') return;
                            attributeByKey.set(String(k).toLowerCase(), cloneForMerge(val));
                        });
                    });
                });
            }
        }

        /** Merge value payloads (gbx_ue_data_table, attribute) from a parsed JSON file into data. */
        function mergeValueFilesIntoData(data, parsedPayload) {
            if (!data || !parsedPayload) return;
            if (!data.datatableValues) data.datatableValues = {};
            if (!data.attributeByKey) data.attributeByKey = new Map();
            VALUE_PAYLOAD_KEYS.forEach(key => {
                if (parsedPayload[key] && Array.isArray(parsedPayload[key].records)) {
                    mergeOnePayloadKey(data, parsedPayload, key);
                }
            });
        }

        /** Merge ui_stat from a single parsed JSON payload into data.uiStatByKey (descriptions and red text). */
        function mergeUiStatIntoData(data, parsedPayload) {
            if (!data || !parsedPayload?.ui_stat?.records) return;
            if (!data.uiStatByKey) data.uiStatByKey = new Map();
            const uiStatByKey = data.uiStatByKey;
            const extractDisplayText = (formattext) => {
                if (formattext == null || typeof formattext !== 'string') return '';
                const parts = formattext.split(', ');
                return parts.length >= 3 ? parts.slice(2).join(', ').trim() : (parts.length === 1 ? formattext.trim() : '');
            };
            parsedPayload.ui_stat.records.forEach(record => {
                (record.entries || []).forEach(entryObj => {
                    if (!entryObj || typeof entryObj !== 'object') return;
                    Object.entries(entryObj).forEach(([entryKey, def]) => {
                        if (!def || typeof def !== 'object') return;
                        const key = String(entryKey).toLowerCase();
                        const formattext = def.statvalue?.formattext || def.statlabel?.formattext;
                        const displayText = extractDisplayText(formattext);
                        const dg = String(def.displaygroup || '');
                        const prev = uiStatByKey.get(key);
                        const value = {
                            descriptionText: prev?.descriptionText,
                            redtext: prev?.redtext,
                            primaryText: prev?.primaryText,
                            primaryArgSpecs: prev?.primaryArgSpecs
                        };
                        if (dg.indexOf('Secondary') !== -1) value.descriptionText = displayText;
                        if (dg.indexOf('RedText') !== -1) value.redtext = displayText;
                        const isPrimaryLikeTextGroup =
                            dg.indexOf("'Text'") !== -1 &&
                            dg.indexOf('RedText') === -1 &&
                            dg.indexOf('Secondary') === -1;
                        if (dg.indexOf('Primary') !== -1 || isPrimaryLikeTextGroup) {
                            value.primaryText = displayText;
                            const specs = parseUiStatPrimaryArgSpecs(def);
                            if (specs) value.primaryArgSpecs = specs;
                        }
                        uiStatByKey.set(key, value);
                    });
                });
            });
        }

        /** Merge skilltrees_data from a single parsed JSON payload into data.skilltreesNodeByGraphAndName. */
        function mergeSkilltreesIntoData(data, parsedPayload) {
            if (!data || !parsedPayload?.skilltrees_data?.records) return;
            if (!data.skilltreesNodeByGraphAndName) data.skilltreesNodeByGraphAndName = new Map();
            const skilltreesNodeByGraphAndName = data.skilltreesNodeByGraphAndName;
            const treeNameToCharacter = {
                dark_siren_skill_trees: 'Siren',
                exo_skill_trees: 'Rafa',
                gravitar_skill_trees: 'Harlowe',
                paladin_skill_trees: 'Paladin',
                robodealer_skill_trees: 'Robodealer'
            };
            parsedPayload.skilltrees_data.records.forEach(record => {
                (record.entries || []).forEach(entryObj => {
                    if (!entryObj || typeof entryObj !== 'object') return;
                    Object.entries(entryObj).forEach(([treeName, treeValue]) => {
                        if (!treeValue || typeof treeValue !== 'object') return;
                        const character = treeNameToCharacter[treeName] || treeName;
                        const skilltrees = treeValue.skilltrees;
                        if (!Array.isArray(skilltrees)) return;
                        skilltrees.forEach(st => {
                            const segments = st.segments;
                            if (!Array.isArray(segments)) return;
                            segments.forEach(seg => {
                                const progressgraph = seg.progressgraph != null ? String(seg.progressgraph) : '';
                                const tiers = seg.tiers;
                                if (!Array.isArray(tiers)) return;
                                tiers.forEach(tier => {
                                    const nodes = tier.nodes;
                                    if (!Array.isArray(nodes)) return;
                                    nodes.forEach(node => {
                                        const name = node.name;
                                        if (!name) return;
                                        const tooltip = node.tooltip;
                                        const iconRaw = node.icon;
                                        const hasTooltip = tooltip && typeof tooltip === 'string';
                                        const hasIcon = iconRaw && typeof iconRaw === 'string';
                                        if (!hasTooltip && !hasIcon) return;
                                        const key = progressgraph + '|' + name;
                                        const prev = skilltreesNodeByGraphAndName.get(key) || {};
                                        const merged = { ...prev, character: character || prev.character };
                                        if (hasTooltip) merged.tooltipRef = tooltip;
                                        if (hasIcon) merged.iconAsset = iconRaw;
                                        skilltreesNodeByGraphAndName.set(key, merged);
                                    });
                                });
                            });
                        });
                    });
                });
            });
        }

        /** Merge uitooltipdata from a single parsed JSON payload into data.uitooltipByKey. */
        function mergeUitooltipdataIntoData(data, parsedPayload) {
            if (!data || !parsedPayload?.uitooltipdata?.records) return;
            if (!data.uitooltipByKey) data.uitooltipByKey = new Map();
            const uitooltipByKey = data.uitooltipByKey;
            parsedPayload.uitooltipdata.records.forEach(record => {
                (record.entries || []).forEach(entryObj => {
                    if (!entryObj || typeof entryObj !== 'object') return;
                    Object.entries(entryObj).forEach(([key, val]) => {
                        if (val && typeof val === 'object') {
                            const mapKey = String(key).toLowerCase();
                            uitooltipByKey.set(mapKey, val);
                        }
                    });
                });
            });
        }

        function extractFromOriginalFormat(rawData) {
            const rootByKey = new Map();
            const subItems = [];
            const dependencyTables = new Map();
            let totalSubItems = 0;

            // Process each record
            if (rawData.inv && rawData.inv.records) {
                rawData.inv.records.forEach((record) => {
                    if (!record.entries) return;

                    record.entries.forEach((entry) => {
                        // First, find the root definition key (the one with _scope === "Root")
                        let rootKey = null;
                        let rootDef = null;
                        
                        Object.keys(entry).forEach(key => {
                            if (key === '__dep_entries') return;

                            const item = entry[key];
                            if (item && item.serialindex && item.serialindex._scope === 'Root') {
                                rootKey = key;
                                // Parse serialIndex to ensure it's a number (handle both string and number formats)
                                const serialIdx = item.serialindex.index;
                                const parsedSerialIndex = serialIdx !== null && serialIdx !== undefined 
                                    ? (typeof serialIdx === 'number' ? serialIdx : parseInt(serialIdx))
                                    : null;
                                
                                rootDef = {
                                    key: key,
                                    serialIndex: parsedSerialIndex,
                                    inv: item.inv,
                                    itembasename: item.itembasename,
                                    basetype: item.basetype,
                                    manufacturer: item.manufacturer,
                                    parttypes: item.parttypes || null, // Store parttypes for slot inclusion
                                    data: item, // Store full item data for accessing all properties
                                    depEntries: []
                                };
                            }
                        });

                        // If we found a root definition, process its dependency entries
                        if (rootDef) {
                            let existingRoot = rootByKey.get(rootDef.key);
                            
                            if (existingRoot) {
                                existingRoot.serialIndex = rootDef.serialIndex;
                                existingRoot.inv = rootDef.inv;
                                existingRoot.itembasename = rootDef.itembasename;
                                existingRoot.basetype = rootDef.basetype;
                                existingRoot.manufacturer = rootDef.manufacturer;
                                existingRoot.parttypes = rootDef.parttypes;
                                existingRoot.data = rootDef.data;
                                rootDef = existingRoot;
                            } else {
                                rootByKey.set(rootDef.key, rootDef);
                            }
                            
                            // Extract dependency entries
                            if (entry.__dep_entries && Array.isArray(entry.__dep_entries)) {
                                    entry.__dep_entries.forEach((depEntry) => {
                                        const depTableName = depEntry.depTableName;
                                        const depTableId = depEntry.depTableId;
                                        const depEntryIndex = depEntry.depIndex;

                                        // Find ALL items in the dependency entry (not just the first one)
                                        // This handles cases where multiple items are in one entry (e.g., part_barrel_01_d and comp_05_legendary_slugger)
                                        const depItemKeys = Object.keys(depEntry).filter(k =>
                                            k !== 'depTableName' &&
                                            k !== 'depTableId' &&
                                            k !== 'depIndex' &&
                                            k !== 'addtags' &&
                                            k !== 'dependencytags' &&
                                            k !== 'exclusiontags'
                                        );

                                        // Process each item found in this dependency entry
                                        depItemKeys.forEach(depItemKey => {
                                            const depItem = depEntry[depItemKey];
                                            
                                            // Only process if it's an object (not a primitive value)
                                            if (depItem && typeof depItem === 'object' && !Array.isArray(depItem) && depItem !== null) {
                                                // Extract serial index - handle both string and number formats
                                                let extractedSerialIndex = null;
                                                if (depItem.serialindex) {
                                                    if (typeof depItem.serialindex === 'object' && depItem.serialindex !== null) {
                                                        // Nested object: { index: "82", ... }
                                                        const indexValue = depItem.serialindex.index;
                                                        if (indexValue !== null && indexValue !== undefined) {
                                                            extractedSerialIndex = typeof indexValue === 'number' ? indexValue : parseInt(indexValue);
                                                            if (isNaN(extractedSerialIndex)) {
                                                                extractedSerialIndex = null;
                                                            }
                                                        }
                                                    } else if (typeof depItem.serialindex === 'number') {
                                                        // Direct number value
                                                        extractedSerialIndex = depItem.serialindex;
                                                    } else if (typeof depItem.serialindex === 'string') {
                                                        // String value - try to parse
                                                        const parsed = parseInt(depItem.serialindex);
                                                        if (!isNaN(parsed)) {
                                                            extractedSerialIndex = parsed;
                                                        }
                                                    }
                                                }
                                                
                                                const subItem = {
                                                    rootKey: rootDef.key,
                                                    rootSerialIndex: rootDef.serialIndex,
                                                    depTableName: depTableName,
                                                    depTableId: depTableId,
                                                    depIndex: depEntryIndex,
                                                    key: depItemKey,
                                                    serialIndex: extractedSerialIndex,
                                                    serialindex: depItem.serialindex || null, // Preserve original structure for fallback
                                                    _category: depItem.serialindex?._category,
                                                    _scope: depItem.serialindex?._scope,
                                                    addtags: depEntry.addtags || depItem.addtags || [],
                                                    dependencytags: depEntry.dependencytags || depItem.dependencytags || [],
                                                    exclusiontags: depEntry.exclusiontags || depItem.exclusiontags || [],
                                                    basetags: depItem.basetags || [],
                                                    parttypeselectionrules: depItem.parttypeselectionrules || null,
                                                    aspects: depItem.aspects || [],
                                                    basecomposition: depItem.basecomposition || null,
                                                    rarity: depItem.rarity || null,
                                                    basetags: depItem.basetags || [],
                                                    bexcludefromglobalpooloverride: depItem.bexcludefromglobalpooloverride ?? null,
                                                    bexcludefromglobalpool: depItem.bexcludefromglobalpool ?? null,
                                                    criterionmethod: depItem.criterionmethod ?? null,
                                                    debugdisplaydescription: depItem.debugdisplaydescription != null ? String(depItem.debugdisplaydescription) : undefined,
                                                    item: depItem
                                                };

                                                rootDef.depEntries.push(subItem);
                                                subItems.push(subItem);
                                                totalSubItems++;

                                                // Track dependency tables
                                                if (!dependencyTables.has(depTableName)) {
                                                    dependencyTables.set(depTableName, {
                                                        name: depTableName,
                                                        id: depTableId,
                                                        items: []
                                                    });
                                                }
                                                dependencyTables.get(depTableName).items.push(subItem);
                                            }
                                        });
                                    });
                                }
                        } else {
                            // Handle continuation entries: entry where root key exists but is null/undefined
                            // These contain additional dependency entries for an existing root
                            // Also handle entries where the key is null but has __dep_entries (like "weapon": null)
                            Object.keys(entry).forEach(key => {
                                if (key === '__dep_entries') return;
                                
                                let existingRoot = rootByKey.get(key);
                                
                                if (!existingRoot && (entry[key] === null || entry[key] === undefined) && entry.__dep_entries && Array.isArray(entry.__dep_entries)) {
                                    existingRoot = {
                                        key: key,
                                        serialIndex: null,
                                        inv: null,
                                        itembasename: key,
                                        basetype: null,
                                        manufacturer: null,
                                        depEntries: []
                                    };
                                    rootByKey.set(key, existingRoot);
                                }
                                
                                if (existingRoot && entry.__dep_entries && Array.isArray(entry.__dep_entries)) {
                                    // Process dependency entries and add them to the existing root
                                    entry.__dep_entries.forEach((depEntry) => {
                                        const depTableName = depEntry.depTableName;
                                        const depTableId = depEntry.depTableId;
                                        const depEntryIndex = depEntry.depIndex;

                                        // Find ALL items in the dependency entry
                                        const depItemKeys = Object.keys(depEntry).filter(k =>
                                            k !== 'depTableName' &&
                                            k !== 'depTableId' &&
                                            k !== 'depIndex' &&
                                            k !== 'addtags' &&
                                            k !== 'dependencytags' &&
                                            k !== 'exclusiontags'
                                        );

                                        // Process each item found in this dependency entry
                                        depItemKeys.forEach(depItemKey => {
                                            const depItem = depEntry[depItemKey];
                                            
                                            // Only process if it's an object (not a primitive value)
                                            if (depItem && typeof depItem === 'object' && !Array.isArray(depItem) && depItem !== null) {
                                                const subItem = {
                                                    rootKey: existingRoot.key,
                                                    rootSerialIndex: existingRoot.serialIndex,
                                                    depTableName: depTableName,
                                                    depTableId: depTableId,
                                                    depIndex: depEntryIndex,
                                                    key: depItemKey,
                                                    serialIndex: depItem.serialindex?.index,
                                                    _category: depItem.serialindex?._category,
                                                    _scope: depItem.serialindex?._scope,
                                                    addtags: depEntry.addtags || depItem.addtags || [],
                                                    dependencytags: depEntry.dependencytags || depItem.dependencytags || [],
                                                    exclusiontags: depEntry.exclusiontags || depItem.exclusiontags || [],
                                                    basetags: depItem.basetags || [],
                                                    parttypeselectionrules: depItem.parttypeselectionrules || null,
                                                    aspects: depItem.aspects || [],
                                                    basecomposition: depItem.basecomposition || null,
                                                    rarity: depItem.rarity || null,
                                                    basetags: depItem.basetags || [],
                                                    bexcludefromglobalpooloverride: depItem.bexcludefromglobalpooloverride ?? null,
                                                    bexcludefromglobalpool: depItem.bexcludefromglobalpool ?? null,
                                                    criterionmethod: depItem.criterionmethod ?? null,
                                                    debugdisplaydescription: depItem.debugdisplaydescription != null ? String(depItem.debugdisplaydescription) : undefined,
                                                    item: depItem
                                                };

                                                existingRoot.depEntries.push(subItem);
                                                subItems.push(subItem);
                                                totalSubItems++;

                                                // Track dependency tables
                                                if (!dependencyTables.has(depTableName)) {
                                                    dependencyTables.set(depTableName, {
                                                        name: depTableName,
                                                        id: depTableId,
                                                        items: []
                                                    });
                                                }
                                                dependencyTables.get(depTableName).items.push(subItem);
                                            }
                                        });
                                    });
                                }
                            });
                        }
                    });
                });
            }

            const rootDefinitions = Array.from(rootByKey.values());

            // Build statistics
            const dependencyTablesObj = {};
            dependencyTables.forEach((table, name) => {
                dependencyTablesObj[name] = {
                    id: table.id,
                    count: table.items.length
                };
            });

            const rootDefinitionsByType = {};
            rootDefinitions.forEach(root => {
                const basetype = root.basetype || 'unknown';
                rootDefinitionsByType[basetype] = (rootDefinitionsByType[basetype] || 0) + 1;
            });

            // Build subItemsByTable (keep aspects so passive_points can resolve skilltree/CDN icons)
            const subItemsByTable = {};
            dependencyTables.forEach((table, name) => {
                subItemsByTable[name] = {
                    depTableId: table.id,
                    items: table.items.map(item => ({
                        rootKey: item.rootKey,
                        rootSerialIndex: item.rootSerialIndex,
                        depIndex: item.depIndex,
                        depTableName: item.depTableName || name,
                        key: item.key,
                        serialIndex: item.serialIndex,
                        addtags: item.addtags || [],
                        dependencytags: item.dependencytags || [],
                        exclusiontags: item.exclusiontags || [],
                        aspects: item.aspects || [],
                        item: item.item || null,
                        debugdisplaydescription: item.debugdisplaydescription
                    }))
                };
            });

            return {
                rootDefinitions: rootDefinitions,
                statistics: {
                    totalRootDefinitions: rootDefinitions.length,
                    totalSubItems: totalSubItems,
                    dependencyTables: dependencyTablesObj,
                    rootDefinitionsByType: rootDefinitionsByType
                },
                subItemsByTable: subItemsByTable
            };
        }

        /** Normalize tooltip ref (e.g. "uitooltipdata'ToolTip_DS_P_DemonicInscriptions'") to uitooltipdata lookup key (e.g. "tooltip_ds_p_demonicinscriptions"). */
        function normalizeTooltipRef(ref) {
            if (ref == null || typeof ref !== 'string') return '';
            const m = ref.match(/uitooltipdata'([^']+)'/i);
            const inner = m ? m[1] : ref.replace(/^uitooltipdata'|'$/gi, '').trim();
            return inner.toLowerCase();
        }

        /** Parse localization-style header/body (e.g. "tooltips_passives, 46F476874BA91F5FCAAA5893B27CD6AD, Fell Inscriptions") to display text (third segment / after last comma). */
        function parseTooltipDisplayText(locKey) {
            if (locKey == null || typeof locKey !== 'string') return '';
            const parts = locKey.split(',');
            const last = parts.length > 0 ? parts[parts.length - 1].trim() : '';
            return last || locKey.trim();
        }

        /** uitooltipdata StringDisplayValue rows use argsmap on the stat object (same shape as ui_stat statvalue). */
        function parseTooltipStringDisplayArgSpecs(stat) {
            if (!stat || typeof stat !== 'object') return undefined;
            return parseUiStatPrimaryArgSpecs({ statvalue: stat });
        }

        /** Resolve uitooltipdata `stats` array to raw display lines (placeholders + [tag] markup) for formatUiStatTextToHtml. */
        function buildUitooltipStatsDescriptionRaw(tooltip, data) {
            if (!tooltip || !Array.isArray(tooltip.stats) || !data) return '';
            const lines = [];
            for (let si = 0; si < tooltip.stats.length; si++) {
                const stat = tooltip.stats[si];
                if (!stat || typeof stat !== 'object') continue;
                const ft = stat.formattext;
                if (!ft || typeof ft !== 'string') continue;
                const st = String(stat.structtype || '');
                if (st.indexOf('StringDisplayValue') === -1 && !/\{[0-9]+\}/.test(ft)) continue;
                const template = parseTooltipDisplayText(ft);
                if (!template) continue;
                const argSpecs = parseTooltipStringDisplayArgSpecs(stat);
                const resolved = resolveUiStatPrimaryPlaceholders(template, argSpecs, data);
                if (resolved && String(resolved).trim()) lines.push(resolved);
            }
            return lines.join('[newline]');
        }

        /** When skillMapping has no description (or no row), fill name/description/character from Nexus skilltrees + uitooltipdata. */
        function enrichPassiveSkillInfoWithNexusUitooltip(skillInfo, item, data) {
            const nx = getSkillInfoForPassive(item, data);
            if (!nx) return skillInfo || null;
            const out = skillInfo && typeof skillInfo === 'object' ? { ...skillInfo } : {};
            if (nx.character && !out.character) out.character = nx.character;
            if (!String(out.description || '').trim() && nx.description) out.description = nx.description;
            if (!(out.name || out.skill_name) && (nx.name || nx.skill_name)) {
                out.name = nx.name;
                out.skill_name = nx.skill_name;
            }
            return out;
        }

        // Get skill info from serial ID
        function getSkillInfo(serialId) {
            if (!serialId) return null;
            
            // Convert serial ID to string format (e.g., 255:194)
            let serialStr = typeof serialId === 'string' 
                ? serialId 
                : `${serialId.rootSerial || serialId.root || 255}:${serialId.subSerial || serialId.sub || serialId}`;
            
            // Try exact match first
            let skillInfo = skillMapping.get(serialStr);
            if (skillInfo) return skillInfo;
            
            // If not found, try different formats (but ONLY with the same rootSerial)
            // The id_index might store keys differently (e.g., as numbers or different string formats)
            // Try parsing the serialStr to extract root and sub, then try variations
            // IMPORTANT: Do NOT try just the subSerial alone, as skills don't traverse classes
            // Each character has their own skills even if they happen to have the same subSerial number
            const parts = serialStr.split(':');
            if (parts.length === 2) {
                const rootSerial = parts[0];
                const subSerial = parts[1];
                
                // Try variations (but ALWAYS include rootSerial to avoid cross-class matches):
                // 1. Original format: "254:192"
                // 2. Number format: try as normalized numbers
                // NOTE: We do NOT try just subSerial alone, as that would match skills from other characters
                const variations = [
                    serialStr,                    // "254:192"
                    `${rootSerial}:${subSerial}`, // Same but explicit
                    `${parseInt(rootSerial)}:${parseInt(subSerial)}`, // "254:192" (normalized)
                ];
                
                for (const variant of variations) {
                    skillInfo = skillMapping.get(variant);
                    if (skillInfo) {
                        // Found it! Log for debugging
                        if (variant !== serialStr) {
                            console.log(`[Skill Lookup] Found skill with variant format: "${serialStr}" -> "${variant}"`);
                        }
                        return skillInfo;
                    }
                }
                
                // NOTE: Removed fallback that tries all classmod root serials
                // This was causing unmapped skills to appear as mapped when they matched a different character's skill
                // Skills should only match their exact serial ID to avoid false positives
            }
            
            return null;
        }
        
        // Get skill image URL (similar to item_editor.html)
        function getSkillImageUrl(skillName, characterName) {
            if (!skillName) return null;
            
            // Convert skill name to filename-safe format
            const skillNameToFilename = (name) => {
                // Strip parenthetical content (e.g., "(Amon)" from "The Thrill (Amon)")
                // The character name is passed separately, so we don't need it in the filename
                let cleanedName = name.replace(/\s*\([^)]*\)\s*/g, "").trim();
                
                return cleanedName
                    .replace(/\s+/g, "_")
                    .replace(/[^a-zA-Z0-9_!]/g, "")
                    .toLowerCase();
            };
            
            const baseUrl = "https://save-editor.be/skill_imagesT/";
            let filename = `${skillNameToFilename(skillName)}.png`;
            
            // Map character names to image suffixes (https://save-editor.be/skill_imagesT/)
            // _1 (Siren/Vex), _2 (Rafa), _3 (Harlowe), _4 (Amon/Paladin - same character on CDN)
            if (characterName) {
                const charLower = characterName.toLowerCase();
                if (charLower === "siren" || charLower === "vex") {
                    filename = filename.replace(".png", "_1.png");
                } else if (charLower === "rafa") {
                    filename = filename.replace(".png", "_2.png");
                } else if (charLower === "harlowe") {
                    filename = filename.replace(".png", "_3.png");
                } else if (charLower === "amon" || charLower === "paladin") {
                    filename = filename.replace(".png", "_4.png");
                }
            }
            
            return `${baseUrl}${filename}`;
        }

        const PASSIVE_UIRESOURCES_EXT = '.png';

        /** UE JSON uses lowercase augments/trait; exported folders are Augments/Trait (matters for file: on case-sensitive FS). */
        function remapUiresourcesPathForLocalDisk(relativePath) {
            let s = String(relativePath || '');
            s = s.replace(/^_shared\//i, '_Shared/');
            s = s.replace(/^skill_icons\/augments\//i, 'skill_icons/Augments/');
            s = s.replace(/^skill_icons\/trait\//i, 'skill_icons/Trait/');
            return s;
        }

        /** CDN in browser; absolute file:// from main/preload in Electron (works before did-finish-load). */
        function getPassiveUiresourcesBase() {
            if (typeof window === 'undefined') {
                return 'https://save-editor.be/uiresources/';
            }
            const injected = window.ELECTRON_UIRESOURCES_BASE;
            if (typeof injected === 'string' && injected.length > 0) {
                return injected.endsWith('/') ? injected : `${injected}/`;
            }
            if (window.electronAPI && typeof window.electronAPI.getUiresourcesBase === 'function') {
                const fromMain = window.electronAPI.getUiresourcesBase();
                if (typeof fromMain === 'string' && fromMain.length > 0) {
                    return fromMain.endsWith('/') ? fromMain : `${fromMain}/`;
                }
            }
            const inElectron = window.IS_ELECTRON_APP === true ||
                (window.electronAPI && typeof window.electronAPI.isElectron === 'function' && window.electronAPI.isElectron());
            if (inElectron) {
                try {
                    return new URL('uiresources/', window.location.href).href;
                } catch (e) {
                    /* fall through */
                }
            }
            return 'https://save-editor.be/uiresources/';
        }

        /** Build URL for a passive/skill-tree icon from an Unreal Asset path (suffix after uiresources/). */
        /** UE icon paths use duplicate basenames (e.g. foo.foo); hosted PNGs are foo.png. */
        function normalizeUiresourcesRelativePath(suffix) {
            const s = String(suffix || '').trim();
            if (!s) return s;
            const slash = s.lastIndexOf('/');
            const dir = slash >= 0 ? s.slice(0, slash + 1) : '';
            const file = slash >= 0 ? s.slice(slash + 1) : s;
            const m = file.match(/^(.+)\.(.+)$/);
            if (m && m[1] === m[2]) {
                return dir + m[1];
            }
            return s;
        }

        function assetPathToUiresourcesUrl(assetString) {
            if (assetString == null || typeof assetString !== 'string') return null;
            const trimmed = assetString.trim();
            let inner = trimmed;
            const assetMatch = trimmed.match(/^Asset'\s*(.+?)\s*'$/i);
            if (assetMatch) inner = assetMatch[1];
            const lower = inner.toLowerCase();
            const idx = lower.lastIndexOf('uiresources/');
            if (idx === -1) return null;
            const suffix = inner.slice(idx + 'uiresources/'.length).trim();
            if (!suffix) return null;
            let normalized = normalizeUiresourcesRelativePath(suffix);
            const base = getPassiveUiresourcesBase();
            if (base.startsWith('file:')) {
                normalized = remapUiresourcesPathForLocalDisk(normalized);
            }
            return `${base}${normalized}${PASSIVE_UIRESOURCES_EXT}`;
        }

        /** Match a dep entry when subItemsByTable rows are shallow or missing aspects. */
        function findDepEntryForTableItem(item, data, tableName) {
            if (!item || !data?.rootDefinitions || !tableName || !item.key) return null;
            const wantKey = String(item.key).toLowerCase();
            const wantRoot = item.rootKey != null && item.rootKey !== '' ? String(item.rootKey) : null;
            const wantDepIndex = item.depIndex;
            for (let r = 0; r < data.rootDefinitions.length; r++) {
                const deps = data.rootDefinitions[r].depEntries;
                if (!Array.isArray(deps)) continue;
                for (let d = 0; d < deps.length; d++) {
                    const dep = deps[d];
                    if (dep.depTableName !== tableName) continue;
                    if (!dep.key || String(dep.key).toLowerCase() !== wantKey) continue;
                    if (wantRoot != null && dep.rootKey != null && String(dep.rootKey) !== wantRoot) continue;
                    if (wantDepIndex != null && wantDepIndex !== undefined && dep.depIndex != null && dep.depIndex !== undefined && Number(dep.depIndex) !== Number(wantDepIndex)) {
                        continue;
                    }
                    return dep;
                }
            }
            return null;
        }

        function getPassiveAspectGraphAndNodeName(item, data, tableName) {
            const tn = tableName || 'passive_points';
            const fromAspects = (aspects) => {
                if (!Array.isArray(aspects)) return { progressgraph: '', nodename: '' };
                for (const aspect of aspects) {
                    if (!aspect || typeof aspect !== 'object') continue;
                    const passives = aspect.passives;
                    if (Array.isArray(passives) && passives.length > 0 && passives[0]) {
                        return {
                            progressgraph: passives[0].progressgraph != null ? String(passives[0].progressgraph) : '',
                            nodename: passives[0].nodename != null ? String(passives[0].nodename) : ''
                        };
                    }
                }
                return { progressgraph: '', nodename: '' };
            };
            let aspects = item?.aspects || item?.item?.aspects;
            if ((!aspects || !aspects.length) && data?.rootDefinitions) {
                const full = findDepEntryForTableItem(item, data, tn);
                if (full) {
                    aspects = full.aspects || full.item?.aspects;
                }
            }
            return fromAspects(aspects);
        }

        /** Prefer hosted uiresources icon for passive_points when skilltrees_data has an icon asset for the node. */
        function getPassiveUiresourcesIconUrl(item, data, tableName) {
            if (!item || !data?.skilltreesNodeByGraphAndName) return null;
            const tn = tableName || item.depTableName || 'passive_points';
            const { progressgraph, nodename } = getPassiveAspectGraphAndNodeName(item, data, tn);
            if (!progressgraph || !nodename) return null;
            const nodeInfo = data.skilltreesNodeByGraphAndName.get(progressgraph + '|' + nodename);
            const asset = nodeInfo?.iconAsset;
            if (!asset || typeof asset !== 'string') return null;
            return assetPathToUiresourcesUrl(asset);
        }

        /** Get skill info for a passive_point item from merged Nexus skilltrees_data + uitooltipdata. */
        function getSkillInfoForPassive(item, data) {
            if (!item || !data?.skilltreesNodeByGraphAndName) return null;
            const { progressgraph, nodename } = getPassiveAspectGraphAndNodeName(item, data, 'passive_points');
            if (!progressgraph || !nodename) return null;
            const key = progressgraph + '|' + nodename;
            const nodeInfo = data.skilltreesNodeByGraphAndName.get(key);
            if (!nodeInfo) return null;
            if (nodeInfo.tooltipRef && data.uitooltipByKey) {
                const tooltipKey = normalizeTooltipRef(nodeInfo.tooltipRef);
                const tooltip = data.uitooltipByKey.get(tooltipKey);
                if (tooltip) {
                    const name = parseTooltipDisplayText(tooltip.header);
                    let description = parseTooltipDisplayText(tooltip.body);
                    const statsRaw = buildUitooltipStatsDescriptionRaw(tooltip, data);
                    if (statsRaw) {
                        description = description ? (description + '[newline]' + statsRaw) : statsRaw;
                    }
                    return {
                        name: name || null,
                        skill_name: name || null,
                        description: description || null,
                        character: nodeInfo.character || null
                    };
                }
            }
            if (nodeInfo.character) {
                return {
                    name: null,
                    skill_name: null,
                    description: null,
                    character: nodeInfo.character
                };
            }
            return null;
        }

        function loadData() {
            if (!data) return;

            invalidateMiGlobalPartCatalog();

            hideError();
            forEachLegitInstance(function () {
                updateStats();
                populateTypeFilter();
            });
            requestAnimationFrame(() => {
                forEachLegitInstance(function () {
                    displayRootDefinitions();
                });
                const scheduleNext = (fn) => {
                    if (typeof requestIdleCallback !== 'undefined') {
                        requestIdleCallback(fn, { timeout: 150 });
                    } else {
                        requestAnimationFrame(fn);
                    }
                };
                scheduleNext(() => {
                    forEachLegitInstance(function () {
                        displayDependencyTables();
                    });
                    scheduleNext(() => {
                        forEachLegitInstance(function () {
                            displayMultiplierStructs();
                            updateSelectedPartsOutput();
                            attachPartSelectionHandler();
                        });
                        const miHost = document.getElementById('mi_simpleBuilderHost');
                        if (miHost && miHost.style.display !== 'none') {
                            miInitOpenModdedPickers(miHost);
                        }
                        document.querySelectorAll('#item-editor-tab .mi-cross-section').forEach(function (sec) {
                            const shell = sec.closest('.mi-compact-dep-shell');
                            if (shell) miInitCrossManufacturerPickers(shell);
                        });
                    });
                });
            });
        }

        function updateStats() {
            if (data.statistics) {
                const sr = lbEl('statRoots');
                if (!sr) return;
                sr.textContent = data.statistics.totalRootDefinitions || 0;
                const ss = lbEl('statSubs');
                if (ss) ss.textContent = data.statistics.totalSubItems || 0;
                const st = lbEl('statTables');
                if (st) st.textContent = Object.keys(data.statistics.dependencyTables || {}).length;
                const sm = lbEl('statMultipliers');
                if (sm) sm.textContent = data.statistics.multiplierStructs || 0;
                const statsEl = lbEl('stats');
                if (statsEl) statsEl.classList.remove('hidden');

                if (loadedFileNames.length > 0) {
                    const fileCount = loadedFileNames.length;
                    const fileListSpan = lbEl('fileList');
                    if (fileListSpan) fileListSpan.textContent = loadedFileNames.join(', ') + ` (${fileCount} file${fileCount > 1 ? 's' : ''})`;
                }

                // Count items per file and track duplicates
                const fileCounts = {};
                const itemToFiles = {}; // Track which files each item appears in
                
                if (data.rootDefinitions) {
                    data.rootDefinitions.forEach(root => {
                        // Use sourceFiles array if available (from merged items), otherwise use sourceFile
                        const sourceFiles = root.sourceFiles || [root.sourceFile || 'Unknown'];
                        const itemKey = root.key || 'unknown';
                        
                        // Count this item for each source file
                        sourceFiles.forEach(sourceFile => {
                            if (!fileCounts[sourceFile]) {
                                fileCounts[sourceFile] = 0;
                            }
                            fileCounts[sourceFile]++;
                            
                            // Track which files contain this item
                            if (!itemToFiles[itemKey]) {
                                itemToFiles[itemKey] = [];
                            }
                            if (!itemToFiles[itemKey].includes(sourceFile)) {
                                itemToFiles[itemKey].push(sourceFile);
                            }
                        });
                    });
                }

                const fileList = Object.keys(fileCounts).sort((a, b) => fileCounts[b] - fileCounts[a]);

                // Update or create file list section (place it in the main content area)
                // Note: This is only shown in Advanced Mode, so we'll insert it in the advancedControls section
                let fileListContainer = lbEl('fileListContainer');
                if (!fileListContainer) {
                    fileListContainer = document.createElement('div');
                    fileListContainer.id = (window.__lbCtx.idPrefix || '') + 'fileListContainer';
                    fileListContainer.className = 'advanced-only';
                    fileListContainer.style.cssText = 'margin: var(--section-gap) 0; padding: var(--section-gap); background: var(--bg-tertiary); border: 1px solid var(--border-color);';
                    // Insert after the stats section, within advancedControls
                    const statsElement = lbEl('stats');
                    const advancedControls = lbEl('advancedControls');
                    if (statsElement && statsElement.parentNode) {
                        // Insert after stats, within the same parent (advancedControls)
                        // Use insertBefore with nextSibling, or appendChild if nextSibling is null
                        const nextSibling = statsElement.nextSibling;
                        if (nextSibling) {
                            statsElement.parentNode.insertBefore(fileListContainer, nextSibling);
                        } else {
                            statsElement.parentNode.appendChild(fileListContainer);
                        }
                    } else if (advancedControls) {
                        // Fallback: append to advancedControls
                        advancedControls.appendChild(fileListContainer);
                    }
                }

                if (Object.keys(fileCounts).length > 0) {
                    fileListContainer._fileCounts = fileCounts;
                    fileListContainer._fileList = fileList;
                    fileListContainer._itemToFiles = itemToFiles;

                    let fileListHTML = `
                        <div class="file-list-files-header" style="font-weight: bold; margin-bottom: 10px; color: #4CAF50; cursor: pointer; user-select: none; display: flex; align-items: center;">
                            <span style="display: inline-block; width: 20px; text-align: center; margin-right: 10px; transition: transform 0.3s;">▶</span>
                            Files Containing Data:
                        </div>
                        <div class="file-list-content collapsed" style="font-size: 12px; color: #aaa; line-height: 1.8;">
                    `;
                    
                    fileList.forEach((fileName, index) => {
                        const count = fileCounts[fileName];
                        fileListHTML += `
                            <div style="margin-bottom: 8px;">
                                <span style="color: #fff;">${index + 1}. ${fileName}</span>
                                <span style="color: #4CAF50; margin-left: 10px;">(${count} items)</span>
                            </div>
                        `;
                    });
                    
                    fileListHTML += '</div>';
                    fileListHTML += '<div style="margin-top: 15px; padding-top: 15px; border-top: 1px solid #444;">';
                    fileListHTML += `
                        <div class="duplicates-header" style="font-weight: bold; margin-bottom: 8px; color: #4CAF50; cursor: pointer; user-select: none; display: flex; align-items: center;">
                            <span style="display: inline-block; width: 20px; text-align: center; margin-right: 10px; transition: transform 0.3s;">▶</span>
                            Duplicated Items Between Files:
                        </div>
                        <div class="duplicates-content collapsed" data-duplicates-placeholder="true" style="font-size: 11px; color: #888; font-style: italic;">Click to compute duplicate items between files.</div>
                    `;
                    fileListHTML += '</div>';
                    fileListContainer.innerHTML = fileListHTML;
                    fileListContainer.style.display = 'block';

                    if (!fileListContainer._fileListClickHandlerAttached) {
                        fileListContainer._fileListClickHandlerAttached = true;
                        fileListContainer.addEventListener('click', function(e) {
                            const filesHeader = e.target.closest('.file-list-files-header');
                            if (filesHeader) {
                                filesHeader.classList.toggle('expanded');
                                filesHeader.nextElementSibling.classList.toggle('collapsed');
                                return;
                            }
                            const dupHeader = e.target.closest('.duplicates-header');
                            if (dupHeader) {
                                const content = dupHeader.nextElementSibling;
                                if (content.getAttribute('data-duplicates-placeholder') === 'true') {
                                    content.removeAttribute('data-duplicates-placeholder');
                                    const fileCounts = fileListContainer._fileCounts;
                                    const fileList = fileListContainer._fileList;
                                    const itemToFiles = fileListContainer._itemToFiles;
                                    const filePairs = {};
                                    const countedItems = new Set();
                                    if (fileList && itemToFiles) {
                                        fileList.forEach((file1, i) => {
                                            fileList.slice(i + 1).forEach(file2 => {
                                                let duplicateCount = 0;
                                                const duplicateItems = [];
                                                Object.entries(itemToFiles).forEach(([itemKey, files]) => {
                                                    if (files.includes(file1) && files.includes(file2)) {
                                                        const itemPairKey = `${itemKey}|${file1}|${file2}`;
                                                        if (!countedItems.has(itemPairKey)) {
                                                            duplicateCount++;
                                                            duplicateItems.push(itemKey);
                                                            countedItems.add(itemPairKey);
                                                        }
                                                    }
                                                });
                                                if (duplicateCount > 0) {
                                                    const pairKey = [file1, file2].sort().join('|');
                                                    if (!filePairs[pairKey]) filePairs[pairKey] = { count: 0, items: [] };
                                                    filePairs[pairKey].count += duplicateCount;
                                                    filePairs[pairKey].items.push(...duplicateItems);
                                                }
                                            });
                                        });
                                    }
                                    if (Object.keys(filePairs).length > 0) {
                                        let inner = '';
                                        Object.entries(filePairs).sort((a, b) => b[1].count - a[1].count).forEach(([pairKey, pairData]) => {
                                            const [file1, file2] = pairKey.split('|');
                                            const duplicateCount = pairData.count;
                                            const sampleItems = pairData.items.slice(0, 5);
                                            inner += `<div style="margin-bottom: 8px; padding: 8px; background: #1a1a1a; border-radius: 4px;">
                                                <div style="margin-bottom: 4px;"><span style="color: #fff;">${file1}</span><span style="color: #888;"> ↔ </span><span style="color: #fff;">${file2}</span><span style="color: #FFA500; margin-left: 10px;">(${duplicateCount} shared items)</span></div>
                                                ${sampleItems.length > 0 ? `<div style="margin-top: 4px; font-size: 10px; color: #666;">Examples: ${sampleItems.map(item => item.replace(/serial_/, '')).join(', ')}${duplicateCount > 5 ? ' (+' + (duplicateCount - 5) + ' more)' : ''}</div>` : ''}
                                            </div>`;
                                        });
                                        content.innerHTML = inner;
                                        content.style.fontStyle = 'normal';
                                    } else {
                                        content.innerHTML = 'No duplicate items detected between files. Items may be merged when loaded.';
                                    }
                                }
                                dupHeader.classList.toggle('expanded');
                                content.classList.toggle('collapsed');
                            }
                        });
                    }
                } else {
                    fileListContainer.style.display = 'none';
                }
            }
        }

        function populateTypeFilter() {
            const filter = lbEl('typeFilter');
            if (!filter) return;
            const types = new Set();
            
            if (data && data.rootDefinitions) {
                data.rootDefinitions.forEach(root => {
                    if (root.basetype) {
                        types.add(root.basetype);
                    }
                });
            }

            filter.innerHTML = '<option value="">All Types</option>';
            Array.from(types).sort().forEach(type => {
                const option = document.createElement('option');
                option.value = type;
                option.textContent = type;
                filter.appendChild(option);
            });
        }

        function categorizeRoot(root) {
            const basetype = (root.basetype || '').toLowerCase();
            const key = (root.key || '').toLowerCase();
            const inv = (root.inv || '').toLowerCase();

            // Check for prefix-based categories first
            if (key.startsWith('classmod_')) {
                return 'Class Mod';
            }
            if (key.startsWith('def_')) {
                return 'Def';
            }
            if (key.startsWith('invdef_')) {
                return 'InvDef';
            }
            if (key.startsWith('ammo') || key.startsWith('ammo_')) {
                return 'Ammo Types';
            }

            // Check for Universal items (top-level category)
            if (key === 'classmod' || key === 'enhancement' || key === 'repair_kit' ||
                key === 'shield' || key === 'armor_shield' || key === 'energy_shield' ||
                key === 'grenade_gadget' || key === 'terminal_gadget' || key === 'turret_gadget' ||
                key === 'heavy_weapon_gadget' ||
                inv === 'classmod' || inv === 'enhancement' || inv === 'repair_kit' ||
                inv === 'shield' || inv === 'armor_shield' || inv === 'energy_shield' ||
                inv === 'grenade_gadget' || inv === 'terminal_gadget' || inv === 'turret_gadget' ||
                inv === 'heavy_weapon_gadget') {
                return 'Universal';
            }

            // Check for hover drives
            if (basetype.includes('hover_drive') || key.includes('hover_drive') || inv.includes('hover_drive') || 
                basetype.includes('hoverdrive') || key.includes('hoverdrive') || inv.includes('hoverdrive')) {
                return 'Hover Drive';
            }

            // Check for Ordnance items (Turret, Terminal, HW, Grenade)
            // But exclude Universal items (grenade_gadget, terminal_gadget, turret_gadget, heavy_weapon_gadget)
            if ((basetype.includes('turret') || key.includes('turret') || inv.includes('turret')) &&
                key !== 'turret_gadget' && inv !== 'turret_gadget') {
                return 'Ordnance';
            }
            if ((basetype.includes('terminal') || key.includes('terminal') || inv.includes('terminal')) &&
                key !== 'terminal_gadget' && inv !== 'terminal_gadget') {
                return 'Ordnance';
            }
            if ((basetype.includes('heavy') || key.includes('hw') || inv.includes('hw') || basetype.includes('hw')) &&
                key !== 'heavy_weapon_gadget' && inv !== 'heavy_weapon_gadget') {
                return 'Ordnance';
            }
            if ((basetype.includes('grenade') || key.includes('grenade') || inv.includes('grenade')) &&
                key !== 'grenade_gadget' && inv !== 'grenade_gadget') {
                return 'Ordnance';
            }

            // Check for specific categories (but not Universal items already caught above)
            if ((basetype.includes('shield') || key.includes('shield') || inv.includes('shield')) &&
                key !== 'shield' && key !== 'armor_shield' && key !== 'energy_shield' &&
                inv !== 'shield' && inv !== 'armor_shield' && inv !== 'energy_shield') {
                return 'Shield';
            }
            if ((basetype.includes('repair') || key.includes('repkit') || inv.includes('repkit') || inv.includes('repair')) &&
                key !== 'repair_kit' && inv !== 'repair_kit') {
                return 'Repkit';
            }
            if ((basetype.includes('class_mod') || key.includes('class_mod') || inv.includes('class_mod')) &&
                key !== 'classmod' && inv !== 'classmod') {
                return 'Class Mod';
            }
            if ((basetype.includes('enhancement') || key.includes('enhancement') || inv.includes('enhancement')) &&
                key !== 'enhancement' && inv !== 'enhancement') {
                return 'Enhancement';
            }

            // Exclude non-weapon items that might match weapon patterns
            if (key.startsWith('inventorydef_') || key.startsWith('inv_contractitem_') ||
                key.startsWith('pickup_devicepart_') || key.startsWith('pickup_explosive_')) {
                return 'Other';
            }

            // Weapon types (SG, SMG, PS, AR, SR)
            if (basetype.includes('weapon_sg') || basetype.includes('shotgun') || inv.includes('sg')) {
                return 'Weapon';
            }
            if (basetype.includes('weapon_smg') || basetype.includes('submachine') || inv.includes('smg')) {
                return 'Weapon';
            }
            if (basetype.includes('weapon_ps') || basetype.includes('pistol') || inv.includes('ps')) {
                return 'Weapon';
            }
            if (basetype.includes('weapon_ar') || basetype.includes('assault') || inv.includes('ar')) {
                return 'Weapon';
            }
            if (basetype.includes('weapon_sr') || basetype.includes('sniper') || inv.includes('sr')) {
                return 'Weapon';
            }
            if (basetype.includes('weapon')) {
                return 'Weapon';
            }

            return 'Other';
        }

        function extractManufacturer(root) {
            const manufacturer = root.manufacturer || '';
            if (!manufacturer) return 'Unknown';
            
            // Extract manufacturer name from format like "manufacturer'Daedalus'"
            const match = manufacturer.match(/manufacturer'([^']+)'/);
            if (match) {
                // Normalize case: capitalize first letter, lowercase the rest
                const name = match[1];
                let normalizedName = name.charAt(0).toUpperCase() + name.slice(1).toLowerCase();
                
                // Map manufacturer names to in-game display names (full names)
                // Handle case-insensitive matching
                const normalizedLower = normalizedName.toLowerCase();
                const manufacturerMap = {
                    'borg': 'Ripper',
                    'mal': 'Maliwan',
                    'malefactor': 'Maliwan',
                    'hyp': 'Hyperion',
                    'ted': 'Tediore',
                    'tor': 'Torgue',
                    'vla': 'Vladof',
                    'jak': 'Jakobs',
                    'atla': 'Atlas',
                    'atlas': 'Atlas',
                    'cov': 'CoV'
                };
                
                return manufacturerMap[normalizedLower] || normalizedName;
            }
            
            // Try other formats - return full manufacturer names
            if (manufacturer.toLowerCase().includes('bor')) return 'Ripper';
            if (manufacturer.toLowerCase().includes('dad') || manufacturer.toLowerCase().includes('daedalus')) return 'Daedalus';
            if (manufacturer.toLowerCase().includes('mal') || manufacturer.toLowerCase().includes('malefactor')) return 'Maliwan';
            if (manufacturer.toLowerCase().includes('cov')) return 'CoV';
            if (manufacturer.toLowerCase().includes('hyp') || manufacturer.toLowerCase().includes('hyperion')) return 'Hyperion';
            if (manufacturer.toLowerCase().includes('ted') || manufacturer.toLowerCase().includes('tediore')) return 'Tediore';
            if (manufacturer.toLowerCase().includes('tor') || manufacturer.toLowerCase().includes('torgue')) return 'Torgue';
            if (manufacturer.toLowerCase().includes('vla') || manufacturer.toLowerCase().includes('vladof')) return 'Vladof';
            if (manufacturer.toLowerCase().includes('jak') || manufacturer.toLowerCase().includes('jakobs')) return 'Jakobs';
            if (manufacturer.toLowerCase().includes('atla') || manufacturer.toLowerCase().includes('atlas')) return 'Atlas';
            
            // Normalize case for any remaining manufacturers
            return manufacturer.charAt(0).toUpperCase() + manufacturer.slice(1).toLowerCase();
        }

        /**
         * Save/backpack UI: human-readable item class from Nexus root (Shotgun, Class Mod, etc.).
         * Uses basetype and key suffixes; falls back to categorizeRoot when needed.
         */
        function inferSaveEditorItemTypeDisplay(root) {
            if (!root) return '';
            const basetype = (root.basetype || '').toLowerCase();
            const key = (root.key || '').toLowerCase();
            if (key.includes('grenade_gadget')) return 'Grenade';
            if (key.includes('heavy_weapon_gadget')) return 'Heavy Weapon';
            if (key.includes('turret_gadget')) return 'Turret';
            if (key.includes('terminal_gadget')) return 'Terminal';
            if (key.includes('repair_kit') || key.includes('repkit')) return 'Repkit';
            if (
                key === 'armor_shield' ||
                key === 'energy_shield' ||
                (key.includes('shield') &&
                    key !== 'shield' &&
                    !key.includes('repair'))
            ) {
                return 'Shield';
            }
            if (key === 'shield') return 'Shield';
            if (key.includes('classmod')) return 'Class Mod';
            if (key.includes('enhancement')) return 'Enhancement';
            if (key.includes('artifact')) return 'Artifact';
            if (basetype.includes('weapon_sg') || basetype.includes('shotgun') || /_sg(?:_|$)/.test(key)) {
                return 'Shotgun';
            }
            if (
                basetype.includes('weapon_smg') ||
                basetype.includes('submachine') ||
                /_sm(?:_|$)/.test(key) ||
                key.includes('_smg')
            ) {
                return 'SMG';
            }
            if (basetype.includes('weapon_ps') || basetype.includes('pistol') || /_ps(?:_|$)/.test(key)) {
                return 'Pistol';
            }
            if (basetype.includes('weapon_ar') || basetype.includes('assault') || /_ar(?:_|$)/.test(key)) {
                return 'Assault Rifle';
            }
            if (basetype.includes('weapon_sr') || basetype.includes('sniper') || /_sr(?:_|$)/.test(key)) {
                return 'Sniper';
            }
            if (basetype.includes('heavy') || key.includes('_hw')) return 'Heavy Weapon';
            if (basetype.includes('weapon') || /^(mal|jak|ted|tor|hyp|vla|cov|bor|atla|ord|daf|daedalus|tediore|vladof|malefactor|ripper)_/.test(key)) {
                return 'Weapon';
            }
            const cat = categorizeRoot(root);
            if (cat && cat !== 'Universal' && cat !== 'Other' && cat !== 'Def' && cat !== 'InvDef') {
                return cat;
            }
            return '';
        }

        /** Save editor: manufacturer + item type from validated root key + loaded game data. */
        window.getSaveEditorManufacturerTypeFromRootKey = function (rootKey, gameDataRef) {
            const gd = gameDataRef || data;
            if (!rootKey || !gd || !Array.isArray(gd.rootDefinitions)) {
                return { manufacturer: '', type: '' };
            }
            const rk = String(rootKey).trim();
            const root = gd.rootDefinitions.find(function (r) {
                return r && String(r.key || '') === rk;
            });
            if (!root) return { manufacturer: '', type: '' };
            let manufacturer = extractManufacturer(root);
            if (manufacturer === 'Unknown') manufacturer = '';
            const type = inferSaveEditorItemTypeDisplay(root);
            return { manufacturer: manufacturer || '', type: type || '' };
        };

        function attachPartSelectionHandler() {
            forEachLegitInstance(function (inst) {
                if (inst.partClickAttached) return;
                const container = lbEl('rootsContent');
                if (!container) return;
                inst.partClickAttached = true;
                container.addEventListener('click', function (e) {
                    window.__lbCtx = inst;
                    const partElement = e.target.closest('.dep-item[data-is-selectable="true"]');
                    if (partElement) {
                        if (partElement.classList.contains('disabled')) {
                            e.stopPropagation();
                            e.preventDefault();
                            return false;
                        }

                        e.stopPropagation();
                        e.preventDefault();
                        e.stopImmediatePropagation();
                        const rootKey = partElement.getAttribute('data-root-key');
                        const partKey = partElement.getAttribute('data-part-key');
                        const partId = partElement.getAttribute('data-part-id');
                        togglePartSelection(rootKey, partKey, partId, e);
                        return false;
                    }
                }, true);
                if (!inst.depSearchInputAttached) {
                    inst.depSearchInputAttached = true;
                    container.addEventListener('input', function (e) {
                        const t = e.target;
                        if (!t || !t.classList || !t.classList.contains('dep-table-search')) return;
                        const depTable = t.closest('.dep-table');
                        if (!depTable) return;
                        clearTimeout(window.__depTableSearchDebounce);
                        window.__depTableSearchDebounce = setTimeout(function () {
                            applyDepTableSearchFilter(depTable);
                        }, 120);
                    });
                }
            });
            if (!window.__miTabCompactChangeBound) {
                window.__miTabCompactChangeBound = true;
                const miTab = document.getElementById('item-editor-tab');
                if (miTab) {
                    const miRootsHost = document.getElementById('mi_rootsContent');
                    if (miRootsHost && !miRootsHost.dataset.miPickedLineRemoveCapture) {
                        miRootsHost.dataset.miPickedLineRemoveCapture = '1';
                        miRootsHost.addEventListener(
                            'click',
                            function (e) {
                                const btn = e.target && e.target.closest && e.target.closest('.mi-picked-line-remove');
                                if (!btn || !miRootsHost.contains(btn)) return;
                                const line = btn.closest('.mi-slot-picked-line, .mi-cross-line');
                                if (line) line.remove();
                                e.preventDefault();
                                e.stopPropagation();
                                e.stopImmediatePropagation();
                                window.__lbCtx = window.legitBuilderInstances && window.legitBuilderInstances[1];
                                syncAllMiPartLinesFromModdedDom();
                                updateSelectedPartsOutput();
                                updateFinalOutput();
                            },
                            true
                        );
                    }
                    if (!window.__miHumanSerialBlurBound) {
                        window.__miHumanSerialBlurBound = true;
                        document.addEventListener(
                            'blur',
                            function (e) {
                                const t = e.target;
                                if (!t || !t.classList) return;
                                if (t.id === 'currentBuildHumanSerial') {
                                    applyLegitGlobalHumanSerialFromTextarea(t);
                                    return;
                                }
                                if (!t.classList.contains('mi-human-serial-textarea')) return;
                                if (!t.closest('#item-editor-tab')) return;
                                applyModdedHumanSerialFromTextarea(t);
                            },
                            true
                        );
                    }
                    miTab.addEventListener('change', function (e) {
                        const t = e.target;
                        if (!t || !t.classList) return;
                        if (t.classList.contains('mi-slot-select-all')) {
                            const slot = t.closest('.mi-compact-slot');
                            const rich = slot && slot.querySelector('.mi-passive-candidate-list');
                            if (rich) {
                                if (rich.tagName === 'SELECT' && rich.classList.contains('mi-slot-native-cand-select')) {
                                    Array.from(rich.options).forEach((o) => {
                                        if (o.disabled) return;
                                        o.selected = t.checked;
                                    });
                                    t.indeterminate = false;
                                    return;
                                }
                                const rows = Array.from(rich.querySelectorAll('.mi-cand-row'));
                                const selBg = '#2a3f5f';
                                const selOutline = '1px solid #5c9bd1';
                                rows.forEach(row => {
                                    if (t.checked) {
                                        row.classList.add('mi-cand-selected');
                                        row.setAttribute('aria-selected', 'true');
                                        row.style.background = selBg;
                                        row.style.outline = selOutline;
                                    } else {
                                        row.classList.remove('mi-cand-selected');
                                        row.setAttribute('aria-selected', 'false');
                                        row.style.background = '';
                                        row.style.outline = '';
                                    }
                                });
                                t.indeterminate = false;
                                miUpdateSlotCandDropdownLabel(slot);
                                const ddSel = slot && slot.querySelector('.mi-slot-cand-dropdown.mi-slot-cand-dropdown--open');
                                if (ddSel) {
                                    miPositionCandDropdownPanel(ddSel);
                                }
                                return;
                            }
                            const sel = slot && slot.querySelector('select.mi-slot-candidate-list');
                            if (sel && sel.multiple) {
                                Array.from(sel.options).forEach((o) => {
                                    if (o.disabled) return;
                                    o.selected = t.checked;
                                });
                                t.indeterminate = false;
                                miUpdateSlotCandDropdownLabel(slot);
                            }
                            return;
                        }
                        if (
                            t.tagName === 'SELECT' &&
                            t.multiple &&
                            (t.classList.contains('mi-slot-candidate-list') ||
                                t.classList.contains('mi-slot-native-cand-select'))
                        ) {
                            const slot = t.closest('.mi-compact-slot');
                            const sa = slot && slot.querySelector('.mi-slot-select-all');
                            if (!sa) return;
                            const opts = Array.from(t.options).filter((o) => !o.disabled);
                            const nSel = opts.filter((o) => o.selected).length;
                            sa.checked = nSel > 0 && nSel === opts.length;
                            sa.indeterminate = nSel > 0 && nSel < opts.length;
                            return;
                        }
                        if (t.classList.contains('mi-cross-type-select')) {
                            const crossSlot = t.closest('.mi-cross-root-slot');
                            if (crossSlot) miCrossRefreshCandidateList(crossSlot);
                            return;
                        }
                        if (
                            t.classList.contains('mi-cross-search') ||
                            (t.classList.contains('mi-slot-search') && t.closest('.mi-cross-root-slot'))
                        ) {
                            const crossSlot = t.closest('.mi-cross-root-slot');
                            if (crossSlot) miCrossRefreshCandidateList(crossSlot);
                            return;
                        }
                        if (t.classList.contains('mi-slot-all-manufacturers')) {
                            const slot = t.closest('.mi-compact-slot');
                            if (slot) {
                                const rk = slot.getAttribute('data-root-key');
                                const tbl = slot.getAttribute('data-table-name');
                                const inst = window.legitBuilderInstances && window.legitBuilderInstances[1];
                                if (inst && rk && tbl) {
                                    const ukey = rk + '\0' + normalizeDepTableName(tbl);
                                    if (t.checked) inst.miSlotAllManufacturersUnlock.set(ukey, true);
                                    else inst.miSlotAllManufacturersUnlock.delete(ukey);
                                }
                            const det = slot.querySelector('.mi-slot-picker-details');
                            if (!det || det.open) {
                                miSlotPopulateNativeSelect(slot);
                                miSlotPopulateCandidateList(slot);
                            }
                            }
                            return;
                        }
                        if (t.classList.contains('mi-compact-dep-filter')) {
                            miApplyCompactDepFilter(t);
                            return;
                        }
                        if (
                            t.classList.contains('mi-part-slot-select') &&
                            t.multiple &&
                            t.closest('.mi-slot-picker-slot')
                        ) {
                            return;
                        }
                        if (!t.classList.contains('mi-part-slot-select') &&
                            !t.classList.contains('mi-part-slot-line-select') &&
                            !t.classList.contains('mi-compact-line-qty') &&
                            !t.classList.contains('mi-picked-line-qty') &&
                            !t.classList.contains('mi-cross-line-qty')) {
                            return;
                        }
                        if (t.classList.contains('mi-slot-inv-comp-only')) {
                            miSyncInvCompSelectRarityColor(t);
                        }
                        window.__lbCtx = window.legitBuilderInstances && window.legitBuilderInstances[1];
                        const rootItem = miClosestRootItem(t);
                        syncMiPartLinesFromRootItem(rootItem);
                        updateSelectedPartsOutput();
                        updateFinalOutput();
                    });
                    miTab.addEventListener('toggle', function (e) {
                        const det = e.target;
                        if (!det || !det.classList) return;
                        if (det.classList.contains('mi-cross-picker-details')) {
                            if (!det.open) return;
                            const crossHostLegacy = det.querySelector('.mi-cross-picker-host') || det;
                            if (crossHostLegacy.dataset.miCatalogInit === '1') return;
                            crossHostLegacy.dataset.miCatalogInit = '1';
                            populateMiCrossTypeSelect(crossHostLegacy);
                            return;
                        }
                        if (det.classList.contains('mi-slot-picker-details')) {
                            if (!det.open) return;
                            if (det.dataset.miSlotInit === '1') return;
                            det.dataset.miSlotInit = '1';
                            const slot = det.closest('.mi-compact-slot');
                            if (slot) miSlotPopulateCandidateList(slot);
                        }
                    }, true);
                    miTab.addEventListener('input', function (e) {
                        const t = e.target;
                        if (!t || !t.classList) return;
                        if (!t.classList.contains('mi-cross-search') && !t.classList.contains('mi-slot-search')) return;
                        const crossHostInp =
                            t.closest('.mi-cross-picker-host') || t.closest('.mi-cross-root-slot');
                        const slotPickInp = t.closest('.mi-compact-slot.mi-slot-picker-slot');
                        if (!crossHostInp && !slotPickInp) return;
                        clearTimeout(window.__miPickerSearchDebounce);
                        window.__miPickerSearchDebounce = setTimeout(function () {
                            if (t.classList.contains('mi-cross-search') && crossHostInp) {
                                miCrossRefreshCandidateList(crossHostInp);
                            } else if (
                                t.classList.contains('mi-slot-search') &&
                                slotPickInp &&
                                !t.closest('.mi-cross-root-slot')
                            ) {
                                miSlotPopulateCandidateList(slotPickInp);
                            }
                        }, 120);
                    });
                    miTab.addEventListener(
                        'mousedown',
                        function (e) {
                            if (e.button !== 0) return;
                            if (e.target && e.target.closest && e.target.closest('select.mi-slot-native-cand-select')) {
                                return;
                            }
                            const candRow = e.target && e.target.closest && e.target.closest('.mi-cand-row');
                            if (!candRow || !candRow.closest('.mi-passive-candidate-list')) return;
                            if (e.target.closest('a, button, textarea, select')) return;
                            if (e.target.closest('.mi-slot-select-all-wrap, .mi-slot-all-mfr-wrap')) return;
                            if (
                                e.target.closest(
                                    'input.mi-slot-select-all, input.mi-slot-search, input.mi-slot-bulk-qty, input.mi-slot-all-manufacturers'
                                )
                            ) {
                                return;
                            }
                            if (e.target.closest('.mi-passive-cand-more')) return;
                            const listEl = candRow.closest('.mi-passive-candidate-list');
                            if (!listEl) return;
                            e.preventDefault();
                            e.stopPropagation();
                            const rows = Array.from(listEl.querySelectorAll('.mi-cand-row'));
                            const idx = rows.indexOf(candRow);
                            if (idx < 0) return;
                            const slotForPick = listEl.closest('.mi-compact-slot');
                            const invCompSingle =
                                slotForPick && slotForPick.getAttribute('data-table-name') === 'inv_comp';
                            const touchLike =
                                !!(e.sourceCapabilities && e.sourceCapabilities.firesTouchEvents) ||
                                !!(
                                    window.matchMedia &&
                                    window.matchMedia('(hover: none), (pointer: coarse)').matches
                                );
                            const multi = invCompSingle ? false : e.ctrlKey || e.metaKey || touchLike;
                            const shift = invCompSingle ? false : e.shiftKey;
                            const selBg = '#2a3f5f';
                            const selOutline = '1px solid #5c9bd1';
                            const clearAll = () => {
                                rows.forEach(el => {
                                    el.classList.remove('mi-cand-selected');
                                    el.setAttribute('aria-selected', 'false');
                                    el.style.background = '';
                                    el.style.outline = '';
                                });
                            };
                            const selectOne = el => {
                                el.classList.add('mi-cand-selected');
                                el.setAttribute('aria-selected', 'true');
                                el.style.background = selBg;
                                el.style.outline = selOutline;
                            };
                            if (shift && listEl._miPassiveAnchorIdx != null && listEl._miPassiveAnchorIdx >= 0) {
                                const a = Math.min(listEl._miPassiveAnchorIdx, idx);
                                const b = Math.max(listEl._miPassiveAnchorIdx, idx);
                                clearAll();
                                for (let j = a; j <= b; j++) selectOne(rows[j]);
                            } else if (!multi) {
                                clearAll();
                                selectOne(candRow);
                                listEl._miPassiveAnchorIdx = idx;
                            } else {
                                candRow.classList.toggle('mi-cand-selected');
                                if (candRow.classList.contains('mi-cand-selected')) {
                                    candRow.setAttribute('aria-selected', 'true');
                                    candRow.style.background = selBg;
                                    candRow.style.outline = selOutline;
                                } else {
                                    candRow.setAttribute('aria-selected', 'false');
                                    candRow.style.background = '';
                                    candRow.style.outline = '';
                                }
                                listEl._miPassiveAnchorIdx = idx;
                            }
                            const slot = listEl.closest('.mi-compact-slot');
                            const sa = slot && slot.querySelector('.mi-slot-select-all');
                            if (sa) {
                                const nSel = listEl.querySelectorAll('.mi-cand-row.mi-cand-selected').length;
                                sa.checked = nSel > 0 && nSel === rows.length;
                                sa.indeterminate = nSel > 0 && nSel < rows.length;
                            }
                            if (slot) {
                                miUpdateSlotCandDropdownLabel(slot);
                                const ddMd = slot.querySelector('.mi-slot-cand-dropdown.mi-slot-cand-dropdown--open');
                                if (ddMd) {
                                    miPositionCandDropdownPanel(ddMd);
                                }
                            }
                        },
                        true
                    );
                    miTab.addEventListener('click', function (e) {
                        const mdTrig =
                            e.target && e.target.closest && e.target.closest('.mi-part-slot-multidrop-trigger');
                        if (mdTrig && miTab.contains(mdTrig)) {
                            e.preventDefault();
                            miTogglePartSlotMultidrop(mdTrig.closest('.mi-part-slot-multidrop'));
                            return;
                        }
                        const mdOpt = e.target && e.target.closest && e.target.closest('.mi-part-slot-multidrop-opt');
                        if (mdOpt && miTab.contains(mdOpt)) {
                            const wrap = mdOpt.closest('.mi-part-slot-multidrop');
                            if (wrap) {
                                requestAnimationFrame(function () {
                                    miPartSlotMultidropSyncSelectFromList(wrap);
                                });
                            }
                            return;
                        }
                        const candTrig =
                            e.target && e.target.closest && e.target.closest('.mi-slot-cand-dropdown-trigger');
                        if (candTrig && miTab.contains(candTrig)) {
                            e.preventDefault();
                            const wrap = candTrig.closest('.mi-slot-cand-dropdown');
                            miToggleCandDropdown(wrap);
                            return;
                        }
                        const expandAll =
                            e.target && e.target.closest && e.target.closest('.mi-compact-expand-all');
                        if (expandAll) {
                            e.preventDefault();
                            const shell = expandAll.closest('.mi-compact-dep-shell');
                            const tables = shell && shell.querySelector('.mi-compact-dep-tables');
                            if (tables) {
                                tables.querySelectorAll('details.mi-compact-slot-card').forEach(function (c) {
                                    c.open = true;
                                });
                            }
                            return;
                        }
                        const collapseAll =
                            e.target && e.target.closest && e.target.closest('.mi-compact-collapse-all');
                        if (collapseAll) {
                            e.preventDefault();
                            const shell = collapseAll.closest('.mi-compact-dep-shell');
                            const tables = shell && shell.querySelector('.mi-compact-dep-tables');
                            if (tables) {
                                tables.querySelectorAll('details.mi-compact-slot-card').forEach(function (c) {
                                    c.open = false;
                                });
                            }
                            return;
                        }
                        const addCatBtn =
                            e.target && e.target.closest && e.target.closest('.mi-compact-add-category-btn');
                        if (addCatBtn) {
                            e.preventDefault();
                            const shell = addCatBtn.closest('.mi-compact-dep-shell');
                            const rootItem = miClosestRootItem(addCatBtn);
                            const sel = shell && shell.querySelector('select.mi-compact-add-category-select');
                            const rootKey =
                                rootItem && rootItem.getAttribute('data-root-key')
                                    ? String(rootItem.getAttribute('data-root-key')).trim()
                                    : '';
                            const val = sel && String(sel.value || '').trim();
                            if (!shell || !rootItem || !rootKey || !val) return;
                            const inst = window.legitBuilderInstances && window.legitBuilderInstances[1];
                            if (!inst || !inst.miModdedExtraPartSlots) return;
                            const n = normalizeDepTableName(val);
                            if (!n || n === 'inv_comp') return;
                            window.__lbCtx = inst;
                            syncMiPartLinesFromRootItem(rootItem);
                            let arr = inst.miModdedExtraPartSlots.get(rootKey) || [];
                            if (arr.some(function (x) {
                                return normalizeDepTableName(String(x || '')) === n;
                            })) {
                                return;
                            }
                            arr = arr.concat([val]);
                            inst.miModdedExtraPartSlots.set(rootKey, arr);
                            miReplaceModdedRootItemHtml(rootKey, rootItem);
                            return;
                        }
                        const rmExtraBtn =
                            e.target && e.target.closest && e.target.closest('.mi-compact-remove-extra-slot');
                        if (rmExtraBtn) {
                            e.preventDefault();
                            const card = rmExtraBtn.closest('.mi-part-slot-card');
                            const rootItem = miClosestRootItem(rmExtraBtn);
                            const rootKey =
                                rootItem && rootItem.getAttribute('data-root-key')
                                    ? String(rootItem.getAttribute('data-root-key')).trim()
                                    : '';
                            const tn = card && card.getAttribute('data-mi-dep-filter');
                            const inst = window.legitBuilderInstances && window.legitBuilderInstances[1];
                            if (!inst || !rootItem || !rootKey || !tn || !inst.miModdedExtraPartSlots) return;
                            window.__lbCtx = inst;
                            syncMiPartLinesFromRootItem(rootItem);
                            const tnNorm = normalizeDepTableName(tn);
                            const lines = (inst.miPartLines.get(rootKey) || []).filter(function (l) {
                                return normalizeDepTableName(l.tableName || '') !== tnNorm;
                            });
                            inst.miPartLines.set(rootKey, lines);
                            let arr = inst.miModdedExtraPartSlots.get(rootKey) || [];
                            arr = arr.filter(function (x) {
                                return normalizeDepTableName(String(x || '')) !== tnNorm;
                            });
                            if (arr.length) inst.miModdedExtraPartSlots.set(rootKey, arr);
                            else inst.miModdedExtraPartSlots.delete(rootKey);
                            miReplaceModdedRootItemHtml(rootKey, rootItem);
                            updateSelectedPartsOutput();
                            updateFinalOutput();
                            return;
                        }
                        const clrCat = e.target && e.target.closest && e.target.closest('.mi-slot-clear-category');
                        if (clrCat) {
                            e.preventDefault();
                            const slot = clrCat.closest('.mi-compact-slot');
                            const rootItem = miClosestRootItem(clrCat);
                            if (!slot || !rootItem) return;
                            const rowsWrap = miResolvePartSlotPickedRowsWrap(slot);
                            const chipHost = miPickedRowsChipHost(rowsWrap);
                            if (chipHost) chipHost.innerHTML = '';
                            miRestorePartSlotEmptyState(slot);
                            window.__lbCtx = window.legitBuilderInstances && window.legitBuilderInstances[1];
                            syncMiPartLinesFromRootItem(rootItem);
                            miRefreshPartSlotPickedListUi(rootItem);
                            updateSelectedPartsOutput();
                            updateFinalOutput();
                            return;
                        }
                        const addPick = e.target && e.target.closest && e.target.closest('.mi-slot-add-selected');
                        if (addPick) {
                            e.preventDefault();
                            const slot = addPick.closest('.mi-compact-slot');
                            const rootItem = miClosestRootItem(addPick);
                            const rowsWrap = slot ? miEnsurePartSlotPickedRows(slot) : null;
                            const partSlotSel = slot && miResolvePartSlotSelect(slot);
                            const richPassive = slot && slot.querySelector('.mi-passive-candidate-list');
                            const list =
                                (slot && slot.querySelector('.mi-cross-candidate-list')) ||
                                (slot && slot.querySelector('.mi-slot-candidate-list'));
                            if (!slot || !rowsWrap || !rootItem) return;
                            if (!partSlotSel && !richPassive && !list) return;
                            const maxR =
                                parseInt(slot.getAttribute('data-mi-max-rows') || String(MI_MODDED_MAX_PICKED_LINES), 10) ||
                                MI_MODDED_MAX_PICKED_LINES;
                            const tableName = slot.getAttribute('data-table-name');
                            const isInvComp = tableName === 'inv_comp';
                            const isInvCompList =
                                !!(list && list.tagName === 'SELECT' && list.classList.contains('mi-slot-inv-comp-only'));
                            const actionScope = addPick.closest('.mi-slot-picker-actions');
                            const qtyIn =
                                (actionScope && actionScope.querySelector('.mi-slot-bulk-qty')) ||
                                slot.querySelector('.mi-slot-bulk-qty');
                            let bulkQ = qtyIn ? parseInt(qtyIn.value, 10) : 1;
                            if (isNaN(bulkQ) || bulkQ < 1) bulkQ = 1;
                            if (bulkQ > 99) bulkQ = 99;
                            if (isInvComp || isInvCompList) bulkQ = 1;
                            let toAdd = [];
                            if (partSlotSel) {
                                toAdd = miGetPartSlotSelectValues(partSlotSel);
                                if ((isInvComp || isInvCompList) && toAdd.length > 1) {
                                    toAdd = [toAdd[0]];
                                }
                            } else if (richPassive) {
                                toAdd = Array.from(richPassive.querySelectorAll('.mi-cand-row.mi-cand-selected'))
                                    .map(row => row.getAttribute('data-ref'))
                                    .filter(Boolean);
                                if (isInvComp && toAdd.length > 1) {
                                    toAdd = [toAdd[0]];
                                }
                            } else if (list && list.tagName === 'SELECT') {
                                if (isInvComp || isInvCompList) {
                                    const o = list.options[list.selectedIndex];
                                    if (o && o.value) toAdd = [o.value];
                                } else {
                                    toAdd = Array.from(list.selectedOptions || [])
                                        .map(o => o.value)
                                        .filter(Boolean);
                                }
                            }
                            if (!toAdd.length) return;
                            const chipHost = miPickedRowsChipHost(rowsWrap);
                            const lineRoot = chipHost || rowsWrap;
                            if (isInvComp || isInvCompList) {
                                lineRoot.innerHTML = '';
                            }
                            for (let i = 0; i < toAdd.length; i++) {
                                const ref = toAdd[i];
                                const rows = lineRoot.querySelectorAll('.mi-slot-picked-line');
                                const last = rows[rows.length - 1];
                                const willMerge = last && last.getAttribute('data-line-ref') === ref;
                                if (!willMerge && rows.length >= maxR) break;
                                miSlotAppendOrMergeLastRow(rowsWrap, ref, bulkQ);
                            }
                            window.__lbCtx = window.legitBuilderInstances && window.legitBuilderInstances[1];
                            syncMiPartLinesFromRootItem(rootItem);
                            miRefreshPartSlotPickedListUi(rootItem);
                            updateSelectedPartsOutput();
                            updateFinalOutput();
                            if (partSlotSel) {
                                miClearPartSlotSelect(partSlotSel);
                            }
                            const ddWrap = slot.querySelector('.mi-slot-cand-dropdown');
                            if (ddWrap && ddWrap.classList.contains('mi-slot-cand-dropdown--open')) {
                                miCloseCandDropdown(ddWrap);
                            }
                            miUpdateSlotCandDropdownLabel(slot);
                            return;
                        }
                        const rmPick = e.target && e.target.closest && e.target.closest('.mi-picked-line-remove');
                        if (rmPick) {
                            e.preventDefault();
                            window.__lbCtx = window.legitBuilderInstances && window.legitBuilderInstances[1];
                            const line = rmPick.closest('.mi-slot-picked-line, .mi-cross-line');
                            if (line) line.remove();
                            syncAllMiPartLinesFromModdedDom();
                            updateSelectedPartsOutput();
                            updateFinalOutput();
                            return;
                        }
                        const movePick =
                            e.target && e.target.closest && e.target.closest('.mi-picked-line-move-up, .mi-picked-line-move-down');
                        if (movePick) {
                            e.preventDefault();
                            e.stopPropagation();
                            const line = movePick.closest('.mi-slot-picked-line');
                            const rootItem = miClosestRootItem(movePick);
                            const parent = line && line.parentElement;
                            if (!line || !parent || !rootItem) return;
                            if (movePick.classList.contains('mi-picked-line-move-up')) {
                                const prev = line.previousElementSibling;
                                if (prev && prev.classList.contains('mi-slot-picked-line')) {
                                    parent.insertBefore(line, prev);
                                }
                            } else {
                                const next = line.nextElementSibling;
                                if (next && next.classList.contains('mi-slot-picked-line')) {
                                    parent.insertBefore(line, next.nextElementSibling);
                                }
                            }
                            window.__lbCtx = window.legitBuilderInstances && window.legitBuilderInstances[1];
                            syncMiPartLinesFromRootItem(rootItem);
                            updateSelectedPartsOutput();
                            updateFinalOutput();
                            return;
                        }
                        const addBtn = e.target && e.target.closest && e.target.closest('.mi-compact-line-add');
                        if (addBtn) {
                            e.preventDefault();
                            const slot = addBtn.closest('.mi-compact-slot');
                            if (!slot) return;
                            const rowsWrap = slot.querySelector('.mi-compact-slot-rows');
                            const maxR = parseInt(slot.getAttribute('data-mi-max-rows') || '24', 10) || 24;
                            if (!rowsWrap || rowsWrap.children.length >= maxR) return;
                            const firstLine = rowsWrap.querySelector('.mi-compact-line');
                            if (!firstLine) return;
                            const newLine = firstLine.cloneNode(true);
                            const ns = newLine.querySelector('.mi-part-slot-line-select');
                            const nq = newLine.querySelector('.mi-compact-line-qty');
                            if (ns) ns.value = '';
                            if (nq) nq.value = '1';
                            rowsWrap.appendChild(newLine);
                            window.__lbCtx = window.legitBuilderInstances && window.legitBuilderInstances[1];
                            syncMiPartLinesFromRootItem(miClosestRootItem(addBtn));
                            updateSelectedPartsOutput();
                            updateFinalOutput();
                            return;
                        }
                        const rm = e.target && e.target.closest && e.target.closest('.mi-compact-line-remove');
                        if (rm) {
                            e.preventDefault();
                            const line = rm.closest('.mi-compact-line');
                            const rowsWrap = line && line.parentElement;
                            if (!line || !rowsWrap) return;
                            if (rowsWrap.children.length <= 1) {
                                const ns = line.querySelector('.mi-part-slot-line-select');
                                const nq = line.querySelector('.mi-compact-line-qty');
                                if (ns) ns.value = '';
                                if (nq) nq.value = '1';
                            } else {
                                line.remove();
                            }
                            window.__lbCtx = window.legitBuilderInstances && window.legitBuilderInstances[1];
                            syncMiPartLinesFromRootItem(miClosestRootItem(rm));
                            updateSelectedPartsOutput();
                            updateFinalOutput();
                        }
                    });
                    if (!window.__miSerialOrderDragBound) {
                        window.__miSerialOrderDragBound = true;
                        function miPickedDropContainer(wrap) {
                            return miPickedRowsChipHost(wrap) || wrap;
                        }
                        function miFindPickedDropTarget(container, dragEl, clientX, clientY) {
                            if (!container) return null;
                            const rows = Array.from(container.querySelectorAll(':scope > .mi-slot-picked-line')).filter(
                                function (row) {
                                    return row !== dragEl;
                                }
                            );
                            if (!rows.length) return null;
                            let best = null;
                            let bestScore = Infinity;
                            rows.forEach(function (row) {
                                const r = row.getBoundingClientRect();
                                if (!r.width || !r.height) return;
                                const dx = clientX < r.left ? r.left - clientX : clientX > r.right ? clientX - r.right : 0;
                                const dy = clientY < r.top ? r.top - clientY : clientY > r.bottom ? clientY - r.bottom : 0;
                                const score = dx * dx + dy * dy;
                                if (score < bestScore) {
                                    bestScore = score;
                                    best = { row: row, rect: r };
                                }
                            });
                            if (!best) return null;
                            const sameVisualRow = clientY >= best.rect.top && clientY <= best.rect.bottom;
                            const before = sameVisualRow
                                ? clientX < best.rect.left + best.rect.width / 2
                                : clientY < best.rect.top + best.rect.height / 2;
                            return { row: best.row, before: before };
                        }
                        miTab.addEventListener('dragstart', function (e) {
                            const handle = e.target.closest('.mi-picked-line-drag-handle');
                            if (handle) {
                                const row = handle.closest('.mi-slot-picked-line');
                                if (!row || !miTab.contains(row)) return;
                                window.__miSerialOrderDragEl = null;
                                e.dataTransfer.effectAllowed = 'move';
                                e.dataTransfer.setData('text/plain', 'mi-picked-row');
                                window.__miPickedRowDragEl = row;
                                row.style.opacity = '0.65';
                                return;
                            }
                            const it = e.target.closest('.mi-serial-order-item');
                            if (!it || !miTab.contains(it)) return;
                            window.__miPickedRowDragEl = null;
                            e.dataTransfer.effectAllowed = 'move';
                            e.dataTransfer.setData('text/plain', 'mi-serial-order');
                            window.__miSerialOrderDragEl = it;
                            it.style.opacity = '0.5';
                        });
                        miTab.addEventListener('dragend', function (e) {
                            const row = e.target.closest('.mi-slot-picked-line');
                            if (row && miTab.contains(row)) row.style.opacity = '1';
                            const it = e.target.closest('.mi-serial-order-item');
                            if (it && miTab.contains(it)) it.style.opacity = '1';
                            window.__miSerialOrderDragEl = null;
                            window.__miPickedRowDragEl = null;
                        });
                        miTab.addEventListener('dragover', function (e) {
                            if (window.__miPickedRowDragEl) {
                                const wrap = e.target.closest('.mi-slot-picked-rows, .mi-cross-slot-rows');
                                const dragEl = window.__miPickedRowDragEl;
                                if (!wrap || !dragEl || !miTab.contains(dragEl)) return;
                                const rDrag = miClosestRootItem(dragEl);
                                const rDrop = miClosestRootItem(wrap);
                                if (!rDrag || rDrag !== rDrop) return;
                                e.preventDefault();
                                e.dataTransfer.dropEffect = 'move';
                                return;
                            }
                            const list = e.target.closest('.mi-serial-order-list');
                            if (!list || !window.__miSerialOrderDragEl || !list.contains(window.__miSerialOrderDragEl)) return;
                            e.preventDefault();
                            e.dataTransfer.dropEffect = 'move';
                        });
                        miTab.addEventListener('drop', function (e) {
                            if (window.__miPickedRowDragEl) {
                                const wrap = e.target.closest('.mi-slot-picked-rows, .mi-cross-slot-rows');
                                const dragEl = window.__miPickedRowDragEl;
                                if (!wrap || !dragEl) return;
                                e.preventDefault();
                                const rootItem = miClosestRootItem(dragEl);
                                if (!rootItem || miClosestRootItem(wrap) !== rootItem) return;
                                const dropContainer = miPickedDropContainer(wrap);
                                const target = miFindPickedDropTarget(dropContainer, dragEl, e.clientX, e.clientY);
                                if (target && target.row && dropContainer.contains(target.row)) {
                                    if (target.before) {
                                        dropContainer.insertBefore(dragEl, target.row);
                                    } else {
                                        dropContainer.insertBefore(dragEl, target.row.nextSibling);
                                    }
                                } else {
                                    dropContainer.appendChild(dragEl);
                                }
                                window.__lbCtx = window.legitBuilderInstances && window.legitBuilderInstances[1];
                                syncMiPartLinesFromRootItem(rootItem);
                                updateSelectedPartsOutput();
                                updateFinalOutput();
                                window.__miPickedRowDragEl = null;
                                return;
                            }
                            const list = e.target.closest('.mi-serial-order-list');
                            const dragEl = window.__miSerialOrderDragEl;
                            if (!list || !dragEl || !list.contains(dragEl)) return;
                            e.preventDefault();
                            const target = e.target.closest('.mi-serial-order-item');
                            if (target && target !== dragEl) {
                                const rect = target.getBoundingClientRect();
                                const before = e.clientY < rect.top + rect.height / 2;
                                if (before) {
                                    list.insertBefore(dragEl, target);
                                } else {
                                    list.insertBefore(dragEl, target.nextSibling);
                                }
                            } else if (!target) {
                                list.appendChild(dragEl);
                            }
                            applyMiSerialOrderFromList(list);
                        });
                    }
                    if (!window.__miCandDropdownOutsideBound) {
                        window.__miCandDropdownOutsideBound = true;
                        document.addEventListener('click', function (e) {
                            const t = e.target;
                            if (!t || !t.closest) return;
                            if (t.closest('#item-editor-tab .mi-slot-cand-dropdown')) return;
                            if (t.closest('#item-editor-tab .mi-part-slot-multidrop')) return;
                            document.querySelectorAll('#item-editor-tab .mi-slot-cand-dropdown--open').forEach(miCloseCandDropdown);
                            document
                                .querySelectorAll('#item-editor-tab .mi-part-slot-multidrop--open')
                                .forEach(miClosePartSlotMultidrop);
                        });
                        miTab.addEventListener(
                            'keydown',
                            function (e) {
                                if (e.key !== 'Escape') return;
                                const openWrap =
                                    e.target.closest &&
                                    e.target.closest('#item-editor-tab .mi-slot-cand-dropdown--open');
                                if (openWrap && miTab.contains(openWrap)) {
                                    miCloseCandDropdown(openWrap);
                                    e.preventDefault();
                                    const tr = openWrap.querySelector('.mi-slot-cand-dropdown-trigger');
                                    if (tr) tr.focus();
                                    return;
                                }
                                const openMd =
                                    e.target.closest &&
                                    e.target.closest('#item-editor-tab .mi-part-slot-multidrop--open');
                                if (!openMd || !miTab.contains(openMd)) return;
                                miClosePartSlotMultidrop(openMd);
                                e.preventDefault();
                                const mdTr = openMd.querySelector('.mi-part-slot-multidrop-trigger');
                                if (mdTr) mdTr.focus();
                            },
                            true
                        );
                        window.addEventListener('scroll', miScheduleCandDropdownRelayout, true);
                        window.addEventListener('resize', miScheduleCandDropdownRelayout);
                        if (!window.__miPartSlotMultidropRelayoutBound) {
                            window.__miPartSlotMultidropRelayoutBound = true;
                            window.addEventListener(
                                'scroll',
                                function () {
                                    document
                                        .querySelectorAll('#item-editor-tab .mi-part-slot-multidrop--open')
                                        .forEach(miPositionPartSlotMultidropPanel);
                                },
                                true
                            );
                            window.addEventListener('resize', function () {
                                document
                                    .querySelectorAll('#item-editor-tab .mi-part-slot-multidrop--open')
                                    .forEach(miPositionPartSlotMultidropPanel);
                            });
                        }
                    }
                }
            }
        }

        const MI_SIMPLE_BUILDER_PLACEHOLDER_HTML =
            'Select <strong style="color: var(--text-primary);">type</strong> and <strong style="color: var(--text-primary);">manufacturer</strong> above to open part slots.';

        function updateMiSimpleBuilderPlaceholderVisibility() {
            const ph = document.getElementById('mi_simpleBuilderPlaceholder');
            const host = document.getElementById('mi_simpleBuilderHost');
            const typeSel = document.getElementById('mi_simpleTypeSelect');
            const manSel = document.getElementById('mi_simpleManufacturerSelect');
            if (!ph) {
                return;
            }
            const hasType = !!(typeSel && String(typeSel.value || '').trim());
            const hasMfr = !!(manSel && String(manSel.value || '').trim());
            const hostVisible =
                !!(host && host.style.display !== 'none' && String(host.innerHTML || '').trim());
            if (hostVisible || (hasType && hasMfr)) {
                ph.style.display = 'none';
                return;
            }
            ph.style.display = 'block';
            if (!String(ph.textContent || '').trim() || ph.dataset.miDefaultHint === '1') {
                ph.innerHTML = MI_SIMPLE_BUILDER_PLACEHOLDER_HTML;
                ph.dataset.miDefaultHint = '1';
            }
        }

        function computeModdedSimpleBucketsFromFiltered(filtered) {
            const classModItems = [];
            const universalItems = [];
            const regularItems = [];
            filtered.forEach(function (root) {
                const key = (root.key || '').toLowerCase();
                const category = categorizeRoot(root);
                if (category === 'Hover Drive') {
                    return;
                }
                if (category === 'Class Mod' && key.startsWith('classmod_')) {
                    classModItems.push(root);
                } else if (category === 'Universal') {
                    universalItems.push(root);
                } else if (
                    (key.startsWith('currency_') ||
                        key.startsWith('ammo') ||
                        key.startsWith('ammo_') ||
                        key.endsWith('_pickup')) &&
                    key !== 'mission_pickup' &&
                    !key.includes('missiontemp')
                ) {
                    return;
                } else {
                    regularItems.push(root);
                }
            });
            const grenadeItems = [];
            const heavyWeaponItems = [];
            const repkitItems = [];
            const enhancementItems = [];
            const weaponItems = [];
            const shieldItems = [];
            const allItemsToCheck = regularItems.concat(universalItems);
            allItemsToCheck.forEach(function (root) {
                const key = (root.key || '').toLowerCase();
                const inv = (root.inv || '').toLowerCase();
                const basetype = (root.basetype || '').toLowerCase();
                const category = categorizeRoot(root);
                if (
                    (key.includes('heavy_weapon_gadget') ||
                        inv.includes('heavy_weapon_gadget') ||
                        basetype.includes('heavy_weapon_gadget')) &&
                    !(key === 'heavy_weapon_gadget' || inv === 'heavy_weapon_gadget')
                ) {
                    heavyWeaponItems.push(root);
                } else if (
                    key.includes('grenade_gadget') ||
                    inv.includes('grenade_gadget') ||
                    basetype.includes('grenade_gadget')
                ) {
                    grenadeItems.push(root);
                } else if (category === 'Shield') {
                    shieldItems.push(root);
                } else if (category === 'Repkit') {
                    repkitItems.push(root);
                } else if (category === 'Enhancement') {
                    enhancementItems.push(root);
                } else if (
                    category === 'Weapon' &&
                    !key.includes('heavy_weapon_gadget') &&
                    !inv.includes('heavy_weapon_gadget') &&
                    !key.includes('grenade_gadget') &&
                    !inv.includes('grenade_gadget')
                ) {
                    weaponItems.push(root);
                }
            });
            function moddedSimpleWeaponSubtype(root) {
                const key = (root.key || '').toLowerCase();
                const inv = (root.inv || '').toLowerCase();
                const basetype = (root.basetype || '').toLowerCase();
                const itembasename = (root.itembasename || '').toLowerCase();
                const name = (root.name || '').toLowerCase();
                if (basetype.includes('weapon_sr') || basetype.includes('sniper') || inv.includes('sr')) {
                    return 'Sniper';
                }
                if (basetype.includes('weapon_sg') || basetype.includes('shotgun') || inv.includes('sg')) {
                    return 'Shotgun';
                }
                if (
                    basetype.includes('weapon_smg') ||
                    basetype.includes('submachine') ||
                    inv.includes('smg') ||
                    key.includes('smg') ||
                    itembasename.includes('smg') ||
                    name.includes('smg')
                ) {
                    return 'SMG';
                }
                if (basetype.includes('weapon_ps') || basetype.includes('pistol') || inv.includes('ps')) {
                    return 'Pistol';
                }
                if (basetype.includes('weapon_ar') || basetype.includes('assault') || inv.includes('ar')) {
                    return 'Assault Rifle';
                }
                return 'Other';
            }
            const weaponsByType = {
                SMG: [],
                'Assault Rifle': [],
                Pistol: [],
                Sniper: [],
                Shotgun: []
            };
            weaponItems.forEach(function (root) {
                const manufacturer = extractManufacturer(root);
                if (manufacturer === 'Unknown') {
                    return;
                }
                const wt = moddedSimpleWeaponSubtype(root);
                if (weaponsByType[wt]) {
                    weaponsByType[wt].push(root);
                }
            });
            return {
                classModItems: classModItems,
                shieldItems: shieldItems,
                grenadeItems: grenadeItems,
                heavyWeaponItems: heavyWeaponItems,
                repkitItems: repkitItems,
                enhancementItems: enhancementItems,
                weaponsByType: weaponsByType
            };
        }

        function getModdedSimpleModeBuckets() {
            if (!data || !data.rootDefinitions) {
                return null;
            }
            const typeFilterEl = document.getElementById('mi_typeFilter');
            const typeFilter = typeFilterEl ? typeFilterEl.value : '';
            let filtered = data.rootDefinitions;
            if (typeFilter) {
                filtered = filtered.filter(function (root) {
                    return root.basetype === typeFilter;
                });
            }
            filtered = filtered.filter(function (root) {
                if (miIsTurretOrTerminalGadgetRoot(root)) {
                    return false;
                }
                const key = (root.key || '').toLowerCase();
                const inv = (root.inv || '').toLowerCase();
                return (
                    !key.includes('splicearmlauncher') &&
                    !key.includes('splicearm_launcher') &&
                    !key.includes('invdef') &&
                    !(key === 'heavy_weapon_gadget' || inv === 'heavy_weapon_gadget') &&
                    !(key === 'grenade_gadget' || inv === 'grenade_gadget') &&
                    !inv.includes('splicearmlauncher') &&
                    !inv.includes('splicearm_launcher') &&
                    !inv.includes('invdef')
                );
            });
            return computeModdedSimpleBucketsFromFiltered(filtered);
        }

        function getModdedSimpleRootsForTypeKey(buckets, typeKey) {
            if (!buckets || !typeKey) {
                return [];
            }
            switch (typeKey) {
                case 'classmod':
                    return buckets.classModItems || [];
                case 'shield':
                    return buckets.shieldItems || [];
                case 'grenades':
                    return buckets.grenadeItems || [];
                case 'heavy_weapon':
                    return buckets.heavyWeaponItems || [];
                case 'repkit':
                    return buckets.repkitItems || [];
                case 'enhancement':
                    return buckets.enhancementItems || [];
                case 'smg':
                    return (buckets.weaponsByType && buckets.weaponsByType['SMG']) || [];
                case 'assault_rifle':
                    return (buckets.weaponsByType && buckets.weaponsByType['Assault Rifle']) || [];
                case 'pistol':
                    return (buckets.weaponsByType && buckets.weaponsByType['Pistol']) || [];
                case 'sniper_rifle':
                    return (buckets.weaponsByType && buckets.weaponsByType['Sniper']) || [];
                case 'shotgun':
                    return (buckets.weaponsByType && buckets.weaponsByType['Shotgun']) || [];
                default:
                    return [];
            }
        }

        /** Human label from class mod root key: classmod_dark_siren → "Dark Siren". */
        function extractClassModClassDisplayFromRoot(root) {
            const k = String((root && root.key) || '').trim();
            const low = k.toLowerCase();
            if (!low.startsWith('classmod_')) {
                return '';
            }
            const suffix = k.slice('classmod_'.length);
            if (!suffix) {
                return '';
            }
            return suffix
                .split('_')
                .map(function (p) {
                    return p.length ? p.charAt(0).toUpperCase() + p.slice(1).toLowerCase() : '';
                })
                .filter(Boolean)
                .join(' ');
        }

        /** Second dropdown value: class names for class mods, manufacturer otherwise. */
        function extractModdedSimpleSecondaryValue(root, typeKey) {
            if (!root) {
                return '';
            }
            if (typeKey === 'classmod') {
                return extractClassModClassDisplayFromRoot(root) || 'Unknown';
            }
            return extractManufacturer(root);
        }

        function miSimpleSecondaryPlaceholderText(typeKey) {
            return typeKey === 'classmod' ? '— Class —' : '— Manufacturer —';
        }

        function updateMiSimpleSecondaryLabel(typeKey) {
            const lab = document.getElementById('mi_simpleSecondaryLabel');
            if (!lab) {
                return;
            }
            lab.textContent = typeKey === 'classmod' ? 'Class' : 'Manufacturer';
        }

        function resolveModdedSimpleRoot(typeKey, secondaryDisplay, buckets) {
            if (!typeKey || !secondaryDisplay || !buckets) {
                return null;
            }
            const roots = getModdedSimpleRootsForTypeKey(buckets, typeKey);
            return (
                roots.find(function (r) {
                    return extractModdedSimpleSecondaryValue(r, typeKey) === secondaryDisplay;
                }) || null
            );
        }

        function getModdedSimpleTypeKeyForRoot(rootKey, buckets) {
            if (!rootKey || !buckets) {
                return '';
            }
            const rk = String(rootKey).trim();
            function has(list) {
                return list && list.some(function (r) {
                    return r && String(r.key || '') === rk;
                });
            }
            if (has(buckets.classModItems)) {
                return 'classmod';
            }
            if (has(buckets.shieldItems)) {
                return 'shield';
            }
            if (has(buckets.grenadeItems)) {
                return 'grenades';
            }
            if (has(buckets.heavyWeaponItems)) {
                return 'heavy_weapon';
            }
            if (has(buckets.repkitItems)) {
                return 'repkit';
            }
            if (has(buckets.enhancementItems)) {
                return 'enhancement';
            }
            const wbt = buckets.weaponsByType || {};
            if (has(wbt['SMG'])) {
                return 'smg';
            }
            if (has(wbt['Assault Rifle'])) {
                return 'assault_rifle';
            }
            if (has(wbt['Pistol'])) {
                return 'pistol';
            }
            if (has(wbt['Sniper'])) {
                return 'sniper_rifle';
            }
            if (has(wbt['Shotgun'])) {
                return 'shotgun';
            }
            return '';
        }

        function clearMiSimpleBuilderToPlaceholder() {
            if (typeof window.legitBuilderSetCtx === 'function') {
                window.legitBuilderSetCtx(1);
            }
            const host = document.getElementById('mi_simpleBuilderHost');
            const ph = document.getElementById('mi_simpleBuilderPlaceholder');
            const inst = window.legitBuilderInstances && window.legitBuilderInstances[1];
            if (inst) {
                if (inst.miPartLines) {
                    inst.miPartLines.clear();
                }
                if (inst.selectedParts) {
                    inst.selectedParts.clear();
                }
            }
            if (host) {
                host.innerHTML = '';
                host.style.display = 'none';
            }
            if (ph) {
                ph.dataset.miDefaultHint = '1';
                ph.innerHTML = MI_SIMPLE_BUILDER_PLACEHOLDER_HTML;
            }
            updateMiSimpleBuilderPlaceholderVisibility();
            updateSelectedPartsOutput();
            updateFinalOutput();
        }

        function showMiSimpleBuilderMessage(htmlOrText) {
            const host = document.getElementById('mi_simpleBuilderHost');
            const ph = document.getElementById('mi_simpleBuilderPlaceholder');
            if (host) {
                host.innerHTML = '';
                host.style.display = 'none';
            }
            if (ph) {
                ph.style.display = 'block';
                ph.dataset.miDefaultHint = '0';
                ph.innerHTML = htmlOrText;
            }
        }

        function renderMiSimpleSingleRoot(root) {
            if (!root) {
                return;
            }
            const inst = window.legitBuilderInstances && window.legitBuilderInstances[1];
            if (!inst) {
                return;
            }
            const rk = String(root.key || '').trim();
            let preservedLines = null;
            if (inst.miPartLines && rk && inst.miPartLines.has(rk)) {
                const cur = inst.miPartLines.get(rk);
                if (cur && cur.length) {
                    preservedLines = cur.map(function (l) {
                        if (!l) return null;
                        const o = {
                            tableName: l.tableName,
                            partKey: l.partKey,
                            qty: l.qty != null ? l.qty : 1
                        };
                        if (l._miOrder != null) o._miOrder = l._miOrder;
                        return o;
                    }).filter(Boolean);
                    if (!preservedLines.length) preservedLines = null;
                }
            }
            if (inst.miPartLines) {
                inst.miPartLines.clear();
            }
            if (inst.miModdedExtraPartSlots) {
                inst.miModdedExtraPartSlots.clear();
            }
            if (inst.selectedParts) {
                inst.selectedParts.clear();
            }
            if (preservedLines && preservedLines.length) {
                inst.miPartLines.set(rk, preservedLines);
            }
            const host = document.getElementById('mi_simpleBuilderHost');
            const ph = document.getElementById('mi_simpleBuilderPlaceholder');
            if (miShouldHideTurretTerminalForModded() && miIsTurretOrTerminalGadgetRoot(root)) {
                if (host) {
                    host.style.display = 'none';
                    host.innerHTML = '';
                }
                if (ph) {
                    ph.dataset.miDefaultHint = '0';
                    ph.style.display = 'block';
                    ph.innerHTML =
                        'Turret and Terminal items are available in <strong style="color: var(--text-primary);">Advanced Mode</strong> only.';
                }
                return;
            }
            if (ph) {
                ph.style.display = 'none';
                ph.dataset.miDefaultHint = '1';
                ph.innerHTML = MI_SIMPLE_BUILDER_PLACEHOLDER_HTML;
            }
            if (host) {
                host.style.display = 'block';
                host.innerHTML = createRootItemHTML(root, 'mi-simple-0', { useCompactDepTables: true });
                miInitOpenModdedPickers(host);
                expandMiSimpleRootItem(host);
                requestAnimationFrame(function () {
                    miInitCrossManufacturerPickers(host);
                });
                const miRc = document.getElementById('mi_rootsContent');
                if (miRc) {
                    wireMiRootsContentRootItemExpand(miRc);
                }
            }
            window.__lbCtx = inst;
            const ri = host && host.querySelector('.root-item');
            if (ri) {
                const rootItem = miClosestRootItem(ri) || ri;
                if (preservedLines && preservedLines.length) {
                    inst.miPartLines.set(rk, preservedLines);
                    const set = new Set();
                    preservedLines.forEach(function (l) {
                        if (!l.partKey) return;
                        const pr = parseMiCrossPartRef(l.partKey);
                        set.add(pr ? pr.partKey : l.partKey);
                    });
                    inst.selectedParts.set(rk, set);
                    applyMiLinesToModdedRootDom(rk, preservedLines);
                    miRefreshPartSlotPickedListUi(rootItem);
                } else {
                    syncMiPartLinesFromRootItem(rootItem);
                }
            }
            updateSelectedPartsOutput();
            updateFinalOutput();
        }

        function applyMiSimpleBuilderFromRootKey(rootKey) {
            if (!rootKey || !data || !data.rootDefinitions) {
                return;
            }
            const rk = String(rootKey).trim();
            const root = data.rootDefinitions.find(function (r) {
                return r && String(r.key || '') === rk;
            });
            if (!root) {
                return;
            }
            const buckets = getModdedSimpleModeBuckets();
            if (!buckets) {
                return;
            }
            const tk = getModdedSimpleTypeKeyForRoot(rk, buckets);
            const typeSel = document.getElementById('mi_simpleTypeSelect');
            const manSel = document.getElementById('mi_simpleManufacturerSelect');
            if (!typeSel || !manSel) {
                return;
            }
            if (!tk) {
                clearMiSimpleBuilderToPlaceholder();
                return;
            }
            typeSel.value = tk;
            manSel.disabled = false;
            updateMiSimpleSecondaryLabel(tk);
            manSel.innerHTML = '<option value="">' + miSimpleSecondaryPlaceholderText(tk) + '</option>';
            const roots = getModdedSimpleRootsForTypeKey(buckets, tk);
            const mans = new Set();
            roots.forEach(function (r) {
                mans.add(extractModdedSimpleSecondaryValue(r, tk));
            });
            Array.from(mans)
                .sort(function (a, b) {
                    return String(a).localeCompare(String(b));
                })
                .forEach(function (m) {
                    const opt = document.createElement('option');
                    opt.value = m;
                    opt.textContent = m;
                    manSel.appendChild(opt);
                });
            const pick = extractModdedSimpleSecondaryValue(root, tk);
            manSel.value = pick;
            renderMiSimpleSingleRoot(root);
        }

        window.miFillParseExample = function miFillParseExample(idx) {
            const ta = document.getElementById('mi_parseSerialInput');
            if (!ta) {
                return;
            }
            const examples = [
                '254, 0, 1, 50| 2, 1|| {217} {221} {204} {172} {234:16} {234:85}|',
                '279, 0, 1, 50| 2, 1|| {12} {34} {56}|'
            ];
            ta.value = examples[idx != null ? idx : 0] || examples[0];
        };

        window.applyMiSimpleBuilderFromRootKey = applyMiSimpleBuilderFromRootKey;

        /** Modded simple #mi_rootsContent skips full displayRootDefinitions(), so root row expand/collapse must be wired here (and matches that handler). */
        function wireMiRootsContentRootItemExpand(container) {
            if (!container || container.id !== 'mi_rootsContent') {
                return;
            }
            if (container._rootItemExpandHandler) {
                container.removeEventListener('click', container._rootItemExpandHandler);
                container._rootItemExpandHandler = null;
            }
            container._rootItemExpandHandler = function (e) {
                const header = e.target.closest('.root-header-clickable');
                if (!header || !container.contains(header)) {
                    return;
                }
                e.stopPropagation();
                const rootItem = header.closest('.root-item');
                if (!rootItem || !container.contains(rootItem)) {
                    return;
                }
                const content = rootItem.querySelector('.root-content');
                if (content) {
                    rootItem.classList.toggle('expanded');
                    content.classList.toggle('collapsed');
                }
            };
            container.addEventListener('click', container._rootItemExpandHandler);
        }

        function expandMiSimpleRootItem(hostEl) {
            const rootItem = hostEl && hostEl.querySelector('.root-item');
            if (!rootItem) {
                return;
            }
            const content = rootItem.querySelector('.root-content');
            if (content) {
                rootItem.classList.add('expanded');
                content.classList.remove('collapsed');
            }
        }

        function displayModdedSimpleBuilderEnsureLayout(container) {
            if (!container || container.id !== 'mi_rootsContent') {
                return;
            }
            const existingHost = container.querySelector('#mi_simpleBuilderHost');
            if (!existingHost) {
                container.innerHTML =
                    '<div id="mi_simpleBuilderPlaceholder" class="simple-only mi-builder-placeholder-hint" style="display: none;" data-mi-default-hint="1">' +
                    MI_SIMPLE_BUILDER_PLACEHOLDER_HTML +
                    '</div>' +
                    '<div id="mi_simpleBuilderHost" style="display: none;"></div>' +
                    '<div class="loading advanced-only">Load a JSON file to view root definitions</div>';
                if (container._rootItemExpandHandler) {
                    container.removeEventListener('click', container._rootItemExpandHandler);
                    container._rootItemExpandHandler = null;
                }
            }
            container._rootsContentSections = undefined;
            delete container._fillRootsLazySection;
            initMiSimpleBuilderDropdownsOnce();
            wireMiRootsContentRootItemExpand(container);
            updateMiSimpleBuilderPlaceholderVisibility();
            if (typeof updateMiItemPartsBreakdown === 'function') {
                updateMiItemPartsBreakdown();
            }
        }

        function initMiSimpleBuilderDropdownsOnce() {
            if (window.__miSimpleBuilderDropdownsWired) {
                return;
            }
            const typeSel = document.getElementById('mi_simpleTypeSelect');
            const manSel = document.getElementById('mi_simpleManufacturerSelect');
            if (!typeSel || !manSel) {
                return;
            }
            window.__miSimpleBuilderDropdownsWired = true;
            initMiItemPartsBreakdownCollapse();
            const levelEl = document.getElementById('mi_outputLevel');
            const seedEl = document.getElementById('mi_outputSeed');
            const onPropsChange = function () {
                if (typeof updateMiItemPartsBreakdown === 'function') {
                    updateMiItemPartsBreakdown();
                }
            };
            if (levelEl) {
                levelEl.addEventListener('input', onPropsChange);
                levelEl.addEventListener('change', onPropsChange);
            }
            if (seedEl) {
                seedEl.addEventListener('input', onPropsChange);
                seedEl.addEventListener('change', onPropsChange);
            }
            typeSel.addEventListener('change', function () {
                window.legitBuilderSetCtx(1);
                const buckets = getModdedSimpleModeBuckets();
                const tk = typeSel.value;
                updateMiSimpleSecondaryLabel(tk);
                manSel.innerHTML = '<option value="">' + miSimpleSecondaryPlaceholderText(tk || '') + '</option>';
                manSel.value = '';
                if (!tk) {
                    manSel.disabled = true;
                    updateMiSimpleSecondaryLabel('');
                    clearMiSimpleBuilderToPlaceholder();
                    return;
                }
                manSel.disabled = false;
                const roots = getModdedSimpleRootsForTypeKey(buckets || {}, tk);
                const mans = new Set();
                roots.forEach(function (r) {
                    mans.add(extractModdedSimpleSecondaryValue(r, tk));
                });
                Array.from(mans)
                    .sort(function (a, b) {
                        return String(a).localeCompare(String(b));
                    })
                    .forEach(function (m) {
                        const opt = document.createElement('option');
                        opt.value = m;
                        opt.textContent = m;
                        manSel.appendChild(opt);
                    });
                clearMiSimpleBuilderToPlaceholder();
                updateMiSimpleBuilderPlaceholderVisibility();
            });
            manSel.addEventListener('change', function () {
                window.legitBuilderSetCtx(1);
                const buckets = getModdedSimpleModeBuckets();
                const mf = manSel.value;
                if (!mf || !typeSel.value) {
                    clearMiSimpleBuilderToPlaceholder();
                    return;
                }
                const resolved = resolveModdedSimpleRoot(typeSel.value, mf, buckets || {});
                if (!resolved) {
                    showMiSimpleBuilderMessage(
                        '<span style="color:var(--text-secondary);">No item matches this type and manufacturer.</span>'
                    );
                    return;
                }
                renderMiSimpleSingleRoot(resolved);
            });
            updateMiSimpleBuilderPlaceholderVisibility();
        }
        
        function displayRootDefinitions() {
            // Show/hide simple mode help based on data availability (Legit tab only; modded help removed)
            const simpleModeHelp = lbEl('simpleModeHelp');
            if (simpleModeHelp) {
                if (!data || !data.rootDefinitions || data.rootDefinitions.length === 0) {
                    simpleModeHelp.style.display = 'none';
                } else if (applicationMode === 'simple') {
                    simpleModeHelp.style.display = 'block';
                } else {
                    simpleModeHelp.style.display = 'none';
                }
            }

            const container = lbEl('rootsContent');
            if (!container) return;

            const viewModeEarly = applicationMode || localStorage.getItem('rootDefinitionsViewMode') || 'simple';
            const ctxInst = window.__lbCtx;
            if (
                container.id === 'mi_rootsContent' &&
                viewModeEarly === 'simple' &&
                ctxInst &&
                ctxInst.relaxedPicker
            ) {
                if (!data || !data.rootDefinitions || data.rootDefinitions.length === 0) {
                    container.innerHTML =
                        '<div class="loading simple-only" style="text-align:center;padding:32px 16px;font-size:13px;color:var(--text-secondary);">Load Nexus/JSON data to use the builder.</div>' +
                        '<div class="loading advanced-only">Load a JSON file to view root definitions</div>';
                    container._rootsContentSections = undefined;
                    return;
                }
                displayModdedSimpleBuilderEnsureLayout(container);
                return;
            }

            if (!data || !data.rootDefinitions || data.rootDefinitions.length === 0) {
                if (applicationMode === 'simple') {
                    container.innerHTML = '<div class="loading simple-only" style="text-align: center; padding: var(--panel-pad) var(--section-gap);"><div style="font-size: var(--panel-title-size); color: var(--text-primary); margin-bottom: var(--section-gap);">🎮 Welcome to Simple Mode!</div><div style="font-size: var(--input-font); color: var(--text-secondary); line-height: 1.45;">Switch to <strong>Advanced Mode</strong> to load JSON files and access all features.<br>In Simple Mode, you can browse and build items once data is loaded.</div></div>';
                } else {
                    container.innerHTML = '<div class="loading advanced-only">Load a JSON file to view root definitions</div>';
                }
                return;
            }

            const typeFilterEl = lbEl('typeFilter');
            const typeFilter = typeFilterEl ? typeFilterEl.value : '';
            let filtered = data.rootDefinitions;

            if (typeFilter) {
                filtered = filtered.filter(root => root.basetype === typeFilter);
            }

            // In simple mode, hide turret/terminal gadgets, splicearmlauncher, invdef, and generic base items
            const currentViewMode = applicationMode || localStorage.getItem('rootDefinitionsViewMode') || 'simple';
            if (currentViewMode === 'simple') {
                filtered = filtered.filter(root => {
                    if (miIsTurretOrTerminalGadgetRoot(root)) {
                        return false;
                    }
                    const key = (root.key || '').toLowerCase();
                    const inv = (root.inv || '').toLowerCase();
                    return !key.includes('splicearmlauncher') &&
                           !key.includes('splicearm_launcher') &&
                           !key.includes('invdef') &&
                           // Hide generic base items (but keep actual items that use them)
                           !(key === 'heavy_weapon_gadget' || inv === 'heavy_weapon_gadget') &&
                           !(key === 'grenade_gadget' || inv === 'grenade_gadget') &&
                           !inv.includes('splicearmlauncher') &&
                           !inv.includes('splicearm_launcher') &&
                           !inv.includes('invdef');
                });
            }

            if (filtered.length === 0) {
                container.innerHTML = '<div class="loading">No root definitions match the filter</div>';
                return;
            }

            // Separate special top-level categories
            const hoverDriveItems = [];
            const classModItems = [];
            const universalItems = [];
            const groundPickupItems = [];
            const regularItems = [];
            
            filtered.forEach(root => {
                const key = (root.key || '').toLowerCase();
                const category = categorizeRoot(root);
                
                if (category === 'Hover Drive') {
                    hoverDriveItems.push(root);
                } else if (category === 'Class Mod' && key.startsWith('classmod_')) {
                    classModItems.push(root);
                } else if (category === 'Universal') {
                    universalItems.push(root);
                } else if ((key.startsWith('currency_') || key.startsWith('ammo') || key.startsWith('ammo_') || key.endsWith('_pickup')) &&
                           key !== 'mission_pickup' && !key.includes('missiontemp')) {
                    groundPickupItems.push(root);
                } else {
                    regularItems.push(root);
                }
            });

            // Group items by manufacturer first, then by category (including Ordnance)
            const byManufacturer = {};
            
            regularItems.forEach(root => {
                const manufacturer = extractManufacturer(root);
                if (!byManufacturer[manufacturer]) {
                    byManufacturer[manufacturer] = {
                        'Ordnance': [],
                        'Shield': [],
                        'Repkit': [],
                        'Class Mod': [],
                        'Def': [],
                        'Enhancement': [],
                        'Weapon': [],
                        'Ammo Types': [],
                        'InvDef': [],
                        'Other': []
                    };
                }
                const category = categorizeRoot(root);
                byManufacturer[manufacturer][category].push(root);
            });

            // Separate Unknown manufacturer to show at bottom
            const unknownManufacturer = byManufacturer['Unknown'];
            delete byManufacturer['Unknown'];
            
            // Collect InvDef items and add them to Unknown
            const invDefItems = [];
            filtered.forEach(root => {
                const category = categorizeRoot(root);
                if (category === 'InvDef') {
                    invDefItems.push(root);
                }
            });

            // Separate Unknown weapons to their own category
            const unknownWeapons = [];
            let unknownNonWeapons = null;
            
            if (unknownManufacturer) {
                unknownNonWeapons = { ...unknownManufacturer };
                if (unknownManufacturer['Weapon'] && unknownManufacturer['Weapon'].length > 0) {
                    unknownWeapons.push(...unknownManufacturer['Weapon']);
                    unknownNonWeapons['Weapon'] = [];
                }
            } else {
                unknownNonWeapons = {
                    'Ordnance': [],
                    'Shield': [],
                    'Repkit': [],
                    'Class Mod': [],
                    'Def': [],
                    'Enhancement': [],
                    'Weapon': [],
                    'Ammo Types': [],
                    'InvDef': [],
                    'Other': []
                };
            }

            // Add InvDef items to Unknown manufacturer
            if (invDefItems.length > 0) {
                if (!unknownNonWeapons['InvDef']) {
                    unknownNonWeapons['InvDef'] = [];
                }
                unknownNonWeapons['InvDef'].push(...invDefItems);
            }
            
            // Sort manufacturers alphabetically (case-insensitive)
            const manufacturers = Object.keys(byManufacturer).sort((a, b) => 
                a.toLowerCase().localeCompare(b.toLowerCase())
            );
            const categoryOrder = ['Ordnance', 'Shield', 'Repkit', 'Class Mod', 'Def', 'Enhancement', 'Weapon', 'Ammo Types', 'InvDef', 'Other'];

            // Get current view mode from application mode
            const viewMode = applicationMode || localStorage.getItem('rootDefinitionsViewMode') || 'simple';

            /** Lazy sections: only render root items when a section is expanded (avoids rendering 404 items when not displayed) */
            const rootsContentSections = {};
            
            let html = '';
            
            // Only show header controls in advanced mode
            if (viewMode === 'advanced') {
                html = `
                    <div class="roots-header-controls" style="display: flex; justify-content: space-between; align-items: center; margin-bottom: var(--section-gap); padding: var(--input-pad-y) var(--input-pad-x); background: var(--bg-tertiary); border: 1px solid var(--border-color);">
                        <div style="flex: 1;">
                            <div style="font-size: 12px; color: var(--text-primary); font-weight: 500; margin-bottom: 2px;">
                                ${filtered.length} of ${data.rootDefinitions.length} Items Available
                            </div>
                            ${loadedFileNames.length > 1 ? `<div style="font-size: 11px; color: var(--text-secondary);">From ${loadedFileNames.length} file(s)</div>` : ''}
                        </div>
                    </div>
                `;
            }

            // Simple Mode: Custom sort order
            if (viewMode === 'simple') {
                // Separate items into categories for simple mode
                const grenadeItems = [];
                const heavyWeaponItems = [];
                const repkitItems = [];
                const enhancementItems = [];
                const weaponItems = [];
                const shieldItems = [];
                
                // Check both regularItems and universalItems (heavy_weapon_gadget might be categorized as Universal)
                const allItemsToCheck = [...regularItems, ...universalItems];
                
                allItemsToCheck.forEach(root => {
                    const key = (root.key || '').toLowerCase();
                    const inv = (root.inv || '').toLowerCase();
                    const basetype = (root.basetype || '').toLowerCase();
                    const category = categorizeRoot(root);
                    
                    // Heavy Weapons (heavy_weapon_gadget) - check first before other categories
                    // But exclude the generic base item (key === 'heavy_weapon_gadget')
                    if ((key.includes('heavy_weapon_gadget') || inv.includes('heavy_weapon_gadget') || 
                        basetype.includes('heavy_weapon_gadget')) &&
                        !(key === 'heavy_weapon_gadget' || inv === 'heavy_weapon_gadget')) {
                        heavyWeaponItems.push(root);
                    }
                    // Grenades (grenade_gadget)
                    else if (key.includes('grenade_gadget') || inv.includes('grenade_gadget') ||
                             basetype.includes('grenade_gadget')) {
                        grenadeItems.push(root);
                    }
                    // Shields
                    else if (category === 'Shield') {
                        shieldItems.push(root);
                    }
                    // Repkits
                    else if (category === 'Repkit') {
                        repkitItems.push(root);
                    }
                    // Enhancements
                    else if (category === 'Enhancement') {
                        enhancementItems.push(root);
                    }
                    // Weapons (but not heavy_weapon_gadget or grenade_gadget)
                    else if (category === 'Weapon' && 
                             !key.includes('heavy_weapon_gadget') && !inv.includes('heavy_weapon_gadget') &&
                             !key.includes('grenade_gadget') && !inv.includes('grenade_gadget')) {
                        weaponItems.push(root);
                    }
                });
                
                // Helper function to create a category section (lazy: content empty until expanded)
                const createCategorySection = (title, items, idPrefix) => {
                    if (items.length === 0) return '';
                    const sectionKey = idPrefix;
                    rootsContentSections[sectionKey] = { items, idPrefix };
                    return `
                        <div class="manufacturer-section">
                            <div class="manufacturer-header" data-manufacturer="${idPrefix}">
                                <span class="manufacturer-expand-icon">▶</span>
                                ${title}
                                <span class="manufacturer-count">(${items.length})</span>
                            </div>
                            <div class="manufacturer-content collapsed" data-section-key="${sectionKey}"></div>
                        </div>
                    `;
                };
                
                // 1. ClassMods
                if (classModItems.length > 0) {
                    html += createCategorySection('ClassMods', classModItems, 'classmods');
                }
                
                // 2. Shields
                if (shieldItems.length > 0) {
                    html += createCategorySection('Shields', shieldItems, 'shields');
                }
                
                // 3. Grenades
                if (grenadeItems.length > 0) {
                    html += createCategorySection('Grenades', grenadeItems, 'grenades');
                }
                
                // 4. Heavy Weapons
                if (heavyWeaponItems.length > 0) {
                    html += createCategorySection('Heavy Weapons', heavyWeaponItems, 'heavyweapons');
                }
                
                // 5. Repkits
                if (repkitItems.length > 0) {
                    html += createCategorySection('Repkits', repkitItems, 'repkits');
                }
                
                // 6. Enhancements
                if (enhancementItems.length > 0) {
                    html += createCategorySection('Enhancements', enhancementItems, 'enhancements');
                }
                
                // 7. Weapons - grouped by type (SMG, Assault Rifle, Pistol, Sniper, Shotgun)
                if (weaponItems.length > 0) {
                    const getWeaponType = (root) => {
                        const key = (root.key || '').toLowerCase();
                        const inv = (root.inv || '').toLowerCase();
                        const basetype = (root.basetype || '').toLowerCase();
                        const itembasename = (root.itembasename || '').toLowerCase();
                        const name = (root.name || '').toLowerCase();
                        if (basetype.includes('weapon_sr') || basetype.includes('sniper') || inv.includes('sr')) return 'Sniper';
                        if (basetype.includes('weapon_sg') || basetype.includes('shotgun') || inv.includes('sg')) return 'Shotgun';
                        if (basetype.includes('weapon_smg') || basetype.includes('submachine') || inv.includes('smg') || key.includes('smg') || itembasename.includes('smg') || name.includes('smg')) return 'SMG';
                        if (basetype.includes('weapon_ps') || basetype.includes('pistol') || inv.includes('ps')) return 'Pistol';
                        if (basetype.includes('weapon_ar') || basetype.includes('assault') || inv.includes('ar')) return 'Assault Rifle';
                        return 'Other';
                    };
                    const weaponsByType = {};
                    weaponItems.forEach(root => {
                        const manufacturer = extractManufacturer(root);
                        if (manufacturer === 'Unknown') return;
                        const type = getWeaponType(root);
                        if (!weaponsByType[type]) weaponsByType[type] = [];
                        weaponsByType[type].push(root);
                    });
                    const typeOrder = ['SMG', 'Assault Rifle', 'Pistol', 'Sniper', 'Shotgun', 'Other'];
                    typeOrder.forEach((type, typeIndex) => {
                        const items = weaponsByType[type] || [];
                        if (items.length === 0) return;
                        const sectionKey = `weapons-${typeIndex}`;
                        rootsContentSections[sectionKey] = { items, idPrefix: sectionKey };
                        html += `
                            <div class="manufacturer-section">
                                <div class="manufacturer-header" data-manufacturer="weapons-${typeIndex}">
                                    <span class="manufacturer-expand-icon">▶</span>
                                    ${type}
                                    <span class="manufacturer-count">(${items.length})</span>
                                </div>
                                <div class="manufacturer-content collapsed" data-section-key="${sectionKey}"></div>
                            </div>
                        `;
                    });
                }
            } else {
                // Advanced Mode: Show ClassMods as its own top-level category (lazy content)
                if (classModItems.length > 0) {
                    rootsContentSections['classmods'] = { items: classModItems, idPrefix: 'classmods' };
                    html += `
                        <div class="manufacturer-section">
                            <div class="manufacturer-header" data-manufacturer="classmods">
                                <span class="manufacturer-expand-icon">▶</span>
                                ClassMods
                                <span class="manufacturer-count">(${classModItems.length})</span>
                            </div>
                            <div class="manufacturer-content collapsed" data-section-key="classmods"></div>
                        </div>
                    `;
                }
            }

            // Show Universal as its own top-level category (lazy content)
            if (universalItems.length > 0 && viewMode !== 'simple') {
                rootsContentSections['universal'] = { items: universalItems, idPrefix: 'universal' };
                html += `
                    <div class="manufacturer-section">
                        <div class="manufacturer-header" data-manufacturer="universal">
                            <span class="manufacturer-expand-icon">▶</span>
                            Universal
                            <span class="manufacturer-count">(${universalItems.length})</span>
                        </div>
                        <div class="manufacturer-content collapsed" data-section-key="universal"></div>
                    </div>
                `;
            }

            // Show Ground Pickups as its own top-level category (Currency, Ammo, and _pickup items)
            // Hide in simple view
            if (groundPickupItems.length > 0 && viewMode !== 'simple') {
                // Group by type: Currency, Ammo, Pickups
                const currencyItems = [];
                const ammoItems = [];
                const pickupItems = [];
                
                groundPickupItems.forEach(root => {
                    const key = (root.key || '').toLowerCase();
                    if (key.startsWith('currency_')) {
                        currencyItems.push(root);
                    } else if (key.startsWith('ammo') || key.startsWith('ammo_')) {
                        ammoItems.push(root);
                    } else if (key.endsWith('_pickup') && key !== 'mission_pickup' && !key.includes('missiontemp')) {
                        pickupItems.push(root);
                    }
                });

                rootsContentSections['groundpickups-currency'] = { items: currencyItems, idPrefix: 'groundpickups-currency' };
                rootsContentSections['groundpickups-ammo'] = { items: ammoItems, idPrefix: 'groundpickups-ammo' };
                const filteredPickupItems = pickupItems.filter(root => {
                    const key = (root.key || '').toLowerCase();
                    return key !== 'mission_pickup' && !key.includes('missiontemp');
                });
                rootsContentSections['groundpickups-pickups'] = { items: filteredPickupItems, idPrefix: 'groundpickups-pickups' };

                html += `
                    <div class="manufacturer-section">
                        <div class="manufacturer-header" data-manufacturer="groundpickups">
                            <span class="manufacturer-expand-icon">▶</span>
                            Ground Pickups
                            <span class="manufacturer-count">(${groundPickupItems.length})</span>
                        </div>
                        <div class="manufacturer-content collapsed">
                `;
                if (currencyItems.length > 0) {
                    html += `
                        <div class="category-section">
                            <div class="category-header" data-manufacturer="groundpickups" data-category="currency">
                                <span class="category-expand-icon">▶</span>
                                Currency
                                <span class="category-count">(${currencyItems.length})</span>
                            </div>
                            <div class="category-content collapsed" data-section-key="groundpickups-currency"></div>
                        </div>
                    `;
                }
                if (ammoItems.length > 0) {
                    html += `
                        <div class="category-section">
                            <div class="category-header" data-manufacturer="groundpickups" data-category="ammo">
                                <span class="category-expand-icon">▶</span>
                                Ammo
                                <span class="category-count">(${ammoItems.length})</span>
                            </div>
                            <div class="category-content collapsed" data-section-key="groundpickups-ammo"></div>
                        </div>
                    `;
                }
                if (filteredPickupItems.length > 0) {
                    html += `
                        <div class="category-section">
                            <div class="category-header" data-manufacturer="groundpickups" data-category="pickups">
                                <span class="category-expand-icon">▶</span>
                                Pickups
                                <span class="category-count">(${filteredPickupItems.length})</span>
                            </div>
                            <div class="category-content collapsed" data-section-key="groundpickups-pickups"></div>
                        </div>
                    `;
                }
                html += `
                        </div>
                    </div>
                `;
            }

            // Show other items grouped by manufacturer (only in Advanced Mode, lazy category content)
            if (viewMode !== 'simple') {
                manufacturers.forEach((manufacturer, manIndex) => {
                    const manufacturerCategories = byManufacturer[manufacturer];
                    const totalItems = categoryOrder.reduce((sum, cat) => sum + manufacturerCategories[cat].length, 0);
                    
                    if (totalItems > 0) {
                        html += `
                            <div class="manufacturer-section">
                                <div class="manufacturer-header" data-manufacturer="${manIndex}">
                                    <span class="manufacturer-expand-icon">▶</span>
                                    ${manufacturer}
                                    <span class="manufacturer-count">(${totalItems})</span>
                                </div>
                                <div class="manufacturer-content collapsed">
                        `;

                        categoryOrder.forEach((category, catIndex) => {
                            const items = manufacturerCategories[category];
                            if (items.length > 0) {
                                const sectionKey = `man-${manIndex}-cat-${catIndex}`;
                                rootsContentSections[sectionKey] = { items, idPrefix: `${manIndex}-${catIndex}` };
                                html += `
                                    <div class="category-section">
                                        <div class="category-header" data-manufacturer="${manIndex}" data-category="${catIndex}">
                                            <span class="category-expand-icon">▶</span>
                                            ${category}
                                            <span class="category-count">(${items.length})</span>
                                        </div>
                                        <div class="category-content collapsed" data-section-key="${sectionKey}"></div>
                                    </div>
                                `;
                            }
                        });

                        html += `
                                </div>
                            </div>
                        `;
                    }
                });
            }

            // Show Unknown Weapons as their own top-level category (lazy content)
            if (unknownWeapons.length > 0 && viewMode !== 'simple') {
                rootsContentSections['unknownweapons'] = { items: unknownWeapons, idPrefix: 'unknownweapons' };
                html += `
                    <div class="manufacturer-section">
                        <div class="manufacturer-header" data-manufacturer="unknownweapons">
                            <span class="manufacturer-expand-icon">▶</span>
                            Unknown Weapons
                            <span class="manufacturer-count">(${unknownWeapons.length})</span>
                        </div>
                        <div class="manufacturer-content collapsed" data-section-key="unknownweapons"></div>
                    </div>
                `;
            }

            // Show Unknown manufacturer at the bottom (lazy category content)
            if (unknownNonWeapons && viewMode !== 'simple') {
                const totalItems = categoryOrder.reduce((sum, cat) => sum + unknownNonWeapons[cat].length, 0);
                if (totalItems > 0) {
                    html += `
                        <div class="manufacturer-section">
                            <div class="manufacturer-header" data-manufacturer="unknown">
                                <span class="manufacturer-expand-icon">▶</span>
                                Unknown
                                <span class="manufacturer-count">(${totalItems})</span>
                            </div>
                            <div class="manufacturer-content collapsed">
                    `;

                    categoryOrder.forEach((category, catIndex) => {
                        const items = unknownNonWeapons[category];
                        if (items.length > 0) {
                            const sectionKey = `unknown-cat-${catIndex}`;
                            rootsContentSections[sectionKey] = { items, idPrefix: `unknown-${catIndex}` };
                            html += `
                                <div class="category-section">
                                    <div class="category-header" data-manufacturer="unknown" data-category="${catIndex}">
                                        <span class="category-expand-icon">▶</span>
                                        ${category}
                                        <span class="category-count">(${items.length})</span>
                                    </div>
                                    <div class="category-content collapsed" data-section-key="${sectionKey}"></div>
                                </div>
                            `;
                        }
                    });

                    html += `
                            </div>
                        </div>
                    `;
                }
            }


            // Show Hover Drive at the bottom, grouped by manufacturer
            // Hide in simple view
            if (hoverDriveItems.length > 0 && viewMode !== 'simple') {
                // Group hover drive items by manufacturer
                const hoverDriveByManufacturer = {};
                hoverDriveItems.forEach(root => {
                    const manufacturer = extractManufacturer(root);
                    if (!hoverDriveByManufacturer[manufacturer]) {
                        hoverDriveByManufacturer[manufacturer] = [];
                    }
                    hoverDriveByManufacturer[manufacturer].push(root);
                });

                // Sort manufacturers alphabetically
                const hoverDriveManufacturers = Object.keys(hoverDriveByManufacturer).sort((a, b) => {
                    if (a === 'Unknown') return 1;
                    if (b === 'Unknown') return -1;
                    return a.localeCompare(b);
                });

                html += `
                    <div class="manufacturer-section">
                        <div class="manufacturer-header" data-manufacturer="hoverdrive">
                            <span class="manufacturer-expand-icon">▶</span>
                            Hover Drive
                            <span class="manufacturer-count">(${hoverDriveItems.length})</span>
                        </div>
                        <div class="manufacturer-content collapsed">
                `;

                hoverDriveManufacturers.forEach((manufacturer, manIndex) => {
                    const items = hoverDriveByManufacturer[manufacturer];
                    const sectionKey = `hoverdrive-${manIndex}`;
                    rootsContentSections[sectionKey] = { items, idPrefix: `hoverdrive-${manIndex}` };
                    html += `
                        <div class="category-section">
                            <div class="category-header" data-manufacturer="hoverdrive" data-category="${manIndex}">
                                <span class="category-expand-icon">▶</span>
                                ${manufacturer}
                                <span class="category-count">(${items.length})</span>
                            </div>
                            <div class="category-content collapsed" data-section-key="${sectionKey}"></div>
                        </div>
                    `;
                });

                html += `
                        </div>
                    </div>
                `;
            }

            container.innerHTML = html;
            container._rootsContentSections = rootsContentSections;

            // Fill section content on first expand (lazy render)
            function fillSectionIfNeeded(contentDiv) {
                const sectionKey = contentDiv.getAttribute('data-section-key');
                if (!sectionKey || contentDiv.children.length > 0) return;
                const sections = container._rootsContentSections;
                if (!sections || !sections[sectionKey]) return;
                const { items, idPrefix } = sections[sectionKey];
                let inner = '';
                const useCompactDepTables = !!contentDiv.closest('#item-editor-tab');
                items.forEach((root, i) => {
                    inner += createRootItemHTML(root, idPrefix + '-' + i, useCompactDepTables ? { useCompactDepTables: true } : undefined);
                });
                contentDiv.innerHTML = inner;
                if (useCompactDepTables) {
                    miInitOpenModdedPickers(contentDiv);
                }
            }

            container._fillRootsLazySection = fillSectionIfNeeded;

            // Add click handlers for manufacturer headers (top level)
            container.querySelectorAll('.manufacturer-header').forEach(header => {
                header.addEventListener('click', function(e) {
                    e.stopPropagation();
                    const manufacturerContent = this.nextElementSibling;
                    fillSectionIfNeeded(manufacturerContent);
                    this.classList.toggle('expanded');
                    manufacturerContent.classList.toggle('collapsed');
                });
            });

            // Add click handlers for category headers (within manufacturers and Ground Pickups)
            container.querySelectorAll('.category-header').forEach(header => {
                header.addEventListener('click', function(e) {
                    e.stopPropagation();
                    const categoryContent = this.nextElementSibling;
                    fillSectionIfNeeded(categoryContent);
                    this.classList.toggle('expanded');
                    categoryContent.classList.toggle('collapsed');
                });
            });

            // Use delegation so lazy-filled root items get expand/collapse without re-attaching (one listener per container)
            if (container._rootItemExpandHandler) {
                container.removeEventListener('click', container._rootItemExpandHandler);
            }
            container._rootItemExpandHandler = function(e) {
                const header = e.target.closest('.root-header-clickable');
                if (!header) return;
                e.stopPropagation();
                const rootItem = header.closest('.root-item');
                if (!rootItem) return;
                const content = rootItem.querySelector('.root-content');
                if (content) {
                    rootItem.classList.toggle('expanded');
                    content.classList.toggle('collapsed');
                }
            };
            container.addEventListener('click', container._rootItemExpandHandler);
            
        }

        /**
         * After push or navigation: lazy root sections stay collapsed unless the user clicked them.
         * Expand the manufacturer/category chain, lazy-fill the section, and open the root row
         * so item-type slots (dep tables) are visible.
         */
        function expandLegitBuilderRootsForRootKey(rootKey, containerOverride) {
            if (!rootKey) {
                return;
            }
            const container = containerOverride || lbEl('rootsContent');
            if (!container) {
                return;
            }
            const sections = container._rootsContentSections;
            if (!sections) {
                if (container.id === 'mi_rootsContent') {
                    window.legitBuilderSetCtx(1);
                    if (typeof applyMiSimpleBuilderFromRootKey === 'function') {
                        applyMiSimpleBuilderFromRootKey(rootKey);
                    }
                }
                return;
            }

            const rootKeyNorm = String(rootKey || '').trim();
            let sectionKey = null;
            for (const sk of Object.keys(sections)) {
                const entry = sections[sk];
                if (
                    entry &&
                    entry.items &&
                    entry.items.some(function (r) {
                        return r && String(r.key || '').trim() === rootKeyNorm;
                    })
                ) {
                    sectionKey = sk;
                    break;
                }
            }

            if (!sectionKey) {
                return;
            }

            const contentDiv = container.querySelector(
                '[data-section-key="' + String(sectionKey).replace(/\\/g, '\\\\').replace(/"/g, '\\"') + '"]'
            );
            if (!contentDiv) {
                return;
            }

            if (typeof container._fillRootsLazySection === 'function') {
                container._fillRootsLazySection(contentDiv);
            }

            let node = contentDiv;
            while (node && node !== container) {
                if (
                    node.classList.contains('manufacturer-content') ||
                    node.classList.contains('category-content')
                ) {
                    node.classList.remove('collapsed');
                    const hdr = node.previousElementSibling;
                    if (
                        hdr &&
                        (hdr.classList.contains('manufacturer-header') ||
                            hdr.classList.contains('category-header'))
                    ) {
                        hdr.classList.add('expanded');
                    }
                }
                node = node.parentElement;
            }

            const rootItem = Array.from(container.querySelectorAll('.root-item')).find(function (item) {
                const el = item.querySelector('[data-root-key]');
                const a = el && el.getAttribute('data-root-key');
                return a != null && String(a).trim() === rootKeyNorm;
            });

            if (rootItem) {
                rootItem.classList.add('expanded');
                const rc = rootItem.querySelector('.root-content');
                if (rc) {
                    rc.classList.remove('collapsed');
                }
            }
        }

        /**
         * Main app tab switch is sometimes deferred (rAF). Modded push needs layout + #item-editor-tab
         * active in the same turn before displayRootDefinitions / scrollIntoView run.
         */
        function miEnsureItemEditorMainTabActive(opt) {
            opt = opt || {};
            var mi = document.getElementById('item-editor-tab');
            if (!mi) {
                return false;
            }
            document.querySelectorAll('.tab-content').forEach(function (t) {
                t.classList.remove('active');
            });
            mi.classList.add('active');
            document.querySelectorAll('.tab-button').forEach(function (btn) {
                var oc = btn.getAttribute('onclick') || '';
                var on =
                    oc.indexOf("'item-editor-tab'") !== -1 || oc.indexOf('"item-editor-tab"') !== -1;
                btn.classList.toggle('active', on);
            });
            if (typeof window.syncLegitBuilderCtxToActiveMainTab === 'function') {
                window.syncLegitBuilderCtxToActiveMainTab();
            }
            window.legitBuilderSetCtx(1);
            if (!opt.skipViewportScroll) {
                if (typeof window.scrollMainViewportToTop === 'function') {
                    window.scrollMainViewportToTop();
                } else {
                    try {
                        window.scrollTo({ top: 0, left: 0, behavior: 'smooth' });
                    } catch (e) {
                        window.scrollTo(0, 0);
                    }
                    try {
                        document.documentElement.scrollTop = 0;
                        document.body.scrollTop = 0;
                    } catch (e2) {}
                }
            }
            return true;
        }

        /**
         * Modded tab: Parser sits above a long main column; scrollIntoView on inner nodes often
         * does not move the window. Also walk overflow:auto ancestors inside #item-editor-tab.
         */
        function miScrollModdedPushTargetIntoView(targetEl) {
            if (!targetEl || typeof targetEl.getBoundingClientRect !== 'function') {
                return;
            }
            var miTab = document.getElementById('item-editor-tab');
            if (miTab && !miTab.classList.contains('active')) {
                miEnsureItemEditorMainTabActive();
            } else {
                window.legitBuilderSetCtx(1);
            }
            try {
                targetEl.scrollIntoView({ block: 'nearest', behavior: 'auto', inline: 'nearest' });
            } catch (eSi0) { /* ignore */ }
            try {
                void targetEl.getBoundingClientRect();
            } catch (e0) { /* ignore */ }
            var docEl = document.documentElement;
            var rect = targetEl.getBoundingClientRect();
            var vpH = window.innerHeight || (docEl && docEl.clientHeight) || 0;
            if (vpH > 0 && (rect.top < 72 || rect.bottom > vpH - 24)) {
                var y = window.scrollY || window.pageYOffset || 0;
                var want = y + rect.top - Math.max(48, vpH * 0.12);
                try {
                    window.scrollTo({ top: Math.max(0, want), left: 0, behavior: 'auto' });
                } catch (eW) {
                    try {
                        window.scrollTo(0, Math.max(0, want));
                    } catch (eW2) { /* ignore */ }
                }
            }
            var p = targetEl.parentElement;
            while (p && p !== document.body && miTab && miTab.contains(p)) {
                var st = window.getComputedStyle(p);
                var oy = st.overflowY;
                var scrollable =
                    (oy === 'auto' || oy === 'scroll' || oy === 'overlay') &&
                    p.scrollHeight > p.clientHeight + 2;
                if (scrollable) {
                    var tr = targetEl.getBoundingClientRect();
                    var pr = p.getBoundingClientRect();
                    var pad = 8;
                    if (tr.top < pr.top + pad) {
                        p.scrollTop += tr.top - pr.top - pad;
                    } else if (tr.bottom > pr.bottom - pad) {
                        p.scrollTop += tr.bottom - pr.bottom + pad;
                    }
                }
                p = p.parentElement;
            }
            try {
                targetEl.scrollIntoView({ block: 'center', behavior: 'auto', inline: 'nearest' });
            } catch (eS) {
                try {
                    targetEl.scrollIntoView(true);
                } catch (eS2) { /* ignore */ }
            }
        }
        window.miScrollModdedPushTargetIntoView = miScrollModdedPushTargetIntoView;

        /**
         * After all comp rules merge, bump soft 0–1 slots to required 1–1 when at least one part
         * is valid under the current tag pool (firmware and explicit min:0 stay optional).
         */
        function finalizeImpliedPartCountsFromTagPool(root, compRules) {
            if (!root || !compRules || !compRules.partCounts || !compRules.tagPool) return;
            const partCounts = compRules.partCounts;
            const tagPool = compRules.tagPool;
            Array.from(partCounts.entries()).forEach(([slotName, countInfo]) => {
                const normalized = normalizeDepTableName(slotName);
                if (!normalized || normalized === 'unknown' || normalized === 'inv_comp') return;
                if (normalized === 'firmware') return;
                if (countInfo.hasExplicitMin) return;
                if (allowsZeroOptionMinBypass(normalized)) return;
                const src = String(countInfo.countSource || '');
                // Slots introduced only from root parttypes are structural hints, not explicit requirements.
                // Keep them optional even when the current tag pool happens to leave qualifying candidates.
                if (src.indexOf('parttypes_fallback') === 0) return;
                if (src.indexOf('pearlescent_implied_1_1') !== -1) return;
                const min = Number(countInfo.min);
                const max = Number(countInfo.max);
                if (min >= 1) return;
                if (!(min === 0 && max === 1)) return;
                const q = getDependencyQualifiedOptionCountForSlot(root, compRules, slotName, tagPool);
                if (q <= 0) return;
                partCounts.set(slotName, {
                    ...countInfo,
                    min: 1,
                    max: 1,
                    hasExplicitMin: false,
                    countSource: src ? `${src}_tagpool_qualified_1_1` : 'tagpool_qualified_1_1'
                });
            });
        }

        /**
         * If no part in a slot passes isPartValid under the current tag pool, do not treat the slot as required.
         * Aligns live "Slot requirements" and forcedParts with resolveRequiredMinForSlot(..., dependencyQualifiedOptions).
         */
        function relaxPartCountsWhenNoQualifiedOptions(root, compRules) {
            if (!root || !compRules || !compRules.partCounts || !compRules.tagPool) return;
            const partCounts = compRules.partCounts;
            const tagPool = compRules.tagPool;
            Array.from(partCounts.entries()).forEach(([slotName, countInfo]) => {
                const normalized = normalizeDepTableName(slotName);
                if (!normalized || normalized === 'unknown' || normalized === 'inv_comp') return;
                if (normalized === 'firmware') return;
                const src = String(countInfo.countSource || '');
                if (src.indexOf('pearlescent_implied_1_1') !== -1) return;
                const min = Number(countInfo.min);
                if (!(min > 0)) return;
                const q = getDependencyQualifiedOptionCountForSlot(root, compRules, slotName, tagPool);
                if (q > 0) return;
                const prevMax = countInfo.max != null ? Number(countInfo.max) : min;
                partCounts.set(slotName, {
                    ...countInfo,
                    min: 0,
                    max: Math.max(prevMax, 1),
                    countSource: src ? `${src}_zero_qualified_relaxed` : 'zero_qualified_relaxed'
                });
            });
        }

        /**
         * Pick the serial's inv_comp key from selected part keys. Validator order is not guaranteed;
         * manufacturer grenade_gadget / *_hw roots often omit base_comp_* from depEntries (only on generic root),
         * and base_comp_* does not start with "comp".
         */
        function resolveSelectedCompKeyFromSelection(root, keys) {
            if (!keys || keys.length === 0) return null;
            const invCompDep = function (r, key) {
                return r && r.depEntries && r.depEntries.some(function (dep) {
                    return dep.depTableName === 'inv_comp' && dep.key === key;
                });
            };
            var k = keys.find(function (key) { return invCompDep(root, key); });
            if (k) return k;
            if (data && data.rootDefinitions) {
                k = keys.find(function (key) {
                    return data.rootDefinitions.some(function (r) { return invCompDep(r, key); });
                });
                if (k) return k;
            }
            var items = data && data.subItemsByTable && data.subItemsByTable.inv_comp && data.subItemsByTable.inv_comp.items;
            if (items) {
                k = keys.find(function (key) {
                    return items.some(function (item) { return (item.key || item.inv_comp) === key; });
                });
                if (k) return k;
            }
            k = keys.find(function (key) { return /^comp_\d/i.test(key); });
            if (k) return k;
            k = keys.find(function (key) { return /^base_comp_/i.test(key); });
            if (k) return k;
            k = keys.find(function (key) { return key.toLowerCase().indexOf('comp_') !== -1; });
            return k || null;
        }

        /**
         * Map basetype-derived keys (e.g. weapon_sg, Weapon_AR) to the Nexus root that holds shared
         * part dep entries (e.g. body_ele / secondary_ele on "weapon", not on weapon_sg).
         */
        function normalizeGenericRootKeyForPartDeps(rawKey) {
            const t = String(rawKey || '').toLowerCase();
            if (!t) return rawKey;
            if (t.includes('terminal_gadget')) return 'terminal_gadget';
            if (t.includes('turret_gadget')) return 'turret_gadget';
            if (t.includes('heavy_weapon_gadget')) return 'heavy_weapon_gadget';
            if (t.includes('grenade_gadget')) return 'grenade_gadget';
            if (t.includes('repair_kit')) return 'repair_kit';
            if (t.includes('classmod')) return 'classmod';
            if (t.includes('enhancement')) return 'enhancement';
            if (t.includes('shield')) return 'shield';
            if (t.includes('grenade')) return 'grenade';
            if (t.includes('artifact')) return 'artifact';
            if (t.includes('weapon')) return 'weapon';
            return rawKey;
        }

        /**
         * Generic root (e.g. weapon) for manufacturer roots (e.g. mal_sg). Keeps duplicate part keys
         * (part_radiation on body_ele vs element) aligned between tag pool and forward scoping.
         */
        function getBaseGenericRootForPartLookup(root, data) {
            if (!root || !data || !data.rootDefinitions) return null;
            const rootBasetype = root.basetype || '';
            let itemType = '';
            if (rootBasetype) {
                const basetypeMatch = rootBasetype.match(/inv'(\w+)/i);
                if (basetypeMatch) {
                    itemType = basetypeMatch[1].toLowerCase();
                }
            }
            if (!itemType) {
                const rootItembasename = root.itembasename || root.key || '';
                const lowerName = rootItembasename.toLowerCase();
                if (lowerName.includes('grenade_gadget')) itemType = 'grenade_gadget';
                else if (lowerName.includes('heavy_weapon_gadget')) itemType = 'heavy_weapon_gadget';
                else if (lowerName.includes('weapon')) itemType = 'weapon';
                else if (lowerName.includes('enhancement')) itemType = 'enhancement';
                else if (lowerName.includes('shield')) itemType = 'shield';
                else if (lowerName.includes('repkit')) itemType = 'repair_kit';
                else if (lowerName.includes('repair') || lowerName.includes('repair_kit')) itemType = 'repair_kit';
                else if (lowerName.includes('grenade')) itemType = 'grenade';
                else if (lowerName.includes('artifact')) itemType = 'artifact';
                else itemType = 'weapon';
            }
            const rootKeyLowerForTagPool = (root.key || '').toLowerCase();
            const baseItemTypePatternsForTagPool = ['weapon', 'enhancement', 'shield', 'repair_kit', 'grenade_gadget', 'heavy_weapon_gadget', 'turret_gadget', 'terminal_gadget', 'grenade', 'artifact', 'classmod'];
            let baseItemTypeForTagPool = itemType;
            if (rootKeyLowerForTagPool.includes('terminal_') && !rootKeyLowerForTagPool.includes('terminal_gadget')) {
                if (rootKeyLowerForTagPool.includes('terminal_combat') || rootKeyLowerForTagPool.includes('terminal_')) {
                    baseItemTypeForTagPool = 'terminal_gadget';
                }
            } else if (rootKeyLowerForTagPool.includes('turret_') && !rootKeyLowerForTagPool.includes('turret_gadget')) {
                if (rootKeyLowerForTagPool.includes('turret_combat') || rootKeyLowerForTagPool.includes('turret_')) {
                    baseItemTypeForTagPool = 'turret_gadget';
                }
            } else {
                for (const pattern of baseItemTypePatternsForTagPool) {
                    if (rootKeyLowerForTagPool.includes(pattern) && rootKeyLowerForTagPool !== pattern) {
                        const match = rootKeyLowerForTagPool.match(new RegExp(`(?:\\w+_)?(${pattern})(?:\\w+)?$`, 'i'));
                        if (match && match[1]) {
                            baseItemTypeForTagPool = match[1].toLowerCase();
                            break;
                        }
                    } else if (rootKeyLowerForTagPool === pattern) {
                        baseItemTypeForTagPool = pattern;
                        break;
                    }
                }
            }
            if (rootKeyLowerForTagPool !== baseItemTypeForTagPool) {
                const lookupKey = String(normalizeGenericRootKeyForPartDeps(baseItemTypeForTagPool) || '').toLowerCase();
                const found = data.rootDefinitions.find(r => (r.key || '').toLowerCase() === lookupKey);
                return found || null;
            }
            return root;
        }

        function findPartDepEntryForRootContext(root, partKey, data, baseGenericRoot) {
            if (!partKey) return null;
            let partEntry = root?.depEntries?.find(dep => dep && dep.key === partKey);
            if (partEntry) return partEntry;
            if (baseGenericRoot?.depEntries) {
                partEntry = baseGenericRoot.depEntries.find(dep => dep && dep.key === partKey);
                if (partEntry) return partEntry;
            }
            if (data?.rootDefinitions) {
                for (const r of data.rootDefinitions) {
                    if (r.depEntries) {
                        partEntry = r.depEntries.find(dep => dep && dep.key === partKey);
                        if (partEntry) return partEntry;
                    }
                }
            }
            return null;
        }

        function getSelectedCompRules(root, selectionOverride = null) {
            // Prefer explicitly provided selections (validator path), otherwise use builder state.
            let selectedKeySet = null;
            if (selectionOverride && typeof selectionOverride[Symbol.iterator] === 'function') {
                selectedKeySet = selectionOverride instanceof Set ? selectionOverride : new Set(selectionOverride);
            } else if (_sp().has(root.key)) {
                selectedKeySet = _sp().get(root.key);
            } else {
                return null;
            }
            
            // Determine item type from root (for base_comp lookup)
            const rootBasetype = root.basetype || '';
            let itemType = '';
            if (rootBasetype) {
                const basetypeMatch = rootBasetype.match(/inv'(\w+)/i);
                if (basetypeMatch) {
                    itemType = basetypeMatch[1].toLowerCase();
                }
            }
            if (!itemType) {
                const rootItembasename = root.itembasename || root.key || '';
                const lowerName = rootItembasename.toLowerCase();
                // Check for specific types first (more specific patterns before generic ones)
                if (lowerName.includes('grenade_gadget')) itemType = 'grenade_gadget';
                else if (lowerName.includes('heavy_weapon_gadget')) itemType = 'heavy_weapon_gadget';
                else if (lowerName.includes('weapon')) itemType = 'weapon';
                else if (lowerName.includes('enhancement')) itemType = 'enhancement';
                else if (lowerName.includes('shield')) itemType = 'shield';
                else if (lowerName.includes('repair') || lowerName.includes('repair_kit')) itemType = 'repair_kit';
                else if (lowerName.includes('grenade')) itemType = 'grenade';
                else if (lowerName.includes('artifact')) itemType = 'artifact';
                else itemType = 'weapon'; // Default
            }

            // Normalize concrete item subtypes (weapon_sg, weapon_ps, etc.) to base families
            // so comp/basecomp lookups stay within the correct root lineage.
            const normalizeItemTypeFamily = (rawType) => {
                const t = String(rawType || '').toLowerCase();
                if (!t) return '';
                if (t.includes('terminal_gadget')) return 'terminal_gadget';
                if (t.includes('turret_gadget')) return 'turret_gadget';
                if (t.includes('heavy_weapon_gadget')) return 'heavy_weapon_gadget';
                if (t.includes('grenade_gadget')) return 'grenade_gadget';
                if (t.includes('repair_kit')) return 'repair_kit';
                if (t.includes('classmod')) return 'classmod';
                if (t.includes('enhancement')) return 'enhancement';
                if (t.includes('shield')) return 'shield';
                if (t.includes('grenade')) return 'grenade';
                if (t.includes('artifact')) return 'artifact';
                if (t.includes('weapon')) return 'weapon';
                return t;
            };
            
            const selectedKeys = Array.from(selectedKeySet);
            if (selectedKeys.length === 0) {
                return null;
            }
            const selectedComp = resolveSelectedCompKeyFromSelection(root, selectedKeys);
            
            if (!selectedComp) {
                return null;
            }
            
            // Find the comp entry (from root's depEntries, or from global inv_comp when validating a serial whose root doesn't list this comp)
            let compEntry = root.depEntries?.find(dep => 
                dep.key === selectedComp && dep.depTableName === 'inv_comp'
            );
            if (!compEntry && data && data.rootDefinitions) {
                for (let ri = 0; ri < data.rootDefinitions.length; ri++) {
                    const r = data.rootDefinitions[ri];
                    const hit = r.depEntries && r.depEntries.find(function (dep) {
                        return dep.depTableName === 'inv_comp' && dep.key === selectedComp;
                    });
                    if (hit) {
                        compEntry = hit;
                        break;
                    }
                }
            }
            if (!compEntry && data && data.subItemsByTable && data.subItemsByTable.inv_comp && data.subItemsByTable.inv_comp.items) {
                const fullComp = data.subItemsByTable.inv_comp.items.find(item => (item.key || item.inv_comp) === selectedComp);
                if (fullComp) {
                    compEntry = { key: fullComp.key || fullComp.inv_comp, basetags: fullComp.basetags, item: fullComp, rarity: fullComp.rarity, basecomposition: fullComp.basecomposition };
                }
            }
            if (!compEntry) {
                return null;
            }
            
            // Debug logging for class mods
            const isClassModRoot = root.key && root.key.toLowerCase().includes('classmod');
            if (isClassModRoot) {
                console.log(`[ClassMod Debug] getSelectedCompRules called for root "${root.key}"`, {
                    selectedComp: selectedComp,
                    compEntry: compEntry ? {
                        key: compEntry.key,
                        basecomposition: compEntry.basecomposition,
                        hasParttypeselectionrules: !!(compEntry.parttypeselectionrules || compEntry.item?.parttypeselectionrules)
                    } : null
                });
            }
            
            // Helper function to format tags
            const formatTags = (tags) => {
                if (!tags || !Array.isArray(tags)) return [];
                return tags.map(tag => {
                    if (typeof tag === 'object' && tag !== null) {
                        const values = Object.values(tag);
                        return values[0] || Object.keys(tag)[0] || '';
                    }
                    return tag;
                }).filter(Boolean);
            };

            const addRarityTagToPool = (entry, pool) => {
                if (!entry || !pool) return;
                const rarityRaw = String(entry.rarity || entry.item?.rarity || '').toLowerCase();
                if (!rarityRaw) return;
                if (/\bpearlescent\b/i.test(rarityRaw)) pool.add('pearlescent');
                if (rarityRaw.includes('legendary')) pool.add('legendary');
                else if (rarityRaw.includes('epic')) pool.add('epic');
                else if (rarityRaw.includes('rare')) pool.add('rare');
                else if (rarityRaw.includes('uncommon')) pool.add('uncommon');
                else if (rarityRaw.includes('common')) pool.add('common');
            };
            
            // Build the current tag pool starting with basetags from comp
            // Also inherit basetags from basecomposition (for comp_* inheritance)
            const tagPool = new Set();
            const baseTags = formatTags(compEntry.basetags || compEntry.item?.basetags || []);
            baseTags.forEach(tag => tagPool.add(tag.toLowerCase()));
            addRarityTagToPool(compEntry, tagPool);
            // Fallback: resolve full comp from global inv_comp list so pearlescent/rarity is set when dep entry is shallow (e.g. from another root)
            if (data && data.subItemsByTable && data.subItemsByTable.inv_comp && data.subItemsByTable.inv_comp.items) {
                const fullComp = data.subItemsByTable.inv_comp.items.find(item => (item.key || item.inv_comp) === selectedComp);
                if (fullComp) {
                    formatTags(fullComp.basetags || fullComp.item?.basetags || []).forEach(tag => tagPool.add(tag.toLowerCase()));
                    addRarityTagToPool(fullComp, tagPool);
                }
            }
            
            // Inherit basetags from basecomposition if it exists
            // This ensures manufacturer comps inherit basetags from their base comp (e.g., comp_03_rare)
            const baseComp = compEntry.basecomposition || compEntry.item?.basecomposition;
            if (baseComp && typeof baseComp === 'string') {
                // Extract comp key from basecomposition (supports both base_comp_* and comp_*)
                // Format: inv'RootType.comp_key' or inv'RootType.base_comp_key'
                const m = baseComp.match(/inv'[^']+\.([^']+)'/i);
                let baseCompKey = m ? m[1] : null;
                
                // Fallback: extract substring after last '.' and before ending quote
                if (!baseCompKey) {
                    const fallbackMatch = baseComp.match(/\.([^']+)'?$/);
                    if (fallbackMatch && fallbackMatch[1]) {
                        baseCompKey = fallbackMatch[1];
                    }
                }
                    
                if (baseCompKey) {
                    // Find the base comp entry to get its basetags
                    let baseCompEntry = root.depEntries?.find(dep => dep.depTableName === 'inv_comp' && dep.key === baseCompKey);
                    if (!baseCompEntry && data && data.rootDefinitions) {
                        for (const r of data.rootDefinitions) {
                            baseCompEntry = r.depEntries?.find(dep => dep.depTableName === 'inv_comp' && dep.key === baseCompKey);
                            if (baseCompEntry) break;
                        }
                    }
                    if (!baseCompEntry && data && data.subItemsByTable && data.subItemsByTable.inv_comp) {
                        baseCompEntry = data.subItemsByTable.inv_comp.items?.find(item => item.key === baseCompKey);
                    }
                    
                    // Merge inherited basetags into tag pool
                    if (baseCompEntry) {
                        const inheritedBaseTags = formatTags(baseCompEntry.basetags || baseCompEntry.item?.basetags || []);
                        inheritedBaseTags.forEach(tag => tagPool.add(tag.toLowerCase()));
                        addRarityTagToPool(baseCompEntry, tagPool);
                    }
                }
            }
            
            const baseGenericRootForTagPool = getBaseGenericRootForPartLookup(root, data);
            const findPartEntry = (partKey) => findPartDepEntryForRootContext(root, partKey, data, baseGenericRootForTagPool);
            
            // Add tags from selected parts (in order)
            // Process parts in selection order to build tag pool incrementally
            const selectedPartsList = Array.from(selectedKeys);
            
            // First pass: add tags from parts that have no dependencies or dependencies are already met
            selectedPartsList.forEach(partKey => {
                // Skip the comp itself
                if (partKey === selectedComp) return;
                
                const partEntry = findPartEntry(partKey);
                if (partEntry) {
                    const addTags = formatTags(partEntry.addtags || []);
                    const depTags = formatTags(partEntry.dependencytags || []);
                    const exclTags = formatTags(partEntry.exclusiontags || []);
                    
                    // Check if part is valid with current tag pool
                    // IMPORTANT: Don't check exclusions against tags this part itself would add
                    // Create a temporary pool that includes this part's addtags to check
                    const tempPool = new Set(tagPool);
                    addTags.forEach(tag => tempPool.add(tag.toLowerCase()));
                    
                    // Check exclusions against the pool WITHOUT this part's own tags
                    // (exclude tags that this part adds from the exclusion check)
                    const partAddTagsSet = new Set(addTags.map(t => t.toLowerCase()));
                    const hasExclusionConflict = exclTags.some(tag => {
                        const tagLower = tag.toLowerCase();
                        // Don't exclude if the conflicting tag is one this part adds
                        if (partAddTagsSet.has(tagLower)) return false;
                        return tagPool.has(tagLower);
                    });
                    const hasDependency = depTags.length === 0 || depTags.some(tag => tagPool.has(tag.toLowerCase()));
                    
                    // Add to tag pool if valid (dependencies met and no external exclusion conflicts)
                    if (!hasExclusionConflict && hasDependency) {
                        addTags.forEach(tag => tagPool.add(tag.toLowerCase()));
                    }
                }
            });
            
            // Second pass: try to add tags from parts that might now be valid after first pass
            // This handles cases where parts depend on tags added by other parts
            let changed = true;
            let iterations = 0;
            while (changed && iterations < 10) { // Max 10 iterations to prevent infinite loops
                changed = false;
                const poolSizeBefore = tagPool.size;
                
                selectedPartsList.forEach(partKey => {
                    if (partKey === selectedComp) return;
                    
                    const partEntry = findPartEntry(partKey);
                    if (partEntry) {
                        const addTags = formatTags(partEntry.addtags || []);
                        const depTags = formatTags(partEntry.dependencytags || []);
                        const exclTags = formatTags(partEntry.exclusiontags || []);
                        
                        // Check if part is valid with current tag pool
                        // Don't exclude based on tags this part itself adds
                        const partAddTagsSet = new Set(addTags.map(t => t.toLowerCase()));
                        const hasExclusionConflict = exclTags.some(tag => {
                            const tagLower = tag.toLowerCase();
                            // Don't exclude if the conflicting tag is one this part adds
                            if (partAddTagsSet.has(tagLower)) return false;
                            return tagPool.has(tagLower);
                        });
                        const hasDependency = depTags.length === 0 || depTags.some(tag => tagPool.has(tag.toLowerCase()));
                        
                        // Add to tag pool if valid and not already added
                        if (!hasExclusionConflict && hasDependency) {
                            addTags.forEach(tag => {
                                const tagLower = tag.toLowerCase();
                                if (!tagPool.has(tagLower)) {
                                    tagPool.add(tagLower);
                                    changed = true;
                                }
                            });
                        }
                    }
                });
                
                iterations++;
                if (tagPool.size === poolSizeBefore) {
                    changed = false; // No change, exit loop
                }
            }
            
            // Extract allowed parts from rules
            const allowedParts = new Map(); // slotName -> Set of part keys
            const forcedParts = new Map(); // slotName -> Set of forced part keys
            const partCounts = new Map(); // slotName -> {min, max}
            const slotConstraints = new Map(); // normalizedSlot -> {slotName, allowedParts, source, inherited, singletonLocked}
            const normalizeSlotConstraintKey = (slotName) => {
                const normalized = normalizeDepTableName(slotName);
                if (normalized && normalized !== 'unknown') return normalized;
                return String(slotName || '').toLowerCase();
            };
            const extractRulePairs = (rawRules) => {
                if (!rawRules || !rawRules.pairs) return [];
                if (Array.isArray(rawRules.pairs)) return rawRules.pairs;
                if (typeof rawRules.pairs === 'object') return Object.values(rawRules.pairs);
                return [];
            };
            const extractAllowedPartSet = (parts) => {
                const set = new Set();
                if (!Array.isArray(parts)) return set;
                parts.forEach(partObj => {
                    if (typeof partObj === 'string') {
                        set.add(partObj);
                    } else if (partObj && typeof partObj === 'object') {
                        if (partObj.part) set.add(partObj.part);
                        else if (partObj.key) set.add(partObj.key);
                    }
                });
                return set;
            };
            const applyRuleSetToSlotConstraints = (rawRules, source, inherited) => {
                const pairs = extractRulePairs(rawRules);
                pairs.forEach(pair => {
                    if (!pair || !pair.key) return;
                    const slotName = pair.key;
                    const nextAllowedSet = extractAllowedPartSet((pair.value || {}).parts || []);
                    if (nextAllowedSet.size === 0) return;
                    const slotKey = normalizeSlotConstraintKey(slotName);
                    slotConstraints.set(slotKey, {
                        slotName,
                        normalizedSlot: slotKey,
                        allowedParts: nextAllowedSet,
                        source,
                        inherited: !!inherited,
                        singletonLocked: nextAllowedSet.size === 1
                    });
                });
            };
            
            // Helper function to recursively find parttypeselectionrules up the base composition hierarchy
            function findPartTypeSelectionRules(compKey, visited = new Set(), preferredRootType = null) {
                if (visited.has(compKey)) return null; // Prevent infinite loops
                visited.add(compKey);

                const preferredRootTypeRaw = String(preferredRootType || '').toLowerCase().trim();
                const normalizedPreferredRootType = normalizeItemTypeFamily(preferredRootTypeRaw);
                const getRootItemTypeFamily = (candidateRoot) => {
                    if (!candidateRoot) return '';
                    const rawBasetype = String(
                        candidateRoot.basetype ||
                        candidateRoot.data?.basetype ||
                        ''
                    ).toLowerCase();
                    const basetypeMatch = rawBasetype.match(/inv'([^']+)/i);
                    if (basetypeMatch && basetypeMatch[1]) {
                        return normalizeItemTypeFamily(basetypeMatch[1]);
                    }
                    return normalizeItemTypeFamily((candidateRoot.key || '').toLowerCase());
                };
                const matchesPreferredRoot = (candidateRoot) => {
                    if (!preferredRootTypeRaw) return true;
                    const candidateRootKey = String(candidateRoot?.key || '').toLowerCase();
                    if (candidateRootKey && candidateRootKey === preferredRootTypeRaw) {
                        return true;
                    }
                    return getRootItemTypeFamily(candidateRoot) === normalizedPreferredRootType;
                };
                
                // First check global base comps storage (with item type)
                if (window.globalBaseComps) {
                    // Try item-type-specific key first
                    const uniqueKey = `${normalizeItemTypeFamily(itemType)}_${compKey}`;
                    if (window.globalBaseComps.has(uniqueKey)) {
                        const globalComp = window.globalBaseComps.get(uniqueKey);
                        if (globalComp.rules && globalComp.rules.pairs) {
                            return globalComp.rules;
                        }
                    }
                    // Fallback to generic key
                    if (window.globalBaseComps.has(compKey)) {
                        const globalComp = window.globalBaseComps.get(compKey);
                        if (globalComp.rules && globalComp.rules.pairs) {
                            return globalComp.rules;
                        }
                    }
                }
                
                // Find the comp entry
                // CRITICAL: If preferredRootType is specified (from basecomposition string), prioritize that root
                // For example: if basecomposition says inv'grenade_gadget.base_comp_01_common', find it in grenade_gadget, not weapon
                let compEntry = null;
                
                // First, if preferredRootType is specified, search in that root first
                if (preferredRootTypeRaw && data && data.rootDefinitions) {
                    const preferredRoot = data.rootDefinitions.find(r => {
                        return matchesPreferredRoot(r);
                    });
                    if (preferredRoot && preferredRoot.depEntries) {
                        compEntry = preferredRoot.depEntries.find(dep => dep.depTableName === 'inv_comp' && dep.key === compKey);
                    }
                }
                
                // Second, check if we have a base generic root and search there (avoid manufacturer root comps that might have circular refs)
                if (!compEntry && baseGenericRoot && baseGenericRoot !== root && baseGenericRoot.depEntries) {
                    compEntry = baseGenericRoot.depEntries.find(dep => dep.depTableName === 'inv_comp' && dep.key === compKey);
                }
                
                // If not found in base generic root, check current root
                if (!compEntry) {
                    compEntry = root.depEntries?.find(dep => dep.depTableName === 'inv_comp' && dep.key === compKey);
                }
                
                // If still not found, search all roots (but skip manufacturer roots if we're looking for a base comp)
                if (!compEntry && data && data.rootDefinitions) {
                    for (const r of data.rootDefinitions) {
                        const rKeyLower = (r.key || '').toLowerCase();
                        
                        // If preferredRootType is specified, skip roots that don't match
                        if (preferredRootTypeRaw && !matchesPreferredRoot(r)) {
                            continue;
                        }
                        
                        // Skip manufacturer roots if we're searching for a comp that should be in base generic root
                        if (baseGenericRoot && baseGenericRoot !== root && rKeyLower !== (baseGenericRoot.key || '').toLowerCase()) {
                            // Only check if this root is not a manufacturer variant of the base generic root
                            const isManufacturerVariant = baseGenericRoot && 
                                rKeyLower.includes((baseGenericRoot.key || '').toLowerCase()) && 
                                rKeyLower !== (baseGenericRoot.key || '').toLowerCase();
                            if (isManufacturerVariant) continue;
                        }
                        compEntry = r.depEntries?.find(dep => dep.depTableName === 'inv_comp' && dep.key === compKey);
                        if (compEntry) break;
                    }
                }
                
                // If not found, search in subItemsByTable
                if (!compEntry && data && data.subItemsByTable && data.subItemsByTable.inv_comp) {
                    compEntry = data.subItemsByTable.inv_comp.items?.find(item => item.key === compKey);
                }
                
                if (!compEntry) return null;
                
                // Check if this comp has parttypeselectionrules
                const rules = compEntry.parttypeselectionrules || compEntry.item?.parttypeselectionrules;
                if (rules && rules.pairs) {
                    return rules;
                }
                
                // If no rules, check basecomposition and recurse
                const baseComp = compEntry.basecomposition || compEntry.item?.basecomposition;
                if (baseComp && typeof baseComp === 'string') {
                    // Extract root type and comp key from basecomposition string
                    // Supports both formats:
                    //   - inv'Weapon.base_comp_01_common' (base_comp_*)
                    //   - inv'Enhancement.comp_03_rare' (comp_*)
                    //   - inv'grenade_gadget.comp_05_legendary' (grenade_gadget root type)
                    const m = baseComp.match(/inv'([^']+)\.([^']+)'/i);
                    let baseRootTypeFromComp = null;
                    let baseCompKey = null;
                    
                    if (m) {
                        baseRootTypeFromComp = m[1].toLowerCase(); // Extract root type (e.g., "grenade_gadget")
                        baseCompKey = m[2]; // Extract comp key (e.g., "base_comp_01_common")
                    } else {
                        // Fallback: extract substring after last '.' and before ending quote
                        const fallbackMatch = baseComp.match(/\.([^']+)'?$/);
                        if (fallbackMatch && fallbackMatch[1]) {
                            baseCompKey = fallbackMatch[1];
                        }
                    }
                    
                    if (baseCompKey) {
                        // FIRST: Check for self-reference BEFORE visited check
                        // If baseCompKey equals compKey, it's pointing to itself (invalid data)
                        // Example: comp_05_legendary with basecomposition: inv'grenade_gadget.comp_05_legendary' pointing to itself
                        if (baseCompKey === compKey) {
                            // Self-reference - stop recursion, no additional rules to inherit
                            // The comp entry already has its rules checked above - if no rules exist, there's nothing to inherit
                            return null;
                        }
                        // SECOND: Prevent infinite loops - check if we've already visited this comp
                        if (visited.has(baseCompKey)) {
                            console.warn(`[basecomposition] Circular reference detected for compKey: ${baseCompKey} (from basecomposition: ${baseComp}, resolving: ${compKey})`);
                            return null;
                        }
                        // Pass the root type from basecomposition to guide the search
                        return findPartTypeSelectionRules(baseCompKey, visited, baseRootTypeFromComp || normalizedPreferredRootType);
                    } else {
                        // Could not parse basecomposition
                        console.warn(`[basecomposition] Could not parse basecomposition string: "${baseComp}" (compKey: ${compKey})`);
                    }
                }
                
                return null;
            }
            
            // Get parttypeselectionrules - first from compEntry, then recursively from base composition
            // Also check base generic root (e.g., "enhancement" for "atl_enhancement")
            let rules = compEntry.parttypeselectionrules || compEntry.item?.parttypeselectionrules;
            
            // Extract base item type from manufacturer roots (e.g., "atl_enhancement" -> "enhancement")
            // Universal roots apply to all manufacturer-specific counterparts
            const baseItemTypePatterns = ['weapon', 'enhancement', 'shield', 'repair_kit', 'grenade_gadget', 'heavy_weapon_gadget', 'turret_gadget', 'terminal_gadget', 'grenade', 'artifact', 'classmod'];
            let baseItemType = normalizeItemTypeFamily(itemType);
            const rootKeyLower = (root.key || '').toLowerCase();
            
            // Check if current root is a manufacturer-specific variant
            // Manufacturer roots typically have format: manufacturer_itemtype (e.g., "atl_enhancement", "dad_enhancement")
            // Or manufacturer_itemtype_variant (e.g., "cov_enhancement")
            // Special handling: terminal_combat, terminal_*, turret_* should map to terminal_gadget/turret_gadget
            if (rootKeyLower.includes('terminal_') && !rootKeyLower.includes('terminal_gadget')) {
                // Map terminal_combat, terminal_*, etc. to terminal_gadget
                if (rootKeyLower.includes('terminal_combat') || rootKeyLower.includes('terminal_')) {
                    baseItemType = 'terminal_gadget';
                }
            } else if (rootKeyLower.includes('turret_') && !rootKeyLower.includes('turret_gadget')) {
                // Map turret_combat, turret_*, etc. to turret_gadget
                if (rootKeyLower.includes('turret_combat') || rootKeyLower.includes('turret_')) {
                    baseItemType = 'turret_gadget';
                }
            } else {
                for (const pattern of baseItemTypePatterns) {
                    if (rootKeyLower.includes(pattern) && rootKeyLower !== pattern) {
                        // Extract the base type (e.g., "atl_enhancement" -> "enhancement")
                        // Try to match pattern at the end of the string (after manufacturer prefix)
                        const match = rootKeyLower.match(new RegExp(`(?:\\w+_)?(${pattern})(?:\\w+)?$`, 'i'));
                        if (match && match[1]) {
                            baseItemType = match[1].toLowerCase();
                            break;
                        }
                    } else if (rootKeyLower === pattern || itemType === pattern) {
                        baseItemType = pattern;
                        break;
                    }
                }
            }
            
            // Also check basetype for base item type extraction
            // Reuse rootBasetype from earlier in the function (already declared at line 2360)
            const rootBasetypeLower = (rootBasetype || '').toLowerCase();
            if (rootBasetypeLower) {
                const basetypeMatch = rootBasetypeLower.match(/inv'(\w+)/i);
                if (basetypeMatch) {
                    const basetypeItemType = basetypeMatch[1].toLowerCase();
                    
                    // Special handling: terminal_gadget_combat, terminal_gadget_*, turret_gadget_* should map to terminal_gadget/turret_gadget
                    if (basetypeItemType.includes('terminal_gadget') && basetypeItemType !== 'terminal_gadget') {
                        // Map terminal_gadget_combat, terminal_gadget_*, etc. to terminal_gadget
                        baseItemType = 'terminal_gadget';
                    } else if (basetypeItemType.includes('turret_gadget') && basetypeItemType !== 'turret_gadget') {
                        // Map turret_gadget_combat, turret_gadget_*, etc. to turret_gadget
                        baseItemType = 'turret_gadget';
                    } else {
                        // Extract base type from basetype (e.g., "Enhancement" -> "enhancement")
                        for (const pattern of baseItemTypePatterns) {
                            if (basetypeItemType.includes(pattern) && basetypeItemType !== pattern) {
                                const match = basetypeItemType.match(new RegExp(`(?:\\w+_)?(${pattern})(?:\\w+)?$`, 'i'));
                                    if (match && match[1]) {
                                        baseItemType = normalizeItemTypeFamily(match[1].toLowerCase());
                                    break;
                                }
                            } else if (basetypeItemType === pattern) {
                                    baseItemType = normalizeItemTypeFamily(pattern);
                                break;
                            }
                        }
                    }
                }
            }
            
            // Find base generic root (e.g., "enhancement" for "atl_enhancement", "dad_enhancement", "cov_enhancement")
            // Universal root definitions apply to ALL manufacturer-specific counterparts
            let baseGenericRoot = null;
            
            // Always try to find base generic root if current root key doesn't match baseItemType exactly
            if (rootKeyLower !== baseItemType) {
                if (data && data.rootDefinitions) {
                    // For repair_kit, prefer the canonical root key "repair_kit" (Nexus inv0 ~13118), not default_repair_kit (which has default_repkit_comp_* and restricts payload to part_payload_small).
                    if (baseItemType === 'repair_kit') {
                        baseGenericRoot = data.rootDefinitions.find(r => (r.key || '').toLowerCase() === 'repair_kit') || null;
                    }
                    // For weapon (and weapon_*), prefer the canonical root key "weapon" so we can merge generic weapon barrel_acc (and other) parts into allowed set for manufacturer roots.
                    if (!baseGenericRoot && (baseItemType === 'weapon' || (baseItemType && baseItemType.toLowerCase().startsWith('weapon')))) {
                        baseGenericRoot = data.rootDefinitions.find(r => (r.key || '').toLowerCase() === 'weapon') || null;
                    }
                    // Prefer canonical root key (e.g. grenade_gadget) over manufacturer roots (borg_grenade_gadget, ord_grenade_gadget):
                    // those also satisfy basetype-derived baseItemType and would otherwise win on array order, dropping generic comp rules (payload 0/0 for uncommon).
                    if (!baseGenericRoot && baseItemType) {
                        baseGenericRoot = data.rootDefinitions.find(r => (r.key || '').toLowerCase() === baseItemType) || null;
                    }
                    if (!baseGenericRoot) {
                        baseGenericRoot = data.rootDefinitions.find(r => {
                            const rKey = (r.key || '').toLowerCase();
                            const rBasetype = (r.basetype || '').toLowerCase();
                            
                            // Match if key is exactly the baseItemType (e.g., "enhancement")
                            if (rKey === baseItemType) {
                                return true;
                            }
                            
                            // Or if basetype matches (e.g., "inv'enhancement'" or "inv'Enhancement'")
                            const basetypeMatch = rBasetype.match(/inv'(\w+)/i);
                            if (basetypeMatch) {
                                const basetypeItemType = basetypeMatch[1].toLowerCase();
                                // Extract base type from basetype if it's a manufacturer root
                                let basetypeBaseType = basetypeItemType;
                                for (const pattern of baseItemTypePatterns) {
                                    if (basetypeItemType.includes(pattern) && basetypeItemType !== pattern) {
                                        const match = basetypeItemType.match(new RegExp(`(?:\\w+_)?(${pattern})(?:\\w+)?$`, 'i'));
                                        if (match && match[1]) {
                                            basetypeBaseType = match[1].toLowerCase();
                                            break;
                                        }
                                    } else if (basetypeItemType === pattern) {
                                        basetypeBaseType = pattern;
                                        break;
                                    }
                                }
                                return basetypeBaseType === baseItemType;
                            }
                            
                            return false;
                        });
                    }
                }
            } else {
                // Current root IS the base generic root
                baseGenericRoot = root;
            }
            
            // If we have a base generic root, get its comp rules first
            let baseRules = null;
            if (baseGenericRoot && baseGenericRoot.depEntries) {
                // Find the same comp in the base generic root (e.g., comp_01_common)
                // DO NOT fallback to other rarity levels - only use explicit matches
                let baseCompEntry = baseGenericRoot.depEntries.find(dep => 
                    dep.depTableName === 'inv_comp' && dep.key === selectedComp
                );
                
                if (baseCompEntry) {
                    baseRules = baseCompEntry.parttypeselectionrules || (baseCompEntry.item && baseCompEntry.item.parttypeselectionrules);
                }
                // Manufacturer repkit (e.g. tor_repair_kit) uses comp keys like comp_05_legendary_shinywarpaint;
                // base generic root only has comp_05_legendary. Resolve base comp key from basecomposition and use it for baseRules.
                var isRepkitMfrRoot = baseItemType === 'repair_kit' || (rootKeyLower && (rootKeyLower.includes('repair') || rootKeyLower.includes('repkit')));
                if (!baseRules && root !== baseGenericRoot && isRepkitMfrRoot) {
                    var baseCompRef = compEntry.basecomposition || (compEntry.item && compEntry.item.basecomposition);
                    if (baseCompRef && typeof baseCompRef === 'string') {
                        var baseCompKeyMatch = baseCompRef.match(new RegExp("inv'[^']+\\.([^']+)'", 'i'));
                        var baseCompKey = baseCompKeyMatch ? baseCompKeyMatch[1] : (baseCompRef.match(/\.([^']+)'?$/) || [])[1];
                        if (baseCompKey) {
                            baseCompEntry = baseGenericRoot.depEntries.find(function (dep) {
                                return dep.depTableName === 'inv_comp' && dep.key === baseCompKey;
                            });
                            // Data may use different key format (e.g. default_repkit_comp_01 instead of comp_01_common)
                            if (!baseCompEntry && baseGenericRoot.key) {
                                var baseKey = (baseGenericRoot.key || '').toLowerCase();
                                var rarityNum = (baseCompKey.match(/\d+/) || [])[0];
                                var altKey = (baseKey.indexOf('default_repair_kit') !== -1 ? 'default_repkit_comp_' + rarityNum : baseKey + '_comp_' + rarityNum);
                                baseCompEntry = baseGenericRoot.depEntries.find(function (dep) {
                                    return dep.depTableName === 'inv_comp' && (dep.key === altKey || (dep.key && dep.key.indexOf('_comp_' + rarityNum) !== -1));
                                });
                            }
                            // If still no match (e.g. only default_repkit_comp_01 exists), use first inv_comp to get full slot list (secondary_augment, element, etc.)
                            if (!baseCompEntry && baseGenericRoot.depEntries) {
                                baseCompEntry = baseGenericRoot.depEntries.find(function (dep) {
                                    return dep.depTableName === 'inv_comp';
                                });
                            }
                            if (baseCompEntry) {
                                baseRules = baseCompEntry.parttypeselectionrules || (baseCompEntry.item && baseCompEntry.item.parttypeselectionrules);
                            }
                        }
                    }
                }
            }
            
            // If no rules in current comp, try base composition hierarchy
            // Also check basecomposition for inheritance (e.g., comp_03_rare from basecomposition)
            let baseCompositionRules = null;
            if (!rules || !rules.pairs) {
                rules = findPartTypeSelectionRules(selectedComp, new Set(), baseItemType || null);
                // Safety fallback: preserve legacy behavior if family-scoped lookup misses.
                if (!rules || !rules.pairs) {
                    rules = findPartTypeSelectionRules(selectedComp, new Set(), null);
                }
            }
            
            // ALWAYS check basecomposition for inheritance - even if current comp has rules
            // This ensures base comp rules are included for repair kits and other items
            const baseCompForRules = compEntry.basecomposition || compEntry.item?.basecomposition;
            if (baseCompForRules && typeof baseCompForRules === 'string') {
                    // Extract comp key from basecomposition (supports both base_comp_* and comp_*)
                    const m = baseCompForRules.match(/inv'[^']+\.([^']+)'/i);
                    let baseCompKey = m ? m[1] : null;
                    
                    // Extract root type from basecomposition (e.g., "grenade_gadget" from "inv'grenade_gadget.comp_01_common'")
                    const rootTypeMatch = baseCompForRules.match(/inv'([^']+)\./i);
                    const baseRootType = rootTypeMatch ? rootTypeMatch[1].toLowerCase() : null;
                    
                    // Fallback: extract substring after last '.' and before ending quote
                    if (!baseCompKey) {
                        const fallbackMatch = baseCompForRules.match(/\.([^']+)'?$/);
                        if (fallbackMatch && fallbackMatch[1]) {
                            baseCompKey = fallbackMatch[1];
                        }
                    }
                    if (baseCompKey && baseCompKey !== selectedComp) {
                        // If basecomposition points to a different root type, try to find it in the base generic root first.
                        // Also enter when comp points to a manufacturer (baseRootType e.g. vla_sr) and a weapon root exists, so the weapon chain can merge base + manufacturer rules (baseGenericRoot may be wrong e.g. heavy_weapon_gadget).
                        const hasWeaponRoot = data && data.rootDefinitions && data.rootDefinitions.some(r => (r.key || '').toLowerCase() === 'weapon');
                        const isWeaponManufacturerCase = baseRootType && baseRootType !== 'weapon' && hasWeaponRoot && data && data.rootDefinitions;
                        if ((baseRootType && baseRootType !== rootKeyLower) || isWeaponManufacturerCase) {
                            // Shield chain: unique comp -> type root (energy_shield/armor_shield) -> base Shield root. Resolve type comp first, then if it points to Shield, use Shield root's comp rules.
                            const isShieldTypeRoot = baseRootType === 'energy_shield' || baseRootType === 'armor_shield';
                            if (isShieldTypeRoot && data && data.rootDefinitions) {
                                const typeShieldRoot = data.rootDefinitions.find(r => (r.key || '').toLowerCase() === baseRootType);
                                if (typeShieldRoot && typeShieldRoot.depEntries) {
                                    const typeCompEntry = typeShieldRoot.depEntries.find(dep =>
                                        dep.depTableName === 'inv_comp' && dep.key === baseCompKey
                                    );
                                    if (typeCompEntry) {
                                        const typeCompBaseRef = typeCompEntry.basecomposition || typeCompEntry.item?.basecomposition;
                                        const shieldBaseMatch = typeCompBaseRef && typeof typeCompBaseRef === 'string' && typeCompBaseRef.match(/inv'([^']+)\.([^']+)'/i);
                                        const typeBaseRoot = shieldBaseMatch && shieldBaseMatch[1] ? shieldBaseMatch[1].toLowerCase() : null;
                                        const typeBaseCompKey = shieldBaseMatch && shieldBaseMatch[2] ? shieldBaseMatch[2] : null;
                                        if (typeBaseRoot === 'shield' && typeBaseCompKey && baseGenericRoot && baseGenericRoot.depEntries) {
                                            const shieldBaseCompEntry = baseGenericRoot.depEntries.find(dep =>
                                                dep.depTableName === 'inv_comp' && dep.key === typeBaseCompKey
                                            );
                                            if (shieldBaseCompEntry) {
                                                const shieldBaseRules = shieldBaseCompEntry.parttypeselectionrules || shieldBaseCompEntry.item?.parttypeselectionrules;
                                                if (shieldBaseRules && shieldBaseRules.pairs) {
                                                    baseCompositionRules = shieldBaseRules;
                                                }
                                            }
                                        }
                                        if (!baseCompositionRules) {
                                            baseCompositionRules = findPartTypeSelectionRules(baseCompKey, new Set([selectedComp]), baseRootType || baseItemType || null);
                                        }
                                    } else {
                                        baseCompositionRules = findPartTypeSelectionRules(baseCompKey, new Set([selectedComp]), baseRootType || baseItemType || null);
                                    }
                                } else {
                                    baseCompositionRules = findPartTypeSelectionRules(baseCompKey, new Set([selectedComp]), baseRootType || baseItemType || null);
                                }
                            } else if (baseRootType && baseRootType !== 'weapon' && data && data.rootDefinitions) {
                                // Weapon chain: manufacturer root -> manufacturer comp -> if comp points to Weapon.base_comp_*, merge base comp rules with manufacturer comp rules. Use resolved weapon root (baseGenericRoot may be wrong e.g. heavy_weapon_gadget).
                                const weaponRoot = (baseGenericRoot && (baseGenericRoot.key || '').toLowerCase() === 'weapon') ? baseGenericRoot : data.rootDefinitions.find(r => (r.key || '').toLowerCase() === 'weapon');
                                if (weaponRoot && weaponRoot.depEntries) {
                                const manufacturerRoot = data.rootDefinitions.find(r => (r.key || '').toLowerCase() === baseRootType);
                                if (manufacturerRoot && manufacturerRoot.depEntries) {
                                    const manufacturerCompEntry = manufacturerRoot.depEntries.find(dep =>
                                        dep.depTableName === 'inv_comp' && dep.key === baseCompKey
                                    );
                                    if (manufacturerCompEntry) {
                                        const mfrBaseRef = manufacturerCompEntry.basecomposition || manufacturerCompEntry.item?.basecomposition;
                                        const weaponBaseMatch = mfrBaseRef && typeof mfrBaseRef === 'string' && mfrBaseRef.match(/inv'([^']+)\.([^']+)'/i);
                                        const weaponBaseRoot = weaponBaseMatch && weaponBaseMatch[1] ? weaponBaseMatch[1].toLowerCase() : null;
                                        const weaponBaseCompKey = weaponBaseMatch && weaponBaseMatch[2] ? weaponBaseMatch[2] : null;
                                        if (weaponBaseRoot === 'weapon' && weaponBaseCompKey && weaponRoot.depEntries) {
                                            const weaponBaseCompEntry = weaponRoot.depEntries.find(dep =>
                                                dep.depTableName === 'inv_comp' && dep.key === weaponBaseCompKey
                                            );
                                            if (weaponBaseCompEntry) {
                                                const baseRulesWeapon = weaponBaseCompEntry.parttypeselectionrules || weaponBaseCompEntry.item?.parttypeselectionrules;
                                                const manufacturerRulesWeapon = manufacturerCompEntry.parttypeselectionrules || manufacturerCompEntry.item?.parttypeselectionrules;
                                                if (baseRulesWeapon && baseRulesWeapon.pairs) {
                                                    const basePairsArr = Array.isArray(baseRulesWeapon.pairs) ? baseRulesWeapon.pairs : Object.values(baseRulesWeapon.pairs);
                                                    const mfrPairsArr = (manufacturerRulesWeapon && manufacturerRulesWeapon.pairs) ? (Array.isArray(manufacturerRulesWeapon.pairs) ? manufacturerRulesWeapon.pairs : Object.values(manufacturerRulesWeapon.pairs)) : [];
                                                    const slotMap = new Map();
                                                    basePairsArr.forEach(pair => { if (pair && pair.key) slotMap.set(pair.key, pair); });
                                                    mfrPairsArr.forEach(pair => { if (pair && pair.key) slotMap.set(pair.key, pair); });
                                                    const mergedPairs = {};
                                                    let idx = 0;
                                                    slotMap.forEach(pair => { mergedPairs[`pair${idx++}`] = pair; });
                                                    baseCompositionRules = { pairs: mergedPairs };
                                                }
                                            }
                                        }
                                    }
                                }
                                }
                            }
                            if (!baseCompositionRules && baseRootType === 'heavy_weapon_gadget' && data && data.rootDefinitions) {
                                const hwRoot = data.rootDefinitions.find(r => (r.key || '').toLowerCase() === 'heavy_weapon_gadget');
                                if (hwRoot && hwRoot.depEntries) {
                                    const hwCompEntry = hwRoot.depEntries.find(dep =>
                                        dep.depTableName === 'inv_comp' && dep.key === baseCompKey
                                    );
                                    if (hwCompEntry) {
                                        const hwRules = hwCompEntry.parttypeselectionrules || hwCompEntry.item?.parttypeselectionrules;
                                        if (hwRules && hwRules.pairs) {
                                            baseCompositionRules = hwRules;
                                        }
                                    }
                                }
                            }
                            if (!baseCompositionRules) {
                                // Try to find the comp in the base generic root
                                if (baseGenericRoot && baseGenericRoot.depEntries) {
                                    const baseCompEntry = baseGenericRoot.depEntries.find(dep =>
                                        dep.depTableName === 'inv_comp' && dep.key === baseCompKey
                                    );

                                    if (baseCompEntry) {
                                        const baseCompRules = baseCompEntry.parttypeselectionrules || baseCompEntry.item?.parttypeselectionrules;
                                        if (baseCompRules && baseCompRules.pairs) {
                                            baseCompositionRules = baseCompRules;
                                        } else {
                                            // Comp exists but has no rules - recurse to find base
                                            baseCompositionRules = findPartTypeSelectionRules(baseCompKey, new Set([selectedComp]), baseRootType || baseItemType || null);
                                        }
                                    } else {
                                        // Not found in base generic root, try all roots (fallback)
                                        baseCompositionRules = findPartTypeSelectionRules(baseCompKey, new Set([selectedComp]), baseRootType || baseItemType || null);
                                    }
                                } else {
                                    // No baseGenericRoot - fallback to standard search
                                    baseCompositionRules = findPartTypeSelectionRules(baseCompKey, new Set([selectedComp]), baseRootType || baseItemType || null);
                                }
                            }
                        } else {
                            // Same root type or couldn't determine - use standard search
                            baseCompositionRules = findPartTypeSelectionRules(baseCompKey, new Set([selectedComp]), baseRootType || baseItemType || null);
                        }
                    }
                    // When basecomposition points to heavy_weapon_gadget but baseCompKey === selectedComp (e.g. vla_hw comp_04_epic),
                    // we skipped the block above; still resolve baseCompositionRules from heavy_weapon_gadget so body_acc/barrel_acc get 2-3 and 2-2.
                    if (!baseCompositionRules && baseRootType === 'heavy_weapon_gadget' && rootKeyLower !== 'heavy_weapon_gadget' && baseCompKey && data && data.rootDefinitions) {
                        const hwRoot = data.rootDefinitions.find(r => (r.key || '').toLowerCase() === 'heavy_weapon_gadget');
                        if (hwRoot && hwRoot.depEntries) {
                            const hwCompEntry = hwRoot.depEntries.find(dep =>
                                dep.depTableName === 'inv_comp' && dep.key === baseCompKey
                            );
                            if (hwCompEntry) {
                                const hwRules = hwCompEntry.parttypeselectionrules || hwCompEntry.item?.parttypeselectionrules;
                                if (hwRules && hwRules.pairs) {
                                    baseCompositionRules = hwRules;
                                }
                            }
                        }
                    }
            }

            // When root is the base generic root and comp has no basecomposition (e.g. repair_kit),
            // treat the comp's rules as baseCompositionRules so the same pipeline as shields runs.
            if (!baseCompositionRules && (root === baseGenericRoot || rootKeyLower === baseItemType) && (rules?.pairs || baseRules?.pairs)) {
                baseCompositionRules = rules || baseRules;
            }

            // Parse and normalize parttagselectionrules.max with inheritance.
            // Base comp rules are applied first; child comp rules override by tag.
            const partTagMaxRuleMap = new Map(); // tag -> { tags: [tag], max, sourceComp }
            const mergePartTagSelectionRules = (rawRules, sourceComp) => {
                if (!Array.isArray(rawRules)) return;
                rawRules.forEach(rule => {
                    if (!rule || typeof rule !== 'object') return;
                    const maxRaw = rule.max;
                    const parsedMax = maxRaw !== undefined && maxRaw !== null ? parseInt(maxRaw) : NaN;
                    if (isNaN(parsedMax)) return;
                    const tags = formatTags(rule.tags || []).map(t => String(t).toLowerCase()).filter(Boolean);
                    if (tags.length === 0) return;
                    tags.forEach(tag => {
                        partTagMaxRuleMap.set(tag, {
                            tags: [tag],
                            max: parsedMax,
                            sourceComp
                        });
                    });
                });
            };
            const findCompEntryByKey = (compKey, preferredRootType = null) => {
                if (!compKey) return null;
                let found = null;
                if (preferredRootType && data && data.rootDefinitions) {
                    const preferredRoot = data.rootDefinitions.find(r => (r.key || '').toLowerCase() === preferredRootType);
                    if (preferredRoot && preferredRoot.depEntries) {
                        found = preferredRoot.depEntries.find(dep => dep.depTableName === 'inv_comp' && dep.key === compKey);
                    }
                }
                if (!found && baseGenericRoot && baseGenericRoot.depEntries) {
                    found = baseGenericRoot.depEntries.find(dep => dep.depTableName === 'inv_comp' && dep.key === compKey);
                }
                if (!found && root.depEntries) {
                    found = root.depEntries.find(dep => dep.depTableName === 'inv_comp' && dep.key === compKey);
                }
                if (!found && data && data.rootDefinitions) {
                    for (const r of data.rootDefinitions) {
                        const rKey = (r.key || '').toLowerCase();
                        if (preferredRootType && rKey !== preferredRootType) continue;
                        found = r.depEntries?.find(dep => dep.depTableName === 'inv_comp' && dep.key === compKey);
                        if (found) break;
                    }
                }
                if (!found && data?.subItemsByTable?.inv_comp?.items) {
                    found = data.subItemsByTable.inv_comp.items.find(item => item.key === compKey);
                }
                return found || null;
            };
            const collectPartTagRulesFromCompEntry = (entry, visited = new Set()) => {
                if (!entry) return;
                const compKey = entry.key || entry.inv_comp || entry.item?.inv_comp || null;
                if (compKey && visited.has(compKey)) return;
                if (compKey) visited.add(compKey);

                const baseCompRef = entry.basecomposition || entry.item?.basecomposition;
                if (baseCompRef && typeof baseCompRef === 'string') {
                    const m = baseCompRef.match(/inv'([^']+)\.([^']+)'/i);
                    const baseRootType = m && m[1] ? m[1].toLowerCase() : null;
                    const baseCompKey = m && m[2] ? m[2] : null;
                    if (baseCompKey && baseCompKey !== compKey) {
                        const baseEntry = findCompEntryByKey(baseCompKey, baseRootType);
                        collectPartTagRulesFromCompEntry(baseEntry, visited);
                    }
                }

                const ownTagRules = entry.parttagselectionrules || entry.item?.parttagselectionrules || [];
                mergePartTagSelectionRules(ownTagRules, compKey || selectedComp || 'unknown_comp');
            };
            collectPartTagRulesFromCompEntry(compEntry);
            if (partTagMaxRuleMap.size === 0 && baseGenericRoot && baseGenericRoot !== root && selectedComp) {
                const baseSameComp = findCompEntryByKey(selectedComp, (baseGenericRoot.key || '').toLowerCase());
                collectPartTagRulesFromCompEntry(baseSameComp);
            }
            
            // If still no rules, try base generic root's comp
            if ((!rules || !rules.pairs) && baseRules && baseRules.pairs) {
                rules = baseRules;
            }
            
            // Extract part counts from baseCompositionRules FIRST - before merging
            // This ensures ALL slot types from base comp get their part counts set
            if (baseCompositionRules && baseCompositionRules.pairs) {
                let baseCompPairs = [];
                if (Array.isArray(baseCompositionRules.pairs)) {
                    baseCompPairs = baseCompositionRules.pairs;
                } else if (typeof baseCompositionRules.pairs === 'object') {
                    baseCompPairs = Object.values(baseCompositionRules.pairs);
                }
                
                // Process ALL slots from base composition and set their part counts
                // This is critical: base comp defines part counts for ALL slot types (body, core_augment, firmware, stat_group1, stat_group2, stat_group3, etc.)
                baseCompPairs.forEach(pair => {
                    if (!pair || !pair.key) return;
                    
                    const slotName = pair.key;
                    const value = pair.value || {};
                    const partCount = value.partcount || {};
                    const hasExplicitMin = partCount.min !== undefined && partCount.min !== null;
                    const hasExplicitMax = partCount.max !== undefined && partCount.max !== null;
                    
                    let minCount = null;
                    let maxCount = null;
                    
                    if (partCount.min !== undefined && partCount.min !== null) {
                        const parsed = parseInt(partCount.min);
                        if (!isNaN(parsed)) minCount = parsed;
                    }
                    if (partCount.max !== undefined && partCount.max !== null) {
                        const parsed = parseInt(partCount.max);
                        if (!isNaN(parsed)) maxCount = parsed;
                    }
                    
                    // Handle defaults
                    let effectiveMin = minCount;
                    let effectiveMax = maxCount;
                    
                    if (minCount === 0 && maxCount === null) {
                        effectiveMax = 1;
                    } else if (minCount === null && maxCount === null) {
                        const partsForCount = value.parts || [];
                        const slotNormBc = normalizeDepTableName(slotName);
                        if (partsForCount.length > 0 && slotNormBc !== 'firmware') {
                            effectiveMin = 1;
                            effectiveMax = 1;
                        } else {
                            effectiveMin = 0;
                            effectiveMax = 1;
                        }
                    } else if (minCount === null) {
                        // Max-only explicit rules imply a required lower bound of 1.
                        effectiveMin = 1;
                    } else if (maxCount === null && minCount !== 0) {
                        effectiveMax = 1;
                    }
                    
                    // Set part count for this slot from base composition
                    // Manufacturer comp can override later if it explicitly specifies partcount
                    partCounts.set(slotName, { 
                        min: effectiveMin,
                        max: effectiveMax,
                        displayMin: minCount,
                        displayMax: maxCount,
                        isExplicit: hasExplicitMin || hasExplicitMax,
                        hasExplicitMin,
                        hasExplicitMax,
                        countSource: 'basecomposition'
                    });
                    
                    // Also add parts from baseCompositionRules to allowed parts list
                    // This ensures slots like payload_augment and firmware from base comp are available
                    const parts = value.parts || [];
                    if (parts.length > 0) {
                        const baseCompAllowedSet = new Set();
                        parts.forEach(partObj => {
                            if (typeof partObj === 'string') {
                                baseCompAllowedSet.add(partObj);
                            } else if (partObj && typeof partObj === 'object') {
                                if (partObj.part) {
                                    baseCompAllowedSet.add(partObj.part);
                                } else if (partObj.key) {
                                    baseCompAllowedSet.add(partObj.key);
                                }
                            }
                        });
                        
                        if (baseCompAllowedSet.size > 0) {
                            // Merge with existing allowed parts for this slot
                            if (!allowedParts.has(slotName)) {
                                allowedParts.set(slotName, new Set());
                            }
                            baseCompAllowedSet.forEach(part => allowedParts.get(slotName).add(part));
                            
                            // If min > 0, these are forced (must be selected)
                            if (effectiveMin > 0) {
                                if (!forcedParts.has(slotName)) {
                                    forcedParts.set(slotName, new Set());
                                }
                                baseCompAllowedSet.forEach(part => forcedParts.get(slotName).add(part));
                            }
                        }
                    }
                });
            }

            // Merge basecomposition rules if found (child rules override, missing slots inherited)
            const hasCurrentCompExplicitRules = !!((compEntry.parttypeselectionrules || compEntry.item?.parttypeselectionrules)?.pairs);
            const allowCurrentCompOverride =
                hasCurrentCompExplicitRules ||
                !baseCompForRules;

            if (baseCompositionRules && baseCompositionRules.pairs && rules && rules.pairs && allowCurrentCompOverride) {
                // Merge by slot name (pair.key): baseCompositionRules provide defaults, rules override
                const mergedPairs = {};
                const slotMap = new Map(); // Track slots by name to handle overrides
                
                // First, add all base composition rules
                let basePairs = [];
                if (Array.isArray(baseCompositionRules.pairs)) {
                    basePairs = baseCompositionRules.pairs;
                } else if (typeof baseCompositionRules.pairs === 'object') {
                    basePairs = Object.values(baseCompositionRules.pairs);
                }
                basePairs.forEach((pair, idx) => {
                    if (pair && pair.key) {
                        const slotName = pair.key;
                        slotMap.set(slotName, { pair, idx: `base_${idx}` });
                    }
                });
                
                // Then, override with current rules (manufacturer comp rules take precedence)
                let currentPairs = [];
                if (Array.isArray(rules.pairs)) {
                    currentPairs = rules.pairs;
                } else if (typeof rules.pairs === 'object') {
                    currentPairs = Object.values(rules.pairs);
                }
                currentPairs.forEach((pair, idx) => {
                    if (pair && pair.key) {
                        const slotName = pair.key;
                        slotMap.set(slotName, { pair, idx: `current_${idx}` }); // Override
                    }
                });
                
                // Build merged pairs object
                let pairIdx = 0;
                slotMap.forEach(({ pair }) => {
                    mergedPairs[`pair${pairIdx++}`] = pair;
                });
                
                rules = { ...rules, pairs: mergedPairs };
            } else if (baseCompositionRules && baseCompositionRules.pairs && rules && rules.pairs && !allowCurrentCompOverride) {
                // Current comp has no explicit rules and inherits via basecomposition:
                // keep inherited basecomposition rules as authoritative.
                rules = baseCompositionRules;
            } else if (baseCompositionRules && baseCompositionRules.pairs && (!rules || !rules.pairs)) {
                // Use basecomposition rules if no current rules
                rules = baseCompositionRules;
            }
            
            // Process base generic root rules first (if exists)
            if (baseRules && baseRules.pairs) {
                // Debug: Log baseRules for grenade gadgets
                if (rootKeyLower.includes('grenade_gadget')) {
                    const baseRulesPairs = Array.isArray(baseRules.pairs) ? baseRules.pairs : Object.values(baseRules.pairs);
                    console.log(`[Part Counts] Root ${root.key}: baseRules has ${baseRulesPairs.length} slots:`, baseRulesPairs.map(p => p.key));
                }
                let basePairs = [];
                if (Array.isArray(baseRules.pairs)) {
                    basePairs = baseRules.pairs;
                } else if (typeof baseRules.pairs === 'object') {
                    basePairs = Object.values(baseRules.pairs);
                }
                
                // Store ALL base rules first (will be overwritten by manufacturer rules only if they explicitly specify partcount)
                // This ensures all part types from base generic root are included
                basePairs.forEach(pair => {
                    if (!pair || !pair.key) return;
                    
                    // Normalize slot name (e.g. "Element" -> "element") so validation can match correctly
                    const slotName = normalizeDepTableName(pair.key);
                    let value = pair.value || {};
                    let partCount = value.partcount || {};
                    const hasExplicitMin = partCount.min !== undefined && partCount.min !== null;
                    const hasExplicitMax = partCount.max !== undefined && partCount.max !== null;
                    
                    // If slot has no value (just a key), try to find it in lower rarity comps
                    // CRITICAL: Rarity levels do NOT inherit from each other.
                    // comp_05_legendary does NOT inherit from comp_04_epic
                    // comp_04_epic does NOT inherit from comp_03_rare
                    // comp_03_rare does NOT inherit from comp_02_uncommon
                    // comp_02_uncommon does NOT inherit from comp_01_common
                    // Only explicit basecomposition fields are followed (e.g., comp_05_legendary_slugger -> base_comp_05_legendary)
                    // If pair.value is empty, partCount will default to 1-1 (minCount=null, maxCount=null)
                    
                    const parts = value.parts || [];
                    
                    let minCount = null;
                    let maxCount = null;
                    
                    if (partCount.min !== undefined && partCount.min !== null) {
                        const parsed = parseInt(partCount.min);
                        if (!isNaN(parsed)) minCount = parsed;
                    }
                    if (partCount.max !== undefined && partCount.max !== null) {
                        const parsed = parseInt(partCount.max);
                        if (!isNaN(parsed)) maxCount = parsed;
                    }
                    
                    // Handle defaults
                    let effectiveMin = minCount;
                    let effectiveMax = maxCount;
                    
                    if (minCount === 0 && maxCount === null) {
                        effectiveMax = 1;
                    } else if (minCount === null && maxCount === null) {
                        const slotNormBr = normalizeDepTableName(slotName);
                        if (parts.length > 0 && slotNormBr !== 'firmware') {
                            effectiveMin = 1;
                            effectiveMax = 1;
                        } else {
                            effectiveMin = 0;
                            effectiveMax = 1;
                        }
                    } else if (minCount === null) {
                        effectiveMin = 1;
                    } else if (maxCount === null && minCount !== 0) {
                        effectiveMax = 1;
                    }
                    
                    // When basecomposition points to heavy_weapon_gadget, keep body_acc/barrel_acc from baseCompositionRules; do not overwrite with weapon baseRules.
                    const baseCompRef = compEntry.basecomposition || compEntry.item?.basecomposition;
                    if (typeof baseCompRef === 'string' && baseCompRef.toLowerCase().includes('heavy_weapon_gadget') && (slotName === 'body_acc' || slotName === 'barrel_acc')) return;
                    
                    // Store base rules for ALL slots mentioned in base generic root
                    // These will be kept unless manufacturer comp explicitly overrides
                    partCounts.set(slotName, { 
                        min: effectiveMin,
                        max: effectiveMax,
                        displayMin: minCount,
                        displayMax: maxCount,
                        isExplicit: hasExplicitMin || hasExplicitMax,
                        hasExplicitMin,
                        hasExplicitMax,
                        countSource: 'base_generic'
                    });
                    
                    // Also add parts from base generic root to allowed parts list
                    // Manufacturer enhancements should show BOTH generic and manufacturer-specific parts
                    // This ensures parts from the universal root are available for selection
                    if (parts.length > 0) {
                        const baseAllowedSet = new Set();
                        parts.forEach(partObj => {
                            if (typeof partObj === 'string') {
                                // Direct string part name
                                baseAllowedSet.add(partObj);
                            } else if (partObj && typeof partObj === 'object') {
                                // Part object with 'part' property (e.g., { "part": "Part_Body_01_Common" })
                                if (partObj.part) {
                                    baseAllowedSet.add(partObj.part);
                                } else if (partObj.key) {
                                    // Part object with 'key' property
                                    baseAllowedSet.add(partObj.key);
                                }
                            }
                        });
                        
                        if (baseAllowedSet.size > 0) {
                            // Merge with existing allowed parts for this slot
                            // This creates a union of base generic root parts + manufacturer-specific parts
                            if (!allowedParts.has(slotName)) {
                                allowedParts.set(slotName, new Set());
                            }
                            baseAllowedSet.forEach(part => allowedParts.get(slotName).add(part));
                            
                            // If min > 0, these are forced (must be selected)
                            if (effectiveMin > 0) {
                                if (!forcedParts.has(slotName)) {
                                    forcedParts.set(slotName, new Set());
                                }
                                baseAllowedSet.forEach(part => forcedParts.get(slotName).add(part));
                            }
                        }
                    }
                });
            }
            
            // CRITICAL: Also include slots from root's parttypes that aren't in comp rules
            // This ensures slots like stat_group3 are included even when not defined in comp_04_epic
            // Check both current root and base generic root for parttypes
            const rootsToCheck = [root];
            if (baseGenericRoot && baseGenericRoot !== root) {
                rootsToCheck.push(baseGenericRoot);
            }
            
            rootsToCheck.forEach(checkRoot => {
                // Check root.data.parttypes (from JSON structure)
                const rootParttypes = checkRoot.data?.parttypes || checkRoot.parttypes;
                if (Array.isArray(rootParttypes)) {
                    rootParttypes.forEach(slotName => {
                        // Only add if not already in partCounts (don't override existing counts)
                        if (slotName && !partCounts.has(slotName)) {
                            // Parttypes-only slot: treat as optional unless explicit rules define counts.
                            partCounts.set(slotName, {
                                min: 0,
                                max: 1,
                                displayMin: null,
                                displayMax: null,
                                isExplicit: false,
                                hasExplicitMin: false,
                                hasExplicitMax: false,
                                countSource: 'parttypes_fallback'
                            });
                        }
                    });
                }
            });

            // Repkit: ensure all standard part types (typeId 243 / Nexus parttypes) have partCounts when roots lack parttypes
            var isRepkitFamily = baseItemType === 'repair_kit' || (rootKeyLower && (rootKeyLower.indexOf('repair') !== -1 || rootKeyLower.indexOf('repkit') !== -1));
            if (isRepkitFamily) {
                var repkitPartTypes = ['body', 'payload', 'element', 'primary_augment', 'secondary_augment', 'augment_element_resist', 'augment_element_immunity', 'augment_element_nova', 'augment_element_splat', 'firmware', 'endgame'];
                repkitPartTypes.forEach(function (slotName) {
                    if (slotName && !partCounts.has(slotName)) {
                        partCounts.set(slotName, {
                            min: 0,
                            max: 1,
                            displayMin: null,
                            displayMax: null,
                            isExplicit: false,
                            hasExplicitMin: false,
                            hasExplicitMax: false,
                            countSource: 'repkit_parttypes_fallback'
                        });
                    }
                });
            }

            // Grenade gadgets (manufacturer or generic): comps often only define element+payload in pairs; Nexus still uses body, stat_augment, etc.
            var isGrenadeGadgetFamily = rootKeyLower && rootKeyLower.indexOf('grenade_gadget') !== -1;
            if (isGrenadeGadgetFamily) {
                var grenadeStandardSlots = ['body', 'element', 'payload', 'stat_augment', 'payload_augment', 'firmware', 'endgame', 'pearl_elem', 'pearl_stat'];
                grenadeStandardSlots.forEach(function (slotName) {
                    if (slotName && !partCounts.has(slotName)) {
                        partCounts.set(slotName, {
                            min: 0,
                            max: 1,
                            displayMin: null,
                            displayMax: null,
                            isExplicit: false,
                            hasExplicitMin: false,
                            hasExplicitMax: false,
                            countSource: 'grenade_gadget_slots_fallback'
                        });
                    }
                });
            }
            
            // Manufacturer comp actually lists Element with key only (no partcount / parts), e.g. mal_grenade_gadget.
            // Do not treat inherited generic rules as that case: tor_grenade_gadget often has no parttypeselectionrules
            // and only inherits grenade_gadget's explicit Element 0-0.
            const mfrPartRulesForElementStub = compEntry.parttypeselectionrules || compEntry.item?.parttypeselectionrules;
            const mfrPairsForElementStub = mfrPartRulesForElementStub && mfrPartRulesForElementStub.pairs
                ? (Array.isArray(mfrPartRulesForElementStub.pairs) ? mfrPartRulesForElementStub.pairs : Object.values(mfrPartRulesForElementStub.pairs))
                : [];
            const mfrDefinesGrenadeElementStub = mfrPairsForElementStub.some(function (p) {
                if (!p || !p.key) return false;
                if (normalizeDepTableName(p.key) !== 'element') return false;
                const v = p.value || {};
                const pc = v.partcount || {};
                const explicitPc = pc.min !== undefined || pc.max !== undefined ||
                    v.minparts !== undefined || v.maxparts !== undefined ||
                    v.minParts !== undefined || v.maxParts !== undefined;
                const plist = v.parts || [];
                return !explicitPc && plist.length === 0;
            });

            // Now process manufacturer/current rules
            // Parts list may be manufacturer-specific, but part counts only override if explicitly specified
            if (rules && rules.pairs) {
                let pairs = [];
                if (Array.isArray(rules.pairs)) {
                    pairs = rules.pairs;
                } else if (typeof rules.pairs === 'object') {
                    pairs = Object.values(rules.pairs);
                }
                
                pairs.forEach(pair => {
                    if (!pair || !pair.key) return;
                    
                    // Normalize slot name (e.g. "Element" -> "element") so validation can match correctly
                    const slotName = normalizeDepTableName(pair.key);
                    const value = pair.value || {};
                    const parts = value.parts || [];
                    const partCount = value.partcount || {};
                    
                    // Debug logging for class mods
                    const isClassModSlot = root.key && root.key.toLowerCase().includes('classmod');
                    if (isClassModSlot) {
                        console.log(`[ClassMod Debug] Processing slot: ${slotName}`, {
                            rootKey: root.key,
                            selectedComp: selectedComp,
                            parts: parts,
                            partCount: partCount,
                            value: value,
                            alreadyInPartCounts: partCounts.has(slotName),
                            existingPartCount: partCounts.get(slotName)
                        });
                    }
                    
                    // Extract min/max - handle multiple formats and ensure valid numbers
                    let minCount = null;
                    let maxCount = null;
                    
                    // Check direct properties first
                    if (partCount.min !== undefined && partCount.min !== null) {
                        const parsed = parseInt(partCount.min);
                        if (!isNaN(parsed)) minCount = parsed;
                    }
                    if (partCount.max !== undefined && partCount.max !== null) {
                        const parsed = parseInt(partCount.max);
                        if (!isNaN(parsed)) maxCount = parsed;
                    }
                    
                    // Fallback: check for minparts/maxparts (alternate format)
                    if (minCount === null && (value.minparts !== undefined || value.minParts !== undefined)) {
                        const val = value.minparts !== undefined ? value.minparts : value.minParts;
                        const parsed = parseInt(val);
                        if (!isNaN(parsed)) minCount = parsed;
                    }
                    if (maxCount === null && (value.maxparts !== undefined || value.maxParts !== undefined)) {
                        const val = value.maxparts !== undefined ? value.maxparts : value.maxParts;
                        const parsed = parseInt(val);
                        if (!isNaN(parsed)) maxCount = parsed;
                    }
                    
                    if (isClassModSlot) {
                        console.log(`[ClassMod Debug] Extracted counts for ${slotName}:`, {
                            minCount: minCount,
                            maxCount: maxCount
                        });
                    }
                    
                    // Handle defaults:
                    // - If min is 0 and no max specified, it means min 0, max 1 (not disabled)
                    // - If both are null, default to min 1, max 1 (but don't store, let it be "Unknown" for display)
                    let effectiveMin = minCount;
                    let effectiveMax = maxCount;
                    
                    if (minCount === 0 && maxCount === null) {
                        // min 0 with no max means min 0, max 1
                        effectiveMax = 1;
                    } else if (minCount === null && maxCount === null) {
                        // Unknown - default to min 1, max 1 for selection logic
                        // EXCEPTION: For grenade Element/Payload slots with no explicit partcount, treat as optional (0-1)
                        // Common/uncommon grenades don't require element/payload per data
                        const isGrenadeElementOrPayload = rootKeyLower && rootKeyLower.indexOf('grenade_gadget') !== -1 && 
                                                           (slotName.toLowerCase() === 'element' || slotName.toLowerCase() === 'payload');
                        if (isGrenadeElementOrPayload) {
                            effectiveMin = 0;
                            effectiveMax = 1;
                        } else {
                            effectiveMin = 1;
                            effectiveMax = 1;
                        }
                    } else if (minCount === null) {
                        // Only max specified, default min to 1
                        effectiveMin = 1;
                    } else if (maxCount === null) {
                        // Only min specified, default max to 1 (unless min is 0, which we handled above)
                        if (minCount !== 0) {
                            effectiveMax = 1;
                        }
                    }
                    
                    const isDisabled = effectiveMin === 0 && effectiveMax === 0;
                    
                    // IMPORTANT: Part counts come from generic comp by default
                    // Manufacturer comps only override part counts if they explicitly specify partcount
                    // Check if manufacturer comp has explicit partcount
                    const hasExplicitPartCount = partCount.min !== undefined || partCount.max !== undefined || 
                                                value.minparts !== undefined || value.maxparts !== undefined ||
                                                value.minParts !== undefined || value.maxParts !== undefined;
                    const hasExplicitMin = partCount.min !== undefined || value.minparts !== undefined || value.minParts !== undefined;
                    const hasExplicitMax = partCount.max !== undefined || value.maxparts !== undefined || value.maxParts !== undefined;
                    
                    if (isClassModSlot) {
                        console.log(`[ClassMod Debug] Part count decision for ${slotName}:`, {
                            hasExplicitPartCount: hasExplicitPartCount,
                            partsLength: parts.length,
                            alreadyInPartCounts: partCounts.has(slotName),
                            effectiveMin: effectiveMin,
                            effectiveMax: effectiveMax,
                            isDisabled: isDisabled
                        });
                    }
                    
                    if (hasExplicitPartCount) {
                        // Manufacturer comp explicitly specifies partcount - override base rules
                        partCounts.set(slotName, { 
                            min: effectiveMin,      // Effective min for selection logic
                            max: effectiveMax,     // Effective max for selection logic
                            displayMin: minCount,   // Original min for display (null if Unknown)
                            displayMax: maxCount,    // Original max for display (null if Unknown)
                            isExplicit: true,
                            hasExplicitMin,
                            hasExplicitMax,
                            countSource: 'manufacturer_explicit'
                        });
                        if (isClassModSlot) {
                            console.log(`[ClassMod Debug] Set explicit partCount for ${slotName}:`, partCounts.get(slotName));
                        }
                    } else if (parts.length > 0) {
                        // Current comp explicitly defines candidate parts for this slot but omits min/max.
                        // Implied rule: min 1, max 1 for this comp. This trumps the base comp rule because the
                        // slot is defined on the unique/manufacturer comp (e.g. barrel_acc with parts list in ted_ps).
                        const existingPartCount = partCounts.get(slotName);
                        partCounts.set(slotName, {
                            min: 1,
                            max: 1,
                            displayMin: null,
                            displayMax: null,
                            isExplicit: false,
                            hasExplicitMin: false,
                            hasExplicitMax: false,
                            countSource: 'current_comp_parts_implied_1_1'
                        });
                        effectiveMin = 1;
                        effectiveMax = 1;
                        if (isClassModSlot) {
                            console.log(`[ClassMod Debug] Set implied 1-1 partCount for ${slotName} (has parts, no explicit count${existingPartCount ? ', overriding inherited count' : ''}):`, partCounts.get(slotName));
                        }
                    } else if (
                        mfrDefinesGrenadeElementStub &&
                        rootKeyLower && rootKeyLower.indexOf('grenade_gadget') !== -1 &&
                        baseGenericRoot && baseGenericRoot !== root &&
                        baseItemType === 'grenade_gadget' &&
                        slotName.toLowerCase() === 'element' &&
                        !hasExplicitPartCount &&
                        parts.length === 0
                    ) {
                        // Manufacturer comp lists Element with key only (no partcount, no parts list). That overrides
                        // generic grenade_gadget comp_01_common's explicit Element 0-0: Nexus implies a real element slot
                        // (e.g. mal_grenade requiredcriteriatags elem). Without this, isPartAllowedForTable rejects all
                        // element parts because partCounts stayed min 0 max 0.
                        partCounts.set(slotName, {
                            min: 1,
                            max: 1,
                            displayMin: null,
                            displayMax: null,
                            isExplicit: false,
                            hasExplicitMin: false,
                            hasExplicitMax: false,
                            countSource: 'grenade_manufacturer_element_stub_implied_1_1'
                        });
                    }
                    // If no explicit partcount in manufacturer comp and slot exists in base comp, keep the base generic comp's count (already stored above)
                    // This is critical: manufacturer comps are composition variants, not new definitions
                    // They may restrict parts list, but counts default to generic comp
                    
                    if (parts.length > 0 && !isDisabled) {
                        const allowedSet = new Set();
                        parts.forEach(partObj => {
                            if (typeof partObj === 'string') {
                                allowedSet.add(partObj);
                            } else if (partObj && typeof partObj === 'object') {
                                if (partObj.part) {
                                    allowedSet.add(partObj.part);
                                } else if (partObj.key) {
                                    allowedSet.add(partObj.key);
                                }
                            }
                        });
                        
                        if (allowedSet.size > 0) {
                            // Merge with existing allowed parts for this slot
                            if (!allowedParts.has(slotName)) {
                                allowedParts.set(slotName, new Set());
                            }
                            allowedSet.forEach(part => allowedParts.get(slotName).add(part));
                            
                            // If min > 0, these are forced
                            // Use effective min from partCounts if available, otherwise use effectiveMin
                            const slotPartCount = partCounts.get(slotName);
                            const effectiveMinForForced = slotPartCount ? slotPartCount.min : effectiveMin;
                            if (effectiveMinForForced > 0) {
                                if (!forcedParts.has(slotName)) {
                                    forcedParts.set(slotName, new Set());
                                }
                                allowedSet.forEach(part => forcedParts.get(slotName).add(part));
                            }
                        }
                    }
                });
            }

            // Normalize slot constraints with explicit precedence (base -> inherited -> current).
            // For manufacturer repkits, only apply baseRules for slots the selected comp or its base composition define.
            // Otherwise default_repkit_comp_01 would restrict e.g. payload to part_payload_small only even when
            // the comp (e.g. comp_05_legendary_killspring) and base (comp_05_legendary) don't define payload.
            var effectiveBaseRules = baseRules;
            if (isRepkitMfrRoot && baseRules && baseRules.pairs) {
                var slotsFromComp = new Set();
                extractRulePairs(rules).forEach(function (p) { if (p && p.key) slotsFromComp.add(p.key); });
                extractRulePairs(baseCompositionRules).forEach(function (p) { if (p && p.key) slotsFromComp.add(p.key); });
                if (slotsFromComp.size > 0) {
                    var basePairsArr = Array.isArray(baseRules.pairs) ? baseRules.pairs : Object.values(baseRules.pairs);
                    var filteredBasePairs = basePairsArr.filter(function (pair) { return pair && pair.key && slotsFromComp.has(pair.key); });
                    if (filteredBasePairs.length < basePairsArr.length) {
                        effectiveBaseRules = { pairs: filteredBasePairs };
                    }
                }
            }
            applyRuleSetToSlotConstraints(effectiveBaseRules, 'base_generic', true);
            applyRuleSetToSlotConstraints(baseCompositionRules, 'basecomposition', true);
            applyRuleSetToSlotConstraints(rules, 'current_comp', false);

            const rebuildForcedPartsFromCounts = function () {
                if (slotConstraints.size > 0) {
                    forcedParts.clear();
                    slotConstraints.forEach((constraint) => {
                        const slotName = constraint.slotName;
                        const countInfo = partCounts.get(slotName) ||
                            Array.from(partCounts.entries()).find(([slotKey]) => normalizeSlotConstraintKey(slotKey) === constraint.normalizedSlot)?.[1] ||
                            null;
                        const minCount = Number.isFinite(Number(countInfo?.min)) ? Number(countInfo.min) : 0;
                        if (minCount > 0) {
                            forcedParts.set(slotName, new Set(constraint.allowedParts));
                        }
                    });
                } else {
                    Array.from(partCounts.entries()).forEach(([slotName, countInfo]) => {
                        const minCount = Number.isFinite(Number(countInfo?.min)) ? Number(countInfo.min) : 0;
                        if (minCount > 0 && allowedParts.has(slotName) && allowedParts.get(slotName).size > 0) {
                            forcedParts.set(slotName, new Set(allowedParts.get(slotName)));
                        }
                    });
                }
            };

            if (slotConstraints.size > 0) {
                slotConstraints.forEach((constraint, normalizedSlot) => {
                    // Remove any pre-normalized union entries for the same slot key.
                    Array.from(allowedParts.keys()).forEach(existingSlot => {
                        if (normalizeSlotConstraintKey(existingSlot) === normalizedSlot) {
                            allowedParts.delete(existingSlot);
                        }
                    });
                    allowedParts.set(constraint.slotName, new Set(constraint.allowedParts));
                });

                rebuildForcedPartsFromCounts();
            }

            // Manufacturer repkit: allow body and other parts from the current root (e.g. part_ted on ted_repair_kit)
            // and from the base generic root (e.g. default_repkit_body, part_payload_medium) so more than one option shows.
            // Base comp rules may only list generic parts; root-specific parts must be allowed for validation and UI.
            var isRepkitMfrForAllowed = (root !== baseGenericRoot) && (baseItemType === 'repair_kit' || (rootKeyLower && (rootKeyLower.indexOf('repair') !== -1 || rootKeyLower.indexOf('repkit') !== -1)));
            var mergeRootPartsIntoAllowed = function (depEntries, slotFilter) {
                if (!depEntries || depEntries.length === 0) return;
                depEntries.forEach(function (dep) {
                    var slotName = dep.depTableName;
                    if (!slotName || slotName === 'inv_comp') return;
                    if (slotFilter && !slotFilter(slotName)) return;
                    var partKey = dep.key;
                    if (!partKey) return;
                    var slotKey = normalizeSlotConstraintKey(slotName);
                    var constraint = slotConstraints.get(slotKey);
                    // Do not merge root parts into slots restricted by the selected unique comp (current_comp).
                    // Those slots must show only the part(s) required by the comp, not all root parts.
                    if (constraint && constraint.source === 'current_comp') return;
                    if (!allowedParts.has(slotName)) {
                        allowedParts.set(slotName, new Set());
                    }
                    allowedParts.get(slotName).add(partKey);
                    if (constraint && constraint.allowedParts) {
                        constraint.allowedParts.add(partKey);
                    } else if (partCounts.has(slotName)) {
                        slotConstraints.set(slotKey, {
                            slotName: slotName,
                            normalizedSlot: slotKey,
                            allowedParts: new Set(allowedParts.get(slotName)),
                            source: 'repkit_root_parts',
                            inherited: false,
                            singletonLocked: false
                        });
                    }
                });
            };
            if (isRepkitMfrForAllowed) {
                if (root.depEntries && root.depEntries.length > 0) {
                    mergeRootPartsIntoAllowed(root.depEntries, null);
                }
                // Also allow all parts from the generic repkit root for slots we have partCounts for (body, payload, etc.)
                // default_repair_kit's __dep_entries only have body + inv_comp; the full payload/body list lives on root key "repair_kit"
                var repkitSlotFilter = partCounts.size > 0 ? function (slotName) {
                    return partCounts.has(slotName) || Array.from(partCounts.keys()).some(function (k) {
                        return normalizeSlotConstraintKey(k) === normalizeSlotConstraintKey(slotName);
                    });
                } : null;
                if (baseGenericRoot && baseGenericRoot.depEntries && baseGenericRoot.depEntries.length > 0 && partCounts.size > 0) {
                    mergeRootPartsIntoAllowed(baseGenericRoot.depEntries, repkitSlotFilter);
                }
                var repkitPartsRoot = baseGenericRoot && (baseGenericRoot.key || '').toLowerCase() !== 'repair_kit' && data && data.rootDefinitions
                    ? data.rootDefinitions.find(function (r) { return (r.key || '').toLowerCase() === 'repair_kit'; })
                    : null;
                if (repkitPartsRoot && repkitPartsRoot.depEntries && repkitPartsRoot.depEntries.length > 0 && partCounts.size > 0) {
                    mergeRootPartsIntoAllowed(repkitPartsRoot.depEntries, repkitSlotFilter);
                }
            }

            // Weapon roots: allow all parts from the current root for slots that have constraints or partCounts,
            // so that manufacturer-specific barrel_acc (and other slots) that exist in the root are allowed for validation and UI.
            // Rules may only list generic parts; root-specific parts must be allowed.
            // *_hw manufacturer roots (e.g. tor_hw) use basetype heavy_weapon_gadget, not "weapon" in the key — they still
            // carry body_ele/secondary_ele parts on the root (e.g. part_normal) that must merge into allowedParts.
            var isWeaponRootForAllowed = !isRepkitMfrForAllowed && rootKeyLower && (
                rootKeyLower.includes('weapon') ||
                rootKeyLower.endsWith('_hw') ||
                ['vla', 'dah', 'jak', 'mal', 'hyperion', 'atl', 'ted', 'ord', 'bor', 'grenade', 'heavy', 'turret', 'terminal'].some(function (m) { return rootKeyLower.includes(m); })
            );
            if (isWeaponRootForAllowed && root.depEntries && root.depEntries.length > 0) {
                var weaponSlotFilter = (slotConstraints.size > 0 || partCounts.size > 0) ? function (slotName) {
                    var slotKey = normalizeSlotConstraintKey(slotName);
                    if (slotConstraints.has(slotKey)) return true;
                    if (partCounts.has(slotName)) return true;
                    return Array.from(partCounts.keys()).some(function (k) {
                        return normalizeSlotConstraintKey(k) === slotKey;
                    });
                } : null;
                mergeRootPartsIntoAllowed(root.depEntries, weaponSlotFilter);
                // Also allow parts from base generic weapon root for constrained slots (e.g. barrel_acc in generic weapon root).
                if (baseGenericRoot && baseGenericRoot !== root && baseGenericRoot.depEntries && baseGenericRoot.depEntries.length > 0 && weaponSlotFilter) {
                    mergeRootPartsIntoAllowed(baseGenericRoot.depEntries, weaponSlotFilter);
                }
                // Ensure any slot we merged into allowedParts (but that had no slotConstraints entry, e.g. barrel_acc when partCounts uses different key) is visible to getAllowedPartsForTable.
                if (weaponSlotFilter) {
                    Array.from(allowedParts.keys()).forEach(function (slotName) {
                        if (!slotName || slotName === 'inv_comp') return;
                        var slotKey = normalizeSlotConstraintKey(slotName);
                        if (!slotConstraints.has(slotKey) && weaponSlotFilter(slotName)) {
                            var set = allowedParts.get(slotName);
                            if (set && set.size > 0) {
                                slotConstraints.set(slotKey, {
                                    slotName: slotName,
                                    normalizedSlot: slotKey,
                                    allowedParts: new Set(set),
                                    source: 'weapon_root_parts',
                                    inherited: false,
                                    singletonLocked: false
                                });
                            }
                        }
                    });
                }
            }

            // Nexus: inv type heavy_weapon_gadget declares basetype inv'Weapon' (inv0). Manufacturer *_hw roots use basetype
            // inv'heavy_weapon_gadget' only, so baseGenericRoot is HW gadget — merge generic weapon depEntries for element slots.
            if (!isRepkitMfrForAllowed && data && data.rootDefinitions && (baseItemType === 'heavy_weapon_gadget' || rootKeyLower.endsWith('_hw'))) {
                var hwGadgetTypeRootForWeapon = data.rootDefinitions.find(function (r) { return (r.key || '').toLowerCase() === 'heavy_weapon_gadget'; });
                var hwGadgetBtForWeapon = hwGadgetTypeRootForWeapon && String(hwGadgetTypeRootForWeapon.basetype || '');
                if (/inv'\s*weapon\s*'/i.test(hwGadgetBtForWeapon)) {
                    var genericWeaponRootForHwAllowed = data.rootDefinitions.find(function (r) { return (r.key || '').toLowerCase() === 'weapon'; });
                    if (genericWeaponRootForHwAllowed && genericWeaponRootForHwAllowed.depEntries && genericWeaponRootForHwAllowed.depEntries.length > 0) {
                        var hwWeaponLineageSlots = ['body_ele', 'secondary_ele', 'pearl_elem'];
                        var hwWeaponLineageSlotFilter = function (slotName) {
                            if (!slotName || slotName === 'inv_comp') return false;
                            var sl = slotName.toLowerCase();
                            if (hwWeaponLineageSlots.indexOf(sl) !== -1) return true;
                            var slotKeyW = normalizeSlotConstraintKey(slotName);
                            if (slotConstraints.has(slotKeyW)) return true;
                            if (partCounts.has(slotName)) return true;
                            return Array.from(partCounts.keys()).some(function (k) {
                                return normalizeSlotConstraintKey(k) === slotKeyW;
                            });
                        };
                        mergeRootPartsIntoAllowed(genericWeaponRootForHwAllowed.depEntries, hwWeaponLineageSlotFilter);
                        Array.from(allowedParts.keys()).forEach(function (slotName) {
                            if (!slotName || slotName === 'inv_comp') return;
                            var slotKeyW = normalizeSlotConstraintKey(slotName);
                            if (!slotConstraints.has(slotKeyW) && hwWeaponLineageSlotFilter(slotName)) {
                                var setW = allowedParts.get(slotName);
                                if (setW && setW.size > 0) {
                                    slotConstraints.set(slotKeyW, {
                                        slotName: slotName,
                                        normalizedSlot: slotKeyW,
                                        allowedParts: new Set(setW),
                                        source: 'heavy_weapon_weapon_lineage',
                                        inherited: false,
                                        singletonLocked: false
                                    });
                                }
                            }
                        });
                    }
                }
            }

            // Manufacturer grenade gadgets: merge generic grenade_gadget root's parts so payload, body, element, etc.
            // are allowed (e.g. part_06_lingering_cryo, part_05_singularity from grenade_gadget in inv4).
            if (!isRepkitMfrForAllowed && rootKeyLower && rootKeyLower.indexOf('grenade_gadget') !== -1 && data && data.rootDefinitions) {
                var genericGrenadeRoot = data.rootDefinitions.find(function (r) { return (r.key || '').toLowerCase() === 'grenade_gadget'; });
                if (genericGrenadeRoot && genericGrenadeRoot !== root && genericGrenadeRoot.depEntries && genericGrenadeRoot.depEntries.length > 0) {
                    var grenadeSlotTypesForAllowed = ['body', 'element', 'payload', 'payload_augment', 'stat_augment', 'firmware', 'endgame', 'pearl_elem', 'pearl_stat'];
                    var grenadeSlotFilter = function (slotName) {
                        if (!slotName || slotName === 'inv_comp') return false;
                        var slotKey = normalizeSlotConstraintKey(slotName);
                        if (slotConstraints.has(slotKey)) return true;
                        if (partCounts.has(slotName)) return true;
                        if (grenadeSlotTypesForAllowed.indexOf(slotName.toLowerCase()) !== -1) return true;
                        return Array.from(partCounts.keys()).some(function (k) { return normalizeSlotConstraintKey(k) === slotKey; });
                    };
                    mergeRootPartsIntoAllowed(genericGrenadeRoot.depEntries, grenadeSlotFilter);
                    Array.from(allowedParts.keys()).forEach(function (slotName) {
                        if (!slotName || slotName === 'inv_comp') return;
                        var slotKey = normalizeSlotConstraintKey(slotName);
                        if (!slotConstraints.has(slotKey) && grenadeSlotFilter(slotName)) {
                            var set = allowedParts.get(slotName);
                            if (set && set.size > 0) {
                                slotConstraints.set(slotKey, {
                                    slotName: slotName,
                                    normalizedSlot: slotKey,
                                    allowedParts: new Set(set),
                                    source: 'grenade_gadget_root_parts',
                                    inherited: false,
                                    singletonLocked: false
                                });
                            }
                        }
                    });
                }
            }

            // Manufacturer shields: merge type-specific root (energy_shield/armor_shield) and generic Shield root
            // so primary_augment/secondary_augment parts from all shield roots are allowed (e.g. part_eng_vagabond_primary,
            // part_unv_turtle_primary from energy_shield or generic Shield in inv0).
            if (!isRepkitMfrForAllowed && rootKeyLower && rootKeyLower.indexOf('shield') !== -1 && data && data.rootDefinitions) {
                var shieldSlotTypesForAllowed = ['body', 'element', 'primary_augment', 'secondary_augment', 'firmware', 'endgame', 'unique', 'pearl_elem', 'pearl_stat'];
                var shieldSlotFilter = function (slotName) {
                    if (!slotName || slotName === 'inv_comp') return false;
                    var slotKey = normalizeSlotConstraintKey(slotName);
                    if (slotConstraints.has(slotKey)) return true;
                    if (partCounts.has(slotName)) return true;
                    if (shieldSlotTypesForAllowed.indexOf(slotName.toLowerCase()) !== -1) return true;
                    return Array.from(partCounts.keys()).some(function (k) { return normalizeSlotConstraintKey(k) === slotKey; });
                };
                var typeSpecificShieldRoot = null;
                if (compEntry) {
                    var baseCompRefShield = compEntry.basecomposition || compEntry.item?.basecomposition;
                    if (baseCompRefShield && typeof baseCompRefShield === 'string') {
                        var baseCompMatchShield = baseCompRefShield.match(/inv'([^']+)\./i);
                        var normalizedShield = baseCompMatchShield && baseCompMatchShield[1] ? baseCompMatchShield[1].toLowerCase() : null;
                        if (normalizedShield === 'energy_shield') {
                            typeSpecificShieldRoot = data.rootDefinitions.find(function (r) { return (r.key || '').toLowerCase() === 'energy_shield'; })
                                || data.rootDefinitions.find(function (r) { return (r.key || '').toLowerCase().includes('energy_shield'); })
                                || data.rootDefinitions.find(function (r) { var s = r.serialIndex; return s === 248 || s === '248' || s === 238 || s === '238'; });
                        } else if (normalizedShield === 'armor_shield') {
                            typeSpecificShieldRoot = data.rootDefinitions.find(function (r) { return (r.key || '').toLowerCase() === 'armor_shield'; })
                                || data.rootDefinitions.find(function (r) { return (r.key || '').toLowerCase().includes('armor_shield'); })
                                || data.rootDefinitions.find(function (r) { return r.serialIndex === 237 || r.serialIndex === '237'; });
                        }
                    }
                }
                if (typeSpecificShieldRoot && typeSpecificShieldRoot.depEntries && typeSpecificShieldRoot.depEntries.length > 0) {
                    mergeRootPartsIntoAllowed(typeSpecificShieldRoot.depEntries, shieldSlotFilter);
                }
                var genericShieldRoot = data.rootDefinitions.find(function (r) { return (r.key || '').toLowerCase() === 'shield'; });
                if (genericShieldRoot && genericShieldRoot !== root && genericShieldRoot.depEntries && genericShieldRoot.depEntries.length > 0) {
                    mergeRootPartsIntoAllowed(genericShieldRoot.depEntries, shieldSlotFilter);
                }
                Array.from(allowedParts.keys()).forEach(function (slotName) {
                    if (!slotName || slotName === 'inv_comp') return;
                    var slotKey = normalizeSlotConstraintKey(slotName);
                    if (!slotConstraints.has(slotKey) && shieldSlotFilter(slotName)) {
                        var set = allowedParts.get(slotName);
                        if (set && set.size > 0) {
                            slotConstraints.set(slotKey, {
                                slotName: slotName,
                                normalizedSlot: slotKey,
                                allowedParts: new Set(set),
                                source: 'shield_type_root_parts',
                                inherited: false,
                                singletonLocked: false
                            });
                        }
                    }
                });
            }

            const debugSlotConstraintResolver = typeof window !== 'undefined' && !!window.debugSlotConstraintResolver;
            if (debugSlotConstraintResolver && slotConstraints.size > 0) {
                console.log('[Slot Constraint Resolver]', {
                    rootKey: root.key,
                    selectedComp,
                    slots: Array.from(slotConstraints.values()).map(entry => ({
                        slotName: entry.slotName,
                        normalizedSlot: entry.normalizedSlot,
                        source: entry.source,
                        inherited: entry.inherited,
                        allowedCount: entry.allowedParts.size,
                        singletonLocked: entry.singletonLocked
                    }))
                });
            }
            
            // Debug: Log final partCounts for class mods
            const isClassMod = root.key && root.key.toLowerCase().includes('classmod');
            if (isClassMod) {
                console.log(`[ClassMod Debug] Final partCounts for root "${root.key}" with comp "${selectedComp}":`, 
                    Array.from(partCounts.entries()).map(([slot, count]) => ({ slot, count }))
                );
                console.log(`[ClassMod Debug] Final forcedParts:`, 
                    Array.from(forcedParts.entries()).map(([slot, parts]) => ({ slot, parts: Array.from(parts) }))
                );
            }
            
            // Debug: Log final partCounts for class mods
            const isClassModDebug = root.key && root.key.toLowerCase().includes('classmod');
            if (isClassModDebug) {
                console.log(`[ClassMod Debug] Final partCounts for root "${root.key}" with comp "${selectedComp}":`, 
                    Array.from(partCounts.entries()).map(([slot, count]) => ({ slot, count }))
                );
                console.log(`[ClassMod Debug] Final forcedParts:`, 
                    Array.from(forcedParts.entries()).map(([slot, parts]) => ({ slot, parts: Array.from(parts) }))
                );
            }

            // Pearlescent rarity comps are entitled to pearl_elem and pearl_stat with min1 max1 (required), like other required slots.
            if (tagPool.has('pearlescent')) {
                ['pearl_elem', 'pearl_stat'].forEach(function (slotName) {
                    partCounts.set(slotName, {
                        min: 1,
                        max: 1,
                        displayMin: 1,
                        displayMax: 1,
                        isExplicit: false,
                        hasExplicitMin: true,
                        hasExplicitMax: false,
                        countSource: 'pearlescent_implied_1_1'
                    });
                });
            }

            const tempCompRulesForFinalize = {
                allowedParts,
                forcedParts,
                partCounts,
                slotConstraints,
                partTagMaxRules: Array.from(partTagMaxRuleMap.values()),
                tagPool,
                compKey: selectedComp,
                compEntry
            };
            finalizeImpliedPartCountsFromTagPool(root, tempCompRulesForFinalize);
            relaxPartCountsWhenNoQualifiedOptions(root, tempCompRulesForFinalize);
            // Keep firmware optional: merged null-null rules often imply min 1 when options exist; serials omit firmware legitimately.
            Array.from(partCounts.entries()).forEach(function ([slotName, countInfo]) {
                if (normalizeDepTableName(slotName) !== 'firmware') return;
                var minV = countInfo.min != null ? Number(countInfo.min) : null;
                var maxV = countInfo.max != null ? Number(countInfo.max) : null;
                if (minV === 0 && maxV === 0) return;
                var maxF = maxV != null ? maxV : 1;
                partCounts.set(slotName, Object.assign({}, countInfo, {
                    min: 0,
                    max: Math.max(maxF, 1),
                    hasExplicitMin: false
                }));
            });
            rebuildForcedPartsFromCounts();

            return {
                allowedParts,
                forcedParts,
                partCounts,
                slotConstraints,
                partTagMaxRules: Array.from(partTagMaxRuleMap.values()),
                tagPool,
                compKey: selectedComp,
                compEntry
            };
        }

        function isPartValid(partItem, compRules, tableName, isSelected = false, options = {}) {
            const ignoreRelaxedPicker = !!(options && options.ignoreRelaxedPicker);
            if (!ignoreRelaxedPicker && window.__lbCtx && window.__lbCtx.relaxedPicker) return true;
            if (!compRules) return true; // No comp selected, all parts valid
            
            const formatTags = (tags) => {
                if (!tags || !Array.isArray(tags)) return [];
                return tags.map(tag => {
                    if (typeof tag === 'object' && tag !== null) {
                        const values = Object.values(tag);
                        return values[0] || Object.keys(tag)[0] || '';
                    }
                    return tag;
                }).filter(Boolean);
            };
            
            // Check tag compatibility FIRST - this is the most important filter
            const partAddTags = new Set(formatTags(partItem.addtags || []).map(t => t.toLowerCase()));
            const partDepTags = new Set(formatTags(partItem.dependencytags || []).map(t => t.toLowerCase()));
            const partExclTags = new Set(formatTags(partItem.exclusiontags || []).map(t => t.toLowerCase()));
            
            // Check exclusion conflicts - if part excludes a tag that's in the pool, it's invalid
            // CRITICAL FIX: Self-exclusion exception ONLY applies to selected parts
            // For non-selected candidates, their addtags aren't in the pool yet, so mutual-exclusion
            // groups should still exclude each other even if they share the same tag
            const hasExclusionConflict = Array.from(partExclTags).some(tag => {
                // Only bypass self-exclusion if this part is selected (its tags are already in the pool)
                // For non-selected parts, if they exclude a tag in the pool, they're excluded
                // (even if they also add that tag - this represents mutual-exclusion groups)
                if (isSelected && partAddTags.has(tag)) {
                    // Selected part: don't exclude if it adds the tag itself (self-exclusion)
                    return false;
                }
                // Non-selected part OR selected part excluding a different tag: check if tag is in pool
                return compRules.tagPool.has(tag);
            });
            if (hasExclusionConflict) {
                return false;
            }
            
            // Check dependencies - if part requires tags, ALL of them must be in the pool
            if (partDepTags.size > 0) {
                const allDependenciesMet = Array.from(partDepTags).every(tag => compRules.tagPool.has(tag));
                if (!allDependenciesMet) {
                    return false;
                }
            }
            
            // Check if part is in allowed parts for this table (from parttypeselectionrules)
            // This check happens AFTER tag compatibility to ensure tags are respected
            if (!isPartAllowedForTable(partItem, compRules, tableName)) {
                return false;
            }
            
            // Check rarity compatibility - extract rarity from tag pool
            // BUT: Don't filter out parts based on rarity if they're unique/legendary variants
            // Unique parts can work with legendary comps and vice versa
            const rarityTags = ['common', 'uncommon', 'rare', 'epic', 'legendary'];
            const poolRarities = Array.from(compRules.tagPool).filter(tag => rarityTags.includes(tag));
            const poolHasUnique = compRules.tagPool.has('unique');
            const partHasUnique = partAddTags.has('unique');
            
            if (poolRarities.length > 0 && !poolHasUnique && !partHasUnique) {
                // Only check rarity compatibility if neither pool nor part has 'unique' tag
                // Unique parts are special and can work with any rarity
                const partRarities = Array.from(partAddTags).filter(tag => rarityTags.includes(tag));
                // If part has rarity tags, they should match pool rarities
                if (partRarities.length > 0) {
                    const hasMatchingRarity = partRarities.some(rarity => poolRarities.includes(rarity));
                    if (!hasMatchingRarity) {
                        return false;
                    }
                }
            }
            
            return true;
        }

        function getSelectedPartValidityState(partItem, compRules, tableName, isSelected = false, root = null) {
            const result = {
                isCurrentlyValid: true,
                status: 'valid',
                message: ''
            };
            if (!isSelected) return result;
            if (!compRules) return result;
            const partScopedRules = (root && partItem && partItem.key)
                ? (getForwardScopedCompRulesForSelectedPart(root, partItem.key, compRules) || compRules)
                : compRules;

            const formatTags = (tags) => {
                if (!tags || !Array.isArray(tags)) return [];
                return tags.map(tag => {
                    if (typeof tag === 'object' && tag !== null) {
                        const values = Object.values(tag);
                        return values[0] || Object.keys(tag)[0] || '';
                    }
                    return tag;
                }).filter(Boolean);
            };

            const partAddTags = new Set(formatTags(partItem.addtags || []).map(t => t.toLowerCase()));
            const partDepTags = new Set(formatTags(partItem.dependencytags || []).map(t => t.toLowerCase()));
            const partExclTags = new Set(formatTags(partItem.exclusiontags || []).map(t => t.toLowerCase()));

            const conflictingExclusions = Array.from(partExclTags).filter(tag => {
                if (partAddTags.has(tag)) return false;
                return partScopedRules.tagPool.has(tag);
            });
            const missingDeps = Array.from(partDepTags).filter(tag => !partScopedRules.tagPool.has(tag));
            const isAllowed = isPartAllowedForTable(partItem, partScopedRules, tableName);
            const isCurrentlyValid = isPartValid(partItem, partScopedRules, tableName, true);
            result.isCurrentlyValid = isCurrentlyValid;

            if (isCurrentlyValid) return result;

            if (conflictingExclusions.length > 0 && missingDeps.length === 0 && isAllowed) {
                result.status = 'order-conflict';
                result.message = 'Conflicts with current tag pool, but remains valid by prior selection order (no backtracking).';
                return result;
            }

            result.status = 'hard-invalid';
            if (missingDeps.length > 0) {
                result.message = `Missing dependencies: ${missingDeps.join(', ')}`;
            } else if (!isAllowed) {
                result.message = `Not allowed for ${tableName}`;
            } else if (conflictingExclusions.length > 0) {
                result.message = `Excluded by tags in pool: ${conflictingExclusions.join(', ')}`;
            } else {
                result.message = 'Fails current validity checks.';
            }
            return result;
        }

        function resolvePartTableNameForRoot(root, partKey) {
            if (!root || !partKey) return null;
            const baseGenericRoot = getBaseGenericRootForPartLookup(root, data);
            const hit = findPartDepEntryForRootContext(root, partKey, data, baseGenericRoot);
            if (hit && hit.depTableName) return normalizeDepTableName(hit.depTableName);
            return null;
        }

        function getForwardScopedCompRulesForSlot(root, tableName, fallbackCompRules = null) {
            if (!root || !tableName) return fallbackCompRules;
            const selectedSet = _sp().has(root.key) ? _sp().get(root.key) : null;
            if (!selectedSet || selectedSet.size === 0) return fallbackCompRules;

            const partSelectionOrder = getPartSelectionOrderFromRoot(root, data?.rootDefinitions) || [];
            const partSelectionOrderLower = partSelectionOrder.map(s => normalizeDepTableName(s));
            const slotName = normalizeDepTableName(tableName);
            const slotIndex = partSelectionOrderLower.indexOf(slotName);
            if (slotIndex === -1) return fallbackCompRules || getSelectedCompRules(root, selectedSet);

            const selectedKeys = Array.from(selectedSet);
            const compKey = (fallbackCompRules && fallbackCompRules.compKey)
                || resolveSelectedCompKeyFromSelection(root, selectedKeys);
            if (!compKey) return fallbackCompRules;

            const scopedSelection = new Set([compKey]);
            selectedKeys.forEach(partKey => {
                if (partKey === compKey) return;
                const partTable = resolvePartTableNameForRoot(root, partKey);
                if (!partTable) return;
                const partIndex = partSelectionOrderLower.indexOf(partTable);
                if (partIndex !== -1 && partIndex <= slotIndex) scopedSelection.add(partKey);
            });

            return getSelectedCompRules(root, scopedSelection) || fallbackCompRules;
        }

        function getForwardScopedCompRulesForSelectedPart(root, targetPartKey, fallbackCompRules = null) {
            if (!root || !targetPartKey) return fallbackCompRules;
            const selectedSet = _sp().has(root.key) ? _sp().get(root.key) : null;
            if (!selectedSet || selectedSet.size === 0) return fallbackCompRules;

            const selectedKeys = Array.from(selectedSet);
            const targetIndex = selectedKeys.indexOf(targetPartKey);
            if (targetIndex === -1) return fallbackCompRules;

            const compKey = (fallbackCompRules && fallbackCompRules.compKey)
                || resolveSelectedCompKeyFromSelection(root, selectedKeys);
            if (!compKey) return fallbackCompRules;

            const scopedSelection = new Set([compKey]);
            for (let i = 0; i <= targetIndex; i++) {
                const partKey = selectedKeys[i];
                if (!partKey || partKey === compKey) continue;
                scopedSelection.add(partKey);
            }

            return getSelectedCompRules(root, scopedSelection) || fallbackCompRules;
        }

        function createPartTypeSelectionRulesHTML(root, renderOpts) {
            // Find components (inv_comp) that have parttypeselectionrules
            const componentsWithRules = [];
            if (root.depEntries) {
                root.depEntries.forEach(dep => {
                    if (dep.depTableName === 'inv_comp' && dep.parttypeselectionrules) {
                        componentsWithRules.push({
                            compKey: dep.key,
                            rules: dep.parttypeselectionrules,
                            aspects: dep.aspects || [],
                            item: dep.item || null
                        });
                    }
                });
            }

            // Check for generic base_comp entries (comp_01_common through comp_05_legendary)
            // These represent the generic component definitions that span all rarities
            // Support multiple item types: weapon, enhancement, shield, repair_kit, grenade, artifact, etc.
            // Store them globally so they're available for all roots
            if (!window.globalBaseComps) {
                window.globalBaseComps = new Map();
            }
            
            // Determine item type from root
            const rootBasetype = root.basetype || '';
            const rootItembasename = root.itembasename || root.key || '';
            
            // Extract item type from basetype (e.g., "inv'Weapon'" -> "weapon", "inv'Shield'" -> "shield")
            let itemType = '';
            if (rootBasetype) {
                const basetypeMatch = rootBasetype.match(/inv'(\w+)/i);
                if (basetypeMatch) {
                    const basetypeItemType = basetypeMatch[1].toLowerCase();
                    
                    // Special handling: terminal_gadget_combat, terminal_gadget_*, turret_gadget_* should map to terminal_gadget/turret_gadget
                    if (basetypeItemType.includes('terminal_gadget') && basetypeItemType !== 'terminal_gadget') {
                        // Map terminal_gadget_combat, terminal_gadget_*, etc. to terminal_gadget
                        itemType = 'terminal_gadget';
                    } else if (basetypeItemType.includes('turret_gadget') && basetypeItemType !== 'turret_gadget') {
                        // Map turret_gadget_combat, turret_gadget_*, etc. to turret_gadget
                        itemType = 'turret_gadget';
                    } else {
                        itemType = basetypeItemType;
                    }
                }
            }
            
            // If no basetype match, try to infer from itembasename or key
            if (!itemType) {
                const lowerName = rootItembasename.toLowerCase();
                if (lowerName.includes('weapon')) itemType = 'weapon';
                else if (lowerName.includes('enhancement')) itemType = 'enhancement';
                else if (lowerName.includes('shield')) itemType = 'shield';
                else if (lowerName.includes('repkit')) itemType = 'repair_kit';
                else if (lowerName.includes('repair') || lowerName.includes('repair_kit')) itemType = 'repair_kit';
                else if (lowerName.includes('grenade_gadget')) itemType = 'grenade_gadget';
                else if (lowerName.includes('heavy_weapon_gadget')) itemType = 'heavy_weapon_gadget';
                else if (lowerName.includes('turret_gadget')) itemType = 'turret_gadget';
                else if (lowerName.includes('terminal_gadget')) itemType = 'terminal_gadget';
                else if (lowerName.includes('terminal_')) itemType = 'terminal_gadget'; // terminal_combat -> terminal_gadget
                else if (lowerName.includes('turret_')) itemType = 'turret_gadget'; // turret_combat -> turret_gadget
                else if (lowerName.includes('grenade')) itemType = 'grenade';
                else if (lowerName.includes('artifact')) itemType = 'artifact';
                else itemType = 'weapon'; // Default to weapon if unknown
            }
            
            const genericBaseComps = [];
            // Build comp keys to search for (comp_01_common through comp_05_legendary)
            // First, try to get comp_* entries from the CURRENT root (manufacturer-specific)
            // This ensures atl_enhancement uses its own comp_* entries, not generic Enhancement
            const compKeys = ['comp_01_common', 'comp_02_uncommon', 'comp_03_rare', 'comp_04_epic', 'comp_05_legendary'];
            
            // Helper function to add comp if found
            function addCompIfFound(compKey, rules, aspects, item) {
                // Check if this is a comp_* entry (comp_01_common through comp_05_legendary)
                if (compKeys.includes(compKey) && rules) {
                    if (!genericBaseComps.some(g => g.compKey === compKey)) {
                        const compData = {
                            compKey: compKey,
                            itemType: itemType,
                            rules: rules,
                            aspects: aspects || [],
                            item: item || null
                        };
                        genericBaseComps.push(compData);
                    }
                }
            }
            
            // FIRST: Check current root's depEntries for comp_* entries (manufacturer-specific)
            // This ensures atl_enhancement uses its own comp_* definitions, not generic Enhancement root
            // The rules will include basecomposition inheritance when actually used by the resolver
            if (root.depEntries) {
                root.depEntries.forEach(dep => {
                    if (dep.depTableName === 'inv_comp') {
                        const compKey = dep.key || '';
                        if (compKeys.includes(compKey)) {
                            // Get rules from this comp entry (manufacturer-specific)
                            // If it has basecomposition, the resolver will handle inheritance when rules are used
                            let rules = dep.parttypeselectionrules || dep.item?.parttypeselectionrules;
                            // Also try to get inherited rules from basecomposition for display
                            // This ensures the section shows the resolved rules, not just empty manufacturer comp rules
                            if (!rules || !rules.pairs) {
                                const baseComp = dep.basecomposition || dep.item?.basecomposition;
                                if (baseComp && typeof baseComp === 'string') {
                                    const m = baseComp.match(/inv'[^']+\.([^']+)'/i);
                                    let baseCompKey = m ? m[1] : null;
                                    if (!baseCompKey) {
                                        const fallbackMatch = baseComp.match(/\.([^']+)'?$/);
                                        if (fallbackMatch && fallbackMatch[1]) {
                                            baseCompKey = fallbackMatch[1];
                                        }
                                    }
                                    if (baseCompKey) {
                                        // Find the base comp entry to get its rules
                                        let baseCompEntry = root.depEntries?.find(d => d.depTableName === 'inv_comp' && d.key === baseCompKey);
                                        if (!baseCompEntry && data && data.rootDefinitions) {
                                            for (const r of data.rootDefinitions) {
                                                baseCompEntry = r.depEntries?.find(d => d.depTableName === 'inv_comp' && d.key === baseCompKey);
                                                if (baseCompEntry) break;
                                            }
                                        }
                                        if (!baseCompEntry && data && data.subItemsByTable && data.subItemsByTable.inv_comp) {
                                            baseCompEntry = data.subItemsByTable.inv_comp.items?.find(item => item.key === baseCompKey);
                                        }
                                        if (baseCompEntry) {
                                            rules = baseCompEntry.parttypeselectionrules || baseCompEntry.item?.parttypeselectionrules;
                                        }
                                    }
                                }
                            }
                            // Add comp entry from current root (manufacturer-specific)
                            // This ensures part counts come from atl_enhancement, not generic Enhancement
                            if (rules && rules.pairs) {
                                addCompIfFound(compKey, rules, dep.aspects, dep.item);
                            }
                        }
                    }
                });
            }
            
            // Build base_comp keys for fallback (if no comp_* found in current root)
            const baseCompKeys = [];
            const rarities = ['01_common', '02_uncommon', '03_rare', '04_epic', '05_legendary'];
            
            // Try item-type-specific first (e.g., base_comp_enhancement_01_common)
            rarities.forEach(rarity => {
                baseCompKeys.push(`base_comp_${itemType}_${rarity}`);
            });
            
            // Also try generic pattern (base_comp_01_common) - works for weapons and as fallback
            rarities.forEach(rarity => {
                baseCompKeys.push(`base_comp_${rarity}`);
            });
            
            // Helper function to add base_comp if found (for fallback)
            function addBaseCompIfFound(compKey, rules, aspects, item) {
                // Check if this compKey matches any of our base_comp patterns
                const matchesPattern = baseCompKeys.some(pattern => {
                    // Exact match
                    if (compKey === pattern) return true;
                    // Pattern match (e.g., base_comp_01_common matches base_comp_*_01_common)
                    const patternParts = pattern.split('_');
                    const keyParts = compKey.split('_');
                    if (patternParts.length === keyParts.length) {
                        // Check if rarity matches (last two parts)
                        return patternParts.slice(-2).join('_') === keyParts.slice(-2).join('_');
                    }
                    return false;
                });
                
                if (matchesPattern && rules) {
                    // Use a unique key that includes item type to avoid conflicts
                    const uniqueKey = `${itemType}_${compKey}`;
                    if (!genericBaseComps.some(g => g.compKey === compKey && g.itemType === itemType)) {
                        const baseCompData = {
                            compKey: compKey,
                            itemType: itemType,
                            rules: rules,
                            aspects: aspects || [],
                            item: item || null
                        };
                        genericBaseComps.push(baseCompData);
                        // Also store globally with unique key
                        window.globalBaseComps.set(uniqueKey, baseCompData);
                    }
                }
            }
            
            // Fallback: Check for base_comp_* entries if no comp_* entries found
            if (genericBaseComps.length === 0 && root.depEntries) {
                root.depEntries.forEach(dep => {
                    if (dep.depTableName === 'inv_comp') {
                        const compKey = dep.key || '';
                        if (compKey.startsWith('base_comp_')) {
                            addBaseCompIfFound(compKey, dep.parttypeselectionrules, dep.aspects, dep.item);
                        }
                    }
                });
            }
            
            // Only search for base_comp_* entries if we didn't find comp_* entries in current root
            // This ensures manufacturer enhancements use their own comp_* entries, not generic base_comp_*
            if (genericBaseComps.length === 0) {
                // FIRST: Check if current root is manufacturer-specific (e.g., vla_grenade_gadget, jak_terminal_combat)
                // If so, look for comp_* entries in the base generic root (e.g., grenade_gadget, terminal_gadget)
                // This handles cases where manufacturer roots don't have comp_* but the base generic root does
                const rootKeyLower = (root.key || '').toLowerCase();
                let baseGenericRootKey = null;
                
                // Special handling: terminal_combat, terminal_*, turret_* should map to terminal_gadget/turret_gadget
                if (rootKeyLower.includes('terminal_') && !rootKeyLower.includes('terminal_gadget')) {
                    // Map terminal_combat, terminal_*, etc. to terminal_gadget
                    if (rootKeyLower.includes('terminal_combat') || rootKeyLower.includes('terminal_')) {
                        baseGenericRootKey = 'terminal_gadget';
                    }
                } else if (rootKeyLower.includes('turret_') && !rootKeyLower.includes('turret_gadget')) {
                    // Map turret_combat, turret_*, etc. to turret_gadget
                    if (rootKeyLower.includes('turret_combat') || rootKeyLower.includes('turret_')) {
                        baseGenericRootKey = 'turret_gadget';
                    }
                } else if (rootKeyLower.indexOf('repkit') !== -1) {
                    baseGenericRootKey = 'repair_kit';
                } else {
                    // Check other patterns
                    const itemTypePatterns = ['grenade_gadget', 'heavy_weapon_gadget', 'turret_gadget', 'terminal_gadget', 'enhancement', 'classmod', 'repair_kit', 'shield'];
                    for (const pattern of itemTypePatterns) {
                        if (rootKeyLower.includes(pattern) && rootKeyLower !== pattern) {
                            baseGenericRootKey = pattern;
                            break;
                        }
                    }
                }
                
                // If we found a base generic root key, search for comp_* entries in it
                if (baseGenericRootKey && data && data.rootDefinitions) {
                    const baseGenericRoot = data.rootDefinitions.find(r => {
                        const rKey = (r.key || '').toLowerCase();
                        return rKey === baseGenericRootKey;
                    });
                    
                    if (baseGenericRoot && baseGenericRoot.depEntries) {
                        // Search for comp_* entries in the base generic root
                        baseGenericRoot.depEntries.forEach(dep => {
                            if (dep.depTableName === 'inv_comp') {
                                const compKey = dep.key || '';
                                if (compKeys.includes(compKey)) {
                                    const rules = dep.parttypeselectionrules || dep.item?.parttypeselectionrules;
                                    if (rules && rules.pairs) {
                                        addCompIfFound(compKey, rules, dep.aspects, dep.item);
                                    }
                                }
                            }
                        });
                    }
                }
                
                // Search in all root definitions for base_comp entries (fallback)
                // Look for roots that match this item type
                if (genericBaseComps.length === 0 && data && data.rootDefinitions) {
                    data.rootDefinitions.forEach(otherRoot => {
                        // Check if this root matches our item type
                        const otherBasetype = otherRoot.basetype || '';
                        const otherItembasename = otherRoot.itembasename || otherRoot.key || '';
                        let otherItemType = '';
                        
                        if (otherBasetype) {
                            const basetypeMatch = otherBasetype.match(/inv'(\w+)/i);
                            if (basetypeMatch) {
                                const basetypeItemType = basetypeMatch[1].toLowerCase();
                                
                                // Special handling: terminal_gadget_combat, terminal_gadget_*, turret_gadget_* should map to terminal_gadget/turret_gadget
                                if (basetypeItemType.includes('terminal_gadget') && basetypeItemType !== 'terminal_gadget') {
                                    otherItemType = 'terminal_gadget';
                                } else if (basetypeItemType.includes('turret_gadget') && basetypeItemType !== 'turret_gadget') {
                                    otherItemType = 'turret_gadget';
                                } else {
                                    otherItemType = basetypeItemType;
                                }
                            }
                        }
                        
                        // If root key matches item type (e.g., "weapon", "shield"), include it
                        const rootKeyLower = (otherRoot.key || '').toLowerCase();
                        if (!otherItemType && (rootKeyLower === itemType || rootKeyLower.includes(itemType))) {
                            otherItemType = itemType;
                        }
                        
                        // Search this root's depEntries if it matches our item type or is a generic root
                        if (!otherItemType || otherItemType === itemType || rootKeyLower === itemType) {
                            if (otherRoot.depEntries) {
                                otherRoot.depEntries.forEach(dep => {
                                    if (dep.depTableName === 'inv_comp') {
                                        const compKey = dep.key || '';
                                        if (compKey.startsWith('base_comp_')) {
                                            addBaseCompIfFound(compKey, dep.parttypeselectionrules, dep.aspects, dep.item);
                                        }
                                    }
                                });
                            }
                        }
                    });
                }
                
                // Also check in subItemsByTable for inv_comp entries
                if (data && data.subItemsByTable && data.subItemsByTable.inv_comp) {
                    const invCompItems = data.subItemsByTable.inv_comp.items || [];
                    invCompItems.forEach(item => {
                        const key = item.key || '';
                        if (key.startsWith('base_comp_')) {
                            const rules = item.parttypeselectionrules || item.item?.parttypeselectionrules || 
                                        (item.item && typeof item.item === 'object' && item.item.parttypeselectionrules);
                            addBaseCompIfFound(key, rules, item.aspects || item.item?.aspects, item);
                        }
                    });
                }
                
                // Also check root.dependencyTables for inv_comp entries
                if (root.dependencyTables && root.dependencyTables.inv_comp) {
                    const invCompItems = root.dependencyTables.inv_comp || [];
                    invCompItems.forEach(item => {
                        const key = item.key || '';
                        if (key.startsWith('base_comp_')) {
                            const rules = item.parttypeselectionrules || item.item?.parttypeselectionrules;
                            addBaseCompIfFound(key, rules, item.aspects || item.item?.aspects, item);
                        }
                    });
                }
                
                // Also check global storage (from previous searches)
                baseCompKeys.forEach(key => {
                    const uniqueKey = `${itemType}_${key}`;
                    if (window.globalBaseComps.has(uniqueKey) && !genericBaseComps.some(g => g.compKey === key && g.itemType === itemType)) {
                        genericBaseComps.push(window.globalBaseComps.get(uniqueKey));
                    }
                });
            }

            // If we have generic base comps, add a special section for them (Legit tab only — hidden on Modded)
            let genericSectionHTML = '';
            const hideGenericPartCounts =
                (renderOpts && renderOpts.useCompactDepTables) ||
                !!(window.__lbCtx && window.__lbCtx.relaxedPicker);
            if (genericBaseComps.length > 0 && !hideGenericPartCounts) {
                // Sort base comps by rarity order (01_common, 02_uncommon, 03_rare, 04_epic, 05_legendary)
                // Handle both comp_* and base_comp_* patterns
                genericBaseComps.sort((a, b) => {
                    const order = {
                        'comp_01_common': 1,
                        'comp_02_uncommon': 2,
                        'comp_03_rare': 3,
                        'comp_04_epic': 4,
                        'comp_05_legendary': 5,
                        'base_comp_01_common': 1,
                        'base_comp_02_uncommon': 2,
                        'base_comp_03_rare': 3,
                        'base_comp_04_epic': 4,
                        'base_comp_05_legendary': 5
                    };
                    return (order[a.compKey] || 99) - (order[b.compKey] || 99);
                });
                
                genericSectionHTML = '<div style="margin-top: var(--section-gap); padding: var(--input-pad-y) var(--input-pad-x); background: #1a1a1a; border-radius: 6px; border-left: 3px solid #9C27B0;">';
                // Use current root's display name instead of generic itemType
                // For manufacturer enhancements (atl_enhancement, cov_enhancement), show the manufacturer name
                let rootDisplayName = root.itembasename || root.key || itemType;
                // Extract manufacturer-specific name (e.g., "ATL Enhancement", "CoV Enhancement")
                if (root.itembasename) {
                    const basenameMatch = root.itembasename.match(/,\s*([^,]+)$/);
                    if (basenameMatch && basenameMatch[1]) {
                        rootDisplayName = basenameMatch[1].trim();
                    }
                } else if (root.key) {
                    // Format root key for display (e.g., "atl_enhancement" -> "ATL Enhancement")
                    rootDisplayName = root.key.split('_').map((part, idx) => {
                        if (idx === 0) {
                            return part.toUpperCase(); // Manufacturer abbreviation (ATL, COV, etc.)
                        } else {
                            return part.charAt(0).toUpperCase() + part.slice(1); // Enhancement, Shield, etc.
                        }
                    }).join(' ');
                }
                const itemTypeDisplay = rootDisplayName || (itemType.charAt(0).toUpperCase() + itemType.slice(1).replace(/_/g, ' '));
                genericSectionHTML += `<div style="font-weight: bold; color: #9C27B0; margin-bottom: 10px; font-size: 14px;">Component Part Counts (${itemTypeDisplay} - comp_01_common-comp_05_legendary)</div>`;
                genericSectionHTML += '<div style="font-size: 11px; color: #aaa; margin-bottom: 10px; font-style: italic;">Part counts from base composition hierarchy. Parts without explicit counts default to Min: 1, Max: 1 (0 if no valid parts exist).</div>';

                // Collect all possible part types from the data structure
                const allPartTypes = new Set();
                
                // Get part types from all root definitions
                if (data && data.rootDefinitions) {
                    data.rootDefinitions.forEach(r => {
                        if (r.dependencyTables) {
                            Object.keys(r.dependencyTables).forEach(tableName => {
                                if (tableName !== 'inv_comp') {
                                    allPartTypes.add(tableName);
                                }
                            });
                        }
                    });
                }
                
                // Get part types from subItemsByTable
                if (data && data.subItemsByTable) {
                    Object.keys(data.subItemsByTable).forEach(tableName => {
                        if (tableName !== 'inv_comp') {
                            allPartTypes.add(tableName);
                        }
                    });
                }
                
                // Get part types from current root
                if (root.dependencyTables) {
                    Object.keys(root.dependencyTables).forEach(tableName => {
                        if (tableName !== 'inv_comp') {
                            allPartTypes.add(tableName);
                        }
                    });
                }
                
                // Also collect part types that appear in any parttypeselectionrules across all base comps
                genericBaseComps.forEach(({ rules }) => {
                    if (rules && rules.pairs) {
                        Object.values(rules.pairs).forEach(pair => {
                            const partType = pair.key;
                            if (partType) {
                                allPartTypes.add(partType);
                            }
                        });
                    }
                });
                
                const sortedPartTypes = Array.from(allPartTypes).sort();

                genericBaseComps.forEach(({ compKey, rules }) => {
                    // Handle both comp_* and base_comp_* patterns
                    const rarityMatch = compKey.match(/(?:base_)?comp_0(\d)_(\w+)/);
                    const rarityNum = rarityMatch ? rarityMatch[1] : '';
                    const rarityName = rarityMatch ? rarityMatch[2] : '';
                    const rarityDisplay = rarityNum + '_' + rarityName;

                    genericSectionHTML += `<div style="margin-bottom: 15px; padding: 8px; background: #0f0f0f; border-radius: 4px;">`;
                    genericSectionHTML += `<div style="font-weight: bold; color: #9C27B0; margin-bottom: 8px; font-size: 13px;">${compKey} (${rarityDisplay})</div>`;

                    // Build a map of part types to their partcount data from rules
                    const partCountMap = new Map();
                    if (rules && rules.pairs) {
                        Object.values(rules.pairs).forEach(pair => {
                            const partType = pair.key || 'Unknown';
                            const value = pair.value || {};
                            const partCount = value.partcount || {};
                            partCountMap.set(partType, {
                                min: partCount.min !== undefined ? parseInt(partCount.min) : null,
                                max: partCount.max !== undefined ? parseInt(partCount.max) : null
                            });
                        });
                    }

                    genericSectionHTML += `<div style="margin-bottom: 10px;">`;
                    genericSectionHTML += `<div style="font-weight: bold; color: #fff; margin-bottom: 6px; font-size: 12px;">Part Count Rules:</div>`;
                    
                    // Filter out part types with unknown part counts (both min and max are null)
                    const definedPartTypes = sortedPartTypes.filter(partType => {
                        const partCountData = partCountMap.get(partType);
                        const minCount = partCountData ? partCountData.min : null;
                        const maxCount = partCountData ? partCountData.max : null;
                        // Only show if at least one count is defined (not null)
                        return minCount !== null || maxCount !== null;
                    });
                    
                    // Display count only for part types with defined counts
                    definedPartTypes.forEach(partType => {
                        const partCountData = partCountMap.get(partType);
                        const minCount = partCountData ? partCountData.min : null;
                        const maxCount = partCountData ? partCountData.max : null;

                        genericSectionHTML += `<div style="margin-bottom: 8px; padding: 6px; background: #0a0a0a; border-radius: 3px; border-left: 2px solid #9C27B0;">`;
                        genericSectionHTML += `<div style="font-weight: bold; color: #9C27B0; margin-bottom: 4px;">${partType}</div>`;
                        
                        // Display partcount (we know at least one is not null from filter above)
                        let countStr = '';
                        if (minCount !== null && maxCount !== null) {
                            if (minCount === maxCount) {
                                countStr = `${minCount}`;
                            } else {
                                countStr = `${minCount} - ${maxCount}`;
                            }
                        } else if (minCount !== null) {
                            countStr = `Min: ${minCount}`;
                        } else if (maxCount !== null) {
                            countStr = `Max: ${maxCount}`;
                        }
                        
                        if (countStr) {
                            const isDisabled = minCount === 0 && maxCount === 0;
                            const countColor = isDisabled ? '#666' : '#9C27B0';
                            genericSectionHTML += `<div style="font-size: 10px; color: ${countColor}; margin-bottom: 4px; font-weight: bold;">Part Count: ${countStr}${isDisabled ? ' (Disabled)' : ''}</div>`;
                        }
                        genericSectionHTML += `</div>`;
                    });
                    genericSectionHTML += `</div>`;
                    genericSectionHTML += `</div>`;
                });

                genericSectionHTML += '</div>';
            }

            if (componentsWithRules.length === 0 && (genericBaseComps.length === 0 || hideGenericPartCounts)) {
                return '';
            }

            let html = '<div style="margin-top: var(--section-gap); padding: var(--input-pad-y) var(--input-pad-x); background: #1a1a1a; border-radius: 6px; border-left: 3px solid #FFA500;">';
            html += '<div style="font-weight: bold; color: #FFA500; margin-bottom: 10px; font-size: 14px;">Part Type Selection Rules & Attribute Modifiers</div>';

            componentsWithRules.forEach(({ compKey, rules, aspects, item }) => {
                html += `<div style="margin-bottom: 15px; padding: 8px; background: #0f0f0f; border-radius: 4px;">`;
                html += `<div style="font-weight: bold; color: #4CAF50; margin-bottom: 8px; font-size: 13px;">Component: ${compKey}</div>`;

                if (rules && rules.pairs) {
                    const pairs = Object.values(rules.pairs).sort((a, b) => (a.key || '').localeCompare(b.key || ''));
                    html += `<div style="margin-bottom: 10px;">`;
                    html += `<div style="font-weight: bold; color: #fff; margin-bottom: 6px; font-size: 12px;">Part Selection Rules:</div>`;
                    
                    pairs.forEach(pair => {
                        const partType = pair.key || 'Unknown';
                        const value = pair.value || {};
                        const parts = value.parts || [];
                        const partCount = value.partcount || {};
                        const minCount = partCount.min !== undefined ? partCount.min : null;
                        const maxCount = partCount.max !== undefined ? partCount.max : null;

                        html += `<div style="margin-bottom: 8px; padding: 6px; background: #0a0a0a; border-radius: 3px; border-left: 2px solid #4CAF50;">`;
                        html += `<div style="font-weight: bold; color: #4CAF50; margin-bottom: 4px;">${partType}</div>`;
                        
                        // Always show partcount if it exists, even if min/max are 0
                        if (partCount.min !== undefined || partCount.max !== undefined) {
                            const minVal = partCount.min !== undefined ? partCount.min : null;
                            const maxVal = partCount.max !== undefined ? partCount.max : null;
                            
                            let countStr = '';
                            if (minVal !== null && maxVal !== null) {
                                if (minVal === maxVal) {
                                    countStr = `${minVal}`;
                                } else {
                                    countStr = `${minVal} - ${maxVal}`;
                                }
                            } else if (minVal !== null) {
                                countStr = `Min: ${minVal}`;
                            } else if (maxVal !== null) {
                                countStr = `Max: ${maxVal}`;
                            }
                            
                            if (countStr) {
                                const isDisabled = minVal === 0 && maxVal === 0;
                                const countColor = isDisabled ? '#666' : '#888';
                                html += `<div style="font-size: 10px; color: ${countColor}; margin-bottom: 4px; font-weight: bold;">Part Count: ${countStr}${isDisabled ? ' (Disabled)' : ''}</div>`;
                            }
                        }

                        if (parts.length > 0) {
                            html += `<div style="font-size: 10px; color: #aaa; margin-top: 4px;">`;
                            html += `<div style="margin-bottom: 3px; color: #fff; font-weight: bold;">Available Parts (${parts.length}):</div>`;
                            parts.forEach(partObj => {
                                const partName = partObj.part || '';
                                const ignoreMinGameStage = partObj.bignoremingamestage === 'true' || partObj.bignoremingamestage === true ? ' ⚠ Ignore Min Stage' : '';
                                html += `<div style="margin-left: 10px; color: #fff; margin-bottom: 2px;">• ${partName}${ignoreMinGameStage}</div>`;
                            });
                            html += `</div>`;
                        } else {
                            html += `<div style="font-size: 10px; color: #666; font-style: italic; margin-top: 4px;">No parts specified</div>`;
                        }
                        html += `</div>`;
                    });
                    html += `</div>`;
                }

                // Display aspects and attribute modifiers
                if (aspects && aspects.length > 0) {
                    html += `<div style="margin-top: 8px; padding: 6px; background: #0a0a0a; border-radius: 3px;">`;
                    html += `<div style="font-weight: bold; color: #FFA500; margin-bottom: 6px; font-size: 12px;">Attribute Scaling Modifiers:</div>`;
                    
                    aspects.forEach((aspect, idx) => {
                        html += `<div style="font-size: 10px; color: #aaa; margin-left: 10px; margin-bottom: 4px; padding: 4px; background: #050505; border-radius: 2px;">`;
                        if (aspect.parent) {
                            const parentName = aspect.parent.replace(/inv_aspect'|'/g, '');
                            html += `<div style="margin-bottom: 2px;"><strong style="color: #FFA500;">Aspect:</strong> <span style="color: #fff;">${parentName}</span></div>`;
                        }
                        if (aspect.dataasset) {
                            const assetPath = aspect.dataasset.replace(/Asset'|'/g, '');
                            html += `<div style="margin-bottom: 2px;"><strong style="color: #4CAF50;">Data Asset:</strong> <span style="color: #aaa; font-size: 9px;">${assetPath}</span></div>`;
                        }
                        if (aspect.attributeeffecttemplate) {
                            const template = aspect.attributeeffecttemplate;
                            if (template.modifiervalue) {
                                const modValue = template.modifiervalue;
                                if (modValue.datatablevalue) {
                                    const dataTable = normalizeDatatableName(modValue.datatablevalue.datatable || '') || 'N/A';
                                    html += `<div style="margin-bottom: 2px;"><strong style="color: #4CAF50;">Data Table:</strong> <span style="color: #aaa;">${dataTable}</span></div>`;
                                    if (modValue.datatablevalue.rowname) {
                                        html += `<div style="margin-bottom: 2px;"><strong style="color: #4CAF50;">Row Name:</strong> <span style="color: #fff;">${modValue.datatablevalue.rowname}</span></div>`;
                                    }
                                    if (data) {
                                        const ref = { type: 'datatable', datatable: modValue.datatablevalue.datatable, rowname: modValue.datatablevalue.rowname, columnname: modValue.datatablevalue.columnname };
                                        const resolvedStr = resolveOneRefForDisplay(ref, data);
                                        if (resolvedStr) html += `<div style="margin-bottom: 2px;"><strong style="color: #8BC34A;">Resolved:</strong> <span style="color: #fff;">${resolvedStr}</span></div>`;
                                    }
                                }
                                if (modValue.attribute) {
                                    const attrName = modValue.attribute.replace(/attribute'|'/g, '');
                                    html += `<div style="margin-bottom: 2px;"><strong style="color: #FFA500;">Attribute:</strong> <span style="color: #fff;">${attrName}</span></div>`;
                                    if (data) {
                                        const ref = { type: 'attribute', ref: modValue.attribute };
                                        const resolvedStr = resolveOneRefForDisplay(ref, data);
                                        if (resolvedStr) html += `<div style="margin-bottom: 2px;"><strong style="color: #8BC34A;">Resolved:</strong> <span style="color: #fff;">${resolvedStr}</span></div>`;
                                    }
                                }
                            }
                        }
                        if (aspect.params) {
                            const params = aspect.params.replace(/inv_params'|'/g, '');
                            if (params !== 'none') {
                                html += `<div style="margin-bottom: 2px;"><strong style="color: #888;">Params:</strong> <span style="color: #aaa;">${params}</span></div>`;
                            }
                        }
                        html += `</div>`;
                    });
                    html += `</div>`;
                }

                html += `</div>`;
            });

            html += '</div>';
            
            // Return generic section first (if exists), then regular component rules
            return genericSectionHTML + (componentsWithRules.length > 0 ? html : '');
        }

        // Helper function to get base generic root part counts (even when no comp is selected)
        // Universal root definitions apply to ALL manufacturer-specific counterparts
        /**
         * Locate inv_comp row on a generic root (e.g. classmod) for parttypeselectionrules / base slots.
         * Legendary (and similar) variants use keys like comp_05_legendary_cowbell on the item root, but
         * Nexus generic ClassMod only defines comp_05_legendary — without fallback, baseCompSlotTypes stays empty
         * and merged dep tables omit stat_group*, firmware, action_skill_mod, etc.
         */
        function resolveBaseCompRulesEntryOnGenericRoot(baseGenericRoot, baseCompKey) {
            const depEntries = baseGenericRoot && baseGenericRoot.depEntries;
            if (!depEntries || !baseCompKey) return null;
            let k = String(baseCompKey);
            for (let guard = 0; guard < 10 && k; guard++) {
                const found = depEntries.find(dep => dep.depTableName === 'inv_comp' && dep.key === k);
                if (found) return found;
                const parts = k.split('_');
                if (parts.length < 4 || parts[0].toLowerCase() !== 'comp') break;
                k = parts.slice(0, -1).join('_');
            }
            return null;
        }

        function getBaseGenericRootPartCounts(root, selectedComp) {
            // Extract base item type from manufacturer roots (e.g., "atl_enhancement" -> "enhancement")
            // Universal roots apply to all manufacturer-specific counterparts
            const baseItemTypePatterns = ['weapon', 'enhancement', 'shield', 'repair_kit', 'grenade_gadget', 'heavy_weapon_gadget', 'turret_gadget', 'terminal_gadget', 'grenade', 'artifact', 'classmod'];
            let baseItemType = '';
            const rootKeyLower = (root.key || '').toLowerCase();
            
            // Check if current root is a manufacturer-specific variant
            // Special handling: terminal_combat, terminal_*, turret_* should map to terminal_gadget/turret_gadget
            if (rootKeyLower.includes('terminal_') && !rootKeyLower.includes('terminal_gadget')) {
                // Map terminal_combat, terminal_*, etc. to terminal_gadget
                if (rootKeyLower.includes('terminal_combat') || rootKeyLower.includes('terminal_')) {
                    baseItemType = 'terminal_gadget';
                }
            } else if (rootKeyLower.includes('turret_') && !rootKeyLower.includes('turret_gadget')) {
                // Map turret_combat, turret_*, etc. to turret_gadget
                if (rootKeyLower.includes('turret_combat') || rootKeyLower.includes('turret_')) {
                    baseItemType = 'turret_gadget';
                }
            } else {
                for (const pattern of baseItemTypePatterns) {
                    if (rootKeyLower.includes(pattern) && rootKeyLower !== pattern) {
                        // Extract the base type (e.g., "atl_enhancement" -> "enhancement")
                        const match = rootKeyLower.match(new RegExp(`(?:\\w+_)?(${pattern.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')})(?:\\w+)?$`, 'i'));
                        if (match && match[1]) {
                            baseItemType = match[1].toLowerCase();
                            break;
                        }
                    } else if (rootKeyLower === pattern) {
                        baseItemType = pattern;
                        break;
                    }
                }
            }
            
            // Also check basetype for base item type extraction
            const rootBasetype = (root.basetype || '').toLowerCase();
            if (!baseItemType && rootBasetype) {
                const basetypeMatch = rootBasetype.match(/inv'(\w+)/i);
                if (basetypeMatch) {
                    const basetypeItemType = basetypeMatch[1].toLowerCase();
                    
                    // Special handling: terminal_gadget_combat, terminal_gadget_*, turret_gadget_* should map to terminal_gadget/turret_gadget
                    if (basetypeItemType.includes('terminal_gadget') && basetypeItemType !== 'terminal_gadget') {
                        // Map terminal_gadget_combat, terminal_gadget_*, etc. to terminal_gadget
                        baseItemType = 'terminal_gadget';
                    } else if (basetypeItemType.includes('turret_gadget') && basetypeItemType !== 'turret_gadget') {
                        // Map turret_gadget_combat, turret_gadget_*, etc. to turret_gadget
                        baseItemType = 'turret_gadget';
                    } else {
                        // Extract base type from basetype (e.g., "Enhancement" -> "enhancement")
                        for (const pattern of baseItemTypePatterns) {
                            if (basetypeItemType.includes(pattern) && basetypeItemType !== pattern) {
                                const match = basetypeItemType.match(new RegExp(`(?:\\w+_)?(${pattern})(?:\\w+)?$`, 'i'));
                                if (match && match[1]) {
                                    baseItemType = match[1].toLowerCase();
                                    break;
                                }
                            } else if (basetypeItemType === pattern) {
                                baseItemType = pattern;
                                break;
                            }
                        }
                    }
                }
            }
            
            // Fallback: try to infer from itembasename or key
            if (!baseItemType) {
                const rootItembasename = root.itembasename || root.key || '';
                const lowerName = rootItembasename.toLowerCase();
                if (lowerName.includes('weapon')) baseItemType = 'weapon';
                else if (lowerName.includes('enhancement')) baseItemType = 'enhancement';
                else if (lowerName.includes('shield')) baseItemType = 'shield';
                else if (lowerName.includes('repair') || lowerName.includes('repair_kit')) baseItemType = 'repair_kit';
                else if (lowerName.includes('grenade')) baseItemType = 'grenade';
                else if (lowerName.includes('artifact')) baseItemType = 'artifact';
                else baseItemType = 'weapon';
            }
            
            // Find base generic root (e.g., "enhancement" for "atl_enhancement", "dad_enhancement", "cov_enhancement")
            // Universal root definitions apply to ALL manufacturer-specific counterparts
            let baseGenericRoot = null;
            
            // Always try to find base generic root if current root key doesn't match baseItemType exactly
            if (rootKeyLower !== baseItemType) {
                if (data && data.rootDefinitions) {
                    baseGenericRoot = data.rootDefinitions.find(r => {
                        const rKey = (r.key || '').toLowerCase();
                        const rBasetype = (r.basetype || '').toLowerCase();
                        
                        // Match if key is exactly the baseItemType (e.g., "enhancement")
                        if (rKey === baseItemType) {
                            return true;
                        }
                        
                        // Or if basetype matches (e.g., "inv'enhancement'" or "inv'Enhancement'")
                        const basetypeMatch = rBasetype.match(/inv'(\w+)/i);
                        if (basetypeMatch) {
                            const basetypeItemType = basetypeMatch[1].toLowerCase();
                            // Extract base type from basetype if it's a manufacturer root
                            let basetypeBaseType = basetypeItemType;
                            for (const pattern of baseItemTypePatterns) {
                                if (basetypeItemType.includes(pattern) && basetypeItemType !== pattern) {
                                    const match = basetypeItemType.match(new RegExp(`(?:\\w+_)?(${pattern})(?:\\w+)?$`, 'i'));
                                    if (match && match[1]) {
                                        basetypeBaseType = match[1].toLowerCase();
                                        break;
                                    }
                                } else if (basetypeItemType === pattern) {
                                    basetypeBaseType = pattern;
                                    break;
                                }
                            }
                            return basetypeBaseType === baseItemType;
                        }
                        
                        return false;
                    });
                }
            } else {
                // Current root IS the base generic root
                baseGenericRoot = root;
            }
            
            // Extract part counts from base generic root's comps.
            // Use the same inv_comp row resolution as merge: exact key first, then strip variant suffix
            // (e.g. comp_05_legendary_cowbell -> comp_05_legendary). Do not cross rarity (no epic -> legendary).
            const basePartCounts = new Map();
            if (baseGenericRoot && baseGenericRoot.depEntries && selectedComp) {
                const defaultComp = resolveBaseCompRulesEntryOnGenericRoot(baseGenericRoot, selectedComp);
                
                if (defaultComp) {
                    const rules = defaultComp.parttypeselectionrules || defaultComp.item?.parttypeselectionrules;
                    if (rules && rules.pairs) {
                            let pairs = [];
                            if (Array.isArray(rules.pairs)) {
                                pairs = rules.pairs;
                            } else if (typeof rules.pairs === 'object') {
                                pairs = Object.values(rules.pairs);
                            }
                            
                            pairs.forEach(pair => {
                                if (!pair || !pair.key) return;
                                
                                const slotName = pair.key;
                                // Only add if we don't already have this slot (first comp found takes precedence)
                                if (!basePartCounts.has(slotName)) {
                                    const value = pair.value || {};
                                    const partCount = value.partcount || {};
                                    const hasExplicitMin = partCount.min !== undefined && partCount.min !== null;
                                    const hasExplicitMax = partCount.max !== undefined && partCount.max !== null;
                                    
                                    let minCount = null;
                                    let maxCount = null;
                                    
                                    if (partCount.min !== undefined && partCount.min !== null) {
                                        const parsed = parseInt(partCount.min);
                                        if (!isNaN(parsed)) minCount = parsed;
                                    }
                                    if (partCount.max !== undefined && partCount.max !== null) {
                                        const parsed = parseInt(partCount.max);
                                        if (!isNaN(parsed)) maxCount = parsed;
                                    }
                                    
                                    // Handle defaults
                                    let effectiveMin = minCount;
                                    let effectiveMax = maxCount;
                                    
                                    if (minCount === 0 && maxCount === null) {
                                        effectiveMax = 1;
                                    } else if (minCount === null && maxCount === null) {
                                        effectiveMin = 0;
                                        effectiveMax = 1;
                                    } else if (minCount === null) {
                                        effectiveMin = 1;
                                    } else if (maxCount === null && minCount !== 0) {
                                        effectiveMax = 1;
                                    }
                                    
                                    basePartCounts.set(slotName, {
                                        min: effectiveMin,
                                        max: effectiveMax,
                                        displayMin: minCount,
                                        displayMax: maxCount,
                                        isExplicit: hasExplicitMin || hasExplicitMax,
                                        hasExplicitMin,
                                        hasExplicitMax,
                                        countSource: 'base_generic'
                                    });
                                }
                            });
                    }
                }
            }
            
            return basePartCounts;
        }

        var MI_CROSS_TABLE = '_mi_cross_manufacturer';
        /** Modded: parts missing from the loaded dataset (still serialized as raw tokens). */
        var MI_DOES_NOT_EXIST_TABLE = '_mi_does_not_exist';
        /** Modded tab: max distinct picked lines per slot (inv_comp stays 1). Avoids unbounded DOM. */
        var MI_MODDED_MAX_PICKED_LINES = 2048;

        /** Larger listbox on narrow viewports / touch for Item Editor browse selects. */
        function miBrowseCandidateListSize() {
            try {
                if (typeof window === 'undefined' || !window.matchMedia) return 8;
                if (window.matchMedia('(max-width: 1024px)').matches) return 14;
                if (window.matchMedia('(pointer: coarse)').matches) return 12;
            } catch (e) {
                /* ignore */
            }
            return 8;
        }

        (function setupMiBrowseListSizeOnResize() {
            if (typeof window === 'undefined' || window.__miBrowseListResizeBound) return;
            window.__miBrowseListResizeBound = true;
            var rTimer = null;
            function syncMiBrowseSelectSizes() {
                var tab = document.getElementById('item-editor-tab');
                if (!tab) return;
                var n = miBrowseCandidateListSize();
                tab.querySelectorAll('select.mi-slot-candidate-list').forEach(function (el) {
                    el.size = n;
                });
            }
            window.addEventListener('resize', function () {
                clearTimeout(rTimer);
                rTimer = setTimeout(syncMiBrowseSelectSizes, 200);
            });
            window.addEventListener('orientationchange', function () {
                setTimeout(syncMiBrowseSelectSizes, 300);
            });
        })();

        /** Preserves deserialized token order when rebuilding modded human serial (slot reorder is DOM-only). */
        function miPartLineEmitOrderFromValidatorPart(p) {
            if (!p) return 0;
            const pi = p.patternTokenIndex;
            const wi = p.withinArrayIndex;
            const piN = pi != null && !isNaN(Number(pi)) ? Number(pi) : 0;
            const wiN = wi != null && !isNaN(Number(wi)) ? Number(wi) : 0;
            let idxN = 0;
            if (typeof p.index === 'number' && !isNaN(p.index)) {
                idxN = p.index;
            } else if (p.index != null) {
                const m = String(p.index).match(/\d+/);
                if (m) idxN = parseInt(m[0], 10) || 0;
            }
            return piN * 1e12 + wiN * 1e6 + idxN;
        }

        /** Normalized dep tables that have a native compact picker slot for this root (no inv_comp body here — slot HTML adds it). */
        function getModdedNativeSlotNormSet(rootDef, rootDefinitions) {
            const set = new Set();
            if (!rootDef) return set;
            set.add(normalizeDepTableName('inv_comp'));
            const partSelectionOrder = getPartSelectionOrderFromRoot(rootDef, rootDefinitions) || [];
            const partSelectionOrderLower = partSelectionOrder.map((s) => (s || '').toLowerCase());
            const depTables = rootDef.dependencyTables || {};
            const depEntries = rootDef.depEntries || [];
            if (Object.keys(depTables).length > 0) {
                Object.entries(depTables).forEach(([tableName]) => {
                    if (partSelectionOrderLower.includes((tableName || '').toLowerCase())) {
                        const n = normalizeDepTableName(tableName);
                        if (n && n !== 'unknown') set.add(n);
                    }
                });
            } else {
                depEntries.forEach((item) => {
                    if (item.depTableName) {
                        const n = normalizeDepTableName(item.depTableName);
                        if (n && n !== 'unknown') set.add(n);
                    }
                });
            }
            const compRules = getSelectedCompRules(rootDef);
            if (compRules && compRules.partCounts && compRules.partCounts.size > 0) {
                compRules.partCounts.forEach((_v, slotKey) => {
                    const n = normalizeDepTableName(slotKey);
                    if (n && n !== 'unknown') set.add(n);
                });
            }
            return set;
        }

        /** Raw stat_group* slot keys from comp rules (partCounts / allowedParts). */
        function moddedStatGroupSlotCandidatesFromComp(compRules) {
            const out = [];
            if (!compRules) return out;
            const seen = new Set();
            const tryAdd = (raw) => {
                if (raw == null || raw === '') return;
                const n = normalizeDepTableName(raw);
                if (n !== 'stat_group1' && n !== 'stat_group2' && n !== 'stat_group3') return;
                const k = String(raw);
                if (seen.has(k)) return;
                seen.add(k);
                out.push(k);
            };
            if (compRules.partCounts && compRules.partCounts.size > 0) {
                compRules.partCounts.forEach((_v, slotKey) => tryAdd(slotKey));
            }
            if (compRules.allowedParts && compRules.allowedParts.size > 0) {
                compRules.allowedParts.forEach((_v, slotKey) => tryAdd(slotKey));
            }
            const orderPref = ['stat_group1', 'stat_group2', 'stat_group3'];
            out.sort((a, b) => orderPref.indexOf(normalizeDepTableName(a)) - orderPref.indexOf(normalizeDepTableName(b)));
            return out;
        }

        /**
         * Map root-234-style stat_* parts to stat_group1/2/3 when comp rules list them (native slot name ≠ dep table on substat root).
         */
        function moddedPickStatGroupTableForPartKey(compRoot, plainPartKey, dataRef) {
            if (!compRoot || !plainPartKey || !dataRef || !dataRef.rootDefinitions) return '';
            const pk = String(plainPartKey).trim();
            if (!pk) return '';
            const pkLower = pk.toLowerCase();
            const compRules = getSelectedCompRules(compRoot);
            if (!compRules) return '';

            const candidates = moddedStatGroupSlotCandidatesFromComp(compRules);
            for (let i = 0; i < candidates.length; i++) {
                const slotKey = candidates[i];
                const allowed = getAllowedPartsForTable(compRules, slotKey);
                if (allowed && allowed.size > 0) {
                    for (const ap of allowed) {
                        if (ap != null && String(ap).toLowerCase() === pkLower) {
                            return slotKey;
                        }
                    }
                }
            }

            const nativeSlots = getModdedNativeSlotNormSet(compRoot, dataRef.rootDefinitions);
            const hasG1 = nativeSlots.has('stat_group1');
            const hasG2 = nativeSlots.has('stat_group2');
            if (pkLower.startsWith('stat2_')) {
                if (hasG2) {
                    return moddedPickRootDepTableKeyForNorm(compRoot, 'stat_group2', dataRef.rootDefinitions) || 'stat_group2';
                }
                if (hasG1) {
                    return moddedPickRootDepTableKeyForNorm(compRoot, 'stat_group1', dataRef.rootDefinitions) || 'stat_group1';
                }
            }
            if (pkLower.startsWith('stat_') && hasG1) {
                return moddedPickRootDepTableKeyForNorm(compRoot, 'stat_group1', dataRef.rootDefinitions) || 'stat_group1';
            }
            return '';
        }

        /** If routing says cross, try stat_group slot from comp rules / heuristics. */
        function moddedResolveMiTableNameAfterCrossCheck(compRoot, tableNameAfterNativeOrCross, partKey, dataRef) {
            if (
                !compRoot ||
                !partKey ||
                !dataRef ||
                tableNameAfterNativeOrCross !== MI_CROSS_TABLE
            ) {
                return tableNameAfterNativeOrCross;
            }
            const crossPr = parseMiCrossPartRef(partKey);
            const plain = crossPr ? crossPr.partKey : String(partKey);
            const sg = moddedPickStatGroupTableForPartKey(compRoot, plain, dataRef);
            return sg || tableNameAfterNativeOrCross;
        }

        /** Pearlescent lines may be depTableName pearl_stat while the compact UI slot is pearl_elem (or vice versa). */
        var MI_MODDED_NATIVE_SLOT_ALIAS_GROUPS = [['pearl_elem', 'pearl_stat']];
        var MI_PEARL_SLOT_TYPES = ['pearl_elem', 'pearl_stat'];

        /** True when this root’s part selection order includes pearl_elem and/or pearl_stat (from Nexus parttypes). */
        function rootPartSelectionIncludesPearl(root, rootDefinitions) {
            const order = getPartSelectionOrderFromRoot(root, rootDefinitions) || [];
            const orderLower = order.map((s) => (s || '').toLowerCase());
            return MI_PEARL_SLOT_TYPES.some((pt) => orderLower.includes(pt));
        }

        /** Pearl dep rows live on generic weapon root (inv0); merge into depTables for shields, HW, grenades, mfr weapons. */
        function mergePearlDepsFromWeaponRoot(depTables, dataRef) {
            if (!depTables || !dataRef || !dataRef.rootDefinitions) return;
            const weaponRoot = dataRef.rootDefinitions.find((r) => (r.key || '').toLowerCase() === 'weapon');
            if (!weaponRoot || !weaponRoot.depEntries) return;
            const pearlSet = new Set(MI_PEARL_SLOT_TYPES);
            weaponRoot.depEntries.forEach((dep) => {
                const tableName = dep.depTableName;
                if (!tableName || tableName === 'inv_comp') return;
                const tableNameLower = (tableName || '').toLowerCase();
                if (!pearlSet.has(tableNameLower)) return;
                if (!depTables[tableName]) depTables[tableName] = [];
                const exists = depTables[tableName].some(
                    (item) =>
                        item.key === dep.key &&
                        item.depTableName === dep.depTableName &&
                        item.rootKey === (dep.rootKey || weaponRoot.key)
                );
                if (exists) return;
                const depWithRootSerial = Object.assign({}, dep);
                if (weaponRoot.serialIndex !== undefined) {
                    depWithRootSerial.rootSerialIndex = weaponRoot.serialIndex;
                    depWithRootSerial.rootKey = weaponRoot.key;
                }
                depTables[tableName].push(depWithRootSerial);
            });
        }

        /** If partNorm matches a rendered native slot (including pearl_elem ↔ pearl_stat), return that slot’s normalized name. */
        function moddedNativeNormMatchesSlot(nativeSlots, partNorm) {
            if (!partNorm || partNorm === 'unknown') return null;
            if (nativeSlots.has(partNorm)) return partNorm;
            for (let g = 0; g < MI_MODDED_NATIVE_SLOT_ALIAS_GROUPS.length; g++) {
                const group = MI_MODDED_NATIVE_SLOT_ALIAS_GROUPS[g];
                if (!group.some((x) => x === partNorm)) continue;
                for (let i = 0; i < group.length; i++) {
                    if (nativeSlots.has(group[i])) return group[i];
                }
            }
            return null;
        }

        /** Root depTables / depEntries key that normalizes to matchedNorm and appears in compact UI (matches data-table-name). */
        function moddedPickRootDepTableKeyForNorm(compRoot, matchedNorm, rootDefinitions) {
            if (!compRoot || !matchedNorm) return '';
            const partSelectionOrder = getPartSelectionOrderFromRoot(compRoot, rootDefinitions) || [];
            const partSelectionOrderLower = partSelectionOrder.map((s) => (s || '').toLowerCase());
            const depTables = compRoot.dependencyTables || {};
            const depEntries = compRoot.depEntries || [];
            const candidates = [];
            if (Object.keys(depTables).length > 0) {
                Object.keys(depTables).forEach((k) => {
                    if (normalizeDepTableName(k) !== matchedNorm) return;
                    if (!partSelectionOrderLower.includes((k || '').toLowerCase())) return;
                    candidates.push(k);
                });
            } else {
                const seen = new Set();
                depEntries.forEach((d) => {
                    if (!d || !d.depTableName) return;
                    if (normalizeDepTableName(d.depTableName) !== matchedNorm) return;
                    const k = d.depTableName;
                    if (seen.has(k)) return;
                    seen.add(k);
                    candidates.push(k);
                });
            }
            if (candidates.length === 0) return matchedNorm;
            candidates.sort((a, b) => {
                const ia = partSelectionOrderLower.indexOf((a || '').toLowerCase());
                const ib = partSelectionOrderLower.indexOf((b || '').toLowerCase());
                if (ia !== -1 && ib !== -1) return ia - ib;
                if (ia !== -1) return -1;
                if (ib !== -1) return 1;
                return String(a).localeCompare(String(b));
            });
            return candidates[0];
        }

        /**
         * Cross-manufacturer bucket only when this composition root has no compact slot for that dep type.
         * Foreign-root parts still use native slots here when the item has them (firmware, pearl_elem, body_ele, …).
         */
        function moddedTableNameNativeOrCross(depTableName, _sourceRootKeyForPart, compRoot) {
            const tn = depTableName || 'Unknown';
            const norm = normalizeDepTableName(tn);
            const nativeSlots = getModdedNativeSlotNormSet(compRoot, data?.rootDefinitions);
            const matchedNorm = moddedNativeNormMatchesSlot(nativeSlots, norm);
            if (!matchedNorm) return MI_CROSS_TABLE;
            return moddedPickRootDepTableKeyForNorm(compRoot, matchedNorm, data?.rootDefinitions) || tn;
        }

        function formatValidatorPartRawToken(p) {
            if (!p) return '';
            if (p.rawPatternText && /^".*"$/.test(String(p.rawPatternText).trim())) {
                return String(p.rawPatternText).trim();
            }
            if (p.rawPatternText && /^\{[^}]+\}$/.test(String(p.rawPatternText).trim())) {
                return String(p.rawPatternText).trim();
            }
            const viaFormat = formatPartTokenForSerial(p);
            if (viaFormat) return viaFormat;
            if (p.serial != null && p.serial !== '') {
                const sub = Number(p.serial);
                if (!Number.isNaN(sub) && p.rootSerial != null && p.rootSerial !== undefined && p.isBaseComp) {
                    const rs = Number(p.rootSerial);
                    if (!Number.isNaN(rs)) return `{${rs}:${sub}}`;
                }
                if (!Number.isNaN(sub)) return `{${sub}}`;
            }
            return '';
        }

        function makeMiMissingPartRef(rawToken) {
            const t = String(rawToken || '').trim();
            if (!t) return '';
            try {
                return (
                    '__miM|' +
                    btoa(unescape(encodeURIComponent(JSON.stringify({ t }))))
                );
            } catch (e) {
                return '';
            }
        }

        function parseMiMissingPartRef(partKey) {
            if (!partKey || String(partKey).indexOf('__miM|') !== 0) return null;
            try {
                const json = decodeURIComponent(escape(atob(String(partKey).slice(6))));
                const o = JSON.parse(json);
                if (o && o.t != null) return { rawToken: String(o.t) };
            } catch (e) {}
            return null;
        }

        /**
         * applyMiLinesToModdedRootDom pulls consecutive same-table lines from the front of the queue per slot.
         * Reorder so buckets follow compact UI slot order, with cross-manufacturer rows last.
         */
        function reorderMiPartLinesForCompactDom(rootDef, lines) {
            if (!lines || !lines.length || !rootDef) return lines || [];
            const buckets = new Map();
            const keyOrder = [];
            lines.forEach((line) => {
                const k = normalizeDepTableName(line.tableName || '');
                const key = k || 'unknown';
                if (!buckets.has(key)) {
                    buckets.set(key, []);
                    keyOrder.push(key);
                }
                buckets.get(key).push(line);
            });
            const slotOrder = (getPartSelectionOrderFromRoot(rootDef, data?.rootDefinitions) || []).map((s) =>
                normalizeDepTableName(s)
            );
            const crossNorm = normalizeDepTableName(MI_CROSS_TABLE);
            const dneNorm = normalizeDepTableName(MI_DOES_NOT_EXIST_TABLE);
            const out = [];
            const used = new Set();
            slotOrder.forEach((slot) => {
                const b = buckets.get(slot);
                if (b && b.length) {
                    b.forEach((l) => out.push(l));
                    used.add(slot);
                }
            });
            const crossB = buckets.get(crossNorm);
            if (crossB && crossB.length && !used.has(crossNorm)) {
                crossB.forEach((l) => out.push(l));
                used.add(crossNorm);
            }
            const dneB = buckets.get(dneNorm);
            if (dneB && dneB.length && !used.has(dneNorm)) {
                dneB.forEach((l) => out.push(l));
                used.add(dneNorm);
            }
            keyOrder.forEach((k) => {
                if (used.has(k)) return;
                const b = buckets.get(k);
                if (b) b.forEach((l) => out.push(l));
            });
            return out;
        }

        function makeMiCrossPartRef(sourceRootKey, partKey) {
            if (!sourceRootKey || !partKey) return '';
            try {
                return (
                    '__miX|' +
                    btoa(unescape(encodeURIComponent(JSON.stringify({ r: String(sourceRootKey), k: String(partKey) }))))
                );
            } catch (e) {
                return '';
            }
        }

        function parseMiCrossPartRef(partKey) {
            if (!partKey || String(partKey).indexOf('__miX|') !== 0) return null;
            try {
                const json = decodeURIComponent(escape(atob(String(partKey).slice(6))));
                const o = JSON.parse(json);
                if (o && o.r && o.k) return { sourceRootKey: o.r, partKey: o.k };
            } catch (e) {}
            return null;
        }

        /** passive_blue_1_1_tier_3 → { chainBase, tier } */
        function parsePassivePointsTierKey(partKey) {
            const m = String(partKey || '').match(/^(.*)_tier_([1-5])$/i);
            if (!m) return null;
            const tier = parseInt(m[2], 10);
            if (Number.isNaN(tier)) return null;
            return { chainBase: m[1], tier };
        }

        /**
         * Modded miLines: qty on a tier_1 passive expands to tier_1…tier_5 in order, then repeats tier_5.
         * Non–tier-1 or non-matching keys repeat the same ref (legacy).
         */
        function expandModdedMiLineToPartKeys(partKey, qty) {
            let q = parseInt(qty, 10);
            if (isNaN(q) || q < 1) q = 1;
            if (q > 99) q = 99;
            if (parseMiMissingPartRef(partKey)) {
                const outM = [];
                for (let i = 0; i < q; i++) outM.push(partKey);
                return outM;
            }
            const cross = parseMiCrossPartRef(partKey);
            const inner = cross ? cross.partKey : String(partKey || '');
            const meta = parsePassivePointsTierKey(inner);
            if (!meta || meta.tier !== 1) {
                const out = [];
                for (let i = 0; i < q; i++) out.push(partKey);
                return out;
            }
            const base = meta.chainBase;
            const sr = cross ? cross.sourceRootKey : null;
            const out = [];
            for (let step = 0; step < q; step++) {
                const tierNum = step < 5 ? step + 1 : 5;
                const plain = `${base}_tier_${tierNum}`;
                out.push(sr ? makeMiCrossPartRef(sr, plain) : plain);
            }
            return out;
        }

        /** Modded: collapse consecutive `{root:sub}` pair tokens with the same root into `{root:[sub1 sub2 ...]}` (runs of length ≥2). */
        function mergeConsecutiveRootPairTokensToArrayTokens(tokens) {
            if (!tokens || !tokens.length) return tokens || [];
            const pairRe = /^\{(\d+):(\d+)\}$/;
            const out = [];
            let i = 0;
            while (i < tokens.length) {
                const m = pairRe.exec(tokens[i]);
                if (!m) {
                    out.push(tokens[i]);
                    i++;
                    continue;
                }
                const rootStr = m[1];
                const subs = [m[2]];
                let j = i + 1;
                while (j < tokens.length) {
                    const m2 = pairRe.exec(tokens[j]);
                    if (!m2 || m2[1] !== rootStr) break;
                    subs.push(m2[2]);
                    j++;
                }
                if (subs.length >= 2) {
                    out.push(`{${rootStr}:[${subs.join(' ')}]}`);
                } else {
                    out.push(tokens[i]);
                }
                i = j;
            }
            return out;
        }

        /**
         * Collapse consecutive `{root:sub}` runs in a modded human line (Deserialize API / Parse / decoded-view rebuild
         * bypass updateFinalOutput's token-array merge).
         */
        function normalizeConsecutiveRootPairTokensInModdedHumanLine(line) {
            const t0 = String(line || '').trim();
            if (!t0 || t0.indexOf('{') < 0 || t0.indexOf('||') < 0) return line;
            const stripped = stripModdedSkinTailFromHumanLine(t0);
            const parts = stripped
                .split('|')
                .map(function (p) {
                    return p.trim();
                })
                .filter(function (p) {
                    return p.length > 0;
                });
            const k = parts.findIndex(function (p) {
                return p.indexOf('{') >= 0;
            });
            if (k < 0) return line;
            const serialListPart = parts.slice(k).join('|');
            const rawTokens = serialListPart.match(/\{[^}]+\}/g);
            if (!rawTokens || rawTokens.length < 2) return line;
            const merged = mergeConsecutiveRootPairTokensToArrayTokens(rawTokens);
            const newSerialListPart = merged.join(' ');
            if (newSerialListPart === serialListPart.trim()) return line;
            const hadTrailingPartListPipe = /\|\s*$/.test(String(stripped).trimEnd());
            const prefixParts = parts.slice(0, k);
            let newStripped =
                prefixParts.length > 0 ? prefixParts.join('|') + '||' + newSerialListPart : newSerialListPart;
            if (hadTrailingPartListPipe && !/\|\s*$/.test(String(newStripped).trimEnd())) {
                newStripped = String(newStripped).trimEnd() + '|';
            }
            return appendMiSkinTailToModdedHumanLine(newStripped);
        }

        /**
         * Stable fingerprint of what updateFinalOutput would emit for one mi line (expanded keys → human tokens).
         * Lets us drop cross rows that repeat the same tokens as a native slot (e.g. pearl_elem vs __miX|… both → {1:55}).
         */
        function miModdedLineSerializationFingerprint(compRoot, line, dataRef) {
            if (!compRoot || !line || !line.partKey || !dataRef) return '';
            const compSer = compRoot.serialIndex;
            if (compSer == null || compSer === undefined || Number.isNaN(Number(compSer))) return '';
            const baseGenericRoot = getBaseGenericRootForPartLookup(compRoot, dataRef);
            const keys = expandModdedMiLineToPartKeys(line.partKey, line.qty != null ? line.qty : 1);
            const parts = [];
            for (let i = 0; i < keys.length; i++) {
                const pk = keys[i];
                const miss = parseMiMissingPartRef(pk);
                if (miss && miss.rawToken) {
                    parts.push(String(miss.rawToken).trim());
                    continue;
                }
                const partEntry = resolvePartEntryForSerialOutput(compRoot, pk, baseGenericRoot, dataRef);
                if (!partEntry) continue;
                const t = formatDeserializedPartToken(compSer, pk, partEntry, dataRef);
                if (t) parts.push(t);
            }
            try {
                return mergeConsecutiveRootPairTokensToArrayTokens(parts).join('\u0001');
            } catch (e) {
                console.warn('miModdedLineSerializationFingerprint merge', e);
                return parts.join('\u0001');
            }
        }

        /** Human-readable token list for breakdown UI (expands passive tier_1 qty into all tier tokens). */
        function formatModdedBreakdownTokensForLine(compRoot, line, baseGenericRoot, dataRef) {
            if (!compRoot || !line || !line.partKey || !dataRef) return '';
            const compSer = compRoot.serialIndex;
            if (compSer == null || compSer === undefined || Number.isNaN(Number(compSer))) return '';
            const keys = expandModdedMiLineToPartKeys(line.partKey, line.qty != null ? line.qty : 1);
            const parts = [];
            for (let i = 0; i < keys.length; i++) {
                const pk = keys[i];
                const miss = parseMiMissingPartRef(pk);
                if (miss && miss.rawToken) {
                    parts.push(String(miss.rawToken).trim());
                    continue;
                }
                const partEntry = resolvePartEntryForSerialOutput(compRoot, pk, baseGenericRoot, dataRef);
                if (!partEntry) continue;
                const t = formatDeserializedPartToken(compSer, pk, partEntry, dataRef);
                if (t) parts.push(t);
            }
            if (!parts.length) return '';
            try {
                return mergeConsecutiveRootPairTokensToArrayTokens(parts).join(' ');
            } catch (e) {
                return parts.join(' ');
            }
        }

        /** Remove _mi_cross_manufacturer lines whose serialized tokens match an earlier non-cross line. */
        function dedupeModdedCrossLinesRedundantWithNative(compRoot, lines, dataRef) {
            if (!lines || !lines.length || !compRoot || !dataRef) return lines || [];
            const crossNorm = normalizeDepTableName(MI_CROSS_TABLE);
            const dneNorm = normalizeDepTableName(MI_DOES_NOT_EXIST_TABLE);
            const nativeFp = new Set();
            lines.forEach((line) => {
                if (!line || !line.partKey) return;
                const tn = normalizeDepTableName(line.tableName || '');
                if (tn === crossNorm || tn === dneNorm) return;
                const fp = miModdedLineSerializationFingerprint(compRoot, line, dataRef);
                if (fp) nativeFp.add(fp);
            });
            const out = [];
            lines.forEach((line) => {
                if (!line || !line.partKey) return;
                const tn = normalizeDepTableName(line.tableName || '');
                if (tn === crossNorm) {
                    const fp = miModdedLineSerializationFingerprint(compRoot, line, dataRef);
                    if (fp && nativeFp.has(fp)) return;
                }
                out.push(line);
            });
            return out;
        }

        function filterPassivePointsBrowseDeps(items, defaultRootKey) {
            const seen = new Set();
            const out = [];
            for (let i = 0; i < (items || []).length; i++) {
                const it = items[i];
                if (!it || !it.key) continue;
                const rk = String(it.rootKey != null ? it.rootKey : defaultRootKey);
                const meta = parsePassivePointsTierKey(it.key);
                if (!meta) {
                    out.push(it);
                    continue;
                }
                if (meta.tier !== 1) continue;
                const ck = rk + '\0' + meta.chainBase.toLowerCase();
                if (seen.has(ck)) continue;
                seen.add(ck);
                out.push(it);
            }
            return out;
        }

        function filterPassivePointsCatalogItemsForBrowse(catalogItems) {
            const seen = new Set();
            const out = [];
            for (let i = 0; i < (catalogItems || []).length; i++) {
                const it = catalogItems[i];
                if (!it || !it.key) continue;
                const meta = parsePassivePointsTierKey(it.key);
                if (!meta) {
                    out.push(it);
                    continue;
                }
                if (meta.tier !== 1) continue;
                const ck = String(it.sourceRootKey || '') + '\0' + meta.chainBase.toLowerCase();
                if (seen.has(ck)) continue;
                seen.add(ck);
                out.push(it);
            }
            return out;
        }

        function invalidateMiGlobalPartCatalog() {
            window.__miGlobalPartCatalog = null;
            window.__miGlobalPartCatalogDataRef = null;
        }

        function miGetSupplementalPartsByTypeId(typeId) {
            try {
                if (typeof partsByTypeId !== 'undefined' && partsByTypeId && partsByTypeId.get) {
                    return partsByTypeId.get(typeId) || [];
                }
            } catch (e) {}
            return [];
        }

        function miResolveSupplementalPartInfo(partKey) {
            const key = String(partKey || '').trim();
            if (!key) return null;
            try {
                if (typeof partsMap !== 'undefined' && partsMap && partsMap.get) {
                    return partsMap.get(key) || null;
                }
            } catch (e) {}
            const m = key.match(/^(\d+):(\d+)$/);
            if (!m) return null;
            const typeId = parseInt(m[1], 10);
            const partId = parseInt(m[2], 10);
            if (!Number.isFinite(typeId) || !Number.isFinite(partId)) return null;
            return {
                key,
                id: String(partId),
                fullId: key,
                name: key,
                serialIndex: partId,
                rootSerialIndex: typeId
            };
        }

        function miSupplementalSlotKeyForPartInfo(partInfo) {
            if (!partInfo || Number(partInfo.typeId) !== 234) return '';
            const explicit = normalizeDepTableName(partInfo.slotKey || '');
            if (explicit && explicit !== 'unknown') return explicit;
            const aliasText = Array.isArray(partInfo.searchAliases)
                ? partInfo.searchAliases.join(' ')
                : '';
            const text = [
                partInfo.sourceName,
                partInfo.spawnCode,
                partInfo.name,
                partInfo.displayName,
                partInfo.string,
                partInfo.partType,
                partInfo.originalPartType,
                partInfo.stats,
                partInfo.description,
                aliasText
            ]
                .map(v => String(v || '').toLowerCase())
                .join(' ');
            if (text.indexOf('classmod.stat3_') !== -1 || text.indexOf('stat3_') !== -1) {
                return 'stat_group3';
            }
            if (text.indexOf('classmod.stat2_') !== -1 || text.indexOf('stat2_') !== -1) {
                return 'stat_group2';
            }
            if (text.indexOf('firmware') !== -1) return 'firmware';
            if (text.indexOf('statspecial_') !== -1 || text.indexOf('classmod.statspecial') !== -1) {
                return 'statspecial';
            }
            if (text.indexOf('classmod.stat_') !== -1 || text.indexOf('stat_') !== -1) {
                return 'stat_group1';
            }
            return '';
        }

        function miSupplementalPartLabel(partInfo) {
            if (!partInfo) return '';
            const name = String(partInfo.name || partInfo.displayName || partInfo.fullId || '').trim();
            const sourceName = String(partInfo.sourceName || partInfo.spawnCode || '').trim();
            const desc = String(partInfo.description || partInfo.stats || '').trim();
            const fullId = String(partInfo.fullId || '').trim();
            const pieces = [];
            if (desc && desc.indexOf('Source: ') !== 0) pieces.push(desc);
            if (sourceName && sourceName !== name) pieces.push(sourceName);
            if (fullId) pieces.push(fullId);
            return pieces.length ? pieces.join(' — ') : (name || fullId);
        }

        function miSupplementalAliasText(partInfo) {
            if (!partInfo) return '';
            const aliases = Array.isArray(partInfo.searchAliases) ? partInfo.searchAliases : [];
            return [
                partInfo.fullId,
                partInfo.sourceName,
                partInfo.spawnCode,
                partInfo.name,
                partInfo.displayName,
                partInfo.description,
                partInfo.stats,
                partInfo.slotKey,
                aliases.join(' ')
            ]
                .map(v => String(v || '').toLowerCase())
                .join(' ');
        }

        function miAppendClassModSupplementalPartsToGlobalCatalog(byType, typesSet) {
            const parts = miGetSupplementalPartsByTypeId(234);
            if (!parts || !parts.length) return;
            const seen = new Set();
            byType.forEach(items => {
                (items || []).forEach(it => {
                    if (it && it.key) seen.add(String(it.key));
                    if (it && it.dep && it.dep.fullId) seen.add(String(it.dep.fullId));
                });
            });
            parts.forEach(partInfo => {
                const fullId = String(partInfo.fullId || '').trim();
                if (!fullId || seen.has(fullId)) return;
                const slotKey = miSupplementalSlotKeyForPartInfo(partInfo);
                if (!slotKey || slotKey === 'statspecial') return;
                const m = fullId.match(/^(\d+):(\d+)$/);
                if (!m) return;
                const rootSerialIndex = parseInt(m[1], 10);
                const serialIndex = parseInt(m[2], 10);
                if (!Number.isFinite(rootSerialIndex) || !Number.isFinite(serialIndex)) return;
                const dep = {
                    key: fullId,
                    fullId: fullId,
                    depTableName: slotKey,
                    serialIndex: serialIndex,
                    rootSerialIndex: rootSerialIndex,
                    debugdisplaydescription: partInfo.description || partInfo.stats || partInfo.name || '',
                    __msbtSupplementalPart: true
                };
                const item = {
                    sourceRootKey: '__msbt_type_234',
                    key: fullId,
                    depTableName: slotKey,
                    dep: dep,
                    label: miSupplementalPartLabel(partInfo),
                    aliasText: miSupplementalAliasText(partInfo)
                };
                if (!byType.has(slotKey)) byType.set(slotKey, []);
                byType.get(slotKey).push(item);
                typesSet.add(slotKey);
                seen.add(fullId);
            });
        }

        function getMiGlobalPartCatalog(dataRef) {
            if (!dataRef || !dataRef.rootDefinitions) return { byType: new Map(), types: [] };
            if (window.__miGlobalPartCatalog && window.__miGlobalPartCatalogDataRef === dataRef) {
                return window.__miGlobalPartCatalog;
            }
            const byType = new Map();
            const typesSet = new Set();
            const passiveChainSeenCatalog = new Set();
            dataRef.rootDefinitions.forEach(r => {
                const rk = r.key;
                if (!rk || !r.depEntries) return;
                r.depEntries.forEach(dep => {
                    if (!dep || !dep.key) return;
                    const rawTn = dep.depTableName;
                    if (!rawTn || rawTn === 'inv_comp') return;
                    const tn = normalizeDepTableName(rawTn);
                    if (!tn || tn === 'unknown') return;
                    if (tn === 'passive_points') {
                        const tm = parsePassivePointsTierKey(dep.key);
                        if (tm) {
                            if (tm.tier !== 1) return;
                            const ck = rk + '\0' + tm.chainBase.toLowerCase();
                            if (passiveChainSeenCatalog.has(ck)) return;
                            passiveChainSeenCatalog.add(ck);
                        }
                    }
                    let label = getDisplayNameForPart(dep, dataRef.invNamePartDisplayNames) || dep.key;
                    let passiveIconUrl = null;
                    let passiveDesc = null;
                    if (tn === 'passive_points') {
                        const p = resolvePassivePointsUiExtras(dep, dataRef, tn);
                        if (p && p.displayName) label = p.displayName;
                        if (p && p.iconUrl) passiveIconUrl = p.iconUrl;
                        if (p && p.description) passiveDesc = p.description;
                    }
                    const item = {
                        sourceRootKey: rk,
                        key: dep.key,
                        depTableName: rawTn,
                        dep: dep,
                        label,
                        passiveIconUrl,
                        passiveDesc
                    };
                    if (!byType.has(tn)) byType.set(tn, []);
                    byType.get(tn).push(item);
                    typesSet.add(tn);
                });
            });
            miAppendClassModSupplementalPartsToGlobalCatalog(byType, typesSet);
            const types = Array.from(typesSet).sort();
            const catalog = { byType, types };
            window.__miGlobalPartCatalog = catalog;
            window.__miGlobalPartCatalogDataRef = dataRef;
            return catalog;
        }

        /** Total browsable rows across all dep-table types in the cross-manufacturer global catalog (excludes inv_comp). */
        function miGlobalCatalogBrowsingPartCount() {
            if (!data || !data.rootDefinitions) return 0;
            const catalog = getMiGlobalPartCatalog(data);
            if (!catalog || !catalog.types || !catalog.types.length) return 0;
            let n = 0;
            for (let i = 0; i < catalog.types.length; i++) {
                const items = miFilterCrossCatalogItemsForViewMode(
                    catalog.byType.get(catalog.types[i]) || [],
                    data
                );
                n += items.length;
            }
            return n;
        }

        /** All inv_comp rows across every root (catalog excludes inv_comp). Deduped by root + comp key. */
        function getMiGlobalInvCompCatalogItems(dataRef) {
            if (!dataRef || !dataRef.rootDefinitions) return [];
            const seen = new Set();
            const out = [];
            dataRef.rootDefinitions.forEach(r => {
                const rk = r.key;
                if (!rk || !r.depEntries) return;
                r.depEntries.forEach(dep => {
                    if (!dep || !dep.key) return;
                    if (normalizeDepTableName(dep.depTableName) !== 'inv_comp') return;
                    const dedup = rk + '\0' + dep.key;
                    if (seen.has(dedup)) return;
                    seen.add(dedup);
                    const disp = getModdedInvCompBrowseDisplayName(dep, dataRef.invNamePartDisplayNames, dataRef, r);
                    const row = {
                        r: rk,
                        k: dep.key,
                        l: disp + ' — ' + rk + ' · ' + dep.key
                    };
                    if (miInvCompDepIsPearlescent(dep)) row.pearl = true;
                    row.s = miInvCompDepRaritySearchText(dep);
                    const dbgIc = getPartDebugDisplayDescription(dep);
                    if (dbgIc) {
                        row.d = dbgIc;
                    }
                    out.push(row);
                });
            });
            return out;
        }

        function getMiCrossLineDisplayLabel(sourceRootKey, partKey, slotTableNameOpt) {
            if (!data || !data.rootDefinitions) return partKey;
            const pref = slotTableNameOpt ? normalizeDepTableName(slotTableNameOpt) : null;
            const dep = findDepEntryInRootByKeyAndTable(data, sourceRootKey, partKey, pref);
            if (!dep) return partKey;
            if (normalizeDepTableName(dep.depTableName) === 'passive_points') {
                const p = resolvePassivePointsUiExtras(dep, data, 'passive_points');
                if (p && p.displayName) return p.displayName;
            }
            if (normalizeDepTableName(dep.depTableName) === 'inv_comp') {
                const rootObj = data.rootDefinitions.find(x => x.key === sourceRootKey);
                return getModdedInvCompBrowseDisplayName(dep, data.invNamePartDisplayNames, data, rootObj);
            }
            return getDisplayNameForPart(dep, data.invNamePartDisplayNames) || dep.key;
        }

        function buildMiCrossManufacturerSectionHTML(rootKey, miSavedLines) {
            if (!rootKey) return '';
            const crossSaved = (miSavedLines || []).filter(
                l => l && l.tableName === MI_CROSS_TABLE && l.partKey
            );
            const crossPickedUnique = new Set(crossSaved.map(l => String(l.partKey || '')).filter(Boolean)).size;
            const crossBrowseCount = miGlobalCatalogBrowsingPartCount();
            function escAttr(s) {
                if (s == null) return '';
                return String(s).replace(/&/g, '&amp;').replace(/"/g, '&quot;');
            }
            const rowsHtml = crossSaved
                .map(l => {
                    if (!l.partKey) return '';
                    const q = l.qty != null ? l.qty : 1;
                    return miSlotBuildPickedLineHtml(l.partKey, q, rootKey);
                })
                .join('');
            const crossInner =
                `<div class="mi-compact-slot mi-cross-root-slot" data-root-key="${escAttr(rootKey)}" data-table-name="${escAttr(MI_CROSS_TABLE)}" data-mi-max-rows="${MI_MODDED_MAX_PICKED_LINES}">` +
                `<div class="mi-cross-picked-host">` +
                `<div class="mi-cross-picked-label">Your cross-manufacturer selections</div>` +
                `<div class="mi-compact-slot-rows mi-cross-slot-rows">${rowsHtml}</div>` +
                `</div>` +
                `<div class="mi-cross-picker-host">` +
                `<div class="mi-cross-picker-panel">` +
                `<div class="mi-cross-picker-toolbar">` +
                `<select class="mi-cross-type-select" aria-label="Filter by part type">` +
                `<option value="">Loading catalog…</option></select>` +
                `<input type="search" class="mi-cross-search mi-slot-search" placeholder="Filter by name, key, item root, or token…" aria-label="Search cross-manufacturer catalog" />` +
                `</div>` +
                `<label class="mi-slot-select-all-wrap">` +
                `<input type="checkbox" class="mi-slot-select-all" title="Selects only rows currently shown (after filter and list cap)." />` +
                `<span class="mi-slot-select-all-label">Select all visible</span></label>` +
                `<div class="mi-cross-picker-scroll">` +
                `<div class="mi-slot-candidate-rich mi-passive-candidate-list mi-cross-candidate-rich" tabindex="0" role="listbox" aria-multiselectable="true" aria-label="Cross-manufacturer parts catalog"></div>` +
                `<div class="mi-passive-cand-foot mi-cross-cand-foot"></div>` +
                `</div>` +
                `<div class="mi-cross-picker-footer">` +
                `<div class="mi-slot-picker-actions">` +
                `<button type="button" class="mi-cross-add-btn mi-slot-add-selected">Add selected</button>` +
                `<label class="mi-cross-qty-label"><span>Qty</span><input type="number" class="mi-slot-bulk-qty" min="1" max="99" value="1" title="Quantity per selected part" /></label>` +
                `<button type="button" class="mi-slot-clear-category">Clear all cross picks</button>` +
                `</div></div>` +
                `</div></div></div>`;
            return (
                `<section class="mi-cross-section" data-mi-dep-filter="${escAttr(MI_CROSS_TABLE)}" data-mi-part-count="${crossBrowseCount}" data-mi-picked-unique="${crossPickedUnique}" aria-labelledby="mi-cross-section-title-${escAttr(rootKey)}">` +
                `<header class="mi-cross-section-header">` +
                `<h3 class="mi-cross-section-title" id="mi-cross-section-title-${escAttr(rootKey)}">Cross-manufacturer parts</h3>` +
                `<p class="mi-cross-section-lead">Didn't find the part you are looking for?</p>` +
                `<p class="mi-cross-section-desc">You can find EVERY part from EVERY Item in this list</p>` +
                `<p class="mi-cross-section-meta">${crossPickedUnique} added · ${crossBrowseCount.toLocaleString()} parts in catalog</p>` +
                `</header>` +
                crossInner +
                `</section>`
            );
        }

        function miSplitSearchTerms(query) {
            return String(query || '')
                .toLowerCase()
                .trim()
                .split(/\s+/)
                .filter(Boolean);
        }

        function stripUiStatTextForSearch(rawText) {
            if (rawText == null) return '';
            return String(rawText)
                .replace(/\[newline\]/gi, ' ')
                .replace(/\[\/[^\]]+\]/gi, ' ')
                .replace(/\[[^\]]+\]/g, ' ')
                .replace(/\s+/g, ' ')
                .trim();
        }

        function appendUiStatInfoToSearchParts(parts, uiStatInfo) {
            if (!uiStatInfo) return;
            ['descriptionTexts', 'primaryTexts', 'redtexts'].forEach(function (key) {
                const list = uiStatInfo[key];
                if (!Array.isArray(list)) return;
                list.forEach(function (text) {
                    const plain = stripUiStatTextForSearch(text);
                    if (plain) parts.push(plain);
                });
            });
        }

        function appendResolvedStatsToSearchParts(parts, resolvedList) {
            if (!Array.isArray(resolvedList)) return;
            resolvedList.forEach(function (r) {
                if (!r) return;
                if (r.label) parts.push(String(r.label).replace(/_/g, ' '));
                if (r.context) parts.push(String(r.context).replace(/_/g, ' '));
                if (r.displayValue != null && String(r.displayValue).trim()) {
                    parts.push(String(r.displayValue));
                }
            });
        }

        function buildDepItemSearchText(item, partDisplayName, extras) {
            extras = extras || {};
            const parts = [];
            if (item && item.key) parts.push(String(item.key));
            if (partDisplayName) parts.push(String(partDisplayName));
            if (extras.skillInfo) {
                const si = extras.skillInfo;
                if (si.name) parts.push(String(si.name));
                if (si.skill_name) parts.push(String(si.skill_name));
                if (si.description) parts.push(stripUiStatTextForSearch(si.description));
                if (si.tree_name) parts.push(String(si.tree_name));
            }
            if (extras.serialFormat) {
                parts.push(String(extras.serialFormat).replace(/[{}]/g, ''));
            }
            if (item && typeof getPartDebugDisplayDescription === 'function') {
                const dbg = getPartDebugDisplayDescription(item);
                if (dbg) parts.push(String(dbg));
            }
            appendUiStatInfoToSearchParts(parts, extras.uiStatInfo);
            appendResolvedStatsToSearchParts(parts, extras.otherResolved);
            const tagLists = [
                extras.addTags,
                extras.depTags,
                extras.exclTags,
                extras.baseTags
            ];
            tagLists.forEach(function (list) {
                if (Array.isArray(list) && list.length) {
                    parts.push(list.join(' '));
                }
            });
            return parts
                .map(function (p) {
                    return String(p || '').toLowerCase();
                })
                .filter(Boolean)
                .join(' ');
        }

        function buildDepTableSearchRowHtml(tableName, itemCount) {
            if (!itemCount || itemCount <= 10) return '';
            const tn = String(tableName || '').trim();
            const label = escapeLegitHtml('Filter ' + tn + ' parts');
            return (
                '<div class="dep-table-search-wrap">' +
                '<input type="search" class="dep-table-search" placeholder="Filter parts…" aria-label="' +
                label +
                '" autocomplete="off" />' +
                '<span class="dep-table-search-meta" aria-live="polite"></span>' +
                '</div>'
            );
        }

        function applyDepTableSearchFilter(depTableEl) {
            if (!depTableEl) return;
            const searchInp = depTableEl.querySelector('.dep-table-search');
            const meta = depTableEl.querySelector('.dep-table-search-meta');
            const q = searchInp && searchInp.value ? String(searchInp.value).trim() : '';
            const terms = miSplitSearchTerms(q);
            const items = depTableEl.querySelectorAll('.dep-items .dep-item');
            let visible = 0;
            const total = items.length;
            items.forEach(function (el) {
                const hay = (el.getAttribute('data-dep-search') || el.textContent || '').toLowerCase();
                const isSelected = el.classList.contains('selected');
                const match = terms.length === 0 || terms.every(function (t) {
                    return hay.indexOf(t) >= 0;
                });
                const show = match || (isSelected && q.length > 0);
                el.style.display = show ? '' : 'none';
                if (show) visible++;
            });
            if (meta) {
                if (q.length > 0) {
                    meta.textContent = 'Showing ' + visible + ' of ' + total;
                } else {
                    meta.textContent = '';
                }
            }
        }

        function captureDepTableSearchState(rootItemEl) {
            const state = {};
            if (!rootItemEl) return state;
            rootItemEl.querySelectorAll('.dep-table[data-dep-table]').forEach(function (depTable) {
                const tn = depTable.getAttribute('data-dep-table');
                const inp = depTable.querySelector('.dep-table-search');
                if (tn && inp && String(inp.value || '').trim()) {
                    state[tn] = String(inp.value).trim();
                }
            });
            return state;
        }

        function restoreDepTableSearchState(rootItemEl, state) {
            if (!rootItemEl || !state || typeof state !== 'object') return;
            Object.keys(state).forEach(function (tableName) {
                const query = state[tableName];
                if (!query) return;
                const depTable = Array.from(
                    rootItemEl.querySelectorAll('.dep-table[data-dep-table]')
                ).find(function (el) {
                    return el.getAttribute('data-dep-table') === tableName;
                });
                if (!depTable) return;
                const inp = depTable.querySelector('.dep-table-search');
                if (inp) {
                    inp.value = query;
                    applyDepTableSearchFilter(depTable);
                }
            });
        }

        function miGetManufacturerAliasSearchText() {
            const raw = Array.prototype.slice
                .call(arguments)
                .map(v => String(v || '').toLowerCase())
                .filter(Boolean)
                .join(' ');
            if (!raw) return '';
            const pairs = [
                ['bor', 'ripper'],
                ['borg', 'ripper'],
                ['dad', 'daedalus'],
                ['mal', 'maliwan'],
                ['malefactor', 'maliwan'],
                ['cov', 'cov'],
                ['hyp', 'hyperion'],
                ['ted', 'tediore'],
                ['tor', 'torgue'],
                ['vla', 'vladof'],
                ['jak', 'jakobs'],
                ['atla', 'atlas']
            ];
            const out = new Set();
            pairs.forEach(pair => {
                const shortName = pair[0];
                const fullName = pair[1];
                if (raw.indexOf(shortName) >= 0 || raw.indexOf(fullName) >= 0) {
                    out.add(shortName);
                    out.add(fullName);
                }
            });
            return Array.from(out).join(' ');
        }

        function miMatchesSearchTerms(terms, fields) {
            if (!terms || !terms.length) return true;
            const haystack = (fields || [])
                .filter(v => v != null && v !== '')
                .join(' ')
                .toLowerCase();
            return terms.every(term => haystack.indexOf(term) >= 0);
        }

        function miCrossPickerHostEl(root) {
            return (
                root &&
                (root.querySelector('.mi-cross-picker-host') ||
                    root.querySelector('.mi-cross-picker-details') ||
                    root.closest('.mi-cross-picker-host') ||
                    root.closest('.mi-cross-picker-details'))
            );
        }

        function miInitCrossManufacturerPickers(rootEl) {
            if (!rootEl || !rootEl.querySelectorAll) return;
            if (!data || !data.rootDefinitions || !data.rootDefinitions.length) {
                rootEl.querySelectorAll('.mi-cross-picker-host').forEach(function (host) {
                    const foot = host.querySelector('.mi-cross-cand-foot');
                    if (foot) {
                        foot.textContent = 'Load Nexus JSON item data to browse the global part catalog.';
                    }
                    const rich = host.querySelector('.mi-cross-candidate-rich');
                    if (rich) rich.innerHTML = '';
                    const sel = host.querySelector('.mi-cross-type-select');
                    if (sel) {
                        sel.innerHTML = '<option value="">Load item data first…</option>';
                    }
                });
                return;
            }
            rootEl.querySelectorAll('.mi-cross-picker-host').forEach(function (host) {
                const richList =
                    host.querySelector('.mi-cross-candidate-rich') ||
                    host.querySelector('.mi-passive-candidate-list.mi-cross-candidate-rich');
                const rowCount = richList ? richList.querySelectorAll('.mi-cand-row').length : 0;
                if (host.dataset.miCatalogInit === '1' && rowCount > 0) return;
                host.dataset.miCatalogInit = '1';
                populateMiCrossTypeSelect(host);
            });
        }

        function populateMiCrossTypeSelect(detailsEl) {
            const hostEl = miCrossPickerHostEl(detailsEl) || detailsEl.closest('.mi-cross-root-slot') || detailsEl;
            const catalog = getMiGlobalPartCatalog(data);
            const sel = hostEl.querySelector('.mi-cross-type-select');
            if (!sel) return;
            const types = miFilteredGlobalCatalogTypeList(catalog, data);
            if (!types.length) {
                sel.innerHTML = '<option value="">No parts in catalog — load Nexus JSON files</option>';
                miCrossRefreshCandidateList(hostEl);
                return;
            }
            sel.innerHTML =
                `<option value="">All part types (${types.length})</option>` +
                types
                    .map(t => {
                        const n = miFilterCrossCatalogItemsForViewMode(catalog.byType.get(t) || [], data).length;
                        const te = String(t).replace(/"/g, '&quot;');
                        return `<option value="${te}">${escapeLegitHtml(String(t))} (${n})</option>`;
                    })
                    .join('');
            miCrossRefreshCandidateList(hostEl);
        }

        function miCrossRefreshCandidateList(detailsEl) {
            const hostEl = miCrossPickerHostEl(detailsEl) || detailsEl;
            const catalog = getMiGlobalPartCatalog(data);
            const typeSel = hostEl.querySelector('.mi-cross-type-select');
            const searchInp = hostEl.querySelector('.mi-cross-search');
            const crossPartSel = hostEl.querySelector('select.mi-cross-part-select');
            const richList =
                hostEl.querySelector('.mi-cross-candidate-rich.mi-passive-candidate-list') ||
                hostEl.querySelector('.mi-cross-candidate-rich') ||
                hostEl.querySelector('.mi-passive-candidate-list.mi-cross-candidate-rich');
            if (!richList && !crossPartSel) return;
            const tn = typeSel && typeSel.value ? typeSel.value : '';
            const q = (searchInp && searchInp.value ? searchInp.value : '').trim().toLowerCase();
            const terms = miSplitSearchTerms(q);
            const crossSlot = hostEl.closest('.mi-cross-root-slot') || hostEl.closest('.mi-compact-slot');
            const rootKeyForComp = crossSlot && crossSlot.getAttribute('data-root-key');
            const compRoot =
                rootKeyForComp && data && data.rootDefinitions
                    ? data.rootDefinitions.find(r => r.key === rootKeyForComp)
                    : null;
            const compSer =
                compRoot != null && compRoot.serialIndex != null && compRoot.serialIndex !== undefined
                    ? compRoot.serialIndex
                    : null;
            const baseGenericRoot = compRoot && data ? getBaseGenericRootForPartLookup(compRoot, data) : null;
            let items = [];
            if (tn && catalog.byType.has(tn)) {
                items = miFilterCrossCatalogItemsForViewMode(catalog.byType.get(tn).slice(), data);
            } else {
                miFilteredGlobalCatalogTypeList(catalog, data).forEach(function (t) {
                    items = items.concat(miFilterCrossCatalogItemsForViewMode(catalog.byType.get(t) || [], data));
                });
            }
            if (terms.length) {
                items = items.filter(it => {
                    const val = makeMiCrossPartRef(it.sourceRootKey, it.key);
                    const pe =
                        data && compRoot && val
                            ? resolvePartEntryForSerialOutput(compRoot, val, baseGenericRoot, data)
                            : null;
                    const dbg = String((it.dep && getPartDebugDisplayDescription(it.dep)) || '');
                    const tok = String(
                        data && compRoot && val
                            ? formatDeserializedPartToken(compSer, val, pe, data)
                            : ''
                    );
                    const typeText = normalizeDepTableName(it.depTableName || '');
                    const aliasText = miGetManufacturerAliasSearchText(
                        it.label,
                        it.key,
                        it.sourceRootKey,
                        it.depTableName,
                        dbg
                    );
                    return miMatchesSearchTerms(terms, [
                        it.label,
                        it.key,
                        it.sourceRootKey,
                        it.depTableName,
                        typeText,
                        dbg,
                        tok,
                        aliasText
                    ]);
                });
            }
            items.sort((a, b) => String(a.label || a.key).localeCompare(String(b.label || b.key)));
            if (tn === 'passive_points') {
                items = filterPassivePointsCatalogItemsForBrowse(items);
            }
            const maxN = tn === 'passive_points' ? 2000 : 500;
            const slice = items.slice(0, maxN);

            function escAttrCross(s) {
                if (s == null) return '';
                return String(s).replace(/&/g, '&amp;').replace(/"/g, '&quot;');
            }
            function miCrossBrowseCandRowHtml(it) {
                const val = makeMiCrossPartRef(it.sourceRootKey, it.key);
                const ve = String(val).replace(/&/g, '&amp;').replace(/"/g, '&quot;');
                let tok = '';
                if (data && compRoot) {
                    const pe = resolvePartEntryForSerialOutput(compRoot, val, baseGenericRoot, data);
                    tok = formatDeserializedPartToken(compSer, val, pe, data) || '';
                }
                const primaryLabel = String(it.label != null ? it.label : it.key || '');
                const invTn = normalizeDepTableName(it.depTableName || '');
                let rarityClass = '';
                let line1PearlClassCross = '';
                if (invTn === 'inv_comp') {
                    const pearlDual =
                        !!(it.dep && miInvCompDepIsPearlescent(it.dep)) ||
                        miInvCompPearlescentFromKeyLabel(it.key, primaryLabel);
                    if (pearlDual) line1PearlClassCross = ' mi-cand-line1--pearlescent-inv-comp';
                    else {
                        const rc = miInvCompRarityClass(it.key, primaryLabel);
                        if (rc) rarityClass = ' ' + rc;
                    }
                }
                const tokSpan = tok ? `<span class="mi-cand-token">${escapeLegitHtml(tok)}</span> ` : '';
                const labelInner = formatUiStatTextToHtml(primaryLabel);
                const metaEsc = escapeLegitHtml(`${it.sourceRootKey} · ${it.key}`);
                const crossDbg = it.dep ? getPartDebugDisplayDescription(it.dep) : '';
                const dbgHtml = crossDbg
                    ? `<span class="mi-cand-sep"> · </span><span class="mi-cand-debug">${formatUiStatTextToHtml(crossDbg)}</span>`
                    : '';
                const line1 =
                    `<div class="mi-cand-line1${line1PearlClassCross}" style="display:flex;flex-wrap:wrap;align-items:baseline;gap:4px 6px;">` +
                    tokSpan +
                    `<span class="mi-cand-name${rarityClass}"><span class="mi-cand-formatted-label">${labelInner}</span></span>` +
                    `<span class="mi-cand-sep"> — </span><span class="mi-cand-cross-meta">${metaEsc}</span>` +
                    dbgHtml +
                    `</div>`;
                const rowStyle =
                    'display:flex;align-items:flex-start;gap:8px;padding:6px 8px;cursor:pointer;border-bottom:1px solid #2a2a2a;font-size:11px;color:#e8e8e8;box-sizing:border-box;';
                if (invTn === 'passive_points' && (it.passiveIconUrl || it.passiveDesc)) {
                    const iconUrl = it.passiveIconUrl ? String(it.passiveIconUrl) : '';
                    const imgHtml = iconUrl
                        ? `<img src="${escAttrCross(iconUrl)}" alt="" width="24" height="24" loading="lazy" style="object-fit:contain;flex-shrink:0;border-radius:3px;border:1px solid #555;background:#151515;align-self:flex-start;margin-top:1px;" onerror="this.style.opacity='0.2';" />`
                        : `<span style="width:24px;height:24px;flex-shrink:0;border:1px dashed #444;border-radius:3px;background:#151515;display:block;box-sizing:border-box;align-self:flex-start;margin-top:1px;"></span>`;
                    const descRaw =
                        it.passiveDesc != null && String(it.passiveDesc).trim() ? String(it.passiveDesc).trim() : '';
                    const descBlock = descRaw
                        ? `<div class="mi-passive-cand-formattext mi-cand-passive-blurb" style="font-size:10px;line-height:1.35;margin-top:3px;white-space:normal;word-break:break-word;">${formatUiStatTextToHtml(descRaw)}</div>`
                        : '';
                    return (
                        `<div class="mi-cand-row" role="option" aria-selected="false" data-ref="${ve}" style="${rowStyle}">` +
                        imgHtml +
                        `<div style="flex:1;min-width:0;display:flex;flex-direction:column;align-items:stretch;gap:0;">` +
                        line1 +
                        descBlock +
                        `</div>` +
                        `</div>`
                    );
                }
                return (
                    `<div class="mi-cand-row" role="option" aria-selected="false" data-ref="${ve}" style="${rowStyle}">` +
                    `<div style="flex:1;min-width:0;display:flex;flex-direction:column;align-items:stretch;gap:0;">` +
                    line1 +
                    `</div>` +
                    `</div>`
                );
            }

            if (crossPartSel) {
                const payload = slice.map(function (it) {
                    const val = makeMiCrossPartRef(it.sourceRootKey, it.key);
                    let lab = String(it.label != null ? it.label : it.key || '');
                    lab = lab + ' — ' + it.sourceRootKey;
                    return { r: it.sourceRootKey, k: it.key, l: lab };
                });
                const prevCrossSelected = crossPartSel.multiple
                    ? Array.from(crossPartSel.selectedOptions || [])
                          .map(function (o) {
                              return o.value;
                          })
                          .filter(Boolean)
                    : crossPartSel.value
                      ? [crossPartSel.value]
                      : [];
                crossPartSel.innerHTML = buildMiPartSlotSelectOptionsHtml(payload, maxN);
                if (prevCrossSelected.length) {
                    Array.from(crossPartSel.options).forEach(function (o) {
                        if (prevCrossSelected.indexOf(o.value) !== -1) {
                            o.selected = true;
                        }
                    });
                }
                const crossSlot = crossPartSel.closest('.mi-compact-slot');
                if (crossSlot) miPartSlotMultidropRefreshList(crossSlot);
            }
            if (richList) {
                richList.innerHTML = slice.map(miCrossBrowseCandRowHtml).join('');
                richList._miPassiveAnchorIdx = null;
            }
            const crossFoot = hostEl.querySelector('.mi-cross-cand-foot');
            if (crossFoot) {
                let msg = `Showing ${slice.length} of ${items.length} after filter`;
                if (slice.length < items.length) {
                    msg += ` (${items.length - slice.length} past list cap — narrow part type or search)`;
                }
                msg += '.';
                crossFoot.textContent = msg;
            }
            if (richList && items.length > maxN) {
                const more = document.createElement('div');
                more.className = 'mi-passive-cand-more';
                more.style.cssText = 'padding:6px 8px;font-size:10px;color:#888;border-top:1px solid #333;';
                more.textContent = `… ${items.length - maxN} more — narrow part type or search`;
                richList.appendChild(more);
            }
            if (crossSlot) {
                const sal = crossSlot.querySelector('.mi-slot-select-all-label');
                if (sal) {
                    sal.textContent = `Select all visible (${slice.length})`;
                }
                const sa = crossSlot.querySelector('.mi-slot-select-all');
                if (sa) {
                    sa.checked = false;
                    sa.indeterminate = false;
                }
            }
        }

        function makeMiSlotItemRef(item, fallbackRootKey) {
            const rk = item && item.rootKey != null ? String(item.rootKey) : String(fallbackRootKey || '');
            const k = item && item.key;
            if (!k) return '';
            if (rk) return makeMiCrossPartRef(rk, k);
            return String(k);
        }

        function miSlotBuildPickedLineHtml(encodedRef, qty, rootKeyForRaw, slotTableNameOpt) {
            function escAttr(s) {
                if (s == null) return '';
                return String(s).replace(/&/g, '&amp;').replace(/"/g, '&quot;');
            }
            const missing = parseMiMissingPartRef(encodedRef);
            if (missing && missing.rawToken) {
                const q = Math.min(99, Math.max(1, parseInt(qty, 10) || 1));
                const rt = String(missing.rawToken);
                const tokenTitle = 'Not in dataset — ' + rt;
                const tokenSpan = `<span style="font-family:ui-monospace,Consolas,monospace;color:#ffb74d;font-weight:600;">${escapeLegitHtml(rt)}</span>`;
                const labelRowHtml =
                    `<div class="mi-slot-picked-line-labelrow" style="display:flex;align-items:flex-start;gap:8px;width:100%;min-width:0;box-sizing:border-box;margin-bottom:6px;">` +
                    `<div style="flex:1;min-width:0;">` +
                    `<span class="mi-missing-line-label" style="display:block;font-size:11px;line-height:1.35;color:#ffcc80;white-space:normal;overflow-wrap:break-word;word-break:normal;" title="${escAttr(tokenTitle)}">${escapeLegitHtml('Not in dataset: ')}${tokenSpan}</span>` +
                    `</div></div>`;
                return (
                    `<div class="mi-slot-picked-line mi-gl-pick mi-missing-part-line" style="display:block;margin-bottom:4px;border-left:3px solid #ff8f00;padding-left:6px;min-width:0;width:100%;box-sizing:border-box;" data-line-ref="${escAttr(encodedRef)}" data-source-root="" data-part-key="">` +
                    labelRowHtml +
                    `<div class="mi-slot-picked-line-top" style="display:flex;align-items:center;flex-wrap:nowrap;gap:6px;width:100%;min-width:0;">` +
                    `<span class="mi-picked-line-drag-handle" draggable="true" title="Drag to reorder (same item)" aria-label="Drag to reorder" style="cursor:grab;color:#ff8f00;font-size:16px;line-height:1;user-select:none;padding:2px 6px 2px 2px;flex-shrink:0;border-radius:3px;border:1px solid #5d4037;background:#2d2218;">⋮⋮</span>` +
                    `<button type="button" class="mi-picked-line-move-up" title="Move up" aria-label="Move this row up" style="width:24px;min-width:24px;min-height:24px;padding:0;font-size:13px;line-height:1;border-radius:4px;border:1px solid rgba(130,195,240,0.45);background:#182333;color:#d7ecff;cursor:pointer;flex-shrink:0;">↑</button>` +
                    `<button type="button" class="mi-picked-line-move-down" title="Move down" aria-label="Move this row down" style="width:24px;min-width:24px;min-height:24px;padding:0;font-size:13px;line-height:1;border-radius:4px;border:1px solid rgba(130,195,240,0.45);background:#182333;color:#d7ecff;cursor:pointer;flex-shrink:0;">↓</button>` +
                    `<div style="flex:1;min-width:0;min-height:0" aria-hidden="true"></div>` +
                    `<input type="number" class="mi-compact-line-qty mi-picked-line-qty" min="1" max="99" value="${q}" title="Quantity" aria-label="Quantity" />` +
                    `<button type="button" class="mi-picked-line-remove" aria-label="Remove row">×</button>` +
                    `</div></div>`
                );
            }
            const parsed = parseMiCrossPartRef(encodedRef);
            let sr = parsed ? parsed.sourceRootKey : '';
            let pk = parsed ? parsed.partKey : encodedRef;
            let refAttr = encodedRef;
            if (!parsed && rootKeyForRaw) {
                sr = rootKeyForRaw;
                refAttr = makeMiCrossPartRef(rootKeyForRaw, pk) || encodedRef;
            }
            const lab = sr && pk ? getMiCrossLineDisplayLabel(sr, pk, slotTableNameOpt) : pk;
            let passiveIconHtml = '';
            let passiveFormatHtml = '';
            let miPickedUiStatHtml = '';
            if (data && sr && pk) {
                const pref = slotTableNameOpt ? normalizeDepTableName(slotTableNameOpt) : null;
                const depPick = findDepEntryInRootByKeyAndTable(data, sr, pk, pref);
                if (depPick && normalizeDepTableName(depPick.depTableName) === 'passive_points') {
                    const pEx = resolvePassivePointsUiExtras(depPick, data, 'passive_points');
                    if (pEx && pEx.iconUrl) {
                        passiveIconHtml = `<img class="mi-picked-passive-icon" src="${escAttr(pEx.iconUrl)}" alt="" width="28" height="28" loading="lazy" style="object-fit:contain;flex-shrink:0;border-radius:4px;border:1px solid #444;background:#1a1a1a;align-self:flex-start;" onerror="this.style.opacity='0.25';" />`;
                    }
                    if (pEx && pEx.description) {
                        passiveFormatHtml = `<div class="mi-picked-passive-formattext" style="font-size:10px;line-height:1.4;margin-top:3px;color:#b8b8b8;white-space:normal;overflow-wrap:break-word;word-break:normal;">${formatUiStatTextToHtml(pEx.description)}</div>`;
                    }
                }
                if (depPick) {
                    const partAspects = depPick.aspects || depPick.item?.aspects;
                    const uiStatInfo = getUiStatDescriptionAndRedtext(partAspects, data);
                    const uisBlock = buildUiStatDescriptionRedtextHTML(uiStatInfo);
                    if (uisBlock) {
                        miPickedUiStatHtml =
                            `<div class="mi-picked-line-uistat" style="font-size:10px;line-height:1.35;margin-top:4px;white-space:normal;overflow-wrap:break-word;word-break:normal;max-width:100%;color:var(--text-secondary);">${uisBlock}</div>`;
                    }
                }
            }
            const q = Math.min(99, Math.max(1, parseInt(qty, 10) || 1));
            let tokenStr = '';
            let pickedDebugDesc = '';
            if (data && data.rootDefinitions && rootKeyForRaw) {
                const compRoot = data.rootDefinitions.find(r => r.key === rootKeyForRaw);
                const compSer =
                    compRoot != null && compRoot.serialIndex != null && compRoot.serialIndex !== undefined
                        ? compRoot.serialIndex
                        : null;
                const baseGenericRoot = compRoot ? getBaseGenericRootForPartLookup(compRoot, data) : null;
                const partEntry = resolvePartEntryForSerialOutput(compRoot, refAttr, baseGenericRoot, data);
                pickedDebugDesc = getPartDebugDisplayDescription(partEntry);
                tokenStr = formatDeserializedPartToken(compSer, refAttr, partEntry, data);
            }
            const rootTip = (sr ? sr + ' · ' : '') + pk;
            const tokenTitle = tokenStr ? rootTip + ' → ' + tokenStr : rootTip;
            const pkDisp = String(pk || '');
            const labStr = lab != null ? String(lab) : '';
            const showLabPrefix = labStr && labStr !== pkDisp;
            const slotNormForPearl = slotTableNameOpt ? normalizeDepTableName(slotTableNameOpt) : '';
            let invCompPearlPicked = false;
            if (data && sr && pk && slotNormForPearl === 'inv_comp') {
                const depIc = findDepEntryInRootByKeyAndTable(data, sr, pk, 'inv_comp');
                invCompPearlPicked =
                    !!(depIc && miInvCompDepIsPearlescent(depIc)) ||
                    miInvCompPearlescentFromKeyLabel(pk, labStr);
            }
            const pearlPickColor = '#00e5ff';
            const labColorPick = invCompPearlPicked ? pearlPickColor : '#e8eaf0';
            const pkColorPick = invCompPearlPicked ? pearlPickColor : '#cfd8dc';
            const tokenColorPick = invCompPearlPicked ? pearlPickColor : '#aed581';
            let pickLabelInnerHtml =
                (showLabPrefix
                    ? `<span style="font-weight:600;color:${labColorPick};">${escapeLegitHtml(labStr)}</span><span style="color:#888;margin:0 5px;">—</span>`
                    : '') +
                `<span style="color:${pkColorPick};">${escapeLegitHtml(pkDisp)}</span>`;
            if (tokenStr) {
                pickLabelInnerHtml += `<span style="font-family:ui-monospace,Consolas,monospace;color:${tokenColorPick};font-weight:600;margin-left:8px;">${escapeLegitHtml(tokenStr)}</span>`;
            }
            const labelRowHtml =
                `<div class="mi-slot-picked-line-labelrow" style="display:flex;align-items:flex-start;gap:8px;width:100%;min-width:0;box-sizing:border-box;margin-bottom:6px;">` +
                passiveIconHtml +
                `<div style="flex:1;min-width:0;display:flex;flex-direction:column;align-items:stretch;gap:2px;">` +
                `<span class="mi-cross-line-label" style="display:block;font-size:11px;line-height:1.35;white-space:normal;overflow-wrap:break-word;word-break:normal;min-width:0;" title="${escAttr(tokenTitle)}">${pickLabelInnerHtml}</span>` +
                (pickedDebugDesc
                    ? `<span class="mi-picked-line-debug-desc" style="font-size:10px;font-weight:600;color:${LEGIT_DEBUG_DESC_FG};line-height:1.25;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;text-shadow:0 1px 2px rgba(0,0,0,0.75);" title="${escAttr(pickedDebugDesc)}">${escapeLegitHtml(pickedDebugDesc)}</span>`
                    : '') +
                `</div></div>`;
            const detailBelowRow = [passiveFormatHtml, miPickedUiStatHtml].filter(Boolean).join('');
            const detailBlock =
                detailBelowRow !== ''
                    ? `<div class="mi-slot-picked-line-body" style="margin-top:4px;min-width:0;width:100%;box-sizing:border-box;">${detailBelowRow}</div>`
                    : '';
            return (
                `<div class="mi-slot-picked-line mi-gl-pick mi-cross-line" style="display:block;margin-bottom:4px;min-width:0;width:100%;box-sizing:border-box;" data-line-ref="${escAttr(refAttr)}" data-source-root="${escAttr(sr)}" data-part-key="${escAttr(pk)}">` +
                labelRowHtml +
                `<div class="mi-slot-picked-line-top" style="display:flex;align-items:center;flex-wrap:nowrap;gap:6px;width:100%;min-width:0;">` +
                `<span class="mi-picked-line-drag-handle" draggable="true" title="Drag to reorder (same item)" aria-label="Drag to reorder" style="cursor:grab;color:#8bc34a;font-size:16px;line-height:1;user-select:none;padding:2px 6px 2px 2px;flex-shrink:0;border-radius:3px;border:1px solid #3d4c2d;background:#1a2215;">⋮⋮</span>` +
                `<button type="button" class="mi-picked-line-move-up" title="Move up" aria-label="Move this row up" style="width:24px;min-width:24px;min-height:24px;padding:0;font-size:13px;line-height:1;border-radius:4px;border:1px solid rgba(130,195,240,0.45);background:#182333;color:#d7ecff;cursor:pointer;flex-shrink:0;">↑</button>` +
                `<button type="button" class="mi-picked-line-move-down" title="Move down" aria-label="Move this row down" style="width:24px;min-width:24px;min-height:24px;padding:0;font-size:13px;line-height:1;border-radius:4px;border:1px solid rgba(130,195,240,0.45);background:#182333;color:#d7ecff;cursor:pointer;flex-shrink:0;">↓</button>` +
                `<div style="flex:1;min-width:0;min-height:0" aria-hidden="true"></div>` +
                `<input type="number" class="mi-compact-line-qty mi-picked-line-qty" min="1" max="99" value="${q}" title="Quantity" aria-label="Quantity" />` +
                `<button type="button" class="mi-picked-line-remove" aria-label="Remove row">×</button>` +
                `</div>` +
                detailBlock +
                `</div>`
            );
        }

        function buildMiDoesNotExistSectionHTML(rootKey, miSavedLines) {
            if (!rootKey) return '';
            const missingSaved = (miSavedLines || []).filter(
                (l) => l && l.tableName === MI_DOES_NOT_EXIST_TABLE && l.partKey
            );
            if (!missingSaved.length) return '';
            function escAttr(s) {
                if (s == null) return '';
                return String(s).replace(/&/g, '&amp;').replace(/"/g, '&quot;');
            }
            const rowsHtml = missingSaved
                .map((l) => {
                    if (!l.partKey) return '';
                    const q = l.qty != null ? l.qty : 1;
                    return miSlotBuildPickedLineHtml(l.partKey, q, rootKey, MI_DOES_NOT_EXIST_TABLE);
                })
                .join('');
            const dneUnique = new Set(missingSaved.map(l => String(l.partKey || '')).filter(Boolean)).size;
            const dneInner =
                `<div class="mi-compact-slot mi-does-not-exist-root-slot" data-root-key="${escAttr(rootKey)}" data-table-name="${escAttr(MI_DOES_NOT_EXIST_TABLE)}" data-mi-max-rows="${MI_MODDED_MAX_PICKED_LINES}">` +
                `<div class="mi-compact-slot-rows mi-cross-slot-rows">${rowsHtml}</div>` +
                `</div>`;
            return (
                `<details class="mi-compact-slot-card" open data-mi-dep-filter="${escAttr(MI_DOES_NOT_EXIST_TABLE)}" data-mi-part-count="${dneUnique}" data-mi-picked-unique="${dneUnique}">` +
                `<summary class="mi-compact-slot-card-summary" title="Tokens not found in the loaded dataset; they are still emitted in the serial.">` +
                `<span class="mi-guidelines-check" aria-hidden="true"></span>` +
                `<span class="mi-compact-slot-card-title">Doesn't exist in dataset</span>` +
                `<span class="mi-compact-slot-card-count"> (${dneUnique} added)</span></summary>` +
                `<div class="mi-compact-slot-card-body">${dneInner}</div></details>`
            );
        }

        /** Guidelines: chips are inside .mi-gl-picked-chips-inner so .mi-slot-picked-rows can scroll without flex-wrap inflating its height. */
        function miPickedRowsChipHost(rowsWrap) {
            if (!rowsWrap) return null;
            const inner =
                rowsWrap.classList &&
                rowsWrap.classList.contains('mi-slot-picked-rows') &&
                rowsWrap.querySelector(':scope > .mi-gl-picked-chips-inner');
            return inner || rowsWrap;
        }

        /**
         * Sync code often sets innerHTML on .mi-slot-picked-rows when .mi-gl-picked-chips-inner is missing,
         * which strips the scroll-host/inner structure. Rebuild host + inner so flex+CSS scrollport applies.
         */
        function miNormalizeGuidelinesPickedSlots(rootEl) {
            if (!rootEl || !rootEl.querySelectorAll) return;
            let fixedHost = 0;
            let fixedInner = 0;
            rootEl.querySelectorAll('.mi-guidelines-layout .mi-compact-slot.mi-slot-picker-slot').forEach(function (slot) {
                let pr = slot.querySelector('.mi-slot-picked-rows');
                if (!pr) return;
                if (!slot.querySelector(':scope > .mi-gl-picked-scroll-host')) {
                    const host = document.createElement('div');
                    host.className = 'mi-gl-picked-scroll-host';
                    slot.insertBefore(host, pr);
                    host.appendChild(pr);
                    pr = slot.querySelector('.mi-slot-picked-rows');
                    if (!pr) return;
                    fixedHost++;
                }
                if (!pr.querySelector(':scope > .mi-gl-picked-chips-inner')) {
                    const inner = document.createElement('div');
                    inner.className = 'mi-gl-picked-chips-inner';
                    while (pr.firstChild) inner.appendChild(pr.firstChild);
                    pr.appendChild(inner);
                    fixedInner++;
                }
            });
        }

        function miSlotAppendPickedLine(rowsWrap, encodedRef, qty, rootKeyOverride) {
            if (!rowsWrap || !encodedRef) return;
            const chipHost = miPickedRowsChipHost(rowsWrap);
            if (!chipHost) return;
            let rk = rootKeyOverride;
            const slot = rowsWrap.closest('.mi-compact-slot');
            if (!rk && slot) rk = slot.getAttribute('data-root-key');
            const slotTable = slot && slot.getAttribute('data-table-name');
            chipHost.insertAdjacentHTML(
                'beforeend',
                miSlotBuildPickedLineHtml(encodedRef, qty, rk || null, slotTable || null).trim()
            );
        }

        function miCrossAppendLineRow(rootSlot, sourceRootKey, partKey, qty) {
            const rowsWrap = rootSlot.querySelector('.mi-cross-slot-rows');
            if (!rowsWrap || !sourceRootKey || !partKey) return;
            const ref = makeMiCrossPartRef(sourceRootKey, partKey);
            if (ref) {
                const rk = rootSlot && rootSlot.getAttribute('data-root-key');
                miSlotAppendPickedLine(rowsWrap, ref, qty, rk);
            }
        }

        /** Rebuild one modded root row after extra-slot add/remove (compact UI only). */
        function miReplaceModdedRootItemHtml(rootKey, rootItemEl) {
            if (!rootKey || !rootItemEl || !data || !data.rootDefinitions) return;
            const root = data.rootDefinitions.find(function (r) {
                return r && r.key === rootKey;
            });
            if (!root) return;
            const parent = rootItemEl.parentElement;
            if (!parent) return;
            const inRootsContent = !!rootItemEl.closest('#mi_rootsContent');
            const wasExpanded = rootItemEl.classList.contains('expanded');
            const content = rootItemEl.querySelector('.root-content');
            const wasCollapsed = content && content.classList.contains('collapsed');
            const rootIndex = data.rootDefinitions.findIndex(function (r) {
                return r && r.key === rootKey;
            });
            const useCompact = !!(parent.closest && parent.closest('#item-editor-tab'));
            const newHTML = createRootItemHTML(
                root,
                rootIndex >= 0 ? rootIndex : 0,
                useCompact ? { useCompactDepTables: true } : undefined
            );
            const tempDiv = document.createElement('div');
            tempDiv.innerHTML = newHTML;
            const newEl = tempDiv.firstElementChild;
            if (!newEl) return;
            parent.replaceChild(newEl, rootItemEl);
            if (wasExpanded) {
                newEl.classList.add('expanded');
                const nc = newEl.querySelector('.root-content');
                if (nc) {
                    if (!wasCollapsed) nc.classList.remove('collapsed');
                    else nc.classList.add('collapsed');
                }
            }
            if (useCompact) {
                miInitOpenModdedPickers(newEl);
                if (newEl.classList && newEl.classList.contains('root-item')) {
                    window.__lbCtx = window.legitBuilderInstances && window.legitBuilderInstances[1];
                    syncMiPartLinesFromRootItem(miClosestRootItem(newEl) || newEl);
                }
            }
            if (!inRootsContent) {
                const header = newEl.querySelector('.root-header-clickable');
                if (header) {
                    header.addEventListener('click', function (e) {
                        e.stopPropagation();
                        const ri = this.closest('.root-item');
                        const c = ri && ri.querySelector('.root-content');
                        if (ri && c) {
                            ri.classList.toggle('expanded');
                            c.classList.toggle('collapsed');
                        }
                    });
                }
            }
        }

        function miPopulateCompactAddCategorySelect(shell) {
            const sel = shell && shell.querySelector('select.mi-compact-add-category-select');
            if (!sel || !data || !data.rootDefinitions) return;
            const catalog = getMiGlobalPartCatalog(data);
            const shown = new Set();
            shell.querySelectorAll('.mi-compact-dep-tables details.mi-compact-slot-card').forEach(function (card) {
                const fk = card.getAttribute('data-mi-dep-filter') || '';
                if (fk === MI_CROSS_TABLE || fk === MI_DOES_NOT_EXIST_TABLE) return;
                const nn = normalizeDepTableName(fk);
                if (nn) shown.add(nn);
            });
            sel.innerHTML = '';
            const ph = document.createElement('option');
            ph.value = '';
            ph.textContent = 'Select type to add…';
            sel.appendChild(ph);
            catalog.types.forEach(function (t) {
                const n = normalizeDepTableName(String(t || ''));
                if (!n || n === 'inv_comp') return;
                if (miShouldHideEndgameForModded() && n === 'endgame') return;
                if (shown.has(n)) return;
                const cnt = miFilterCrossCatalogItemsForViewMode(catalog.byType.get(t) || [], data).length;
                if (!cnt) return;
                const opt = document.createElement('option');
                opt.value = String(t);
                opt.textContent = String(t) + ' (' + cnt + ' in global catalog)';
                sel.appendChild(opt);
            });
        }

        /** Unique picked rows in one part-slot card (by encoded line ref). */
        function miCountUniquePickedInCompactCard(card) {
            if (!card || !card.querySelector) return 0;
            const slot =
                card.querySelector('.mi-compact-slot[data-table-name]') ||
                card.querySelector('.mi-cross-root-slot');
            if (!slot) return 0;
            const refs = new Set();
            slot.querySelectorAll('.mi-slot-picked-line[data-line-ref]').forEach(function (line) {
                const r = line.getAttribute('data-line-ref');
                if (r) refs.add(r);
            });
            return refs.size;
        }

        function miApplyCompactDepFilter(selectEl) {
            if (!selectEl || !selectEl.classList || !selectEl.classList.contains('mi-compact-dep-filter')) {
                return;
            }
            const shell = selectEl.closest('.mi-compact-dep-shell');
            if (!shell) return;
            const valRaw = String(selectEl.value || '').trim();
            const valNorm = valRaw ? normalizeDepTableName(valRaw) : '';
            const dneNorm = normalizeDepTableName(MI_DOES_NOT_EXIST_TABLE);
            const crossNorm = normalizeDepTableName(MI_CROSS_TABLE);
            const cards = shell.querySelectorAll('.mi-compact-dep-tables details.mi-compact-slot-card');
            cards.forEach(function (card) {
                const key = card.getAttribute('data-mi-dep-filter') || '';
                if (!valRaw) {
                    card.style.display = '';
                    return;
                }
                if (key === MI_CROSS_TABLE) {
                    card.style.display = valRaw === MI_CROSS_TABLE || valNorm === crossNorm ? '' : 'none';
                    return;
                }
                if (key === MI_DOES_NOT_EXIST_TABLE) {
                    card.style.display = valRaw === MI_DOES_NOT_EXIST_TABLE || valNorm === dneNorm ? '' : 'none';
                    return;
                }
                const keyNorm = normalizeDepTableName(key);
                card.style.display = keyNorm === valNorm ? '' : 'none';
            });
        }

        /**
         * Category filter: options match this root’s rendered slot cards only (avoids picking a global type
         * that hides every card). Labels show unique parts added vs catalog size.
         */
        function miInitCompactDepToolbar(rootEl) {
            if (!rootEl || !rootEl.querySelectorAll) return;
            rootEl.querySelectorAll('.mi-compact-dep-shell').forEach(function (shell) {
                const sel = shell.querySelector('select.mi-compact-dep-filter');
                if (!sel) return;
                const prev = sel.value;
                const cards = shell.querySelectorAll('.mi-compact-dep-tables details.mi-compact-slot-card');
                const cardList = Array.prototype.slice.call(cards);
                let totalCatalogNoCross = 0;
                let totalAddedAll = 0;

                const crossSection = shell.querySelector('.mi-cross-section');
                if (crossSection) {
                    const crossSlot = crossSection.querySelector('.mi-cross-root-slot');
                    let crossAdded = 0;
                    if (crossSlot) {
                        const refs = new Set();
                        crossSlot.querySelectorAll('.mi-slot-picked-line[data-line-ref]').forEach(function (line) {
                            const r = line.getAttribute('data-line-ref');
                            if (r) refs.add(r);
                        });
                        crossAdded = refs.size;
                    }
                    totalAddedAll += crossAdded;
                    const crossBrowse = parseInt(crossSection.getAttribute('data-mi-part-count') || '0', 10) || 0;
                    crossSection.setAttribute('data-mi-picked-unique', String(crossAdded));
                    const crossMeta = crossSection.querySelector('.mi-cross-section-meta');
                    if (crossMeta) {
                        crossMeta.textContent =
                            crossAdded + ' added · ' + crossBrowse.toLocaleString() + ' parts in catalog';
                    }
                }

                cardList.forEach(function (card) {
                    const fk = card.getAttribute('data-mi-dep-filter') || '';
                    const added = miCountUniquePickedInCompactCard(card);
                    const browse = parseInt(card.getAttribute('data-mi-part-count') || '0', 10) || 0;
                    totalAddedAll += added;
                    if (fk !== MI_CROSS_TABLE) totalCatalogNoCross += browse;
                    card.setAttribute('data-mi-picked-unique', String(added));
                    const countEl = card.querySelector('.mi-compact-slot-card-count');
                    if (countEl) {
                        if (fk === MI_DOES_NOT_EXIST_TABLE) {
                            countEl.textContent = ' (' + added + ' added)';
                        } else {
                            countEl.textContent = ' (' + added + ' added · ' + browse + ' in catalog)';
                        }
                    }
                });

                sel.innerHTML = '';
                const allOpt = document.createElement('option');
                allOpt.value = '';
                allOpt.textContent =
                    'All categories (' + totalCatalogNoCross + ' in catalog · ' + totalAddedAll + ' added)';
                sel.appendChild(allOpt);

                cardList.forEach(function (card) {
                    const fk = card.getAttribute('data-mi-dep-filter') || '';
                    if (!fk) return;
                    const added = miCountUniquePickedInCompactCard(card);
                    const browse = parseInt(card.getAttribute('data-mi-part-count') || '0', 10) || 0;
                    const opt = document.createElement('option');
                    opt.value = fk;
                    if (fk === MI_DOES_NOT_EXIST_TABLE) {
                        opt.textContent = "Doesn't exist in dataset (" + added + ' added)';
                    } else if (fk === MI_CROSS_TABLE) {
                        opt.textContent =
                            'Cross-manufacturer parts (' + added + ' added · ' + browse + ' in catalog)';
                    } else {
                        opt.textContent = fk + ' (' + added + ' added · ' + browse + ' in catalog)';
                    }
                    sel.appendChild(opt);
                });

                if (Array.prototype.some.call(sel.options, function (o) { return o.value === prev; })) {
                    sel.value = prev;
                } else {
                    sel.value = '';
                }
                miApplyCompactDepFilter(sel);
                miPopulateCompactAddCategorySelect(shell);
            });
        }

        /** Compact part-slot grid: native selects + cross-manufacturer catalog. */
        function miInitOpenModdedPickers(rootEl) {
            if (!rootEl || !rootEl.querySelectorAll) return;
            rootEl.querySelectorAll('.mi-cross-root-slot').forEach(function (crossSlot) {
                if (crossSlot.dataset.miCrossInit === '1') return;
                crossSlot.dataset.miCrossInit = '1';
                populateMiCrossTypeSelect(crossSlot);
                miCrossRefreshCandidateList(crossSlot);
            });
            rootEl.querySelectorAll('.mi-compact-slot.mi-slot-picker-slot').forEach(function (s) {
                if (s.dataset.miSlotInit === '1') return;
                s.dataset.miSlotInit = '1';
                miSlotPopulateNativeSelect(s);
                miSlotPopulateCandidateList(s);
            });
        }

        /** Generic BL4 rare-tier composition keys (never Pearlescent UI tier). */
        function miInvCompKeyIsRareCompositionKey(key) {
            const k = String(key || '').toLowerCase();
            return k === 'comp_03_rare' || k === 'base_comp_03_rare';
        }

        /** True when Nexus dep marks this inv_comp as Pearlescent (tier 6), independent of comp_05_* key naming. */
        function miInvCompDepIsPearlescent(dep) {
            if (!dep) return false;
            const key = String(dep.key || dep.inv_comp || (dep.item && dep.item.key) || '').toLowerCase();
            if (miInvCompKeyIsRareCompositionKey(key)) return false;
            const r = String(dep.rarity || (dep.item && dep.item.rarity) || '').toLowerCase();
            if (
                /\bpearlescent\b|06_pearlescent|_06_pearlescent|rarity'?0*6\b|rarity06/i.test(r)
            )
                return true;
            if (/comp_06|_06_pearlescent|base_comp_06/.test(key)) return true;
            const tagSets = [dep.basetags, dep.item && dep.item.basetags].filter(Boolean);
            for (let ti = 0; ti < tagSets.length; ti++) {
                const bt = tagSets[ti];
                if (!bt) continue;
                if (!Array.isArray(bt)) continue;
                for (let j = 0; j < bt.length; j++) {
                    const t = bt[j];
                    if (t == null) continue;
                    if (typeof t === 'object') {
                        const ks = Object.keys(t);
                        for (let k = 0; k < ks.length; k++) {
                            if (String(ks[k]).toLowerCase() === 'pearlescent') return true;
                            const v = t[ks[k]];
                            if (v != null && /\bpearlescent\b/i.test(String(v))) return true;
                        }
                    } else if (/\bpearlescent\b/i.test(String(t))) return true;
                }
            }
            return false;
        }

        /** Fallback when catalog row has no dep: key/label-only pearlescent hints (e.g. comp_06_*). */
        function miInvCompPearlescentFromKeyLabel(key, label) {
            if (miInvCompKeyIsRareCompositionKey(key)) return false;
            const s = `${String(key || '')} ${String(label || '')}`.toLowerCase();
            return /\bpearlescent\b|comp_06|_06_pearlescent|base_comp_06/.test(s);
        }

        /** Space-separated rarity synonyms for inv_comp slot filter (lab/key often omit "pearlescent"). */
        function miInvCompDepRaritySearchText(dep) {
            if (!dep) return '';
            const parts = [];
            const add = function (w) {
                const t = String(w || '')
                    .toLowerCase()
                    .trim();
                if (t && parts.indexOf(t) < 0) parts.push(t);
            };
            const rawKey = String(dep.key || dep.inv_comp || (dep.item && dep.item.key) || '').toLowerCase();
            const rar = String(dep.rarity || (dep.item && dep.item.rarity) || '').toLowerCase();
            rar.split(/[^a-z0-9_]+/i).forEach(function (tok) {
                if (tok && tok.length > 1) add(tok);
            });
            if (miInvCompDepIsPearlescent(dep)) {
                add('pearlescent');
                add('pearl');
                add('rarity_06');
                add('06_pearlescent');
                add('tier6');
                add('tier_6');
                add('rarity06');
            } else if (/comp_06|base_comp_06|_06_pearlescent/.test(rawKey)) {
                add('pearlescent');
                add('pearl');
                add('rarity_06');
            } else if (/comp_05|_legendary|base_comp_05/.test(rawKey)) {
                add('legendary');
                add('comp_05');
                add('orange');
            } else if (/comp_04|_epic|base_comp_04/.test(rawKey)) {
                add('epic');
                add('comp_04');
                add('purple');
            } else if (/comp_03|_rare|base_comp_03/.test(rawKey)) {
                add('rare');
                add('comp_03');
            } else if (/comp_02|_uncommon|base_comp_02/.test(rawKey)) {
                add('uncommon');
                add('comp_02');
            } else if (/comp_01|_common|base_comp_01/.test(rawKey)) {
                add('common');
                add('comp_01');
            }
            return parts.join(' ');
        }

        /** CSS class suffix for inv_comp browse rows from key/label (comp_01_common, etc.). */
        function miInvCompRarityClass(key, label) {
            const s = `${String(key || '')} ${String(label || '')}`.toLowerCase();
            if (/\bpearlescent\b|comp_06|_06_pearlescent|base_comp_06/.test(s)) return '';
            if (/\blegendary\b|comp_05|_legendary/.test(s)) return 'mi-rarity-legendary';
            if (/\bepic\b|comp_04|_epic/.test(s)) return 'mi-rarity-epic';
            if (/\brare\b|comp_03|_rare/.test(s)) return 'mi-rarity-rare';
            if (/\buncommon\b|comp_02|_uncommon/.test(s)) return 'mi-rarity-uncommon';
            if (/\bcommon\b|comp_01|_common/.test(s)) return 'mi-rarity-common';
            return '';
        }

        const MI_INV_COMP_RARITY_SELECT_CLASSES = [
            'mi-rarity-common',
            'mi-rarity-uncommon',
            'mi-rarity-rare',
            'mi-rarity-epic',
            'mi-rarity-legendary',
            'mi-rarity-pearlescent'
        ];

        function miInvCompOptionRarityClass(payload) {
            if (!payload) return '';
            const tn = normalizeDepTableName(payload.t || '');
            if (tn !== 'inv_comp') return '';
            if (payload.pearl || miInvCompPearlescentFromKeyLabel(payload.k, payload.l)) {
                return 'mi-rarity-pearlescent';
            }
            return miInvCompRarityClass(payload.k, payload.l) || '';
        }

        function miSyncInvCompSelectRarityColor(sel) {
            if (!sel || !sel.classList || !sel.classList.contains('mi-slot-inv-comp-only')) return;
            MI_INV_COMP_RARITY_SELECT_CLASSES.forEach(function (c) {
                sel.classList.remove(c);
            });
            const opt = sel.options && sel.selectedIndex >= 0 ? sel.options[sel.selectedIndex] : null;
            if (!opt || !opt.value) return;
            MI_INV_COMP_RARITY_SELECT_CLASSES.forEach(function (c) {
                if (opt.classList.contains(c)) sel.classList.add(c);
            });
        }

        /** Nearest ancestor that establishes a containing block for position:fixed (same as transform). */
        function miGetFixedContainingBlock(el) {
            let p = el && el.parentElement;
            while (p) {
                const s = window.getComputedStyle(p);
                if (s.transform && s.transform !== 'none') return p;
                if (s.filter && s.filter !== 'none') return p;
                if (s.perspective && s.perspective !== 'none') return p;
                const bd = s.backdropFilter || s.getPropertyValue('-webkit-backdrop-filter');
                if (bd && bd !== 'none' && String(bd).trim() !== '') return p;
                if (s.contain && /paint|strict|layout/.test(s.contain)) return p;
                p = p.parentElement;
            }
            return null;
        }

        function miPositionCandDropdownPanel(wrap) {
            if (!wrap || !wrap.classList || !wrap.classList.contains('mi-slot-cand-dropdown--open')) return;
            const tr = wrap.querySelector('.mi-slot-cand-dropdown-trigger');
            const pan = wrap.querySelector('.mi-slot-cand-dropdown-panel');
            if (!tr || !pan || pan.hidden) return;
            const r = tr.getBoundingClientRect();
            const margin = 4;
            const cap = Math.min(560, window.innerHeight * 0.68);
            const spaceBelow = window.innerHeight - r.bottom - margin;
            const spaceAbove = r.top - margin;
            let topV = r.bottom + margin;
            let maxHeight = Math.min(cap, Math.max(0, spaceBelow - margin));
            if (maxHeight < 140 && spaceAbove > spaceBelow) {
                maxHeight = Math.min(cap, Math.max(0, spaceAbove - margin));
                topV = Math.max(margin, r.top - maxHeight - margin);
            }
            maxHeight = Math.max(120, maxHeight);
            let leftV = r.left;
            let width = r.width;
            const pad = 8;
            if (leftV + width > window.innerWidth - pad) {
                leftV = Math.max(pad, window.innerWidth - width - pad);
            }
            if (leftV < pad) {
                width = Math.max(200, Math.min(width, window.innerWidth - pad * 2));
                leftV = pad;
            }
            const fcb = miGetFixedContainingBlock(pan);
            let left = leftV;
            let top = topV;
            if (fcb) {
                const cbr = fcb.getBoundingClientRect();
                left = leftV - cbr.left;
                top = topV - cbr.top;
            }
            pan.classList.add('mi-slot-cand-dropdown-panel--fixed');
            pan.style.position = 'fixed';
            pan.style.left = left + 'px';
            pan.style.top = top + 'px';
            pan.style.width = width + 'px';
            pan.style.maxHeight = maxHeight + 'px';
            pan.style.height = maxHeight + 'px';
            pan.style.zIndex = '10060';
            pan.style.boxSizing = 'border-box';
        }

        function miClearCandDropdownPanelLayout(pan) {
            if (!pan || !pan.style) return;
            pan.classList.remove('mi-slot-cand-dropdown-panel--fixed');
            pan.style.position = '';
            pan.style.left = '';
            pan.style.top = '';
            pan.style.width = '';
            pan.style.maxHeight = '';
            pan.style.height = '';
            pan.style.zIndex = '';
            pan.style.boxSizing = '';
        }

        let __miCandDropdownRelayoutScheduled = false;
        function miScheduleCandDropdownRelayout() {
            if (__miCandDropdownRelayoutScheduled) return;
            __miCandDropdownRelayoutScheduled = true;
            requestAnimationFrame(function () {
                __miCandDropdownRelayoutScheduled = false;
                document.querySelectorAll('#item-editor-tab .mi-slot-cand-dropdown--open').forEach(miPositionCandDropdownPanel);
            });
        }

        function miCloseCandDropdown(wrap) {
            if (!wrap || !wrap.classList) return;
            const card = wrap.closest('details.mi-compact-slot-card');
            wrap.classList.remove('mi-slot-cand-dropdown--open');
            const tr = wrap.querySelector('.mi-slot-cand-dropdown-trigger');
            const pan = wrap.querySelector('.mi-slot-cand-dropdown-panel');
            if (tr) tr.setAttribute('aria-expanded', 'false');
            if (pan) {
                miClearCandDropdownPanelLayout(pan);
                pan.hidden = true;
            }
            if (card && !card.querySelector('.mi-slot-cand-dropdown--open')) {
                card.classList.remove('mi-gl-dropdown-open');
            }
        }

        function miOpenCandDropdown(wrap) {
            if (!wrap) return;
            const card = wrap.closest('details.mi-compact-slot-card');
            if (card) card.classList.add('mi-gl-dropdown-open');
            wrap.classList.add('mi-slot-cand-dropdown--open');
            const tr = wrap.querySelector('.mi-slot-cand-dropdown-trigger');
            const pan = wrap.querySelector('.mi-slot-cand-dropdown-panel');
            if (tr) tr.setAttribute('aria-expanded', 'true');
            if (pan) pan.hidden = false;
            miPositionCandDropdownPanel(wrap);
            miScheduleCandDropdownRelayout();
        }

        function miToggleCandDropdown(wrap) {
            if (!wrap) return;
            if (wrap.classList.contains('mi-slot-cand-dropdown--open')) {
                miCloseCandDropdown(wrap);
            } else {
                document.querySelectorAll('#item-editor-tab .mi-slot-cand-dropdown--open').forEach(function (w) {
                    if (w !== wrap) {
                        miCloseCandDropdown(w);
                    }
                });
                miOpenCandDropdown(wrap);
            }
        }

        function miUpdateSlotCandDropdownLabel(slot) {
            if (!slot) return;
            const dd = slot.querySelector('.mi-slot-cand-dropdown');
            if (!dd) return;
            const labelEl = dd.querySelector('.mi-slot-cand-dropdown-label');
            if (!labelEl) return;
            const rich = slot.querySelector('.mi-passive-candidate-list.mi-slot-candidate-rich');
            const inv = slot.getAttribute('data-table-name') === 'inv_comp';
            const nSel = rich ? rich.querySelectorAll('.mi-cand-row.mi-cand-selected').length : 0;
            if (nSel === 0) {
                labelEl.textContent = inv ? 'Choose inventory comp…' : 'Choose parts from catalog…';
                return;
            }
            if (nSel === 1 && rich) {
                const row = rich.querySelector('.mi-cand-row.mi-cand-selected');
                const line1 = row && row.querySelector('.mi-cand-line1');
                const t = line1 ? line1.innerText.replace(/\s+/g, ' ').trim() : '';
                labelEl.textContent = t.length > 72 ? t.slice(0, 69) + '…' : (t || '1 part selected');
                return;
            }
            labelEl.textContent = nSel + ' parts selected';
        }

        /** Populate native &lt;select class="mi-part-slot-select"&gt; from slot JSON or cross-manufacturer catalog. */
        function miSlotItemsJsonScript(slot) {
            if (!slot) return null;
            return (
                slot.querySelector('.mi-slot-items-json') ||
                (slot.closest('.mi-part-slot-card') &&
                    slot.closest('.mi-part-slot-card').querySelector('.mi-slot-items-json'))
            );
        }

        function miSlotPopulateNativeSelect(slot) {
            if (!slot) return;
            const nativeSel = slot.querySelector('select.mi-part-slot-select');
            if (!nativeSel || nativeSel.classList.contains('mi-cross-part-select')) return;
            const tableName = slot.getAttribute('data-table-name') || '';
            const tn = normalizeDepTableName(tableName);
            const unlockCb = slot.querySelector('.mi-slot-all-manufacturers');
            const allMfr = !!(unlockCb && unlockCb.checked);
            const rootKeyForComp = slot.getAttribute('data-root-key');
            const compRoot =
                rootKeyForComp && data && data.rootDefinitions
                    ? data.rootDefinitions.find(function (r) {
                          return r.key === rootKeyForComp;
                      })
                    : null;
            const compSer =
                compRoot != null && compRoot.serialIndex != null && compRoot.serialIndex !== undefined
                    ? compRoot.serialIndex
                    : null;
            const baseGenericRoot = compRoot && data ? getBaseGenericRootForPartLookup(compRoot, data) : null;
            let raw = [];
            if (allMfr && data) {
                if (tn === 'inv_comp') {
                    raw = getMiGlobalInvCompCatalogItems(data).map(function (it) {
                        return {
                            r: it.sourceRootKey || it.rootKey,
                            k: it.key,
                            t: tn,
                            l: (it.label || it.key) + ' — ' + (it.sourceRootKey || it.rootKey)
                        };
                    });
                } else {
                    const catalog = getMiGlobalPartCatalog(data);
                    const items = miFilterCrossCatalogItemsForViewMode(
                        (catalog.byType && catalog.byType.get(tn)) || [],
                        data
                    );
                    raw = items.map(function (it) {
                        return {
                            r: it.sourceRootKey,
                            k: it.key,
                            t: tn,
                            l: (it.label || it.key) + ' — ' + it.sourceRootKey
                        };
                    });
                }
            } else {
                const scr = miSlotItemsJsonScript(slot);
                if (!scr || !scr.textContent) return;
                try {
                    raw = JSON.parse(scr.textContent);
                } catch (e) {
                    return;
                }
                if (!Array.isArray(raw)) return;
            }
            const searchInp = slot.querySelector('.mi-slot-search');
            const q = (searchInp && searchInp.value ? searchInp.value : '').trim().toLowerCase();
            const terms = miSplitSearchTerms(q);
            let filtered = raw;
            if (terms.length) {
                filtered = raw.filter(function (x) {
                    const lab = String(x.l || '').toLowerCase();
                    const kk = String(x.k || '').toLowerCase();
                    const rr = String(x.r || '').toLowerCase();
                    const dd = String(x.d || '').toLowerCase();
                    const val = makeMiCrossPartRef(x.r, x.k);
                    const pe =
                        data && compRoot && val
                            ? resolvePartEntryForSerialOutput(compRoot, val, baseGenericRoot, data)
                            : null;
                    const tok = String(
                        data && compRoot && val
                            ? formatDeserializedPartToken(compSer, val, pe, data)
                            : ''
                    ).toLowerCase();
                    const tt = String(x.t || '').toLowerCase();
                    const aliasText = miGetManufacturerAliasSearchText(x.l, x.k, x.r, x.t, x.d);
                    const rarityHay = tn === 'inv_comp' ? String(x.s || '') : '';
                    return miMatchesSearchTerms(terms, [lab, kk, rr, dd, tok, tt, aliasText, rarityHay]);
                });
            }
            filtered.sort(function (a, b) {
                return String(a.l || a.k).localeCompare(String(b.l || b.k));
            });
            const maxN = tn === 'passive_points' ? 2000 : 500;
            const slice = filtered.slice(0, maxN);
            const prevSelected = nativeSel.multiple
                ? Array.from(nativeSel.selectedOptions || [])
                      .map(function (o) {
                          return o.value;
                      })
                      .filter(Boolean)
                : nativeSel.value
                  ? [nativeSel.value]
                  : [];
            nativeSel.innerHTML = buildMiPartSlotSelectOptionsHtml(slice, maxN);
            if (prevSelected.length) {
                Array.from(nativeSel.options).forEach(function (o) {
                    if (prevSelected.indexOf(o.value) !== -1) {
                        o.selected = true;
                    }
                });
            }
            miPartSlotMultidropRefreshList(slot);
            if (filtered.length > maxN) {
                nativeSel.title = 'Showing ' + slice.length + ' of ' + filtered.length + ' — narrow search or disable cross-manufacturer';
            } else {
                nativeSel.title = '';
            }
            if (tn === 'inv_comp') {
                miSyncInvCompSelectRarityColor(nativeSel);
            }
        }

        function miSlotPopulateCandidateList(slot) {
            if (!slot) return;
            const tableName = slot.getAttribute('data-table-name') || '';
            const tn = normalizeDepTableName(tableName);
            const richPassive = slot.querySelector('.mi-slot-candidate-rich.mi-passive-candidate-list');
            const list = slot.querySelector('.mi-slot-candidate-list');
            if (!richPassive && !list) return;
            const unlockCb = slot.querySelector('.mi-slot-all-manufacturers');
            const allMfr = !!(unlockCb && unlockCb.checked);

            let raw;
            if (allMfr && data) {
                if (tn === 'inv_comp') {
                    raw = getMiGlobalInvCompCatalogItems(data);
                } else {
                    const catalog = getMiGlobalPartCatalog(data);
                    const items = miFilterCrossCatalogItemsForViewMode(
                        (catalog.byType && catalog.byType.get(tn)) || [],
                        data
                    );
                    raw = items.map(it => {
                        const rowAm = {
                            r: it.sourceRootKey,
                            k: it.key,
                            t: tn,
                            l:
                                (it.label || it.key) +
                                ' — ' +
                                it.sourceRootKey +
                                ' · ' +
                                it.key
                        };
                        if (it.passiveIconUrl) rowAm.i = it.passiveIconUrl;
                        const dbgAm = getPartDebugDisplayDescription(it.dep);
                        if (dbgAm) {
                            rowAm.d = dbgAm;
                        }
                        if (it.passiveDesc) {
                            rowAm.p = it.passiveDesc;
                        }
                        return rowAm;
                    });
                }
            } else {
                const scr = miSlotItemsJsonScript(slot);
                if (!scr || !scr.textContent) return;
                try {
                    raw = JSON.parse(scr.textContent);
                } catch (e) {
                    return;
                }
                if (!Array.isArray(raw)) return;
            }

            const searchInp = slot.querySelector('.mi-slot-search');
            const q = (searchInp && searchInp.value ? searchInp.value : '').trim().toLowerCase();
            const terms = miSplitSearchTerms(q);
            const rootKeyForComp = slot.getAttribute('data-root-key');
            const compRoot =
                rootKeyForComp && data && data.rootDefinitions
                    ? data.rootDefinitions.find(r => r.key === rootKeyForComp)
                    : null;
            const compSer =
                compRoot != null && compRoot.serialIndex != null && compRoot.serialIndex !== undefined
                    ? compRoot.serialIndex
                    : null;
            const baseGenericRoot = compRoot && data ? getBaseGenericRootForPartLookup(compRoot, data) : null;
            let filtered = raw;
            if (terms.length) {
                filtered = raw.filter(x => {
                    const lab = String(x.l || '').toLowerCase();
                    const kk = String(x.k || '').toLowerCase();
                    const rr = String(x.r || '').toLowerCase();
                    const dd = String(x.d || '').toLowerCase();
                    const pp = String(x.p || '').toLowerCase();
                    const val = makeMiCrossPartRef(x.r, x.k);
                    const pe =
                        data && compRoot && val
                            ? resolvePartEntryForSerialOutput(compRoot, val, baseGenericRoot, data)
                            : null;
                    const tok = String(
                        data && compRoot && val
                            ? formatDeserializedPartToken(compSer, val, pe, data)
                            : ''
                    ).toLowerCase();
                    const tt = String(x.t || '').toLowerCase();
                    const aliasText = miGetManufacturerAliasSearchText(x.l, x.k, x.r, x.t, x.d);
                    const rarityHay = tn === 'inv_comp' ? String(x.s || '') : '';
                    return miMatchesSearchTerms(terms, [lab, kk, rr, dd, pp, tok, tt, aliasText, rarityHay]);
                });
            }
            filtered.sort((a, b) => String(a.l || a.k).localeCompare(String(b.l || b.k)));
            const maxN = tn === 'passive_points' ? 2000 : 500;
            const slice = filtered.slice(0, maxN);

            function escAttrMiCand(s) {
                if (s == null) return '';
                return String(s).replace(/&/g, '&amp;').replace(/"/g, '&quot;');
            }
            function miModdedBrowseCandRowHtml(x) {
                const val = makeMiCrossPartRef(x.r, x.k);
                const ve = String(val).replace(/&/g, '&amp;').replace(/"/g, '&quot;');
                let tok = '';
                if (data && compRoot) {
                    const pe = resolvePartEntryForSerialOutput(compRoot, val, baseGenericRoot, data);
                    tok = formatDeserializedPartToken(compSer, val, pe, data) || '';
                }
                const nameText = String(x.l || x.k);
                const dbg = x.d != null && String(x.d).trim() ? String(x.d) : '';
                const pearlDual =
                    tn === 'inv_comp' &&
                    (!!x.pearl || miInvCompPearlescentFromKeyLabel(x.k, nameText));
                let rarityClass = '';
                if (tn === 'inv_comp' && !pearlDual) {
                    const rc = miInvCompRarityClass(x.k, nameText);
                    if (rc) rarityClass = ' ' + rc;
                }
                const line1PearlClass = pearlDual ? ' mi-cand-line1--pearlescent-inv-comp' : '';
                const tokSpan = tok ? `<span class="mi-cand-token">${escapeLegitHtml(tok)}</span> ` : '';
                const dbgSpan = dbg
                    ? `<span class="mi-cand-sep"> — </span><span class="mi-cand-debug">${escapeLegitHtml(dbg)}</span>`
                    : '';
                const line1 =
                    `<div class="mi-cand-line1${line1PearlClass}" style="display:flex;flex-wrap:wrap;align-items:baseline;gap:4px 6px;">` +
                    tokSpan +
                    `<span class="mi-cand-name${rarityClass}">${escapeLegitHtml(nameText)}</span>` +
                    dbgSpan +
                    `</div>`;
                const rowStyle =
                    'display:flex;align-items:flex-start;gap:8px;padding:6px 8px;cursor:pointer;border-bottom:1px solid #2a2a2a;font-size:11px;color:#e8e8e8;box-sizing:border-box;';
                if (tn === 'passive_points') {
                    const iconUrl = x.i ? String(x.i) : '';
                    const imgHtml = iconUrl
                        ? `<img src="${escAttrMiCand(iconUrl)}" alt="" width="24" height="24" loading="lazy" style="object-fit:contain;flex-shrink:0;border-radius:3px;border:1px solid #555;background:#151515;align-self:flex-start;margin-top:1px;" onerror="this.style.opacity='0.2';" />`
                        : `<span style="width:24px;height:24px;flex-shrink:0;border:1px dashed #444;border-radius:3px;background:#151515;display:block;box-sizing:border-box;align-self:flex-start;margin-top:1px;"></span>`;
                    const descRaw = x.p != null && String(x.p).trim() ? String(x.p).trim() : '';
                    const descBlock = descRaw
                        ? `<div class="mi-passive-cand-formattext mi-cand-passive-blurb" style="font-size:10px;line-height:1.35;margin-top:3px;white-space:normal;word-break:break-word;">${formatUiStatTextToHtml(descRaw)}</div>`
                        : '';
                    return (
                        `<div class="mi-cand-row" role="option" aria-selected="false" data-ref="${ve}" style="${rowStyle}">` +
                        imgHtml +
                        `<div style="flex:1;min-width:0;display:flex;flex-direction:column;align-items:stretch;gap:0;">` +
                        line1 +
                        descBlock +
                        `</div>` +
                        `</div>`
                    );
                }
                return (
                    `<div class="mi-cand-row" role="option" aria-selected="false" data-ref="${ve}" style="${rowStyle}">` +
                    `<div style="flex:1;min-width:0;display:flex;flex-direction:column;align-items:stretch;gap:0;">` +
                    line1 +
                    `</div>` +
                    `</div>`
                );
            }

            if (richPassive) {
                richPassive.innerHTML = slice.map(miModdedBrowseCandRowHtml).join('');
                richPassive._miPassiveAnchorIdx = null;
                const foot = slot.querySelector('.mi-passive-cand-foot');
                if (foot) {
                    let msg = `Showing ${slice.length} of ${filtered.length} after filter`;
                    if (slice.length < filtered.length) {
                        msg += ` (${filtered.length - slice.length} past list cap — narrow search)`;
                    }
                    msg += '.';
                    if (!allMfr) {
                        msg += ' Enable cross-manufacturer above to list this slot from every root.';
                    }
                    foot.textContent = msg;
                }
                const sal = slot.querySelector('.mi-slot-select-all-label');
                if (sal) {
                    sal.textContent = `Select all visible (${slice.length})`;
                }
                const sa = slot.querySelector('.mi-slot-select-all');
                if (sa) {
                    sa.checked = false;
                    sa.indeterminate = false;
                }
                if (filtered.length > maxN) {
                    const more = document.createElement('div');
                    more.className = 'mi-passive-cand-more';
                    more.style.cssText = 'padding:6px 8px;font-size:10px;color:#888;border-top:1px solid #333;';
                    more.textContent = `… ${filtered.length - maxN} more — narrow search`;
                    richPassive.appendChild(more);
                }
                miUpdateSlotCandDropdownLabel(slot);
                const ddRep = slot.querySelector('.mi-slot-cand-dropdown.mi-slot-cand-dropdown--open');
                if (ddRep) {
                    miPositionCandDropdownPanel(ddRep);
                }
                return;
            }

            if (!list) return;
            list.innerHTML = slice
                .map(x => {
                    const val = makeMiCrossPartRef(x.r, x.k);
                    const ve = String(val).replace(/&/g, '&amp;').replace(/"/g, '&quot;');
                    let labelText = String(x.l || x.k);
                    if (x.d) {
                        labelText = labelText + ' — ' + String(x.d);
                    }
                    if (data && compRoot) {
                        const pe = resolvePartEntryForSerialOutput(compRoot, val, baseGenericRoot, data);
                        const tok = formatDeserializedPartToken(compSer, val, pe, data);
                        if (tok) {
                            labelText = labelText + '  ' + tok;
                        }
                    }
                    return `<option value="${ve}">${escapeLegitHtml(labelText)}</option>`;
                })
                .join('');
            if (filtered.length > maxN) {
                const opt = document.createElement('option');
                opt.disabled = true;
                opt.textContent = `… ${filtered.length - maxN} more — narrow search`;
                list.appendChild(opt);
            }
            if (list.multiple) {
                const sal = slot.querySelector('.mi-slot-select-all-label');
                if (sal) {
                    sal.textContent = `Select all visible (${slice.length})`;
                }
                const sa = slot.querySelector('.mi-slot-select-all');
                if (sa) {
                    sa.checked = false;
                    sa.indeterminate = false;
                }
            }
        }

        /**
         * After push-from-parse, passive_points often arrives as one row per tier (tier_1…tier_5, extra tier_5s).
         * Collapse each run that matches expandModdedMiLineToPartKeys(tier_1_ref, Q) into one line { tier_1_ref, qty: Q },
         * same as adding one passive from browse with quantity Q.
         */
        function collapsePassivePointsTierRunsForMiLines(lines) {
            if (!lines || !lines.length) return lines;
            const out = [];
            let i = 0;
            while (i < lines.length) {
                const line = lines[i];
                if (!line || !line.partKey) {
                    i++;
                    continue;
                }
                const tn = normalizeDepTableName(line.tableName);
                if (tn !== 'passive_points') {
                    out.push(line);
                    i++;
                    continue;
                }
                const cross = parseMiCrossPartRef(line.partKey);
                const inner = cross ? cross.partKey : String(line.partKey);
                const meta = parsePassivePointsTierKey(inner);
                if (!meta || meta.tier !== 1) {
                    out.push(line);
                    i++;
                    continue;
                }
                const firstRef = line.partKey;
                let bestQ = 0;
                for (let cand = 1; cand <= 99; cand++) {
                    const expanded = expandModdedMiLineToPartKeys(firstRef, cand);
                    if (!expanded.length || i + expanded.length > lines.length) break;
                    let ok = true;
                    for (let k = 0; k < expanded.length; k++) {
                        const ln = lines[i + k];
                        if (!ln || !ln.partKey) {
                            ok = false;
                            break;
                        }
                        if (normalizeDepTableName(ln.tableName) !== 'passive_points') {
                            ok = false;
                            break;
                        }
                        let q = ln.qty != null ? parseInt(ln.qty, 10) : 1;
                        if (isNaN(q) || q < 1) q = 1;
                        if (q !== 1) {
                            ok = false;
                            break;
                        }
                        if (String(ln.partKey) !== String(expanded[k])) {
                            ok = false;
                            break;
                        }
                    }
                    if (ok) bestQ = cand;
                    else break;
                }
                if (bestQ >= 1) {
                    const consumed = expandModdedMiLineToPartKeys(firstRef, bestQ).length;
                    const mergedLine = {
                        tableName: line.tableName,
                        partKey: firstRef,
                        qty: bestQ
                    };
                    if (line._miOrder != null) mergedLine._miOrder = line._miOrder;
                    out.push(mergedLine);
                    i += consumed;
                } else {
                    out.push(line);
                    i++;
                }
            }
            return out;
        }

        /** Merge consecutive identical (tableName, partKey) into one line with summed qty (max 99). Preserves serial order. */
        function miAppendLineEntryConsecutive(arr, tableName, partKey, qty, orderMeta) {
            if (!arr || !tableName || !partKey) return;
            let q = parseInt(qty, 10);
            if (isNaN(q) || q < 1) q = 1;
            if (q > 99) q = 99;
            const ord =
                orderMeta && orderMeta._miOrder != null && !isNaN(Number(orderMeta._miOrder))
                    ? Number(orderMeta._miOrder)
                    : null;
            const last = arr[arr.length - 1];
            if (last && last.tableName === tableName && last.partKey === partKey) {
                let sum = (parseInt(last.qty, 10) || 1) + q;
                if (sum > 99) sum = 99;
                last.qty = sum;
                if (ord != null) {
                    const lo = last._miOrder != null ? last._miOrder : ord;
                    last._miOrder = Math.min(lo, ord);
                }
            } else {
                const row = { tableName, partKey, qty: q };
                if (ord != null) row._miOrder = ord;
                arr.push(row);
            }
        }

        function mergeConsecutiveMiPartLines(lines) {
            if (!lines || !lines.length) return lines;
            const out = [];
            lines.forEach(entry => {
                if (!entry || !entry.partKey) return;
                miAppendLineEntryConsecutive(
                    out,
                    entry.tableName,
                    entry.partKey,
                    entry.qty != null ? entry.qty : 1,
                    entry._miOrder != null ? { _miOrder: entry._miOrder } : null
                );
            });
            return out;
        }

        /** Group identical (tableName, partKey) runs next to first occurrence so mergeConsecutive can sum qty. */
        function coalesceMiModdedLinesByFirstAppearance(lines) {
            if (!lines || !lines.length) return lines;
            const keyStr = (l) => String(l.tableName || '') + '\0' + String(l.partKey || '');
            const withIdx = lines.map((line, i) => ({ line, i }));
            const groupFirst = new Map();
            withIdx.forEach(({ line, i }) => {
                const k = keyStr(line);
                if (!groupFirst.has(k) || groupFirst.get(k) > i) {
                    groupFirst.set(k, i);
                }
            });
            const keysUnique = [];
            const seenK = new Set();
            withIdx.forEach(({ line }) => {
                const k = keyStr(line);
                if (!seenK.has(k)) {
                    seenK.add(k);
                    keysUnique.push(k);
                }
            });
            keysUnique.sort((a, b) => groupFirst.get(a) - groupFirst.get(b));
            const out = [];
            keysUnique.forEach((k) => {
                withIdx
                    .filter((x) => keyStr(x.line) === k)
                    .sort((a, b) => a.i - b.i)
                    .forEach((x) => out.push(x.line));
            });
            return out;
        }

        function miSlotAppendOrMergeLastRow(rowsWrap, encodedRef, addQty) {
            if (!rowsWrap || !encodedRef) return;
            let add = parseInt(addQty, 10);
            if (isNaN(add) || add < 1) add = 1;
            const chipHost = miPickedRowsChipHost(rowsWrap);
            const rows = (chipHost || rowsWrap).querySelectorAll('.mi-slot-picked-line');
            const last = rows[rows.length - 1];
            if (last && last.getAttribute('data-line-ref') === encodedRef) {
                const qIn = last.querySelector('.mi-picked-line-qty');
                let q = qIn ? parseInt(qIn.value, 10) : 1;
                if (isNaN(q) || q < 1) q = 1;
                q = Math.min(99, q + add);
                if (qIn) qIn.value = String(q);
            } else {
                const slot = rowsWrap.closest('.mi-compact-slot');
                const rk = slot && slot.getAttribute('data-root-key');
                miSlotAppendPickedLine(rowsWrap, encodedRef, add, rk);
            }
        }

        /** Find .root-item for modded compact controls (fallback via .mi-compact-slot). */
        function miClosestRootItem(el) {
            if (!el || typeof el.closest !== 'function') return null;
            const direct = el.closest('.root-item');
            if (direct) return direct;
            const slot = el.closest('.mi-compact-slot');
            return slot ? slot.closest('.root-item') : null;
        }

        /**
         * True once miPartLines has been synced for this root (including [] = all picked rows removed).
         * Do not fall back to selectedParts in that case — the Set can be stale vs DOM.
         */
        function miPartLinesHasSyncedRoot(inst, rootKey) {
            return !!(
                inst &&
                inst.relaxedPicker &&
                inst.miPartLines &&
                rootKey &&
                inst.miPartLines.has(rootKey)
            );
        }

        /** Token inners after `||` in human serial: `248`, `234:3`, etc. */
        function extractHumanSerialTokenInners(humanLine) {
            if (!humanLine || typeof humanLine !== 'string') return [];
            const idx = humanLine.indexOf('||');
            if (idx === -1) return [];
            const rest = humanLine.slice(idx + 2);
            const beforePipe = rest.split('|')[0] || '';
            const re = /\{([^}]+)\}/g;
            const out = [];
            let m;
            while ((m = re.exec(beforePipe)) !== null) {
                const inner = String(m[1] || '').trim();
                if (inner) out.push(inner);
            }
            return out;
        }

        /**
         * Expand packed token inners to multiple `rootSerial:sub` strings for resolveSerialTokenInnerToPartKey.
         * Supports `{234:[1 2 3]}`, `{234:[1,2,3]}`, and `234:1 2 3` / `234:1,2,3` when multiple subs are present.
         */
        function explodeModdedHumanSerialTokenInners(inners) {
            const out = [];
            (inners || []).forEach((inner) => {
                const s = String(inner || '').trim();
                if (!s) return;
                const mBracket = s.match(/^(\d+)\s*:\s*\[([^\]]+)\]\s*$/);
                if (mBracket) {
                    const root = mBracket[1];
                    const parts = mBracket[2]
                        .split(/[\s,]+/)
                        .map((x) => x.trim())
                        .filter((x) => /^\d+$/.test(x));
                    if (parts.length > 0) {
                        parts.forEach((p) => out.push(`${root}:${p}`));
                        return;
                    }
                }
                const mMulti = s.match(/^(\d+)\s*:\s*([\s\d,]+)$/);
                if (mMulti) {
                    const root = mMulti[1];
                    const subs = mMulti[2]
                        .split(/[\s,]+/)
                        .map((x) => x.trim())
                        .filter((x) => /^\d+$/.test(x));
                    if (subs.length > 1) {
                        subs.forEach((sub) => out.push(`${root}:${sub}`));
                        return;
                    }
                }
                out.push(s);
            });
            return out;
        }

        /** Resolve composition root from the header segment before the first `|` (numeric serial or quoted root key). */
        function parseCompositionRootFromHumanSerialLine(line, dataRef) {
            if (!line || typeof line !== 'string' || !dataRef || !dataRef.rootDefinitions) return null;
            const pipe = line.indexOf('|');
            if (pipe <= 0) return null;
            const header = line.slice(0, pipe).trim();
            if (header.startsWith('"')) {
                const end = header.indexOf('"', 1);
                if (end > 1) {
                    const key = header.slice(1, end);
                    return (
                        dataRef.rootDefinitions.find(
                            (r) => String(r.key || '').toLowerCase() === String(key).toLowerCase()
                        ) || null
                    );
                }
            }
            const first = header.split(',')[0].trim();
            const n = Number(first);
            if (!Number.isNaN(n)) {
                return (
                    dataRef.rootDefinitions.find((r) => Number(r.serialIndex) === n) || null
                );
            }
            return dataRef.rootDefinitions.find((r) => String(r.key || '').toLowerCase() === first.toLowerCase()) || null;
        }

        /** Apply edited #currentBuildHumanSerial (Legit tab): one line per build; tab away updates selected parts. */
        function applyLegitGlobalHumanSerialFromTextarea(textarea) {
            if (!textarea || textarea.id !== 'currentBuildHumanSerial') return;
            if (!data || !data.rootDefinitions) return;
            window.legitBuilderSetCtx(0);
            window.__lbCtx = window.legitBuilderInstances && window.legitBuilderInstances[0];
            const placeholder = '— Select parts to see the human-readable serial —';
            const lines = String(textarea.value || '')
                .split(/\r?\n/)
                .map((s) => s.trim())
                .filter((s) => s.length > 0 && s !== placeholder);
            if (!lines.length) return;
            if (textMightReferencePack4ClassMod(lines.join('\n'))) {
                void ensurePack4NexusLoaded();
            }
            const byRoot = new Map();
            for (let li = 0; li < lines.length; li++) {
                const line = lines[li];
                const compRoot = parseCompositionRootFromHumanSerialLine(line, data);
                if (!compRoot || compRoot.serialIndex == null) continue;
                const compSer = Number(compRoot.serialIndex);
                if (Number.isNaN(compSer)) continue;
                const inners = extractHumanSerialTokenInners(line);
                const keySet = new Set();
                for (let ti = 0; ti < inners.length; ti++) {
                    const pk = resolveSerialTokenInnerToPartKey(inners[ti], compSer, data);
                    if (pk) keySet.add(pk);
                }
                byRoot.set(compRoot.key, keySet);
            }
            if (byRoot.size === 0) return;
            const toDelete = [];
            _sp().forEach((_, rk) => {
                if (!byRoot.has(rk)) toDelete.push(rk);
            });
            toDelete.forEach((rk) => _sp().delete(rk));
            byRoot.forEach((set, rk) => _sp().set(rk, set));
            updateSelectedPartsOutput();
            updateFinalOutput();
            displayRootDefinitions();
        }

        /** Map `{sub}` or `{rootSerial:sub}` to part key / __miX ref for the composition root. */
        function resolveSerialTokenInnerToPartKey(inner, compositionRootSerial, dataRef) {
            if (!inner || dataRef == null || dataRef.rootDefinitions == null) return '';
            const parts = String(inner).split(':');
            let srcSerial = Number(compositionRootSerial);
            let subStr = String(inner).trim();
            if (parts.length === 2) {
                srcSerial = Number(parts[0]);
                subStr = parts[1].trim();
            }
            const sub = Number(subStr);
            if (Number.isNaN(srcSerial) || Number.isNaN(sub)) return '';
            const srcRoot = dataRef.rootDefinitions.find((r) => Number(r.serialIndex) === srcSerial);
            if (!srcRoot || !srcRoot.depEntries) return '';
            const dep = srcRoot.depEntries.find((d) => {
                const si = d.serialIndex != null ? Number(d.serialIndex) : Number(d.serialindex?.index);
                return si === sub;
            });
            if (!dep || !dep.key) return '';
            const compSer = Number(compositionRootSerial);
            if (!Number.isNaN(compSer) && Number(srcRoot.serialIndex) !== compSer) {
                return makeMiCrossPartRef(srcRoot.key, dep.key);
            }
            return dep.key;
        }

        /** Merge tier_1…tier_5(+5…) runs back into one `_tier_1` line + qty (matches expandModdedMiLineToPartKeys). */
        function collapseConsecutivePassiveTierMiLines(lines) {
            if (!lines || !lines.length) return lines;
            const out = [];
            let i = 0;
            while (i < lines.length) {
                const cur = lines[i];
                const cross = parseMiCrossPartRef(cur.partKey);
                const plain = cross ? cross.partKey : cur.partKey;
                const m0 = parsePassivePointsTierKey(plain);
                if (!m0 || m0.tier !== 1) {
                    out.push(cur);
                    i++;
                    continue;
                }
                const base = m0.chainBase;
                const sr = cross ? cross.sourceRootKey : null;
                const tier1Ref = sr ? makeMiCrossPartRef(sr, `${base}_tier_1`) : `${base}_tier_1`;
                let pos = 0;
                let j = i;
                while (j < lines.length) {
                    const L = lines[j];
                    const cj = parseMiCrossPartRef(L.partKey);
                    const pj = cj ? cj.partKey : L.partKey;
                    const mj = parsePassivePointsTierKey(pj);
                    if (!mj || String(mj.chainBase).toLowerCase() !== String(base).toLowerCase()) break;
                    const cjSr = cj ? cj.sourceRootKey : null;
                    if ((cjSr || null) !== (sr || null)) break;
                    const wantTier = pos < 5 ? pos + 1 : 5;
                    if (mj.tier !== wantTier) break;
                    j++;
                    pos++;
                }
                if (pos === 0) {
                    out.push(cur);
                    i++;
                    continue;
                }
                const tierMerge = {
                    tableName: cur.tableName,
                    partKey: tier1Ref,
                    qty: Math.min(99, Math.max(1, pos))
                };
                if (cur._miOrder != null) tierMerge._miOrder = cur._miOrder;
                out.push(tierMerge);
                i = j;
            }
            return out;
        }

        /** Write mi lines back to each compact slot’s picked rows (same slot order as syncMiPartLinesFromRootItem). */
        function findMiModdedRootItemEl(rootKey) {
            const miRc = document.getElementById('mi_rootsContent');
            if (!miRc || !rootKey) return null;
            const rk = String(rootKey).trim();
            const host = document.getElementById('mi_simpleBuilderHost');
            if (host && host.style.display !== 'none') {
                const inHost = host.querySelector('.root-item[data-root-key]');
                if (inHost && String(inHost.getAttribute('data-root-key') || '').trim() === rk) {
                    return inHost;
                }
            }
            return (
                Array.from(miRc.querySelectorAll('.root-item[data-root-key]')).find(function (ri) {
                    return String(ri.getAttribute('data-root-key') || '').trim() === rk;
                }) || null
            );
        }

        function applyMiLinesToModdedRootDom(rootKey, lines) {
            if (!rootKey || !lines || !data) return;
            const rootItem = findMiModdedRootItemEl(rootKey);
            if (!rootItem) return;
            /** tableNameNorm → FIFO lines (same as reorder buckets; avoids queue/ DOM walk order drift + duplicate rows on sync). */
            const byNorm = new Map();
            lines.forEach((line) => {
                const k = normalizeDepTableName(line.tableName || '') || 'unknown';
                if (!byNorm.has(k)) byNorm.set(k, []);
                byNorm.get(k).push(line);
            });
            rootItem.querySelectorAll('.mi-compact-slot').forEach((slot) => {
                if (slot.getAttribute('data-root-key') !== rootKey) return;
                const tn = slot.getAttribute('data-table-name');
                if (!tn) return;
                const isPicker =
                    tn === 'inv_comp' ||
                    tn === MI_CROSS_TABLE ||
                    tn === MI_DOES_NOT_EXIST_TABLE ||
                    slot.classList.contains('mi-slot-picker-slot');
                if (!isPicker) return;
                const slotNorm = normalizeDepTableName(tn);
                const bucket = byNorm.get(slotNorm);
                const slotLines = bucket && bucket.length ? bucket.splice(0, bucket.length) : [];
                if (!slotLines.length) {
                    miRestorePartSlotEmptyState(slot);
                    return;
                }
                const rowsWrap = miEnsurePartSlotPickedRows(slot);
                if (!rowsWrap) return;
                const chipHost = miPickedRowsChipHost(rowsWrap);
                (chipHost || rowsWrap).innerHTML = slotLines
                    .map((sl) => miSlotBuildPickedLineHtml(sl.partKey, sl.qty != null ? sl.qty : 1, rootKey, tn))
                    .join('');
            });
            miRefreshPartSlotPickedListUi(rootItem);
            const dneNorm = normalizeDepTableName(MI_DOES_NOT_EXIST_TABLE);
            const inputHasDne = lines.some(
                (l) => normalizeDepTableName(l.tableName || '') === dneNorm
            );
            const depTablesEl = rootItem.querySelector('.mi-part-grid') || rootItem.querySelector('.mi-compact-dep-tables');
            const leftoverDne = byNorm.get(dneNorm);
            if (depTablesEl && leftoverDne && leftoverDne.length) {
                const existingDne = rootItem.querySelector('.mi-part-slot-card--dne');
                if (!existingDne) {
                    depTablesEl.insertAdjacentHTML(
                        'beforeend',
                        buildMiDoesNotExistPartSlotCard(rootKey, leftoverDne)
                    );
                }
            }
            if (!inputHasDne) {
                const dneCard = rootItem.querySelector('.mi-part-slot-card--dne');
                if (dneCard) dneCard.remove();
            }
            miInitOpenModdedPickers(rootItem);
        }

        /** Parse modded per-root human textarea and push into miPartLines + slot DOM + outputs. */
        function applyModdedHumanSerialFromTextarea(textarea) {
            if (!textarea || !data || !data.rootDefinitions) return;
            const rootKey = textarea.getAttribute('data-root-key');
            if (!rootKey) return;
            const inst = window.legitBuilderInstances && window.legitBuilderInstances[1];
            if (!inst || !inst.relaxedPicker) return;
            const raw = String(textarea.value || '').trim();
            const placeholder = '— Select parts to see the human-readable serial —';
            if (!raw || raw === placeholder) return;
            if (textMightReferencePack4ClassMod(raw)) {
                void ensurePack4NexusLoaded();
            }
            const compRoot = data.rootDefinitions.find((r) => r.key === rootKey);
            if (!compRoot) return;
            const compSer = Number(compRoot.serialIndex);
            if (Number.isNaN(compSer)) return;
            const inners = explodeModdedHumanSerialTokenInners(extractHumanSerialTokenInners(raw));
            if (!inners.length) return;
            const baseGenericRoot = getBaseGenericRootForPartLookup(compRoot, data);
            const flat = [];
            for (let ti = 0; ti < inners.length; ti++) {
                const pk = resolveSerialTokenInnerToPartKey(inners[ti], compSer, data);
                if (!pk) {
                    const rawTok = `{${inners[ti]}}`;
                    const missRef = makeMiMissingPartRef(rawTok);
                    if (missRef) {
                        flat.push({ tableName: MI_DOES_NOT_EXIST_TABLE, partKey: missRef, qty: 1 });
                    }
                    continue;
                }
                const partEntry = resolvePartEntryForSerialOutput(compRoot, pk, baseGenericRoot, data);
                let tname = (partEntry && partEntry.depTableName) || 'Unknown';
                const crossPr = parseMiCrossPartRef(pk);
                const sourceRootForRoute =
                    crossPr && crossPr.sourceRootKey ? crossPr.sourceRootKey : compRoot.key;
                tname = moddedTableNameNativeOrCross(tname, sourceRootForRoute, compRoot);
                tname = moddedResolveMiTableNameAfterCrossCheck(compRoot, tname, pk, data);
                flat.push({ tableName: tname, partKey: pk, qty: 1 });
            }
            if (!flat.length) return;
            let collapsed = collapseConsecutivePassiveTierMiLines(flat);
            collapsed = mergeConsecutiveMiPartLines(collapsed);
            collapsed = coalesceMiModdedLinesByFirstAppearance(collapsed);
            collapsed = mergeConsecutiveMiPartLines(collapsed);
            collapsed = reorderMiPartLinesForCompactDom(compRoot, collapsed);
            collapsed = mergeConsecutiveMiPartLines(collapsed);
            collapsed = dedupeModdedCrossLinesRedundantWithNative(compRoot, collapsed, data);
            collapsed = mergeConsecutiveMiPartLines(collapsed);
            inst.miPartLines.set(rootKey, collapsed);
            const set = new Set();
            collapsed.forEach((l) => {
                if (!l.partKey) return;
                const pr = parseMiCrossPartRef(l.partKey);
                set.add(pr ? pr.partKey : l.partKey);
            });
            inst.selectedParts.set(rootKey, set);
            window.__lbCtx = inst;
            applyMiLinesToModdedRootDom(rootKey, collapsed);
            const ri = textarea.closest('.root-item');
            syncMiPartLinesFromRootItem(miClosestRootItem(ri) || ri);
            updateSelectedPartsOutput();
            updateFinalOutput();
        }

        /** Refresh modded instance miPartLines from every class/item card under #mi_rootsContent (DOM is source of truth). */
        function syncAllMiPartLinesFromModdedDom() {
            const inst = window.legitBuilderInstances && window.legitBuilderInstances[1];
            if (!inst || !inst.relaxedPicker) return;
            const miRc = document.getElementById('mi_rootsContent');
            if (!miRc) return;
            miRc.querySelectorAll('.root-item').forEach((ri) => {
                syncMiPartLinesFromRootItem(miClosestRootItem(ri) || ri);
            });
        }

        /**
         * Modded compact UI: read per-slot lines + qty from DOM into instance.miPartLines and refresh selectedParts Set.
         */
        function syncMiPartLinesFromRootItem(rootItem) {
            const inst = window.legitBuilderInstances && window.legitBuilderInstances[1];
            if (!inst || !inst.relaxedPicker || !rootItem) return;
            let rootKey = rootItem.getAttribute('data-root-key');
            if (!rootKey || !String(rootKey).trim()) {
                const firstSlot = rootItem.querySelector('.mi-compact-slot[data-root-key]');
                rootKey = firstSlot && firstSlot.getAttribute('data-root-key');
            }
            if (!rootKey || !String(rootKey).trim()) {
                const tel = rootItem.querySelector('.lb-current-build-human-serial-in-root');
                rootKey = tel && tel.getAttribute('data-root-key');
            }
            rootKey = String(rootKey || '').trim();
            if (!rootKey) return;
            const lines = [];
            rootItem.querySelectorAll('.mi-compact-slot').forEach(slot => {
                if (slot.getAttribute('data-root-key') !== rootKey) return;
                const tableName = slot.getAttribute('data-table-name');
                if (!tableName) return;
                if (
                    tableName === 'inv_comp' ||
                    tableName === MI_CROSS_TABLE ||
                    tableName === MI_DOES_NOT_EXIST_TABLE ||
                    slot.classList.contains('mi-slot-picker-slot')
                ) {
                    const rowsWrap = miResolvePartSlotPickedRowsWrap(slot);
                    const lineRoot = miResolvePartSlotPickedLineRoot(slot);
                    const rawLines = [];
                    lineRoot.querySelectorAll('.mi-slot-picked-line').forEach(line => {
                        const ref = line.getAttribute('data-line-ref');
                        if (!ref) return;
                        const qIn = line.querySelector('.mi-picked-line-qty');
                        let qty = qIn ? parseInt(qIn.value, 10) : 1;
                        if (isNaN(qty) || qty < 1) qty = 1;
                        if (qty > 99) qty = 99;
                        if (tableName === 'inv_comp') qty = 1;
                        rawLines.push({ ref, qty });
                    });
                    const slotLines = [];
                    rawLines.forEach(({ ref, qty }) => {
                        miAppendLineEntryConsecutive(slotLines, tableName, ref, qty, null);
                    });
                    slotLines.forEach(sl => lines.push(sl));
                    if (
                        rowsWrap &&
                        tableName !== 'inv_comp' &&
                        slotLines.length > 0 &&
                        slotLines.length < rawLines.length
                    ) {
                        const chipHost = miPickedRowsChipHost(rowsWrap);
                        (chipHost || rowsWrap).innerHTML = slotLines
                            .map(sl => miSlotBuildPickedLineHtml(sl.partKey, sl.qty, rootKey, tableName))
                            .join('');
                    }
                    return;
                }
                const slotLegacy = [];
                slot.querySelectorAll('.mi-compact-line').forEach(line => {
                    const sel = line.querySelector('.mi-part-slot-line-select');
                    const qIn = line.querySelector('.mi-compact-line-qty');
                    const pk = sel && sel.value ? String(sel.value).trim() : '';
                    if (!pk) return;
                    let qty = qIn ? parseInt(qIn.value, 10) : 1;
                    if (isNaN(qty) || qty < 1) qty = 1;
                    if (qty > 99) qty = 99;
                    miAppendLineEntryConsecutive(slotLegacy, tableName, pk, qty);
                });
                slotLegacy.forEach(sl => lines.push(sl));
            });
            if (!inst.miPartLines) inst.miPartLines = new Map();
            const compRoot =
                data && data.rootDefinitions ? data.rootDefinitions.find((r) => r.key === rootKey) : null;
            let afterDedupe = lines;
            if (compRoot && data) {
                afterDedupe = dedupeModdedCrossLinesRedundantWithNative(compRoot, lines, data);
            }
            const linesOut = mergeConsecutiveMiPartLines(afterDedupe);
            inst.miPartLines.set(rootKey, linesOut);
            const set = new Set();
            linesOut.forEach((l) => {
                if (!l.partKey) return;
                const pr = parseMiCrossPartRef(l.partKey);
                set.add(pr ? pr.partKey : l.partKey);
            });
            inst.selectedParts.set(rootKey, set);
            window.__lbCtx = inst;
            applyMiLinesToModdedRootDom(rootKey, linesOut);
            miRefreshPartSlotPickedListUi(rootItem);
        }

        function miEncodeSerialOrderLineB64(line) {
            if (!line || !line.partKey) return '';
            try {
                return btoa(
                    unescape(
                        encodeURIComponent(
                            JSON.stringify({
                                tableName: line.tableName || '',
                                partKey: line.partKey,
                                qty: line.qty != null ? line.qty : 1
                            })
                        )
                    )
                );
            } catch (e) {
                return '';
            }
        }

        function miDecodeSerialOrderLineB64(b64) {
            if (!b64) return null;
            try {
                const json = decodeURIComponent(escape(atob(String(b64))));
                const o = JSON.parse(json);
                if (o && o.partKey) {
                    return {
                        tableName: o.tableName || 'Unknown',
                        partKey: o.partKey,
                        qty: o.qty != null ? o.qty : 1
                    };
                }
            } catch (e) {}
            return null;
        }

        function applyMiSerialOrderFromList(listEl) {
            if (!listEl) return;
            const rootKey = listEl.getAttribute('data-root-key');
            if (!rootKey) return;
            const inst = window.legitBuilderInstances && window.legitBuilderInstances[1];
            if (!inst || !inst.miPartLines) return;
            const lines = [];
            listEl.querySelectorAll('.mi-serial-order-item').forEach(row => {
                const b64 = row.getAttribute('data-line-b64');
                const o = miDecodeSerialOrderLineB64(b64);
                if (o) lines.push(o);
            });
            if (!lines.length) return;
            inst.miPartLines.set(rootKey, lines);
            const set = new Set();
            lines.forEach(l => {
                if (!l.partKey) return;
                const pr = parseMiCrossPartRef(l.partKey);
                set.add(pr ? pr.partKey : l.partKey);
            });
            inst.selectedParts.set(rootKey, set);
            window.__lbCtx = inst;
            updateSelectedPartsOutput();
        }

        /** Modded builder: hide turret/terminal roots and part slots unless application mode is advanced. */
        function miShouldHideTurretTerminalForModded() {
            if (!(window.__lbCtx && window.__lbCtx.relaxedPicker)) {
                return false;
            }
            const viewMode =
                applicationMode ||
                (typeof localStorage !== 'undefined' && localStorage.getItem('rootDefinitionsViewMode')) ||
                'simple';
            return viewMode !== 'advanced';
        }

        function shouldHideEndgameSlotOutsideAdvanced() {
            const viewMode =
                applicationMode ||
                (typeof localStorage !== 'undefined' && localStorage.getItem('rootDefinitionsViewMode')) ||
                'simple';
            return viewMode !== 'advanced';
        }

        function miShouldHideEndgameForModded() {
            if (!(window.__lbCtx && window.__lbCtx.relaxedPicker)) {
                return false;
            }
            return shouldHideEndgameSlotOutsideAdvanced();
        }

        function miIsTurretOrTerminalGadgetRoot(root) {
            if (!root) {
                return false;
            }
            const key = (root.key || '').toLowerCase();
            const inv = (root.inv || '').toLowerCase();
            const basetype = (root.basetype || '').toLowerCase();
            if (key.includes('turret_gadget') || inv.includes('turret_gadget')) {
                return true;
            }
            if (key.includes('terminal_gadget') || inv.includes('terminal_gadget')) {
                return true;
            }
            if (
                (basetype.includes('turret') || key.includes('turret') || inv.includes('turret')) &&
                key !== 'turret_gadget' &&
                inv !== 'turret_gadget'
            ) {
                return true;
            }
            if (
                (basetype.includes('terminal') || key.includes('terminal') || inv.includes('terminal')) &&
                key !== 'terminal_gadget' &&
                inv !== 'terminal_gadget'
            ) {
                return true;
            }
            return false;
        }

        function miIsTurretOrTerminalPartSlot(tableName) {
            const tn = normalizeDepTableName(String(tableName || '')).toLowerCase();
            if (!tn) {
                return false;
            }
            return tn.indexOf('turret') !== -1 || tn.indexOf('terminal') !== -1;
        }

        function miIsTurretOrTerminalSourceRootKey(sourceRootKey, dataRef) {
            if (!sourceRootKey || !dataRef || !dataRef.rootDefinitions) return false;
            const rk = String(sourceRootKey).trim();
            const root = dataRef.rootDefinitions.find(function (r) {
                return r && String(r.key || '') === rk;
            });
            if (root) return miIsTurretOrTerminalGadgetRoot(root);
            const key = rk.toLowerCase();
            return (
                key.includes('terminal_gadget') ||
                key.includes('turret_gadget') ||
                ((key.includes('terminal') || key.includes('turret')) &&
                    !key.includes('invdef') &&
                    key !== 'mission_pickup')
            );
        }

        /** Cross-manufacturer catalog rows from turret/terminal roots (hidden in modded simple mode). */
        function miFilterCrossCatalogItemsForViewMode(items, dataRef) {
            if (!items || !items.length) return items || [];
            if (!miShouldHideTurretTerminalForModded()) return items;
            return items.filter(function (it) {
                return !miIsTurretOrTerminalSourceRootKey(it.sourceRootKey, dataRef);
            });
        }

        function miFilteredGlobalCatalogTypeList(catalog, dataRef) {
            if (!catalog || !catalog.types) return [];
            if (!miShouldHideTurretTerminalForModded()) return catalog.types.slice();
            return catalog.types.filter(function (t) {
                return miFilterCrossCatalogItemsForViewMode(catalog.byType.get(t) || [], dataRef).length > 0;
            });
        }

        var MI_PART_TYPE_LABELS = {
            inv_comp: 'Inventory Comp',
            body: 'Body',
            body_acc: 'Body Accessories',
            body_ele: 'Body Element',
            barrel: 'Barrel',
            barrel_acc: 'Barrel Accessories',
            barrel_licensed: 'Licensed Barrel',
            hyperion_secondary_acc: 'Secondary Accessories',
            magazine: 'Magazine',
            magazine_acc: 'Magazine Accessories',
            magazine_ted_thrown: 'Thrown Magazine',
            scope: 'Scope',
            scope_acc: 'Scope Accessories',
            grip: 'Grip',
            foregrip: 'Foregrip',
            underbarrel: 'Underbarrel',
            secondary_ammo: 'Secondary Ammo',
            secondary_ele: 'Secondary Element',
            pearl_elem: 'Pearl Elem',
            pearl_stat: 'Pearl Stat',
            element: 'Element',
            primary_augment: 'Primary Augment',
            secondary_augment: 'Secondary Augment',
            firmware: 'Firmware',
            endgame: 'Endgame',
            unique: 'Unique',
            payload: 'Payload',
            passive_points: 'Passive Points',
            stat_group1: 'Stat Group 1',
            stat_group2: 'Stat Group 2',
            stat_group3: 'Stat Group 3',
            special_passive: 'Special Passive',
            core_augment: 'Core Augment',
            augment_element_resist: 'Elemental Resistances',
            augment_element_immunity: 'Elemental Immunities',
            augment_element_nova: 'Elemental Novas',
            augment_element_splat: 'Elemental Splats'
        };

        /** Plain-language hint shown in parentheses next to slot titles (modded builder). */
        var MI_PART_TYPE_DUMMY_TAGS = {
            inv_comp: 'Rarity',
            body: 'Required Gun Mid Section',
            body_acc: 'Body Mods',
            body_ele: 'Primary Element',
            barrel: 'Legendary Perk',
            barrel_acc: 'Barrel Mods',
            barrel_licensed: 'Licensed Barrel',
            magazine: 'Ammo / Mag Type',
            magazine_acc: 'Magazine Mods',
            scope: 'Optic',
            scope_acc: 'Scope Mods',
            grip: 'Grip Mod',
            foregrip: 'Foregrip Mod',
            underbarrel: 'Underbarrel',
            secondary_ele: 'Secondary Element',
            pearl_elem: 'Pearl Element',
            pearl_stat: 'Pearl Stat',
            element: 'Element',
            firmware: 'Firmware',
            payload: 'Payload',
            primary_augment: 'Primary Augment',
            secondary_augment: 'Secondary Augment',
            stat_augment: 'Stat Augment',
            payload_augment: 'Payload Augment',
            passive_points: 'Passive Points',
            unique: 'Unique Part'
        };

        function getMiPartTypeLabel(tableName) {
            const n = normalizeDepTableName(String(tableName || ''));
            if (!n) return 'Part';
            if (MI_PART_TYPE_LABELS[n]) return MI_PART_TYPE_LABELS[n];
            if (n === MI_CROSS_TABLE) return 'Cross-manufacturer';
            if (n === MI_DOES_NOT_EXIST_TABLE) return 'Not in dataset';
            return String(n)
                .split('_')
                .map(function (w) {
                    return w ? w.charAt(0).toUpperCase() + w.slice(1) : '';
                })
                .join(' ');
        }

        /** Slot title for modded UI: short name plus optional dummy tag in parentheses. */
        function getMiPartTypeDisplayLabel(tableName) {
            const n = normalizeDepTableName(String(tableName || ''));
            const base = getMiPartTypeLabel(tableName);
            const tag = MI_PART_TYPE_DUMMY_TAGS[n];
            return tag ? base + ' (' + tag + ')' : base;
        }

        function miPartSlotStatusClass(requiredMin, pickedCount) {
            const req = Number(requiredMin) || 0;
            const picked = Number(pickedCount) || 0;
            if (req > 0 && picked === 0) return 'mi-part-slot-status--missing';
            if (picked > 0 && (req === 0 || picked >= req)) return 'mi-part-slot-status--ok';
            return 'mi-part-slot-status--neutral';
        }

        function miGetPartSlotControlsHost(slot) {
            if (!slot) return null;
            return slot.closest('.mi-part-slot-controls') || slot.parentElement;
        }

        /** Picked rows live in .mi-part-slot-controls (sibling of .mi-compact-slot), not inside the slot. */
        function miResolvePartSlotPickedRowsWrap(slot) {
            if (!slot) return null;
            const host = miGetPartSlotControlsHost(slot);
            const scopes = host ? [slot, host] : [slot];
            for (let i = 0; i < scopes.length; i++) {
                const scope = scopes[i];
                if (!scope) continue;
                const rowsWrap =
                    scope.querySelector('.mi-part-slot-picked-list .mi-slot-picked-rows') ||
                    scope.querySelector('.mi-part-slot-picked-list .mi-cross-slot-rows') ||
                    scope.querySelector('.mi-part-slot-picked-list .mi-compact-slot-rows') ||
                    scope.querySelector('.mi-cross-slot-rows') ||
                    scope.querySelector('.mi-slot-picked-rows');
                if (rowsWrap) return rowsWrap;
            }
            return null;
        }

        function miResolvePartSlotPickedLineRoot(slot) {
            const rowsWrap = miResolvePartSlotPickedRowsWrap(slot);
            if (rowsWrap) {
                return miPickedRowsChipHost(rowsWrap) || rowsWrap;
            }
            return miGetPartSlotControlsHost(slot) || slot;
        }

        function miEnsurePartSlotPickedRows(slot) {
            if (!slot) return null;
            const existing = miResolvePartSlotPickedRowsWrap(slot);
            if (existing) return existing;
            const host = miGetPartSlotControlsHost(slot);
            if (!host) return null;
            const emptyEl = host.querySelector('.mi-part-slot-empty');
            if (emptyEl) emptyEl.remove();
            const emptyHint = host.querySelector('.selected-parts-empty');
            if (emptyHint) emptyHint.remove();
            const isCross = slot.classList.contains('mi-cross-root-slot');
            const rowsClass = isCross
                ? 'mi-compact-slot-rows mi-cross-slot-rows'
                : 'mi-compact-slot-rows mi-slot-picked-rows';
            host.insertAdjacentHTML(
                'beforeend',
                '<div class="mi-part-slot-selected selected-parts"><div class="mi-part-slot-picked-list selected-parts-list"><div class="' +
                    rowsClass +
                    '"></div></div></div>'
            );
            return miResolvePartSlotPickedRowsWrap(slot);
        }

        function miRestorePartSlotEmptyState(slot) {
            if (!slot) return;
            const controls = slot.closest('.mi-part-slot-controls');
            const host = controls || slot.parentElement;
            if (!host) return;
            const selected = host.querySelector('.mi-part-slot-selected');
            if (selected) {
                selected.remove();
            } else {
                const list = host.querySelector('.mi-part-slot-picked-list');
                if (list) list.remove();
            }
            if (!host.querySelector('.selected-parts-empty')) {
                host.insertAdjacentHTML(
                    'beforeend',
                    '<p class="mi-part-slot-empty-hint selected-parts-empty">No parts selected</p>'
                );
            }
        }

        function miRefreshPartSlotPickedListUi(rootEl) {
            const scope =
                rootEl && rootEl.querySelectorAll
                    ? rootEl
                    : document.getElementById('mi_rootsContent') || document;
            scope.querySelectorAll('.mi-part-slot-card').forEach(function (card) {
                const list = card.querySelector('.mi-part-slot-picked-list');
                const lines = card.querySelectorAll('.mi-slot-picked-line');
                const count = lines.length;
                if (list) {
                    list.classList.toggle('scrollable', count > 10);
                    list.classList.remove('has-many');
                }
                const controls = card.querySelector('.mi-part-slot-controls');
                let emptyHint =
                    controls && controls.querySelector('.selected-parts-empty');
                if (count === 0) {
                    if (!emptyHint && controls) {
                        controls.insertAdjacentHTML(
                            'beforeend',
                            '<p class="mi-part-slot-empty-hint selected-parts-empty">No parts selected</p>'
                        );
                    }
                } else if (emptyHint) {
                    emptyHint.remove();
                }
                const clearBtn = card.querySelector('.mi-slot-clear-category');
                if (clearBtn) {
                    clearBtn.style.display = count > 0 ? '' : 'none';
                }
            });
        }

        function miGetPartSlotSelectValues(sel) {
            if (!sel) return [];
            if (sel.multiple) {
                return Array.from(sel.selectedOptions || [])
                    .map(function (o) {
                        return o.value;
                    })
                    .filter(Boolean);
            }
            return sel.value ? [String(sel.value)] : [];
        }

        function miClearPartSlotSelect(sel) {
            if (!sel) return;
            if (sel.multiple) {
                Array.from(sel.options).forEach(function (o) {
                    o.selected = false;
                });
            } else {
                sel.selectedIndex = 0;
            }
            const wrap = sel.closest('.mi-part-slot-multidrop');
            if (wrap) {
                wrap.querySelectorAll('.mi-part-slot-multidrop-opt input[type="checkbox"]').forEach(function (cb) {
                    cb.checked = false;
                });
                miPartSlotMultidropUpdateTrigger(wrap);
            }
        }

        function miResolvePartSlotSelect(slot) {
            if (!slot) return null;
            const wrap = slot.querySelector('.mi-part-slot-multidrop');
            if (wrap) {
                return wrap.querySelector('select.mi-part-slot-select');
            }
            return (
                slot.querySelector('select.mi-part-slot-select:not(.mi-cross-part-select)') ||
                slot.querySelector('select.mi-part-slot-select')
            );
        }

        function miPartSlotMultidropUpdateTrigger(wrap) {
            if (!wrap) return;
            const sel = wrap.querySelector('select.mi-part-slot-select');
            const trigger = wrap.querySelector('.mi-part-slot-multidrop-trigger');
            if (!sel || !trigger) return;
            const picked = Array.from(sel.selectedOptions || []).filter(function (o) {
                return o.value;
            });
            if (!picked.length) {
                trigger.textContent = 'Select part(s)…';
                return;
            }
            if (picked.length === 1) {
                trigger.textContent = String(picked[0].textContent || picked[0].value).trim();
                return;
            }
            trigger.textContent = picked.length + ' parts selected';
        }

        function miPartSlotMultidropRefreshList(slot) {
            const wrap = slot && slot.querySelector('.mi-part-slot-multidrop');
            if (!wrap) return;
            const sel = wrap.querySelector('select.mi-part-slot-select');
            const list = wrap.querySelector('.mi-part-slot-multidrop-list');
            if (!sel || !list) return;
            let html = '';
            Array.from(sel.options).forEach(function (o) {
                if (!o.value) return;
                const ve = String(o.value)
                    .replace(/&/g, '&amp;')
                    .replace(/"/g, '&quot;');
                const checked = o.selected ? ' checked' : '';
                html +=
                    '<label class="mi-part-slot-multidrop-opt">' +
                    '<input type="checkbox" data-ms-value="' +
                    ve +
                    '"' +
                    checked +
                    ' />' +
                    '<span>' +
                    escapeLegitHtml(String(o.textContent || o.value)) +
                    '</span></label>';
            });
            list.innerHTML = html || '<p class="mi-part-slot-multidrop-empty">No parts available</p>';
            miPartSlotMultidropUpdateTrigger(wrap);
        }

        function miPartSlotMultidropSyncSelectFromList(wrap) {
            if (!wrap) return;
            const sel = wrap.querySelector('select.mi-part-slot-select');
            if (!sel) return;
            const vals = new Set();
            wrap.querySelectorAll('.mi-part-slot-multidrop-opt input[type="checkbox"]').forEach(function (cb) {
                if (cb.checked) {
                    const v = cb.getAttribute('data-ms-value');
                    if (v) vals.add(v);
                }
            });
            Array.from(sel.options).forEach(function (o) {
                o.selected = vals.has(o.value);
            });
            miPartSlotMultidropUpdateTrigger(wrap);
        }

        function miClearPartSlotMultidropPanelLayout(pan) {
            if (!pan || !pan.style) return;
            pan.classList.remove('mi-part-slot-multidrop-panel--fixed');
            pan.style.position = '';
            pan.style.left = '';
            pan.style.top = '';
            pan.style.width = '';
            pan.style.maxWidth = '';
            pan.style.maxHeight = '';
            pan.style.height = '';
            pan.style.minHeight = '';
            pan.style.overflow = '';
            pan.style.overflowX = '';
            pan.style.zIndex = '';
            pan.style.boxSizing = '';
            const list = pan.querySelector('.mi-part-slot-multidrop-list');
            if (list && list.style) {
                list.style.maxHeight = '';
                list.style.overflowY = '';
                list.style.overflowX = '';
            }
        }

        function miPositionPartSlotMultidropPanel(wrap) {
            if (!wrap || !wrap.classList.contains('mi-part-slot-multidrop--open')) return;
            const tr = wrap.querySelector('.mi-part-slot-multidrop-trigger');
            const pan = wrap.querySelector('.mi-part-slot-multidrop-panel');
            if (!tr || !pan || pan.hidden) return;
            const r = tr.getBoundingClientRect();
            const margin = 4;
            const cap = Math.min(320, window.innerHeight * 0.5);
            const spaceBelow = window.innerHeight - r.bottom - margin;
            const spaceAbove = r.top - margin;
            let topV = r.bottom + margin;
            let maxHeight = Math.min(cap, Math.max(0, spaceBelow - margin));
            if (maxHeight < 120 && spaceAbove > spaceBelow) {
                maxHeight = Math.min(cap, Math.max(0, spaceAbove - margin));
                topV = Math.max(margin, r.top - maxHeight - margin);
            }
            maxHeight = Math.max(100, maxHeight);
            let leftV = r.left;
            let width = r.width;
            const pad = 8;
            if (leftV + width > window.innerWidth - pad) {
                leftV = Math.max(pad, window.innerWidth - width - pad);
            }
            if (leftV < pad) {
                width = Math.max(200, Math.min(width, window.innerWidth - pad * 2));
                leftV = pad;
            }
            const fcb = miGetFixedContainingBlock(pan);
            let left = leftV;
            let top = topV;
            if (fcb) {
                const cbr = fcb.getBoundingClientRect();
                left = leftV - cbr.left;
                top = topV - cbr.top;
            }
            const list = pan.querySelector('.mi-part-slot-multidrop-list');
            pan.classList.add('mi-part-slot-multidrop-panel--fixed');
            pan.style.position = 'fixed';
            pan.style.left = left + 'px';
            pan.style.top = top + 'px';
            pan.style.width = width + 'px';
            pan.style.maxWidth = width + 'px';
            pan.style.maxHeight = '';
            pan.style.height = 'auto';
            pan.style.minHeight = '0';
            pan.style.overflow = 'hidden';
            pan.style.overflowX = 'hidden';
            pan.style.zIndex = '10060';
            pan.style.boxSizing = 'border-box';
            if (list) {
                list.style.maxHeight = maxHeight + 'px';
                list.style.overflowY = 'auto';
                list.style.overflowX = 'hidden';
            }
        }

        function miClosePartSlotMultidrop(wrap) {
            if (!wrap) return;
            wrap.classList.remove('mi-part-slot-multidrop--open');
            const tr = wrap.querySelector('.mi-part-slot-multidrop-trigger');
            const pan = wrap.querySelector('.mi-part-slot-multidrop-panel');
            if (tr) tr.setAttribute('aria-expanded', 'false');
            if (pan) {
                miClearPartSlotMultidropPanelLayout(pan);
                pan.hidden = true;
            }
        }

        function miOpenPartSlotMultidrop(wrap) {
            if (!wrap) return;
            const slot = wrap.closest('.mi-compact-slot');
            if (slot) miPartSlotMultidropRefreshList(slot);
            wrap.classList.add('mi-part-slot-multidrop--open');
            const tr = wrap.querySelector('.mi-part-slot-multidrop-trigger');
            const pan = wrap.querySelector('.mi-part-slot-multidrop-panel');
            if (tr) tr.setAttribute('aria-expanded', 'true');
            if (pan) pan.hidden = false;
            miPositionPartSlotMultidropPanel(wrap);
        }

        function miTogglePartSlotMultidrop(wrap) {
            if (!wrap) return;
            if (wrap.classList.contains('mi-part-slot-multidrop--open')) {
                miClosePartSlotMultidrop(wrap);
            } else {
                document.querySelectorAll('#item-editor-tab .mi-part-slot-multidrop--open').forEach(function (w) {
                    if (w !== wrap) miClosePartSlotMultidrop(w);
                });
                document.querySelectorAll('#item-editor-tab .mi-slot-cand-dropdown--open').forEach(miCloseCandDropdown);
                miOpenPartSlotMultidrop(wrap);
            }
        }

        function buildMiPartSlotMultidropHtml(selectInner, disabled, ariaLabel, extraSelectClass) {
            const dis = disabled ? ' disabled' : '';
            const extraCls = extraSelectClass ? ' ' + extraSelectClass : '';
            return (
                '<div class="mi-part-slot-multidrop">' +
                '<button type="button" class="mi-part-slot-multidrop-trigger part-slot-select"' +
                dis +
                ' aria-haspopup="listbox" aria-expanded="false" aria-label="' +
                ariaLabel +
                '">Select part(s)…</button>' +
                '<div class="mi-part-slot-multidrop-panel" hidden role="listbox" aria-multiselectable="true">' +
                '<div class="mi-part-slot-multidrop-list"></div></div>' +
                '<select class="mi-part-slot-select mi-part-slot-select-row mi-part-slot-select-native part-slot-select' +
                extraCls +
                '" multiple tabindex="-1" aria-hidden="true"' +
                dis +
                '>' +
                selectInner +
                '</select></div>'
            );
        }

        function buildMiSlotCandDropdownBlock(opts) {
            const o = opts || {};
            const isInvComp = !!o.isInvComp;
            const tableName = o.tableName || '';
            const unlockChecked = o.unlockChecked || '';
            const crossMfrLine = o.crossMfrLine || '';
            const allMfrTitle = o.allMfrTitle || '';
            const btnLabel = o.btnLabel || (isInvComp ? 'Set comp' : 'Add selected');
            const candDropdownLabel = o.candDropdownLabel || (isInvComp ? 'Choose inventory comp…' : 'Choose parts from catalog…');
            const escAttr = o.escAttr || function (s) {
                if (s == null) return '';
                return String(s).replace(/&/g, '&amp;').replace(/"/g, '&quot;');
            };
            const hintCompact = isInvComp
                ? '<div class="mi-slot-picker-hint">Pick a comp in the list below, then <strong>Set comp</strong> (replaces current).</div>'
                : '<div class="mi-slot-picker-hint">Tap rows to select multiple on mobile. Desktop: <strong>Ctrl+click</strong> / <strong>⌘+click</strong> to multi-select (or <strong>Shift+click</strong> for a range). Set <strong>Qty</strong> below, then <strong>Add selected</strong>.</div>';
            const selectAllRich = isInvComp
                ? ''
                : '<label class="mi-slot-select-all-wrap">' +
                  '<input type="checkbox" class="mi-slot-select-all" title="Selects only rows currently shown (after filter and list cap)." />' +
                  '<span class="mi-slot-select-all-label">Select all visible</span></label>';
            const richListOpen = isInvComp
                ? '<div class="mi-passive-candidate-list mi-slot-candidate-rich" tabindex="0" role="listbox" aria-label="' +
                  escAttr('Inventory composition') +
                  '" aria-multiselectable="false"></div>'
                : '<div class="mi-passive-candidate-list mi-slot-candidate-rich" tabindex="0" role="listbox" aria-label="' +
                  escAttr(tableName) +
                  '" aria-multiselectable="true"></div>';
            const dropdownActions =
                '<div class="mi-slot-picker-actions mi-slot-picker-actions--dropdown">' +
                (isInvComp
                    ? ''
                    : '<label class="mi-part-slot-qty-label"><span>Qty</span><input type="number" class="mi-slot-bulk-qty" min="1" max="99" value="1" title="Quantity per selected part" /></label>') +
                '<button type="button" class="mi-slot-add-selected">' +
                btnLabel +
                '</button></div>';
            return (
                '<div class="mi-slot-cand-dropdown">' +
                '<button type="button" class="mi-slot-cand-dropdown-trigger" aria-expanded="false" aria-haspopup="listbox">' +
                '<span class="mi-slot-cand-dropdown-label">' +
                escapeLegitHtml(candDropdownLabel) +
                '</span>' +
                '<span class="mi-slot-cand-dropdown-chevron" aria-hidden="true">▼</span>' +
                '</button>' +
                '<div class="mi-slot-cand-dropdown-panel" hidden>' +
                hintCompact +
                '<label class="mi-slot-all-mfr-wrap" title="' +
                escAttr(allMfrTitle) +
                '">' +
                '<input type="checkbox" class="mi-slot-all-manufacturers"' +
                unlockChecked +
                ' title="' +
                escAttr(allMfrTitle) +
                '" />' +
                '<span>' +
                escapeLegitHtml(crossMfrLine) +
                '</span></label>' +
                '<input type="search" class="mi-slot-search" placeholder="Filter by name, key, or root…" aria-label="Filter parts" />' +
                selectAllRich +
                richListOpen +
                '<div class="mi-passive-cand-foot"></div>' +
                dropdownActions +
                '</div></div>'
            );
        }

        function buildMiCrossSlotCandDropdownBlock(opts) {
            const o = opts || {};
            const escAttr = o.escAttr || function (s) {
                if (s == null) return '';
                return String(s).replace(/&/g, '&amp;').replace(/"/g, '&quot;');
            };
            const candDropdownLabel = o.candDropdownLabel || 'Choose cross-manufacturer parts…';
            return (
                '<div class="mi-slot-cand-dropdown mi-cross-cand-dropdown">' +
                '<button type="button" class="mi-slot-cand-dropdown-trigger" aria-expanded="false" aria-haspopup="listbox">' +
                '<span class="mi-slot-cand-dropdown-label">' +
                escapeLegitHtml(candDropdownLabel) +
                '</span>' +
                '<span class="mi-slot-cand-dropdown-chevron" aria-hidden="true">▼</span>' +
                '</button>' +
                '<div class="mi-slot-cand-dropdown-panel" hidden>' +
                '<select class="mi-cross-type-select" aria-label="Filter by part type"><option value="">Loading…</option></select>' +
                '<input type="search" class="mi-cross-search mi-slot-search" placeholder="Filter by name, key, item root, or token…" aria-label="Search cross-manufacturer catalog" />' +
                '<label class="mi-slot-select-all-wrap">' +
                '<input type="checkbox" class="mi-slot-select-all" title="Selects only rows currently shown (after filter and list cap)." />' +
                '<span class="mi-slot-select-all-label">Select all visible</span></label>' +
                '<div class="mi-slot-candidate-rich mi-passive-candidate-list mi-cross-candidate-rich" tabindex="0" role="listbox" aria-multiselectable="true" aria-label="' +
                escAttr('Cross-manufacturer parts catalog') +
                '"></div>' +
                '<div class="mi-passive-cand-foot mi-cross-cand-foot"></div>' +
                '<div class="mi-slot-picker-actions mi-slot-picker-actions--dropdown">' +
                '<label class="mi-part-slot-qty-label"><span>Qty</span><input type="number" class="mi-slot-bulk-qty" min="1" max="99" value="1" title="Quantity per selected part" /></label>' +
                '<button type="button" class="mi-cross-add-btn mi-slot-add-selected">Add selected</button>' +
                '</div></div></div>'
            );
        }

        function buildMiPartSlotSelectOptionsHtml(itemsPayload, maxOptions) {
            const cap = maxOptions != null ? maxOptions : 500;
            const slice = (itemsPayload || []).slice(0, cap);
            let html =
                '<option value="">Select part(s) to add…</option>' +
                slice
                    .map(function (x) {
                        const val = makeMiCrossPartRef(x.r, x.k);
                        const ve = String(val || '')
                            .replace(/&/g, '&amp;')
                            .replace(/"/g, '&quot;');
                        let labelText = String(x.l || x.k);
                        if (x.d) labelText = labelText + ' — ' + String(x.d);
                        const rarityCls = miInvCompOptionRarityClass(x);
                        const clsAttr = rarityCls ? ' class="' + rarityCls + '"' : '';
                        return '<option value="' + ve + '"' + clsAttr + '>' + escapeLegitHtml(labelText) + '</option>';
                    })
                    .join('');
            if ((itemsPayload || []).length > cap) {
                html +=
                    '<option value="" disabled>… ' +
                    ((itemsPayload || []).length - cap) +
                    ' more — enable cross-manufacturer or narrow catalog</option>';
            }
            return html;
        }

        function buildMiPartSlotCard(
            root,
            tableName,
            depTables,
            partSelectionOrder,
            compRules,
            basePartCounts,
            miSavedLines,
            extraSlotSet,
            miInst
        ) {
            const rootKey = root.key || '';
            function escAttr(s) {
                if (s == null) return '';
                return String(s).replace(/&/g, '&amp;').replace(/"/g, '&quot;');
            }
            function getKeysInTable(items) {
                return [...new Set((items || []).map(it => it.key).filter(Boolean))];
            }
            const slotTnNorm = normalizeDepTableName(tableName);
            const items = depTables[tableName];
            let catalogItems = items && items.length ? items.slice() : [];
            if (catalogItems.length === 0 && data) {
                const catalog = getMiGlobalPartCatalog(data);
                const catList = catalog.byType.get(slotTnNorm);
                if (catList && catList.length) {
                    catalogItems = catList.map(function (it) {
                        const dep = it.dep;
                        if (dep && typeof dep === 'object') {
                            const row = Object.assign({}, dep);
                            if (row.rootKey == null && it.sourceRootKey) row.rootKey = it.sourceRootKey;
                            return row;
                        }
                        return {
                            key: it.key,
                            depTableName: it.depTableName || slotTnNorm,
                            rootKey: it.sourceRootKey
                        };
                    });
                }
            }
            const slotItems =
                slotTnNorm === 'passive_points'
                    ? filterPassivePointsBrowseDeps(catalogItems, rootKey)
                    : catalogItems;
            const keysInTable = getKeysInTable(slotItems);
            const set = _sp().has(rootKey) ? _sp().get(rootKey) : new Set();
            const maxRows = tableName === 'inv_comp' ? 1 : MI_MODDED_MAX_PICKED_LINES;
            const isInvComp = tableName === 'inv_comp';
            const invMap = data && data.invNamePartDisplayNames;
            const itemsPayload = slotItems.map(function (it) {
                let primary =
                    slotTnNorm === 'inv_comp'
                        ? getModdedInvCompBrowseDisplayName(it, invMap, data, root)
                        : getDisplayNameForPart(it, invMap) || it.key;
                if (slotTnNorm === 'passive_points' && data) {
                    const p = resolvePassivePointsUiExtras(it, data, 'passive_points');
                    if (p && p.displayName) primary = p.displayName;
                }
                const row = {
                    r: String(it.rootKey != null ? it.rootKey : rootKey),
                    k: it.key,
                    t: slotTnNorm,
                    l: primary + (it.rootKey && it.rootKey !== rootKey ? ' — ' + it.rootKey : '')
                };
                if (slotTnNorm === 'inv_comp' && miInvCompDepIsPearlescent(it)) row.pearl = true;
                if (slotTnNorm === 'inv_comp') row.s = miInvCompDepRaritySearchText(it);
                const dbgPl = getPartDebugDisplayDescription(it);
                if (dbgPl) row.d = dbgPl;
                return row;
            });
            let itemsJson = JSON.stringify(itemsPayload);
            itemsJson = itemsJson.replace(/</g, '\\u003c');
            const itemsScript =
                '<script type="application/json" class="mi-slot-items-json">' + itemsJson + '<\/script>';
            const savedForTable = miSavedLines.filter(function (l) {
                return l.tableName === tableName;
            });
            let lineRows =
                savedForTable.length > 0
                    ? savedForTable.map(function (l) {
                          return { partKey: l.partKey, qty: l.qty != null ? l.qty : 1 };
                      })
                    : [];
            if (lineRows.length === 0) {
                const picked = keysInTable.filter(function (k) {
                    if (!set.has(k)) return false;
                    return !miSavedLines.some(function (ol) {
                        if (ol.tableName !== tableName || !ol.partKey) return false;
                        const pr = parseMiCrossPartRef(ol.partKey);
                        return pr ? pr.partKey === k : ol.partKey === k;
                    });
                });
                if (picked.length > 0) {
                    lineRows = picked.map(function (k) {
                        const itemHit =
                            slotItems.find(function (it) {
                                return it.key === k;
                            }) ||
                            catalogItems.find(function (it) {
                                return it.key === k;
                            });
                        const ref = itemHit ? makeMiSlotItemRef(itemHit, rootKey) : makeMiCrossPartRef(rootKey, k);
                        return { partKey: ref || k, qty: 1 };
                    });
                }
            }
            const pickedHtml = lineRows
                .filter(function (row) {
                    return row.partKey;
                })
                .map(function (row) {
                    return miSlotBuildPickedLineHtml(row.partKey, row.qty, rootKey, tableName);
                })
                .join('');
            const pickedUnique = new Set(
                lineRows.filter(function (row) {
                    return row && row.partKey;
                }).map(function (row) {
                    return String(row.partKey);
                })
            ).size;
            const partCountBrowse = slotItems.length;
            let partCountInfo = null;
            if (compRules && compRules.partCounts && compRules.partCounts.has(tableName)) {
                partCountInfo = compRules.partCounts.get(tableName);
            } else if (basePartCounts && basePartCounts.has(tableName)) {
                partCountInfo = basePartCounts.get(tableName);
            }
            const requiredMin = resolveRequiredMinForSlot(partCountInfo, root, compRules, tableName, {
                availableOptions: partCountBrowse
            });
            const statusCls = miPartSlotStatusClass(requiredMin, pickedUnique);
            const slotLabel = getMiPartTypeDisplayLabel(tableName);
            const isUserExtraSlot = !!(extraSlotSet && extraSlotSet.has(slotTnNorm));
            const extraRemoveBtn = isUserExtraSlot
                ? '<button type="button" class="mi-compact-remove-extra-slot" title="Remove this category">Remove</button>'
                : '';
            const btnLabel = isInvComp ? 'Set comp' : 'Add selected';
            const unlockKey = rootKey + '\0' + slotTnNorm;
            const unlockChecked =
                miInst && miInst.miSlotAllManufacturersUnlock && miInst.miSlotAllManufacturersUnlock.get(unlockKey)
                    ? ' checked'
                    : '';
            const crossMfrLine = isInvComp
                ? 'List comps from all roots'
                : 'All manufacturers for this slot';
            const allMfrTitle = isInvComp
                ? 'Lists inv_comp from every item root'
                : 'Lists this slot from every item root';
            const candDropdownBlock = buildMiSlotCandDropdownBlock({
                isInvComp: isInvComp,
                tableName: tableName,
                unlockChecked: unlockChecked,
                crossMfrLine: crossMfrLine,
                allMfrTitle: allMfrTitle,
                btnLabel: btnLabel,
                escAttr: escAttr
            });
            const pickedLineCount = lineRows.filter(function (row) {
                return row && row.partKey;
            }).length;
            const pickedBlock = pickedHtml
                ? '<div class="mi-part-slot-selected selected-parts"><div class="mi-part-slot-picked-list selected-parts-list' +
                  (pickedLineCount > 10 ? ' scrollable' : '') +
                  '"><div class="mi-compact-slot-rows mi-slot-picked-rows">' +
                  pickedHtml +
                  '</div></div></div>'
                : '<p class="mi-part-slot-empty-hint selected-parts-empty">No parts selected</p>';
            const clearBtn = pickedHtml
                ? '<button type="button" class="mi-slot-clear-category">Clear</button>'
                : '';
            return (
                '<article class="mi-part-slot-card part-slot-card" data-mi-dep-filter="' +
                escAttr(tableName) +
                '" data-table-name="' +
                escAttr(tableName) +
                '" data-mi-part-count="' +
                partCountBrowse +
                '" data-mi-picked-unique="' +
                pickedUnique +
                '" data-mi-required-min="' +
                requiredMin +
                '">' +
                '<header class="mi-part-slot-card-header">' +
                '<span class="mi-part-slot-status ' +
                statusCls +
                '" aria-hidden="true"></span>' +
                '<span class="mi-part-slot-title">' +
                escapeLegitHtml(slotLabel) +
                '</span>' +
                extraRemoveBtn +
                '</header>' +
                '<hr class="mi-part-slot-divider part-slot-divider" aria-hidden="true" />' +
                '<div class="mi-part-slot-controls">' +
                '<div class="mi-compact-slot mi-slot-picker-slot" data-root-key="' +
                escAttr(rootKey) +
                '" data-table-name="' +
                escAttr(tableName) +
                '" data-mi-max-rows="' +
                maxRows +
                '">' +
                candDropdownBlock +
                clearBtn +
                itemsScript +
                '</div>' +
                pickedBlock +
                '</div></article>'
            );
        }

        function buildMiCrossPartSlotCard(rootKey, miSavedLines) {
            if (!rootKey) return '';
            function escAttr(s) {
                if (s == null) return '';
                return String(s).replace(/&/g, '&amp;').replace(/"/g, '&quot;');
            }
            const crossSaved = (miSavedLines || []).filter(function (l) {
                return l && l.tableName === MI_CROSS_TABLE && l.partKey;
            });
            const crossPickedUnique = new Set(crossSaved.map(function (l) {
                return String(l.partKey || '');
            }).filter(Boolean)).size;
            const crossBrowseCount = miGlobalCatalogBrowsingPartCount();
            const rowsHtml = crossSaved
                .map(function (l) {
                    if (!l.partKey) return '';
                    const q = l.qty != null ? l.qty : 1;
                    return miSlotBuildPickedLineHtml(l.partKey, q, rootKey);
                })
                .join('');
            const crossPickedCount = crossSaved.filter(function (l) {
                return l && l.partKey;
            }).length;
            const crossPickedBlock = rowsHtml
                ? '<div class="mi-part-slot-selected selected-parts"><div class="mi-part-slot-picked-list selected-parts-list' +
                  (crossPickedCount > 10 ? ' scrollable' : '') +
                  '"><div class="mi-compact-slot-rows mi-cross-slot-rows">' +
                  rowsHtml +
                  '</div></div></div>'
                : '<p class="mi-part-slot-empty-hint selected-parts-empty">No parts selected</p>';
            const crossClearBtn = rowsHtml
                ? '<button type="button" class="mi-slot-clear-category">Clear</button>'
                : '';
            const statusCls = miPartSlotStatusClass(0, crossPickedUnique);
            return (
                '<article class="mi-part-slot-card part-slot-card mi-part-slot-card--cross" data-mi-dep-filter="' +
                escAttr(MI_CROSS_TABLE) +
                '" data-mi-part-count="' +
                crossBrowseCount +
                '" data-mi-picked-unique="' +
                crossPickedUnique +
                '">' +
                '<header class="mi-part-slot-card-header">' +
                '<span class="mi-part-slot-status ' +
                statusCls +
                '" aria-hidden="true"></span>' +
                '<span class="mi-part-slot-title">Cross-manufacturer</span>' +
                '</header>' +
                '<hr class="mi-part-slot-divider part-slot-divider" aria-hidden="true" />' +
                '<div class="mi-part-slot-controls">' +
                '<div class="mi-compact-slot mi-cross-root-slot" data-root-key="' +
                escAttr(rootKey) +
                '" data-table-name="' +
                escAttr(MI_CROSS_TABLE) +
                '" data-mi-max-rows="' +
                MI_MODDED_MAX_PICKED_LINES +
                '">' +
                buildMiCrossSlotCandDropdownBlock({ escAttr: escAttr }) +
                crossClearBtn +
                '</div>' +
                crossPickedBlock +
                '</div></article>'
            );
        }

        function buildMiDoesNotExistPartSlotCard(rootKey, miSavedLines) {
            if (!rootKey) return '';
            const missingSaved = (miSavedLines || []).filter(function (l) {
                return l && l.tableName === MI_DOES_NOT_EXIST_TABLE && l.partKey;
            });
            if (!missingSaved.length) return '';
            function escAttr(s) {
                if (s == null) return '';
                return String(s).replace(/&/g, '&amp;').replace(/"/g, '&quot;');
            }
            const rowsHtml = missingSaved
                .map(function (l) {
                    if (!l.partKey) return '';
                    const q = l.qty != null ? l.qty : 1;
                    return miSlotBuildPickedLineHtml(l.partKey, q, rootKey, MI_DOES_NOT_EXIST_TABLE);
                })
                .join('');
            const dneUnique = new Set(missingSaved.map(function (l) {
                return String(l.partKey || '');
            }).filter(Boolean)).size;
            const statusCls = miPartSlotStatusClass(0, dneUnique);
            return (
                '<article class="mi-part-slot-card mi-part-slot-card--dne" data-mi-dep-filter="' +
                escAttr(MI_DOES_NOT_EXIST_TABLE) +
                '" data-mi-picked-unique="' +
                dneUnique +
                '">' +
                '<header class="mi-part-slot-card-header">' +
                '<span class="mi-part-slot-status ' +
                statusCls +
                '" aria-hidden="true"></span>' +
                '<span class="mi-part-slot-title">Not in dataset</span>' +
                '</header>' +
                '<div class="mi-compact-slot mi-does-not-exist-root-slot" data-root-key="' +
                escAttr(rootKey) +
                '" data-table-name="' +
                escAttr(MI_DOES_NOT_EXIST_TABLE) +
                '" data-mi-max-rows="' +
                MI_MODDED_MAX_PICKED_LINES +
                '">' +
                '<div class="mi-part-slot-picked-list">' +
                '<div class="mi-compact-slot-rows mi-cross-slot-rows">' +
                rowsHtml +
                '</div></div></div></article>'
            );
        }

        /** Modded tab: compact part-slot card grid (native select + qty + Add per parttypes entry). */
        function buildModdedCompactDepTablesHTML(root, depTables, partSelectionOrder, depEntries, compRules, basePartCounts, extraSlotNormSet) {
            const rootKey = root.key || '';
            const miInst = window.legitBuilderInstances && window.legitBuilderInstances[1];
            const miSavedLines =
                miInst && miInst.relaxedPicker && miInst.miPartLines
                    ? miInst.miPartLines.get(rootKey) || []
                    : [];
            const extraSlotSet =
                extraSlotNormSet &&
                typeof extraSlotNormSet.has === 'function' &&
                extraSlotNormSet.size > 0
                    ? extraSlotNormSet
                    : null;
            const cards = [];
            const partSelectionOrderLower = partSelectionOrder.map(function (s) {
                return (s || '').toLowerCase();
            });
            const slotsToRender = [];

            function queueSlot(tableName) {
                const tn = String(tableName || '');
                if (!tn) return;
                const norm = normalizeDepTableName(tn);
                if (norm === MI_CROSS_TABLE || norm === MI_DOES_NOT_EXIST_TABLE) return;
                if (
                    miShouldHideTurretTerminalForModded() &&
                    (miIsTurretOrTerminalGadgetRoot(root) || miIsTurretOrTerminalPartSlot(tn))
                ) {
                    return;
                }
                if (miShouldHideEndgameForModded() && norm === 'endgame') {
                    return;
                }
                if (
                    !slotsToRender.some(function (s) {
                        return normalizeDepTableName(s) === norm;
                    })
                ) {
                    slotsToRender.push(tn);
                }
            }

            partSelectionOrder.forEach(queueSlot);
            if (Object.keys(depTables).length > 0) {
                Object.keys(depTables).forEach(function (tableName) {
                    if (partSelectionOrderLower.includes((tableName || '').toLowerCase())) {
                        queueSlot(tableName);
                    }
                });
            } else if (depEntries.length > 0) {
                depEntries.forEach(function (item) {
                    if (item && item.depTableName) queueSlot(item.depTableName);
                });
            }

            slotsToRender.sort(function (a, b) {
                const orderA = partSelectionOrderLower.indexOf((a || '').toLowerCase());
                const orderB = partSelectionOrderLower.indexOf((b || '').toLowerCase());
                if (orderA !== -1 && orderB !== -1) return orderA - orderB;
                if (orderA !== -1) return -1;
                if (orderB !== -1) return 1;
                return String(a).localeCompare(String(b));
            });

            slotsToRender.forEach(function (tableName) {
                cards.push(
                    buildMiPartSlotCard(
                        root,
                        tableName,
                        depTables,
                        partSelectionOrder,
                        compRules,
                        basePartCounts,
                        miSavedLines,
                        extraSlotSet,
                        miInst
                    )
                );
            });

            cards.push(buildMiCrossPartSlotCard(rootKey, miSavedLines));
            const dneCard = buildMiDoesNotExistPartSlotCard(rootKey, miSavedLines);
            if (dneCard) cards.push(dneCard);

            if (!cards.length) return '';
            return (
                '<div class="mi-part-grid-shell">' +
                '<div class="dep-tables mi-part-grid">' +
                cards.join('') +
                '</div></div>'
            );
        }

        /**
         * Simple view: true when this dep table has reached max selections — caller should omit unselected rows.
         */
        function shouldSimpleHideUnselectedWhenSlotFull(root, tableName, items, partCountInfo, compRules, basePartCounts) {
            const viewMode = applicationMode || localStorage.getItem('rootDefinitionsViewMode') || 'simple';
            if (viewMode !== 'simple') {
                return false;
            }
            if (window.__lbCtx && window.__lbCtx.relaxedPicker) {
                return false;
            }
            if (!root || !items || items.length === 0) {
                return false;
            }
            let effectiveMax = null;
            if (tableName === 'inv_comp') {
                let pciIc = compRules && compRules.partCounts ? compRules.partCounts.get('inv_comp') : null;
                if (!pciIc && basePartCounts && basePartCounts.has('inv_comp')) {
                    pciIc = basePartCounts.get('inv_comp');
                }
                if (pciIc && pciIc.max != null && isFinite(pciIc.max)) {
                    effectiveMax = pciIc.max;
                } else {
                    effectiveMax = 1;
                }
            } else if (partCountInfo && partCountInfo.max != null && isFinite(partCountInfo.max)) {
                effectiveMax = partCountInfo.max;
            } else if (!partCountInfo) {
                effectiveMax = 1;
            } else if (partCountInfo.max == null && partCountInfo.min === 0) {
                effectiveMax = 1;
            }
            if (effectiveMax == null || !isFinite(effectiveMax) || effectiveMax <= 0) {
                return false;
            }
            const sel = _sp().has(root.key) ? _sp().get(root.key) : null;
            let selectedInTable = 0;
            for (let i = 0; i < items.length; i++) {
                if (sel && sel.has(items[i].key)) {
                    selectedInTable++;
                }
            }
            return selectedInTable >= effectiveMax;
        }

        function createRootItemHTML(root, index, renderOpts) {
            let depTables = root.dependencyTables || {};
            const depEntries = root.depEntries || [];
            const sourceFile = root.sourceFile ? ` <span style="font-size: 10px; color: #666;">[${root.sourceFile}]</span>` : '';
            
            // Get current view mode from application mode
            const viewMode = applicationMode || localStorage.getItem('rootDefinitionsViewMode') || 'simple';
            const isSimpleView = viewMode === 'simple';

            // Get filtering rules from selected comp
            const compRules = getSelectedCompRules(root);
            const hasFilter = compRules !== null && !(window.__lbCtx && window.__lbCtx.relaxedPicker);
            const selectedComp = compRules ? compRules.compKey : null;
            const formatTagsForUI = (tags) => {
                if (!tags || !Array.isArray(tags)) return [];
                return tags.map(tag => {
                    if (typeof tag === 'object' && tag !== null) {
                        const values = Object.values(tag);
                        return values[0] || Object.keys(tag)[0] || '';
                    }
                    return tag;
                }).filter(Boolean);
            };
            const selectedCompBaseTags = formatTagsForUI(compRules?.compEntry?.basetags || compRules?.compEntry?.item?.basetags || []);
            const selectedCompBaseTagsHTML = selectedCompBaseTags.length > 0
                ? `<div style="margin-top: 4px; font-size: 11px; color: var(--text-secondary);"><span style="color: var(--accent-primary);">◆</span> BaseTags: ${selectedCompBaseTags.join(', ')}</div>`
                : '';
            
            // Get base generic root part counts (for display when no comp is selected)
            const basePartCounts = getBaseGenericRootPartCounts(root, selectedComp);
            
            // Build depTables from depEntries if not already set
            if (Object.keys(depTables).length === 0 && depEntries.length > 0) {
                depTables = {};
                depEntries.forEach(dep => {
                    const tableName = dep.depTableName;
                    if (tableName) {
                        if (!depTables[tableName]) {
                            depTables[tableName] = [];
                        }
                        depTables[tableName].push(dep);
                    }
                });
            }
            
            // Also include parts from base generic root for manufacturer-specific roots
            // This ensures forced parts from base comp (like Part_Body_01_Common) and elemental slots (body_ele, secondary_ele) are visible
            // Run even when no comp selected so body_ele/secondary_ele show for weapon roots
            if (data && data.rootDefinitions) {
                // Find base generic root (e.g., "Enhancement" for "cov_enhancement")
                // IMPORTANT: Order matters - more specific patterns must come first
                // e.g., "grenade_gadget" must come before "grenade" to match correctly
                const baseItemTypePatterns = ['weapon', 'enhancement', 'shield', 'repair_kit', 'grenade_gadget', 'heavy_weapon_gadget', 'turret_gadget', 'terminal_gadget', 'grenade', 'artifact', 'classmod'];
                let baseItemType = '';
                const rootKeyLower = (root.key || '').toLowerCase();
                
                // Debug: Log for grenade gadgets and weapons to verify we're entering this code path
                if (rootKeyLower.includes('grenade_gadget') || rootKeyLower.includes('weapon')) {
                    console.log(`[Part Display] Root ${root.key}: Entering base generic root merge logic, compRules exists=${!!compRules}, selectedComp=${compRules?.compKey}`);
                }
                
                // Check if this is a weapon by checking for weapon-specific part types
                // Manufacturer weapons like "bor_sm" don't have "weapon" in their key, but they have weapon parts
                const weaponPartTypes = ['body', 'barrel', 'magazine', 'scope', 'grip', 'foregrip', 'body_ele', 'secondary_ele'];
                const hasWeaponParts = root.depEntries && root.depEntries.some(dep => 
                    dep.depTableName && weaponPartTypes.includes(dep.depTableName.toLowerCase())
                );
                
                // Special handling: terminal_combat, terminal_*, turret_* should map to terminal_gadget/turret_gadget
                if (rootKeyLower.includes('terminal_') && !rootKeyLower.includes('terminal_gadget')) {
                    // Map terminal_combat, terminal_*, etc. to terminal_gadget
                    if (rootKeyLower.includes('terminal_combat') || rootKeyLower.includes('terminal_')) {
                        baseItemType = 'terminal_gadget';
                    }
                } else if (rootKeyLower.includes('turret_') && !rootKeyLower.includes('turret_gadget')) {
                    // Map turret_combat, turret_*, etc. to turret_gadget
                    if (rootKeyLower.includes('turret_combat') || rootKeyLower.includes('turret_')) {
                        baseItemType = 'turret_gadget';
                    }
                } else if (rootKeyLower.indexOf('repkit') !== -1) {
                    // Manufacturer roots use "repkit" in the key; generic Nexus root is repair_kit
                    baseItemType = 'repair_kit';
                } else {
                    // Check patterns in order - more specific first
                    for (const pattern of baseItemTypePatterns) {
                        if (rootKeyLower.includes(pattern) && rootKeyLower !== pattern) {
                            const match = rootKeyLower.match(new RegExp(`(?:\\w+_)?(${pattern.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')})(?:\\w+)?$`, 'i'));
                            if (match && match[1]) {
                                baseItemType = match[1].toLowerCase();
                                break;
                            }
                        } else if (rootKeyLower === pattern) {
                            baseItemType = pattern;
                            break;
                        }
                    }
                }
                
                // Manufacturer heavy weapons (*_hw): merge firmware and other HW slots from heavy_weapon_gadget, not weapon
                const rootBasetypeLowerForMerge = (root.basetype || '').toLowerCase();
                if (!baseItemType && (rootKeyLower.endsWith('_hw') || rootBasetypeLowerForMerge.includes('heavy_weapon'))) {
                    baseItemType = 'heavy_weapon_gadget';
                }
                
                // If no pattern matched but this root has weapon parts, it's likely a manufacturer weapon
                // Find the generic weapon root (key "weapon" or serial 1)
                if (!baseItemType && hasWeaponParts) {
                    baseItemType = 'weapon';
                }
                
                if (baseItemType) {
                    // Find the base generic root (e.g., "Enhancement" or "Weapon")
                    let baseGenericRoot = data.rootDefinitions.find(r => {
                        const rKey = (r.key || '').toLowerCase();
                        return rKey === baseItemType;
                    });
                    
                    // If not found by key, try to find by serial (weapon root is usually serial 1)
                    if (!baseGenericRoot && baseItemType === 'weapon') {
                        baseGenericRoot = data.rootDefinitions.find(r => {
                            const rSerial = r.serialIndex;
                            return rSerial === 1 || rSerial === '1';
                        });
                    }
                    
                    // heavy_weapon_gadget root is serial 244 in Nexus inv0
                    if (!baseGenericRoot && baseItemType === 'heavy_weapon_gadget') {
                        baseGenericRoot = data.rootDefinitions.find(r => {
                            const rSerial = r.serialIndex;
                            return rSerial === 244 || rSerial === '244';
                        });
                    }
                    
                    // Debug: Log for grenade gadgets and weapons
                    if (rootKeyLower.includes('grenade_gadget') || hasWeaponParts) {
                        console.log(`[Part Display] Root ${root.key}: baseItemType="${baseItemType}", baseGenericRoot found=${!!baseGenericRoot}, hasWeaponParts=${hasWeaponParts}`);
                        if (baseGenericRoot) {
                            console.log(`[Part Display] Root ${root.key}: baseGenericRoot key="${baseGenericRoot.key}", serial=${baseGenericRoot.serialIndex}`);
                            if (baseGenericRoot.depEntries) {
                                const tableTypes = new Set();
                                baseGenericRoot.depEntries.forEach(dep => {
                                    if (dep.depTableName) {
                                        tableTypes.add(dep.depTableName);
                                    }
                                });
                                console.log(`[Part Display] Root ${root.key}: baseGenericRoot has table types:`, Array.from(tableTypes).sort());
                                
                                // Check specifically for elemental parts
                                const hasBodyEle = baseGenericRoot.depEntries.some(dep => dep.depTableName && dep.depTableName.toLowerCase() === 'body_ele');
                                const hasSecondaryEle = baseGenericRoot.depEntries.some(dep => dep.depTableName && dep.depTableName.toLowerCase() === 'secondary_ele');
                                console.log(`[Part Display] Root ${root.key}: baseGenericRoot has body_ele=${hasBodyEle}, has secondary_ele=${hasSecondaryEle}`);
                            }
                        } else {
                            console.warn(`[Part Display] Root ${root.key}: Could not find base generic root with baseItemType="${baseItemType}"`);
                            // Try to find any root with "weapon" in key for debugging
                            const weaponRoots = data.rootDefinitions.filter(r => {
                                const rKey = (r.key || '').toLowerCase();
                                return rKey.includes('weapon') && rKey.length < 10; // Short keys like "weapon"
                            });
                            console.log(`[Part Display] Root ${root.key}: Found ${weaponRoots.length} potential weapon roots:`, weaponRoots.map(r => r.key));
                        }
                        if (compRules && compRules.partCounts) {
                            console.log(`[Part Display] Root ${root.key}: partCounts keys:`, Array.from(compRules.partCounts.keys()).sort());
                            console.log(`[Part Display] Root ${root.key}: allowedParts keys:`, Array.from(compRules.allowedParts.keys()).sort());
                        }
                    }
                    
                    if (baseGenericRoot && baseGenericRoot.depEntries) {
                        
                        // Get base comp rules to identify which slot types are defined there
                        // The base comp defines the slots (like stat_group1, stat_group2, stat_group3, Element, Payload, etc.)
                        // while manufacturer comps only filter allowed parts within those slots
                        const baseCompEntry = compRules ? compRules.compEntry : null;
                        const baseComp = baseCompEntry ? (baseCompEntry.basecomposition || baseCompEntry.item?.basecomposition) : null;
                        let baseCompKey = null;
                        if (baseComp && typeof baseComp === 'string') {
                            const m = baseComp.match(/inv'[^']+\.([^']+)'/i);
                            baseCompKey = m ? m[1] : null;
                            if (!baseCompKey) {
                                const fallbackMatch = baseComp.match(/\.([^']+)'?$/);
                                if (fallbackMatch && fallbackMatch[1]) {
                                    baseCompKey = fallbackMatch[1];
                                }
                            }
                        }
                        
                        // For manufacturer-specific roots (e.g., vla_grenade_gadget), if no basecomposition found,
                        // use the selected comp key directly (e.g., comp_05_legendary) since it has the same name in base generic root
                        // For variant comps (e.g., comp_05_legendary_blockbuster), extract the base comp (comp_05_legendary)
                        if (!baseCompKey && compRules && compRules.compKey) {
                            const selectedCompKey = compRules.compKey;
                            // Check if it's a comp_* key (not base_comp_*)
                            if (selectedCompKey.match(/^comp_\d+_/)) {
                                // Extract base comp name (e.g., "comp_05_legendary" from "comp_05_legendary_blockbuster")
                                // Format: comp_XX_rarity or comp_XX_rarity_variant
                                const match = selectedCompKey.match(/^(comp_\d+_\w+)/);
                                if (match) {
                                    baseCompKey = match[1]; // Use base comp name (comp_05_legendary)
                                } else {
                                    baseCompKey = selectedCompKey; // Fallback to full key
                                }
                            }
                        }
                        
                        // Find base comp entry in base generic root to get its rules (variant comp keys resolve to base comp_* row)
                        let baseCompRulesEntry = null;
                        if (baseCompKey && baseGenericRoot.depEntries) {
                            baseCompRulesEntry = resolveBaseCompRulesEntryOnGenericRoot(baseGenericRoot, baseCompKey);
                        }
                        
                        // Extract slot types defined in base comp rules (e.g., stat_group1, stat_group2, stat_group3)
                        const baseCompSlotTypes = new Set();
                        if (baseCompRulesEntry) {
                            const baseRules = baseCompRulesEntry.parttypeselectionrules || baseCompRulesEntry.item?.parttypeselectionrules;
                            if (baseRules && baseRules.pairs) {
                                const pairs = Array.isArray(baseRules.pairs) ? baseRules.pairs : Object.values(baseRules.pairs);
                                pairs.forEach(pair => {
                                    if (pair && pair.key) {
                                        baseCompSlotTypes.add(pair.key);
                                    }
                                });
                            }
                        }
                        // ClassMod: Nexus parttypes list stat_group2/3, special_passive, etc., but a given comp_* row may only
                        // name a subset in parttypeselectionrules — still merge the full generic ClassMod catalog for those slots.
                        if (baseItemType === 'classmod' && baseGenericRoot) {
                            getCanonicalPartTypeOrder(baseGenericRoot).forEach(slot => {
                                if (slot && String(slot).toLowerCase() !== 'inv_comp') {
                                    baseCompSlotTypes.add(slot);
                                }
                            });
                        }
                        // Modded tab: no inv_comp yet → baseCompSlotTypes stays empty and generic catalogs never merge.
                        // Fill from generic root parttypes so enhancements, weapons, HW, ordnance, repkit, etc. get base slots like shields already do in the compact merge below.
                        if (renderOpts && renderOpts.useCompactDepTables === true && baseGenericRoot) {
                            getCanonicalPartTypeOrder(baseGenericRoot).forEach(slot => {
                                if (slot && String(slot).toLowerCase() !== 'inv_comp') {
                                    baseCompSlotTypes.add(slot);
                                }
                            });
                        }
                        
                        // Debug: Log baseCompKey and baseCompRulesEntry for grenade gadgets and terminal/turret ordnances
                        if (rootKeyLower.includes('grenade_gadget') || rootKeyLower.includes('terminal_') || rootKeyLower.includes('turret_')) {
                            console.log(`[Part Display] Root ${root.key}: baseItemType="${baseItemType}", baseCompKey="${baseCompKey}", baseCompRulesEntry found=${!!baseCompRulesEntry}, baseGenericRoot found=${!!baseGenericRoot}`);
                            if (baseCompRulesEntry) {
                                const baseRules = baseCompRulesEntry.parttypeselectionrules || baseCompRulesEntry.item?.parttypeselectionrules;
                                if (baseRules && baseRules.pairs) {
                                    const slots = Object.values(baseRules.pairs).map(p => p.key).filter(Boolean);
                                    console.log(`[Part Display] Root ${root.key}: baseCompRulesEntry has slots:`, slots);
                                }
                            }
                            if (compRules && compRules.partCounts) {
                                console.log(`[Part Display] Root ${root.key}: partCounts keys:`, Array.from(compRules.partCounts.keys()).sort());
                            }
                        }
                        
                        // Merge parts from base generic root
                        // Debug: Log all table types in base generic root for grenade gadgets
                        if (rootKeyLower.includes('grenade_gadget')) {
                            const tableTypes = new Set();
                            baseGenericRoot.depEntries.forEach(dep => {
                                if (dep.depTableName && dep.depTableName !== 'inv_comp') {
                                    tableTypes.add(dep.depTableName);
                                }
                            });
                            console.log(`[Part Display] Root ${root.key}: Base generic root has table types:`, Array.from(tableTypes).sort());
                        }
                        
                        baseGenericRoot.depEntries.forEach(dep => {
                            const tableName = dep.depTableName;
                            // Only include actual part types (not inv_comp)
                            if (tableName && tableName !== 'inv_comp') {
                                // Check if this table type is defined in base comp rules (e.g., stat_group1, stat_group2, stat_group3)
                                // Normalize for case-insensitive comparison (e.g., "Element" vs "element")
                                const tableNameLower = tableName.toLowerCase();
                                const isBaseCompSlot = Array.from(baseCompSlotTypes).some(slot => slot.toLowerCase() === tableNameLower);
                                
                                // Check if slot has part counts (from baseCompositionRules or baseRules)
                                // If a slot has partCounts defined, it should be included even if not in allowedParts
                                // Normalize slot names for case-insensitive comparison (e.g., "Element" vs "element")
                                const hasPartCounts = compRules?.partCounts && (
                                    compRules.partCounts.has(tableName) || 
                                    Array.from(compRules.partCounts.keys()).some(key => key.toLowerCase() === tableNameLower)
                                );
                                
                                // Elemental parts (body_ele, secondary_ele) should always be included from generic weapon root
                                // These are used across all weapons for elemental damage types
                                // Check case-insensitively since table names might vary in capitalization
                                const isElementalPart = tableNameLower === 'body_ele' || tableNameLower === 'secondary_ele';
                                const isPearlSlot = MI_PEARL_SLOT_TYPES.indexOf(tableNameLower) !== -1;
                                
                                // Debug: Log for elemental parts on weapons
                                if (isElementalPart && (hasWeaponParts || rootKeyLower.includes('weapon'))) {
                                    console.log(`[Part Display] ${tableName} for weapon ${root.key}: isBaseCompSlot=${isBaseCompSlot}, hasPartCounts=${hasPartCounts}, isElementalPart=${isElementalPart}, hasAllowedParts=${compRules?.allowedParts?.has(tableName)}, willInclude=${isBaseCompSlot || hasPartCounts || isElementalPart || isPearlSlot || (compRules && compRules.allowedParts.has(tableName))}`);
                                }
                                
                                // Debug: Log for payload_augment and firmware
                                if ((tableName === 'payload_augment' || tableName === 'firmware' || tableName === 'element') && rootKeyLower.includes('grenade_gadget')) {
                                    console.log(`[Part Display] ${tableName}: isBaseCompSlot=${isBaseCompSlot}, hasPartCounts=${hasPartCounts}, partCountsKeys=${Array.from(compRules?.partCounts?.keys() || []).join(', ')}, hasAllowedParts=${compRules?.allowedParts?.has(tableName)}, allowedSize=${compRules?.allowedParts?.get(tableName)?.size || 0}, willInclude=${isBaseCompSlot || hasPartCounts || (compRules && compRules.allowedParts.has(tableName))}`);
                                }
                                
                                // For stat groups and other base comp slots, always include from base generic root
                                // Also include slots that have partCounts (e.g., payload_augment, firmware from base comp)
                                // Also include elemental parts (body_ele, secondary_ele) and pearl slots from generic weapon root
                                // For other parts, check if they're allowed by comp rules
                                if (isBaseCompSlot || hasPartCounts || isElementalPart || isPearlSlot || (compRules && compRules.allowedParts.has(tableName))) {
                                    let isAllowed = false;
                                    
                                    const effectiveAllowed = getAllowedPartsForTable(compRules, tableName);
                                    if (effectiveAllowed && effectiveAllowed.size > 0) {
                                        const partKey = dep.key;
                                        const partSlotValue = dep[tableName]; // e.g., dep.body
                                        for (const allowedKey of effectiveAllowed) {
                                            if (allowedKey.toLowerCase() === partKey.toLowerCase() ||
                                                (partSlotValue && typeof partSlotValue === 'string' &&
                                                    allowedKey.toLowerCase() === partSlotValue.toLowerCase())) {
                                                isAllowed = true;
                                                break;
                                            }
                                        }
                                    } else if (isBaseCompSlot || isElementalPart || isPearlSlot || hasPartCounts) {
                                        // No explicit allowed list for this slot: include base-root parts.
                                        isAllowed = true;
                                    }
                                    if (isBaseCompSlot && !isAllowed) {
                                        isAllowed = true;
                                    }
                                    
                                    if (isAllowed) {
                                        // Add to depTables if not already present
                                        if (!depTables[tableName]) {
                                            depTables[tableName] = [];
                                        }
                                        // Check if this part already exists (avoid duplicates)
                                        const exists = depTables[tableName].some(item => 
                                            item.key === dep.key && 
                                            item.depTableName === dep.depTableName &&
                                            item.rootKey === dep.rootKey
                                        );
                                        if (!exists) {
                                            // Ensure parts from baseGenericRoot have root serial info for display
                                            const depWithRootSerial = { ...dep };
                                            if (baseGenericRoot && baseGenericRoot.serialIndex !== undefined) {
                                                depWithRootSerial.rootSerialIndex = baseGenericRoot.serialIndex;
                                                depWithRootSerial.rootKey = baseGenericRoot.key;
                                            }
                                            depTables[tableName].push(depWithRootSerial);
                                            // Debug: Log when elemental parts are added for weapons
                                            if ((isElementalPart || isPearlSlot) && (hasWeaponParts || rootKeyLower.includes('weapon'))) {
                                                console.log(`[Part Display] ✓ Added elemental part "${dep.key}" to slot "${tableName}" from base generic root (serial ${baseGenericRoot?.serialIndex}) for weapon ${root.key}`);
                                            }
                                            // Debug: Log when parts are added for grenade gadgets
                                            if (rootKeyLower.includes('grenade_gadget') && (tableName === 'payload_augment' || tableName === 'firmware' || tableName === 'element' || tableName === 'payload' || tableName === 'stat_augment')) {
                                                console.log(`[Part Display] Added part "${dep.key}" to slot "${tableName}" from base generic root`);
                                            }
                                        } else if (isElementalPart && (hasWeaponParts || rootKeyLower.includes('weapon'))) {
                                            console.log(`[Part Display] Elemental part "${dep.key}" in slot "${tableName}" already exists in depTables for weapon ${root.key} (skipping duplicate)`);
                                        }
                                    } else {
                                        // Debug: Log when elemental parts are NOT allowed
                                        if (isElementalPart && (hasWeaponParts || rootKeyLower.includes('weapon'))) {
                                            console.log(`[Part Display] ✗ Elemental part "${dep.key}" in slot "${tableName}" was NOT allowed for weapon ${root.key}: isBaseCompSlot=${isBaseCompSlot}, isElementalPart=${isElementalPart}, hasPartCounts=${hasPartCounts}, allowedParts.has=${compRules?.allowedParts?.has(tableName)}, isAllowed=${isAllowed}`);
                                        }
                                        if (rootKeyLower.includes('grenade_gadget') && (tableName === 'payload_augment' || tableName === 'firmware' || tableName === 'element')) {
                                            console.log(`[Part Display] Part "${dep.key}" in slot "${tableName}" was NOT allowed: isBaseCompSlot=${isBaseCompSlot}, hasPartCounts=${hasPartCounts}, allowedParts.has=${compRules?.allowedParts?.has(tableName)}`);
                                        }
                                    }
                                }
                            }
                        });

                        // Nexus heavy_weapon_gadget basetype inv'Weapon': elemental deps live on generic weapon root, not heavy_weapon_gadget
                        if (baseItemType === 'heavy_weapon_gadget' && data && data.rootDefinitions) {
                            const hwMetaForDisplay = data.rootDefinitions.find(r => (r.key || '').toLowerCase() === 'heavy_weapon_gadget');
                            if (hwMetaForDisplay && /inv'\s*weapon\s*'/i.test(String(hwMetaForDisplay.basetype || ''))) {
                                const weaponGenForDisplay = data.rootDefinitions.find(r => (r.key || '').toLowerCase() === 'weapon');
                                if (weaponGenForDisplay && weaponGenForDisplay.depEntries && baseGenericRoot !== weaponGenForDisplay) {
                                    const hwLineageSlotSet = new Set(['body_ele', 'secondary_ele'].concat(MI_PEARL_SLOT_TYPES));
                                    weaponGenForDisplay.depEntries.forEach(dep => {
                                        const tableName = dep.depTableName;
                                        if (!tableName || tableName === 'inv_comp') return;
                                        if (!hwLineageSlotSet.has(tableName.toLowerCase())) return;
                                        const tableNameLower = tableName.toLowerCase();
                                        const isBaseCompSlot = Array.from(baseCompSlotTypes).some(slot => slot.toLowerCase() === tableNameLower);
                                        const hasPartCounts = compRules?.partCounts && (
                                            compRules.partCounts.has(tableName) ||
                                            Array.from(compRules.partCounts.keys()).some(key => key.toLowerCase() === tableNameLower)
                                        );
                                        const isHwWeaponLineageSlot =
                                            tableNameLower === 'body_ele' ||
                                            tableNameLower === 'secondary_ele' ||
                                            MI_PEARL_SLOT_TYPES.indexOf(tableNameLower) !== -1;
                                        if (!(isBaseCompSlot || hasPartCounts || isHwWeaponLineageSlot || (compRules && compRules.allowedParts.has(tableName)))) return;
                                        let isAllowed = false;
                                        const effectiveAllowed = getAllowedPartsForTable(compRules, tableName);
                                        if (effectiveAllowed && effectiveAllowed.size > 0) {
                                            const partKey = dep.key;
                                            const partSlotValue = dep[tableName];
                                            for (const allowedKey of effectiveAllowed) {
                                                if (allowedKey.toLowerCase() === partKey.toLowerCase() ||
                                                    (partSlotValue && typeof partSlotValue === 'string' &&
                                                        allowedKey.toLowerCase() === partSlotValue.toLowerCase())) {
                                                    isAllowed = true;
                                                    break;
                                                }
                                            }
                                        } else if (isBaseCompSlot || isHwWeaponLineageSlot || hasPartCounts) {
                                            isAllowed = true;
                                        }
                                        if (isAllowed) {
                                            if (!depTables[tableName]) {
                                                depTables[tableName] = [];
                                            }
                                            const exists = depTables[tableName].some(item =>
                                                item.key === dep.key &&
                                                item.depTableName === dep.depTableName &&
                                                item.rootKey === dep.rootKey
                                            );
                                            if (!exists) {
                                                const depWithRootSerial = { ...dep };
                                                if (weaponGenForDisplay.serialIndex !== undefined) {
                                                    depWithRootSerial.rootSerialIndex = weaponGenForDisplay.serialIndex;
                                                    depWithRootSerial.rootKey = weaponGenForDisplay.key;
                                                }
                                                depTables[tableName].push(depWithRootSerial);
                                            }
                                        }
                                    });
                                }
                            }
                        }
                    }
                }
            }

            // Shield type-specific roots (237 armor_shield, 248/238 energy_shield): merge when selected comp's basecomposition points to that root. Run for any root whose selected comp points to energy_shield/armor_shield (e.g. bor_shield + comp_05_legendary_watts4dinner).
            const rootKeyLowerShield = (root.key || '').toLowerCase();
            const isShield = rootKeyLowerShield === 'shield' || rootKeyLowerShield.includes('energy_shield') || rootKeyLowerShield.includes('armor_shield');
            const baseCompRefForGuard = compRules && compRules.compEntry ? (compRules.compEntry.basecomposition || compRules.compEntry.item?.basecomposition) : null;
            const normalizedBaseRef = baseCompRefForGuard && typeof baseCompRefForGuard === 'string' && baseCompRefForGuard.match(/inv'([^']+)\./i) ? (baseCompRefForGuard.match(/inv'([^']+)\./i)[1] || '').toLowerCase() : '';
            const compPointsToTypeShield = normalizedBaseRef === 'energy_shield' || normalizedBaseRef === 'armor_shield';
            if ((isShield || compPointsToTypeShield) && compRules && compRules.compEntry && data.rootDefinitions) {
                const shieldSlotTypesForMerge = new Set(['body', 'element', 'primary_augment', 'secondary_augment', 'firmware', 'endgame', 'unique', 'pearl_elem', 'pearl_stat']);
                const baseCompRef = compRules.compEntry.basecomposition || compRules.compEntry.item?.basecomposition;
                let typeSpecificShieldRoot = null;
                if (baseCompRef && typeof baseCompRef === 'string') {
                    const baseCompMatch = baseCompRef.match(/inv'([^']+)\./i);
                    const normalized = baseCompMatch && baseCompMatch[1] ? baseCompMatch[1].toLowerCase() : null;
                    if (normalized === 'energy_shield') {
                        // Prefer root key (exact then contains); then serial 248/238
                        typeSpecificShieldRoot = data.rootDefinitions.find(r => (r.key || '').toLowerCase() === 'energy_shield')
                            || data.rootDefinitions.find(r => (r.key || '').toLowerCase().includes('energy_shield'))
                            || data.rootDefinitions.find(r => {
                                const s = r.serialIndex;
                                return s === 248 || s === '248' || s === 238 || s === '238';
                            });
                    } else if (normalized === 'armor_shield') {
                        typeSpecificShieldRoot = data.rootDefinitions.find(r => (r.key || '').toLowerCase() === 'armor_shield')
                            || data.rootDefinitions.find(r => (r.key || '').toLowerCase().includes('armor_shield'))
                            || data.rootDefinitions.find(r => r.serialIndex === 237 || r.serialIndex === '237');
                    }
                }
                if (!typeSpecificShieldRoot && baseCompRef && typeof baseCompRef === 'string') {
                    const baseCompMatch = baseCompRef.match(/inv'([^']+)\./i);
                    const normalized = baseCompMatch && baseCompMatch[1] ? baseCompMatch[1].toLowerCase() : null;
                    if (normalized === 'energy_shield' || normalized === 'armor_shield') {
                        const shieldRootKeys = (data.rootDefinitions || []).filter(r => {
                            const k = (r.key || '').toLowerCase();
                            return k === 'shield' || k.includes('shield');
                        }).map(r => r.key);
                        console.warn('[Part Display] Shield type-specific root not found for basecomposition:', baseCompRef, '(normalized:', normalized, '). Loaded roots with "shield" in key:', shieldRootKeys);
                    }
                }
                if (typeSpecificShieldRoot && typeSpecificShieldRoot.depEntries) {
                    let mergedCount = 0;
                    typeSpecificShieldRoot.depEntries.forEach(dep => {
                        const tableName = dep.depTableName;
                        if (tableName && tableName !== 'inv_comp') {
                            const tableNameLower = tableName.toLowerCase();
                            const isBaseCompSlot = shieldSlotTypesForMerge.has(tableNameLower);
                            const hasPartCounts = compRules?.partCounts && (
                                compRules.partCounts.has(tableName) ||
                                Array.from(compRules.partCounts.keys()).some(key => key.toLowerCase() === tableNameLower)
                            );
                            const isElementalPart = tableNameLower === 'body_ele' || tableNameLower === 'secondary_ele';
                            // Include any shield slot type from type-specific root; also allowParts (case-insensitive)
                            const hasAllowedParts = compRules && compRules.allowedParts && (
                                compRules.allowedParts.has(tableName) ||
                                Array.from(compRules.allowedParts.keys()).some(key => key.toLowerCase() === tableNameLower)
                            );
                            if (isBaseCompSlot || hasPartCounts || isElementalPart || hasAllowedParts) {
                                let isAllowed = false;
                                // Type-specific root parts in shield slots are always shown: comp points to this type (e.g. energy_shield), so all parts from that root for this slot are valid. Do not restrict to base comp's allowed list only (which would show 16/16 instead of 27).
                                if (isBaseCompSlot || isElementalPart || hasPartCounts) {
                                    isAllowed = true;
                                } else {
                                    const effectiveAllowed = getAllowedPartsForTable(compRules, tableName);
                                    if (effectiveAllowed && effectiveAllowed.size > 0) {
                                        const partKey = dep.key;
                                        const partSlotValue = dep[tableName];
                                        for (const allowedKey of effectiveAllowed) {
                                            if (allowedKey.toLowerCase() === partKey.toLowerCase() ||
                                                (partSlotValue && typeof partSlotValue === 'string' &&
                                                    allowedKey.toLowerCase() === partSlotValue.toLowerCase())) {
                                                isAllowed = true;
                                                break;
                                            }
                                        }
                                    } else {
                                        isAllowed = true;
                                    }
                                }
                                if (isAllowed) {
                                    if (!depTables[tableName]) depTables[tableName] = [];
                                    const exists = depTables[tableName].some(item =>
                                        item.key === dep.key && item.depTableName === dep.depTableName && item.rootKey === typeSpecificShieldRoot.key
                                    );
                                    if (!exists) {
                                        const depWithRootSerial = { ...dep };
                                        if (typeSpecificShieldRoot.serialIndex !== undefined) {
                                            depWithRootSerial.rootSerialIndex = typeSpecificShieldRoot.serialIndex;
                                            depWithRootSerial.rootKey = typeSpecificShieldRoot.key;
                                        }
                                        depTables[tableName].push(depWithRootSerial);
                                        mergedCount++;
                                    }
                                }
                            }
                        }
                    });
                    if (mergedCount > 0) {
                        console.log('[Part Display] Merged', mergedCount, 'parts from type-specific shield root:', typeSpecificShieldRoot.key, '(serial', typeSpecificShieldRoot.serialIndex + ')');
                    } else if (typeSpecificShieldRoot.depEntries.length > 0) {
                        const nonCompCount = typeSpecificShieldRoot.depEntries.filter(d => d.depTableName && d.depTableName !== 'inv_comp').length;
                        if (nonCompCount > 0) {
                            console.warn('[Part Display] Type-specific shield root', typeSpecificShieldRoot.key, 'has', nonCompCount, 'non-inv_comp parts but none were merged. Check slot names vs shieldSlotTypesForMerge.');
                        }
                    }
                }
            }

            // Modded Items compact UI (useCompactDepTables): manufacturer roots like bor_shield are not `isShield`, and getSelectedCompRules is null until inv_comp is chosen — so energy_shield / armor_shield / generic Shield catalogs never merged. Merge all three lineage roots here (same pool as slot-constraint merge). Use renderOpts, not __lbCtx — __lbCtx may still be instance 0 while lazy-rendering under #item-editor-tab.
            if (renderOpts && renderOpts.useCompactDepTables === true && data && data.rootDefinitions) {
                const rklRel = (root.key || '').toLowerCase();
                if (rklRel.indexOf('shield') !== -1) {
                    const shieldSlotsRelaxed = new Set([
                        'body', 'element', 'primary_augment', 'secondary_augment', 'firmware', 'endgame', 'unique',
                        'pearl_elem', 'pearl_stat', 'body_ele', 'secondary_ele'
                    ]);
                    const findEnergyShieldRoot = function () {
                        return data.rootDefinitions.find(r => (r.key || '').toLowerCase() === 'energy_shield')
                            || data.rootDefinitions.find(r => (r.key || '').toLowerCase().includes('energy_shield'))
                            || data.rootDefinitions.find(r => {
                                const s = r.serialIndex;
                                return s === 248 || s === '248' || s === 238 || s === '238';
                            });
                    };
                    const findArmorShieldRoot = function () {
                        return data.rootDefinitions.find(r => (r.key || '').toLowerCase() === 'armor_shield')
                            || data.rootDefinitions.find(r => (r.key || '').toLowerCase().includes('armor_shield'))
                            || data.rootDefinitions.find(r => r.serialIndex === 237 || r.serialIndex === '237');
                    };
                    const findGenericShieldRoot = function () {
                        return data.rootDefinitions.find(r => (r.key || '').toLowerCase() === 'shield');
                    };
                    const lineageSeen = new Set();
                    [findEnergyShieldRoot(), findArmorShieldRoot(), findGenericShieldRoot()].forEach(function (lineageRoot) {
                        if (!lineageRoot || !lineageRoot.depEntries || lineageSeen.has(lineageRoot.key)) return;
                        lineageSeen.add(lineageRoot.key);
                        lineageRoot.depEntries.forEach(function (dep) {
                            const tableName = dep.depTableName;
                            if (!tableName || tableName === 'inv_comp') return;
                            if (!shieldSlotsRelaxed.has(tableName.toLowerCase())) return;
                            if (!depTables[tableName]) depTables[tableName] = [];
                            const exists = depTables[tableName].some(function (item) {
                                return item.key === dep.key && item.depTableName === dep.depTableName && item.rootKey === lineageRoot.key;
                            });
                            if (exists) return;
                            const depWithRootSerial = Object.assign({}, dep);
                            if (lineageRoot.serialIndex !== undefined) {
                                depWithRootSerial.rootSerialIndex = lineageRoot.serialIndex;
                                depWithRootSerial.rootKey = lineageRoot.key;
                            }
                            depTables[tableName].push(depWithRootSerial);
                        });
                    });
                }
            }

            // Part selection order from Nexus (root.parttypes); for variant roots (e.g. classmod_dark_siren) use generic root (classmod from inv0)
            const partSelectionOrder = getPartSelectionOrderFromRoot(root, data?.rootDefinitions);
            partSelectionOrder.forEach(slotName => {
                if (slotName && !depTables[slotName]) {
                    depTables[slotName] = [];
                }
            });
            if (rootPartSelectionIncludesPearl(root, data?.rootDefinitions)) {
                mergePearlDepsFromWeaponRoot(depTables, data);
            }

            const miInstExtra =
                window.legitBuilderInstances && window.legitBuilderInstances[1];
            const extraRaw =
                miInstExtra && miInstExtra.relaxedPicker && miInstExtra.miModdedExtraPartSlots
                    ? miInstExtra.miModdedExtraPartSlots.get(root.key) || []
                    : [];
            const extraNormOrdered = [];
            const extraNormSeen = new Set();
            extraRaw.forEach(function (t) {
                const n = normalizeDepTableName(String(t || ''));
                if (!n || n === 'inv_comp' || n === 'unknown' || extraNormSeen.has(n)) return;
                extraNormSeen.add(n);
                extraNormOrdered.push(n);
            });
            let partSelectionOrderExtended = partSelectionOrder.slice();
            const extraSlotNormSetForBuild =
                extraNormOrdered.length > 0 ? new Set(extraNormOrdered) : null;
            if (extraNormOrdered.length && data && data.rootDefinitions) {
                const catalogMerge = getMiGlobalPartCatalog(data);
                extraNormOrdered.forEach(function (n) {
                    let list = miFilterCrossCatalogItemsForViewMode(
                        catalogMerge.byType.get(n) || [],
                        data
                    );
                    if (!list || !list.length) return;
                    if (n === 'passive_points') {
                        list = filterPassivePointsCatalogItemsForBrowse(list.slice());
                    }
                    if (!list.length) return;
                    const depRows = list.map(function (it) {
                        const dep = it.dep;
                        if (dep && typeof dep === 'object') {
                            const row = Object.assign({}, dep);
                            if (row.rootKey == null && it.sourceRootKey) row.rootKey = it.sourceRootKey;
                            return row;
                        }
                        return {
                            key: it.key,
                            depTableName: it.depTableName || n,
                            rootKey: it.sourceRootKey
                        };
                    });
                    const cur = depTables[n];
                    if (!cur || cur.length === 0) {
                        depTables[n] = depRows;
                    }
                });
                extraNormOrdered.forEach(function (n) {
                    const inOrder = partSelectionOrderExtended.some(function (s) {
                        return normalizeDepTableName(String(s || '')) === n;
                    });
                    if (!inOrder) partSelectionOrderExtended.push(n);
                });
            }

            const useCompactDep = renderOpts && renderOpts.useCompactDepTables === true;
            let depTablesHTML = '';
            if (useCompactDep) {
                depTablesHTML = buildModdedCompactDepTablesHTML(
                    root,
                    depTables,
                    partSelectionOrderExtended,
                    depEntries,
                    compRules,
                    basePartCounts,
                    extraSlotNormSetForBuild
                );
            } else if (Object.keys(depTables).length > 0) {
                depTablesHTML = '<div class="dep-tables">';
                
                // Sort depTables entries according to game's part selection order; only show slots that are in this root's Nexus parttypes
                const partSelectionOrderLower = partSelectionOrder.map(s => (s || '').toLowerCase());
                const sortedDepTables = Object.entries(depTables)
                    .filter(([tableName]) => {
                        if (
                            shouldHideEndgameSlotOutsideAdvanced() &&
                            normalizeDepTableName(tableName) === 'endgame'
                        ) {
                            return false;
                        }
                        return partSelectionOrderLower.includes((tableName || '').toLowerCase());
                    })
                    .sort((a, b) => {
                        const orderA = partSelectionOrderLower.indexOf(a[0].toLowerCase());
                        const orderB = partSelectionOrderLower.indexOf(b[0].toLowerCase());
                        if (orderA !== -1 && orderB !== -1) return orderA - orderB;
                        if (orderA !== -1) return -1;
                        if (orderB !== -1) return 1;
                        return a[0].localeCompare(b[0]);
                    });
                
                sortedDepTables.forEach(([tableName, items]) => {
                    // Rarity outline for barrel (and other parts): use selected inv_comp's rarity when part has no explicit rarity
                    const selectedPartKeysForComp = _sp().has(root.key) ? _sp().get(root.key) : new Set();
                    const invCompItemsForRarity = depTables.inv_comp || [];
                    const selectedCompEntry = invCompItemsForRarity.find(it => selectedPartKeysForComp.has(it.key)) || null;
                    let selectedCompDisplayName = selectedCompEntry
                        ? getLegitBuilderInvCompPartDisplayName(selectedCompEntry, data, root, false)
                        : '';
                    const compRarityOutlineColor = selectedCompEntry ? getInvCompRarityOutlineColor(selectedCompEntry, selectedCompDisplayName) : null;

                    // Filter items based on comp rules
                    // Always include selected parts, even if they become invalid (so they can be deselected)
                    let filteredItems = items;
                    let hiddenItems = []; // Track items hidden due to missing dependencies
                    
                    // Determine if this is a classmod root and which character it belongs to
                    const isClassModRoot = root.key && root.key.toLowerCase().includes('classmod');
                    // Serial indices: 254=Siren, 255=Paladin, 256=Rafa, 259=Harlowe, 404=Robodealer (DLC inv4)
                    const classModCharacterMap = {
                        254: 'Siren',
                        255: 'Paladin',
                        256: 'Rafa',
                        259: 'Harlowe',
                        404: 'Robodealer'
                    };
                    const classModCharacter = isClassModRoot && root.serialIndex !== null && root.serialIndex !== undefined
                        ? classModCharacterMap[root.serialIndex]
                        : null;
                    
                    const scopedCompRulesForSlot = getForwardScopedCompRulesForSlot(root, tableName, compRules);
                    const activeCompRules = scopedCompRulesForSlot || compRules;
                    if (hasFilter && tableName !== 'inv_comp') {
                        const selectedPartKeys = _sp().has(root.key) ? _sp().get(root.key) : new Set();
                        // CRITICAL FIX: Check selection status FIRST, then validity
                        // This ensures only selected parts override exclusion rules
                        filteredItems = items.filter(item => {
                            const isSelected = selectedPartKeys.has(item.key);
                            
                            // CRITICAL: Only selected parts can override exclusion
                            // If part is selected, always show it (even if invalid) so user can deselect
                            if (isSelected) {
                                return true; // Force show selected parts - exit early
                            }
                            
                            // For passive_points on classmods: filter by character
                            // Only show passive_points whose skills belong to this classmod's character
                            if (tableName === 'passive_points' && classModCharacter && item.serialIndex !== null && item.serialIndex !== undefined) {
                                // Determine the root serial for this passive_point
                                let passiveRootSerial = item.rootSerialIndex !== undefined ? item.rootSerialIndex : 
                                    (item.rootKey && data && data.rootDefinitions ? 
                                     (data.rootDefinitions.find(r => r.key === item.rootKey)?.serialIndex) : 
                                     root.serialIndex);
                                
                                // If root serial not set, assume it's from the current classmod root
                                if (passiveRootSerial === null || passiveRootSerial === undefined) {
                                    passiveRootSerial = root.serialIndex;
                                }
                                
                                // Get the skill info to check its character
                                // CRITICAL: Only try the current classmod's root serial first
                                // Don't try other characters' root serials - we only want skills for THIS character
                                let serialId = `${root.serialIndex}:${item.serialIndex}`;
                                let skillInfo = getSkillInfo(serialId);
                                
                                // If not found with the classmod's root serial, check if the passive_point has a different root serial
                                // But only use it if it matches the classmod's root serial
                                if (!skillInfo) {
                                    // Try the passive_point's own root serial if it's different
                                    if (passiveRootSerial !== root.serialIndex && passiveRootSerial !== null && passiveRootSerial !== undefined) {
                                        serialId = `${passiveRootSerial}:${item.serialIndex}`;
                                        skillInfo = getSkillInfo(serialId);
                                        
                                        // If we found a skill with a different root serial, it's from a different character - filter it out
                                        if (skillInfo && passiveRootSerial !== root.serialIndex) {
                                            return false;
                                        }
                                    }
                                }
                                if (data) {
                                    skillInfo = enrichPassiveSkillInfoWithNexusUitooltip(skillInfo, item, data);
                                }
                                
                                // If skill info exists, check if it belongs to this classmod's character
                                if (skillInfo && skillInfo.character) {
                                    // Normalize character names for comparison
                                    const skillChar = skillInfo.character;
                                    const normalizedSkillChar = skillChar.toLowerCase();
                                    const normalizedClassModChar = classModCharacter.toLowerCase();
                                    
                                    // Map character names to match (Siren/Vex are the same)
                                    const characterMatch = normalizedSkillChar === normalizedClassModChar ||
                                        (normalizedSkillChar === 'vex' && normalizedClassModChar === 'siren') ||
                                        (normalizedSkillChar === 'siren' && normalizedClassModChar === 'siren');
                                    
                                    if (!characterMatch) {
                                        // This skill doesn't belong to this classmod's character - filter it out
                                        return false;
                                    }
                                } else {
                                    // Skill not found or has no character info
                                    // If the passive_point's root serial doesn't match the classmod's root serial,
                                    // it's likely from a different character, so filter it out
                                    if (passiveRootSerial !== root.serialIndex && passiveRootSerial !== null && passiveRootSerial !== undefined) {
                                        return false;
                                    }
                                    // If root serial matches but skill not found, allow it (might be an empty skill)
                                }
                            }
                            
                            // For non-selected parts: check validity (pass isSelected=false)
                            const isValid = isPartValid(item, activeCompRules, tableName, false);
                            
                            // Track hidden items for display (only for non-selected invalid items)
                            if (!isValid && activeCompRules) {
                                const formatTags = (tags) => {
                                    if (!tags || !Array.isArray(tags)) return [];
                                    return tags.map(tag => {
                                        if (typeof tag === 'object' && tag !== null) {
                                            const values = Object.values(tag);
                                            return values[0] || Object.keys(tag)[0] || '';
                                        }
                                        return tag;
                                    }).filter(Boolean);
                                };
                                
                                // Check for missing dependencies
                                const depTags = new Set(formatTags(item.dependencytags || []).map(t => t.toLowerCase()));
                                const missingDeps = depTags.size > 0 
                                    ? Array.from(depTags).filter(tag => !activeCompRules.tagPool.has(tag))
                                    : [];
                                
                                // Check for exclusion conflicts
                                const exclTags = new Set(formatTags(item.exclusiontags || []).map(t => t.toLowerCase()));
                                const addTags = new Set(formatTags(item.addtags || []).map(t => t.toLowerCase()));
                                const excludedBy = [];
                                
                                if (exclTags.size > 0) {
                                    // Find tags that are excluded and in the pool, but not self-excluded
                                    exclTags.forEach(exclTag => {
                                        // Don't count if this part adds the tag (self-exclusion)
                                        if (!addTags.has(exclTag) && activeCompRules.tagPool.has(exclTag)) {
                                            excludedBy.push(exclTag);
                                        }
                                    });
                                }
                                
                                // Only add to hidden items if there's a reason (missing deps or exclusions)
                                if (missingDeps.length > 0 || excludedBy.length > 0) {
                                    hiddenItems.push({
                                        item: item,
                                        missingTags: missingDeps,
                                        excludedBy: excludedBy
                                    });
                                }
                                // Hide this part - it's invalid and NOT selected
                                return false;
                            }
                            
                            // Part is valid - show it
                            return true;
                        });
                    }
                    
                    // Get partcount info for this table if available
                    // Only show part counts for actual part types (not dependency tables like inv_comp)
                    // Part types are those that appear in parttypeselectionrules
                    let partCountInfo = null;
                    let partCountDisplay = '';
                    
                    // Skip part count display for dependency tables (inv_comp, etc.)
                    // Only show for actual part types (body, core_augment, firmware, stat_group1, etc.)
                    const isPartType = tableName !== 'inv_comp';
                    
                    if (isPartType) {
                        // Priority: 1) compRules.partCounts (includes base generic root + manufacturer overrides)
                        //           2) basePartCounts (when no comp selected)
                        if (compRules && compRules.partCounts && compRules.partCounts.has(tableName)) {
                            partCountInfo = compRules.partCounts.get(tableName);
                        } else if (basePartCounts && basePartCounts.has(tableName)) {
                            partCountInfo = basePartCounts.get(tableName);
                        }
                    }
                    
                    if (partCountInfo) {
                        // Use display values if available, otherwise use effective values
                        const displayMin = partCountInfo.displayMin !== undefined ? partCountInfo.displayMin : partCountInfo.min;
                        const displayMax = partCountInfo.displayMax !== undefined ? partCountInfo.displayMax : partCountInfo.max;
                        const effectiveMin = partCountInfo.min;
                        const effectiveMax = partCountInfo.max;
                        const availableOptions = Array.isArray(filteredItems) ? filteredItems.length : 0;
                        const inferredRequired = isInferredRequiredSlot(partCountInfo, availableOptions, tableName);
                        
                        // Show display values if they exist
                        if (displayMin !== null && displayMax !== null) {
                            if (displayMin === displayMax) {
                                partCountDisplay = ` [Count: ${displayMin}]`;
                            } else {
                                partCountDisplay = ` [Count: ${displayMin}-${displayMax}]`;
                            }
                        } else if (displayMin !== null) {
                            // min 0 with no max means min 0, max 1 (show as "min 0" but effective is 0-1)
                            partCountDisplay = ` [Count: min ${displayMin} (assumed ${effectiveMin}-${effectiveMax})]`;
                        } else if (displayMax !== null) {
                            partCountDisplay = ` [Count: max ${displayMax} (assumed ${effectiveMin}-${effectiveMax})]`;
                        } else if (effectiveMin !== null && effectiveMax !== null) {
                            // Fallback to effective values
                            if (inferredRequired) {
                                partCountDisplay = ` [Count: 1 (inferred from available parts)]`;
                            } else if (effectiveMin === effectiveMax) {
                                partCountDisplay = ` [Count: ${effectiveMin}]`;
                            } else {
                                partCountDisplay = ` [Count: ${effectiveMin}-${effectiveMax}]`;
                            }
                        } else {
                            // Unknown - but effective is 1-1
                            partCountDisplay = ` [Count: Unknown (assumed ${effectiveMin}-${effectiveMax})]`;
                        }
                    } else {
                        // No partcount data available - Unknown, but assumed 1-1 for selection
                        partCountDisplay = ` [Count: Unknown (assumed 1-1)]`;
                    }
                    
                    // Build hidden items display if there are any
                    let hiddenItemsHTML = '';
                    if (hiddenItems.length > 0) {
                        const hasMissingDeps = hiddenItems.some(h => h.missingTags && h.missingTags.length > 0);
                        const hasExclusions = hiddenItems.some(h => h.excludedBy && h.excludedBy.length > 0);
                        const reasonText = hasMissingDeps && hasExclusions 
                            ? 'Missing dependency tags or excluded'
                            : hasExclusions 
                                ? 'Excluded by selected parts'
                                : 'Missing dependency tags';
                        const hiddenSectionLabel = `${hiddenItems.length} part(s) hidden - ${reasonText}`;
                        const hiddenSectionLabelAttr = hiddenSectionLabel.replace(/"/g, '&quot;');
                        
                        hiddenItemsHTML = `
                            <div class="legit-hidden-parts hidden-items-section">
                                <div class="legit-hidden-parts-toggle" role="button" tabindex="0" data-label="${hiddenSectionLabelAttr}" onclick="toggleLegitHiddenPartsSection(this)">▶ ${hiddenSectionLabel}</div>
                                <div class="legit-hidden-parts-body" style="display: none;">
                                    ${hiddenItems.map(hidden => {
                                        const reasons = [];
                                        if (hidden.missingTags && hidden.missingTags.length > 0) {
                                            reasons.push(`Missing: ${hidden.missingTags.join(', ')}`);
                                        }
                                        if (hidden.excludedBy && hidden.excludedBy.length > 0) {
                                            reasons.push(`Excluded by: ${hidden.excludedBy.join(', ')}`);
                                        }
                                        
                                        return `
                                            <div class="legit-hidden-reason-card">
                                                <div class="legit-hidden-reason-title">${hidden.item.key}</div>
                                                ${reasons.map(reason => `<div class="legit-hidden-reason-line">${reason}</div>`).join('')}
                                            </div>
                                        `;
                                    }).join('')}
                                </div>
                            </div>
                        `;
                    }

                    const simpleHideUnselectedWhenSlotFull = shouldSimpleHideUnselectedWhenSlotFull(
                        root,
                        tableName,
                        items,
                        partCountInfo,
                        compRules,
                        basePartCounts
                    );
                    const selectedInFilteredCount = filteredItems.filter(function (item) {
                        return _sp().has(root.key) && _sp().get(root.key).has(item.key);
                    }).length;
                    const depItemVisibleCount = simpleHideUnselectedWhenSlotFull
                        ? selectedInFilteredCount
                        : filteredItems.length;
                    const showDepTableSearch = filteredItems.length > 10;
                    const depTableSearchRowHtml = showDepTableSearch
                        ? buildDepTableSearchRowHtml(tableName, filteredItems.length)
                        : '';
                    
                    depTablesHTML += `
                        <div class="dep-table" data-dep-table="${escapeLegitHtml(tableName)}">
                            <div class="dep-table-header">
                                <span>${tableName}${hasFilter && tableName !== 'inv_comp' ? ` (${filteredItems.length}/${items.length})` : ''}${partCountDisplay}</span>
                                <span>${depItemVisibleCount} items</span>
                            </div>
                            ${depTableSearchRowHtml}
                            <div class="dep-items">
                                ${filteredItems.map(item => {
                                    const isComp = item.key && item.key.toLowerCase().startsWith('comp');
                                    
                                    // Check if this slot is disabled (partcount min=0, max=0)
                                    // Use base generic root part counts if no comp selected
                                    let partCountInfo = compRules && compRules.partCounts ? compRules.partCounts.get(tableName) : null;
                                    if (!partCountInfo && basePartCounts && basePartCounts.has(tableName)) {
                                        partCountInfo = basePartCounts.get(tableName);
                                    }
                                    const isSlotDisabled = !(window.__lbCtx && window.__lbCtx.relaxedPicker) && partCountInfo && partCountInfo.min === 0 && partCountInfo.max === 0;
                                    
                                    const isSelectable = !isSlotDisabled; // Disable if slot has count 0-0
                                    const partId = `${root.key || 'unknown'}_${item.key || 'unknown'}`;
                                    const isSelected = _sp().has(root.key) && _sp().get(root.key).has(item.key);
                                    if (simpleHideUnselectedWhenSlotFull && !isSelected) {
                                        return '';
                                    }
                                    
                                    const selectedValidityState = getSelectedPartValidityState(item, activeCompRules, tableName, isSelected, root);
                                    const selectableClass = isSelectable ? 'selectable' : '';
                                    const selectedClass = isSelected ? 'selected' : '';
                                    const invalidClass = isSelected && selectedValidityState.status === 'hard-invalid' ? 'invalid-selected' : '';
                                    const orderConflictClass = isSelected && selectedValidityState.status === 'order-conflict' ? 'order-conflict-selected' : '';
                                    const disabledClass = isSlotDisabled ? 'disabled' : '';
                                    const selectedStateIcon = isSelected && selectedValidityState.status === 'hard-invalid'
                                        ? ' ⛔'
                                        : (isSelected && selectedValidityState.status === 'order-conflict' ? ' ⚠' : '');
                                    
                                    // Format tags for display
                                    const formatTags = (tags) => {
                                        if (!tags || !Array.isArray(tags)) return [];
                                        return tags.map(tag => {
                                            if (typeof tag === 'object' && tag !== null) {
                                                const values = Object.values(tag);
                                                return values[0] || Object.keys(tag)[0] || '';
                                            }
                                            return tag;
                                        }).filter(Boolean);
                                    };
                                    
                                    const addTags = formatTags(item.addtags || []);
                                    const depTags = formatTags(item.dependencytags || []);
                                    const exclTags = formatTags(item.exclusiontags || []);
                                    const baseTags = formatTags(item.basetags || []);
                                    const hasTags = addTags.length > 0 || depTags.length > 0 || exclTags.length > 0 || baseTags.length > 0;
                                    
                                    // Check if we're in simple view
                                    const isSimpleView = localStorage.getItem('rootDefinitionsViewMode') === 'simple';
                                    
                                    // Get friendly part name (remove common prefixes)
                                    const getFriendlyPartName = (key) => {
                                        if (!key) return 'Unknown';
                                        // Remove common prefixes like "Part_", "Inv_", "Comp_", etc.
                                        return key.replace(/^(Part_|Inv_|Comp_)/i, '').replace(/_/g, ' ').replace(/\b\w/g, l => l.toUpperCase());
                                    };
                                    
                                    // Always show serial index for all parts
                                    // Format: {serial} for parts from current root, {rootSerial:subSerial} for parts from different roots
                                    let serialFormat = '';
                                    const itemRootSerial = item.rootSerialIndex !== undefined ? item.rootSerialIndex : (item.rootKey && data && data.rootDefinitions ? (data.rootDefinitions.find(r => r.key === item.rootKey)?.serialIndex) : null);
                                    const itemSubSerial = item.serialIndex;
                                    const currentRootSerial = root.serialIndex;
                                    if (itemSubSerial !== null && itemSubSerial !== undefined) {
                                        if (itemRootSerial !== null && itemRootSerial !== undefined && itemRootSerial !== currentRootSerial) {
                                            // Part comes from a different root - show {rootSerial:subSerial} format
                                            serialFormat = ` {${itemRootSerial}:${itemSubSerial}}`;
                                        } else {
                                            // Part from current root - show {serial} format
                                            serialFormat = ` {${itemSubSerial}}`;
                                        }
                                    }
                                    
                                    // Check if this is a passive_point and get skill info
                                    let skillInfo = null;
                                    let skillImageUrl = null;
                                    let skillDebugInfo = null; // Store debug info for unmapped skills
                                    if (tableName === 'passive_points' && itemSubSerial !== null) {
                                        // For passive_points, determine root serial
                                        let passiveRootSerial = itemRootSerial;
                                        
                                        // If root serial not set, try to find it from the root
                                        if (passiveRootSerial === null || passiveRootSerial === undefined) {
                                            // Check if current root is a classmod root
                                            const rootKeyLower = (root.key || '').toLowerCase();
                                            if (rootKeyLower.includes('classmod')) {
                                                passiveRootSerial = root.serialIndex;
                                            } else {
                                                // Try to find the passive_point in subItemsByTable to get its root
                                                if (data && data.subItemsByTable && data.subItemsByTable.passive_points) {
                                                    const passiveItem = data.subItemsByTable.passive_points.items?.find(p => 
                                                        p.serialIndex === itemSubSerial || 
                                                        (p.key && p.key.toLowerCase() === (item.key || '').toLowerCase())
                                                    );
                                                    if (passiveItem && passiveItem.rootKey) {
                                                        const passiveRoot = data.rootDefinitions?.find(r => r.key === passiveItem.rootKey);
                                                        if (passiveRoot) {
                                                            passiveRootSerial = passiveRoot.serialIndex;
                                                        }
                                                    } else if (passiveItem && passiveItem.rootSerialIndex !== undefined) {
                                                        passiveRootSerial = passiveItem.rootSerialIndex;
                                                    }
                                                }
                                                
                                                // If still not found, try all classmod root serials (255, 254, 253, 252)
                                                if ((passiveRootSerial === null || passiveRootSerial === undefined) && skillMapping && skillMapping.size > 0) {
                                                    const classmodSerials = [255, 254, 253, 252, 404];
                                                    for (const rootSerial of classmodSerials) {
                                                        const testSerialId = `${rootSerial}:${itemSubSerial}`;
                                                        if (skillMapping.has(testSerialId)) {
                                                            passiveRootSerial = rootSerial;
                                                            break;
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                        
                                        if (passiveRootSerial !== null && passiveRootSerial !== undefined) {
                                            const serialId = `${passiveRootSerial}:${itemSubSerial}`;
                                            skillInfo = getSkillInfo(serialId);
                                            
                                            // NOTE: Removed fallback that tries all classmod root serials
                                            // This was causing unmapped skills to appear as mapped when they matched a different character's skill
                                            // Skills should only match their exact serial ID for the current character's root serial
                                            // If a skill is not found with the determined root serial, it's truly unmapped for that character
                                            
                                            // Debug: Log when skill is not found
                                            if (!skillInfo) {
                                                // Check if the serial ID exists in the mapping (for debugging)
                                                const hasExact = skillMapping.has(serialId);
                                                const sampleKeys = Array.from(skillMapping.keys()).slice(0, 5);
                                                console.warn(`[Skill Lookup Failed] Serial: ${serialId}, Key: ${item.key}, HasExact: ${hasExact}, SampleKeys:`, sampleKeys);
                                            }
                                            
                                            if (skillInfo) {
                                                // Skill is mapped - check if it has valid data
                                                // Empty skills (with no name/description) are valid - they're just empty placeholders
                                                if (skillInfo.name || skillInfo.skill_name || skillInfo.description) {
                                                    skillImageUrl = getSkillImageUrl(skillInfo.name || skillInfo.skill_name, skillInfo.character);
                                                }
                                                // If skillInfo exists but has no data, that's fine - it's an empty skill
                                                // Don't log a warning for these
                                            } else {
                                                // Only log warning if skill is truly not in mapping
                                                // Check if it might be an empty skill that was intentionally not mapped
                                                const isInMapping = skillMapping.has(serialId) || 
                                                    Array.from(skillMapping.keys()).some(key => {
                                                        if (typeof key === 'string' && key.includes(':')) {
                                                            const [r, s] = key.split(':');
                                                            return (r === passiveRootSerial.toString() || parseInt(r) === passiveRootSerial) &&
                                                                   (s === itemSubSerial.toString() || parseInt(s) === itemSubSerial);
                                                        }
                                                        return key === itemSubSerial || key === itemSubSerial.toString();
                                                    });
                                                
                                                // Only log if truly unmapped (not in mapping at all)
                                                if (!isInMapping) {
                                                    skillDebugInfo = {
                                                        partKey: item.key,
                                                        serialId: serialId,
                                                        rootSerial: passiveRootSerial,
                                                        subSerial: itemSubSerial,
                                                        rootKey: root.key,
                                                        reason: 'Skill not found in skillMapping'
                                                    };
                                                    console.warn('[Skill Debug] Unmapped passive_point:', skillDebugInfo);
                                                }
                                            }
                                        } else {
                                            // Debug: Log missing root serial
                                            skillDebugInfo = {
                                                partKey: item.key,
                                                subSerial: itemSubSerial,
                                                rootKey: root.key,
                                                itemRootSerial: itemRootSerial,
                                                reason: 'Could not determine root serial'
                                            };
                                            console.warn('[Skill Debug] Missing root serial for passive_point:', skillDebugInfo);
                                        }
                                    }
                                    if (tableName === 'passive_points' && !skillInfo && data) {
                                        skillInfo = getSkillInfoForPassive(item, data);
                                        if (skillInfo && (skillInfo.name || skillInfo.skill_name) && skillInfo.character) {
                                            skillImageUrl = getSkillImageUrl(skillInfo.name || skillInfo.skill_name, skillInfo.character);
                                        }
                                    }
                                    if (tableName === 'passive_points' && data) {
                                        skillInfo = enrichPassiveSkillInfoWithNexusUitooltip(skillInfo, item, data);
                                        if (skillInfo && (skillInfo.name || skillInfo.skill_name) && skillInfo.character && !skillImageUrl) {
                                            skillImageUrl = getSkillImageUrl(skillInfo.name || skillInfo.skill_name, skillInfo.character);
                                        }
                                        const cdnPassiveIcon = getPassiveUiresourcesIconUrl(item, data, tableName);
                                        if (cdnPassiveIcon) skillImageUrl = cdnPassiveIcon;
                                    }

                                    let partDisplayName =
                                        tableName === 'inv_comp'
                                            ? getLegitBuilderInvCompPartDisplayName(item, data, root, isSimpleView)
                                            : getDisplayNameForPart(item, data.invNamePartDisplayNames) ||
                                              (isSimpleView ? getFriendlyPartName(item.key) : item.key);
                                    const passiveIconAlt = escapeLegitHtml(String(
                                        (skillInfo && (skillInfo.name || skillInfo.skill_name)) ? (skillInfo.name || skillInfo.skill_name) : (partDisplayName || '')
                                    ));
                                    const showPassiveSkillRow = tableName === 'passive_points' && (!!skillInfo || !!skillImageUrl);
                                    const partDebugDesc = getPartDebugDisplayDescription(item);
                                    const partDebugDescHtml = partDebugDesc
                                        ? `<div class="dep-item-debug-display" style="font-size: 10px; font-weight: 600; color: ${LEGIT_DEBUG_DESC_FG}; margin-top: 4px; line-height: 1.35; text-shadow: 0 1px 2px rgba(0,0,0,0.75);">${escapeLegitHtml(partDebugDesc)}</div>`
                                        : '';
                                    const baseTooltipText = isSimpleView 
                                        ? (isSlotDisabled ? 'Slot disabled - cannot select' : 'Click to select this part')
                                        : `depIndex: ${item.depIndex}, serialIndex: ${item.serialIndex}${isSlotDisabled ? ' (Slot disabled - Count: 0)' : ''}${skillInfo ? ` | Skill: ${skillInfo.name}` : ''}`;
                                    const warningTooltip = isSelected && selectedValidityState.status !== 'valid'
                                        ? ` | ${selectedValidityState.message}`
                                        : '';
                                    const tooltipText = `${baseTooltipText}${warningTooltip}`;
                                    const invCompLootCtx = tableName === 'inv_comp'
                                        ? { rootKey: (item.rootKey && String(item.rootKey).trim()) || root.key }
                                        : {};
                                    const invCompBadges = tableName === 'inv_comp'
                                        ? getInvCompBadges(item, { compact: true }, data, invCompLootCtx)
                                        : [];
                                    const invCompGenericLegendaryWarn = tableName === 'inv_comp' && isBareGenericLegendaryInvalidDrop(item, invCompLootCtx);
                                    const invCompMissingLootWarn = tableName === 'inv_comp' && isInvCompMissingIndexedLootSource(item, data, invCompLootCtx);
                                    const invCompNpcWeaponWarn = tableName === 'inv_comp' && entryHasNpcWeaponBasetag(item);
                                    const invCompLootWarnStyle = invCompGenericLegendaryWarn
                                        ? ' box-shadow: inset 4px 0 0 #ff7043; background: rgba(255, 112, 67, 0.08);'
                                        : invCompMissingLootWarn
                                            ? ' box-shadow: inset 4px 0 0 #ff9800; background: rgba(255, 152, 0, 0.07);'
                                            : invCompNpcWeaponWarn
                                                ? ' box-shadow: inset 4px 0 0 #b388ff; background: rgba(179, 136, 255, 0.1);'
                                                : '';
                                    const invCompLootWarnLineHtml = invCompGenericLegendaryWarn
                                        ? `<div class="dep-item-loot-warn" style="font-size: 9px; color: #ff8a65; margin-top: 4px; line-height: 1.35; font-weight: 600;">✗ GENERIC Legendary — invalid drop. Use a unique comp (comp_05_legendary_*), not bare comp_05_legendary.</div>`
                                        : invCompMissingLootWarn
                                            ? `<div class="dep-item-loot-warn" style="font-size: 9px; color: #ffb74d; margin-top: 4px; line-height: 1.35; font-weight: 600;">⚠ Likely no normal drop source — global pool excluded and no dedicated pool in loaded data.</div>`
                                            : invCompNpcWeaponWarn
                                                ? `<div class="dep-item-loot-warn" style="font-size: 9px; color: #d1c4e9; margin-top: 4px; line-height: 1.35; font-weight: 600;">NPC Only — npc_weapon basetag; not normal player-ground loot.</div>`
                                                : '';
                                    const invCompBadgesHTML = renderBadgePills(invCompBadges, { marginTop: 2, fontSize: 8 });
                                    const damageInfo = getPartDamageTypeAndAmmo(item);
                                    const partAspects = item.aspects || item.item?.aspects;
                                    const otherRefs = collectAllAspectRefs(partAspects);
                                    const otherResolved = resolveAspectRefs(otherRefs, data);
                                    const uiStatInfo = getUiStatDescriptionAndRedtext(partAspects, data);
                                    const damageAmmoHTML = buildPartStatsLineHTML(damageInfo, data, isSimpleView) + buildOtherStatsLineHTML(otherResolved, 5, isSimpleView) + buildUiStatDescriptionRedtextHTML(uiStatInfo);
                                    const rarityOutlineColor = tableName === 'inv_comp' ? getInvCompRarityOutlineColor(item, partDisplayName) : (tableName === 'barrel' ? compRarityOutlineColor : null);
                                    const invCompRarityStyle = rarityOutlineColor ? `border: var(--legit-rarity-border-w) solid ${rarityOutlineColor};` : '';
                                    const invCompCardStyle = `${invCompRarityStyle}${invCompLootWarnStyle}`;
                                    const advancedTagBlockHtml = hasTags ? `
                                                    <div class="dep-item-tag-block">
                                                        ${addTags.length > 0 ? `
                                                            <div class="dep-item-tag-line">
                                                                <span style="color: var(--accent-secondary); font-weight: bold;">+</span>
                                                                <span style="color: var(--text-secondary); margin-left: 2px;">${addTags.join(', ')}</span>
                                                            </div>
                                                        ` : ''}
                                                        ${baseTags.length > 0 ? `
                                                            <div class="dep-item-tag-line">
                                                                <span style="color: var(--accent-primary); font-weight: bold;">◆</span>
                                                                <span style="color: var(--text-secondary); margin-left: 2px;">${baseTags.join(', ')}</span>
                                                            </div>
                                                        ` : ''}
                                                        ${depTags.length > 0 ? `
                                                            <div class="dep-item-tag-line">
                                                                <span style="color: var(--accent-warning); font-weight: bold;">→</span>
                                                                <span style="color: var(--text-secondary); margin-left: 2px;">${depTags.join(', ')}</span>
                                                            </div>
                                                        ` : ''}
                                                        ${exclTags.length > 0 ? `
                                                            <div class="dep-item-tag-line">
                                                                <span style="color: var(--accent-danger); font-weight: bold;">✗</span>
                                                                <span style="color: var(--text-secondary); margin-left: 2px;">${exclTags.join(', ')}</span>
                                                            </div>
                                                        ` : ''}
                                                    </div>
                                                ` : '';
                                    const skillDescHtmlAdvanced = (skillInfo && skillInfo.description) ? `<div class="dep-item-skill-desc">${formatUiStatTextToHtml(skillInfo.description)}</div>` : '';
                                    const advancedDetailsInner = skillDescHtmlAdvanced + invCompBadgesHTML + damageAmmoHTML + advancedTagBlockHtml;
                                    const hasUiStatContent = !!(uiStatInfo && ((uiStatInfo.descriptionTexts && uiStatInfo.descriptionTexts.length > 0) || (uiStatInfo.redtexts && uiStatInfo.redtexts.length > 0) || (uiStatInfo.primaryTexts && uiStatInfo.primaryTexts.length > 0)));
                                    const showAdvancedDetails = invCompBadges.length > 0 || (damageAmmoHTML && damageAmmoHTML.trim().length > 0) || hasTags || !!skillDescHtmlAdvanced || hasUiStatContent;
                                    const advancedDetailsSection = showAdvancedDetails ? `<div class="dep-item-advanced-block"><div class="dep-item-details-body">${advancedDetailsInner}</div></div>` : '';
                                    const depSearchAttr = escapeLegitHtml(
                                        buildDepItemSearchText(item, partDisplayName, {
                                            skillInfo: skillInfo,
                                            serialFormat: serialFormat,
                                            uiStatInfo: uiStatInfo,
                                            otherResolved: otherResolved,
                                            addTags: addTags,
                                            depTags: depTags,
                                            exclTags: exclTags,
                                            baseTags: baseTags
                                        })
                                    );

                                    if (isSimpleView) {
                                        // Simple view: cleaner part display with all tags (add, dependency, exclusion)
                                        return `
                                            <div class="dep-item ${selectableClass} ${selectedClass} ${invalidClass} ${orderConflictClass} ${disabledClass}" style="${invCompCardStyle}" data-part-id="${partId}" data-root-key="${root.key}" data-part-key="${item.key}" data-table-name="${tableName}" data-is-selectable="${isSelectable}" data-dep-search="${depSearchAttr}" title="${tooltipText}">
                                                ${showPassiveSkillRow ? `
                                                    <div class="dep-item-skill-row${isSlotDisabled ? ' legit-opacity-50' : ''}">
                                                        ${skillImageUrl ? `<img class="dep-item-skill-icon dep-item-skill-icon--sm" src="${skillImageUrl}" alt="${passiveIconAlt}" onerror="this.style.opacity='0.25';">` : ''}
                                                        <div class="dep-item-skill-meta">
                                                            <div class="dep-item-head">
                                                                ${(skillInfo && (skillInfo.name || skillInfo.skill_name)) ? (skillInfo.name || skillInfo.skill_name) : partDisplayName}${serialFormat ? `<span class="dep-item-serial-muted">${serialFormat}</span>` : ''}
                                                                ${isSelected ? ' ✓' : ''}
                                                                ${selectedStateIcon}
                                                                ${isSlotDisabled ? ' (Disabled)' : ''}
                                                            </div>
                                                            ${skillInfo && skillInfo.tree_name ? `<div class="dep-item-skill-sub">${skillInfo.tree_name} - ${skillInfo.tier || ''}</div>` : ''}
                                                            ${skillInfo && skillInfo.description ? `<div class="dep-item-skill-desc dep-item-skill-desc--simple" style="font-size:10px;line-height:1.35;color:var(--text-secondary);margin-top:5px;font-weight:500;">${formatUiStatTextToHtml(skillInfo.description)}</div>` : ''}
                                                        </div>
                                                        ${partDebugDescHtml}
                                                    </div>
                                                ` : (tableName === 'passive_points' && (!skillInfo || item.key.toLowerCase().startsWith('passive_'))) ? `
                                                    <div class="dep-item-passive-warn">
                                                        <div class="dep-item-passive-warn-title">
                                                            ${partDisplayName}${serialFormat ? `<span class="dep-item-serial-muted">${serialFormat}</span>` : ''}
                                                            ${isSelected ? ' ✓' : ''}
                                                            ${selectedStateIcon}
                                                            ${isSlotDisabled ? ' (Disabled)' : ''}
                                                        </div>
                                                        <div class="dep-item-passive-warn-meta">
                                                            <span>⚠</span>
                                                            <span>Mapping Not Found</span>
                                                            ${skillDebugInfo && skillDebugInfo.serialId ? ` (Serial: ${skillDebugInfo.serialId})` : serialFormat ? ` (Serial: ${serialFormat.replace(/[{}]/g, '')})` : ''}
                                                            ${skillDebugInfo && skillDebugInfo.rootSerial !== null && skillDebugInfo.rootSerial !== undefined ? ` | Root: ${skillDebugInfo.rootSerial}` : ''}
                                                            ${skillDebugInfo && skillDebugInfo.subSerial !== null && skillDebugInfo.subSerial !== undefined ? ` | Sub: ${skillDebugInfo.subSerial}` : ''}
                                                        </div>
                                                        ${partDebugDescHtml}
                                                    </div>
                                                ` : `
                                                    <div class="dep-item-head${isSlotDisabled ? ' legit-opacity-50' : ''}">
                                                        ${partDisplayName}${serialFormat ? `<span class="dep-item-serial-muted">${serialFormat}</span>` : ''}
                                                        ${isSelected ? ' ✓' : ''}
                                                        ${selectedStateIcon}
                                                        ${isSlotDisabled ? ' (Disabled)' : ''}
                                                    </div>
                                                ${partDebugDescHtml}
                                                `}
                                                ${invCompLootWarnLineHtml}
                                                ${invCompBadgesHTML}
                                                ${damageAmmoHTML}
                                                ${hasTags ? `
                                                    <div class="dep-item-tag-block--simple">
                                                        ${addTags.length > 0 ? `
                                                            <div class="dep-item-tag-line">
                                                                <span style="color: var(--accent-secondary);">+</span> ${addTags.slice(0, 3).join(', ')}${addTags.length > 3 ? '...' : ''}
                                                            </div>
                                                        ` : ''}
                                                        ${baseTags.length > 0 ? `
                                                            <div class="dep-item-tag-line">
                                                                <span style="color: var(--accent-primary);">◆</span> ${baseTags.slice(0, 3).join(', ')}${baseTags.length > 3 ? '...' : ''}
                                                            </div>
                                                        ` : ''}
                                                        ${depTags.length > 0 ? `
                                                            <div class="dep-item-tag-line">
                                                                <span style="color: var(--accent-warning);">→</span> ${depTags.slice(0, 3).join(', ')}${depTags.length > 3 ? '...' : ''}
                                                            </div>
                                                        ` : ''}
                                                        ${exclTags.length > 0 ? `
                                                            <div class="dep-item-tag-line">
                                                                <span style="color: var(--accent-danger);">✗</span> ${exclTags.slice(0, 3).join(', ')}${exclTags.length > 3 ? '...' : ''}
                                                            </div>
                                                        ` : ''}
                                                    </div>
                                                ` : ''}
                                            </div>
                                        `;
                                    } else {
                                        // Advanced view: header row + stats/tags always visible
                                        return `
                                            <div class="dep-item ${selectableClass} ${selectedClass} ${invalidClass} ${orderConflictClass} ${disabledClass}" style="${invCompCardStyle}" data-part-id="${partId}" data-root-key="${root.key}" data-part-key="${item.key}" data-table-name="${tableName}" data-is-selectable="${isSelectable}" data-dep-search="${depSearchAttr}" title="${tooltipText}">
                                                ${showPassiveSkillRow ? `
                                                    <div class="dep-item-skill-row dep-item-skill-row--start${isSlotDisabled ? ' legit-opacity-50' : ''}">
                                                        ${skillImageUrl ? `<img class="dep-item-skill-icon dep-item-skill-icon--lg" src="${skillImageUrl}" alt="${passiveIconAlt}" onerror="this.style.opacity='0.25';">` : ''}
                                                        <div class="dep-item-skill-meta">
                                                            <div class="dep-item-head--bold">
                                                                ${(skillInfo && (skillInfo.name || skillInfo.skill_name)) ? (skillInfo.name || skillInfo.skill_name) : partDisplayName}${serialFormat ? `<span class="dep-item-serial-muted">${serialFormat}</span>` : ''}${selectedStateIcon}${isSlotDisabled ? ' (Disabled)' : ''}
                                                            </div>
                                                            <div class="dep-item-skill-sub" style="margin-bottom: 2px;">${item.key}</div>
                                                            ${skillInfo && skillInfo.tree_name ? `<div class="dep-item-skill-sub">${skillInfo.tree_name} - ${skillInfo.tier || ''}</div>` : ''}
                                                            ${skillInfo && skillInfo.description ? `<div class="dep-item-skill-desc" style="font-size:10px;line-height:1.35;color:var(--text-secondary);margin-top:5px;font-weight:500;">${formatUiStatTextToHtml(skillInfo.description)}</div>` : ''}
                                                        </div>
                                                        ${partDebugDescHtml}
                                                    </div>
                                                ` : skillDebugInfo ? `
                                                    <div class="dep-item-skill-debug${isSlotDisabled ? ' legit-opacity-50' : ''}">
                                                        <div class="dep-item-skill-meta">
                                                            <div class="dep-item-skill-debug-title">
                                                                ${partDisplayName}${serialFormat ? `<span class="dep-item-serial-muted">${serialFormat}</span>` : ''}${selectedStateIcon}${isSlotDisabled ? ' (Disabled)' : ''}
                                                            </div>
                                                            <div class="dep-item-skill-debug-meta">
                                                                ⚠ ${skillDebugInfo.reason}${skillDebugInfo.serialId ? ` (Serial: ${skillDebugInfo.serialId})` : ''}${skillDebugInfo.rootSerial ? ` | Root Serial: ${skillDebugInfo.rootSerial}` : ''}${skillDebugInfo.subSerial ? ` | Sub Serial: ${skillDebugInfo.subSerial}` : ''}
                                                            </div>
                                                        </div>
                                                        ${partDebugDescHtml}
                                                    </div>
                                                ` : `
                                                    <div class="dep-item-head--bold${isSlotDisabled ? ' legit-opacity-50' : ''}">${partDisplayName}${serialFormat ? `<span class="dep-item-serial-muted">${serialFormat}</span>` : ''}${selectedStateIcon}${isSlotDisabled ? ' (Disabled)' : ''}</div>
                                                    ${partDebugDescHtml}
                                                `}
                                                ${invCompLootWarnLineHtml}${advancedDetailsSection}
                                            </div>
                                        `;
                                    }
                                }).join('')}
                            </div>
                            ${hiddenItemsHTML}
                        </div>
                    `;
                });
                depTablesHTML += '</div>';
            } else if (depEntries.length > 0) {
                depTablesHTML = '<div class="dep-tables">';
                const grouped = {};
                depEntries.forEach(item => {
                    if (!grouped[item.depTableName]) {
                        grouped[item.depTableName] = [];
                    }
                    grouped[item.depTableName].push(item);
                });
                // Define the game's part selection order (matches how items are built in-game)
                const partSelectionOrder = [
                    'inv_comp',
                    'body',
                    'body_acc',
                    'body_ele',
                    'barrel',
                    'barrel_acc',
                    'magazine',
                    'magazine_ted_thrown',
                    'scope',
                    'scope_acc',
                    'grip',
                    'foregrip',
                    'secondary_ele'
                ];
                
                // Sort grouped entries according to game's part selection order
                const sortedGrouped = Object.entries(grouped).sort((a, b) => {
                    const orderA = partSelectionOrder.indexOf(a[0].toLowerCase());
                    const orderB = partSelectionOrder.indexOf(b[0].toLowerCase());
                    
                    // If both are in the order list, sort by their position
                    if (orderA !== -1 && orderB !== -1) {
                        return orderA - orderB;
                    }
                    // If only A is in the order list, it comes first
                    if (orderA !== -1) {
                        return -1;
                    }
                    // If only B is in the order list, it comes first
                    if (orderB !== -1) {
                        return 1;
                    }
                    // If neither is in the order list, maintain alphabetical order
                    return a[0].localeCompare(b[0]);
                });
                
                sortedGrouped.forEach(([tableName, items]) => {
                    if (
                        shouldHideEndgameSlotOutsideAdvanced() &&
                        normalizeDepTableName(tableName) === 'endgame'
                    ) {
                        return;
                    }
                    // Rarity outline for barrel (and other parts): use selected inv_comp's rarity when part has no explicit rarity
                    const selectedPartKeysForComp = _sp().has(root.key) ? _sp().get(root.key) : new Set();
                    const invCompItemsForRarity = grouped['inv_comp'] || [];
                    const selectedCompEntry = invCompItemsForRarity.find(it => selectedPartKeysForComp.has(it.key)) || null;
                    let selectedCompDisplayName = selectedCompEntry
                        ? getLegitBuilderInvCompPartDisplayName(selectedCompEntry, data, root, false)
                        : '';
                    const compRarityOutlineColor = selectedCompEntry ? getInvCompRarityOutlineColor(selectedCompEntry, selectedCompDisplayName) : null;

                    // Filter items based on comp rules
                    // Always include selected parts, even if they become invalid (so they can be deselected)
                    let filteredItems = items;
                    let hiddenItems = []; // Track items hidden due to missing dependencies
                    const scopedCompRulesForSlot = getForwardScopedCompRulesForSlot(root, tableName, compRules);
                    const activeCompRules = scopedCompRulesForSlot || compRules;
                    
                    if (hasFilter && tableName !== 'inv_comp') {
                        const selectedPartKeys = _sp().has(root.key) ? _sp().get(root.key) : new Set();
                        // CRITICAL FIX: Check selection status FIRST, then validity
                        // This ensures only selected parts override exclusion rules
                        filteredItems = items.filter(item => {
                            const isSelected = selectedPartKeys.has(item.key);
                            
                            // CRITICAL: Only selected parts can override exclusion
                            // If part is selected, always show it (even if invalid) so user can deselect
                            if (isSelected) {
                                return true; // Force show selected parts - exit early
                            }
                            
                            // For non-selected parts: check validity (pass isSelected=false)
                            const isValid = isPartValid(item, activeCompRules, tableName, false);
                            
                            // Track hidden items for display (only for non-selected invalid items)
                            if (!isValid && activeCompRules) {
                                const formatTags = (tags) => {
                                    if (!tags || !Array.isArray(tags)) return [];
                                    return tags.map(tag => {
                                        if (typeof tag === 'object' && tag !== null) {
                                            const values = Object.values(tag);
                                            return values[0] || Object.keys(tag)[0] || '';
                                        }
                                        return tag;
                                    }).filter(Boolean);
                                };
                                
                                // Check for missing dependencies
                                const depTags = new Set(formatTags(item.dependencytags || []).map(t => t.toLowerCase()));
                                const missingDeps = depTags.size > 0 
                                    ? Array.from(depTags).filter(tag => !activeCompRules.tagPool.has(tag))
                                    : [];
                                
                                // Check for exclusion conflicts
                                const exclTags = new Set(formatTags(item.exclusiontags || []).map(t => t.toLowerCase()));
                                const addTags = new Set(formatTags(item.addtags || []).map(t => t.toLowerCase()));
                                const excludedBy = [];
                                
                                if (exclTags.size > 0) {
                                    // Find tags that are excluded and in the pool, but not self-excluded
                                    exclTags.forEach(exclTag => {
                                        // Don't count if this part adds the tag (self-exclusion)
                                        if (!addTags.has(exclTag) && activeCompRules.tagPool.has(exclTag)) {
                                            excludedBy.push(exclTag);
                                        }
                                    });
                                }
                                
                                // Only add to hidden items if there's a reason (missing deps or exclusions)
                                if (missingDeps.length > 0 || excludedBy.length > 0) {
                                    hiddenItems.push({
                                        item: item,
                                        missingTags: missingDeps,
                                        excludedBy: excludedBy
                                    });
                                }
                                // Hide this part - it's invalid and NOT selected
                                return false;
                            }
                            
                            // Part is valid - show it
                            return true;
                        });
                    }
                    
                    // Get partcount info for this table if available
                    // Only show part counts for actual part types (not dependency tables like inv_comp)
                    // Part types are those that appear in parttypeselectionrules
                    let partCountInfo = null;
                    let partCountDisplay = '';
                    
                    // Skip part count display for dependency tables (inv_comp, etc.)
                    // Only show for actual part types (body, core_augment, firmware, stat_group1, etc.)
                    const isPartType = tableName !== 'inv_comp';
                    
                    if (isPartType) {
                        // Priority: 1) compRules.partCounts (includes base generic root + manufacturer overrides)
                        //           2) basePartCounts (when no comp selected)
                        if (compRules && compRules.partCounts && compRules.partCounts.has(tableName)) {
                            partCountInfo = compRules.partCounts.get(tableName);
                        } else if (basePartCounts && basePartCounts.has(tableName)) {
                            partCountInfo = basePartCounts.get(tableName);
                        }
                    }
                    
                    if (partCountInfo) {
                        // Use display values if available, otherwise use effective values
                        const displayMin = partCountInfo.displayMin !== undefined ? partCountInfo.displayMin : partCountInfo.min;
                        const displayMax = partCountInfo.displayMax !== undefined ? partCountInfo.displayMax : partCountInfo.max;
                        const effectiveMin = partCountInfo.min;
                        const effectiveMax = partCountInfo.max;
                        const availableOptions = Array.isArray(filteredItems) ? filteredItems.length : 0;
                        const inferredRequired = isInferredRequiredSlot(partCountInfo, availableOptions, tableName);
                        
                        // Show display values if they exist
                        if (displayMin !== null && displayMax !== null) {
                            if (displayMin === displayMax) {
                                partCountDisplay = ` [Count: ${displayMin}]`;
                            } else {
                                partCountDisplay = ` [Count: ${displayMin}-${displayMax}]`;
                            }
                        } else if (displayMin !== null) {
                            // min 0 with no max means min 0, max 1 (show as "min 0" but effective is 0-1)
                            partCountDisplay = ` [Count: min ${displayMin} (assumed ${effectiveMin}-${effectiveMax})]`;
                        } else if (displayMax !== null) {
                            partCountDisplay = ` [Count: max ${displayMax} (assumed ${effectiveMin}-${effectiveMax})]`;
                        } else if (effectiveMin !== null && effectiveMax !== null) {
                            // Fallback to effective values
                            if (inferredRequired) {
                                partCountDisplay = ` [Count: 1 (inferred from available parts)]`;
                            } else if (effectiveMin === effectiveMax) {
                                partCountDisplay = ` [Count: ${effectiveMin}]`;
                            } else {
                                partCountDisplay = ` [Count: ${effectiveMin}-${effectiveMax}]`;
                            }
                        } else {
                            // Unknown - but effective is 1-1
                            partCountDisplay = ` [Count: Unknown (assumed ${effectiveMin}-${effectiveMax})]`;
                        }
                    } else {
                        // No partcount data available - Unknown, but assumed 1-1 for selection
                        partCountDisplay = ` [Count: Unknown (assumed 1-1)]`;
                    }
                    
                    // Build hidden items display if there are any
                    let hiddenItemsHTML = '';
                    if (hiddenItems.length > 0) {
                        const hasMissingDeps = hiddenItems.some(h => h.missingTags && h.missingTags.length > 0);
                        const hasExclusions = hiddenItems.some(h => h.excludedBy && h.excludedBy.length > 0);
                        const reasonText = hasMissingDeps && hasExclusions 
                            ? 'Missing dependency tags or excluded'
                            : hasExclusions 
                                ? 'Excluded by selected parts'
                                : 'Missing dependency tags';
                        const hiddenSectionLabel = `${hiddenItems.length} part(s) hidden - ${reasonText}`;
                        const hiddenSectionLabelAttr = hiddenSectionLabel.replace(/"/g, '&quot;');
                        
                        hiddenItemsHTML = `
                            <div class="legit-hidden-parts hidden-items-section">
                                <div class="legit-hidden-parts-toggle" role="button" tabindex="0" data-label="${hiddenSectionLabelAttr}" onclick="toggleLegitHiddenPartsSection(this)">▶ ${hiddenSectionLabel}</div>
                                <div class="legit-hidden-parts-body" style="display: none;">
                                    ${hiddenItems.map(hidden => {
                                        const reasons = [];
                                        if (hidden.missingTags && hidden.missingTags.length > 0) {
                                            reasons.push(`Missing: ${hidden.missingTags.join(', ')}`);
                                        }
                                        if (hidden.excludedBy && hidden.excludedBy.length > 0) {
                                            reasons.push(`Excluded by: ${hidden.excludedBy.join(', ')}`);
                                        }
                                        
                                        return `
                                            <div class="legit-hidden-reason-card">
                                                <div class="legit-hidden-reason-title">${hidden.item.key}</div>
                                                ${reasons.map(reason => `<div class="legit-hidden-reason-line">${reason}</div>`).join('')}
                                            </div>
                                        `;
                                    }).join('')}
                                </div>
                            </div>
                        `;
                    }

                    const simpleHideUnselectedWhenSlotFull = shouldSimpleHideUnselectedWhenSlotFull(
                        root,
                        tableName,
                        items,
                        partCountInfo,
                        compRules,
                        basePartCounts
                    );
                    const selectedInFilteredCount = filteredItems.filter(function (item) {
                        return _sp().has(root.key) && _sp().get(root.key).has(item.key);
                    }).length;
                    const depItemVisibleCount = simpleHideUnselectedWhenSlotFull
                        ? selectedInFilteredCount
                        : filteredItems.length;
                    const showDepTableSearch = filteredItems.length > 10;
                    const depTableSearchRowHtml = showDepTableSearch
                        ? buildDepTableSearchRowHtml(tableName, filteredItems.length)
                        : '';
                    
                    depTablesHTML += `
                        <div class="dep-table" data-dep-table="${escapeLegitHtml(tableName)}">
                            <div class="dep-table-header">
                                <span>${tableName}${hasFilter && tableName !== 'inv_comp' ? ` (${filteredItems.length}/${items.length})` : ''}${partCountDisplay}</span>
                                <span>${depItemVisibleCount} items</span>
                            </div>
                            ${depTableSearchRowHtml}
                            <div class="dep-items">
                                ${filteredItems.map(item => {
                                    const isComp = item.key && item.key.toLowerCase().startsWith('comp');
                                    
                                    // Check if this slot is disabled (partcount min=0, max=0)
                                    // Use base generic root part counts if no comp selected
                                    let partCountInfo = compRules && compRules.partCounts ? compRules.partCounts.get(tableName) : null;
                                    if (!partCountInfo && basePartCounts && basePartCounts.has(tableName)) {
                                        partCountInfo = basePartCounts.get(tableName);
                                    }
                                    const isSlotDisabled = !(window.__lbCtx && window.__lbCtx.relaxedPicker) && partCountInfo && partCountInfo.min === 0 && partCountInfo.max === 0;
                                    
                                    const isSelectable = !isSlotDisabled; // Disable if slot has count 0-0
                                    const partId = `${root.key || 'unknown'}_${item.key || 'unknown'}`;
                                    const isSelected = _sp().has(root.key) && _sp().get(root.key).has(item.key);
                                    if (simpleHideUnselectedWhenSlotFull && !isSelected) {
                                        return '';
                                    }
                                    
                                    const selectedValidityState = getSelectedPartValidityState(item, activeCompRules, tableName, isSelected, root);
                                    const selectableClass = isSelectable ? 'selectable' : '';
                                    const selectedClass = isSelected ? 'selected' : '';
                                    const invalidClass = isSelected && selectedValidityState.status === 'hard-invalid' ? 'invalid-selected' : '';
                                    const orderConflictClass = isSelected && selectedValidityState.status === 'order-conflict' ? 'order-conflict-selected' : '';
                                    const disabledClass = isSlotDisabled ? 'disabled' : '';
                                    const selectedStateIcon = isSelected && selectedValidityState.status === 'hard-invalid'
                                        ? ' ⛔'
                                        : (isSelected && selectedValidityState.status === 'order-conflict' ? ' ⚠' : '');
                                    
                                    // Format tags for display
                                    const formatTags = (tags) => {
                                        if (!tags || !Array.isArray(tags)) return [];
                                        return tags.map(tag => {
                                            if (typeof tag === 'object' && tag !== null) {
                                                const values = Object.values(tag);
                                                return values[0] || Object.keys(tag)[0] || '';
                                            }
                                            return tag;
                                        }).filter(Boolean);
                                    };
                                    
                                    const addTags = formatTags(item.addtags || []);
                                    const depTags = formatTags(item.dependencytags || []);
                                    const exclTags = formatTags(item.exclusiontags || []);
                                    const hasTags = addTags.length > 0 || depTags.length > 0 || exclTags.length > 0;
                                    
                                    // Check if we're in simple view
                                    const isSimpleView = localStorage.getItem('rootDefinitionsViewMode') === 'simple';
                                    
                                    // Get friendly part name (remove common prefixes)
                                    const getFriendlyPartName = (key) => {
                                        if (!key) return 'Unknown';
                                        // Remove common prefixes like "Part_", "Inv_", "Comp_", etc.
                                        return key.replace(/^(Part_|Inv_|Comp_)/i, '').replace(/_/g, ' ').replace(/\b\w/g, l => l.toUpperCase());
                                    };
                                    
                                    // Always show serial index for all parts
                                    // Format: {serial} for parts from current root, {rootSerial:subSerial} for parts from different roots
                                    let serialFormat = '';
                                    const itemRootSerial = item.rootSerialIndex !== undefined ? item.rootSerialIndex : (item.rootKey && data && data.rootDefinitions ? (data.rootDefinitions.find(r => r.key === item.rootKey)?.serialIndex) : null);
                                    const itemSubSerial = item.serialIndex;
                                    const currentRootSerial = root.serialIndex;
                                    if (itemSubSerial !== null && itemSubSerial !== undefined) {
                                        if (itemRootSerial !== null && itemRootSerial !== undefined && itemRootSerial !== currentRootSerial) {
                                            // Part comes from a different root - show {rootSerial:subSerial} format
                                            serialFormat = ` {${itemRootSerial}:${itemSubSerial}}`;
                                        } else {
                                            // Part from current root - show {serial} format
                                            serialFormat = ` {${itemSubSerial}}`;
                                        }
                                    }
                                    
                                    // Check if this is a passive_point and get skill info
                                    let skillInfo = null;
                                    let skillImageUrl = null;
                                    let skillDebugInfo = null; // Store debug info for unmapped skills
                                    if (tableName === 'passive_points' && itemSubSerial !== null) {
                                        // For passive_points, determine root serial
                                        let passiveRootSerial = itemRootSerial;
                                        
                                        // If root serial not set, try to find it from the root
                                        if (passiveRootSerial === null || passiveRootSerial === undefined) {
                                            // Check if current root is a classmod root
                                            const rootKeyLower = (root.key || '').toLowerCase();
                                            if (rootKeyLower.includes('classmod')) {
                                                passiveRootSerial = root.serialIndex;
                                            } else {
                                                // Try to find the passive_point in subItemsByTable to get its root
                                                if (data && data.subItemsByTable && data.subItemsByTable.passive_points) {
                                                    const passiveItem = data.subItemsByTable.passive_points.items?.find(p => 
                                                        p.serialIndex === itemSubSerial || 
                                                        (p.key && p.key.toLowerCase() === (item.key || '').toLowerCase())
                                                    );
                                                    if (passiveItem && passiveItem.rootKey) {
                                                        const passiveRoot = data.rootDefinitions?.find(r => r.key === passiveItem.rootKey);
                                                        if (passiveRoot) {
                                                            passiveRootSerial = passiveRoot.serialIndex;
                                                        }
                                                    } else if (passiveItem && passiveItem.rootSerialIndex !== undefined) {
                                                        passiveRootSerial = passiveItem.rootSerialIndex;
                                                    }
                                                }
                                                
                                                // If still not found, try all classmod root serials (255, 254, 253, 252)
                                                if ((passiveRootSerial === null || passiveRootSerial === undefined) && skillMapping && skillMapping.size > 0) {
                                                    const classmodSerials = [255, 254, 253, 252, 404];
                                                    for (const rootSerial of classmodSerials) {
                                                        const testSerialId = `${rootSerial}:${itemSubSerial}`;
                                                        if (skillMapping.has(testSerialId)) {
                                                            passiveRootSerial = rootSerial;
                                                            break;
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                        
                                        if (passiveRootSerial !== null && passiveRootSerial !== undefined) {
                                            const serialId = `${passiveRootSerial}:${itemSubSerial}`;
                                            skillInfo = getSkillInfo(serialId);
                                            
                                            // NOTE: Removed fallback that tries all classmod root serials
                                            // This was causing unmapped skills to appear as mapped when they matched a different character's skill
                                            // Skills should only match their exact serial ID for the current character's root serial
                                            // If a skill is not found with the determined root serial, it's truly unmapped for that character
                                            
                                            // Debug: Log when skill is not found
                                            if (!skillInfo) {
                                                // Check if the serial ID exists in the mapping (for debugging)
                                                const hasExact = skillMapping.has(serialId);
                                                const sampleKeys = Array.from(skillMapping.keys()).slice(0, 5);
                                                console.warn(`[Skill Lookup Failed] Serial: ${serialId}, Key: ${item.key}, HasExact: ${hasExact}, SampleKeys:`, sampleKeys);
                                            }
                                            
                                            if (skillInfo) {
                                                // Skill is mapped - check if it has valid data
                                                // Empty skills (with no name/description) are valid - they're just empty placeholders
                                                if (skillInfo.name || skillInfo.skill_name || skillInfo.description) {
                                                    skillImageUrl = getSkillImageUrl(skillInfo.name || skillInfo.skill_name, skillInfo.character);
                                                }
                                                // If skillInfo exists but has no data, that's fine - it's an empty skill
                                                // Don't log a warning for these
                                            } else {
                                                // Only log warning if skill is truly not in mapping
                                                // Check if it might be an empty skill that was intentionally not mapped
                                                const isInMapping = skillMapping.has(serialId) || 
                                                    Array.from(skillMapping.keys()).some(key => {
                                                        if (typeof key === 'string' && key.includes(':')) {
                                                            const [r, s] = key.split(':');
                                                            return (r === passiveRootSerial.toString() || parseInt(r) === passiveRootSerial) &&
                                                                   (s === itemSubSerial.toString() || parseInt(s) === itemSubSerial);
                                                        }
                                                        return key === itemSubSerial || key === itemSubSerial.toString();
                                                    });
                                                
                                                // Only log if truly unmapped (not in mapping at all)
                                                if (!isInMapping) {
                                                    skillDebugInfo = {
                                                        partKey: item.key,
                                                        serialId: serialId,
                                                        rootSerial: passiveRootSerial,
                                                        subSerial: itemSubSerial,
                                                        rootKey: root.key,
                                                        reason: 'Skill not found in skillMapping'
                                                    };
                                                    console.warn('[Skill Debug] Unmapped passive_point:', skillDebugInfo);
                                                }
                                            }
                                        } else {
                                            // Debug: Log missing root serial
                                            skillDebugInfo = {
                                                partKey: item.key,
                                                subSerial: itemSubSerial,
                                                rootKey: root.key,
                                                itemRootSerial: itemRootSerial,
                                                reason: 'Could not determine root serial'
                                            };
                                            console.warn('[Skill Debug] Missing root serial for passive_point:', skillDebugInfo);
                                        }
                                    }
                                    if (tableName === 'passive_points' && !skillInfo && data) {
                                        skillInfo = getSkillInfoForPassive(item, data);
                                        if (skillInfo && (skillInfo.name || skillInfo.skill_name) && skillInfo.character) {
                                            skillImageUrl = getSkillImageUrl(skillInfo.name || skillInfo.skill_name, skillInfo.character);
                                        }
                                    }
                                    if (tableName === 'passive_points' && data) {
                                        skillInfo = enrichPassiveSkillInfoWithNexusUitooltip(skillInfo, item, data);
                                        if (skillInfo && (skillInfo.name || skillInfo.skill_name) && skillInfo.character && !skillImageUrl) {
                                            skillImageUrl = getSkillImageUrl(skillInfo.name || skillInfo.skill_name, skillInfo.character);
                                        }
                                        const cdnPassiveIcon = getPassiveUiresourcesIconUrl(item, data, tableName);
                                        if (cdnPassiveIcon) skillImageUrl = cdnPassiveIcon;
                                    }

                                    let partDisplayName =
                                        tableName === 'inv_comp'
                                            ? getLegitBuilderInvCompPartDisplayName(item, data, root, isSimpleView)
                                            : getDisplayNameForPart(item, data.invNamePartDisplayNames) ||
                                              (isSimpleView ? getFriendlyPartName(item.key) : item.key);
                                    const passiveIconAlt = escapeLegitHtml(String(
                                        (skillInfo && (skillInfo.name || skillInfo.skill_name)) ? (skillInfo.name || skillInfo.skill_name) : (partDisplayName || '')
                                    ));
                                    const showPassiveSkillRow = tableName === 'passive_points' && (!!skillInfo || !!skillImageUrl);
                                    const partDebugDesc = getPartDebugDisplayDescription(item);
                                    const partDebugDescHtml = partDebugDesc
                                        ? `<div class="dep-item-debug-display" style="font-size: 10px; font-weight: 600; color: ${LEGIT_DEBUG_DESC_FG}; margin-top: 4px; line-height: 1.35; text-shadow: 0 1px 2px rgba(0,0,0,0.75);">${escapeLegitHtml(partDebugDesc)}</div>`
                                        : '';
                                    const baseTooltipText = isSimpleView 
                                        ? (isSlotDisabled ? 'Slot disabled - cannot select' : 'Click to select this part')
                                        : `depIndex: ${item.depIndex}, serialIndex: ${item.serialIndex}${isSlotDisabled ? ' (Slot disabled - Count: 0)' : ''}${skillInfo ? ` | Skill: ${skillInfo.name}` : ''}`;
                                    const warningTooltip = isSelected && selectedValidityState.status !== 'valid'
                                        ? ` | ${selectedValidityState.message}`
                                        : '';
                                    const tooltipText = `${baseTooltipText}${warningTooltip}`;
                                    const invCompLootCtx = tableName === 'inv_comp'
                                        ? { rootKey: (item.rootKey && String(item.rootKey).trim()) || root.key }
                                        : {};
                                    const invCompBadges = tableName === 'inv_comp'
                                        ? getInvCompBadges(item, { compact: true }, data, invCompLootCtx)
                                        : [];
                                    const invCompGenericLegendaryWarn = tableName === 'inv_comp' && isBareGenericLegendaryInvalidDrop(item, invCompLootCtx);
                                    const invCompMissingLootWarn = tableName === 'inv_comp' && isInvCompMissingIndexedLootSource(item, data, invCompLootCtx);
                                    const invCompNpcWeaponWarn = tableName === 'inv_comp' && entryHasNpcWeaponBasetag(item);
                                    const invCompLootWarnStyle = invCompGenericLegendaryWarn
                                        ? ' box-shadow: inset 4px 0 0 #ff7043; background: rgba(255, 112, 67, 0.08);'
                                        : invCompMissingLootWarn
                                            ? ' box-shadow: inset 4px 0 0 #ff9800; background: rgba(255, 152, 0, 0.07);'
                                            : invCompNpcWeaponWarn
                                                ? ' box-shadow: inset 4px 0 0 #b388ff; background: rgba(179, 136, 255, 0.1);'
                                                : '';
                                    const invCompLootWarnLineHtml = invCompGenericLegendaryWarn
                                        ? `<div class="dep-item-loot-warn" style="font-size: 9px; color: #ff8a65; margin-top: 4px; line-height: 1.35; font-weight: 600;">✗ GENERIC Legendary — invalid drop. Use a unique comp (comp_05_legendary_*), not bare comp_05_legendary.</div>`
                                        : invCompMissingLootWarn
                                            ? `<div class="dep-item-loot-warn" style="font-size: 9px; color: #ffb74d; margin-top: 4px; line-height: 1.35; font-weight: 600;">⚠ Likely no normal drop source — global pool excluded and no dedicated pool in loaded data.</div>`
                                            : invCompNpcWeaponWarn
                                                ? `<div class="dep-item-loot-warn" style="font-size: 9px; color: #d1c4e9; margin-top: 4px; line-height: 1.35; font-weight: 600;">NPC Only — npc_weapon basetag; not normal player-ground loot.</div>`
                                                : '';
                                    const invCompBadgesHTML = renderBadgePills(invCompBadges, { marginTop: 2, fontSize: 8 });
                                    const damageInfo = getPartDamageTypeAndAmmo(item);
                                    const partAspects = item.aspects || item.item?.aspects;
                                    const otherRefs = collectAllAspectRefs(partAspects);
                                    const otherResolved = resolveAspectRefs(otherRefs, data);
                                    const uiStatInfo = getUiStatDescriptionAndRedtext(partAspects, data);
                                    const damageAmmoHTML = buildPartStatsLineHTML(damageInfo, data, isSimpleView) + buildOtherStatsLineHTML(otherResolved, 5, isSimpleView) + buildUiStatDescriptionRedtextHTML(uiStatInfo);
                                    const rarityOutlineColor = tableName === 'inv_comp' ? getInvCompRarityOutlineColor(item, partDisplayName) : (tableName === 'barrel' ? compRarityOutlineColor : null);
                                    const invCompRarityStyle = rarityOutlineColor ? `border: var(--legit-rarity-border-w) solid ${rarityOutlineColor};` : '';
                                    const invCompCardStyle = `${invCompRarityStyle}${invCompLootWarnStyle}`;
                                    const advancedTagBlockHtml = hasTags ? `
                                                    <div class="dep-item-tag-block">
                                                        ${addTags.length > 0 ? `
                                                            <div class="dep-item-tag-line">
                                                                <span style="color: var(--accent-secondary); font-weight: bold;">+</span>
                                                                <span style="color: var(--text-secondary); margin-left: 2px;">${addTags.join(', ')}</span>
                                                            </div>
                                                        ` : ''}
                                                        ${baseTags.length > 0 ? `
                                                            <div class="dep-item-tag-line">
                                                                <span style="color: var(--accent-primary); font-weight: bold;">◆</span>
                                                                <span style="color: var(--text-secondary); margin-left: 2px;">${baseTags.join(', ')}</span>
                                                            </div>
                                                        ` : ''}
                                                        ${depTags.length > 0 ? `
                                                            <div class="dep-item-tag-line">
                                                                <span style="color: var(--accent-warning); font-weight: bold;">→</span>
                                                                <span style="color: var(--text-secondary); margin-left: 2px;">${depTags.join(', ')}</span>
                                                            </div>
                                                        ` : ''}
                                                        ${exclTags.length > 0 ? `
                                                            <div class="dep-item-tag-line">
                                                                <span style="color: var(--accent-danger); font-weight: bold;">✗</span>
                                                                <span style="color: var(--text-secondary); margin-left: 2px;">${exclTags.join(', ')}</span>
                                                            </div>
                                                        ` : ''}
                                                    </div>
                                                ` : '';
                                    const skillDescHtmlAdvanced = (skillInfo && skillInfo.description) ? `<div class="dep-item-skill-desc">${formatUiStatTextToHtml(skillInfo.description)}</div>` : '';
                                    const advancedDetailsInner = skillDescHtmlAdvanced + invCompBadgesHTML + damageAmmoHTML + advancedTagBlockHtml;
                                    const hasUiStatContent = !!(uiStatInfo && ((uiStatInfo.descriptionTexts && uiStatInfo.descriptionTexts.length > 0) || (uiStatInfo.redtexts && uiStatInfo.redtexts.length > 0) || (uiStatInfo.primaryTexts && uiStatInfo.primaryTexts.length > 0)));
                                    const showAdvancedDetails = invCompBadges.length > 0 || (damageAmmoHTML && damageAmmoHTML.trim().length > 0) || hasTags || !!skillDescHtmlAdvanced || hasUiStatContent;
                                    const advancedDetailsSection = showAdvancedDetails ? `<div class="dep-item-advanced-block"><div class="dep-item-details-body">${advancedDetailsInner}</div></div>` : '';
                                    const depSearchAttr = escapeLegitHtml(
                                        buildDepItemSearchText(item, partDisplayName, {
                                            skillInfo: skillInfo,
                                            serialFormat: serialFormat,
                                            uiStatInfo: uiStatInfo,
                                            otherResolved: otherResolved,
                                            addTags: addTags,
                                            depTags: depTags,
                                            exclTags: exclTags,
                                            baseTags: baseTags
                                        })
                                    );

                                    if (isSimpleView) {
                                        // Simple view: cleaner part display with all tags (add, dependency, exclusion)
                                        return `
                                            <div class="dep-item ${selectableClass} ${selectedClass} ${invalidClass} ${orderConflictClass} ${disabledClass}" style="${invCompCardStyle}" data-part-id="${partId}" data-root-key="${root.key}" data-part-key="${item.key}" data-table-name="${tableName}" data-is-selectable="${isSelectable}" data-dep-search="${depSearchAttr}" title="${tooltipText}">
                                                ${showPassiveSkillRow ? `
                                                    <div class="dep-item-skill-row${isSlotDisabled ? ' legit-opacity-50' : ''}">
                                                        ${skillImageUrl ? `<img class="dep-item-skill-icon dep-item-skill-icon--sm" src="${skillImageUrl}" alt="${passiveIconAlt}" onerror="this.style.opacity='0.25';">` : ''}
                                                        <div class="dep-item-skill-meta">
                                                            <div class="dep-item-head">
                                                                ${(skillInfo && (skillInfo.name || skillInfo.skill_name)) ? (skillInfo.name || skillInfo.skill_name) : partDisplayName}${serialFormat ? `<span class="dep-item-serial-muted">${serialFormat}</span>` : ''}
                                                                ${isSelected ? ' ✓' : ''}
                                                                ${selectedStateIcon}
                                                                ${isSlotDisabled ? ' (Disabled)' : ''}
                                                            </div>
                                                            ${skillInfo && skillInfo.tree_name ? `<div class="dep-item-skill-sub">${skillInfo.tree_name} - ${skillInfo.tier || ''}</div>` : ''}
                                                            ${skillInfo && skillInfo.description ? `<div class="dep-item-skill-desc dep-item-skill-desc--simple" style="font-size:10px;line-height:1.35;color:var(--text-secondary);margin-top:5px;font-weight:500;">${formatUiStatTextToHtml(skillInfo.description)}</div>` : ''}
                                                        </div>
                                                        ${partDebugDescHtml}
                                                    </div>
                                                ` : (tableName === 'passive_points' && (!skillInfo || item.key.toLowerCase().startsWith('passive_'))) ? `
                                                    <div class="dep-item-passive-warn">
                                                        <div class="dep-item-passive-warn-title">
                                                            ${partDisplayName}${serialFormat ? `<span class="dep-item-serial-muted">${serialFormat}</span>` : ''}
                                                            ${isSelected ? ' ✓' : ''}
                                                            ${selectedStateIcon}
                                                            ${isSlotDisabled ? ' (Disabled)' : ''}
                                                        </div>
                                                        <div class="dep-item-passive-warn-meta">
                                                            <span>⚠</span>
                                                            <span>Mapping Not Found</span>
                                                            ${skillDebugInfo && skillDebugInfo.serialId ? ` (Serial: ${skillDebugInfo.serialId})` : serialFormat ? ` (Serial: ${serialFormat.replace(/[{}]/g, '')})` : ''}
                                                            ${skillDebugInfo && skillDebugInfo.rootSerial !== null && skillDebugInfo.rootSerial !== undefined ? ` | Root: ${skillDebugInfo.rootSerial}` : ''}
                                                            ${skillDebugInfo && skillDebugInfo.subSerial !== null && skillDebugInfo.subSerial !== undefined ? ` | Sub: ${skillDebugInfo.subSerial}` : ''}
                                                        </div>
                                                        ${partDebugDescHtml}
                                                    </div>
                                                ` : `
                                                    <div class="dep-item-head${isSlotDisabled ? ' legit-opacity-50' : ''}">
                                                        ${partDisplayName}${serialFormat ? `<span class="dep-item-serial-muted">${serialFormat}</span>` : ''}
                                                        ${isSelected ? ' ✓' : ''}
                                                        ${selectedStateIcon}
                                                        ${isSlotDisabled ? ' (Disabled)' : ''}
                                                    </div>
                                                ${partDebugDescHtml}
                                                `}
                                                ${invCompLootWarnLineHtml}
                                                ${invCompBadgesHTML}
                                                ${damageAmmoHTML}
                                                ${hasTags ? `
                                                    <div class="dep-item-tag-block--simple">
                                                        ${addTags.length > 0 ? `
                                                            <div class="dep-item-tag-line">
                                                                <span style="color: var(--accent-secondary);">+</span> ${addTags.slice(0, 3).join(', ')}${addTags.length > 3 ? '...' : ''}
                                                            </div>
                                                        ` : ''}
                                                        ${baseTags.length > 0 ? `
                                                            <div class="dep-item-tag-line">
                                                                <span style="color: var(--accent-primary);">◆</span> ${baseTags.slice(0, 3).join(', ')}${baseTags.length > 3 ? '...' : ''}
                                                            </div>
                                                        ` : ''}
                                                        ${depTags.length > 0 ? `
                                                            <div class="dep-item-tag-line">
                                                                <span style="color: var(--accent-warning);">→</span> ${depTags.slice(0, 3).join(', ')}${depTags.length > 3 ? '...' : ''}
                                                            </div>
                                                        ` : ''}
                                                        ${exclTags.length > 0 ? `
                                                            <div class="dep-item-tag-line">
                                                                <span style="color: var(--accent-danger);">✗</span> ${exclTags.slice(0, 3).join(', ')}${exclTags.length > 3 ? '...' : ''}
                                                            </div>
                                                        ` : ''}
                                                    </div>
                                                ` : ''}
                                            </div>
                                        `;
                                    } else {
                                        // Advanced view: header row + stats/tags always visible
                                        return `
                                            <div class="dep-item ${selectableClass} ${selectedClass} ${invalidClass} ${orderConflictClass} ${disabledClass}" style="${invCompCardStyle}" data-part-id="${partId}" data-root-key="${root.key}" data-part-key="${item.key}" data-table-name="${tableName}" data-is-selectable="${isSelectable}" data-dep-search="${depSearchAttr}" title="${tooltipText}">
                                                ${showPassiveSkillRow ? `
                                                    <div class="dep-item-skill-row dep-item-skill-row--start${isSlotDisabled ? ' legit-opacity-50' : ''}">
                                                        ${skillImageUrl ? `<img class="dep-item-skill-icon dep-item-skill-icon--lg" src="${skillImageUrl}" alt="${passiveIconAlt}" onerror="this.style.opacity='0.25';">` : ''}
                                                        <div class="dep-item-skill-meta">
                                                            <div class="dep-item-head--bold">
                                                                ${(skillInfo && (skillInfo.name || skillInfo.skill_name)) ? (skillInfo.name || skillInfo.skill_name) : partDisplayName}${serialFormat ? `<span class="dep-item-serial-muted">${serialFormat}</span>` : ''}${selectedStateIcon}${isSlotDisabled ? ' (Disabled)' : ''}
                                                            </div>
                                                            <div class="dep-item-skill-sub" style="margin-bottom: 2px;">${item.key}</div>
                                                            ${skillInfo && skillInfo.tree_name ? `<div class="dep-item-skill-sub">${skillInfo.tree_name} - ${skillInfo.tier || ''}</div>` : ''}
                                                            ${skillInfo && skillInfo.description ? `<div class="dep-item-skill-desc" style="font-size:10px;line-height:1.35;color:var(--text-secondary);margin-top:5px;font-weight:500;">${formatUiStatTextToHtml(skillInfo.description)}</div>` : ''}
                                                        </div>
                                                        ${partDebugDescHtml}
                                                    </div>
                                                ` : skillDebugInfo ? `
                                                    <div class="dep-item-skill-debug${isSlotDisabled ? ' legit-opacity-50' : ''}">
                                                        <div class="dep-item-skill-meta">
                                                            <div class="dep-item-skill-debug-title">
                                                                ${partDisplayName}${serialFormat ? `<span class="dep-item-serial-muted">${serialFormat}</span>` : ''}${selectedStateIcon}${isSlotDisabled ? ' (Disabled)' : ''}
                                                            </div>
                                                            <div class="dep-item-skill-debug-meta">
                                                                ⚠ ${skillDebugInfo.reason}${skillDebugInfo.serialId ? ` (Serial: ${skillDebugInfo.serialId})` : ''}${skillDebugInfo.rootSerial ? ` | Root Serial: ${skillDebugInfo.rootSerial}` : ''}${skillDebugInfo.subSerial ? ` | Sub Serial: ${skillDebugInfo.subSerial}` : ''}
                                                            </div>
                                                        </div>
                                                        ${partDebugDescHtml}
                                                    </div>
                                                ` : `
                                                    <div class="dep-item-head--bold${isSlotDisabled ? ' legit-opacity-50' : ''}">${partDisplayName}${serialFormat ? `<span class="dep-item-serial-muted">${serialFormat}</span>` : ''}${selectedStateIcon}${isSlotDisabled ? ' (Disabled)' : ''}</div>
                                                    ${partDebugDescHtml}
                                                `}
                                                ${invCompLootWarnLineHtml}${advancedDetailsSection}
                                            </div>
                                        `;
                                    }
                                }).join('')}
                            </div>
                            ${hiddenItemsHTML}
                        </div>
                    `;
                });
                depTablesHTML += '</div>';
            }

            // Get friendly name for the item
            const getFriendlyName = (key) => {
                if (!key) return 'Unknown Item';
                const keyLower = key.toLowerCase();
                
                // Extract manufacturer and type
                const parts = key.split('_');
                if (parts.length > 1) {
                    const manufacturerAbbr = parts[0].toLowerCase();
                    const typeParts = parts.slice(1);
                    
                    // Map manufacturer abbreviations to full names
                    const manufacturerMap = {
                        'bor': 'Ripper',
                        'dad': 'Daedalus',
                        'mal': 'Maliwan',
                        'cov': 'CoV',
                        'hyp': 'Hyperion',
                        'ted': 'Tediore',
                        'tor': 'Torgue',
                        'vla': 'Vladof',
                        'jak': 'Jakobs',
                        'atla': 'Atlas'
                    };
                    
                    const manufacturer = manufacturerMap[manufacturerAbbr] || manufacturerAbbr.toUpperCase();
                    
                    // Map weapon type abbreviations to full names
                    const weaponTypeMap = {
                        'sr': 'Sniper Rifle',
                        'sg': 'Shotgun',
                        'ps': 'Pistol',
                        'sm': 'SMG',
                        'smg': 'SMG',
                        'ar': 'Assault Rifle',
                        'hw': 'Heavy Weapon'
                    };
                    
                    // Check if type starts with a weapon abbreviation
                    let type = typeParts.join(' ');
                    const firstTypePart = typeParts[0].toLowerCase();
                    
                    // Handle heavy weapon special case
                    if (firstTypePart === 'hw' || keyLower.includes('heavy_weapon_gadget')) {
                        type = 'Heavy Weapon';
                    } else if (weaponTypeMap[firstTypePart]) {
                        // Replace the abbreviation with full name
                        type = weaponTypeMap[firstTypePart] + (typeParts.length > 1 ? ' ' + typeParts.slice(1).join(' ') : '');
                    } else {
                        // Capitalize normally
                        type = type.replace(/\b\w/g, l => l.toUpperCase());
                    }
                    
                    return `${manufacturer} ${type}`;
                }
                return key.replace(/\b\w/g, l => l.toUpperCase());
            };
            
            const friendlyName = getFriendlyName(root.key);
            const manufacturer = extractManufacturer(root);
            const category = categorizeRoot(root);

            const currentBuildInRootHTML = '';
            
            if (isSimpleView) {
                // Simple view: cleaner, more user-friendly
                const partCount = Object.keys(depTables).filter(t => t !== 'inv_comp').length;
                const hasParts = partCount > 0;
                
                return `
                    <div class="root-item simple-view-item" data-root-key="${escapeLegitHtml(String(root.key || ''))}">
                        <div class="root-header root-header-clickable">
                            <div style="display: flex; align-items: center; width: 100%; gap: 12px;">
                                <span class="expand-icon">▶</span>
                                <div style="flex: 1; min-width: 0;">
                                    <div class="root-title" style="display: flex; align-items: center; gap: 8px; flex-wrap: wrap;">
                                        <span>${friendlyName}</span>
                                        ${category !== 'Other' ? `<span style="font-size: 11px; padding: 2px 8px; background: var(--bg-tertiary); border-radius: 6px; color: var(--accent-primary); font-weight: 500;">${category}</span>` : ''}
                                    </div>
                                    <div class="root-meta" style="margin-top: 6px; display: flex; gap: 12px; flex-wrap: wrap;">
                                        ${manufacturer !== 'Unknown' ? `<span style="color: var(--text-secondary); font-size: 12px;">🏭 ${manufacturer}</span>` : ''}
                                        ${hasParts ? `<span style="color: var(--accent-secondary); font-size: 12px;">🔧 ${partCount} part type${partCount !== 1 ? 's' : ''}</span>` : ''}
                                        ${selectedComp ? `<span style="color: var(--accent-primary); font-size: 12px;">✨ Filtered</span>` : ''}
                                    </div>
                                    ${selectedComp ? selectedCompBaseTagsHTML : ''}
                                </div>
                            </div>
                        </div>
                        <div class="root-content collapsed">
                            ${hasFilter ? `
                                <div class="legit-filter-banner">
                                    <div class="legit-filter-banner-text">
                                        <strong>🔍 Filtered View:</strong> Only showing parts compatible with your current selection
                                    </div>
                                </div>
                            ` : ''}
                            ${currentBuildInRootHTML}
                            ${depTablesHTML}
                        </div>
                    </div>
                `;
            } else {
                // Advanced view: original detailed view
                return `
                    <div class="root-item" data-root-key="${escapeLegitHtml(String(root.key || ''))}">
                        <div class="root-header root-header-clickable">
                            <div style="display: flex; align-items: center; width: 100%;">
                                <span class="expand-icon">▶</span>
                                <div style="flex: 1;">
                                    <div class="root-title">${root.key || 'Unknown'}${sourceFile}</div>
                                    <div class="root-meta">
                                        <span>Serial: ${root.serialIndex || 'N/A'}</span>
                                        <span>Inv: ${root.inv || 'N/A'}</span>
                                        ${depEntries.length > 0 ? `<span>Deps: ${depEntries.length}</span>` : ''}
                                        ${selectedComp ? `<span style="color: var(--accent-secondary);">Comp: ${selectedComp}</span>` : ''}
                                    </div>
                                    ${selectedComp ? selectedCompBaseTagsHTML : ''}
                                </div>
                            </div>
                        </div>
                        <div class="root-content collapsed">
                            <div style="margin-bottom: 10px; font-size: 14px;">
                                <div><strong>Base Name:</strong> ${root.itembasename || 'N/A'}</div>
                                <div><strong>Base Type:</strong> ${root.basetype || 'N/A'}</div>
                                <div><strong>Manufacturer:</strong> ${root.manufacturer || 'N/A'}</div>
                                ${hasFilter ? `<div style="color: var(--accent-warning); margin-top: 8px;"><strong>⚠ Filtered by selected comp:</strong> Only showing parts allowed by "${selectedComp}" rules and tags.</div>` : ''}
                            </div>
                            ${currentBuildInRootHTML}
                            ${depTablesHTML}
                            ${renderOpts && renderOpts.useCompactDepTables ? '' : createPartTypeSelectionRulesHTML(root, renderOpts)}
                        </div>
                    </div>
                `;
            }
        }

        /** Build HTML for a single dependency table item (used when lazy-filling manufacturer content). */
        function buildDependencyTableItemHTML(item) {
            const rootKey = item.rootKey || 'Unknown';
            const partKey = item.key || 'Unknown';
            const displayName = rootKey;
            const fullString = `${rootKey}.${partKey}`;
            const rootSerial = item.rootSerialIndex || 'N/A';
            const subSerial = item.serialIndex || 'N/A';
            const serialID = `${rootSerial}:${subSerial}`;
            const debugDesc = getPartDebugDisplayDescription(item);
            const debugDescRow = debugDesc
                ? `<div style="margin-bottom: 4px;"><span style="color: ${LEGIT_DEBUG_LABEL_FG}; font-weight: 700;">Debug:</span><span style="color: ${LEGIT_DEBUG_DESC_FG}; font-weight: 600; margin-left: 6px; text-shadow: 0 1px 2px rgba(0,0,0,0.75);">${escapeLegitHtml(debugDesc)}</span></div>`
                : '';
            const formatTags = (tags) => {
                if (!tags || !Array.isArray(tags)) return [];
                return tags.map(tag => {
                    if (typeof tag === 'object' && tag !== null) {
                        const values = Object.values(tag);
                        return values[0] || Object.keys(tag)[0] || '';
                    }
                    return tag;
                }).filter(Boolean);
            };
            const addTags = formatTags(item.addtags || []);
            const depTags = formatTags(item.dependencytags || []);
            const exclTags = formatTags(item.exclusiontags || []);
            const hasTags = addTags.length > 0 || depTags.length > 0 || exclTags.length > 0;
            return `
                <div style="padding: 6px 8px; background: #0a0a0a; border-radius: 3px; border-left: 2px solid #4CAF50;">
                    <div style="font-size: 11px; color: #fff; font-weight: bold; margin-bottom: ${hasTags ? '4px' : '0'};">${displayName}</div>
                    ${hasTags ? `
                        <div style="font-size: 9px; margin-top: 4px; line-height: 1.4;">
                            ${addTags.length > 0 ? `<div style="margin-bottom: 2px;"><span style="color: #4CAF50; font-weight: bold;">Add:</span><span style="color: #aaa; margin-left: 4px;">${addTags.join(', ')}</span></div>` : ''}
                            ${depTags.length > 0 ? `<div style="margin-bottom: 2px;"><span style="color: #FFA500; font-weight: bold;">Depends:</span><span style="color: #aaa; margin-left: 4px;">${depTags.join(', ')}</span></div>` : ''}
                            ${exclTags.length > 0 ? `<div style="margin-bottom: 2px;"><span style="color: #ff4444; font-weight: bold;">Excludes:</span><span style="color: #aaa; margin-left: 4px;">${exclTags.join(', ')}</span></div>` : ''}
                        </div>
                    ` : ''}
                    <div style="font-size: 9px; margin-top: 4px; line-height: 1.4;">
                        ${debugDescRow}
                        <div style="margin-bottom: 2px;"><span style="color: #888; font-weight: bold;">Fullstring:</span><span style="color: #aaa; margin-left: 4px;">${fullString}</span></div>
                        <div style="margin-bottom: 2px;"><span style="color: #888; font-weight: bold;">SerialID:</span><span style="color: #aaa; margin-left: 4px;">${serialID}</span></div>
                    </div>
                </div>
            `;
        }

        function displayDependencyTables() {
            const container = lbEl('tablesContent');
            if (!container) return;

            if (!data.subItemsByTable && !data.statistics) {
                container.innerHTML = '<div class="loading">No dependency table data available</div>';
                return;
            }

            const tables = data.subItemsByTable || {};
            const stats = data.statistics?.dependencyTables || {};

            if (Object.keys(tables).length === 0 && Object.keys(stats).length === 0) {
                container.innerHTML = '<div class="loading">No dependency tables found</div>';
                return;
            }

            // Precompute table data for lazy fill (tableName -> { depTableId, items, sortedParts })
            const tablesDataMap = {};
            if (Object.keys(tables).length > 0) {
                Object.entries(tables).sort((a, b) => a[0].localeCompare(b[0])).forEach(([name, table]) => {
                    const items = table.items || [];
                    const partsMap = {};
                    items.forEach(item => {
                        const partKey = item.key || 'Unknown';
                        if (!partsMap[partKey]) partsMap[partKey] = [];
                        partsMap[partKey].push(item);
                    });
                    const sortedParts = Object.entries(partsMap).sort((a, b) => a[0].localeCompare(b[0])).map(([partKey, partItems]) => {
                        partItems.sort((a, b) => {
                            const rootCompare = (a.rootKey || '').localeCompare(b.rootKey || '');
                            if (rootCompare !== 0) return rootCompare;
                            return (a.depIndex || 0) - (b.depIndex || 0);
                        });
                        const manufacturerMap = {};
                        partItems.forEach(item => {
                            const manufacturer = (item.rootKey || 'Unknown').split('_')[0] || 'Unknown';
                            if (!manufacturerMap[manufacturer]) manufacturerMap[manufacturer] = [];
                            manufacturerMap[manufacturer].push(item);
                        });
                        const sortedManufacturers = Object.entries(manufacturerMap).sort((a, b) => a[0].localeCompare(b[0]));
                        return { partKey, partItems, sortedManufacturers };
                    });
                    tablesDataMap[name] = { depTableId: table.depTableId, items, sortedParts };
                });
            }
            container._tablesDataMap = tablesDataMap;

            let html = '<div class="table-list">';

            if (Object.keys(tables).length > 0) {
                Object.keys(tablesDataMap).sort((a, b) => a.localeCompare(b)).forEach(name => {
                    const table = tablesDataMap[name];
                    const itemCount = table.items.length;
                    const partCount = table.sortedParts.length;
                    html += `
                        <div class="table-card" style="margin-bottom: 15px;">
                            <div class="table-header-clickable" style="background: #2a2a2a; padding: 12px 15px; border-radius: 6px; cursor: pointer; user-select: none; display: flex; align-items: center; transition: background 0.3s;">
                                <span class="table-expand-icon" style="display: inline-block; width: 20px; text-align: center; margin-right: 10px; transition: transform 0.3s;">▶</span>
                                <div style="flex: 1;">
                                    <div class="table-name" style="font-size: 16px; font-weight: bold; color: #4CAF50; margin-bottom: 4px;">${name}</div>
                                    <div class="table-info" style="font-size: 12px; color: #aaa;">
                                        Table ID: ${table.depTableId || 'N/A'} | Items: ${itemCount} | Parts: ${partCount}
                                    </div>
                                </div>
                            </div>
                            <div class="table-content collapsed" data-table-name="${name.replace(/"/g, '&quot;')}" style="margin-top: 10px; margin-left: 30px; display: none;"></div>
                        </div>
                    `;
                });
            } else {
                Object.entries(stats).sort((a, b) => a[0].localeCompare(b[0])).forEach(([name, info]) => {
                    html += `
                        <div class="table-card" style="margin-bottom: 15px;">
                            <div class="table-header-clickable" style="background: #2a2a2a; padding: 12px 15px; border-radius: 6px; cursor: pointer; user-select: none; display: flex; align-items: center; transition: background 0.3s;">
                                <span class="table-expand-icon" style="display: inline-block; width: 20px; text-align: center; margin-right: 10px; transition: transform 0.3s;">▶</span>
                                <div style="flex: 1;">
                                    <div class="table-name" style="font-size: 16px; font-weight: bold; color: #4CAF50; margin-bottom: 4px;">${name}</div>
                                    <div class="table-info" style="font-size: 12px; color: #aaa;">
                                        Table ID: ${info.id || 'N/A'} | Items: ${info.count || 0}
                                    </div>
                                </div>
                            </div>
                            <div class="table-content collapsed" style="margin-top: 10px; margin-left: 30px; font-size: 12px; color: #888; font-style: italic;">
                                Item details not available (statistics only)
                            </div>
                        </div>
                    `;
                });
            }

            html += '</div>';
            container.innerHTML = html;

            function fillTableContent(contentDiv) {
                const tableName = contentDiv.getAttribute('data-table-name');
                if (!tableName || contentDiv.children.length > 0) return;
                const tablesDataMap = container._tablesDataMap;
                if (!tablesDataMap || !tablesDataMap[tableName]) return;
                const { sortedParts } = tablesDataMap[tableName];
                let inner = '<div style="font-size: 12px; color: #888; margin-bottom: 8px;">Parts (grouped by part, then manufacturer):</div>';
                sortedParts.forEach(({ partKey, partItems, sortedManufacturers }) => {
                    const partId = `part-${tableName}-${partKey}`.replace(/[^a-zA-Z0-9]/g, '-');
                    inner += `
                        <div style="margin-bottom: 10px;">
                            <div class="part-header-clickable" style="background: #1a1a1a; padding: 8px 12px; border-radius: 4px; cursor: pointer; user-select: none; display: flex; align-items: center; transition: background 0.3s;" data-part-id="${partId}">
                                <span class="part-expand-icon" style="display: inline-block; width: 16px; text-align: center; margin-right: 8px; transition: transform 0.3s; font-size: 10px;">▶</span>
                                <span style="color: #4CAF50; font-weight: bold; font-size: 13px;">${partKey}</span>
                                <span style="color: #888; margin-left: 10px; font-size: 11px;">(${partItems.length} root${partItems.length !== 1 ? 's' : ''}, ${sortedManufacturers.length} manufacturer${sortedManufacturers.length !== 1 ? 's' : ''})</span>
                            </div>
                            <div class="part-content collapsed" data-part-id="${partId}" data-table-name="${tableName.replace(/"/g, '&quot;')}" style="margin-top: 6px; margin-left: 24px; display: none;"></div>
                        </div>
                    `;
                });
                contentDiv.innerHTML = inner;
            }

            function fillPartContent(contentDiv) {
                const partId = contentDiv.getAttribute('data-part-id');
                const tableName = contentDiv.getAttribute('data-table-name');
                if (!partId || !tableName || contentDiv.children.length > 0) return;
                const tablesDataMap = container._tablesDataMap;
                if (!tablesDataMap || !tablesDataMap[tableName]) return;
                const entry = tablesDataMap[tableName].sortedParts.find(p => `part-${tableName}-${p.partKey}`.replace(/[^a-zA-Z0-9]/g, '-') === partId);
                if (!entry) return;
                let inner = '';
                entry.sortedManufacturers.forEach(([manufacturer, manufacturerItems]) => {
                    const manufacturerId = `${partId}-${manufacturer}`.replace(/[^a-zA-Z0-9]/g, '-');
                    inner += `
                        <div style="margin-bottom: 8px;">
                            <div class="manufacturer-header-clickable" style="background: #0f0f0f; padding: 6px 10px; border-radius: 3px; cursor: pointer; user-select: none; display: flex; align-items: center; transition: background 0.3s;" data-manufacturer-id="${manufacturerId}" data-part-id="${partId}" data-table-name="${tableName.replace(/"/g, '&quot;')}">
                                <span class="manufacturer-expand-icon" style="display: inline-block; width: 14px; text-align: center; margin-right: 6px; transition: transform 0.3s; font-size: 9px;">▶</span>
                                <span style="color: #FFA500; font-weight: bold; font-size: 12px;">${manufacturer.toUpperCase()}</span>
                                <span style="color: #666; margin-left: 8px; font-size: 10px;">(${manufacturerItems.length} root${manufacturerItems.length !== 1 ? 's' : ''})</span>
                            </div>
                            <div class="manufacturer-content collapsed" data-manufacturer-id="${manufacturerId}" data-part-id="${partId}" data-table-name="${tableName.replace(/"/g, '&quot;')}" style="margin-top: 4px; margin-left: 20px; display: none;"></div>
                        </div>
                    `;
                });
                contentDiv.innerHTML = inner;
            }

            function fillManufacturerContent(contentDiv) {
                const manufacturerId = contentDiv.getAttribute('data-manufacturer-id');
                const partId = contentDiv.getAttribute('data-part-id');
                const tableName = contentDiv.getAttribute('data-table-name');
                if (!manufacturerId || !partId || !tableName || contentDiv.children.length > 0) return;
                const tablesDataMap = container._tablesDataMap;
                if (!tablesDataMap || !tablesDataMap[tableName]) return;
                const entry = tablesDataMap[tableName].sortedParts.find(p => `part-${tableName}-${p.partKey}`.replace(/[^a-zA-Z0-9]/g, '-') === partId);
                if (!entry) return;
                const manuEntry = entry.sortedManufacturers.find(([m]) => (partId + '-' + m).replace(/[^a-zA-Z0-9]/g, '-') === manufacturerId);
                if (!manuEntry) return;
                const manufacturerItems = manuEntry[1];
                const itemsHtml = manufacturerItems.map(item => buildDependencyTableItemHTML(item)).join('');
                contentDiv.innerHTML = '<div style="display: flex; flex-direction: column; gap: 6px;">' + itemsHtml + '</div>';
            }

            function toggleExpand(header, content, icon, collapsedClass) {
                header.classList.toggle('expanded');
                content.classList.toggle(collapsedClass);
                if (content.classList.contains('collapsed')) content.style.display = 'none';
                else content.style.display = 'block';
                if (icon) icon.style.transform = header.classList.contains('expanded') ? 'rotate(90deg)' : 'rotate(0deg)';
            }

            container.addEventListener('click', function(e) {
                const tableHeader = e.target.closest('.table-header-clickable');
                if (tableHeader) {
                    e.stopPropagation();
                    const content = tableHeader.nextElementSibling;
                    if (content && content.classList.contains('table-content') && content.getAttribute('data-table-name')) {
                        fillTableContent(content);
                        toggleExpand(tableHeader, content, tableHeader.querySelector('.table-expand-icon'), 'collapsed');
                    } else if (content) {
                        toggleExpand(tableHeader, content, tableHeader.querySelector('.table-expand-icon'), 'collapsed');
                    }
                    return;
                }
                const partHeader = e.target.closest('.part-header-clickable');
                if (partHeader) {
                    e.stopPropagation();
                    const partId = partHeader.getAttribute('data-part-id');
                    const content = container.querySelector('.part-content[data-part-id="' + partId + '"]');
                    if (content) {
                        fillPartContent(content);
                        toggleExpand(partHeader, content, partHeader.querySelector('.part-expand-icon'), 'collapsed');
                        if (content.classList.contains('collapsed')) content.style.display = 'none';
                        else content.style.display = 'block';
                    }
                    return;
                }
                const manufacturerHeader = e.target.closest('.manufacturer-header-clickable');
                if (manufacturerHeader) {
                    e.stopPropagation();
                    const manufacturerId = manufacturerHeader.getAttribute('data-manufacturer-id');
                    const content = container.querySelector('.manufacturer-content[data-manufacturer-id="' + manufacturerId + '"]');
                    if (content) {
                        fillManufacturerContent(content);
                        toggleExpand(manufacturerHeader, content, manufacturerHeader.querySelector('.manufacturer-expand-icon'), 'collapsed');
                        if (content.classList.contains('collapsed')) content.style.display = 'none';
                        else content.style.display = 'block';
                    }
                }
            });
        }


        // API endpoint for serialization/deserialization
        const SERIAL_API_URL = 'https://save-editor.be/nicnl/api.php';
        // Set to true to see [Bulk validator] and related console logs
        const BULK_VALIDATOR_DEBUG = true;
        
        // Check if input is Base85 format (starts with @ and contains typical Base85 characters)
        function isBase85Format(input) {
            const trimmed = input.trim();
            // Base85 typically starts with @ and contains characters from Base85 alphabet
            // Base85 alphabet: !"#$%&'()*+,-./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ[\]^_`abcdefghijklmnopqrstu
            // More lenient check: starts with @, has reasonable length, and doesn't look like human-readable format
            if (trimmed.length < 10) return false;
            if (!trimmed.startsWith('@')) return false;
            
            // Human-readable format has specific patterns:
            // - Contains | separators (like "| seed|| {serials}|")
            // - Has pattern like "number, 0, 1, number|" at the start
            // - Has pattern like "{number}" or "{number:number}" for parts
            const hasHumanReadablePattern = /^\d+\s*,\s*0\s*,\s*1\s*,\s*\d+\s*\|/.test(trimmed) || 
                                            (trimmed.includes('|') && /\{\d+/.test(trimmed));
            
            if (hasHumanReadablePattern) {
                return false; // It's human-readable format
            }
            
            // Remove whitespace and check if it's mostly Base85-like characters
            const cleaned = trimmed.replace(/\s+/g, '');
            // Base85 uses ASCII 33-117 (! to u), but we'll be more lenient
            // Check if it contains mostly printable ASCII characters that could be Base85
            // Allow some characters outside the strict range (like ~) in case of encoding issues
            const base85Pattern = /^[@!-~]+$/;
            return base85Pattern.test(cleaned) && cleaned.length >= 10;
        }

        window.__legitBackpackContext = window.__legitBackpackContext || {
            lastBase85: null,
            lastDeserialized: null,
            pendingFromB85: false
        };
        window.__legitBackpackInspectPayload = window.__legitBackpackInspectPayload || { deserialized: '', base85: null };

        function isProfileLoadedForInventoryTarget() {
            return !!(
                window.profileMonacoEditor &&
                typeof window.profileMonacoEditor.getValue === 'function' &&
                window.profileMonacoEditor.getValue().trim().length > 0
            );
        }

        function getActiveInventoryEditTarget() {
            if (
                window.editingEquippedSlot !== null &&
                window.editingEquippedSlot !== undefined &&
                String(window.editingEquippedSlot).trim() !== ''
            ) {
                return {
                    kind: 'equipped',
                    label: 'Update Equipped Slot',
                    title: `Overwrite the equipped slot: ${window.editingEquippedSlot}`,
                    ready: !!(window.saveEditorState && window.saveEditorState.isLoaded),
                };
            }
            if (
                window.editingBankSlot !== null &&
                window.editingBankSlot !== undefined &&
                String(window.editingBankSlot).trim() !== ''
            ) {
                const slotMatch = String(window.editingBankSlot).match(/^slot_(\d+)$/);
                const slotLabel = slotMatch ? slotMatch[1] : window.editingBankSlot;
                return {
                    kind: 'bank',
                    label: `Overwrite Bank Slot ${slotLabel}`,
                    title: `Overwrite the original item in bank slot ${slotLabel}`,
                    ready: isProfileLoadedForInventoryTarget(),
                };
            }
            if (
                window.editingBackpackSlot !== null &&
                window.editingBackpackSlot !== undefined &&
                Number(window.editingBackpackSlot) >= 0
            ) {
                return {
                    kind: 'backpack',
                    label: `Overwrite Slot ${window.editingBackpackSlot}`,
                    title: `Overwrite the original item in slot ${window.editingBackpackSlot}`,
                    ready: !!(window.saveEditorState && window.saveEditorState.isLoaded),
                };
            }
            return null;
        }

        function captureInventoryEditSessionState() {
            return {
                backpackSlot: window.editingBackpackSlot,
                equippedSlot: window.editingEquippedSlot,
                bankSlot: window.editingBankSlot,
            };
        }

        function restoreInventoryEditSessionState(snapshot) {
            window.editingBackpackSlot = snapshot ? snapshot.backpackSlot : null;
            window.editingEquippedSlot = snapshot ? snapshot.equippedSlot : null;
            window.editingBankSlot = snapshot ? snapshot.bankSlot : null;
            if (typeof updateBackpackButtons === 'function') {
                updateBackpackButtons();
            }
            if (typeof updateBankButtons === 'function') {
                updateBankButtons();
            }
        }

        async function withAppendBackpackSession(action) {
            const target = getActiveInventoryEditTarget();
            if (!target || (target.kind !== 'backpack' && target.kind !== 'equipped')) {
                return await action();
            }
            const snapshot = captureInventoryEditSessionState();
            window.editingBackpackSlot = null;
            window.editingEquippedSlot = null;
            if (typeof updateBackpackButtons === 'function') {
                updateBackpackButtons();
            }
            if (typeof updateBankButtons === 'function') {
                updateBankButtons();
            }
            try {
                return await action();
            } finally {
                restoreInventoryEditSessionState(snapshot);
            }
        }

        async function writeSerializedToInventoryTarget(serialized, options) {
            const overwriteSource = options && options.overwriteSource === true;
            const target = getActiveInventoryEditTarget();
            if (overwriteSource) {
                if (!target) {
                    throw new Error('No source slot is active for overwrite.');
                }
                if (target.kind === 'bank') {
                    if (typeof addItemToBank === 'function') {
                        return await addItemToBank(serialized);
                    }
                    throw new Error('addItemToBank is not available.');
                }
                if (typeof addItemToBackpack === 'function') {
                    return await addItemToBackpack(serialized, { stayOnCurrentTab: true });
                }
                throw new Error('addItemToBackpack is not available.');
            }
            if (typeof addItemToBackpack !== 'function') {
                throw new Error('addItemToBackpack is not available.');
            }
            if (target && (target.kind === 'backpack' || target.kind === 'equipped')) {
                return await withAppendBackpackSession(() =>
                    addItemToBackpack(serialized, { stayOnCurrentTab: true })
                );
            }
            return await addItemToBackpack(serialized, { stayOnCurrentTab: true });
        }

        /** Collapsed by default; uses computed style so CSS-hidden bodies toggle correctly on first click. */
        window.toggleLegitHiddenPartsSection = function toggleLegitHiddenPartsSection(toggleEl) {
            const body = toggleEl && toggleEl.nextElementSibling;
            if (!body || !body.classList || !body.classList.contains('legit-hidden-parts-body')) return;
            const hidden = window.getComputedStyle(body).display === 'none';
            body.style.display = hidden ? 'block' : 'none';
            const label = toggleEl.getAttribute('data-label') || '';
            toggleEl.textContent = (hidden ? '▼ ' : '▶ ') + label;
        };

        async function resolveSerialB85ForBackpack(deserialized, base85Hint) {
            const hint = (base85Hint != null ? String(base85Hint) : '').trim();
            if (hint && isBase85Format(hint)) {
                return hint.startsWith('@') ? hint : '@' + hint;
            }
            const d = (deserialized != null ? String(deserialized) : '').trim();
            if (!d) {
                throw new Error('No deserialized serial to serialize.');
            }
            let retries = 3;
            let delay = 1000;
            while (retries > 0) {
                try {
                    const response = await fetch(SERIAL_API_URL, {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({ deserialized: d })
                    });
                    if (!response.ok) {
                        if (response.status === 429 || (response.status >= 500 && response.status < 600)) {
                            throw new Error(`HTTP error! status: ${response.status}`);
                        }
                        throw new Error(`Serialize API error: ${response.status}`);
                    }
                    const responseText = await response.text();
                    let data;
                    try {
                        data = JSON.parse(responseText);
                    } catch (parseErr) {
                        throw new Error('Invalid response format from serialize API');
                    }
                    if (data.error) {
                        throw new Error(data.error);
                    }
                    if (data.serial_b85 && typeof data.serial_b85 === 'string') {
                        return data.serial_b85.trim();
                    }
                    throw new Error('No serial_b85 in response');
                } catch (err) {
                    retries--;
                    const msg = err && err.message ? String(err.message) : String(err);
                    if (retries > 0 && (msg.includes('429') || msg.includes('500') || msg.includes('502') || msg.includes('503'))) {
                        await new Promise(r => setTimeout(r, delay));
                        delay *= 2;
                    } else {
                        throw err;
                    }
                }
            }
            throw new Error('Serialize failed after retries');
        }

        const LEGIT_BACKPACK_TOAST_ID = 'legit-backpack-toast';

        function yieldToPaintLegit() {
            return new Promise(function (resolve) {
                requestAnimationFrame(function () {
                    requestAnimationFrame(resolve);
                });
            });
        }

        function notifyLegitBackpackToast(message, type, durationMs) {
            const n =
                typeof window.showNotification === 'function'
                    ? window.showNotification
                    : typeof showNotification === 'function'
                        ? showNotification
                        : null;
            if (!n) return;
            n(message, type, {
                replaceId: LEGIT_BACKPACK_TOAST_ID,
                durationMs: durationMs == null ? 3000 : durationMs
            });
        }

        function legitBackpackSerialInputMatchesValidated() {
            const ctx = window.__legitBackpackContext || {};
            const last = (ctx.lastDeserialized || '').trim();
            const el =
                window.__lbCtx && window.__lbCtx.relaxedPicker ? lbGeneratedHumanEl() : lbSerialInputEl();
            const raw = el ? el.value.trim() : '';
            return raw.length > 0 && last.length > 0 && last === raw;
        }

        function legitBackpackInputMatchesValidatedFor(serialInputId) {
            const ctx = window.__legitBackpackContext || {};
            const last = (ctx.lastDeserialized || '').trim();
            const el = document.getElementById(serialInputId);
            const raw = el ? el.value.trim() : '';
            return raw.length > 0 && last.length > 0 && last === raw;
        }

        async function deserializeFromBase85AsyncForBackpack() {
            const serialInput = lbSerialInputEl();
            const input = serialInput ? serialInput.value.trim() : '';
            if (!input) {
                throw new Error('Please paste a Base85 serial string');
            }
            if (!isBase85Format(input)) {
                throw new Error('Input does not appear to be Base85 format. Expected string starting with @');
            }
            const response = await fetch(SERIAL_API_URL, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ serial_b85: input })
            });
            if (!response.ok) {
                throw new Error(`API error: ${response.status}`);
            }
            const result = await response.json();
            if (result.error) {
                throw new Error(result.error);
            }
            if (!result.deserialized) {
                throw new Error('No deserialized result from API.');
            }
            const rawB85 = input.trim();
            const normB85 = rawB85.startsWith('@') ? rawB85 : '@' + rawB85;
            window.__legitBackpackContext = window.__legitBackpackContext || {};
            window.__legitBackpackContext.lastBase85 = normB85;
            window.__legitBackpackContext.pendingFromB85 = true;
            let deser = result.deserialized.trim();
            if (window.__lbCtx && window.__lbCtx.relaxedPicker) {
                deser = normalizeConsecutiveRootPairTokensInModdedHumanLine(deser);
            }
            serialInput.value = deser;
            window.skipBase85Check = true;
            decodeAndValidateSerial();
            window.skipBase85Check = false;
        }

        async function ensureLegitSerialValidatedForBackpackAsync() {
            const serialInputEl =
                window.__lbCtx && window.__lbCtx.relaxedPicker ? lbGeneratedHumanEl() : lbSerialInputEl();
            const raw = serialInputEl ? serialInputEl.value.trim() : '';
            if (!raw) {
                throw new Error('Paste a serial in the field above first.');
            }
            if (legitBackpackSerialInputMatchesValidated()) {
                return;
            }
            if (isBase85Format(raw)) {
                await deserializeFromBase85AsyncForBackpack();
            } else {
                decodeAndValidateSerial();
            }
            const rawAfter = serialInputEl ? serialInputEl.value.trim() : '';
            const last = (window.__legitBackpackContext.lastDeserialized || '').trim();
            if (!last || last !== rawAfter) {
                throw new Error('Could not validate this serial. Fix errors above or run Parse serial / Decode & Validate.');
            }
        }

        window.updateLegitValidatorBackpackButton = function updateLegitValidatorBackpackButton() {
            const loaded = window.saveEditorState && window.saveEditorState.isLoaded;
            const overwriteTarget = getActiveInventoryEditTarget();
            const btn = lbEl('legitValidatorBackpackBtn');
            if (btn) {
                if (loaded) {
                    const serialEl = lbEl('serialInput');
                    const hasInput = !!(serialEl && serialEl.value.trim());
                    if (!hasInput) {
                        btn.innerHTML = '<span>📋</span> Paste serial';
                        btn.title = 'Paste a serial above, then validate and add it to a new backpack slot in your loaded save';
                    } else if (!legitBackpackSerialInputMatchesValidated()) {
                        btn.innerHTML = '<span>✓</span> Validate &amp; Add to Backpack';
                        btn.title = 'Runs Decode & Validate on the current field, then adds it to a new backpack slot';
                    } else {
                        btn.innerHTML = '<span>📦</span> Add to Backpack';
                        btn.title = 'Add the validated serial in the field to a new backpack slot in your loaded save';
                    }
                } else {
                    btn.innerHTML = '<span>💾</span> Load Save';
                    btn.title = 'Load a save file to use Add to Backpack';
                }
            }
            const validatorOverwriteBtn = lbEl('legitValidatorOverwriteBtn');
            if (validatorOverwriteBtn) {
                if (overwriteTarget) {
                    const serialEl = lbEl('serialInput');
                    const hasInput = !!(serialEl && serialEl.value.trim());
                    const needsValidation = hasInput && !legitBackpackSerialInputMatchesValidated();
                    const label = needsValidation
                        ? `Validate & ${overwriteTarget.label}`
                        : overwriteTarget.label;
                    validatorOverwriteBtn.style.display = 'inline-flex';
                    validatorOverwriteBtn.innerHTML = `<span>💾</span> ${label}`;
                    if (overwriteTarget.ready) {
                        validatorOverwriteBtn.title = hasInput
                            ? overwriteTarget.title
                            : `${overwriteTarget.title}. Paste a serial above first.`;
                    } else {
                        validatorOverwriteBtn.title = overwriteTarget.kind === 'bank'
                            ? 'Load a profile to overwrite the original bank slot'
                            : 'Load a save file to overwrite the original slot';
                    }
                } else {
                    validatorOverwriteBtn.style.display = 'none';
                }
            }
            const miAdd = document.getElementById('mi_addToBackpackBtn');
            if (miAdd) {
                if (loaded) {
                    miAdd.style.background = 'var(--accent-secondary)';
                    const serialEl = document.getElementById('mi_finalOutputString');
                    const hasInput = !!(serialEl && serialEl.value.trim());
                    if (!hasInput) {
                        miAdd.textContent = 'Add to Backpack';
                        miAdd.title =
                            'Paste or build a serial (human or Base85), then add it to a new backpack slot in your loaded save';
                    } else {
                        miAdd.textContent = 'Add to Backpack';
                        miAdd.title =
                            'Add the current Modded serial to a new backpack slot in your loaded save (serializes if needed; does not require Parse / validation)';
                    }
                } else {
                    miAdd.style.background = 'var(--accent-primary)';
                    miAdd.textContent = 'Load Save';
                    miAdd.title = 'Load a save file; then this button adds items to a new backpack slot';
                }
            }
            const miOverwriteBtn = document.getElementById('mi_overwriteSourceBtn');
            if (miOverwriteBtn) {
                if (overwriteTarget) {
                    const serialEl = document.getElementById('mi_finalOutputString');
                    const hasInput = !!(serialEl && serialEl.value.trim());
                    miOverwriteBtn.style.display = 'inline-flex';
                    miOverwriteBtn.innerHTML = `<span>💾</span> ${overwriteTarget.label}`;
                    if (overwriteTarget.ready) {
                        miOverwriteBtn.title = hasInput
                            ? overwriteTarget.title
                            : `${overwriteTarget.title}. Paste or build a serial first.`;
                    } else {
                        miOverwriteBtn.title = overwriteTarget.kind === 'bank'
                            ? 'Load a profile to overwrite the original bank slot'
                            : 'Load a save file to overwrite the original slot';
                    }
                } else {
                    miOverwriteBtn.style.display = 'none';
                }
            }
            const bulkBtn = lbEl('legitBulkInspectBackpackBtn');
            if (bulkBtn) {
                if (loaded) {
                    bulkBtn.textContent = 'Add to Backpack';
                    bulkBtn.title = 'Add this serial to a new backpack slot in your loaded save';
                } else {
                    bulkBtn.textContent = 'Load Save';
                    bulkBtn.title = 'Load a save file first';
                }
            }
        };

        window.moddedLoadSaveForBackpack = function moddedLoadSaveForBackpack() {
            window.legitBuilderSetCtx(1);
            if (typeof loadSaveFileFromItemEditor === 'function') {
                loadSaveFileFromItemEditor();
            }
        };

        window.moddedAddToBackpackOrLoadSaveClick = async function moddedAddToBackpackOrLoadSaveClick() {
            window.legitBuilderSetCtx(1);
            if (!window.saveEditorState || !window.saveEditorState.isLoaded) {
                if (typeof loadSaveFileFromItemEditor === 'function') {
                    loadSaveFileFromItemEditor();
                }
                return;
            }
            await moddedAddSerialToBackpack({ overwriteSource: false });
        };

        window.moddedOverwriteSourceButtonClick = async function moddedOverwriteSourceButtonClick() {
            await moddedAddSerialToBackpack({ overwriteSource: true });
        };

        window.moddedAddSerialToBackpack = async function moddedAddSerialToBackpack(options) {
            if (typeof window.syncLegitBuilderCtxToActiveMainTab === 'function') {
                window.syncLegitBuilderCtxToActiveMainTab();
            }
            window.legitBuilderSetCtx(1);
            const overwriteSource = options && options.overwriteSource === true;
            const overwriteTarget = getActiveInventoryEditTarget();
            if (overwriteSource) {
                if (!overwriteTarget) {
                    const msg = 'Open an item from Save Editor or Profile Editor first to overwrite its original slot.';
                    notifyLegitBackpackToast(msg, 'error', 5000);
                    if (typeof showError === 'function') {
                        showError(msg);
                    } else {
                        alert(msg);
                    }
                    return;
                }
                if (overwriteTarget.kind === 'bank') {
                    if (!isProfileLoadedForInventoryTarget()) {
                        if (typeof loadProfileFileFromItemEditor === 'function') {
                            loadProfileFileFromItemEditor();
                        }
                        return;
                    }
                } else if (!window.saveEditorState || !window.saveEditorState.isLoaded) {
                    if (typeof loadSaveFileFromItemEditor === 'function') {
                        loadSaveFileFromItemEditor();
                    }
                    return;
                }
            } else if (!window.saveEditorState || !window.saveEditorState.isLoaded) {
                if (typeof loadSaveFileFromItemEditor === 'function') {
                    loadSaveFileFromItemEditor();
                }
                return;
            }
            const serialInputEl =
                window.__lbCtx && window.__lbCtx.relaxedPicker ? lbGeneratedHumanEl() : lbSerialInputEl();
            let rawEarly = serialInputEl ? serialInputEl.value.trim() : '';
            if (!rawEarly) {
                const msg = 'Paste or build a serial in Generated Item Code first.';
                notifyLegitBackpackToast(msg, 'error', 5000);
                if (typeof showError === 'function') {
                    showError(msg);
                } else {
                    alert(msg);
                }
                return;
            }
            try {
                if (isBase85Format(rawEarly)) {
                    const normB85 = rawEarly.startsWith('@') ? rawEarly : '@' + rawEarly;
                    window.__legitBackpackContext = window.__legitBackpackContext || {};
                    window.__legitBackpackContext.lastBase85 = normB85;
                    window.__legitBackpackContext.pendingFromB85 = false;
                    await writeSerializedToInventoryTarget(normB85, { overwriteSource });
                    return;
                }
                let deser =
                    typeof mergeModdedSkinUiIntoHumanSerialText === 'function'
                        ? mergeModdedSkinUiIntoHumanSerialText(rawEarly)
                        : rawEarly;
                deser = String(deser || '').trim();
                if (
                    typeof normalizeDeserializedSerialInterPartSpacing === 'function' &&
                    deser
                ) {
                    const spaced = normalizeDeserializedSerialInterPartSpacing(deser);
                    if (spaced && spaced !== deser) {
                        deser = spaced;
                        if (serialInputEl) {
                            serialInputEl.value = deser;
                        }
                    }
                }
                window.__legitBackpackContext = window.__legitBackpackContext || {};
                window.__legitBackpackContext.lastDeserialized = deser;
                window.__legitBackpackContext.lastBase85 = null;
                window.__legitBackpackContext.pendingFromB85 = false;
                const b85El = lbEl('finalOutputBase85');
                let b85 = '';
                if (b85El) {
                    const t = String(b85El.value || '').trim();
                    if (t && t !== 'Serializing...' && t.indexOf('Error:') !== 0) {
                        b85 = t.startsWith('@') ? t : '@' + t;
                    }
                }
                if (!b85) {
                    b85 = await resolveSerialB85ForBackpack(deser, null);
                }
                await yieldToPaintLegit();
                await writeSerializedToInventoryTarget(b85, { overwriteSource });
            } catch (err) {
                console.error(err);
                const m = err && err.message ? err.message : String(err);
                notifyLegitBackpackToast(m, 'error', 5000);
                if (typeof showError === 'function') {
                    showError(m);
                } else {
                    alert(m);
                }
            }
        };

        window.legitValidatorOverwriteButtonClick = async function legitValidatorOverwriteButtonClick() {
            await legitValidatorBackpackButtonClick({ overwriteSource: true });
        };

        window.legitValidatorBackpackButtonClick = async function legitValidatorBackpackButtonClick(options) {
            const overwriteSource = options && options.overwriteSource === true;
            const overwriteTarget = getActiveInventoryEditTarget();
            if (overwriteSource) {
                if (!overwriteTarget) {
                    const noTargetMsg = 'Open an item from Save Editor or Profile Editor first to overwrite its original slot.';
                    notifyLegitBackpackToast(noTargetMsg, 'error', 5000);
                    if (typeof showError === 'function') {
                        showError(noTargetMsg);
                    } else {
                        alert(noTargetMsg);
                    }
                    return;
                }
                if (overwriteTarget.kind === 'bank') {
                    if (!isProfileLoadedForInventoryTarget()) {
                        if (typeof loadProfileFileFromItemEditor === 'function') {
                            loadProfileFileFromItemEditor();
                        }
                        return;
                    }
                } else if (!window.saveEditorState || !window.saveEditorState.isLoaded) {
                    if (typeof loadSaveFileFromItemEditor === 'function') {
                        loadSaveFileFromItemEditor();
                    }
                    return;
                }
            } else if (!window.saveEditorState || !window.saveEditorState.isLoaded) {
                if (typeof loadSaveFileFromItemEditor === 'function') {
                    loadSaveFileFromItemEditor();
                }
                return;
            }
            const serialInputEarly = lbSerialInputEl();
            const rawEarly = serialInputEarly ? serialInputEarly.value.trim() : '';
            if (!rawEarly) {
                const msg = 'Paste a serial in the field above first.';
                notifyLegitBackpackToast(msg, 'error', 5000);
                if (typeof showError === 'function') {
                    showError(msg);
                } else {
                    alert(msg);
                }
                return;
            }
            if (!legitBackpackSerialInputMatchesValidated()) {
                notifyLegitBackpackToast('Validating…', 'info', 0);
                await yieldToPaintLegit();
            }
            try {
                await ensureLegitSerialValidatedForBackpackAsync();
            } catch (vErr) {
                console.warn(vErr);
                const vm = vErr && vErr.message ? String(vErr.message) : String(vErr);
                notifyLegitBackpackToast(vm, 'error', 5000);
                if (typeof showError === 'function') {
                    showError(vm);
                } else {
                    alert(vm);
                }
                return;
            }
            const ctx = window.__legitBackpackContext || {};
            const deser = (ctx.lastDeserialized || '').trim();
            if (!deser) {
                notifyLegitBackpackToast('Nothing validated to add.', 'error', 5000);
                if (typeof showError === 'function') {
                    showError('Run Decode & Validate on a serial first.');
                } else {
                    alert('Run Decode & Validate on a serial first.');
                }
                return;
            }
            try {
                const b85 = await resolveSerialB85ForBackpack(deser, ctx.lastBase85);
                await yieldToPaintLegit();
                await writeSerializedToInventoryTarget(b85, { overwriteSource });
            } catch (err) {
                console.error(err);
                const m = err && err.message ? err.message : String(err);
                notifyLegitBackpackToast(m, 'error', 5000);
                if (typeof showError === 'function') {
                    showError(m);
                } else {
                    alert(m);
                }
            }
        };

        window.legitBulkInspectAddToBackpackClick = async function legitBulkInspectAddToBackpackClick() {
            if (!window.saveEditorState || !window.saveEditorState.isLoaded) {
                if (typeof loadSaveFileFromItemEditor === 'function') {
                    loadSaveFileFromItemEditor();
                }
                return;
            }
            const payload = window.__legitBackpackInspectPayload || {};
            const deser = (payload.deserialized || '').trim();
            const b85Stored = (payload.base85 || '').trim();
            if (!deser && !b85Stored) {
                if (typeof showError === 'function') {
                    showError('No serial context for this inspect view.');
                } else {
                    alert('No serial context for this inspect view.');
                }
                return;
            }
            try {
                const b85 = await resolveSerialB85ForBackpack(deser, b85Stored || null);
                await yieldToPaintLegit();
                if (typeof addItemToBackpack === 'function') {
                    await addItemToBackpack(b85, { stayOnCurrentTab: true });
                } else {
                    notifyLegitBackpackToast('Could not add: editor not ready.', 'error', 5000);
                    alert('addItemToBackpack is not available.');
                }
            } catch (err) {
                console.error(err);
                const m = err && err.message ? err.message : String(err);
                notifyLegitBackpackToast(m, 'error', 5000);
                if (typeof showError === 'function') {
                    showError(m);
                } else {
                    alert(m);
                }
            }
        };
        
        // Deserialize Base85 to human-readable format
        async function deserializeFromBase85() {
            if (typeof window.syncLegitBuilderCtxToActiveMainTab === 'function') {
                window.syncLegitBuilderCtxToActiveMainTab();
            }
            const serialInput = lbSerialInputEl();
            const input = serialInput.value.trim();
            const resultDiv = lbEl('serialValidationResult');
            
            if (!input) {
                showError('Please paste a Base85 serial string');
                return;
            }
            
            if (!isBase85Format(input)) {
                showError('Input does not appear to be Base85 format. Expected string starting with @');
                return;
            }
            
            try {
                // Show loading state
                resultDiv.style.display = 'block';
                ensureMiModdedDecodeAccordionCollapsedForModdedResult();
                resultDiv.innerHTML = '<div style="color: #00BCD4;">Deserializing Base85...</div>';
                
                const response = await fetch(SERIAL_API_URL, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ serial_b85: input })
                });
                
                if (!response.ok) {
                    throw new Error(`API error: ${response.status}`);
                }
                
                const result = await response.json();
                
                if (result.error) {
                    throw new Error(result.error);
                }
                
                if (result.deserialized) {
                    const rawB85 = input.trim();
                    const normB85 = rawB85.startsWith('@') ? rawB85 : '@' + rawB85;
                    window.__legitBackpackContext = window.__legitBackpackContext || {};
                    window.__legitBackpackContext.lastBase85 = normB85;
                    window.__legitBackpackContext.pendingFromB85 = true;
                    // Update input field with deserialized format
                    let deserializedValue = result.deserialized.trim();
                    if (window.__lbCtx && window.__lbCtx.relaxedPicker) {
                        deserializedValue = normalizeConsecutiveRootPairTokensInModdedHumanLine(deserializedValue);
                    }
                    serialInput.value = deserializedValue;
                    // Set flag to skip Base85 check in decodeAndValidateSerial
                    window.skipBase85Check = true;
                    // Automatically decode and validate (with a small delay to ensure input is updated)
                    setTimeout(() => {
                        decodeAndValidateSerial();
                    }, 50);
                } else {
                    // Log the full result for debugging
                    console.error('API deserialization result:', result);
                    throw new Error('No deserialized result from API. API returned: ' + JSON.stringify(result));
                }
            } catch (error) {
                console.error('Deserialization error:', error);
                resultDiv.innerHTML = `<div style="color: #ff4444; padding: 10px; background: #3a1a1a; border-radius: 4px; white-space: pre-wrap;">Error deserializing: ${error.message}</div>`;
                resultDiv.style.display = 'block';
                ensureMiModdedDecodeAccordionCollapsedForModdedResult();
            }
        }
        
        // Parse serial patterns including arrays: {serial}, {root:serial}, {root:[serial1 serial2 ...]}
        function parseSerialPatterns(serialListStr) {
            const patterns = [];
            let i = 0;
            
            while (i < serialListStr.length) {
                // Skip whitespace
                if (/\s/.test(serialListStr[i])) {
                    i++;
                    continue;
                }
                
                // Handle quoted strings: "MAL_SM.comp_05_legendary_firework"
                if (serialListStr[i] === '"') {
                    let start = i;
                    let j = i + 1;
                    let escaped = false;
                    
                    while (j < serialListStr.length) {
                        if (escaped) {
                            escaped = false;
                        } else if (serialListStr[j] === '\\') {
                            escaped = true;
                        } else if (serialListStr[j] === '"') {
                            // Found closing quote
                            const pattern = serialListStr.substring(start, j + 1);
                            patterns.push(pattern);
                            i = j + 1;
                            break;
                        }
                        j++;
                    }
                    
                    if (j >= serialListStr.length) {
                        // Unmatched quote, skip this character
                        i++;
                    }
                }
                // Handle brace patterns: {serial}, {root:serial}, {root:[serial1 serial2 ...]}
                else if (serialListStr[i] === '{') {
                    // Found start of a pattern
                    let braceCount = 0;
                    let start = i;
                    let j = i;
                    
                    while (j < serialListStr.length) {
                        if (serialListStr[j] === '{') {
                            braceCount++;
                        } else if (serialListStr[j] === '}') {
                            braceCount--;
                            if (braceCount === 0) {
                                // Found matching closing brace
                                const pattern = serialListStr.substring(start, j + 1);
                                patterns.push(pattern);
                                i = j + 1;
                                break;
                            }
                        }
                        j++;
                    }
                    
                    if (braceCount !== 0) {
                        // Unmatched braces, skip this character
                        i++;
                    }
                } else {
                    i++;
                }
            }
            
            return patterns;
        }

        /** Same tokenization as {@link parseSerialPatterns}, with start/end offsets into `serialListStr`. */
        function parseSerialPatternSpans(serialListStr) {
            const spans = [];
            if (!serialListStr || typeof serialListStr !== 'string') return spans;
            let i = 0;
            while (i < serialListStr.length) {
                if (/\s/.test(serialListStr[i])) {
                    i++;
                    continue;
                }
                if (serialListStr[i] === '"') {
                    let start = i;
                    let j = i + 1;
                    let escaped = false;
                    while (j < serialListStr.length) {
                        if (escaped) {
                            escaped = false;
                        } else if (serialListStr[j] === '\\') {
                            escaped = true;
                        } else if (serialListStr[j] === '"') {
                            spans.push({ text: serialListStr.substring(start, j + 1), start, end: j + 1 });
                            i = j + 1;
                            break;
                        }
                        j++;
                    }
                    if (j >= serialListStr.length) {
                        i++;
                    }
                } else if (serialListStr[i] === '{') {
                    let braceCount = 0;
                    let start = i;
                    let j = i;
                    while (j < serialListStr.length) {
                        if (serialListStr[j] === '{') {
                            braceCount++;
                        } else if (serialListStr[j] === '}') {
                            braceCount--;
                            if (braceCount === 0) {
                                spans.push({ text: serialListStr.substring(start, j + 1), start, end: j + 1 });
                                i = j + 1;
                                break;
                            }
                        }
                        j++;
                    }
                    if (braceCount !== 0) {
                        i++;
                    }
                } else {
                    i++;
                }
            }
            return spans;
        }

        function isValidatorCosmeticListSegment(segmentTrimmed) {
            return /^["']?c["']?\s*,/i.test(segmentTrimmed);
        }

        function splitValidatorSerialListSegmentsPreservingCosmetics(rawSerialListStr) {
            const segments = String(rawSerialListStr || '')
                .split('|')
                .map(s => s.trim())
                .filter(s => s.length > 0);
            const order = [];
            const partSegments = [];
            segments.forEach(seg => {
                if (isValidatorCosmeticListSegment(seg)) {
                    order.push({ type: 'c', seg });
                } else {
                    order.push({ type: 'p', idx: partSegments.length });
                    partSegments.push(seg);
                }
            });
            return { order, partSegments, partListJoin: partSegments.join('|') };
        }

        function rebuildValidatorRawSerialListFromPartSegments(order, partSegments) {
            const out = [];
            let pi = 0;
            for (let oi = 0; oi < order.length; oi++) {
                const item = order[oi];
                if (item.type === 'c') {
                    out.push(item.seg);
                } else if (pi < partSegments.length) {
                    out.push(partSegments[pi]);
                    pi++;
                }
            }
            return out.join('|');
        }

        function findValidatorSegmentIndexForPatternSpan(partListJoin, span) {
            if (!partListJoin || !span) return -1;
            let pos = 0;
            const segs = partListJoin.split('|');
            for (let i = 0; i < segs.length; i++) {
                const seg = segs[i];
                const segStart = pos;
                const segEnd = pos + seg.length;
                if (span.start >= segStart && span.end <= segEnd) {
                    return i;
                }
                pos = segEnd + 1;
            }
            return -1;
        }

        /**
         * Remove one entry from a brace array pattern `{root:[a b c]}` or `{[a b]}`; returns new pattern or null if array becomes empty.
         */
        function editValidatorBracePatternRemoveArraySlot(patternText, withinIndex) {
            if (withinIndex == null || withinIndex < 0 || !patternText || patternText.length < 2) return patternText;
            const serialStr = patternText.slice(1, -1);
            if (!(serialStr.includes('[') && serialStr.includes(']'))) return null;
            const colonIndex = serialStr.indexOf(':');
            let beforeArr = '';
            let arrayPart = serialStr;
            if (colonIndex > 0) {
                beforeArr = serialStr.substring(0, colonIndex + 1);
                arrayPart = serialStr.substring(colonIndex + 1).trim();
            } else {
                beforeArr = '';
                arrayPart = serialStr.trim();
            }
            if (!arrayPart.startsWith('[') || !arrayPart.endsWith(']')) return null;
            const arrayContent = arrayPart.slice(1, -1).trim();
            const nums = arrayContent.split(/\s+/).filter(Boolean);
            if (withinIndex >= nums.length) return patternText;
            nums.splice(withinIndex, 1);
            if (nums.length === 0) return null;
            const newArr = '[' + nums.join(' ') + ']';
            return '{' + beforeArr + newArr + '}';
        }

        function removeValidatorPartOrderEntryAtSegmentIndex(order, partSegmentIndex) {
            if (!Array.isArray(order) || partSegmentIndex < 0) return order;
            let pSeen = -1;
            const newOrder = order.slice();
            for (let i = 0; i < newOrder.length; i++) {
                if (newOrder[i].type !== 'p') continue;
                pSeen++;
                if (pSeen === partSegmentIndex) {
                    newOrder.splice(i, 1);
                    return newOrder;
                }
            }
            return order;
        }

        /**
         * Collapse whitespace between adjacent `{…}` / quoted tokens in one pipe segment to a single space; trim trailing space after the last token.
         */
        function normalizeSegmentBraceTokenSpacing(seg) {
            if (seg === '' || seg == null) return '';
            const s = String(seg);
            if (!s.trim()) return '';
            const spans = parseSerialPatternSpans(s);
            if (spans.length === 0) return s.trim();
            const lead = s.slice(0, spans[0].start);
            const trail = s.slice(spans[spans.length - 1].end);
            const core = spans.map(sp => sp.text).join(' ');
            const leadNorm = /^\s+$/.test(lead) ? (lead.length ? ' ' : '') : lead;
            const trailNorm = /^\s*$/.test(trail) ? '' : trail;
            return leadNorm + core + trailNorm;
        }

        function normalizePipeJoinedPartListBraceSpacing(pipeJoinedStr) {
            if (!pipeJoinedStr || typeof pipeJoinedStr !== 'string') return pipeJoinedStr;
            return pipeJoinedStr.split('|').map(normalizeSegmentBraceTokenSpacing).join('|');
        }

        /**
         * Single space between `{…}` (and quoted) part tokens in the list after the first `{`; preserves `|` runs (e.g. seed||) and trailing list `|`.
         */
        function normalizeDeserializedSerialInterPartSpacing(deserializedSerial) {
            if (!deserializedSerial || typeof deserializedSerial !== 'string') return deserializedSerial;
            const pipeParts = deserializedSerial.split('|');
            const k = pipeParts.findIndex(p => p.includes('{'));
            if (k < 0) return deserializedSerial;
            const joinedFromK = pipeParts.slice(k).join('|');
            const hadTrailingPartListPipe = /\|\s*$/.test(joinedFromK);
            let listPrefix = pipeParts.slice(0, k).join('|');
            if (k > 0 && pipeParts[k - 1] === '') {
                listPrefix += '|';
            }
            let newRawList = normalizePipeJoinedPartListBraceSpacing(joinedFromK);
            if (hadTrailingPartListPipe && newRawList.length > 0 && !/\|\s*$/.test(newRawList)) {
                newRawList += '|';
            }
            const sep = listPrefix.length === 0 ? '' : listPrefix.endsWith('|') ? '' : '|';
            return listPrefix + sep + newRawList;
        }

        /**
         * Strip one part token from a decoded pipe serial (same layout as validateSerialCore). Preserves cosmetic tail segments.
         * @param {string} deserializedSerial
         * @param {{ patternTokenIndex: number, withinArrayIndex: number|null, isArrayBlock?: boolean }} part
         * @returns {string|null}
         */
        function removeOneValidatorPartFromDeserializedSerial(deserializedSerial, part) {
            if (!deserializedSerial || !part || part.patternTokenIndex == null) return null;
            // Keep empty segments so `seed|| {parts}` and trailing `{…}|` are not collapsed to a single `|`.
            const pipeParts = deserializedSerial.split('|');
            const k = pipeParts.findIndex(p => p.includes('{'));
            if (k < 0) return null;
            const rawSerialListStr = pipeParts.slice(k).join('|');
            let listPrefix = pipeParts.slice(0, k).join('|');
            if (k > 0 && pipeParts[k - 1] === '') {
                listPrefix += '|';
            }
            const hadTrailingPartListPipe = /\|\s*$/.test(rawSerialListStr);

            const { order, partSegments, partListJoin } = splitValidatorSerialListSegmentsPreservingCosmetics(rawSerialListStr);
            const spans = parseSerialPatternSpans(partListJoin);
            const ti = part.patternTokenIndex;
            if (ti < 0 || ti >= spans.length) return null;
            const span = spans[ti];

            const segIdx = findValidatorSegmentIndexForPatternSpan(partListJoin, span);
            if (segIdx < 0) return null;

            const isArrSlot =
                part.isArrayBlock === true &&
                part.withinArrayIndex != null &&
                !part.isQuotedKeyPart;

            const oldSegCount = partSegments.length;
            let newPartListJoin;
            if (isArrSlot) {
                const edited = editValidatorBracePatternRemoveArraySlot(span.text, part.withinArrayIndex);
                if (edited === null) {
                    newPartListJoin = partListJoin.slice(0, span.start) + partListJoin.slice(span.end);
                } else {
                    newPartListJoin = partListJoin.slice(0, span.start) + edited + partListJoin.slice(span.end);
                }
            } else {
                newPartListJoin = partListJoin.slice(0, span.start) + partListJoin.slice(span.end);
            }

            newPartListJoin = newPartListJoin
                .replace(/\s*\|\s*/g, '|')
                .replace(/^\|+|\|+$/g, '')
                .trim();

            const newPartSegments = newPartListJoin.length
                ? newPartListJoin.split('|').map(s => s.trim()).filter(s => s.length > 0)
                : [];
            const newSegCount = newPartSegments.length;

            // Only drop `p` slots when we lost a whole pipe-delimited segment. Multiple `{...}` in one segment
            // (no | between) keeps the same segment count — removing one span must not delete the only `p` row.
            let orderAfter = order;
            if (newSegCount < oldSegCount) {
                let working = order;
                for (let r = 0, drop = oldSegCount - newSegCount; r < drop; r++) {
                    working = removeValidatorPartOrderEntryAtSegmentIndex(working, segIdx);
                }
                orderAfter = working;
            }

            let newRawList = rebuildValidatorRawSerialListFromPartSegments(orderAfter, newPartSegments);
            if (hadTrailingPartListPipe && newRawList.length > 0 && !/\|\s*$/.test(newRawList)) {
                newRawList += '|';
            }
            const sep = listPrefix.length === 0 ? '' : listPrefix.endsWith('|') ? '' : '|';
            return normalizeDeserializedSerialInterPartSpacing(listPrefix + sep + newRawList);
        }

        /**
         * Split pipe-separated serial parts into header (parts[0]), seed segment, and serial list.
         * Optional flag segments (e.g. "9, 1" firmware lock, "10, 1") may appear between header and seed;
         * the seed is always the segment immediately before the first part that contains '{'.
         * @param {string[]} parts - Non-empty trimmed segments from serial.split('|').filter(p => p.trim().length > 0)
         * @returns {{ seedPart: string|null, serialListStr: string, rawPipeBeforePartList: string|null }}
         */
        function splitSerialHeaderSeedAndList(parts) {
            const k = parts.findIndex(p => p.includes('{'));
            if (k < 0) return { seedPart: null, serialListStr: '', rawPipeBeforePartList: null };
            const seedPart = k >= 1 ? parts[k - 1].trim() : null;
            const serialListStr = parts.slice(k).join('|').trim();
            const rawPipeBeforePartList = k >= 2 ? parts.slice(1, k).join('|') : null;
            return { seedPart, serialListStr, rawPipeBeforePartList };
        }

        /**
         * Parse the pipe segment before the part list (e.g. "2, 306").
         * Game format: leading "2" is a fixed prefix; the next integer is the RNG seed.
         * Other "a, b" pairs (first !== 2) keep legacy meaning: seed=a, seed2=b (builder / dual-seed).
         */
        function parseNexusSeedSegment(seedPart) {
            if (seedPart == null || seedPart === '') {
                return { seed: null, seed2: null };
            }
            const trimmed = String(seedPart).trim();
            if (!trimmed.includes(',')) {
                const n = parseInt(trimmed, 10);
                return { seed: isNaN(n) ? null : n, seed2: null };
            }
            const nums = trimmed
                .split(',')
                .map(function (s) {
                    return parseInt(s.trim(), 10);
                })
                .filter(function (n) {
                    return !isNaN(n);
                });
            if (nums.length >= 2 && nums[0] === 2) {
                return {
                    seed: nums[1],
                    seed2: nums.length >= 3 ? nums[2] : null
                };
            }
            if (nums.length >= 2) {
                return { seed: nums[0], seed2: nums[1] };
            }
            return { seed: nums[0] != null ? nums[0] : null, seed2: null };
        }

        // Cosmetic tail segments (e.g. `"c", "Cosmetics_Weapon_Shiny_Demo"`) do not impact part validation.
        function stripCosmeticSelectionSegments(serialListStr) {
            if (!serialListStr || typeof serialListStr !== 'string') return '';

            const filteredSegments = serialListStr
                .split('|')
                .map(segment => segment.trim())
                .filter(segment => segment.length > 0)
                .filter(segment => {
                    if (/^["']?c["']?\s*,/i.test(segment)) {
                        return false;
                    }
                    if (/^"c"\s*,?\s*("[^"]*"|\d+)\s*$/i.test(segment)) {
                        return false;
                    }
                    return true;
                });

            return filteredSegments.join('|');
        }

        function parseModdedFirmwareBuybackFlagsFromDeserialized(deserializedSerial) {
            const raw = String(deserializedSerial || '').trim();
            if (!raw || raw.indexOf('|') === -1) {
                return { firmwareLock: false, buyback: false };
            }
            const parts = raw.split('|').filter(function (p) {
                return p.trim().length > 0;
            });
            const k = parts.findIndex(function (p) {
                return p.indexOf('{') >= 0;
            });
            if (k < 2) {
                return { firmwareLock: false, buyback: false };
            }
            let fw = false;
            let bb = false;
            for (let i = 1; i < k; i++) {
                const seg = parts[i].trim().replace(/\s+/g, ' ');
                if (/^9\s*,\s*1$/.test(seg)) {
                    fw = true;
                }
                if (/^10\s*,\s*1$/.test(seg)) {
                    bb = true;
                }
            }
            return { firmwareLock: fw, buyback: bb };
        }

        function applyMiSkinUIFromDeserializedTail(serial) {
            const dd = lbEl('skinCustomizationDropdown');
            const nv = lbEl('skinCustomizationValue');
            if (!dd || !nv) {
                return;
            }
            const code = String(serial || '').trim();
            const re = /\|\s*"c"\s*,?\s*("[^"]*"|\d+)\s*\|/gi;
            let m;
            let lastToken = null;
            while ((m = re.exec(code)) !== null) {
                lastToken = m[1];
            }
            if (lastToken == null) {
                dd.value = '';
                nv.value = '';
                return;
            }
            const rawTok = String(lastToken).trim();
            if (/^\d+$/.test(rawTok)) {
                dd.value = '';
                nv.value = rawTok;
                return;
            }
            let v = rawTok;
            if (v.length >= 2 && v.charAt(0) === '"' && v.charAt(v.length - 1) === '"') {
                v = v.slice(1, -1);
            }
            if (
                Array.from(dd.options || []).some(function (o) {
                    return o.value === v;
                })
            ) {
                dd.value = v;
                nv.value = '';
                return;
            }
            if (v.indexOf('Cosmetics_Weapon_Mat') === 0 || v.indexOf('Cosmetics_Weapon_Shiny') === 0) {
                dd.value = v;
                nv.value = '';
                return;
            }
            dd.value = '';
            nv.value = v;
        }

        /**
         * Move cosmetic wrongly placed after the last {…} part token into canonical | "c", …| form.
         * Example: `{61} "c" "Cosmetics_Weapon_Mat01_Synthwave"|` → `{61}| "c", "Cosmetics_Weapon_Mat01_Synthwave"|`
         */
        function normalizeModdedHumanInlineCosmeticToPipeTail(s) {
            let t = String(s || '');
            const re = /(\{[^}]+\})\s+"c"\s*,?\s*(?:"([^"]+)"|(\d+))\s*\|\s*$/i;
            for (;;) {
                const n = t.replace(re, function (_, brace, strVal, numVal) {
                    if (numVal != null && numVal !== '') {
                        const n0 = parseInt(numVal, 10);
                        return brace + '| "c", ' + (Number.isNaN(n0) ? numVal : String(n0)) + '|';
                    }
                    return brace + '| "c", ' + JSON.stringify(strVal || '') + '|';
                });
                if (n === t) {
                    return t;
                }
                t = n;
            }
        }

        /**
         * Remove every trailing | "c", "…"| or | "c" "…"| or | "c", n| (comma optional; game / bad tools omit it).
         * Repeats so chained duplicates are cleared before appending one canonical tail.
         */
        function stripModdedSkinTailFromHumanLine(s) {
            let t = normalizeModdedHumanInlineCosmeticToPipeTail(String(s || ''));
            const re = /\s*\|\s*"c"\s*,?\s*("[^"]*"|\d+)\s*\|\s*$/i;
            let next;
            for (;;) {
                next = t.replace(re, '').trimEnd();
                if (next === t) {
                    return t;
                }
                t = next;
            }
        }

        /** Strip any existing c tail then append from Modded skin dropdown / numeric field (supports multi-line). */
        function mergeModdedSkinUiIntoHumanSerialText(text) {
            const s = String(text || '');
            if (!s.trim()) {
                return s;
            }
            const trimmed = s.trim();
            if (typeof isBase85Format === 'function' && isBase85Format(trimmed)) {
                return s;
            }
            return s
                .split(/\r?\n/)
                .map(function (line) {
                    const t = line.trim();
                    if (!t) {
                        return line;
                    }
                    return normalizeConsecutiveRootPairTokensInModdedHumanLine(
                        appendMiSkinTailToModdedHumanLine(stripModdedSkinTailFromHumanLine(t))
                    );
                })
                .join('\n');
        }

        /**
         * Rebuild Modded human serial level, firmware/buyback, and seed from Decoded view inputs.
         * Preserves the brace part list and skin tail. Returns null if the line is not parseable.
         */
        function mergeModdedDecodedViewFieldsIntoHumanSerialLine(line) {
            const t0 = String(line || '').trim();
            if (!t0) {
                return null;
            }
            const stripped = stripModdedSkinTailFromHumanLine(t0);
            const parts = stripped
                .split('|')
                .map(function (p) {
                    return p.trim();
                })
                .filter(function (p) {
                    return p.length > 0;
                });
            const k = parts.findIndex(function (p) {
                return p.indexOf('{') >= 0;
            });
            if (k < 0) {
                return null;
            }
            const m0 =
                parts[0].match(/^(\d+)\s*,\s*0\s*,\s*1\s*,\s*\d+\s*$/) ||
                parts[0].match(/^"(\d+)"\s*,\s*0\s*,\s*1\s*,\s*\d+\s*$/);
            if (!m0) {
                return null;
            }
            const rootSerial = m0[1];
            const levelEl = document.getElementById('mi_outputLevel');
            const seedEl = document.getElementById('mi_outputSeed');
            const rawLv = levelEl && levelEl.value != null ? String(levelEl.value).trim() : '60';
            const rawSd = seedEl && seedEl.value != null ? String(seedEl.value).trim() : '1';
            const lvN = parseInt(rawLv, 10);
            const sdN = parseInt(rawSd, 10);
            const levelOut = !isNaN(lvN) ? String(lvN) : rawLv;
            const seedOut = !isNaN(sdN) ? String(sdN) : rawSd;

            const fwEl = document.getElementById('mi_firmwareLock');
            const bbEl = document.getElementById('mi_buyback');
            const seg = [];
            if (fwEl && fwEl.checked) {
                seg.push('9, 1');
            }
            if (bbEl && bbEl.checked) {
                seg.push('10, 1');
            }
            seg.push('2, ' + seedOut);
            const serialListPart = parts.slice(k).join('|');
            const core =
                rootSerial +
                ', 0, 1, ' +
                levelOut +
                '| ' +
                seg.join('| ') +
                '|| ' +
                serialListPart +
                '|';
            return appendMiSkinTailToModdedHumanLine(
                normalizeConsecutiveRootPairTokensInModdedHumanLine(core)
            );
        }

        /** Apply Decoded view fields to each human line; per-line fallback to skin-only merge. */
        function mergeModdedDecodedViewUiIntoHumanSerialText(text) {
            const s = String(text || '');
            if (!s.trim()) {
                return s;
            }
            const trimmed = s.trim();
            if (typeof isBase85Format === 'function' && isBase85Format(trimmed)) {
                return s;
            }
            return s
                .split(/\r?\n/)
                .map(function (line) {
                    const t = line.trim();
                    if (!t) {
                        return line;
                    }
                    const rebuilt = mergeModdedDecodedViewFieldsIntoHumanSerialLine(t);
                    if (rebuilt != null) {
                        return rebuilt;
                    }
                    return mergeModdedSkinUiIntoHumanSerialText(t);
                })
                .join('\n');
        }

        function appendMiSkinTailToModdedHumanLine(outputString) {
            const dd = lbEl('skinCustomizationDropdown');
            const nv = lbEl('skinCustomizationValue');
            let tail = '';
            if (dd && dd.value) {
                tail = ` "c", ${JSON.stringify(String(dd.value))}|`;
            } else if (nv) {
                const t = nv.value.trim();
                if (t !== '') {
                    const n = parseInt(t, 10);
                    if (!isNaN(n)) {
                        tail = ` "c", ${n}|`;
                    }
                }
            }
            let s = normalizeModdedHumanInlineCosmeticToPipeTail(String(outputString || ''));
            if (!tail) {
                if (s.indexOf('||') >= 0 && s.indexOf('{') >= 0 && !/\|\s*$/.test(String(s).trimEnd())) {
                    s = String(s).trimEnd() + '|';
                }
                return s;
            }
            let base = stripModdedSkinTailFromHumanLine(s);
            base = String(base).replace(/\s*$/, '');
            if (!base.endsWith('|')) {
                base += '|';
            }
            return base + tail;
        }

        /** After Parse Code (modded): load root + part slots from validator output (same as Push to Modded Builder). */
        function applyModdedParseValidationToBuilder(validation) {
            if (!(window.__lbCtx && window.__lbCtx.relaxedPicker)) {
                return false;
            }
            if (!validation || !validation.details || !validation.details.rootKey) {
                if (typeof updateModdedDecodedSummaryFromValidation === 'function') {
                    updateModdedDecodedSummaryFromValidation(validation);
                }
                return false;
            }
            window.__legitLastSingleValidation = validation;
            if (typeof pushValidationToLegitBuilder !== 'function') {
                if (typeof updateModdedDecodedSummaryFromValidation === 'function') {
                    updateModdedDecodedSummaryFromValidation(validation);
                }
                return false;
            }
            const pushed = pushValidationToLegitBuilder(validation, {
                moddedRelaxed: true,
                inventoryPushSilent: true
            });
            if (!pushed && typeof updateModdedDecodedSummaryFromValidation === 'function') {
                updateModdedDecodedSummaryFromValidation(validation);
            }
            if (typeof updateMiSimpleBuilderPlaceholderVisibility === 'function') {
                updateMiSimpleBuilderPlaceholderVisibility();
            }
            return pushed;
        }

        /** Modded simple: type/manufacturer selects in Item Properties; level/seed/flags/skin stay as inputs. */
        function updateModdedDecodedSummaryFromValidation(validation) {
            const d = validation && validation.details;
            if (!d || !d.rootKey) {
                if (!(window.__lbCtx && window.__lbCtx.relaxedPicker)) {
                    return;
                }
                const typeSel = document.getElementById('mi_simpleTypeSelect');
                const manSel = document.getElementById('mi_simpleManufacturerSelect');
                if (typeSel) {
                    typeSel.value = '';
                }
                if (manSel) {
                    manSel.innerHTML =
                        '<option value="">' + miSimpleSecondaryPlaceholderText('') + '</option>';
                    manSel.value = '';
                    manSel.disabled = true;
                }
                updateMiSimpleSecondaryLabel('');
                if (typeof clearMiSimpleBuilderToPlaceholder === 'function') {
                    clearMiSimpleBuilderToPlaceholder();
                }
                return;
            }
            if (typeof applyMiSimpleBuilderFromRootKey === 'function') {
                applyMiSimpleBuilderFromRootKey(d.rootKey);
            }
        }

        /** Push parsed level/seed into #mi_outputLevel / #mi_outputSeed (Parse serial did not do this before). */
        function syncModdedLevelAndSeedFromDecode(serialForValidate, validation) {
            if (!(window.__lbCtx && window.__lbCtx.relaxedPicker)) {
                return;
            }
            const d = validation && validation.details;
            const levelEl = document.getElementById('mi_outputLevel');
            const seedEl = document.getElementById('mi_outputSeed');
            if (d) {
                if (d.level != null && !isNaN(Number(d.level)) && levelEl) {
                    levelEl.value = String(d.level);
                }
                if (d.seed != null && !isNaN(Number(d.seed)) && seedEl) {
                    seedEl.value = String(d.seed);
                }
                return;
            }
            const raw = String(serialForValidate || '').trim();
            if (!raw) {
                return;
            }
            const m = raw.match(/^(\d+|"[^"]*")\s*,\s*0\s*,\s*1\s*,\s*(\d+)\s*\|/);
            if (m && levelEl) {
                const lv = parseInt(m[2], 10);
                if (!isNaN(lv)) {
                    levelEl.value = String(lv);
                }
            }
            const parts = raw.split('|').filter(function (p) {
                return p.trim().length > 0;
            });
            const k = parts.findIndex(function (p) {
                return p.indexOf('{') >= 0;
            });
            if (k >= 1 && seedEl) {
                const seedPart = parts[k - 1].trim();
                const parsed = parseNexusSeedSegment(seedPart);
                if (parsed.seed != null && !isNaN(Number(parsed.seed))) {
                    seedEl.value = String(parsed.seed);
                }
            }
        }

        function clearModdedDecodedSummaryAndSkinUi() {
            const typeSel = document.getElementById('mi_simpleTypeSelect');
            const manSel = document.getElementById('mi_simpleManufacturerSelect');
            if (typeSel) {
                typeSel.value = '';
            }
            if (manSel) {
                manSel.innerHTML =
                    '<option value="">' + miSimpleSecondaryPlaceholderText('') + '</option>';
                manSel.value = '';
                manSel.disabled = true;
            }
            updateMiSimpleSecondaryLabel('');
            if (typeof clearMiSimpleBuilderToPlaceholder === 'function') {
                clearMiSimpleBuilderToPlaceholder();
            }
            window.legitBuilderSetCtx(1);
            const dd = lbEl('skinCustomizationDropdown');
            const nv = lbEl('skinCustomizationValue');
            if (dd) {
                dd.value = '';
            }
            if (nv) {
                nv.value = '';
            }
        }

        function populateSkinCustomizationDropdownSelect(sel) {
            if (!sel) {
                return;
            }
            const prev = sel.value;
            sel.innerHTML = '<option value="">— None (dropdown) —</option>';
            const fromInvCustom = Array.isArray(window.NEXUS_WEAPON_SKIN_OPTIONS)
                ? window.NEXUS_WEAPON_SKIN_OPTIONS
                : [];
            if (fromInvCustom.length > 0) {
                fromInvCustom.forEach(function (row) {
                    if (!row || typeof row.invCustom !== 'string' || !row.invCustom.trim()) {
                        return;
                    }
                    const o = document.createElement('option');
                    o.value = row.invCustom.trim();
                    const lab = row.label != null && String(row.label).trim() !== '' ? String(row.label).trim() : o.value;
                    const indexSuffix = row.serialIndex != null ? ' #' + row.serialIndex : '';
                    o.textContent = lab + indexSuffix + ' — ' + o.value;
                    sel.appendChild(o);
                });
            } else {
                const list = Array.isArray(window.NEXUS_ALL_COSMETICS) ? window.NEXUS_ALL_COSMETICS : [];
                const opts = new Set();
                list.forEach(function (id) {
                    const s = String(id);
                    if (!/Cosmetics_Weapon_(Mat|Shiny)/i.test(s)) {
                        return;
                    }
                    const dot = s.lastIndexOf('.');
                    const token = dot >= 0 ? s.slice(dot + 1) : s;
                    if (/^Cosmetics_Weapon_(Mat|Shiny)/i.test(token)) {
                        opts.add(token);
                    }
                });
                Array.from(opts)
                    .sort()
                    .forEach(function (t) {
                        const o = document.createElement('option');
                        o.value = t;
                        o.textContent = t;
                        sel.appendChild(o);
                    });
            }
            if (prev && Array.from(sel.options).some(function (o) { return o.value === prev; })) {
                sel.value = prev;
            }
        }

        window.refreshSkinCustomizationDropdown = function refreshSkinCustomizationDropdown() {
            forEachLegitInstance(function () {
                populateSkinCustomizationDropdownSelect(lbEl('skinCustomizationDropdown'));
            });
        };
        window.refreshMiSkinCustomizationDropdown = window.refreshSkinCustomizationDropdown;

        // Normalize depTableName to consistent validation keys
        function normalizeDepTableName(depTableName) {
            if (!depTableName) return 'unknown';
            const raw = String(depTableName).trim();
            if (!raw) return 'unknown';
            const lower = raw.toLowerCase();

            if (lower === 'invcomp' || lower === 'inv_comp' || lower === 'invcomponent' || lower === 'inv_component') {
                return 'inv_comp';
            }
            if (lower.includes('firmware')) return 'firmware';
            if (lower.includes('perk')) return 'perk';
            if (lower === 'body_ele' || lower === 'body_elemental' || lower.includes('body_ele')) return 'body_ele';
            if (lower === 'secondary_ele' || lower === 'secondary_elemental' || lower.includes('secondary_ele')) return 'secondary_ele';

            return lower;
        }

        function getCanonicalPartTypeOrder(rootDef) {
            const canonicalOrder = ['inv_comp'];
            if (!rootDef) return canonicalOrder;

            const rootParttypes = Array.isArray(rootDef.parttypes)
                ? rootDef.parttypes
                : (Array.isArray(rootDef.data?.parttypes) ? rootDef.data.parttypes : []);

            rootParttypes.forEach(partType => {
                const normalized = normalizeDepTableName(partType);
                if (normalized && normalized !== 'unknown' && !canonicalOrder.includes(normalized)) {
                    canonicalOrder.push(normalized);
                }
            });

            return canonicalOrder;
        }

        /** Part selection order for builder UI: from root.parttypes (Nexus) when present; else from generic root (e.g. classmod from inv0) when root is variant (e.g. classmod_dark_siren); else fallback by root type. */
        function getPartSelectionOrderFromRoot(root, rootDefinitions) {
            const fromParttypes = getCanonicalPartTypeOrder(root);
            const rootParttypes = Array.isArray(root?.parttypes) ? root.parttypes : (Array.isArray(root?.data?.parttypes) ? root.data.parttypes : []);
            if (rootParttypes.length > 0) return fromParttypes;
            // Variant roots (e.g. classmod_dark_siren in inv4) often lack parttypes; use generic root from inv0 (e.g. classmod) when available
            if (rootDefinitions && Array.isArray(rootDefinitions)) {
                const key = (root?.key || '').toLowerCase();
                let baseKey = null;
                if (key === 'classmod' || key.startsWith('classmod_')) baseKey = 'classmod';
                else if (key === 'enhancement' || key.endsWith('_enhancement')) baseKey = 'enhancement';
                else if (key === 'repair_kit' || key.startsWith('repair_kit') || key.indexOf('repkit') !== -1) baseKey = 'repair_kit';
                else if (key === 'grenade_gadget' || key.includes('grenade_gadget')) baseKey = 'grenade_gadget';
                else if (key === 'weapon' || key.startsWith('weapon_')) baseKey = 'weapon';
                if (baseKey) {
                    const baseRoot = rootDefinitions.find(r => (r.key || '').toLowerCase() === baseKey);
                    const baseParttypes = baseRoot && (Array.isArray(baseRoot.parttypes) ? baseRoot.parttypes : (Array.isArray(baseRoot.data?.parttypes) ? baseRoot.data.parttypes : []));
                    if (baseParttypes && baseParttypes.length > 0) return getCanonicalPartTypeOrder(baseRoot);
                }
                // Heavy weapons: roots are per-manufacturer (bor_hw, vla_hw, etc.); each has parttypes in Nexus. Use any HW root that has parttypes.
                const basetype = (root?.basetype || '').toLowerCase();
                const isHeavyWeapon = key.endsWith('_hw') || basetype.includes('heavy_weapon');
                if (isHeavyWeapon) {
                    const hwRootWithParttypes = rootDefinitions.find(r => {
                        const rKey = (r.key || '').toLowerCase();
                        const rBasetype = (r.basetype || '').toLowerCase();
                        const isHw = rKey.endsWith('_hw') || rBasetype.includes('heavy_weapon');
                        const pt = Array.isArray(r.parttypes) ? r.parttypes : (Array.isArray(r.data?.parttypes) ? r.data.parttypes : []);
                        return isHw && pt.length > 0;
                    });
                    if (hwRootWithParttypes) return getCanonicalPartTypeOrder(hwRootWithParttypes);
                }
            }
            const key = (root?.key || '').toLowerCase();
            const basetype = (root?.basetype || '').toLowerCase();
            const isManufacturerOrTypeShieldRoot = key.endsWith('_shield') ||
                basetype.includes('armor_shield') ||
                basetype.includes('energy_shield');
            if (key === 'shield' || key.includes('energy_shield') || key.includes('armor_shield') || isManufacturerOrTypeShieldRoot) {
                return ['inv_comp', 'body', 'element', 'primary_augment', 'secondary_augment', 'firmware', 'endgame', 'unique', 'pearl_elem', 'pearl_stat'];
            }
            if (key.indexOf('repair') !== -1 || key.indexOf('repkit') !== -1) {
                return ['inv_comp', 'body', 'payload', 'element', 'primary_augment', 'secondary_augment', 'augment_element_resist', 'augment_element_immunity', 'augment_element_nova', 'augment_element_splat', 'firmware', 'endgame'];
            }
            // Heavy weapons (bor_hw, vla_hw, etc.): no secondary_ele, magazine, scope, grip, foregrip
            if (key.endsWith('_hw') || basetype.includes('heavy_weapon')) {
                return ['inv_comp', 'body', 'body_acc', 'body_ele', 'barrel', 'barrel_acc', 'firmware', 'endgame', 'pearl_elem', 'pearl_stat'];
            }
            return ['inv_comp', 'body', 'body_acc', 'body_ele', 'barrel', 'barrel_acc', 'magazine', 'magazine_ted_thrown', 'scope', 'scope_acc', 'grip', 'foregrip', 'secondary_ele', 'pearl_elem', 'pearl_stat'];
        }

        function buildCanonicalPartTypeMap(rootDef) {
            const orderMap = new Map();
            getCanonicalPartTypeOrder(rootDef).forEach((partType, idx) => {
                orderMap.set(partType, idx);
            });
            return orderMap;
        }

        /** Returns roots in the exact same order they are displayed in the builder UI (simple or advanced mode). */
        function getRootsInBuilderDisplayOrder() {
            const rootDefinitions = data && data.rootDefinitions ? data.rootDefinitions : [];
            if (rootDefinitions.length === 0) return [];

            const typeFilterEl = lbEl('typeFilter');
            const typeFilter = typeFilterEl ? typeFilterEl.value : '';
            const viewMode = applicationMode || (typeof localStorage !== 'undefined' && localStorage.getItem('rootDefinitionsViewMode')) || 'simple';

            let filtered = rootDefinitions.slice();
            if (typeFilter) {
                filtered = filtered.filter(root => root.basetype === typeFilter);
            }
            if (viewMode === 'simple') {
                filtered = filtered.filter(root => {
                    const key = (root.key || '').toLowerCase();
                    const inv = (root.inv || '').toLowerCase();
                    const basetype = (root.basetype || '').toLowerCase();
                    return !key.includes('terminal_gadget') &&
                           !key.includes('turret_gadget') &&
                           !key.includes('splicearmlauncher') &&
                           !key.includes('splicearm_launcher') &&
                           !key.includes('invdef') &&
                           !(key === 'heavy_weapon_gadget' || inv === 'heavy_weapon_gadget') &&
                           !(key === 'grenade_gadget' || inv === 'grenade_gadget') &&
                           !inv.includes('terminal_gadget') &&
                           !inv.includes('turret_gadget') &&
                           !inv.includes('splicearmlauncher') &&
                           !inv.includes('splicearm_launcher') &&
                           !inv.includes('invdef');
                });
            }
            if (filtered.length === 0) return [];

            const hoverDriveItems = [];
            const classModItems = [];
            const universalItems = [];
            const groundPickupItems = [];
            const regularItems = [];

            filtered.forEach(root => {
                const key = (root.key || '').toLowerCase();
                const category = categorizeRoot(root);
                if (category === 'Hover Drive') {
                    hoverDriveItems.push(root);
                } else if (category === 'Class Mod' && key.startsWith('classmod_')) {
                    classModItems.push(root);
                } else if (category === 'Universal') {
                    universalItems.push(root);
                } else if ((key.startsWith('currency_') || key.startsWith('ammo') || key.startsWith('ammo_') || key.endsWith('_pickup')) &&
                           key !== 'mission_pickup' && !key.includes('missiontemp')) {
                    groundPickupItems.push(root);
                } else {
                    regularItems.push(root);
                }
            });

            const byManufacturer = {};
            regularItems.forEach(root => {
                const manufacturer = extractManufacturer(root);
                if (!byManufacturer[manufacturer]) {
                    byManufacturer[manufacturer] = {
                        'Ordnance': [], 'Shield': [], 'Repkit': [], 'Class Mod': [], 'Def': [],
                        'Enhancement': [], 'Weapon': [], 'Ammo Types': [], 'InvDef': [], 'Other': []
                    };
                }
                const category = categorizeRoot(root);
                byManufacturer[manufacturer][category].push(root);
            });

            const unknownManufacturer = byManufacturer['Unknown'];
            delete byManufacturer['Unknown'];

            const invDefItems = [];
            filtered.forEach(root => {
                if (categorizeRoot(root) === 'InvDef') invDefItems.push(root);
            });

            const unknownWeapons = [];
            let unknownNonWeapons = {
                'Ordnance': [], 'Shield': [], 'Repkit': [], 'Class Mod': [], 'Def': [],
                'Enhancement': [], 'Weapon': [], 'Ammo Types': [], 'InvDef': [], 'Other': []
            };
            if (unknownManufacturer) {
                unknownNonWeapons = { ...unknownManufacturer };
                if (unknownManufacturer['Weapon'] && unknownManufacturer['Weapon'].length > 0) {
                    unknownWeapons.push(...unknownManufacturer['Weapon']);
                    unknownNonWeapons['Weapon'] = [];
                }
            }
            if (invDefItems.length > 0) {
                if (!unknownNonWeapons['InvDef']) unknownNonWeapons['InvDef'] = [];
                unknownNonWeapons['InvDef'].push(...invDefItems);
            }

            const categoryOrder = ['Ordnance', 'Shield', 'Repkit', 'Class Mod', 'Def', 'Enhancement', 'Weapon', 'Ammo Types', 'InvDef', 'Other'];
            const manufacturers = Object.keys(byManufacturer).sort((a, b) => a.toLowerCase().localeCompare(b.toLowerCase()));

            const out = [];

            if (viewMode === 'simple') {
                const grenadeItems = [];
                const heavyWeaponItems = [];
                const repkitItems = [];
                const enhancementItems = [];
                const weaponItems = [];
                const shieldItems = [];
                const allItemsToCheck = [...regularItems, ...universalItems];
                allItemsToCheck.forEach(root => {
                    const key = (root.key || '').toLowerCase();
                    const inv = (root.inv || '').toLowerCase();
                    const basetype = (root.basetype || '').toLowerCase();
                    const category = categorizeRoot(root);
                    if ((key.includes('heavy_weapon_gadget') || inv.includes('heavy_weapon_gadget') || basetype.includes('heavy_weapon_gadget')) &&
                        !(key === 'heavy_weapon_gadget' || inv === 'heavy_weapon_gadget')) {
                        heavyWeaponItems.push(root);
                    } else if (key.includes('grenade_gadget') || inv.includes('grenade_gadget') || basetype.includes('grenade_gadget')) {
                        grenadeItems.push(root);
                    } else if (category === 'Shield') {
                        shieldItems.push(root);
                    } else if (category === 'Repkit') {
                        repkitItems.push(root);
                    } else if (category === 'Enhancement') {
                        enhancementItems.push(root);
                    } else if (category === 'Weapon' && !key.includes('heavy_weapon_gadget') && !inv.includes('heavy_weapon_gadget') && !key.includes('grenade_gadget') && !inv.includes('grenade_gadget')) {
                        weaponItems.push(root);
                    }
                });
                out.push(...classModItems);
                out.push(...shieldItems);
                out.push(...grenadeItems);
                out.push(...heavyWeaponItems);
                out.push(...repkitItems);
                out.push(...enhancementItems);
                const getWeaponType = (r) => {
                    const k = (r.key || '').toLowerCase();
                    const invR = (r.inv || '').toLowerCase();
                    const bt = (r.basetype || '').toLowerCase();
                    const name = (r.itembasename || '').toLowerCase() + (r.name || '').toLowerCase();
                    if (bt.includes('weapon_sr') || bt.includes('sniper') || invR.includes('sr')) return 'Sniper';
                    if (bt.includes('weapon_sg') || bt.includes('shotgun') || invR.includes('sg')) return 'Shotgun';
                    if (bt.includes('weapon_smg') || bt.includes('submachine') || invR.includes('smg') || k.includes('smg') || name.includes('smg')) return 'SMG';
                    if (bt.includes('weapon_ps') || bt.includes('pistol') || invR.includes('ps')) return 'Pistol';
                    if (bt.includes('weapon_ar') || bt.includes('assault') || invR.includes('ar')) return 'Assault Rifle';
                    return 'Other';
                };
                const typeOrder = ['SMG', 'Assault Rifle', 'Pistol', 'Sniper', 'Shotgun', 'Other'];
                const weaponsByType = {};
                weaponItems.forEach(root => {
                    if (extractManufacturer(root) === 'Unknown') return;
                    const type = getWeaponType(root);
                    if (!weaponsByType[type]) weaponsByType[type] = [];
                    weaponsByType[type].push(root);
                });
                typeOrder.forEach(type => {
                    (weaponsByType[type] || []).forEach(root => out.push(root));
                });
            } else {
                out.push(...classModItems);
                out.push(...universalItems);
                const currencyItems = [];
                const ammoItems = [];
                const pickupItems = [];
                groundPickupItems.forEach(root => {
                    const key = (root.key || '').toLowerCase();
                    if (key.startsWith('currency_')) currencyItems.push(root);
                    else if (key.startsWith('ammo') || key.startsWith('ammo_')) ammoItems.push(root);
                    else if (key.endsWith('_pickup') && key !== 'mission_pickup' && !key.includes('missiontemp')) pickupItems.push(root);
                });
                out.push(...currencyItems);
                out.push(...ammoItems);
                out.push(...pickupItems.filter(r => {
                    const k = (r.key || '').toLowerCase();
                    return k !== 'mission_pickup' && !k.includes('missiontemp');
                }));
                manufacturers.forEach(manufacturer => {
                    const manufacturerCategories = byManufacturer[manufacturer];
                    categoryOrder.forEach(category => {
                        (manufacturerCategories[category] || []).forEach(root => out.push(root));
                    });
                });
                out.push(...unknownWeapons);
                categoryOrder.forEach(category => {
                    (unknownNonWeapons[category] || []).forEach(root => out.push(root));
                });
                const hoverDriveByManufacturer = {};
                hoverDriveItems.forEach(root => {
                    const manufacturer = extractManufacturer(root);
                    if (!hoverDriveByManufacturer[manufacturer]) hoverDriveByManufacturer[manufacturer] = [];
                    hoverDriveByManufacturer[manufacturer].push(root);
                });
                const hoverDriveManufacturers = Object.keys(hoverDriveByManufacturer).sort((a, b) => {
                    if (a === 'Unknown') return 1;
                    if (b === 'Unknown') return -1;
                    return a.localeCompare(b);
                });
                hoverDriveManufacturers.forEach(manufacturer => {
                    (hoverDriveByManufacturer[manufacturer] || []).forEach(root => out.push(root));
                });
            }
            return out;
        }

        function sortPartsByCanonicalOrder(parts, rootDef) {
            const orderMap = buildCanonicalPartTypeMap(rootDef);
            return [...parts].sort((a, b) => {
                const typeA = normalizeDepTableName(a?.partType);
                const typeB = normalizeDepTableName(b?.partType);
                const orderA = orderMap.has(typeA) ? orderMap.get(typeA) : Number.MAX_SAFE_INTEGER;
                const orderB = orderMap.has(typeB) ? orderMap.get(typeB) : Number.MAX_SAFE_INTEGER;
                if (orderA !== orderB) return orderA - orderB;
                return 0;
            });
        }

        /** Within same part type, order so that part A comes before part B if B's dependencytags need tags that A's addtags provide. */
        function sortPartsByCanonicalAndDependencyOrder(parts, rootDef, formatTags) {
            const byType = new Map();
            const partToType = (p) => normalizeDepTableName(p?.partType);
            parts.forEach((p, idx) => {
                const type = partToType(p);
                if (!byType.has(type)) byType.set(type, []);
                byType.get(type).push({ part: p, originalIndex: idx });
            });
            const typeOrder = getCanonicalPartTypeOrder(rootDef);
            const sorted = [];
            const fmt = formatTags || (t => (Array.isArray(t) ? t : []).map(x => (typeof x === 'object' && x !== null ? Object.values(x)[0] || Object.keys(x)[0] : x)).filter(Boolean));
            typeOrder.forEach(partType => {
                const group = byType.get(partType);
                if (!group || group.length === 0) return;
                if (group.length === 1) {
                    sorted.push(group[0].part);
                    return;
                }
                if (partType === 'passive_points') {
                    const passiveSorted = [...group].sort((a, b) => {
                        const ma = getClassModPassiveTierMeta(a.part, fmt);
                        const mb = getClassModPassiveTierMeta(b.part, fmt);
                        if (!ma && !mb) return a.originalIndex - b.originalIndex;
                        if (!ma) return 1;
                        if (!mb) return -1;
                        if (ma.chainKey !== mb.chainKey) return (ma.chainKey || '').localeCompare(mb.chainKey || '');
                        return ma.tier - mb.tier;
                    });
                    passiveSorted.forEach(({ part }) => sorted.push(part));
                    return;
                }
                const addTagSets = group.map(({ part }) => new Set((part.foundPart ? fmt(part.foundPart.addtags || []) : []).map(t => String(t).toLowerCase())));
                const depTagSets = group.map(({ part }) => new Set((part.foundPart ? fmt(part.foundPart.dependencytags || []) : []).map(t => String(t).toLowerCase())));
                const n = group.length;
                const inDegree = Array(n).fill(0);
                for (let j = 0; j < n; j++) {
                    const depJ = depTagSets[j];
                    for (let i = 0; i < n; i++) {
                        if (i === j) continue;
                        const addI = addTagSets[i];
                        const needsFromI = [...depJ].some(tag => addI.has(tag));
                        if (needsFromI) inDegree[j]++;
                    }
                }
                const groupSorted = [];
                const used = new Set();
                for (let k = 0; k < n; k++) {
                    let chosen = -1;
                    for (let j = 0; j < n; j++) {
                        if (used.has(j)) continue;
                        if (inDegree[j] === 0) {
                            if (chosen < 0) chosen = j;
                            else {
                                const serialJ = Number(group[j].part.serial);
                                const serialChosen = Number(group[chosen].part.serial);
                                if (!Number.isNaN(serialJ) && !Number.isNaN(serialChosen) && serialJ !== serialChosen) {
                                    if (serialJ < serialChosen) chosen = j;
                                } else if (group[j].originalIndex < group[chosen].originalIndex) chosen = j;
                            }
                        }
                    }
                    if (chosen < 0) {
                        const remaining = group.map((g, i) => ({ part: g.part, originalIndex: g.originalIndex })).filter((_, i) => !used.has(i));
                        remaining.sort((a, b) => {
                            const sa = Number(a.part.serial);
                            const sb = Number(b.part.serial);
                            if (!Number.isNaN(sa) && !Number.isNaN(sb) && sa !== sb) return sa - sb;
                            return a.originalIndex - b.originalIndex;
                        });
                        remaining.forEach(({ part }) => groupSorted.push(part));
                        break;
                    }
                    used.add(chosen);
                    groupSorted.push(group[chosen].part);
                    const addChosen = addTagSets[chosen];
                    for (let j = 0; j < n; j++) {
                        if (used.has(j)) continue;
                        const depJ = depTagSets[j];
                        const needsFromChosen = [...depJ].some(tag => addChosen.has(tag));
                        if (needsFromChosen) inDegree[j]--;
                    }
                }
                var groupSortedFinal = groupSorted;
                if (group.length > 0 && group.every(g => g.part && g.part.isArrayBlock)) {
                    groupSortedFinal = [...groupSorted].sort((a, b) => {
                        var sa = Number(a.serial);
                        var sb = Number(b.serial);
                        if (!Number.isNaN(sa) && !Number.isNaN(sb)) return sa - sb;
                        return 0;
                    });
                }
                groupSortedFinal.forEach(p => sorted.push(p));
            });
            byType.forEach((group, partType) => {
                if (typeOrder.indexOf(partType) >= 0) return;
                group.sort((a, b) => a.originalIndex - b.originalIndex);
                group.forEach(({ part }) => sorted.push(part));
            });
            return sorted.length > 0 ? sorted : sortPartsByCanonicalOrder(parts, rootDef);
        }

        function isValidNumericPartSerial(serialIndex) {
            if (serialIndex === null || serialIndex === undefined) return false;
            if (typeof serialIndex === 'number') return !Number.isNaN(serialIndex);
            if (typeof serialIndex === 'string') {
                const trimmed = serialIndex.trim();
                if (!trimmed || /^".*"$/.test(trimmed)) return false;
                const parsed = parseInt(trimmed, 10);
                return !isNaN(parsed);
            }
            return false;
        }

        function normalizeNumericPartSerial(serialIndex) {
            if (!isValidNumericPartSerial(serialIndex)) return null;
            if (typeof serialIndex === 'number') return serialIndex;
            return parseInt(String(serialIndex).trim(), 10);
        }

        /** Quoted spawn-code token for parts with no numeric {#} or {#:#} (e.g. "classmod.statspecial_ricochet_damage"). */
        function formatQuotedPartSerialToken(partKey, partEntry) {
            const pk = String(partKey || partEntry?.key || '').trim();
            if (!pk) return '';
            if (/^".*"$/.test(pk)) return pk;
            if (/^[A-Za-z0-9_]+\.[A-Za-z0-9_.]+$/.test(pk)) {
                return `"${pk}"`;
            }
            const lower = pk.toLowerCase();
            if (/^(statspecial_|stat2_|stat3_|stat_)/.test(lower)) {
                return `"classmod.${pk}"`;
            }
            const rootKey = partEntry?.rootKey || partEntry?.foundInRoot;
            if (rootKey && /^comp_/i.test(pk)) {
                return `"${rootKey}.${pk}"`;
            }
            return `"${pk}"`;
        }

        function formatValidatorPartTokenFallback(part) {
            if (!part) return '';
            if (part.rawPatternText && /^".*"$/.test(String(part.rawPatternText).trim())) {
                return String(part.rawPatternText).trim();
            }
            if (part.isQuotedKeyPart && part.serial && /^".*"$/.test(String(part.serial).trim())) {
                return String(part.serial).trim();
            }
            if (typeof part.serial === 'string' && /^".*"$/.test(part.serial.trim())) {
                return part.serial.trim();
            }
            const subSer = normalizeNumericPartSerial(part.serial);
            if (subSer !== null) {
                if (part.isBaseComp && part.rootSerial != null && part.rootSerial !== undefined) {
                    return `{${part.rootSerial}:${subSer}}`;
                }
                return `{${subSer}}`;
            }
            return formatQuotedPartSerialToken(part.partKey, part.foundPart);
        }

        function formatPartTokenForSerial(part) {
            if (!part) return null;
            const fallback = formatValidatorPartTokenFallback(part);
            return fallback || null;
        }

        function buildDeserializedSerialString(rootSerial, rootSerialKey, level, seed, seed2, orderedParts) {
            const rootHeader = rootSerialKey ? `"${rootSerialKey}"` : `${rootSerial}`;
            const levelValue = (level !== null && level !== undefined && !Number.isNaN(level)) ? level : 1;
            const seedValue = (seed !== null && seed !== undefined && !Number.isNaN(seed)) ? seed : 1;
            const seedSegment = (seed2 !== null && seed2 !== undefined && !Number.isNaN(seed2))
                ? `${seedValue}, ${seed2}`
                : `${seedValue}`;
            const partTokens = (Array.isArray(orderedParts) ? orderedParts : [])
                .map(formatPartTokenForSerial)
                .filter(Boolean)
                .join(' ');
            return `${rootHeader}, 0, 1, ${levelValue}| ${seedSegment}|| ${partTokens}|`;
        }

        /** Re-sort each contiguous run of parts with the same arrayBlockKey by dependency order (so e.g. 69 before 84 when 84 needs 69's tag). */
        function sortArrayBlockRunsByDependency(orderedParts, formatTags) {
            const fmt = formatTags || (t => (Array.isArray(t) ? t : []).map(x => (typeof x === 'object' && x !== null ? Object.values(x)[0] : x)).filter(Boolean));
            const out = [];
            let i = 0;
            while (i < orderedParts.length) {
                const p = orderedParts[i];
                if (!p || (p.arrayBlockKey == null || !p.isArrayBlock)) {
                    out.push(p);
                    i++;
                    continue;
                }
                const key = p.arrayBlockKey;
                const run = [];
                while (i < orderedParts.length && orderedParts[i].arrayBlockKey === key && orderedParts[i].isArrayBlock) {
                    run.push(orderedParts[i]);
                    i++;
                }
                if (run.length <= 1) {
                    run.forEach(part => out.push(part));
                    continue;
                }
                const addTagSets = run.map(part => new Set((part.foundPart ? fmt(part.foundPart.addtags || []) : []).map(t => String(t).toLowerCase())));
                const depTagSets = run.map(part => new Set((part.foundPart ? fmt(part.foundPart.dependencytags || []) : []).map(t => String(t).toLowerCase())));
                const n = run.length;
                const inDegree = Array(n).fill(0);
                for (let j = 0; j < n; j++) {
                    for (let ii = 0; ii < n; ii++) {
                        if (ii === j) continue;
                        const needsFromI = [...depTagSets[j]].some(tag => addTagSets[ii].has(tag));
                        if (needsFromI) inDegree[j]++;
                    }
                }
                const sortedRun = [];
                const used = new Set();
                for (let k = 0; k < n; k++) {
                    let chosen = -1;
                    for (let j = 0; j < n; j++) {
                        if (used.has(j) || inDegree[j] !== 0) continue;
                        if (chosen < 0) chosen = j;
                        else {
                            const sa = Number(run[j].serial);
                            const sb = Number(run[chosen].serial);
                            if (!Number.isNaN(sa) && !Number.isNaN(sb) && sa !== sb) { if (sa < sb) chosen = j; }
                            else if (j < chosen) chosen = j;
                        }
                    }
                    if (chosen < 0) {
                        const rest = run.filter((_, idx) => !used.has(idx)).sort((a, b) => Number(a.serial) - Number(b.serial));
                        rest.forEach(part => sortedRun.push(part));
                        break;
                    }
                    used.add(chosen);
                    sortedRun.push(run[chosen]);
                    for (let j = 0; j < n; j++) {
                        if (used.has(j)) continue;
                        const needsChosen = [...depTagSets[j]].some(tag => addTagSets[chosen].has(tag));
                        if (needsChosen) inDegree[j]--;
                    }
                }
                sortedRun.forEach(part => out.push(part));
            }
            return out;
        }

        /** Build reordered serial string preserving array blocks: {root:[s1 s2 ...]} for parts that share arrayBlockKey.
         *  When rawPipeBeforePartList is set (from the input serial), it is copied verbatim (trimmed) between level| and || so game prefixes like "2, 287" are not lost. */
        function buildReorderedSerialWithArrayBlocks(rootSerial, rootSerialKey, level, seed, seed2, orderedParts, rawPipeBeforePartList) {
            const rootHeader = rootSerialKey ? `"${rootSerialKey}"` : `${rootSerial}`;
            const levelValue = (level !== null && level !== undefined && !Number.isNaN(level)) ? level : 1;
            const seedValue = (seed !== null && seed !== undefined && !Number.isNaN(seed)) ? seed : 1;
            const rawTrim = rawPipeBeforePartList != null && String(rawPipeBeforePartList).trim() !== ''
                ? String(rawPipeBeforePartList).trim()
                : '';
            const seedSegment = rawTrim
                ? rawTrim
                : ((seed2 !== null && seed2 !== undefined && !Number.isNaN(seed2))
                    ? `${seedValue}, ${seed2}`
                    : `${seedValue}`);
            const parts = Array.isArray(orderedParts) ? orderedParts : [];
            const tokens = [];
            let i = 0;
            while (i < parts.length) {
                const part = parts[i];
                if (!part) { i++; continue; }
                const key = part.arrayBlockKey;
                const root = part.rootSerial;
                if (key != null && part.isArrayBlock && root != null) {
                    const group = [];
                    while (i < parts.length && parts[i].arrayBlockKey === key && parts[i].isArrayBlock && parts[i].rootSerial === root) {
                        group.push(parts[i].serial);
                        i++;
                    }
                    if (group.length > 1) {
                        tokens.push(`{${root}:[${group.join(' ')}]}`);
                    } else if (group.length === 1) {
                        tokens.push(`{${root}:${group[0]}}`);
                    }
                } else {
                    const one = formatPartTokenForSerial(part);
                    if (one) tokens.push(one);
                    i++;
                }
            }
            return `${rootHeader}, 0, 1, ${levelValue}| ${seedSegment}|| ${tokens.join(' ')}|`;
        }

        function detectCanonicalOrderViolation(validatedParts, rootDef) {
            const orderedParts = validatedParts
                .map((part, idx) => ({ part, idx }))
                .filter(({ part }) => part && part.found && part.partType);

            if (orderedParts.length <= 1) {
                return { violated: false, observed: [], expected: [], mismatchedPartIndexes: new Set() };
            }

            const orderMap = buildCanonicalPartTypeMap(rootDef);
            const observed = orderedParts.map(({ part }) => normalizeDepTableName(part.partType));

            const expected = [...orderedParts].sort((a, b) => {
                const typeA = normalizeDepTableName(a.part.partType);
                const typeB = normalizeDepTableName(b.part.partType);
                const orderA = orderMap.has(typeA) ? orderMap.get(typeA) : Number.MAX_SAFE_INTEGER;
                const orderB = orderMap.has(typeB) ? orderMap.get(typeB) : Number.MAX_SAFE_INTEGER;
                if (orderA !== orderB) return orderA - orderB;
                return a.idx - b.idx;
            }).map(({ part }) => normalizeDepTableName(part.partType));

            const mismatchedPartIndexes = new Set();
            for (let i = 0; i < observed.length; i++) {
                if (observed[i] !== expected[i]) {
                    mismatchedPartIndexes.add(i);
                }
            }

            return {
                violated: mismatchedPartIndexes.size > 0,
                observed,
                expected,
                mismatchedPartIndexes
            };
        }

        function parseClassModTierToken(token) {
            if (!token || typeof token !== 'string') return null;
            const normalized = token.trim().toLowerCase();
            const match = normalized.match(/^([a-z0-9_]+)_tier_([1-5])$/);
            if (!match) return null;
            const chainKey = match[1];
            const tier = parseInt(match[2], 10);
            if (!chainKey || Number.isNaN(tier)) return null;
            return {
                chainKey,
                tier,
                tag: `${chainKey}_tier_${tier}`
            };
        }

        function getClassModPassiveTierMeta(part, formatTags) {
            if (!part || !part.foundPart || normalizeDepTableName(part.partType) !== 'passive_points') {
                return null;
            }

            const partKey = part.partKey || part.foundPart.key;
            const fromKey = parsePassivePointsTierKey(partKey);
            if (fromKey) {
                let chainKey = String(fromKey.chainBase || '');
                chainKey = chainKey.replace(/^passive_/i, '');
                chainKey = chainKey.toLowerCase();
                if (chainKey && Number.isFinite(fromKey.tier)) {
                    return {
                        chainKey,
                        tier: fromKey.tier,
                        observedTag: `${chainKey}_tier_${fromKey.tier}`
                    };
                }
            }

            const depTokens = formatTags(part.foundPart.dependencytags || []);
            const addTokens = formatTags(part.foundPart.addtags || []);

            let depTier = null;
            let addTier = null;

            for (const token of depTokens) {
                const parsed = parseClassModTierToken(token);
                if (parsed) {
                    depTier = parsed;
                    break;
                }
            }

            for (const token of addTokens) {
                const parsed = parseClassModTierToken(token);
                if (parsed) {
                    addTier = parsed;
                    break;
                }
            }

            const chosen = addTier || depTier;
            if (!chosen) return null;
            return {
                chainKey: chosen.chainKey,
                tier: chosen.tier,
                observedTag: chosen.tag
            };
        }

        function detectClassModPassiveTierOrderIssues(sortedParts, rootDef, formatTags) {
            const issueByPart = new Map();
            const issues = [];

            const rootKey = (rootDef?.key || '').toLowerCase();
            const isClassModRoot = rootKey.includes('classmod');
            if (!isClassModRoot || !Array.isArray(sortedParts) || sortedParts.length === 0) {
                return { issueByPart, issues };
            }

            // Track expected next tier per passive chain; tiers must start at 1 and advance contiguously.
            const expectedTierByChain = new Map();
            // Track tier tags granted by non-passive parts (e.g., class_mod_body) before passive selection.
            const seededTiersByChain = new Map();

            const addSeedTier = (chainKey, tier) => {
                if (!chainKey || !Number.isFinite(tier)) return;
                if (!seededTiersByChain.has(chainKey)) {
                    seededTiersByChain.set(chainKey, new Set());
                }
                seededTiersByChain.get(chainKey).add(tier);
            };

            const getInitialExpectedTier = (chainKey) => {
                const seeded = seededTiersByChain.get(chainKey);
                if (!seeded || seeded.size === 0) return 1;

                // Example: seeded tiers {1,2} => first expected passive is tier_3.
                let contiguousMax = 0;
                while (seeded.has(contiguousMax + 1)) {
                    contiguousMax++;
                }
                return contiguousMax + 1;
            };

            sortedParts.forEach(part => {
                if (!part || !part.found || !part.foundPart) return;
                if (normalizeDepTableName(part.partType) !== 'passive_points') {
                    const addTokens = formatTags(part.foundPart.addtags || []);
                    addTokens.forEach(token => {
                        const parsed = parseClassModTierToken(token);
                        // Only pre-seed chains that have not started passive evaluation yet.
                        if (parsed && !expectedTierByChain.has(parsed.chainKey)) {
                            addSeedTier(parsed.chainKey, parsed.tier);
                        }
                    });
                    return;
                }

                const tierMeta = getClassModPassiveTierMeta(part, formatTags);
                if (!tierMeta) return;

                const currentExpected = expectedTierByChain.has(tierMeta.chainKey)
                    ? expectedTierByChain.get(tierMeta.chainKey)
                    : getInitialExpectedTier(tierMeta.chainKey);

                if (tierMeta.tier !== currentExpected) {
                    const expectedTag = `${tierMeta.chainKey}_tier_${currentExpected}`;
                    const observedTag = `${tierMeta.chainKey}_tier_${tierMeta.tier}`;
                    const message = `Out-of-order passive tier selection: expected ${expectedTag} before ${observedTag}`;
                    issueByPart.set(part, {
                        chainKey: tierMeta.chainKey,
                        expectedTier: currentExpected,
                        observedTier: tierMeta.tier,
                        expectedTag,
                        observedTag,
                        message
                    });
                    issues.push(`Part "${part.partKey}" (${part.partType}): ${message}`);
                    return;
                }

                expectedTierByChain.set(tierMeta.chainKey, currentExpected + 1);
            });

            return { issueByPart, issues };
        }

        function getAllowedPartsForTable(compRules, tableName) {
            if (!compRules || !tableName) return null;
            const normalizedSlot = normalizeDepTableName(tableName);

            if (compRules.slotConstraints && compRules.slotConstraints.size > 0) {
                const constrained = compRules.slotConstraints.get(normalizedSlot);
                if (constrained && constrained.allowedParts && constrained.allowedParts.size > 0) {
                    return constrained.allowedParts;
                }
            }

            if (compRules.allowedParts && compRules.allowedParts.size > 0) {
                const direct = compRules.allowedParts.get(tableName);
                if (direct && direct.size > 0) return direct;

                const normalizedMatch = Array.from(compRules.allowedParts.entries()).find(
                    ([key]) => normalizeDepTableName(key) === normalizedSlot
                );
                if (normalizedMatch && normalizedMatch[1] && normalizedMatch[1].size > 0) {
                    return normalizedMatch[1];
                }
            }

            return null;
        }

        function buildSlotConstraintDiagnostics(rootDef, compRules) {
            if (!compRules || !compRules.slotConstraints || compRules.slotConstraints.size === 0) {
                return [];
            }

            const rootDepEntries = Array.isArray(rootDef?.depEntries) ? rootDef.depEntries : [];
            const diagnostics = [];

            compRules.slotConstraints.forEach((constraint, normalizedSlot) => {
                const slotName = constraint?.slotName || normalizedSlot;
                const allowedParts = Array.from(constraint?.allowedParts || []).filter(Boolean);
                if (allowedParts.length === 0) return;

                const allRootParts = [];
                const seen = new Set();
                rootDepEntries.forEach(dep => {
                    if (normalizeDepTableName(dep?.depTableName) !== normalizedSlot) return;
                    const slotValue = dep?.depTableName ? dep?.[dep.depTableName] : null;
                    const candidates = [dep?.key, slotValue];
                    candidates.forEach(candidate => {
                        const partKey = String(candidate || '').trim();
                        if (!partKey) return;
                        const normalized = partKey.toLowerCase();
                        if (seen.has(normalized)) return;
                        seen.add(normalized);
                        allRootParts.push(partKey);
                    });
                });

                // If root table introspection didn't resolve candidates, still surface allowed list.
                if (allRootParts.length === 0) {
                    allowedParts.forEach(partKey => {
                        const normalized = String(partKey).toLowerCase();
                        if (seen.has(normalized)) return;
                        seen.add(normalized);
                        allRootParts.push(partKey);
                    });
                }

                const allowedLookup = new Set(allowedParts.map(part => String(part).toLowerCase()));
                const excludedParts = allRootParts.filter(part => !allowedLookup.has(String(part).toLowerCase()));

                diagnostics.push({
                    slotName,
                    normalizedSlot,
                    source: constraint?.source || 'unknown',
                    inherited: !!constraint?.inherited,
                    allowedParts,
                    allRootParts,
                    excludedParts
                });
            });

            return diagnostics.sort((a, b) => String(a.slotName).localeCompare(String(b.slotName)));
        }

        function getAvailableOptionCountForSlot(rootDef, compRules, slotName) {
            const normalizedSlot = normalizeDepTableName(slotName);
            if (!normalizedSlot || normalizedSlot === 'unknown' || normalizedSlot === 'inv_comp') {
                return 0;
            }

            // Primary source: currently resolved/allowed parts for this slot.
            const effectiveAllowed = getAllowedPartsForTable(compRules, slotName);
            if (effectiveAllowed && effectiveAllowed.size > 0) {
                return effectiveAllowed.size;
            }

            // Fallback source: root dependency table entries.
            if (rootDef && Array.isArray(rootDef.depEntries)) {
                const entryCount = rootDef.depEntries.filter(dep => {
                    return normalizeDepTableName(dep?.depTableName) === normalizedSlot;
                }).length;
                if (entryCount > 0) return entryCount;
            }

            return 0;
        }

        /** Dep entries for a slot that pass isPartValid under the given tag pool (same pool as slot min resolution). */
        function getDependencyQualifiedDepEntriesForSlot(rootDef, compRules, slotName, tagPoolSnapshot) {
            const normalizedSlot = normalizeDepTableName(slotName);
            if (!normalizedSlot || normalizedSlot === 'unknown' || normalizedSlot === 'inv_comp') {
                return [];
            }
            if (!rootDef || !Array.isArray(rootDef.depEntries) || !compRules) {
                return [];
            }

            const snapshotPool = tagPoolSnapshot instanceof Set
                ? new Set(Array.from(tagPoolSnapshot, t => String(t).toLowerCase()))
                : new Set();

            const scopedRules = {
                ...compRules,
                tagPool: snapshotPool
            };

            const byKey = new Map();
            rootDef.depEntries.forEach(dep => {
                if (!dep || !dep.key) return;
                if (normalizeDepTableName(dep.depTableName) !== normalizedSlot) return;
                byKey.set(String(dep.key).toLowerCase(), dep);
            });

            const effectiveAllowed = getAllowedPartsForTable(compRules, slotName);
            if (effectiveAllowed && effectiveAllowed.size > 0 && data && Array.isArray(data.rootDefinitions)) {
                effectiveAllowed.forEach(partKey => {
                    if (partKey == null || partKey === '') return;
                    const lk = String(partKey).toLowerCase();
                    if (byKey.has(lk)) return;
                    let entry = null;
                    for (let ri = 0; ri < data.rootDefinitions.length; ri++) {
                        const r = data.rootDefinitions[ri];
                        if (!r || !Array.isArray(r.depEntries)) continue;
                        const e = r.depEntries.find(dep =>
                            dep &&
                            dep.key &&
                            String(dep.key).toLowerCase() === lk &&
                            normalizeDepTableName(dep.depTableName) === normalizedSlot
                        );
                        if (e) {
                            entry = e;
                            break;
                        }
                    }
                    if (entry) {
                        byKey.set(lk, entry);
                    }
                });
            }

            if (byKey.size === 0 && data && Array.isArray(data.rootDefinitions)) {
                const k = String(rootDef.key || '').toLowerCase();
                const genericKey =
                    k.includes('classmod') ? 'classmod' :
                    k.includes('enhancement') ? 'enhancement' :
                    null;
                if (genericKey) {
                    const gr = data.rootDefinitions.find(r => String(r.key || '').toLowerCase() === genericKey);
                    if (gr && Array.isArray(gr.depEntries)) {
                        gr.depEntries.forEach(dep => {
                            if (!dep || !dep.key) return;
                            if (normalizeDepTableName(dep.depTableName) !== normalizedSlot) return;
                            byKey.set(String(dep.key).toLowerCase(), dep);
                        });
                    }
                }
            }

            const matchingEntries = Array.from(byKey.values());
            if (matchingEntries.length === 0) {
                return [];
            }

            return matchingEntries.filter(dep =>
                isPartValid(dep, scopedRules, slotName, false, { ignoreRelaxedPicker: true })
            );
        }

        function getDependencyQualifiedOptionCountForSlot(rootDef, compRules, slotName, tagPoolSnapshot) {
            const normalizedSlot = normalizeDepTableName(slotName);
            if (!normalizedSlot || normalizedSlot === 'unknown' || normalizedSlot === 'inv_comp') {
                return 0;
            }
            if (!rootDef || !Array.isArray(rootDef.depEntries) || !compRules) {
                return getAvailableOptionCountForSlot(rootDef, compRules, slotName);
            }
            return getDependencyQualifiedDepEntriesForSlot(rootDef, compRules, slotName, tagPoolSnapshot).length;
        }

        function getDepSerialIndexForPicker(dep) {
            if (!dep) return null;
            let serialIndex = dep.serialIndex;
            if (serialIndex === null || serialIndex === undefined) {
                serialIndex = dep.serialindex?.index;
            }
            if ((serialIndex === null || serialIndex === undefined) && dep.serialindex != null) {
                serialIndex = typeof dep.serialindex === 'number' ? dep.serialindex : null;
            }
            if (serialIndex !== null && serialIndex !== undefined && typeof serialIndex === 'string') {
                const parsed = parseInt(serialIndex, 10);
                serialIndex = isNaN(parsed) ? null : parsed;
            }
            return serialIndex;
        }

        function allowsZeroOptionMinBypass(normalizedSlot) {
            if (!normalizedSlot) return false;
            // Compatibility-style slots can become unsatisfiable based on tag-gated rolls.
            return normalizedSlot.endsWith('_acc') || normalizedSlot === 'barrel_licensed';
        }

        function resolveRequiredMinForSlot(countInfo, rootDef, compRules, slotName, options = {}) {
            const min = countInfo && countInfo.min !== undefined && countInfo.min !== null
                ? Number(countInfo.min)
                : 0;
            const max = countInfo && countInfo.max !== undefined && countInfo.max !== null
                ? Number(countInfo.max)
                : null;
            const normalizedSlot = normalizeDepTableName(slotName);
            // Firmware is optional in Nexus comps; never infer min 1 from available options (fixes bulk false invalids).
            if (normalizedSlot === 'firmware') {
                return 0;
            }
            const hasExplicitMin = !!countInfo?.hasExplicitMin;
            const countSource = countInfo?.countSource || 'unknown';
            const isDisabled = min === 0 && max === 0;
            const overrideAvailable = Number.isFinite(options?.availableOptions)
                ? Math.max(0, Number(options.availableOptions))
                : null;
            const availableOptions = overrideAvailable !== null
                ? overrideAvailable
                : getAvailableOptionCountForSlot(rootDef, compRules, slotName);

            let requiredMin = hasExplicitMin ? (Number.isFinite(min) ? min : 0) : 0;

            // Root parttypes without explicit count rules should remain optional.
            if (!hasExplicitMin && String(countSource).indexOf('parttypes_fallback') === 0) {
                requiredMin = 0;
            }

            // Unknown/inferred mins require one selection when slot is available and not disabled.
            // This also covers max-only rules (explicit max with no min), where min is implicitly 1.
            if (!hasExplicitMin && !isDisabled && availableOptions > 0 && String(countSource).indexOf('parttypes_fallback') !== 0) {
                requiredMin = 1;
            }

            // Compatibility-like slots are dependency-gated at roll time.
            // Require only what is currently available, capped by configured min.
            if (allowsZeroOptionMinBypass(normalizedSlot)) {
                requiredMin = Math.min(requiredMin, availableOptions);
            }

            // When no parts exist for this slot (e.g. empty magazine_borg table), do not require any.
            // Exception: pearlescent-implied slots (pearl_elem, pearl_stat) must still be enforced when this root
            // has no depEntries for that slot. Do NOT apply this to other explicit-min slots (e.g. scope_acc)
            // which are compatibility-gated and should not require parts when 0 options are available.
            if (availableOptions === 0) {
                if (countSource === 'pearlescent_implied_1_1' && Number.isFinite(min) && min > 0) return min;
                return 0;
            }

            const debugRequiredMin = typeof window !== 'undefined' && !!window.debugRequiredMinResolver;
            if (debugRequiredMin) {
                console.log('[RequiredMin Resolver]', {
                    slotName,
                    normalizedSlot,
                    countSource,
                    hasExplicitMin,
                    min,
                    max,
                    availableOptions,
                    availableOptionsOverride: overrideAvailable,
                    resolvedRequiredMin: requiredMin
                });
            }

            return requiredMin;
        }

        function isInferredRequiredSlot(partCountInfo, availableOptions, slotNameOpt) {
            if (!partCountInfo) return false;
            if (slotNameOpt && normalizeDepTableName(slotNameOpt) === 'firmware') return false;
            if (partCountInfo.hasExplicitMin) return false;
            if (partCountInfo.min === 0 && partCountInfo.max === 0) return false;
            return availableOptions > 0;
        }

        function isPartAllowedForTable(partItem, compRules, tableName) {
            if (!compRules) return true;
            const normalizedSlot = normalizeDepTableName(tableName);
            const countInfo = compRules.partCounts && compRules.partCounts.get(tableName)
                || Array.from(compRules.partCounts?.entries() || []).find(([k]) => normalizeDepTableName(k) === normalizedSlot)?.[1];
            if (countInfo && countInfo.min === 0 && countInfo.max === 0) return false;
            const allowed = getAllowedPartsForTable(compRules, tableName);
            if (!allowed || allowed.size === 0) return true;
            const hasNormalizedConstraint = !!(compRules.slotConstraints && compRules.slotConstraints.has(normalizedSlot));

            const partKey = partItem?.key || '';
            if (!partKey) return false;
            const partSlotValue = partItem?.[tableName];

            let isExactMatch = false;
            for (const allowedKey of allowed) {
                if (allowedKey.toLowerCase() === partKey.toLowerCase()) {
                    isExactMatch = true;
                    break;
                }
                if (partSlotValue && typeof partSlotValue === 'string' &&
                    allowedKey.toLowerCase() === partSlotValue.toLowerCase()) {
                    isExactMatch = true;
                    break;
                }
            }

            if (isExactMatch) return true;

            // For normalized slot constraints, require exact key/slot-value matches only.
            if (hasNormalizedConstraint) {
                return false;
            }

            return Array.from(allowed).some(allowedKey => {
                if (allowedKey.toLowerCase() === partKey.toLowerCase()) return true;

                if (partKey.startsWith(allowedKey + '_')) return true;
                if (allowedKey.startsWith(partKey + '_')) return true;

                const partBaseMatch = partKey.match(/^(part_\w+_\d+)/);
                const allowedBaseMatch = allowedKey.match(/^(part_\w+_\d+)/);

                if (partBaseMatch && allowedBaseMatch) {
                    const partBase = partBaseMatch[1];
                    const allowedBase = allowedBaseMatch[1];
                    if (partBase === allowedBase) {
                        return allowedKey === allowedBase;
                    }
                }

                return false;
            });
        }

        function parseBooleanLikeFlag(value) {
            if (value === true || value === false) return value;
            if (value === null || value === undefined) return false;
            if (typeof value === 'number') return value !== 0;
            if (typeof value === 'string') return value.trim().toLowerCase() === 'true';
            return false;
        }

        function getRawGlobalPoolFlags(entry) {
            const top = entry || {};
            const nested = entry?.item || {};
            return {
                bexcludefromglobalpooloverride: parseBooleanLikeFlag(
                    top.bexcludefromglobalpooloverride ?? nested.bexcludefromglobalpooloverride
                ),
                bexcludefromglobalpool: parseBooleanLikeFlag(
                    top.bexcludefromglobalpool ?? nested.bexcludefromglobalpool
                )
            };
        }

        function classifyGlobalPoolState(entry) {
            const flags = getRawGlobalPoolFlags(entry);
            const state = (flags.bexcludefromglobalpooloverride || flags.bexcludefromglobalpool)
                ? 'excluded'
                : 'allowed';

            const debugGlobalPoolFlags = typeof window !== 'undefined' && !!window.debugGlobalPoolFlags;
            if (debugGlobalPoolFlags) {
                console.log('[GlobalPoolFlags]', {
                    key: entry?.key || entry?.inv_comp || entry?.item?.key || 'unknown',
                    flags,
                    state
                });
            }

            return state;
        }

        function getGlobalPoolBadge(entry, options = {}) {
            const state = classifyGlobalPoolState(entry);
            const compact = !!options.compact;
            if (state === 'excluded') {
                return {
                    state,
                    text: compact ? 'Global Pool: Excluded' : 'Global Pool Excluded',
                    color: '#ff9f43',
                    background: 'rgba(255, 159, 67, 0.15)',
                    border: '1px solid rgba(255, 159, 67, 0.45)'
                };
            }
            return {
                state,
                text: compact ? 'Global Pool: Allowed' : 'Global Pool Allowed',
                color: '#4CAF50',
                background: 'rgba(76, 175, 80, 0.15)',
                border: '1px solid rgba(76, 175, 80, 0.4)'
            };
        }

        function getInvCompEntryKey(entry) {
            const raw = entry?.key || entry?.inv_comp || entry?.item?.key || entry?.item?.inv_comp;
            if (!raw || typeof raw !== 'string') return null;
            const key = raw.trim().toLowerCase();
            return key.startsWith('comp_') ? key : null;
        }

        function getInvCompQualifiedEntryKey(entry, context = {}) {
            const compKey = getInvCompEntryKey(entry);
            if (!compKey) return null;

            const rootCandidates = [
                context?.rootKey,
                entry?.rootKey,
                entry?.item?.rootKey
            ];
            const rootKey = rootCandidates.find(v => typeof v === 'string' && v.trim().length > 0);
            if (!rootKey) return null;
            return `${rootKey.trim().toLowerCase()}.${compKey}`;
        }

        function getDedicatedLootPoolSources(entry, dataset = data, context = {}) {
            const qualifiedKey = getInvCompQualifiedEntryKey(entry, context);
            const index = dataset?.itemPoolIndex;
            if (!qualifiedKey || !(index instanceof Map) || !index.has(qualifiedKey)) {
                return [];
            }
            return Array.from(index.get(qualifiedKey) || []);
        }

        function classifyDedicatedLootPoolState(entry, dataset = data, context = {}) {
            const pools = getDedicatedLootPoolSources(entry, dataset, context);
            return pools.length > 0 ? 'dedicated' : 'none';
        }

        function getDedicatedLootPoolBadge(entry, options = {}, dataset = data, context = {}) {
            const state = classifyDedicatedLootPoolState(entry, dataset, context);
            if (state !== 'dedicated') return null;
            const compact = !!options.compact;
            const pools = getDedicatedLootPoolSources(entry, dataset, context).sort((a, b) => a.localeCompare(b));
            return {
                state,
                text: compact ? 'Dedicated Loot Pool' : 'Dedicated Loot Pool Source',
                subtextLines: pools,
                tooltip: pools.join('\n'),
                color: '#58a6ff',
                background: 'rgba(88, 166, 255, 0.15)',
                border: '1px solid rgba(88, 166, 255, 0.45)'
            };
        }

        /** Exact key `comp_05_legendary` only — uniques are `comp_05_legendary_*`. */
        function isBareGenericLegendaryComp(entry) {
            return getInvCompEntryKey(entry) === 'comp_05_legendary';
        }

        function isEnhancementRootKey(rootKey) {
            if (rootKey == null || rootKey === '') return false;
            const k = String(rootKey).trim().toLowerCase();
            return k === 'enhancement' || k.endsWith('_enhancement');
        }

        /**
         * Bare comp_05_legendary invalid-drop rule applies to weapons, shields, grenades, etc.
         * Enhancement roots (generic `enhancement` or `*_enhancement`) are excluded — they legitimately use bare comp_05_legendary.
         */
        function isBareGenericLegendaryInvalidDrop(entry, context = {}) {
            if (!isBareGenericLegendaryComp(entry)) return false;
            const rootKey =
                (context.rootKey && String(context.rootKey).trim()) ||
                (entry?.rootKey && String(entry.rootKey).trim()) ||
                '';
            if (isEnhancementRootKey(rootKey)) return false;
            return true;
        }

        const MISSING_INDEXED_LOOT_TOOLTIP =
            'Excluded from the global pool and no dedicated pool references this composition in loaded item pool data. It likely cannot drop from normal sources. Exceptions: SHiFT, Twitch, promos, gifts, hotfixes, or incomplete pool exports.';

        function isInvCompMissingIndexedLootSource(entry, dataset = data, context = {}) {
            if (!getInvCompEntryKey(entry)) return false;
            if (isBareGenericLegendaryInvalidDrop(entry, context)) return false;
            if (classifyGlobalPoolState(entry) !== 'excluded') return false;
            return classifyDedicatedLootPoolState(entry, dataset, context) === 'none';
        }

        const GENERIC_LEGENDARY_INVALID_TOOLTIP =
            'Bare comp_05_legendary is the non-unique legendary composition. Legitimate drops use a unique key such as comp_05_legendary_slugger. This tool flags bare comp_05_legendary as an invalid drop for most item types (dedicated pool hints can mislead, e.g. shields). Enhancement items are excluded.';

        function getGenericLegendaryInvalidDropBadge(entry, options = {}, context = {}) {
            if (!isBareGenericLegendaryInvalidDrop(entry, context)) return null;
            const compact = !!options.compact;
            const qk = getInvCompQualifiedEntryKey(entry, context);
            return {
                state: 'generic_legendary_invalid',
                text: compact ? '✗ GENERIC Legendary' : '✗ GENERIC Legendary (invalid drop)',
                tooltip: GENERIC_LEGENDARY_INVALID_TOOLTIP + (qk ? `\n\nKey: ${qk}` : ''),
                color: '#ff7043',
                background: 'rgba(255, 112, 67, 0.12)',
                border: '1px solid rgba(255, 112, 67, 0.55)'
            };
        }

        function getMissingIndexedLootSourceBadge(entry, options = {}, dataset = data, context = {}) {
            if (!isInvCompMissingIndexedLootSource(entry, dataset, context)) return null;
            const compact = !!options.compact;
            const qk = getInvCompQualifiedEntryKey(entry, context);
            return {
                state: 'missing_indexed_loot',
                text: compact ? '⚠ No known drop pool' : '⚠ No known drop pool (indexed data)',
                tooltip: MISSING_INDEXED_LOOT_TOOLTIP + (qk ? `\n\nKey: ${qk}` : ''),
                color: '#ffc107',
                background: 'rgba(255, 193, 7, 0.14)',
                border: '1px solid rgba(255, 193, 7, 0.5)'
            };
        }

        /** Normalize basetag array (object or string entries) to lowercase tokens. */
        function collectBasetagTokenStrings(entry) {
            const raw = entry?.basetags || entry?.item?.basetags || [];
            if (!Array.isArray(raw)) return [];
            return raw.map(tag => {
                if (typeof tag === 'object' && tag !== null) {
                    const values = Object.values(tag);
                    const keys = Object.keys(tag);
                    return String(values[0] || keys[0] || '').trim().toLowerCase();
                }
                return String(tag || '').trim().toLowerCase();
            }).filter(Boolean);
        }

        function entryHasNpcWeaponBasetag(entry) {
            return collectBasetagTokenStrings(entry).some(t => t === 'npc_weapon' || t.includes('npc_weapon'));
        }

        function getNpcWeaponOnlyBadge(entry, options = {}) {
            if (!entryHasNpcWeaponBasetag(entry)) return null;
            const compact = !!options.compact;
            return {
                state: 'npc_weapon_only',
                text: compact ? 'NPC Only' : 'NPC Only (npc_weapon)',
                tooltip: 'This entry includes the npc_weapon basetag — NPC loadout item, not normal player loot.',
                color: '#ede7f6',
                background: 'rgba(179, 136, 255, 0.35)',
                border: '1px solid rgba(179, 136, 255, 0.85)'
            };
        }

        function getInvCompBadges(entry, options = {}, dataset = data, context = {}) {
            const badges = [];
            const globalBadge = getGlobalPoolBadge(entry, options);
            if (globalBadge) badges.push(globalBadge);
            const dedicatedBadge = getDedicatedLootPoolBadge(entry, options, dataset, context);
            if (dedicatedBadge) badges.push(dedicatedBadge);
            const genericLegendaryBadge = getGenericLegendaryInvalidDropBadge(entry, options, context);
            if (genericLegendaryBadge) badges.push(genericLegendaryBadge);
            const missingLootBadge = getMissingIndexedLootSourceBadge(entry, options, dataset, context);
            if (missingLootBadge) badges.push(missingLootBadge);
            const npcOnlyBadge = getNpcWeaponOnlyBadge(entry, options);
            if (npcOnlyBadge) badges.push(npcOnlyBadge);
            return badges;
        }

        const RARITY_OUTLINE_COLORS = {
            common: '#9e9e9e',
            uncommon: '#4caf50',
            rare: '#2196f3',
            epic: '#9c27b0',
            legendary: '#f57c00',
            pearlescent: '#00e5ff'
        };

        function getInvCompRarityOutlineColor(entry, partDisplayName) {
            if (miInvCompDepIsPearlescent(entry)) return RARITY_OUTLINE_COLORS.pearlescent;
            const rarityField = (entry?.rarity || entry?.item?.rarity || '').toLowerCase();
            if (/\bpearlescent\b/i.test(rarityField)) return RARITY_OUTLINE_COLORS.pearlescent;
            const key = (entry?.key || '').toLowerCase();
            const name = (partDisplayName || '').toLowerCase();
            const combined = key + ' ' + name;
            if (/\bpearlescent\b/i.test(combined)) return RARITY_OUTLINE_COLORS.pearlescent;
            if (combined.includes('legendary')) return RARITY_OUTLINE_COLORS.legendary;
            if (combined.includes('epic')) return RARITY_OUTLINE_COLORS.epic;
            if (combined.includes('rare')) return RARITY_OUTLINE_COLORS.rare;
            if (combined.includes('uncommon')) return RARITY_OUTLINE_COLORS.uncommon;
            if (combined.includes('common')) return RARITY_OUTLINE_COLORS.common;
            return null;
        }

        function renderBadgePills(badges, options = {}) {
            if (!Array.isArray(badges) || badges.length === 0) return '';
            const fontSize = options.fontSize || 8;
            const subtextFontSize = Math.max(7, fontSize - 1);
            const marginTop = options.marginTop !== undefined ? options.marginTop : 2;
            const marginBottom = options.marginBottom;
            const marginBottomCss = marginBottom !== undefined ? ` margin-bottom: ${marginBottom}px;` : '';
            const gap = options.gap || 3;

            return `
                <div style="margin-top: ${marginTop}px;${marginBottomCss} display: flex; flex-wrap: wrap; align-items: flex-start; gap: ${gap}px;">
                    ${badges.map(badge => `
                        <span title="${escapeLegitHtml(badge.tooltip || '')}" style="display: inline-flex; flex-direction: column; align-items: flex-start; box-sizing: border-box; width: fit-content; max-width: min(100%, 420px); padding: 2px 6px; border-radius: 8px; font-size: ${fontSize}px; line-height: 1.2; font-weight: 600; color: ${badge.color}; background: ${badge.background}; border: ${badge.border};">
                            <span style="display: block;">${badge.text}</span>
                            ${(Array.isArray(badge.subtextLines) && badge.subtextLines.length > 0) ? `
                                <span style="display: block; margin-top: 2px; font-size: ${subtextFontSize}px; line-height: 1.25; font-weight: 600; color: rgba(236, 246, 255, 0.98); white-space: normal; word-break: break-word; text-wrap: pretty;">
                                    ${badge.subtextLines.map(line => `<span style="display: block;">${line}</span>`).join('')}
                                </span>
                            ` : (badge.subtext ? `<span style="display: block; margin-top: 2px; font-size: ${subtextFontSize}px; line-height: 1.25; font-weight: 600; color: rgba(236, 246, 255, 0.98); white-space: normal; word-break: break-word; text-wrap: pretty;">${badge.subtext}</span>` : '')}
                        </span>
                    `).join('')}
                </div>
            `;
        }

        // Enforce comp-level tag caps from parttagselectionrules (e.g., licensed max).
        function enforcePartTagMaxRules(validatedParts, compRules, formatTags) {
            const rules = Array.isArray(compRules?.partTagMaxRules) ? compRules.partTagMaxRules : [];
            if (rules.length === 0) return [];

            const partsByTag = new Map();
            validatedParts.forEach(part => {
                if (!part || !part.found || part.isValid === false || part.partType === 'inv_comp' || !part.foundPart) return;
                const addTags = formatTags(part.foundPart.addtags || []).map(t => String(t).toLowerCase());
                addTags.forEach(tag => {
                    if (!partsByTag.has(tag)) partsByTag.set(tag, []);
                    partsByTag.get(tag).push(part);
                });
            });

            const violations = [];
            rules.forEach(rule => {
                const max = typeof rule.max === 'number' && !isNaN(rule.max) ? rule.max : null;
                const tags = Array.isArray(rule.tags) ? rule.tags : [];
                if (max === null || tags.length === 0) return;

                tags.forEach(rawTag => {
                    const tag = String(rawTag).toLowerCase();
                    const taggedParts = partsByTag.get(tag) || [];
                    if (taggedParts.length > max) {
                        violations.push({
                            tag,
                            max,
                            found: taggedParts.length,
                            overflowParts: taggedParts.slice(max)
                        });
                    }
                });
            });

            return violations;
        }

        /** Same part→tag indexing as enforcePartTagMaxRules (for hypothetical slot fill checks). */
        function buildPartsByAddTagMap(validatedParts, formatTagsFn) {
            const partsByTag = new Map();
            if (!validatedParts || !formatTagsFn) return partsByTag;
            validatedParts.forEach(part => {
                if (!part || !part.found || part.isValid === false || part.partType === 'inv_comp' || !part.foundPart) return;
                const addTags = formatTagsFn(part.foundPart.addtags || []).map(t => String(t).toLowerCase());
                addTags.forEach(tag => {
                    if (!partsByTag.has(tag)) partsByTag.set(tag, []);
                    partsByTag.get(tag).push(part);
                });
            });
            return partsByTag;
        }

        function isDepFillableUnderPartTagMaxRules(dep, partsByTag, partTagMaxRules, formatTagsFn) {
            const rules = Array.isArray(partTagMaxRules) ? partTagMaxRules : [];
            if (rules.length === 0) return true;
            if (!dep || !formatTagsFn) return true;
            const depAddTags = new Set(formatTagsFn(dep.addtags || []).map(t => String(t).toLowerCase()));
            for (let i = 0; i < rules.length; i++) {
                const rule = rules[i];
                const max = typeof rule.max === 'number' && !isNaN(rule.max) ? rule.max : null;
                const tags = Array.isArray(rule.tags) ? rule.tags : [];
                if (max === null || tags.length === 0) continue;
                for (let j = 0; j < tags.length; j++) {
                    const tag = String(tags[j]).toLowerCase();
                    if (!depAddTags.has(tag)) continue;
                    const current = (partsByTag.get(tag) || []).length;
                    if (current + 1 > max) return false;
                }
            }
            return true;
        }

        function filterDepsFillableUnderPartTagMaxRules(deps, compRules, validatedParts, formatTagsFn) {
            if (!deps || deps.length === 0) return [];
            const rules = compRules && compRules.partTagMaxRules;
            if (!Array.isArray(rules) || rules.length === 0) return deps.slice();
            const partsByTag = buildPartsByAddTagMap(validatedParts, formatTagsFn);
            return deps.filter(dep => isDepFillableUnderPartTagMaxRules(dep, partsByTag, rules, formatTagsFn));
        }

        function formatValidationTags(tags) {
            if (!tags || !Array.isArray(tags)) return [];
            return tags.map(tag => {
                if (typeof tag === 'object' && tag !== null) {
                    const values = Object.values(tag);
                    return values[0] || Object.keys(tag)[0] || '';
                }
                return tag;
            }).filter(Boolean);
        }

        function renderSlotConstraintDiagnosticsHtml(slotConstraintDiagnostics) {
            if (!Array.isArray(slotConstraintDiagnostics) || slotConstraintDiagnostics.length === 0) {
                return '';
            }

            const sourceLabels = {
                current_comp: 'Current Comp',
                basecomposition: 'Basecomp',
                base_generic: 'Base Generic',
                unknown: 'Unknown'
            };
            const toLimitedList = (parts, emptyLabel = 'None', maxItems = 14) => {
                if (!Array.isArray(parts) || parts.length === 0) return emptyLabel;
                if (parts.length <= maxItems) return parts.join(', ');
                const remaining = parts.length - maxItems;
                return `${parts.slice(0, maxItems).join(', ')} ... (+${remaining} more)`;
            };

            const rows = slotConstraintDiagnostics.map(entry => {
                const sourceLabel = sourceLabels[entry.source] || entry.source || 'Unknown';
                const totalCandidates = Array.isArray(entry.allRootParts) ? entry.allRootParts.length : 0;
                const allowedCount = Array.isArray(entry.allowedParts) ? entry.allowedParts.length : 0;
                const excludedCount = Array.isArray(entry.excludedParts) ? entry.excludedParts.length : 0;
                return `
                    <div style="margin-bottom: 8px; padding: 8px; background: #2a2a2a; border-radius: 4px; border-left: 2px solid #4a9eff;">
                        <div style="font-size: 11px; color: #9ecbff; font-weight: 700; margin-bottom: 4px;">
                            ${entry.slotName}
                            <span style="margin-left: 8px; color: #cfd8dc; font-weight: 500;">(${sourceLabel}${entry.inherited ? ', inherited' : ''})</span>
                            <span style="margin-left: 8px; color: #8bc34a; font-weight: 500;">allowed ${allowedCount}/${totalCandidates}</span>
                        </div>
                        <div style="font-size: 10px; color: #aaa;"><strong>Allowed by definition:</strong> <span style="color: #8bc34a;">${toLimitedList(entry.allowedParts)}</span></div>
                        <div style="font-size: 10px; color: #aaa;"><strong>Excluded by definition:</strong> <span style="color: #ffb74d;">${toLimitedList(entry.excludedParts)}</span></div>
                    </div>
                `;
            }).join('');

            return `
                <details style="margin-top: 10px; padding: 8px; background: #1f2730; border-radius: 4px; border-left: 2px solid #4a9eff;">
                    <summary style="cursor: pointer; font-size: 11px; color: #4a9eff; font-weight: bold; user-select: none;">Definition Constraints (Allowed vs Excluded)</summary>
                    <div style="margin-top: 8px;">${rows}</div>
                </details>
            `;
        }

        function renderTagLineForValidation(label, tags, symbol, symbolColor) {
            if (!tags || tags.length === 0) {
                return `<div><strong>${label}:</strong> None</div>`;
            }
            const styled = tags.map(tag => `<span style="color: ${symbolColor};">${tag}</span>`).join(', ');
            return `<div><strong>${label}:</strong> <span style="color: ${symbolColor};">${symbol}</span> ${styled}</div>`;
        }

        /** Derive a short display name from part key when no ui name exists (e.g. part_barrel_01_kickballer -> Kickballer). */
        function getDisplayNameFromPartKey(partKey) {
            if (!partKey || typeof partKey !== 'string') return '';
            const segments = partKey.trim().split('_').filter(Boolean);
            if (segments.length === 0) return '';
            const last = segments[segments.length - 1];
            return last.charAt(0).toUpperCase() + last.slice(1).toLowerCase();
        }

        /** Canonical element order for validator card display (must match roundup sort). */
        const VALIDATION_ELEMENT_ORDER = ['normal', 'fire', 'cryo', 'shock', 'corrosive', 'radiation'];
        const VALIDATION_ELEMENT_NAME_SET = new Set(VALIDATION_ELEMENT_ORDER);

        /** Parse Nexus-style damagetype strings (e.g. damagetype'Fire') to lowercase element key or null. */
        function parseValidatorAspectDamagetype(val) {
            if (val == null || typeof val !== 'string') return null;
            const m = val.match(/damagetype'([^']*)'/i) || val.match(/'([^']*)'/);
            const raw = m ? m[1].trim() : val.trim();
            if (!raw) return null;
            return raw.toLowerCase();
        }

        function collectValidatorAspectDamageTypes(foundPart, outSet) {
            if (!foundPart) return;
            const lists = [foundPart.aspects, foundPart.item && foundPart.item.aspects];
            lists.forEach(function (aspects) {
                if (!aspects || !Array.isArray(aspects)) return;
                aspects.forEach(function (aspect) {
                    if (!aspect) return;
                    let dt = parseValidatorAspectDamagetype(aspect.damagetype);
                    if (dt && VALIDATION_ELEMENT_NAME_SET.has(dt)) outSet.add(dt);
                    if (aspect.behavior && aspect.behavior.damagetype) {
                        dt = parseValidatorAspectDamagetype(aspect.behavior.damagetype);
                        if (dt && VALIDATION_ELEMENT_NAME_SET.has(dt)) outSet.add(dt);
                    }
                });
            });
        }

        function collectValidatorKeyElementTokens(partKey, outSet) {
            if (!partKey || typeof partKey !== 'string') return;
            partKey
                .toLowerCase()
                .split('_')
                .filter(Boolean)
                .forEach(function (seg) {
                    if (VALIDATION_ELEMENT_NAME_SET.has(seg)) outSet.add(seg);
                });
        }

        function validationTagImpliesElement(tagLower, elem) {
            return (
                tagLower === elem ||
                tagLower.startsWith(elem + '_') ||
                tagLower.endsWith('_' + elem) ||
                tagLower.includes('_' + elem + '_')
            );
        }

        function collectValidatorAddtagElements(foundPart, outSet) {
            if (!foundPart) return;
            const addTags = formatValidationTags(foundPart.addtags || []);
            addTags.forEach(function (t) {
                const tagLower = String(t).toLowerCase();
                VALIDATION_ELEMENT_ORDER.forEach(function (elem) {
                    if (validationTagImpliesElement(tagLower, elem)) outSet.add(elem);
                });
            });
        }

        function sortValidationElementTypes(types) {
            const order = VALIDATION_ELEMENT_ORDER;
            return types.slice().sort(function (a, b) {
                return order.indexOf(a) - order.indexOf(b);
            });
        }

        /**
         * All elemental types for validator card (body_ele / secondary_ele): union of aspect damagetype,
         * part-key tokens (dual secondary_ele), and addtags (e.g. radiation_secondary).
         */
        function getElementalDamageTypesForValidationPart(part) {
            const partType = (part.partType || '').toLowerCase();
            if (partType !== 'body_ele' && partType !== 'secondary_ele') return [];
            const out = new Set();
            if (part.foundPart) {
                collectValidatorAspectDamageTypes(part.foundPart, out);
                collectValidatorAddtagElements(part.foundPart, out);
            }
            collectValidatorKeyElementTokens(part.partKey, out);
            return sortValidationElementTypes(Array.from(out));
        }

        function buildValidatorDamageLineHtml(elementalTypes) {
            if (!elementalTypes || elementalTypes.length === 0) return '';
            const spans = elementalTypes
                .map(function (e) {
                    return `<span style="color: ${getDamageTypeColor(e)}; font-weight: 600;">${e.charAt(0).toUpperCase() + e.slice(1)}</span>`;
                })
                .join(', ');
            return `<div style="margin-top: 2px;"><strong>Damage:</strong> ${spans}</div>`;
        }

        function renderSerialValidationDetailsHtml(validation, options = {}) {
            const details = validation?.details || {};
            const validatedParts = Array.isArray(details.validatedParts) ? details.validatedParts : [];
            const validationIssues = Array.isArray(details.validationIssues) ? details.validationIssues : [];
            const validationClass = details.validationClass || (validation?.valid ? SERIAL_VALIDATION_CLASS.PASS_ALL : SERIAL_VALIDATION_CLASS.HARD_FAIL);
            const classPresentation = getSerialValidationClassPresentation(validationClass);
            const legitimacyColor = classPresentation.color;
            const legitimacyIcon = classPresentation.icon;
            const legitimacyLabel = classPresentation.label;
            const reorderedSerial = details.reorderedSerial || '';
            const renderContext = options?.source === 'bulk' ? 'bulk' : 'single';
            const lineNumber = Number.isFinite(Number(validation?.lineNumber)) ? Number(validation.lineNumber) : '';
            const translationOnly = !!options.translationOnly;
            const moddedDecodeNote = translationOnly ? details.moddedDecodeNote : null;
            const moddedUnknownCount =
                translationOnly && details.unknownPartsCount != null
                    ? Number(details.unknownPartsCount)
                    : null;
            const moddedForeignCount =
                translationOnly && details.foreignRootPartsCount != null
                    ? Number(details.foreignRootPartsCount)
                    : null;
            const slotConstraintDiagnostics = Array.isArray(details.slotConstraintDiagnostics) ? details.slotConstraintDiagnostics : [];
            const slotConstraintDiagnosticsHtml = translationOnly
                ? ''
                : renderSlotConstraintDiagnosticsHtml(slotConstraintDiagnostics);

            const data = options.data || null;
            const partRows = validatedParts.map(part => {
                if (translationOnly) {
                    const partDisplay = part.displaySerial || formatValidatorPartTokenFallback(part);
                    const isForeignRoot = part.found && part.rootMatches === false;
                    const isUnknown = !part.found;
                    const rowBorder = isUnknown ? '#ef5350' : isForeignRoot ? '#ffb74d' : '#9575cd';
                    let descRedHtmlTr = '';
                    if (part.found && part.foundPart && data) {
                        const aspects = part.foundPart.aspects || part.foundPart.item?.aspects || [];
                        const uiStatInfo = getUiStatDescriptionAndRedtext(aspects, data);
                        if (uiStatInfo.descriptionTexts?.length || uiStatInfo.redtexts?.length || uiStatInfo.primaryTexts?.length) {
                            descRedHtmlTr =
                                (uiStatInfo.descriptionTexts?.length
                                    ? `<div style="margin-top: 2px;"><span style="color: var(--text-secondary);">Description:</span> <span style="font-weight: 500;">${formatUiStatTextToHtml(uiStatInfo.descriptionTexts[0])}</span></div>`
                                    : '') +
                                (uiStatInfo.primaryTexts?.length
                                    ? uiStatInfo.primaryTexts.map((t) => `<div style="margin-top: 2px;"><span style="color: var(--text-secondary);">Effect:</span> <span style="font-weight: 500;">${formatUiStatTextToHtml(t)}</span></div>`).join('')
                                    : '') +
                                (uiStatInfo.redtexts?.length
                                    ? `<div style="margin-top: 2px;"><span style="color: var(--text-secondary);">Red text:</span> <span style="font-style: italic; color: #ef5350;">${formatUiStatTextToHtml(uiStatInfo.redtexts[0])}</span></div>`
                                    : '');
                        }
                    }
                    const friendlyName = part.partKey ? getDisplayNameFromPartKey(part.partKey) : '';
                    const passiveFmtTr = part.found && part.foundPart && data ? getPassivePointsTooltipFormatText(part, data) : '';
                    const passiveFmtTrHtml = passiveFmtTr
                        ? `<div style="margin-top: 6px;"><strong style="color: #9e9e9e;">Format text:</strong> <span style="display:block;margin-top:3px;font-weight:500;color:#d0d0d0;line-height:1.45;word-break:break-word;">${formatUiStatTextToHtml(passiveFmtTr)}</span></div>`
                        : '';
                    const removePartBtnTr =
                        part.patternTokenIndex != null
                            ? `<button type="button" onclick="legitRemoveValidatorPart(${part.index}, this)" title="Remove this token from the serial" style="flex-shrink: 0; background: #5d2727; border: 1px solid #b71c1c; color: #ffbcbc; font-size: 10px; padding: 3px 8px; border-radius: 4px; cursor: pointer; font-weight: 600;">Remove</button>`
                            : '';
                    const foreignRootHint =
                        isForeignRoot && part.rootSerial != null
                            ? `<div style="font-size: 10px; color: #ffb74d; margin-bottom: 4px;">Cross-root — from inventory root <strong>${part.rootSerial}</strong> (expected for modded; fails legit balance on this item)</div>`
                            : isForeignRoot
                              ? `<div style="font-size: 10px; color: #ffb74d; margin-bottom: 4px;">Cross-root part (expected for modded)</div>`
                              : '';
                    return `
                    <div style="margin-bottom: 8px; padding: 8px; background: #2a2a2a; border-radius: 4px; border-left: 2px solid ${rowBorder};">
                        <div style="display: flex; justify-content: space-between; align-items: flex-start; gap: 8px; margin-bottom: 4px;">
                            <div style="font-size: 11px; color: #ce93d8; font-weight: bold;">Part ${part.index}: ${partDisplay}</div>
                            ${removePartBtnTr}
                        </div>
                        ${foreignRootHint}
                        ${
                            part.found
                                ? `
                            <div style="font-size: 10px; color: #aaa;">
                                <div><strong>Part Key:</strong> ${part.partKey || 'N/A'}</div>
                                ${friendlyName ? `<div><strong>Name:</strong> ${escapeLegitHtml(friendlyName)}</div>` : ''}
                                ${passiveFmtTrHtml}
                                ${descRedHtmlTr}
                            </div>
                        `
                                : `
                            <div style="font-size: 10px; color: #ff4444;">
                                <strong>Unknown token:</strong> not in Nexus for this item root${
                                    part.rootSerial != null ? ` (tried root ${part.rootSerial})` : ''
                                }
                            </div>
                        `
                        }
                    </div>
                `;
                }
                const partFoundAndRootMatch = part.found && (part.isBaseComp ? part.rootMatches : true);
                const partIsValid = part.isValid !== undefined ? part.isValid : partFoundAndRootMatch;
                const orderIssue = !!part.orderIssue || /out[- ]of[- ]canonical[- ]order|out-of-order/i.test(String(part.validationIssue || ''));
                const statusColor = orderIssue ? '#ff9800' : (partIsValid ? '#4CAF50' : '#ff4444');
                const statusIcon = orderIssue ? '⚠' : (partIsValid ? '✓' : '✗');
                const partDisplay = part.displaySerial || formatValidatorPartTokenFallback(part);
                const addTags = part.foundPart ? formatValidationTags(part.foundPart.addtags || []) : [];
                const depTags = part.foundPart ? formatValidationTags(part.foundPart.dependencytags || []) : [];
                const exclTags = part.foundPart ? formatValidationTags(part.foundPart.exclusiontags || []) : [];
                const baseTags = part.foundPart ? formatValidationTags(part.foundPart.basetags || []) : [];
                const elementalTypesRow =
                    part.found && part.foundPart ? getElementalDamageTypesForValidationPart(part) : [];
                const damageLineHtml = buildValidatorDamageLineHtml(elementalTypesRow);
                let descRedHtml = '';
                if (part.found && part.foundPart && data) {
                    const aspects = part.foundPart.aspects || part.foundPart.item?.aspects || [];
                    const uiStatInfo = getUiStatDescriptionAndRedtext(aspects, data);
                    if (uiStatInfo.descriptionTexts?.length || uiStatInfo.redtexts?.length || uiStatInfo.primaryTexts?.length) {
                        descRedHtml = (uiStatInfo.descriptionTexts?.length ? `<div style="margin-top: 2px;"><span style="color: var(--text-secondary);">Description:</span> <span style="font-weight: 500;">${formatUiStatTextToHtml(uiStatInfo.descriptionTexts[0])}</span></div>` : '') +
                            (uiStatInfo.primaryTexts?.length ? uiStatInfo.primaryTexts.map((t) => `<div style="margin-top: 2px;"><span style="color: var(--text-secondary);">Effect:</span> <span style="font-weight: 500;">${formatUiStatTextToHtml(t)}</span></div>`).join('') : '') +
                            (uiStatInfo.redtexts?.length ? `<div style="margin-top: 2px;"><span style="color: var(--text-secondary);">Red text:</span> <span style="font-style: italic; color: #ef5350;">${formatUiStatTextToHtml(uiStatInfo.redtexts[0])}</span></div>` : '');
                    }
                }
                const partDebugDescVal = part.found && part.foundPart ? getPartDebugDisplayDescription(part.foundPart) : '';
                const partDebugLineHtml = partDebugDescVal
                    ? `<div style="margin-top: 4px;"><span style="color: ${LEGIT_DEBUG_LABEL_FG}; font-weight: 700;">Debug:</span> <span style="color: ${LEGIT_DEBUG_DESC_FG}; font-weight: 600; text-shadow: 0 1px 2px rgba(0,0,0,0.75);">${escapeLegitHtml(partDebugDescVal)}</span></div>`
                    : '';
                const lootCtxVal = {
                    rootKey:
                        (part.foundInRoot && String(part.foundInRoot).trim()) ||
                        (details.rootKey || '')
                };
                const validatorInvCompBadges =
                    (part.partType || '').toLowerCase() === 'inv_comp' && part.foundPart && data
                        ? getInvCompBadges(part.foundPart, { compact: true }, data, lootCtxVal)
                        : [];
                const validatorInvCompBadgesHtml = validatorInvCompBadges.length
                    ? renderBadgePills(validatorInvCompBadges, { marginTop: 4, fontSize: 8 })
                    : '';
                const passiveFmtVal = part.found && part.foundPart && data ? getPassivePointsTooltipFormatText(part, data) : '';
                const passiveFmtHtml =
                    passiveFmtVal
                        ? `<div style="margin-top: 6px;"><strong style="color: #9e9e9e;">Format text:</strong> <span style="display:block;margin-top:3px;font-weight:500;color:#d0d0d0;line-height:1.45;word-break:break-word;">${formatUiStatTextToHtml(passiveFmtVal)}</span></div>`
                        : '';
                const removePartBtn =
                    part.patternTokenIndex != null
                        ? `<button type="button" onclick="legitRemoveValidatorPart(${part.index}, this)" title="Remove this token from the serial" style="flex-shrink: 0; background: #5d2727; border: 1px solid #b71c1c; color: #ffbcbc; font-size: 10px; padding: 3px 8px; border-radius: 4px; cursor: pointer; font-weight: 600;">Remove</button>`
                        : '';
                return `
                    <div style="margin-bottom: 8px; padding: 8px; background: #2a2a2a; border-radius: 4px; border-left: 2px solid ${statusColor};">
                        <div style="display: flex; justify-content: space-between; align-items: flex-start; gap: 8px; margin-bottom: 4px;">
                            <div style="font-size: 11px; color: ${statusColor}; font-weight: bold;">${statusIcon} Part ${part.index}: ${partDisplay}</div>
                            ${removePartBtn}
                        </div>
                        ${part.found ? `
                            <div style="font-size: 10px; color: #aaa;">
                                <div><strong>Part Key:</strong> ${part.partKey || 'N/A'}</div>
                                <div><strong>Type:</strong> ${part.partType}</div>
                                <div><strong>Found In:</strong> ${part.foundInRoot || 'N/A'}</div>
                                ${partDebugLineHtml}
                                ${validatorInvCompBadgesHtml}
                                ${damageLineHtml}
                                ${renderTagLineForValidation('Addtags', addTags, '+', 'var(--accent-secondary)')}
                                ${renderTagLineForValidation('Basetags', baseTags, '◆', 'var(--accent-primary)')}
                                ${renderTagLineForValidation('Dependencytags', depTags, '→', 'var(--accent-warning)')}
                                ${renderTagLineForValidation('Exclusiontags', exclTags, '✗', 'var(--accent-danger)')}
                                ${passiveFmtHtml}
                                ${descRedHtml}
                                ${orderIssue ? `<div style="color: #ff9800; margin-top: 4px;"><strong>Order Issue:</strong> Part order mismatch (highlighted in orange)</div>` : ''}
                                ${part.validationIssue ? `<div style="color: #ff4444; margin-top: 4px;"><strong>Validation Issue:</strong> ${part.validationIssue}</div>` : ''}
                            </div>
                        ` : `
                            <div style="font-size: 10px; color: #ff4444;">
                                <strong>Not Found:</strong> Serial ${part.serial} not found in loaded data
                            </div>
                        `}
                    </div>
                `;
            }).join('');

            // Top-card roundup: unique names from parts (comp, barrel, body, etc.) and elements from body_ele/secondary_ele
            const namePartOrder = ['inv_comp', 'barrel', 'body', 'body_acc', 'body_ele', 'magazine', 'scope', 'grip', 'foregrip', 'secondary_ele'];
            const nameParts = [];
            namePartOrder.forEach(tableName => {
                const part = validatedParts.find(p => (p.partType || '').toLowerCase() === tableName.toLowerCase() && p.found);
                if (!part) return;
                const d = (part.displaySerial || '').trim();
                const name = (!d || /^\{\d+(:\d+)?\}$/.test(d)) ? getDisplayNameFromPartKey(part.partKey) : d;
                if (!name) return;
                nameParts.push({ type: tableName, name });
            });
            const nameRoundupHtml = nameParts.length > 0
                ? `<div style="margin-top: 6px; padding: 6px 8px; background: rgba(0,0,0,0.2); border-radius: 4px;"><strong>Name:</strong> ${nameParts.map(p => `<span style="color: var(--text-primary);">${p.name}</span>`).join(' <span style="color: var(--text-muted);">/</span> ')}</div>`
                : '';
            const elementTypes = [];
            validatedParts.forEach(p => {
                if (!p.found || !p.foundPart) return;
                getElementalDamageTypesForValidationPart(p).forEach(function (elem) {
                    if (elem && !elementTypes.includes(elem)) elementTypes.push(elem);
                });
            });
            elementTypes.sort(
                (a, b) => VALIDATION_ELEMENT_ORDER.indexOf(a) - VALIDATION_ELEMENT_ORDER.indexOf(b)
            );
            let elementsRoundupHtml = elementTypes.length > 0
                ? `<div style="margin-top: 4px;"><strong>Elements:</strong> ${elementTypes.map(e => `<span style="color: ${getDamageTypeColor(e)}; font-weight: 600;">${e.charAt(0).toUpperCase() + e.slice(1)}</span>`).join(', ')}</div>`
                : '';
            if (translationOnly) elementsRoundupHtml = '';

            const missingSlotsRaw = Array.isArray(details.missingRequiredSlots) ? details.missingRequiredSlots : [];
            const missingSlotsFixHtml =
                translationOnly || missingSlotsRaw.length === 0
                    ? ''
                    : `
                <div style="margin-top: 12px; padding: 10px; background: rgba(76, 175, 80, 0.08); border: 1px solid rgba(76, 175, 80, 0.35); border-radius: 6px;">
                    <div style="font-size: 12px; color: #81c784; font-weight: 700; margin-bottom: 8px;">Fix missing slots</div>
                    <div style="font-size: 10px; color: #aaa; margin-bottom: 10px;">Pick a qualifying part and apply: selections sync to the Legit Builder and the human-readable serial is regenerated.</div>
                    ${missingSlotsRaw
                        .map((slot, slotIdx) => {
                            const cands = Array.isArray(slot.candidates) ? [...slot.candidates] : [];
                            cands.sort((a, b) => String(a.label || a.partKey || '').localeCompare(String(b.label || b.partKey || '')));
                            const slotTitle = escapeLegitHtml(slot.slotName || slot.normalizedSlot || 'slot');
                            const opts =
                                cands.length === 0
                                    ? ''
                                    : `<select data-missing-slot-select style="flex: 1; min-width: 140px; max-width: 100%; background: #111; color: #ddd; border: 1px solid #444; border-radius: 4px; font-size: 11px; padding: 4px 6px;">${cands
                                          .map(
                                              c =>
                                                  `<option value="${escapeLegitHtml(c.partKey || '')}">${escapeLegitHtml(c.label || c.partKey || '')}</option>`
                                          )
                                          .join('')}</select>`;
                            const emptyNote =
                                cands.length === 0
                                    ? `<div style="font-size: 10px; color: #888;">No parts qualify under the current tag pool for this slot.</div>`
                                    : '';
                            const btn =
                                cands.length === 0
                                    ? ''
                                    : `<button type="button" onclick="legitApplyValidatorSlotPick('missing', ${slotIdx}, this)" style="background: #2e7d32; border: 1px solid #3d9a43; color: #fff; font-size: 11px; padding: 5px 10px; border-radius: 4px; cursor: pointer; font-weight: 600; white-space: nowrap;">Apply &amp; update serial</button>`;
                            return `
                        <div data-missing-slot-row="${slotIdx}" style="margin-bottom: 10px; padding: 8px; background: rgba(0,0,0,0.25); border-radius: 4px; border-left: 2px solid #66bb6a;">
                            <div style="font-size: 11px; color: #c8e6c9; font-weight: 600; margin-bottom: 6px;">${slotTitle}${slot.compatibility ? ' <span style="color:#ffb74d;">(compatibility)</span>' : ''} — need ${escapeLegitHtml(String(slot.required))}, have ${escapeLegitHtml(String(slot.current))}</div>
                            <div style="display: flex; flex-wrap: wrap; gap: 8px; align-items: center;">${opts}${btn}</div>
                            ${emptyNote}
                        </div>`;
                        })
                        .join('')}
                </div>`;

            const extraSlotsRaw = Array.isArray(details.extraOptionalSlots) ? details.extraOptionalSlots : [];
            const extraOptionalSlotsFixHtml =
                translationOnly || extraSlotsRaw.length === 0
                    ? ''
                    : `
                <div style="margin-top: 12px; padding: 10px; background: rgba(66, 165, 245, 0.08); border: 1px solid rgba(66, 165, 245, 0.35); border-radius: 6px;">
                    <div style="font-size: 12px; color: #64b5f6; font-weight: 700; margin-bottom: 8px;">Extra options</div>
                    <div style="font-size: 10px; color: #aaa; margin-bottom: 10px;">Add optional parts up to each slot&apos;s maximum. Only parts compatible with the current tag pool are listed; parts already in the serial for that slot are hidden.</div>
                    ${extraSlotsRaw
                        .map((slot, slotIdx) => {
                            const cands = Array.isArray(slot.candidates) ? [...slot.candidates] : [];
                            cands.sort((a, b) => String(a.label || a.partKey || '').localeCompare(String(b.label || b.partKey || '')));
                            const slotTitle = escapeLegitHtml(slot.slotName || slot.normalizedSlot || 'slot');
                            const opts =
                                cands.length === 0
                                    ? ''
                                    : `<select data-extra-slot-select style="flex: 1; min-width: 140px; max-width: 100%; background: #111; color: #ddd; border: 1px solid #444; border-radius: 4px; font-size: 11px; padding: 4px 6px;">${cands
                                          .map(
                                              c =>
                                                  `<option value="${escapeLegitHtml(c.partKey || '')}">${escapeLegitHtml(c.label || c.partKey || '')}</option>`
                                          )
                                          .join('')}</select>`;
                            const emptyNote =
                                cands.length === 0
                                    ? `<div style="font-size: 10px; color: #888;">No additional qualifying parts for this slot.</div>`
                                    : '';
                            const btn =
                                cands.length === 0
                                    ? ''
                                    : `<button type="button" onclick="legitApplyValidatorSlotPick('extra', ${slotIdx}, this)" style="background: #1565c0; border: 1px solid #1e7ad8; color: #fff; font-size: 11px; padding: 5px 10px; border-radius: 4px; cursor: pointer; font-weight: 600; white-space: nowrap;">Apply &amp; update serial</button>`;
                            return `
                        <div data-extra-slot-row="${slotIdx}" style="margin-bottom: 10px; padding: 8px; background: rgba(0,0,0,0.25); border-radius: 4px; border-left: 2px solid #42a5f5;">
                            <div style="font-size: 11px; color: #90caf9; font-weight: 600; margin-bottom: 6px;">${slotTitle}${slot.compatibility ? ' <span style="color:#ffb74d;">(compatibility)</span>' : ''} — ${escapeLegitHtml(String(slot.current))} / ${escapeLegitHtml(String(slot.max))} (can add more)</div>
                            <div style="display: flex; flex-wrap: wrap; gap: 8px; align-items: center;">${opts}${btn}</div>
                            ${emptyNote}
                        </div>`;
                        })
                        .join('')}
                </div>`;

            const hasDecodedSerial =
                (details.inputSerial != null && String(details.inputSerial).trim().length > 0) ||
                validatedParts.some(p => p && p.found);
            const hasFoundPartKey = validatedParts.some(p => p && p.found && p.partKey);
            const canPushBuilder = translationOnly
                ? hasDecodedSerial && hasFoundPartKey
                : validationClass === SERIAL_VALIDATION_CLASS.PASS_ALL ||
                  (validationClass === SERIAL_VALIDATION_CLASS.FAIL_ORDER &&
                      String(reorderedSerial || '').trim().length > 0);

            const pushBuilderTitleOk = translationOnly
                ? 'Apply decoded parts to Modded Items builder'
                : 'Clear builder and apply these parts';
            const pushBuilderTitleNo = translationOnly
                ? 'Need decoded serial with at least one resolved part'
                : renderContext === 'single'
                  ? 'Only pass or order-only results can be pushed (reorder flow when needed)'
                  : 'Only pass or order-only results can be pushed';

            const bulkBackpackHtml = renderContext === 'bulk'
                ? `
                    <div style="margin-top: 12px; padding-top: 10px; border-top: 1px solid #333; display: flex; flex-wrap: wrap; gap: 8px; align-items: center;">
                        <button type="button" id="legitBulkInspectBackpackBtn" onclick="legitBulkInspectAddToBackpackClick()" style="background: var(--accent-primary, #4a9eff); border: none; padding: 6px 12px; color: #fff; font-size: 12px; cursor: pointer; font-weight: 600; border-radius: 4px;">Add to Backpack</button>
                        <button type="button" onclick="pushValidationToLegitBuilderFromInspectModal()" ${canPushBuilder ? '' : 'disabled'} style="background: ${canPushBuilder ? '#6a1b9a' : '#444'}; border: none; padding: 6px 12px; color: #fff; font-size: 12px; cursor: ${canPushBuilder ? 'pointer' : 'not-allowed'}; font-weight: 600; border-radius: 4px; opacity: ${canPushBuilder ? '1' : '0.55'};" title="${canPushBuilder ? pushBuilderTitleOk : pushBuilderTitleNo}">Push to builder</button>
                    </div>
                `
                : '';

            const saveEditorModal = !!options.saveEditorModal;
            const singlePushOnclick = saveEditorModal
                ? 'pushValidationToLegitBuilderFromSaveEditorModal()'
                : 'pushValidationToLegitBuilderFromUi()';
            const singlePushBuilderHtml =
                renderContext === 'single' && !options.suppressSinglePushButton
                    ? `
                    <div style="margin-top: 10px; display: flex; flex-wrap: wrap; gap: 8px; align-items: center;">
                        <button type="button" onclick="${singlePushOnclick}" ${canPushBuilder ? '' : 'disabled'} style="background: ${canPushBuilder ? '#6a1b9a' : '#444'}; border: none; padding: 6px 12px; color: #fff; font-size: 12px; cursor: ${canPushBuilder ? 'pointer' : 'not-allowed'}; font-weight: 600; border-radius: 4px; opacity: ${canPushBuilder ? '1' : '0.55'};" title="${canPushBuilder ? pushBuilderTitleOk : pushBuilderTitleNo}">Push to builder</button>
                    </div>
                `
                    : '';

            const lootNote = details.lootSourceNote;
            const genericLegNote = details.genericLegendaryDropNote;
            const npcWeaponDetailEarly = details.npcWeaponNote;

            const genericLegendaryCalloutHtml =
                translationOnly || !genericLegNote
                    ? ''
                    : `
                <div style="margin-top: 10px; padding: 8px; background: rgba(255, 112, 67, 0.1); border: 1px solid rgba(255, 112, 67, 0.45); border-radius: 4px;">
                    <div style="font-size: 11px; color: #ff8a65; font-weight: 700; margin-bottom: 4px;">GENERIC Legendary — invalid drop</div>
                    <div style="font-size: 10px; color: #ffccbc; line-height: 1.35;">${escapeLegitHtml(genericLegNote.message)}</div>
                    ${genericLegNote.qualifiedKey ? `<div style="font-size: 9px; color: #888; margin-top: 4px; word-break: break-all;">Composition: <code style="background: rgba(0,0,0,0.25); padding: 2px 4px; border-radius: 3px;">${escapeLegitHtml(genericLegNote.qualifiedKey)}</code></div>` : ''}
                </div>`;

            const lootSourceCalloutHtml =
                translationOnly || !lootNote
                    ? ''
                    : `
                <div style="margin-top: 10px; padding: 8px; background: rgba(255, 152, 0, 0.08); border: 1px solid rgba(255, 152, 0, 0.35); border-radius: 4px;">
                    <div style="font-size: 11px; color: #ffb74d; font-weight: 700; margin-bottom: 4px;">Loot source warning (indexed pools)</div>
                    <div style="font-size: 10px; color: #ffcc80; line-height: 1.35;">${escapeLegitHtml(lootNote.message)}</div>
                    ${lootNote.qualifiedKey ? `<div style="font-size: 9px; color: #888; margin-top: 4px; word-break: break-all;">Composition: <code style="background: rgba(0,0,0,0.25); padding: 2px 4px; border-radius: 3px;">${escapeLegitHtml(lootNote.qualifiedKey)}</code></div>` : ''}
                </div>`;

            const npcWeaponDetail = npcWeaponDetailEarly;
            const npcWeaponCalloutHtml =
                translationOnly || !npcWeaponDetail
                    ? ''
                    : `
                <div style="margin-top: 10px; padding: 8px; background: rgba(179, 136, 255, 0.12); border: 1px solid rgba(179, 136, 255, 0.4); border-radius: 4px;">
                    <div style="font-size: 11px; color: #b388ff; font-weight: 700; margin-bottom: 4px;">NPC Only</div>
                    <div style="font-size: 10px; color: #d1c4e9; line-height: 1.35;">${escapeLegitHtml(npcWeaponDetail.message)}</div>
                </div>`;

            const GENERIC_LEGENDARY_WARN_COLOR = '#ff8a65';
            const LOOT_SOURCE_WARN_COLOR = '#ffb74d';
            const NPC_WEAPON_WARN_COLOR = '#b388ff';
            const dropLegitimacyHeaderOverride =
                !translationOnly &&
                validationClass === SERIAL_VALIDATION_CLASS.PASS_ALL &&
                (!!genericLegNote || !!lootNote);
            const npcWeaponPassOverride =
                !translationOnly &&
                validationClass === SERIAL_VALIDATION_CLASS.PASS_ALL &&
                !!npcWeaponDetail &&
                !genericLegNote &&
                !lootNote;

            const cardAccent = translationOnly
                ? '#b39ddb'
                : genericLegNote
                    ? GENERIC_LEGENDARY_WARN_COLOR
                    : dropLegitimacyHeaderOverride
                        ? LOOT_SOURCE_WARN_COLOR
                        : npcWeaponPassOverride
                            ? NPC_WEAPON_WARN_COLOR
                            : legitimacyColor;
            const cardTitleText = translationOnly
                ? 'Modded decode / translation'
                : genericLegNote
                    ? '✗ GENERIC Legendary — invalid drop — Decoded Serial'
                    : lootNote
                    ? '⚠ No known drop source — Decoded Serial'
                    : npcWeaponPassOverride
                    ? 'NPC Only — Decoded Serial'
                    : `${legitimacyIcon} ${legitimacyLabel} - Decoded Serial`;

            const validationCardBg = npcWeaponPassOverride
                ? 'linear-gradient(90deg, rgba(179, 136, 255, 0.12) 0%, #1a1a1a 50%)'
                : '#1a1a1a';
            const validationCardBorderLeft = npcWeaponPassOverride ? '4px' : '3px';

            return `
                <div data-legit-validation-card data-render-context="${renderContext}" data-line-number="${lineNumber}" style="padding: 15px; background: ${validationCardBg}; border-radius: 6px; border-left: ${validationCardBorderLeft} solid ${cardAccent};">
                    <h4 style="color: ${cardAccent}; margin-bottom: 10px;">
                        ${cardTitleText}
                    </h4>
                    <div style="font-size: 12px; color: #aaa; margin-bottom: 15px;">
                        ${(details.inputSerial != null && details.inputSerial !== '') ? `<div style="margin-bottom: 4px;"><strong>Decoded Serial:</strong> <code style="background: rgba(0,0,0,0.3); padding: 2px 6px; border-radius: 3px; font-size: 11px; word-break: break-all;">${String(details.inputSerial)}</code></div>` : ''}
                        <div><strong>Root Serial:</strong> ${details.rootSerial ?? 'N/A'} ${details.rootKey ? `(${details.rootKey}${details.rootName ? ' - ' + details.rootName : ''})` : '<span style="color: #ff4444;">(Not Found)</span>'}</div>
                        <div><strong>Level:</strong> ${details.level ?? 'N/A'}</div>
                        <div><strong>Seed:</strong> ${details.seed ?? 'N/A'}${details.seed2 !== null && details.seed2 !== undefined ? `, ${details.seed2}` : ''}</div>
                        <div><strong>Parts:</strong> ${details.totalParts ?? validatedParts.length}${
                            translationOnly && moddedForeignCount != null && moddedForeignCount > 0
                                ? ` <span style="color:#ffb74d;">(${moddedForeignCount} cross-root)</span>`
                                : ''
                        }${
                            translationOnly && moddedUnknownCount != null && moddedUnknownCount > 0
                                ? ` <span style="color:#ef5350;">(${moddedUnknownCount} unknown)</span>`
                                : ''
                        }</div>
                        ${
                            translationOnly && moddedDecodeNote
                                ? `<div style="margin-top: 10px; padding: 10px; background: rgba(179, 157, 219, 0.1); border: 1px solid rgba(179, 157, 219, 0.35); border-radius: 4px; font-size: 11px; line-height: 1.5; color: #d1c4e9;">${moddedDecodeNote}</div>`
                                : ''
                        }
                        ${translationOnly
                            ? ''
                            : genericLegNote
                              ? `<div><strong>Result:</strong> <span style="color: ${GENERIC_LEGENDARY_WARN_COLOR}; font-weight: 600;">GENERIC Legendary (invalid drop)</span></div>`
                              : lootNote
                                ? `<div><strong>Result:</strong> <span style="color: ${LOOT_SOURCE_WARN_COLOR}; font-weight: 600;">No known drop source</span></div>`
                                : npcWeaponPassOverride
                                  ? `<div><strong>Result:</strong> <span style="color: ${NPC_WEAPON_WARN_COLOR}; font-weight: 600;">NPC Only</span></div>`
                                  : `<div><strong>Result:</strong> <span style="color: ${legitimacyColor};">${legitimacyLabel}</span></div>`}
                        ${nameRoundupHtml}
                        ${elementsRoundupHtml}
                        ${translationOnly ? '' : renderValidationChecksHtml(details, validationClass)}
                        ${genericLegendaryCalloutHtml}
                        ${lootSourceCalloutHtml}
                        ${npcWeaponCalloutHtml}
                        ${translationOnly || !(Array.isArray(details.classificationReasons) && details.classificationReasons.length > 0)
                            ? ''
                            : `<div><strong>Outcome Reasons:</strong> ${details.classificationReasons.join(' | ')}</div>`}
                        ${translationOnly || !(details.suppressSlotRepairPickers && validationClass !== SERIAL_VALIDATION_CLASS.PASS_ALL)
                            ? ''
                            : `<div style="margin-top: 10px; padding: 8px; background: rgba(255, 183, 77, 0.08); border: 1px solid rgba(255, 183, 77, 0.3); border-radius: 4px; font-size: 10px; color: #ffb74d;">Fix missing slots and Extra options are disabled while this serial has structural problems (duplicate parts, wrong-root parts, unknown parts, or exceeding a slot maximum). Correct those before using slot repair.</div>`}
                        ${missingSlotsFixHtml}
                        ${extraOptionalSlotsFixHtml}
                        ${singlePushBuilderHtml}
                        ${
                            !translationOnly &&
                            validationClass === SERIAL_VALIDATION_CLASS.FAIL_ORDER &&
                            reorderedSerial
                                ? `
                                <div data-reordered-serial-container data-render-context="${renderContext}" data-line-number="${lineNumber}" style="margin-top: 10px; padding: 8px; background: rgba(255, 152, 0, 0.08); border: 1px solid rgba(255, 152, 0, 0.35); border-radius: 4px;">
                                    <div style="font-size: 11px; color: #ffb74d; font-weight: 700; margin-bottom: 6px;">Reorder to Make Legit</div>
                                    <textarea data-reordered-serial-text style="width: 100%; min-height: 54px; resize: vertical; background: #111; color: #ddd; border: 1px solid #3c3c3c; border-radius: 4px; font-size: 11px; padding: 6px;">${reorderedSerial}</textarea>
                                    <div style="display: flex; gap: 8px; margin-top: 6px;">
                                        ${renderContext === 'bulk'
                                            ? `
                                                <button type="button" onclick="applyReorderedSerialToSingleValidator(this)" style="background: #2e7d32; color: #fff; border: 1px solid #3d9a43; border-radius: 4px; padding: 4px 10px; font-size: 11px; cursor: pointer;">Send to Single Validator</button>
                                                <button type="button" onclick="updateBulkValidatorWithReorderedSerial(this)" style="background: #1565c0; color: #fff; border: 1px solid #1e7ad8; border-radius: 4px; padding: 4px 10px; font-size: 11px; cursor: pointer;">Update Bulk Validator</button>
                                            `
                                            : `<button type="button" onclick="applyReorderedSerialToSingleValidator(this)" style="background: #2e7d32; color: #fff; border: 1px solid #3d9a43; border-radius: 4px; padding: 4px 10px; font-size: 11px; cursor: pointer;">Use Reordered Serial</button>`
                                        }
                                        <button type="button" onclick="copyReorderedSerialToClipboard(this)" style="background: #2d2d2d; color: #ddd; border: 1px solid #4a4a4a; border-radius: 4px; padding: 4px 10px; font-size: 11px; cursor: pointer;">Copy</button>
                                    </div>
                                </div>
                            `
                                : ''
                        }
                    </div>
                    <h5 style="color: #FFA500; margin-bottom: 8px; font-size: 13px;">${translationOnly ? 'Parts' : 'Part Validation:'}</h5>
                    <div style="max-height: 400px; overflow-y: auto;">${partRows}</div>
                    ${slotConstraintDiagnosticsHtml}
                    ${
                        validation?.error
                            ? `<div style="margin-top: 10px; font-size: 11px; color: ${
                                  translationOnly ? '#ef9a9a' : '#ffaa44'
                              };">${translationOnly ? '<strong>Notes:</strong> ' : ''}${escapeLegitHtml(validation.error)}</div>`
                            : ''
                    }
                    ${translationOnly || validationIssues.length === 0 ? '' : `<div style="margin-top: 10px; font-size: 11px; color: #ffaa44;">${validationIssues.map(issue => `• ${issue}`).join('<br>')}</div>`}
                    ${bulkBackpackHtml}
                </div>
            `;
        }

        function openValidationDetailsModal(contentHtml) {
            let overlay = document.getElementById('validationDetailsModalOverlay');
            if (!overlay) {
                overlay = document.createElement('div');
                overlay.id = 'validationDetailsModalOverlay';
                overlay.style.cssText = [
                    'position: fixed',
                    'inset: 0',
                    'background: rgba(0, 0, 0, 0.72)',
                    'z-index: 10050',
                    'display: none',
                    'align-items: center',
                    'justify-content: center',
                    'padding: var(--page-pad)'
                ].join(';');
                overlay.innerHTML = `
                    <div id="validationDetailsModalDialog" style="width: min(1200px, 96vw); max-height: 92vh; overflow: auto; background: #141414; border: 1px solid #3b3b3b; border-radius: var(--panel-radius); box-shadow: 0 16px 48px rgba(0,0,0,0.6);">
                        <div style="display: flex; justify-content: space-between; align-items: center; padding: var(--input-pad-y) var(--input-pad-x); border-bottom: 1px solid #2f2f2f; position: sticky; top: 0; background: #1a1a1a; z-index: 2;">
                            <div style="color: #4a9eff; font-weight: 700; font-size: var(--input-font);">Validation Details</div>
                            <button id="validationDetailsModalCloseBtn" type="button" style="background: #2d2d2d; border: 1px solid #4a4a4a; color: #ddd; border-radius: 4px; font-size: 11px; padding: 3px 8px; cursor: pointer;">Close</button>
                        </div>
                        <div id="validationDetailsModalContent" style="padding: var(--section-gap);"></div>
                    </div>
                `;
                document.body.appendChild(overlay);

                overlay.addEventListener('click', (e) => {
                    const dialog = document.getElementById('validationDetailsModalDialog');
                    if (dialog && !dialog.contains(e.target)) {
                        overlay.style.display = 'none';
                    }
                });
                document.addEventListener('keydown', (e) => {
                    if (e.key === 'Escape') {
                        const modalEl = document.getElementById('validationDetailsModalOverlay');
                        if (modalEl && modalEl.style.display !== 'none') {
                            modalEl.style.display = 'none';
                        }
                    }
                });
            }

            const contentEl = document.getElementById('validationDetailsModalContent');
            if (contentEl) {
                contentEl.innerHTML = contentHtml || '<div style="color:#ffaa44;">No detail content available.</div>';
            }
            if (typeof window.updateLegitValidatorBackpackButton === 'function') {
                window.updateLegitValidatorBackpackButton();
            }
            const closeBtn = document.getElementById('validationDetailsModalCloseBtn');
            if (closeBtn) {
                closeBtn.onclick = () => {
                    overlay.style.display = 'none';
                };
            }
            overlay.style.display = 'flex';
        }

        function getReorderedSerialFromButton(buttonEl) {
            const container = buttonEl?.closest('[data-reordered-serial-container]');
            const textArea = container?.querySelector('textarea[data-reordered-serial-text]');
            return textArea ? textArea.value.trim() : '';
        }

        function getReorderContainerMeta(buttonEl) {
            const container = buttonEl?.closest('[data-reordered-serial-container]');
            if (!container) return { container: null, renderContext: 'single', lineNumber: null };
            const renderContext = container.getAttribute('data-render-context') || 'single';
            const lineRaw = container.getAttribute('data-line-number');
            const parsedLine = Number.parseInt(lineRaw, 10);
            return {
                container,
                renderContext,
                lineNumber: Number.isNaN(parsedLine) ? null : parsedLine
            };
        }

        function copyReorderedSerialToClipboard(buttonEl) {
            const reorderedSerial = getReorderedSerialFromButton(buttonEl);
            if (!reorderedSerial) return;
            navigator.clipboard.writeText(reorderedSerial).catch(() => {
                // best effort copy
            });
        }

        function applyReorderedSerialToSingleValidator(buttonEl) {
            const reorderedSerial = getReorderedSerialFromButton(buttonEl);
            if (!reorderedSerial) return;
            const serialInput =
                window.__lbCtx && window.__lbCtx.relaxedPicker ? lbGeneratedHumanEl() : lbSerialInputEl();
            if (!serialInput) return;
            serialInput.value = reorderedSerial;
            const modalOverlay = document.getElementById('validationDetailsModalOverlay');
            if (modalOverlay) {
                modalOverlay.style.display = 'none';
            }
            // Ensure user lands on the single validator tab when action originates from bulk modal.
            try {
                if (typeof switchLegitTab === 'function') {
                    switchLegitTab('serial');
                }
            } catch (err) {
                console.warn('Could not switch to serial tab before decode:', err);
            }
            decodeAndValidateSerial();
            const resultDiv = lbEl('serialValidationResult');
            if (resultDiv && typeof resultDiv.scrollIntoView === 'function') {
                resultDiv.scrollIntoView({ behavior: 'smooth', block: 'start' });
            }
        }

        function getValidationCardMeta(buttonEl) {
            const card = buttonEl && buttonEl.closest ? buttonEl.closest('[data-legit-validation-card]') : null;
            if (!card) {
                return { card: null, renderContext: 'single', lineNumber: null };
            }
            const renderContext = card.getAttribute('data-render-context') || 'single';
            const lineRaw = card.getAttribute('data-line-number');
            const parsedLine = Number.parseInt(lineRaw, 10);
            return {
                card,
                renderContext,
                lineNumber: Number.isNaN(parsedLine) ? null : parsedLine
            };
        }

        /**
         * Remove one part token from the serial shown in the validator (single or bulk inspect modal) and re-run validation.
         */
        function legitRemoveValidatorPart(partIndex, buttonEl) {
            const saveEditorOverlay =
                buttonEl &&
                typeof buttonEl.closest === 'function' &&
                buttonEl.closest('#save-editor-validation-modal-overlay');
            const useInspect =
                buttonEl &&
                typeof buttonEl.closest === 'function' &&
                document.getElementById('validationDetailsModalContent') &&
                buttonEl.closest('#validationDetailsModalContent');
            const validation = useInspect
                ? window.__legitInspectValidation
                : saveEditorOverlay
                  ? window.__legitSaveEditorModalValidation || window.__legitLastSingleValidation
                  : window.__legitLastSingleValidation;

            const err = function (msg) {
                if (typeof showError === 'function') {
                    showError(msg);
                } else {
                    alert(msg);
                }
            };

            if (!validation || !validation.details) {
                err('No validation data. Validate a serial first.');
                return false;
            }
            const details = validation.details;
            const parts = Array.isArray(details.validatedParts) ? details.validatedParts : [];
            const part = parts.find(p => p && Number(p.index) === Number(partIndex));
            if (!part || part.patternTokenIndex == null) {
                err('Could not resolve that part in the current validation result.');
                return false;
            }

            const baseSerial =
                details.inputSerial != null && String(details.inputSerial).trim() !== ''
                    ? String(details.inputSerial).trim()
                    : '';
            if (!baseSerial) {
                err('No decoded serial on this validation result.');
                return false;
            }

            const newSerial = removeOneValidatorPartFromDeserializedSerial(baseSerial, part);
            if (!newSerial || newSerial === baseSerial) {
                err('Could not remove that part from the serial (format may be unsupported).');
                return false;
            }

            if (typeof __validatorResultCache !== 'undefined' && __validatorResultCache && __validatorResultCache.clear) {
                __validatorResultCache.clear();
            }

            const meta = getValidationCardMeta(buttonEl);

            if (saveEditorOverlay) {
                const base85 = saveEditorOverlay.getAttribute('data-inv-base85-serial');
                if (!base85 || typeof window.applySaveEditorModalDecodedChange !== 'function') {
                    err('Save editor modal is missing item context.');
                    return false;
                }
                void window
                    .applySaveEditorModalDecodedChange(base85, newSerial)
                    .catch(function (e) {
                        console.warn('applySaveEditorModalDecodedChange failed:', e);
                        err(e && e.message ? e.message : String(e));
                    });
                return true;
            }

            if (meta.renderContext === 'bulk' && meta.lineNumber != null) {
                const bulkInputEl = lbEl('bulkSerialInput');
                if (!bulkInputEl) {
                    err('Bulk validator input not found.');
                    return false;
                }
                const lines = String(bulkInputEl.value || '').split('\n');
                const targetLine = meta.lineNumber - 1;
                if (targetLine >= 0 && targetLine < lines.length) {
                    const currentLine = lines[targetLine] || '';
                    const yamlLikeMatch = currentLine.match(/^(.*serial\s*[:=]\s*)['"]?([^'"]*)['"]?\s*$/i);
                    if (yamlLikeMatch) {
                        lines[targetLine] = `${yamlLikeMatch[1]}${newSerial}`;
                    } else {
                        lines[targetLine] = newSerial;
                    }
                } else {
                    err('Bulk line not found for this inspect view.');
                    return false;
                }
                bulkInputEl.value = lines.join('\n');
                try {
                    if (typeof switchLegitTab === 'function') {
                        switchLegitTab('bulk');
                    }
                } catch (e) {
                    console.warn('Could not switch to bulk tab:', e);
                }
                if (typeof bulkValidateSerials === 'function') {
                    bulkValidateSerials();
                }
                const bulkResultDiv = lbEl('bulkValidationResult');
                if (bulkResultDiv && typeof bulkResultDiv.scrollIntoView === 'function') {
                    bulkResultDiv.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }
            } else {
                const serialInputEl = lbSerialInputEl();
                if (!serialInputEl) {
                    err('Serial input not found.');
                    return false;
                }
                serialInputEl.value = newSerial;
                if (window.__lbCtx && window.__lbCtx.relaxedPicker) {
                    const genEl = lbGeneratedHumanEl();
                    if (genEl) {
                        genEl.value = newSerial;
                    }
                }
                if (useInspect) {
                    const modalOverlay = document.getElementById('validationDetailsModalOverlay');
                    if (modalOverlay) {
                        modalOverlay.style.display = 'none';
                    }
                }
                try {
                    if (typeof switchLegitTab === 'function') {
                        switchLegitTab('serial');
                    }
                } catch (e) {
                    console.warn('Could not switch to serial tab:', e);
                }
                if (typeof decodeAndValidateSerial === 'function') {
                    decodeAndValidateSerial();
                }
                const resultDiv = lbEl('serialValidationResult');
                if (resultDiv && typeof resultDiv.scrollIntoView === 'function') {
                    resultDiv.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }
            }

            return true;
        }

        window.legitRemoveValidatorPart = legitRemoveValidatorPart;

        function updateBulkValidatorWithReorderedSerial(buttonEl) {
            const reorderedSerial = getReorderedSerialFromButton(buttonEl);
            if (!reorderedSerial) return;

            const meta = getReorderContainerMeta(buttonEl);
            const bulkInputEl = lbEl('bulkSerialInput');
            if (!bulkInputEl) return;

            const lines = String(bulkInputEl.value || '').split('\n');
            const targetLine = meta.lineNumber ? meta.lineNumber - 1 : -1;
            if (targetLine >= 0 && targetLine < lines.length) {
                const currentLine = lines[targetLine] || '';
                const yamlLikeMatch = currentLine.match(/^(.*serial\s*[:=]\s*)['"]?([^'"]*)['"]?\s*$/i);
                if (yamlLikeMatch) {
                    lines[targetLine] = `${yamlLikeMatch[1]}${reorderedSerial}`;
                } else {
                    lines[targetLine] = reorderedSerial;
                }
            } else {
                lines.push(reorderedSerial);
            }

            bulkInputEl.value = lines.join('\n');

            const modalOverlay = document.getElementById('validationDetailsModalOverlay');
            if (modalOverlay) {
                modalOverlay.style.display = 'none';
            }

            try {
                if (typeof switchLegitTab === 'function') {
                    switchLegitTab('bulk');
                }
            } catch (err) {
                console.warn('Could not switch to bulk tab before revalidation:', err);
            }

            bulkValidateSerials();
            const bulkResultDiv = lbEl('bulkValidationResult');
            if (bulkResultDiv && typeof bulkResultDiv.scrollIntoView === 'function') {
                bulkResultDiv.scrollIntoView({ behavior: 'smooth', block: 'start' });
            }
        }

        /** Escape text for safe insertion into HTML strings built by the builder. */
        function escapeLegitHtml(s) {
            if (s == null) return '';
            return String(s)
                .replace(/&/g, '&amp;')
                .replace(/</g, '&lt;')
                .replace(/>/g, '&gt;')
                .replace(/"/g, '&quot;');
        }

        /** Amber-gold: stands out on dark cards (not white/grey body copy). */
        const LEGIT_DEBUG_DESC_FG = '#fac018';
        /** Cyan label so “Debug:” reads as its own cue. */
        const LEGIT_DEBUG_LABEL_FG = '#34e2ff';

        /** `stattagname` (lowercase) → amber debug line when `debugdisplaydescription` is missing. */
        const LEGIT_STAT_TAG_TO_DEBUG_LABEL = {
            damage: 'Damage mod',
            elemental_power: 'Elemental power mod',
            accuracy: 'Accuracy mod',
            reload_speed: 'Reload speed mod',
            fire_rate: 'Fire rate mod',
            crit_damage: 'Crit damage mod',
            mag_size: 'Mag size mod',
            ads_proficiency: 'ADS prof mod'
        };

        function legitTitleCaseStatTagFallback(tagLc) {
            const parts = String(tagLc || '').split('_').filter(Boolean);
            if (parts.length === 0) return '';
            return parts.map(w => w.charAt(0).toUpperCase() + w.slice(1).toLowerCase()).join(' ') + ' mod';
        }

        function legitLabelForStatTagName(tagLc) {
            if (!tagLc) return '';
            const key = String(tagLc).trim().toLowerCase();
            if (LEGIT_STAT_TAG_TO_DEBUG_LABEL[key]) return LEGIT_STAT_TAG_TO_DEBUG_LABEL[key];
            return legitTitleCaseStatTagFallback(key);
        }

        /** Match dedicated aspects like inv_aspect'stat_mod_mag_size' (no statmodifiers array on export). */
        const LEGIT_STAT_MOD_ASPECT_PARENT_RE = /inv_aspect'stat_mod_([a-z0-9_]+)'/i;

        /** Collect ordered unique `stattagname` values from stat_modifiers aspects on part or nested item. */
        function collectStatTagNames(partEntry) {
            const seen = Object.create(null);
            const ordered = [];
            const aspectLists = [partEntry.aspects, partEntry.item && partEntry.item.aspects];
            for (let s = 0; s < aspectLists.length; s++) {
                const aspects = aspectLists[s];
                if (!Array.isArray(aspects)) continue;
                for (let i = 0; i < aspects.length; i++) {
                    const aspect = aspects[i];
                    if (!aspect) continue;
                    const parent = String(aspect.parent || '');
                    if (parent.includes('stat_modifiers')) {
                        const mods = aspect.statmodifiers || aspect.statModifiers;
                        if (Array.isArray(mods)) {
                            for (let j = 0; j < mods.length; j++) {
                                const m = mods[j];
                                const name = m && m.stattagname != null ? String(m.stattagname).trim().toLowerCase() : '';
                                if (!name || seen[name]) continue;
                                seen[name] = true;
                                ordered.push(name);
                            }
                        }
                    }
                    const aspMatch = parent.match(LEGIT_STAT_MOD_ASPECT_PARENT_RE);
                    if (aspMatch && aspMatch[1]) {
                        const name = aspMatch[1].trim().toLowerCase();
                        if (name && !seen[name]) {
                            seen[name] = true;
                            ordered.push(name);
                        }
                    }
                }
            }
            return ordered;
        }

        function deriveDebugDisplayFromStatModifiers(partEntry) {
            if (!partEntry) return '';
            const tags = collectStatTagNames(partEntry);
            if (tags.length === 0) return '';
            const labels = tags.map(legitLabelForStatTagName).filter(Boolean);
            return labels.length ? labels.join(', ') : '';
        }

        /** Inv export `debugdisplaydescription` on a dep entry or raw `item` blob. */
        function getPartDebugDisplayDescription(partEntry) {
            if (!partEntry) return '';
            const rawOfficial = partEntry.debugdisplaydescription != null
                ? partEntry.debugdisplaydescription
                : (partEntry.item && partEntry.item.debugdisplaydescription);
            const officialTrim = rawOfficial == null ? '' : String(rawOfficial).trim();
            let t = officialTrim;
            let fromDerived = false;
            if (!t) {
                t = deriveDebugDisplayFromStatModifiers(partEntry);
                fromDerived = !!t;
            }
            if (!t) return '';
            if (!fromDerived) {
                const modCommaTail = t.match(/^(.+?\bmod|\bmod\b)(\s*,.*)?$/i);
                if (modCommaTail && modCommaTail[2]) {
                    return modCommaTail[1].trim();
                }
            }
            return t;
        }

        /**
         * Collect inv_name_part ids from titlepartlist that match /^np_/i (order preserved).
         * When two or more exist, display prefers the second (e.g. NP_DualDamage over base np_weap_*).
         * @param {Array} list
         * @returns {{ npIds: string[], preferredIndex: number }}
         */
        function pickInvNamePartIdForDisplayFromTitlePartList(list) {
            const npIds = [];
            if (!Array.isArray(list)) {
                return { npIds, preferredIndex: 0 };
            }
            for (let j = 0; j < list.length; j++) {
                const ref = list[j];
                if (typeof ref !== 'string') continue;
                const m = ref.match(/inv_name_part'([^']+)'/);
                if (m && m[1] && /^np_/i.test(m[1])) {
                    npIds.push(m[1]);
                }
            }
            const preferredIndex = npIds.length >= 2 ? 1 : 0;
            return { npIds, preferredIndex };
        }

        /**
         * Concatenate every InventoryNamingAspect titlepartlist string in order (aspects[] then item.aspects[]).
         * Multiple naming aspects on one part (e.g. base np_weap_* then override NP_DualDamage) behave like one list.
         * @param {Array<Array|undefined|null>} aspectArrays
         * @returns {string[]}
         */
        function flattenInventoryNamingTitlePartRefs(aspectArrays) {
            const refs = [];
            if (!Array.isArray(aspectArrays)) return refs;
            for (let a = 0; a < aspectArrays.length; a++) {
                const aspects = aspectArrays[a];
                if (!Array.isArray(aspects)) continue;
                for (let i = 0; i < aspects.length; i++) {
                    const aspect = aspects[i];
                    if (!aspect || !String(aspect.structtype || '').includes('InventoryNamingAspect')) continue;
                    const list = aspect.titlepartlist;
                    if (!Array.isArray(list)) continue;
                    for (let j = 0; j < list.length; j++) {
                        if (typeof list[j] === 'string') refs.push(list[j]);
                    }
                }
            }
            return refs;
        }

        function lookupInvNamePartByPreferredNpIds(npIds, preferredIndex, invNamePartDisplayNames) {
            if (!npIds.length || !invNamePartDisplayNames) return null;
            const at = function (idx) {
                const id = npIds[idx];
                if (!id) return null;
                const n = invNamePartDisplayNames[id.toLowerCase()];
                return n && String(n).trim() ? n : null;
            };
            let n = at(preferredIndex);
            if (n) return n;
            for (let k = preferredIndex - 1; k >= 0; k--) {
                n = at(k);
                if (n) return n;
            }
            for (let k = preferredIndex + 1; k < npIds.length; k++) {
                n = at(k);
                if (n) return n;
            }
            return null;
        }

        /**
         * Resolve display name for a part from InventoryNamingAspect titlepartlist using inv_name_part table.
         * @param {Object} foundPart - Part entry with aspects (e.g. dep entry from inv)
         * @param {Object.<string, string>} [invNamePartDisplayNames] - Map np_xxx (lowercase) -> display name
         * @returns {string|null} Display name (e.g. "Rowan's Charge") or null
         */
        function getDisplayNameForPart(foundPart, invNamePartDisplayNames) {
            if (!foundPart || !invNamePartDisplayNames || typeof invNamePartDisplayNames !== 'object') return null;
            const allRefs = flattenInventoryNamingTitlePartRefs([
                foundPart.aspects,
                foundPart.item && foundPart.item.aspects
            ]);
            if (allRefs.length === 0) return null;

            const picked = pickInvNamePartIdForDisplayFromTitlePartList(allRefs);
            if (picked.npIds.length > 0) {
                return lookupInvNamePartByPreferredNpIds(
                    picked.npIds,
                    picked.preferredIndex,
                    invNamePartDisplayNames
                );
            }
            for (let j = 0; j < allRefs.length; j++) {
                const match = allRefs[j].match(/inv_name_part'([^']+)'/);
                if (match && match[1]) {
                    const n = invNamePartDisplayNames[match[1].toLowerCase()];
                    if (n) return n;
                }
            }
            return null;
        }

        /**
         * When inv_name_part JSON was not loaded, derive a readable label from np_* in InventoryNamingAspect
         * (skip patterns that look like class-mod internal ids).
         */
        function displayNameFromNpKeyLoose(npKeyRaw) {
            const np = String(npKeyRaw || '').trim();
            if (!np) return null;
            const lower = np.toLowerCase();
            if (lower.includes('_cm_') || lower.includes('cm_ds_leg') || /_leg_\d{1,3}$/i.test(lower)) return null;
            const raw = lower.replace(/^np_/, '');
            if (!raw) return null;
            const parts = raw.split('_').filter(Boolean);
            if (parts.length === 0 || parts.length > 6) return null;
            return parts.map(w => w.charAt(0).toUpperCase() + w.slice(1).toLowerCase()).join(' ');
        }

        /** Prefer inv_name_part table; else title-case from aspect np_* ref. */
        function getDisplayNameForPartOrFallback(foundPart, invNamePartDisplayNames) {
            if (!foundPart) return null;
            const invMap = invNamePartDisplayNames && typeof invNamePartDisplayNames === 'object' ? invNamePartDisplayNames : {};
            const mapped = getDisplayNameForPart(foundPart, invMap);
            if (mapped) return mapped;
            const allRefs = flattenInventoryNamingTitlePartRefs([
                foundPart.aspects,
                foundPart.item && foundPart.item.aspects
            ]);
            if (allRefs.length === 0) return null;

            const picked = pickInvNamePartIdForDisplayFromTitlePartList(allRefs);
            if (picked.npIds.length > 0) {
                const tryLooseAt = function (idx) {
                    return displayNameFromNpKeyLoose(picked.npIds[idx]);
                };
                let loose = tryLooseAt(picked.preferredIndex);
                if (loose) return loose;
                for (let k = picked.preferredIndex - 1; k >= 0; k--) {
                    loose = tryLooseAt(k);
                    if (loose) return loose;
                }
                for (let k = picked.preferredIndex + 1; k < picked.npIds.length; k++) {
                    loose = tryLooseAt(k);
                    if (loose) return loose;
                }
            } else {
                for (let j = 0; j < allRefs.length; j++) {
                    const match = allRefs[j].match(/inv_name_part'([^']+)'/i);
                    if (match && match[1]) {
                        const looseAlt = displayNameFromNpKeyLoose(match[1]);
                        if (looseAlt) return looseAlt;
                    }
                }
            }
            return null;
        }

        /**
         * Match comp_05_legendary_<suffix> to inv_name_part display strings (e.g. dlc1/raid1 -> "Whale"/"Rounder").
         * Shared by black-market picker and modded inv_comp browse lists.
         */
        function getInvLegendarySuffixMatchedDisplayNames(suffixLc, invMap) {
            const names = [];
            if (!suffixLc || !invMap || typeof invMap !== 'object') return names;
            const normUnderscores = function (s) {
                return String(s || '')
                    .toLowerCase()
                    .replace(/^np_/, '')
                    .replace(/_/g, '');
            };
            const sn = normUnderscores(suffixLc);
            const seenLower = new Set();
            Object.keys(invMap).forEach(function (mapKey) {
                const base = String(mapKey).toLowerCase().replace(/^np_/, '');
                if (!base) return;
                const matches =
                    base === suffixLc ||
                    normUnderscores(base) === sn ||
                    base.endsWith('_' + suffixLc) ||
                    suffixLc.endsWith('_' + base);
                if (!matches) return;
                const d = invMap[mapKey];
                if (!d || !String(d).trim()) return;
                const t = String(d).trim();
                const tl = t.toLowerCase();
                if (seenLower.has(tl)) return;
                seenLower.add(tl);
                names.push(t);
            });
            return names;
        }

        /**
         * Suffix token after comp_05_legendary_ / comp_06_pearl_ / comp_06_pearlescent_ (for inv_name_part lookup).
         */
        function extractUniqueInvCompSuffix(rawKey) {
            const lower = String(rawKey || '').trim().toLowerCase();
            let m = lower.match(/^comp_05_legendary_(.+)$/);
            if (m && m[1]) return m[1].toLowerCase();
            m = lower.match(/^comp_06_pearl_(.+)$/);
            if (m && m[1]) return m[1].toLowerCase();
            m = lower.match(/^comp_06_pearlescent_(.+)$/);
            if (m && m[1]) return m[1].toLowerCase();
            return lower;
        }

        function invCompKeyHasUniqueDisplayName(rawKey) {
            const lower = String(rawKey || '').trim().toLowerCase();
            if (
                lower === 'comp_05_legendary' ||
                lower === 'comp_06_pearlescent' ||
                lower === 'comp_06_pearl'
            ) {
                return false;
            }
            return (
                /^comp_05_legendary_[a-z0-9_]+$/i.test(lower) ||
                /^comp_06_pearl_[a-z0-9_]+$/i.test(lower) ||
                /^comp_06_pearlescent_[a-z0-9_]+$/i.test(lower)
            );
        }

        function getInvCompFriendlyBaseFromKey(rawKey) {
            const key = String(rawKey || '').trim();
            if (!key) return '';
            return key
                .replace(/^comp_/i, '')
                .replace(/_/g, ' ')
                .replace(/\b\w/g, function (c) {
                    return c.toUpperCase();
                });
        }

        function formatInvCompWithUniqueSuffix(baseLabel, uniqueName) {
            const base = String(baseLabel || '').trim();
            const uniq = String(uniqueName || '').trim();
            if (!uniq) return base || '';
            if (!base) return uniq;
            if (base.toLowerCase() === uniq.toLowerCase()) return base;
            if (base.indexOf('(' + uniq + ')') >= 0 || base.indexOf('(' + uniq.toLowerCase() + ')') >= 0) {
                return base;
            }
            return base + ' (' + uniq + ')';
        }

        /**
         * User-facing label for modded inv_comp browse rows (inventory naming + legendary suffix → inv_name_part).
         * @param {object} [dataOpt] - Merged Nexus data (enables root-scoped legendary names for class mods).
         * @param {object} [rootOpt] - Root being edited or source root; required with dataOpt for correct class-mod uniques.
         */
        function getModdedInvCompBrowseDisplayName(depEntry, invMap, dataOpt, rootOpt) {
            if (!depEntry) return '';
            const rawKey =
                depEntry.key != null
                    ? String(depEntry.key).trim()
                    : depEntry.inv_comp != null
                      ? String(depEntry.inv_comp).trim()
                      : depEntry.item && depEntry.item.key != null
                        ? String(depEntry.item.key).trim()
                        : '';
            if (!rawKey) return '';
            const invMapObj = invMap && typeof invMap === 'object' ? invMap : {};
            const lower = rawKey.toLowerCase();
            if (lower === 'comp_01_common') return 'Generic Common composition';
            if (lower === 'comp_02_uncommon') return 'Generic Uncommon composition';
            if (lower === 'comp_03_rare') return 'Generic Rare composition';
            if (lower === 'comp_04_epic') return 'Generic Epic composition';
            if (lower === 'comp_05_legendary') return 'Generic Legendary composition';
            if (lower === 'base_comp_01_common') return 'Common (base)';
            if (lower === 'base_comp_02_uncommon') return 'Uncommon (base)';
            if (lower === 'base_comp_03_rare') return 'Rare (base)';
            if (lower === 'base_comp_04_epic') return 'Epic (base)';
            if (lower === 'base_comp_05_legendary') return 'Legendary (base)';
            if (lower === 'base_comp_06_pearlescent') return 'Pearlescent (base)';
            if (
                dataOpt &&
                rootOpt &&
                lower.startsWith('comp_05_legendary_') &&
                lower !== 'comp_05_legendary'
            ) {
                const base = getInvCompFriendlyBaseFromKey(rawKey);
                const scoped = resolveLegendaryInvCompDisplaySuffix(depEntry, dataOpt, rootOpt);
                if (scoped && String(scoped).trim()) {
                    return formatInvCompWithUniqueSuffix(base, scoped);
                }
            }
            if (dataOpt && rootOpt && /^comp_06_pearl_/i.test(lower)) {
                const base = getInvCompFriendlyBaseFromKey(rawKey);
                const scopedPearl = resolvePearlescentInvCompDisplaySuffix(depEntry, dataOpt, rootOpt);
                if (scopedPearl && String(scopedPearl).trim()) {
                    return formatInvCompWithUniqueSuffix(base, scopedPearl);
                }
            }
            if (lower.startsWith('comp_05_legendary_') && lower !== 'comp_05_legendary') {
                const base = getInvCompFriendlyBaseFromKey(rawKey);
                const suffixLc = extractUniqueInvCompSuffix(rawKey);
                const fromInvTable = getInvLegendarySuffixMatchedDisplayNames(suffixLc, invMapObj);
                const unique = fromInvTable[0] || null;
                if (unique) return formatInvCompWithUniqueSuffix(base, unique);
                return base;
            }
            if (/^comp_06_pearl_/i.test(lower) || /^comp_06_pearlescent_/i.test(lower)) {
                const base = getInvCompFriendlyBaseFromKey(rawKey);
                const suffixLc = extractUniqueInvCompSuffix(rawKey);
                const fromInvTable = getInvLegendarySuffixMatchedDisplayNames(suffixLc, invMapObj);
                const unique = fromInvTable[0] || null;
                if (unique) return formatInvCompWithUniqueSuffix(base, unique);
                return base;
            }
            return getDisplayNameForPartOrFallback(depEntry, invMapObj) || rawKey;
        }

        /** Legit + modded builder: human label for inv_comp rows (np_* / barrel-linked names). */
        function getLegitBuilderInvCompPartDisplayName(item, data, root, isSimpleView) {
            if (!item || !item.key) return 'Unknown';
            const invMap =
                data && data.invNamePartDisplayNames && typeof data.invNamePartDisplayNames === 'object'
                    ? data.invNamePartDisplayNames
                    : {};
            const browse = getModdedInvCompBrowseDisplayName(item, invMap, data, root);
            if (browse && String(browse).trim()) {
                return String(browse).trim();
            }
            const friendly = function (key) {
                if (!key) return 'Unknown';
                return getInvCompFriendlyBaseFromKey(key) || key;
            };
            return isSimpleView ? friendly(item.key) : item.key;
        }

        /**
         * Build Quick Access + black-market catalog from merged Nexus data (legendary + pearlescent inv_comp).
         * Last successful merge wins (shared across dual Legit Builder instances).
         */
        function buildUniqueCompQuickAccessCatalog(merged) {
            const quickAccess = [];
            const legendaryOnly = [];
            const seen = new Set();
            const invMap =
                merged && merged.invNamePartDisplayNames && typeof merged.invNamePartDisplayNames === 'object'
                    ? merged.invNamePartDisplayNames
                    : {};
            function considerEntry(dep, rootKey, skipDepTableCheck) {
                if (!dep) return;
                if (!skipDepTableCheck && String(dep.depTableName || '') !== 'inv_comp') return;
                const rawKey =
                    dep.key != null
                        ? String(dep.key).trim()
                        : dep.inv_comp != null
                          ? String(dep.inv_comp).trim()
                          : dep.item && dep.item.key != null
                            ? String(dep.item.key).trim()
                            : '';
                if (!rawKey || !rawKey.toLowerCase().startsWith('comp_')) return;
                const lower = rawKey.toLowerCase();
                const rk = rootKey != null ? String(rootKey).trim() : '';
                if (!rk) return;
                const isPearl =
                    miInvCompDepIsPearlescent(dep) ||
                    /comp_06|base_comp_06|_06_pearlescent/.test(lower);
                const isLeg =
                    lower !== 'comp_05_legendary' && /^comp_05_legendary_[a-z0-9_]+$/i.test(lower);
                if (!isPearl && !isLeg) return;
                if (isLeg && isEnhancementRootKey(rk)) return;
                if (isPearl && (lower === 'comp_06_pearlescent' || lower === 'base_comp_06_pearlescent')) {
                    return;
                }
                const tier = isPearl ? 'pearlescent' : 'legendary';
                const value = rk + '.' + rawKey;
                if (seen.has(value)) return;
                seen.add(value);
                const rootObj =
                    rk && merged && Array.isArray(merged.rootDefinitions)
                        ? merged.rootDefinitions.find(function (x) {
                              return String(x.key) === rk;
                          })
                        : null;
                let label = getModdedInvCompBrowseDisplayName(dep, invMap, merged, rootObj);
                const suffixLc = extractUniqueInvCompSuffix(rawKey);
                const fromInvTable = getInvLegendarySuffixMatchedDisplayNames(suffixLc, invMap);
                if (!label || label === rawKey) {
                    const base = getInvCompFriendlyBaseFromKey(rawKey);
                    const unique =
                        (rootObj &&
                            (tier === 'pearlescent'
                                ? resolvePearlescentInvCompDisplaySuffix(dep, merged, rootObj)
                                : resolveLegendaryInvCompDisplaySuffix(dep, merged, rootObj))) ||
                        fromInvTable[0] ||
                        null;
                    label = unique
                        ? formatInvCompWithUniqueSuffix(base, unique)
                        : base ||
                          getDisplayNameForPartOrFallback(dep, invMap) ||
                          suffixLc.replace(/_/g, ' ').replace(/\b\w/g, function (c) {
                              return c.toUpperCase();
                          });
                }
                const manufacturer = rootObj ? extractManufacturer(rootObj) : '';
                const category = rootObj ? categorizeRoot(rootObj) : '';
                const itemType = rootObj ? inferSaveEditorItemTypeDisplay(rootObj) : '';
                const tierKeywords = tier === 'pearlescent' ? 'pearlescent pearl pearl_comp' : 'legendary unique';
                const searchText = [
                    label,
                    value,
                    rawKey,
                    suffixLc,
                    suffixLc.replace(/_/g, ' '),
                    fromInvTable.join(' '),
                    rk,
                    manufacturer,
                    category,
                    itemType,
                    tierKeywords
                ]
                    .join(' ')
                    .toLowerCase();
                const entry = {
                    rootKey: rk,
                    compKey: rawKey,
                    label: label,
                    value: value,
                    searchText: searchText,
                    tier: tier,
                    itemtype: rk,
                    itemType: itemType,
                    manufacturer: manufacturer,
                    category: category
                };
                quickAccess.push(entry);
                if (tier === 'legendary') {
                    legendaryOnly.push({
                        value: value,
                        itemtype: rk,
                        compKey: rawKey,
                        label: label + ' — ' + value,
                        searchText: searchText
                    });
                }
            }
            const roots = merged && Array.isArray(merged.rootDefinitions) ? merged.rootDefinitions : [];
            for (let r = 0; r < roots.length; r++) {
                const root = roots[r];
                const rootKey = root && root.key != null ? String(root.key) : '';
                const deps = root && root.depEntries;
                if (!Array.isArray(deps)) continue;
                for (let d = 0; d < deps.length; d++) {
                    considerEntry(deps[d], rootKey);
                }
            }
            const subInv = merged && merged.subItemsByTable && merged.subItemsByTable.inv_comp;
            const subItems = subInv && Array.isArray(subInv.items) ? subInv.items : [];
            for (let i = 0; i < subItems.length; i++) {
                const it = subItems[i];
                const rk =
                    it && it.rootKey != null
                        ? String(it.rootKey)
                        : it && it.item && it.item.rootKey != null
                          ? String(it.item.rootKey)
                          : '';
                considerEntry(it, rk, true);
            }
            quickAccess.sort(function (a, b) {
                return String(a.label).localeCompare(String(b.label));
            });
            legendaryOnly.sort(function (a, b) {
                return a.label.localeCompare(b.label);
            });
            window.__legitBuilderUniqueCompQuickAccess = quickAccess;
            window.__legitBuilderUniqueLegendaryComps = legendaryOnly;
            window.legitBuilderGetUniqueCompQuickAccessOptions = function () {
                return window.__legitBuilderUniqueCompQuickAccess || [];
            };
            window.legitBuilderGetUniqueLegendaryCompOptions = function () {
                return window.__legitBuilderUniqueLegendaryComps || [];
            };
            try {
                window.dispatchEvent(
                    new CustomEvent('legit-builder-unique-legendary-comps-updated', {
                        detail: { count: legendaryOnly.length, quickAccessCount: quickAccess.length }
                    })
                );
            } catch (_) {
                /* ignore */
            }
        }

        /** @deprecated alias — black-market picker calls this name */
        function publishUniqueLegendaryCompOptionsForBlackMarket(merged) {
            buildUniqueCompQuickAccessCatalog(merged);
        }

        const UNIQUE_QUICK_ACCESS_CATEGORY_ORDER = [
            'Assault Rifle',
            'SMG',
            'Pistol',
            'Sniper',
            'Shotgun',
            'Heavy Weapon',
            'Shield',
            'Grenade',
            'Class Mod',
            'Enhancement',
            'Repkit',
            'Terminal',
            'Artifact'
        ];

        const UNIQUE_QUICK_ACCESS_CATEGORY_SHORT = {
            'Assault Rifle': 'Assault Rifle',
            'Heavy Weapon': 'Heavy',
            'Class Mod': 'Class Mod'
        };

        function uniqueQuickAccessNormalizeCategoryFilter(filterId) {
            if (!filterId || filterId === 'all') return 'all';
            const lc = String(filterId).toLowerCase().replace(/_/g, ' ').trim();
            const aliases = {
                'assault rifle': 'Assault Rifle',
                ar: 'Assault Rifle',
                smg: 'SMG',
                submachine: 'SMG',
                pistol: 'Pistol',
                ps: 'Pistol',
                sniper: 'Sniper',
                sr: 'Sniper',
                shotgun: 'Shotgun',
                sg: 'Shotgun',
                'heavy weapon': 'Heavy Weapon',
                heavy: 'Heavy Weapon',
                hw: 'Heavy Weapon',
                shield: 'Shield',
                grenade: 'Grenade',
                'class mod': 'Class Mod',
                classmod: 'Class Mod',
                enhancement: 'Enhancement',
                repkit: 'Repkit',
                'repair kit': 'Repkit',
                terminal: 'Terminal',
                artifact: 'Artifact'
            };
            return aliases[lc] || String(filterId).trim();
        }

        function uniqueQuickAccessEntryMatchesCategory(entry, categoryFilter) {
            const norm = uniqueQuickAccessNormalizeCategoryFilter(categoryFilter);
            if (norm === 'all') return true;
            const itemType = String((entry && entry.itemType) || '').trim();
            if (itemType && itemType.toLowerCase() === norm.toLowerCase()) return true;
            const rk = String((entry && entry.rootKey) || '').toLowerCase();
            if (norm === 'Assault Rifle' && /_ar(?:_|$)/.test(rk)) return true;
            if (norm === 'SMG' && (/_smg(?:_|$)/.test(rk) || /_sm(?:_|$)/.test(rk))) return true;
            if (norm === 'Pistol' && /_ps(?:_|$)/.test(rk)) return true;
            if (norm === 'Sniper' && /_sr(?:_|$)/.test(rk)) return true;
            if (norm === 'Shotgun' && /_sg(?:_|$)/.test(rk)) return true;
            if (norm === 'Heavy Weapon' && (/_hw(?:_|$)/.test(rk) || rk.includes('heavy_weapon'))) return true;
            if (norm === 'Shield' && rk.includes('shield')) return true;
            if (norm === 'Grenade' && rk.includes('grenade')) return true;
            if (norm === 'Class Mod' && rk.includes('classmod')) return true;
            if (norm === 'Enhancement' && rk.includes('enhancement')) return true;
            if (norm === 'Repkit' && (rk.includes('repkit') || rk.includes('repair_kit'))) return true;
            return false;
        }

        function getUniqueQuickAccessCategoryCounts() {
            const opts = window.legitBuilderGetUniqueCompQuickAccessOptions
                ? window.legitBuilderGetUniqueCompQuickAccessOptions()
                : window.__legitBuilderUniqueCompQuickAccess || [];
            const counts = {};
            for (let i = 0; i < opts.length; i++) {
                const o = opts[i];
                const t = String((o && o.itemType) || '').trim() || 'Other';
                counts[t] = (counts[t] || 0) + 1;
            }
            return counts;
        }

        function renderUniqueQuickAccessCategoryChips(panelEl) {
            if (!panelEl) return;
            const wrap = panelEl.querySelector('.unique-quick-access-categories');
            if (!wrap) return;
            const counts = getUniqueQuickAccessCategoryCounts();
            const total = window.legitBuilderGetUniqueCompQuickAccessOptions
                ? window.legitBuilderGetUniqueCompQuickAccessOptions().length
                : 0;
            const activeBtn = wrap.querySelector('.unique-quick-access-category.is-active');
            const activeId = activeBtn ? String(activeBtn.getAttribute('data-category') || 'all') : 'all';
            let html =
                '<button type="button" class="unique-quick-access-category' +
                (activeId === 'all' ? ' is-active' : '') +
                '" data-category="all">All</button>';
            let c;
            for (c = 0; c < UNIQUE_QUICK_ACCESS_CATEGORY_ORDER.length; c++) {
                const cat = UNIQUE_QUICK_ACCESS_CATEGORY_ORDER[c];
                if (!counts[cat]) continue;
                const short = UNIQUE_QUICK_ACCESS_CATEGORY_SHORT[cat] || cat;
                html +=
                    '<button type="button" class="unique-quick-access-category' +
                    (activeId === cat ? ' is-active' : '') +
                    '" data-category="' +
                    escapeLegitHtml(cat) +
                    '" title="' +
                    escapeLegitHtml(String(counts[cat]) + ' uniques') +
                    '">' +
                    escapeLegitHtml(short) +
                    '</button>';
            }
            const extraKeys = Object.keys(counts).sort();
            for (c = 0; c < extraKeys.length; c++) {
                const cat = extraKeys[c];
                if (
                    cat === 'Other' ||
                    cat === 'Weapon' ||
                    cat === 'Turret' ||
                    UNIQUE_QUICK_ACCESS_CATEGORY_ORDER.indexOf(cat) >= 0
                ) {
                    continue;
                }
                html +=
                    '<button type="button" class="unique-quick-access-category' +
                    (activeId === cat ? ' is-active' : '') +
                    '" data-category="' +
                    escapeLegitHtml(cat) +
                    '" title="' +
                    escapeLegitHtml(String(counts[cat]) + ' uniques') +
                    '">' +
                    escapeLegitHtml(cat) +
                    '</button>';
            }
            wrap.innerHTML = html;
            if (total === 0) {
                wrap.style.display = 'none';
            } else {
                wrap.style.display = '';
            }
        }

        function filterUniqueCompQuickAccess(query, tierFilter, categoryFilter) {
            const opts = window.legitBuilderGetUniqueCompQuickAccessOptions
                ? window.legitBuilderGetUniqueCompQuickAccessOptions()
                : window.__legitBuilderUniqueCompQuickAccess || [];
            const q = (query || '').trim().toLowerCase();
            const tier = (tierFilter || 'all').toLowerCase();
            const cat = categoryFilter || 'all';
            let list = opts.slice();
            if (tier === 'legendary' || tier === 'pearlescent') {
                list = list.filter(function (o) {
                    return o && String(o.tier).toLowerCase() === tier;
                });
            }
            if (cat && cat !== 'all') {
                list = list.filter(function (o) {
                    return uniqueQuickAccessEntryMatchesCategory(o, cat);
                });
            }
            if (q) {
                list = list.filter(function (o) {
                    const hay = o.searchText || [o.label, o.value, o.compKey, o.rootKey].join(' ').toLowerCase();
                    return hay.indexOf(q) >= 0;
                });
            }
            list.sort(function (a, b) {
                return String(a.label || '').localeCompare(String(b.label || ''));
            });
            const total = list.length;
            const cap = 80;
            if (q && total > cap) {
                return { items: list.slice(0, cap), total: total, truncated: true };
            }
            return { items: list, total: total, truncated: false };
        }

        function clearUniqueQuickAccessSuggestLayout(panelEl) {
            if (!panelEl) return;
            panelEl.classList.remove('unique-quick-access-open');
            const sug = panelEl.querySelector('.unique-quick-access-suggest');
            if (!sug) return;
            sug.classList.remove('unique-quick-access-suggest--fixed');
            sug.style.position = '';
            sug.style.left = '';
            sug.style.top = '';
            sug.style.width = '';
            sug.style.maxHeight = '';
            sug.style.zIndex = '';
        }

        function positionUniqueQuickAccessSuggest(panelEl) {
            if (!panelEl || !panelEl.classList.contains('unique-quick-access-open')) return;
            const input = panelEl.querySelector('.unique-quick-access-input');
            const sug = panelEl.querySelector('.unique-quick-access-suggest');
            if (!input || !sug || sug.style.display === 'none') return;
            const r = input.getBoundingClientRect();
            const margin = 4;
            const cap = Math.min(560, window.innerHeight * 0.68);
            const spaceBelow = window.innerHeight - r.bottom - margin;
            const spaceAbove = r.top - margin;
            let topV = r.bottom + margin;
            let maxHeight = Math.min(cap, Math.max(0, spaceBelow - margin));
            if (maxHeight < 140 && spaceAbove > spaceBelow) {
                maxHeight = Math.min(cap, Math.max(0, spaceAbove - margin));
                topV = Math.max(margin, r.top - maxHeight - margin);
            }
            maxHeight = Math.max(160, maxHeight);
            let leftV = r.left;
            let width = r.width;
            const pad = 8;
            if (leftV + width > window.innerWidth - pad) {
                leftV = Math.max(pad, window.innerWidth - width - pad);
            }
            if (leftV < pad) {
                width = Math.max(200, Math.min(width, window.innerWidth - pad * 2));
                leftV = pad;
            }
            const fcb = miGetFixedContainingBlock(sug);
            let left = leftV;
            let top = topV;
            if (fcb) {
                const cbr = fcb.getBoundingClientRect();
                left = leftV - cbr.left;
                top = topV - cbr.top;
            }
            sug.classList.add('unique-quick-access-suggest--fixed');
            sug.style.position = 'fixed';
            sug.style.left = left + 'px';
            sug.style.top = top + 'px';
            sug.style.width = width + 'px';
            sug.style.maxHeight = maxHeight + 'px';
            sug.style.zIndex = '10050';
        }

        let __uqaRelayoutScheduled = false;
        function scheduleUniqueQuickAccessRelayout() {
            if (__uqaRelayoutScheduled) return;
            __uqaRelayoutScheduled = true;
            window.requestAnimationFrame(function () {
                __uqaRelayoutScheduled = false;
                document.querySelectorAll('.unique-quick-access-panel.unique-quick-access-open').forEach(
                    positionUniqueQuickAccessSuggest
                );
            });
        }

        function showUniqueQuickAccessSuggest(panelEl) {
            if (!panelEl) return;
            panelEl.classList.add('unique-quick-access-open');
            window.requestAnimationFrame(function () {
                positionUniqueQuickAccessSuggest(panelEl);
            });
        }

        function flashLegitBuilderJumpHighlight(el) {
            if (!el || !el.classList) return;
            el.classList.add('lb-jump-highlight');
            window.setTimeout(function () {
                el.classList.remove('lb-jump-highlight');
            }, 2200);
        }

        function findLegitBuilderInvCompDepItem(container, rootKey, compKey) {
            if (!container || !rootKey || !compKey) return null;
            const rk = String(rootKey).trim();
            const pk = String(compKey).trim();
            const sel =
                '.dep-item[data-table-name="inv_comp"][data-root-key="' +
                rk.replace(/\\/g, '\\\\').replace(/"/g, '\\"') +
                '"][data-part-key="' +
                pk.replace(/\\/g, '\\\\').replace(/"/g, '\\"') +
                '"]';
            let hit = container.querySelector(sel);
            if (hit) return hit;
            return Array.from(container.querySelectorAll('.dep-item[data-table-name="inv_comp"]')).find(function (el) {
                return (
                    String(el.getAttribute('data-root-key') || '').trim() === rk &&
                    String(el.getAttribute('data-part-key') || '').trim() === pk
                );
            }) || null;
        }

        function miFindModdedInvCompCandRow(invSlot, rootKey, compKey) {
            if (!invSlot || !compKey) return null;
            const crossRef = makeMiCrossPartRef(rootKey, compKey);
            let candRow =
                invSlot.querySelector('.mi-cand-row[data-ref="' + compKey.replace(/"/g, '\\"') + '"]') ||
                (crossRef
                    ? invSlot.querySelector(
                          '.mi-cand-row[data-ref="' + crossRef.replace(/"/g, '\\"') + '"]'
                      )
                    : null);
            if (!candRow) {
                candRow =
                    Array.from(invSlot.querySelectorAll('.mi-cand-row')).find(function (row) {
                        const ref = String(row.getAttribute('data-ref') || '');
                        const parsed = parseMiCrossPartRef(ref);
                        if (parsed && parsed.partKey === compKey) return true;
                        return ref === compKey;
                    }) || null;
            }
            return candRow;
        }

        /** Quick Access / jump: set inv_comp picked row and sync modded builder state. */
        function miApplyModdedInvCompSelection(rootItem, rootKey, compKey, invSlot, candRow) {
            if (!rootItem || !rootKey || !compKey) return false;
            const slot =
                invSlot || rootItem.querySelector('.mi-compact-slot[data-table-name="inv_comp"]');
            if (!slot) return false;
            window.__lbCtx = window.legitBuilderInstances && window.legitBuilderInstances[1];
            const crossRef = makeMiCrossPartRef(rootKey, compKey);
            let encodedRef = crossRef || compKey;
            if (candRow) {
                const rowRef = candRow.getAttribute('data-ref');
                if (rowRef) encodedRef = rowRef;
            } else {
                const matchedRow = miFindModdedInvCompCandRow(slot, rootKey, compKey);
                if (matchedRow) {
                    candRow = matchedRow;
                    const rowRef = matchedRow.getAttribute('data-ref');
                    if (rowRef) encodedRef = rowRef;
                }
            }
            const rowsWrap = miEnsurePartSlotPickedRows(slot);
            if (!rowsWrap) return false;
            const chipHost = miPickedRowsChipHost(rowsWrap);
            const lineRoot = chipHost || rowsWrap;
            lineRoot.innerHTML = '';
            miSlotAppendOrMergeLastRow(rowsWrap, encodedRef, 1);
            const rich = slot.querySelector('.mi-passive-candidate-list.mi-slot-candidate-rich');
            if (rich) {
                const selBg = '#2a3f5f';
                const selOutline = '1px solid #5c9bd1';
                rich.querySelectorAll('.mi-cand-row').forEach(function (row) {
                    const ref = String(row.getAttribute('data-ref') || '');
                    const parsed = parseMiCrossPartRef(ref);
                    const match =
                        row === candRow ||
                        ref === encodedRef ||
                        ref === compKey ||
                        (parsed && parsed.partKey === compKey);
                    if (match) {
                        row.classList.add('mi-cand-selected');
                        row.setAttribute('aria-selected', 'true');
                        row.style.background = selBg;
                        row.style.outline = selOutline;
                    } else {
                        row.classList.remove('mi-cand-selected');
                        row.setAttribute('aria-selected', 'false');
                        row.style.background = '';
                        row.style.outline = '';
                    }
                });
                miUpdateSlotCandDropdownLabel(slot);
            }
            syncMiPartLinesFromRootItem(rootItem);
            miRefreshPartSlotPickedListUi(rootItem);
            updateSelectedPartsOutput();
            updateFinalOutput();
            return true;
        }

        function jumpToBuilderUniqueCompModded(entry, attempt) {
            const rootKey = String(entry.rootKey || '').trim();
            const compKey = String(entry.compKey || '').trim();
            const n = attempt != null ? attempt : 0;
            if (n === 0 && typeof applyMiSimpleBuilderFromRootKey === 'function') {
                applyMiSimpleBuilderFromRootKey(rootKey);
            }
            const miRoots = document.getElementById('mi_rootsContent');
            expandLegitBuilderRootsForRootKey(rootKey, miRoots);
            window.legitBuilderSetCtx(1);
            const rootItem = findMiModdedRootItemEl(rootKey);
            if (!rootItem && n < 20) {
                window.requestAnimationFrame(function () {
                    jumpToBuilderUniqueCompModded(entry, n + 1);
                });
                return;
            }
            if (rootItem) {
                rootItem.classList.add('expanded');
                const rc = rootItem.querySelector('.root-content');
                if (rc) rc.classList.remove('collapsed');
            }
            const invSlot =
                rootItem &&
                rootItem.querySelector('.mi-compact-slot[data-table-name="inv_comp"]');
            if (invSlot) {
                if (typeof miSlotPopulateNativeSelect === 'function') {
                    miSlotPopulateNativeSelect(invSlot);
                }
                if (typeof miSlotPopulateCandidateList === 'function') {
                    miSlotPopulateCandidateList(invSlot);
                }
            }
            const candRow = invSlot ? miFindModdedInvCompCandRow(invSlot, rootKey, compKey) : null;
            const applied =
                rootItem && invSlot
                    ? miApplyModdedInvCompSelection(rootItem, rootKey, compKey, invSlot, candRow)
                    : false;
            const depHit = findLegitBuilderInvCompDepItem(rootItem || miRoots, rootKey, compKey);
            const scrollTarget = candRow || invSlot || depHit || rootItem;
            if (scrollTarget) {
                miScrollModdedPushTargetIntoView(scrollTarget);
                flashLegitBuilderJumpHighlight(candRow || depHit || rootItem);
            }
            if ((!rootItem || !invSlot || !applied) && n < 20) {
                window.requestAnimationFrame(function () {
                    jumpToBuilderUniqueCompModded(entry, n + 1);
                });
            }
        }

        function jumpToBuilderUniqueCompLegit(entry, attempt) {
            const rootKey = String(entry.rootKey || '').trim();
            const compKey = String(entry.compKey || '').trim();
            const n = attempt != null ? attempt : 0;
            const rootsEl = document.getElementById('rootsContent');
            expandLegitBuilderRootsForRootKey(rootKey, null);
            window.legitBuilderSetCtx(0);
            if (rootsEl && typeof rootsEl.getBoundingClientRect === 'function') {
                try {
                    void rootsEl.getBoundingClientRect();
                } catch (eBr) {
                    /* ignore */
                }
            }
            const rootItem =
                rootsEl &&
                Array.from(rootsEl.querySelectorAll('.root-item')).find(function (item) {
                    const el = item.querySelector('[data-root-key]');
                    return el && String(el.getAttribute('data-root-key') || '').trim() === rootKey;
                });
            if (rootItem) {
                rootItem.classList.add('expanded');
                const rc = rootItem.querySelector('.root-content');
                if (rc) rc.classList.remove('collapsed');
            }
            const depHit = findLegitBuilderInvCompDepItem(rootItem || rootsEl, rootKey, compKey);
            if (depHit) {
                const depTable = depHit.closest('.dep-table');
                if (depTable) {
                    const hdr = depTable.querySelector('.dep-table-header');
                    if (hdr && depTable.classList.contains('collapsed')) {
                        depTable.classList.remove('collapsed');
                    }
                }
                depHit.scrollIntoView({ block: 'center', behavior: n > 0 ? 'auto' : 'smooth' });
                flashLegitBuilderJumpHighlight(depHit);
                return;
            }
            if (n < 20) {
                window.requestAnimationFrame(function () {
                    jumpToBuilderUniqueCompLegit(entry, n + 1);
                });
            } else if (rootItem) {
                rootItem.scrollIntoView({ block: 'center', behavior: 'smooth' });
                flashLegitBuilderJumpHighlight(rootItem);
            }
        }

        function jumpToBuilderUniqueComp(entry, options) {
            options = options || {};
            if (!entry || !entry.rootKey || !entry.compKey) return;
            const modded = !!options.modded;
            if (modded) {
                if (typeof switchTab === 'function') {
                    window.__suppressScrollMainViewportToTopOnce = true;
                    switchTab('item-editor-tab');
                }
                window.legitBuilderSetCtx(1);
            } else {
                if (typeof switchTab === 'function') {
                    window.__suppressScrollMainViewportToTopOnce = true;
                    switchTab('legit-builder-tab');
                }
                window.legitBuilderSetCtx(0);
            }
            if (typeof switchLegitTab === 'function') {
                switchLegitTab('roots');
            }
            window.requestAnimationFrame(function () {
                if (modded) {
                    jumpToBuilderUniqueCompModded(entry, 0);
                } else {
                    jumpToBuilderUniqueCompLegit(entry, 0);
                }
            });
        }
        window.jumpToBuilderUniqueComp = jumpToBuilderUniqueComp;

        function renderUniqueQuickAccessSuggest(panelEl, query, tierFilter, show, categoryFilter) {
            if (!panelEl) return;
            const sug = panelEl.querySelector('.unique-quick-access-suggest');
            const input = panelEl.querySelector('.unique-quick-access-input');
            if (!sug) return;
            const opts = window.legitBuilderGetUniqueCompQuickAccessOptions
                ? window.legitBuilderGetUniqueCompQuickAccessOptions()
                : [];
            const filtered = filterUniqueCompQuickAccess(query, tierFilter, categoryFilter);
            const list = filtered.items || [];
            panelEl._uqaActiveIdx = -1;
            if (opts.length === 0) {
                sug.innerHTML =
                    '<div class="unique-quick-access-empty">Load Nexus JSON to index legendary and pearlescent uniques.</div>';
                sug._uqaList = null;
            } else if (list.length === 0) {
                sug.innerHTML =
                    '<div class="unique-quick-access-empty">No matches. Try another search or tier filter.</div>';
                sug._uqaList = null;
            } else {
                let html = list
                    .map(function (o, idx) {
                        const tierClass =
                            o.tier === 'pearlescent'
                                ? 'unique-quick-access-item--pearl'
                                : 'unique-quick-access-item--leg';
                        const tierLabel = o.tier === 'pearlescent' ? 'Pearlescent' : 'Legendary';
                        const meta = [o.rootKey, o.compKey].filter(Boolean).join(' · ');
                        return (
                            '<div class="unique-quick-access-item ' +
                            tierClass +
                            '" role="option" data-idx="' +
                            idx +
                            '" data-root-key="' +
                            escapeLegitHtml(o.rootKey) +
                            '" data-comp-key="' +
                            escapeLegitHtml(o.compKey) +
                            '" data-tier="' +
                            escapeLegitHtml(o.tier) +
                            '">' +
                            '<div class="unique-quick-access-item-label">' +
                            escapeLegitHtml(o.label) +
                            '</div>' +
                            '<div class="unique-quick-access-item-meta">' +
                            '<span class="unique-quick-access-tier">' +
                            escapeLegitHtml(tierLabel) +
                            '</span> · ' +
                            escapeLegitHtml(meta) +
                            '</div></div>'
                        );
                    })
                    .join('');
                if (filtered.truncated) {
                    html +=
                        '<div class="unique-quick-access-footer">' +
                        escapeLegitHtml(
                            String(list.length) + ' of ' + String(filtered.total) + ' matches — refine search to see more'
                        ) +
                        '</div>';
                } else if (!(query || '').trim() && filtered.total > 0) {
                    html +=
                        '<div class="unique-quick-access-footer">' +
                        escapeLegitHtml(String(filtered.total) + ' uniques — type to filter') +
                        '</div>';
                }
                sug.innerHTML = html;
                sug._uqaList = list;
            }
            if (!show) return;
            sug.style.display = 'block';
            if (input) input.setAttribute('aria-expanded', 'true');
            showUniqueQuickAccessSuggest(panelEl);
        }

        function uniqueQuickAccessSuggestIsOpen(panelEl) {
            if (!panelEl) return false;
            const sug = panelEl.querySelector('.unique-quick-access-suggest');
            return !!(sug && sug.style.display !== 'none' && panelEl.classList.contains('unique-quick-access-open'));
        }

        function hideUniqueQuickAccessSuggest(panelEl) {
            if (!panelEl) return;
            const sug = panelEl.querySelector('.unique-quick-access-suggest');
            const input = panelEl.querySelector('.unique-quick-access-input');
            clearUniqueQuickAccessSuggestLayout(panelEl);
            if (sug) {
                sug.style.display = 'none';
                sug.innerHTML = '';
                sug._uqaList = null;
            }
            panelEl._uqaActiveIdx = -1;
            if (input) input.setAttribute('aria-expanded', 'false');
        }

        function pickUniqueQuickAccessEntry(panelEl, entry) {
            if (!entry) return;
            hideUniqueQuickAccessSuggest(panelEl);
            const input = panelEl.querySelector('.unique-quick-access-input');
            if (input) {
                input.value = entry.label || entry.compKey || '';
            }
            jumpToBuilderUniqueComp(entry, { modded: !!panelEl._uqaModded });
        }

        function wireUniqueQuickAccessPanel(isModded) {
            const prefix = isModded ? 'mi_' : '';
            const panel = document.getElementById(prefix + 'uniqueQuickAccessPanel');
            if (!panel || panel._uqaWired) return;
            panel._uqaWired = true;
            panel._uqaModded = isModded;
            const input = panel.querySelector('.unique-quick-access-input');
            const sug = panel.querySelector('.unique-quick-access-suggest');
            const tierBtns = panel.querySelectorAll('.unique-quick-access-tier');
            const catWrap = panel.querySelector('.unique-quick-access-categories');
            function currentTier() {
                const active = panel.querySelector('.unique-quick-access-tier.is-active');
                return active ? String(active.getAttribute('data-tier') || 'all') : 'all';
            }
            function currentCategory() {
                const active = panel.querySelector('.unique-quick-access-category.is-active');
                return active ? String(active.getAttribute('data-category') || 'all') : 'all';
            }
            function openSuggest() {
                renderUniqueQuickAccessSuggest(
                    panel,
                    input ? input.value : '',
                    currentTier(),
                    true,
                    currentCategory()
                );
            }
            hideUniqueQuickAccessSuggest(panel);
            renderUniqueQuickAccessCategoryChips(panel);
            if (catWrap && !catWrap._uqaDelegated) {
                catWrap._uqaDelegated = true;
                catWrap.addEventListener('click', function (e) {
                    const btn = e.target.closest('.unique-quick-access-category');
                    if (!btn) return;
                    catWrap.querySelectorAll('.unique-quick-access-category').forEach(function (b) {
                        b.classList.remove('is-active');
                    });
                    btn.classList.add('is-active');
                    if (input) {
                        input.focus();
                        openSuggest();
                    }
                });
            }
            tierBtns.forEach(function (btn) {
                btn.addEventListener('click', function () {
                    tierBtns.forEach(function (b) {
                        b.classList.remove('is-active');
                    });
                    btn.classList.add('is-active');
                    if (uniqueQuickAccessSuggestIsOpen(panel)) {
                        openSuggest();
                    }
                });
            });
            if (input) {
                input.addEventListener('focus', openSuggest);
                input.addEventListener('input', openSuggest);
                input.addEventListener('keydown', function (e) {
                    if (e.key === 'ArrowDown' && !uniqueQuickAccessSuggestIsOpen(panel)) {
                        e.preventDefault();
                        openSuggest();
                    }
                    if (!sug || sug.style.display === 'none') return;
                    const items = Array.from(sug.querySelectorAll('.unique-quick-access-item'));
                    if (!items.length) return;
                    let idx = panel._uqaActiveIdx != null ? panel._uqaActiveIdx : -1;
                    if (e.key === 'ArrowDown') {
                        e.preventDefault();
                        idx = Math.min(idx + 1, items.length - 1);
                    } else if (e.key === 'ArrowUp') {
                        e.preventDefault();
                        idx = Math.max(idx - 1, 0);
                    } else if (e.key === 'Enter') {
                        e.preventDefault();
                        if (idx >= 0 && sug._uqaList && sug._uqaList[idx]) {
                            pickUniqueQuickAccessEntry(panel, sug._uqaList[idx]);
                        }
                        return;
                    } else if (e.key === 'Escape') {
                        hideUniqueQuickAccessSuggest(panel);
                        return;
                    } else {
                        return;
                    }
                    panel._uqaActiveIdx = idx;
                    items.forEach(function (it, i) {
                        it.classList.toggle('is-active', i === idx);
                    });
                    if (idx >= 0 && items[idx]) {
                        items[idx].scrollIntoView({ block: 'nearest' });
                    }
                });
            }
            panel.addEventListener('mousedown', function (e) {
                const pick = e.target.closest('.unique-quick-access-item');
                if (!pick || !sug || !sug._uqaList) return;
                e.preventDefault();
                const idx = parseInt(pick.getAttribute('data-idx'), 10);
                if (!isNaN(idx) && sug._uqaList[idx]) {
                    pickUniqueQuickAccessEntry(panel, sug._uqaList[idx]);
                }
            });
            document.addEventListener('mousedown', function (e) {
                if (!panel.contains(e.target)) {
                    hideUniqueQuickAccessSuggest(panel);
                }
            });
            window.addEventListener('legit-builder-unique-legendary-comps-updated', function () {
                renderUniqueQuickAccessCategoryChips(panel);
                if (uniqueQuickAccessSuggestIsOpen(panel)) {
                    openSuggest();
                }
            });
        }

        function initUniqueQuickAccessPanels() {
            wireUniqueQuickAccessPanel(false);
            wireUniqueQuickAccessPanel(true);
            if (!window.__uqaRelayoutListenersWired) {
                window.__uqaRelayoutListenersWired = true;
                window.addEventListener('scroll', scheduleUniqueQuickAccessRelayout, true);
                window.addEventListener('resize', scheduleUniqueQuickAccessRelayout);
            }
        }
        window.initUniqueQuickAccessPanels = initUniqueQuickAccessPanels;

        /**
         * Modded passive_points UI: display name from skilltrees + uitooltip, icon from uiresources or save-editor CDN.
         */
        function resolvePassivePointsUiExtras(depLike, dataRef, depTableName) {
            if (!depLike || !dataRef) return null;
            const tn = normalizeDepTableName(depTableName || depLike.depTableName || 'passive_points');
            if (tn !== 'passive_points') return null;
            const skillInfo = getSkillInfoForPassive(depLike, dataRef);
            let iconUrl = getPassiveUiresourcesIconUrl(depLike, dataRef, 'passive_points');
            if (!iconUrl && skillInfo && (skillInfo.name || skillInfo.skill_name) && skillInfo.character) {
                iconUrl = getSkillImageUrl(skillInfo.name || skillInfo.skill_name, skillInfo.character);
            }
            const invMap =
                dataRef.invNamePartDisplayNames && typeof dataRef.invNamePartDisplayNames === 'object'
                    ? dataRef.invNamePartDisplayNames
                    : {};
            const fallbackName =
                getDisplayNameForPartOrFallback(depLike, invMap) || getDisplayNameForPart(depLike, invMap);
            const displayName =
                (skillInfo && (skillInfo.name || skillInfo.skill_name)) ||
                fallbackName ||
                (depLike.key != null ? String(depLike.key) : '');
            const passiveDescription =
                skillInfo && String(skillInfo.description || '').trim()
                    ? String(skillInfo.description).trim()
                    : '';
            if (!displayName && !iconUrl && !passiveDescription) return null;
            return {
                displayName: displayName || String(depLike.key || ''),
                iconUrl: iconUrl || null,
                description: passiveDescription || null
            };
        }

        function findDepEntryInRootByKeyAndTable(dataRef, sourceRootKey, partKey, preferredTableNorm) {
            if (!dataRef || !dataRef.rootDefinitions || !sourceRootKey || !partKey) return null;
            const r = dataRef.rootDefinitions.find(x => x.key === sourceRootKey);
            if (!r || !Array.isArray(r.depEntries)) return null;
            const matches = r.depEntries.filter(d => d && d.key === partKey);
            if (matches.length === 0) return null;
            if (
                preferredTableNorm &&
                preferredTableNorm !== '_mi_cross_manufacturer' &&
                preferredTableNorm !== '_mi_does_not_exist'
            ) {
                const hit = matches.find(d => normalizeDepTableName(d.depTableName) === preferredTableNorm);
                if (hit) return hit;
            }
            const passiveHit = matches.find(d => normalizeDepTableName(d.depTableName) === 'passive_points');
            if (passiveHit) return passiveHit;
            return matches[0];
        }

        /**
         * Missing-slot dropdown label: full part key, {serial}, then optional (inventory name) and (debugdisplaydescription).
         */
        function buildMissingSlotCandidateLabel(dep) {
            if (!dep) return '';
            const pk = dep.key || '';
            if (!pk) return '';
            const s = getDepSerialIndexForPicker(dep);
            const invMap =
                data && data.invNamePartDisplayNames && typeof data.invNamePartDisplayNames === 'object'
                    ? data.invNamePartDisplayNames
                    : {};
            const uiName = getDisplayNameForPartOrFallback(dep, invMap);
            const debugTxt = getPartDebugDisplayDescription(dep);
            let label = pk;
            if (s != null && !isNaN(Number(s))) {
                label += ` {${s}}`;
            }
            const uiTrim = uiName != null ? String(uiName).trim() : '';
            const dbgTrim = debugTxt != null ? String(debugTxt).trim() : '';
            const parens = [];
            if (uiTrim) {
                parens.push(`(${uiTrim})`);
            }
            if (dbgTrim && dbgTrim.toLowerCase() !== uiTrim.toLowerCase()) {
                parens.push(`(${dbgTrim})`);
            }
            if (parens.length > 0) {
                label += ` ${parens.join(' ')}`;
            }
            return label;
        }

        /** Part keys already present in the serial for a normalized dep table (e.g. barrel_acc). */
        function collectPartKeysInNormalizedSlot(validatedParts, normalizedSlotName) {
            const set = new Set();
            if (!validatedParts || !normalizedSlotName) return set;
            const norm = normalizeDepTableName(normalizedSlotName);
            if (!norm || norm === 'unknown') return set;
            validatedParts.forEach(p => {
                if (!p || !p.found || !p.partKey) return;
                if (normalizeDepTableName(p.partType) !== norm) return;
                set.add(String(p.partKey).toLowerCase());
            });
            return set;
        }

        /** Qualified deps as picker rows, excluding keys already selected in that slot. */
        function filterPickerCandidates(qualifiedDeps, selectedKeySet) {
            if (!qualifiedDeps || !qualifiedDeps.length) return [];
            const sel = selectedKeySet instanceof Set ? selectedKeySet : new Set();
            return qualifiedDeps
                .map(dep => {
                    const pk = dep.key || '';
                    if (!pk) return null;
                    if (sel.has(String(pk).toLowerCase())) return null;
                    return { partKey: pk, label: buildMissingSlotCandidateLabel(dep) || pk };
                })
                .filter(Boolean);
        }

        /** Flatten tag arrays on dep entries (object map entries or raw strings). */
        function flattenPartTagStrings(tags) {
            if (!tags || !Array.isArray(tags)) return [];
            return tags.map(tag => {
                if (typeof tag === 'object' && tag !== null) {
                    const values = Object.values(tag);
                    return values[0] || Object.keys(tag)[0] || '';
                }
                return tag;
            }).filter(Boolean).map(t => String(t));
        }

        const LEGIT_INV_COMP_LABEL_CACHE_VER = 9;
        function invalidateLegitInvCompLabelCachesIfStale(data) {
            if (!data) return;
            if (data._legitInvCompLabelCacheVer === LEGIT_INV_COMP_LABEL_CACHE_VER) return;
            data._legitInvCompLabelCacheVer = LEGIT_INV_COMP_LABEL_CACHE_VER;
            data._legendaryInvCompDisplayCache = Object.create(null);
            data._pearlescentInvCompDisplayCache = Object.create(null);
            data._classModUniLegDisplayByRoot = null;
        }

        /** Ordered slot keys: first match with a resolvable inv_name_part display name wins. */
        const LEGENDARY_COMP_NAME_SLOT_PRIORITY = [
            'barrel',
            'class_mod_body',
            'primary_augment',
            'body',
            'energy_shield_body',
            'armor_shield_body',
            'shield_body',
            'grenade_gadget_body',
            'underbarrel',
            'magazine',
            'magazine_borg',
            'magazine_mal',
            'magazine_ted',
            'magazine_torgue',
            'magazine_cov',
            'magazine_hyp',
            'magazine_jak',
            'magazine_vla',
            'magazine_atl',
            'magazine_dad'
        ];

        function legendaryCompNameSlotOrder(slotKey) {
            const i = LEGENDARY_COMP_NAME_SLOT_PRIORITY.indexOf(slotKey);
            return i === -1 ? 1000 : i;
        }

        /** Collect { slotKey, partKeys } from inv_comp parttypeselectionrules.pairs. */
        function getPartTypeSelectionSlotParts(invCompItem) {
            const pairs = invCompItem?.parttypeselectionrules?.pairs;
            if (!pairs || typeof pairs !== 'object') return [];
            const out = [];
            Object.values(pairs).forEach(p => {
                if (!p || typeof p !== 'object') return;
                const slotKey = p.key != null ? String(p.key) : '';
                const partsArr = p.value?.parts;
                if (!slotKey || !Array.isArray(partsArr) || partsArr.length === 0) return;
                const partKeys = partsArr.map(x => (x && x.part != null ? String(x.part) : '')).filter(Boolean);
                if (partKeys.length) out.push({ slotKey, partKeys });
            });
            return out;
        }

        /**
         * Some Nexus exports list a barrel (etc.) slot on inv_comp with key only and no value.parts,
         * so getPartTypeSelectionSlotParts skips it. Match legendary parts by shared Leg_* tags instead.
         */
        function tryLegendarySuffixFromSharedLegTags(invCompItem, data, invMap) {
            if (!invCompItem || !data?.subItemsByTable || !invMap || typeof invMap !== 'object') return null;
            const compFlat = flattenPartTagStrings(invCompItem.basetags).concat(
                flattenPartTagStrings(invCompItem.addtags)
            );
            const legTags = new Set();
            for (let i = 0; i < compFlat.length; i++) {
                const s = String(compFlat[i] || '').trim();
                if (/^leg_/i.test(s)) legTags.add(s.toLowerCase());
            }
            if (legTags.size === 0) return null;

            for (let si = 0; si < LEGENDARY_COMP_NAME_SLOT_PRIORITY.length; si++) {
                const slotKey = LEGENDARY_COMP_NAME_SLOT_PRIORITY[si];
                const tbl = data.subItemsByTable[slotKey];
                if (!tbl?.items?.length) continue;
                for (let ii = 0; ii < tbl.items.length; ii++) {
                    const it = tbl.items[ii];
                    if (!it) continue;
                    const pTags = flattenPartTagStrings(it.dependencytags)
                        .concat(flattenPartTagStrings(it.addtags))
                        .concat(flattenPartTagStrings(it.basetags));
                    let matched = false;
                    for (let pi = 0; pi < pTags.length; pi++) {
                        if (legTags.has(String(pTags[pi] || '').toLowerCase())) {
                            matched = true;
                            break;
                        }
                    }
                    if (!matched) continue;
                    const name = getDisplayNameForPartOrFallback(it, invMap);
                    if (name) return name;
                }
            }
            return null;
        }

        function findSubItemByTableAndPartKey(data, tableName, partKey) {
            if (!data?.subItemsByTable || !tableName || !partKey) return null;
            const tbl = data.subItemsByTable[tableName];
            if (!tbl?.items?.length) return null;
            const want = String(partKey).toLowerCase();
            const found = tbl.items.find(it => {
                if (!it) return false;
                if (it.key && String(it.key).toLowerCase() === want) return true;
                const bySlot = it[tableName];
                if (bySlot != null && String(bySlot).toLowerCase() === want) return true;
                return false;
            }) || null;
            if (!found) return null;
            const hasAspects = (found.aspects && found.aspects.length) ||
                (found.item && found.item.aspects && found.item.aspects.length);
            if (hasAspects) return found;
            const roots = data.rootDefinitions;
            if (!Array.isArray(roots)) return found;
            for (let r = 0; r < roots.length; r++) {
                const deps = roots[r].depEntries;
                if (!Array.isArray(deps)) continue;
                const full = deps.find(dep =>
                    dep.depTableName === tableName && dep.key && String(dep.key).toLowerCase() === want
                );
                if (full && ((full.aspects && full.aspects.length) || (full.item && full.item.aspects && full.item.aspects.length))) {
                    return Object.assign({}, found, full);
                }
            }
            return found;
        }

        /** Merge full inv_comp payload (parttypeselectionrules, tags) when the list item is shallow. */
        function getEffectiveInvCompItemForLegendary(invCompItem, data) {
            if (!invCompItem || !data) return invCompItem;
            const k = invCompItem.key;
            if (!k) return invCompItem;
            const merged = Object.assign({}, invCompItem);
            const invList = data.subItemsByTable?.inv_comp?.items;
            if (invList) {
                const full = invList.find(it =>
                    it.key === k ||
                    (it.inv_comp && String(it.inv_comp).toLowerCase() === String(k).toLowerCase())
                );
                if (full) {
                    const rules = full.parttypeselectionrules || full.item?.parttypeselectionrules;
                    if (!merged.parttypeselectionrules?.pairs && rules?.pairs) merged.parttypeselectionrules = rules;
                    if ((!merged.basetags || merged.basetags.length === 0) && full.basetags && full.basetags.length) {
                        merged.basetags = full.basetags;
                    }
                    if ((!merged.addtags || merged.addtags.length === 0) && full.addtags && full.addtags.length) {
                        merged.addtags = full.addtags;
                    }
                }
            }
            if (!merged.parttypeselectionrules?.pairs && Array.isArray(data.rootDefinitions)) {
                for (let i = 0; i < data.rootDefinitions.length; i++) {
                    const dep = data.rootDefinitions[i].depEntries?.find(d =>
                        d.depTableName === 'inv_comp' && d.key === k
                    );
                    if (dep?.parttypeselectionrules?.pairs) {
                        merged.parttypeselectionrules = dep.parttypeselectionrules;
                        break;
                    }
                }
            }
            return merged;
        }

        function partKeyMatchesCompUniTags(partKey, uniTags) {
            const pk = String(partKey).toLowerCase();
            for (let i = 0; i < uniTags.length; i++) {
                const u = String(uniTags[i]).toLowerCase();
                const tail = u.replace(/^uni_/, '');
                if (tail && pk.includes(tail)) return true;
            }
            return false;
        }

        /** Stable id for caching per Nexus root (class mod / weapon def). */
        function legendaryInvCompRootCacheId(root) {
            if (!root) return '';
            if (root.key != null && root.key !== '') return String(root.key);
            if (root.serialIndex != null && root.serialIndex !== undefined) return '\0serial:' + root.serialIndex;
            return '';
        }

        /**
         * uni_classmod_leg_* is shared across every class; only bodies listed on THIS root
         * map to the correct display names (e.g. Paladin Furnace vs Siren Technomancer for leg_01).
         */
        function buildClassModUniLegDisplayMapForRoot(root, data) {
            const map = Object.create(null);
            if (!root || !data) return map;
            const invMap = (data.invNamePartDisplayNames && typeof data.invNamePartDisplayNames === 'object')
                ? data.invNamePartDisplayNames
                : {};
            const deps = root.depEntries;
            if (!Array.isArray(deps)) return map;
            for (let i = 0; i < deps.length; i++) {
                const d = deps[i];
                if (!d || d.depTableName !== 'class_mod_body' || !d.key) continue;
                let body = findSubItemByTableAndPartKey(data, 'class_mod_body', d.key);
                if (!body) body = d;
                else body = Object.assign({}, body, d);
                const depArr = body.dependencytags;
                if (!Array.isArray(depArr)) continue;
                for (let j = 0; j < depArr.length; j++) {
                    const tagObj = depArr[j];
                    let tagStr = '';
                    if (typeof tagObj === 'object' && tagObj !== null) {
                        const vals = Object.values(tagObj);
                        tagStr = String(vals[0] || Object.keys(tagObj)[0] || '');
                    } else tagStr = String(tagObj || '');
                    if (!/^uni_classmod_leg_/i.test(tagStr)) continue;
                    const lk = tagStr.toLowerCase();
                    if (map[lk]) continue;
                    const name = getDisplayNameForPartOrFallback(body, invMap);
                    if (name) map[lk] = name;
                }
            }
            return map;
        }

        function getClassModUniLegDisplayMapForRoot(root, data) {
            if (!data || !root) return null;
            invalidateLegitInvCompLabelCachesIfStale(data);
            const rid = legendaryInvCompRootCacheId(root);
            if (!rid) return buildClassModUniLegDisplayMapForRoot(root, data);
            if (!data._classModUniLegDisplayByRoot) data._classModUniLegDisplayByRoot = Object.create(null);
            if (data._classModUniLegDisplayByRoot[rid]) return data._classModUniLegDisplayByRoot[rid];
            const built = buildClassModUniLegDisplayMapForRoot(root, data);
            data._classModUniLegDisplayByRoot[rid] = built;
            return built;
        }

        /**
         * Prefer class-specific leg tags on inv_comp (grav_classmod_leg_01, exo_classmod_leg_01, …)
         * over uni_classmod_leg_01, which is identical for every class.
         */
        function findClassModLegendaryBodyDisplayName(invCompItem, root, data) {
            if (!invCompItem || !root || !data) return null;
            const tags = flattenPartTagStrings(invCompItem.basetags).concat(flattenPartTagStrings(invCompItem.addtags));
            const classLegTag = tags.find(t => {
                const s = String(t).toLowerCase();
                return s.includes('classmod_leg') && !/^uni_classmod_leg_/i.test(t);
            });
            if (!classLegTag) return null;
            const want = String(classLegTag).toLowerCase();
            const deps = root.depEntries;
            if (!Array.isArray(deps)) return null;
            const invMap = (data.invNamePartDisplayNames && typeof data.invNamePartDisplayNames === 'object')
                ? data.invNamePartDisplayNames
                : {};
            for (let i = 0; i < deps.length; i++) {
                const d = deps[i];
                if (!d || d.depTableName !== 'class_mod_body' || !d.key) continue;
                let body = findSubItemByTableAndPartKey(data, 'class_mod_body', d.key);
                if (!body) body = d;
                else body = Object.assign({}, body, d);
                const depTags = flattenPartTagStrings(body.dependencytags);
                if (!depTags.some(t => String(t).toLowerCase() === want)) continue;
                const name = getDisplayNameForPartOrFallback(body, invMap);
                if (name) return name;
            }
            return null;
        }

        function legendaryCompSuffixFromUniTagsOrKey(invCompItem) {
            const tags = flattenPartTagStrings(invCompItem.basetags).concat(flattenPartTagStrings(invCompItem.addtags));
            const uni = tags.find(t => /^uni_/i.test(t) && !/^uni_classmod_leg_/i.test(t));
            if (uni) {
                const tail = String(uni).replace(/^uni_/i, '').replace(/_/g, ' ').trim();
                if (tail) {
                    return tail.split(/\s+/).map(w => w.charAt(0).toUpperCase() + w.slice(1).toLowerCase()).join(' ');
                }
            }
            const m = String(invCompItem.key || '').match(/^comp_05_legendary_(.+)$/i);
            if (m && m[1]) {
                const slug = m[1];
                if (/^\d{2}$/.test(slug) || /^raid\d+$/i.test(slug)) return null;
                return slug.split('_').map(w => w.charAt(0).toUpperCase() + w.slice(1).toLowerCase()).join(' ');
            }
            const mPearl = String(invCompItem.key || '').match(/^comp_06_pearl_(.+)$/i);
            if (mPearl && mPearl[1]) {
                const slug = mPearl[1];
                return slug.split('_').map(w => w.charAt(0).toUpperCase() + w.slice(1).toLowerCase()).join(' ');
            }
            const mPearl2 = String(invCompItem.key || '').match(/^comp_06_pearlescent_(.+)$/i);
            if (mPearl2 && mPearl2[1]) {
                const slug = mPearl2[1];
                return slug.split('_').map(w => w.charAt(0).toUpperCase() + w.slice(1).toLowerCase()).join(' ');
            }
            return null;
        }

        /** Match comp uni_* basetags to barrel (etc.) dependencytags and read np_* display name. */
        function tryUniqueSuffixFromSharedUniTags(invCompItem, data, invMap) {
            if (!invCompItem || !data?.subItemsByTable || !invMap || typeof invMap !== 'object') return null;
            const compFlat = flattenPartTagStrings(invCompItem.basetags).concat(
                flattenPartTagStrings(invCompItem.addtags)
            );
            const uniTags = new Set();
            for (let i = 0; i < compFlat.length; i++) {
                const s = String(compFlat[i] || '').trim().toLowerCase();
                if (/^uni_/.test(s) && !/^uni_classmod_leg_/.test(s)) uniTags.add(s);
            }
            if (uniTags.size === 0) return null;

            for (let si = 0; si < LEGENDARY_COMP_NAME_SLOT_PRIORITY.length; si++) {
                const slotKey = LEGENDARY_COMP_NAME_SLOT_PRIORITY[si];
                const tbl = data.subItemsByTable[slotKey];
                if (!tbl?.items?.length) continue;
                for (let ii = 0; ii < tbl.items.length; ii++) {
                    const it = tbl.items[ii];
                    if (!it) continue;
                    const pTags = flattenPartTagStrings(it.dependencytags)
                        .concat(flattenPartTagStrings(it.addtags))
                        .concat(flattenPartTagStrings(it.basetags));
                    let matched = false;
                    for (let pi = 0; pi < pTags.length; pi++) {
                        if (uniTags.has(String(pTags[pi] || '').toLowerCase())) {
                            matched = true;
                            break;
                        }
                    }
                    if (!matched) continue;
                    const name = getDisplayNameForPartOrFallback(it, invMap);
                    if (name) return name;
                }
            }
            return null;
        }

        /** Prefer linked parts on the same root (barrel with InventoryNamingAspect) before global sub-item scan. */
        function tryUniqueDisplayNameFromRootDeps(invCompItem, data, root, invMap) {
            if (!root || !Array.isArray(root.depEntries) || !invCompItem) return null;
            const effective = getEffectiveInvCompItemForLegendary(invCompItem, data);
            const slots = getPartTypeSelectionSlotParts(effective).sort(function (a, b) {
                const oa = legendaryCompNameSlotOrder(a.slotKey);
                const ob = legendaryCompNameSlotOrder(b.slotKey);
                if (oa !== ob) return oa - ob;
                return String(a.slotKey).localeCompare(String(b.slotKey));
            });
            for (let s = 0; s < slots.length; s++) {
                const slotKey = slots[s].slotKey;
                const partKeys = slots[s].partKeys;
                for (let k = 0; k < partKeys.length; k++) {
                    const want = String(partKeys[k]).toLowerCase();
                    const dep = root.depEntries.find(function (d) {
                        return (
                            d &&
                            String(d.depTableName || '').toLowerCase() === String(slotKey).toLowerCase() &&
                            d.key &&
                            String(d.key).toLowerCase() === want
                        );
                    });
                    if (!dep) continue;
                    const name = getDisplayNameForPartOrFallback(dep, invMap);
                    if (name) return name;
                }
            }
            return null;
        }

        function computeLegendaryInvCompDisplaySuffix(invCompItem, data, root) {
            if (!invCompItem?.key) return null;
            const keyLc = String(invCompItem.key).toLowerCase();
            if (
                !/^comp_05_legendary/i.test(keyLc) &&
                !/^comp_06_pearl/i.test(keyLc) &&
                !/^comp_06_pearlescent/i.test(keyLc)
            ) {
                return null;
            }
            const invMap = (data && data.invNamePartDisplayNames && typeof data.invNamePartDisplayNames === 'object')
                ? data.invNamePartDisplayNames
                : {};

            const compUniTags = flattenPartTagStrings(invCompItem.basetags).concat(flattenPartTagStrings(invCompItem.addtags))
                .filter(t => /^uni_/i.test(t));

            // Class-mod legendaries list generic class_mod_body keys (leg_body_01, …) in slots; global lookup hits the wrong class.
            // Resolve via class-specific tags + this root's deps before any global slot walk.
            const uniLegEarly = compUniTags.find(t => /^uni_classmod_leg_/i.test(t));
            if (uniLegEarly && root) {
                const byClassTag = findClassModLegendaryBodyDisplayName(invCompItem, root, data);
                if (byClassTag) return byClassTag;
                const mmapEarly = getClassModUniLegDisplayMapForRoot(root, data);
                const nEarly = mmapEarly && mmapEarly[uniLegEarly.toLowerCase()];
                if (nEarly) return nEarly;
            }

            if (root) {
                const fromRootDeps = tryUniqueDisplayNameFromRootDeps(invCompItem, data, root, invMap);
                if (fromRootDeps) return fromRootDeps;
            }

            const slots = getPartTypeSelectionSlotParts(invCompItem).sort((a, b) => {
                const oa = legendaryCompNameSlotOrder(a.slotKey);
                const ob = legendaryCompNameSlotOrder(b.slotKey);
                if (oa !== ob) return oa - ob;
                return String(a.slotKey).localeCompare(String(b.slotKey));
            });

            for (let s = 0; s < slots.length; s++) {
                const { slotKey, partKeys } = slots[s];
                let keysToTry = [];
                if (partKeys.length === 1) {
                    keysToTry = partKeys;
                } else {
                    keysToTry = partKeys.filter(pk => {
                        const low = pk.toLowerCase();
                        return low.includes('unique') || partKeyMatchesCompUniTags(pk, compUniTags);
                    });
                }
                for (let k = 0; k < keysToTry.length; k++) {
                    const pk = keysToTry[k];
                    if (root && Array.isArray(root.depEntries)) {
                        const want = String(pk).toLowerCase();
                        const rootDep = root.depEntries.find(function (dep) {
                            return (
                                dep &&
                                String(dep.depTableName || '').toLowerCase() === String(slotKey).toLowerCase() &&
                                dep.key &&
                                String(dep.key).toLowerCase() === want
                            );
                        });
                        if (rootDep) {
                            const nameRoot = getDisplayNameForPartOrFallback(rootDep, invMap);
                            if (nameRoot) return nameRoot;
                        }
                    }
                    const sub = findSubItemByTableAndPartKey(data, slotKey, pk);
                    if (!sub) continue;
                    const name = getDisplayNameForPartOrFallback(sub, invMap);
                    if (name) return name;
                }
            }

            const fromLegTags = tryLegendarySuffixFromSharedLegTags(invCompItem, data, invMap);
            if (fromLegTags) return fromLegTags;

            const fromUniTags = tryUniqueSuffixFromSharedUniTags(invCompItem, data, invMap);
            if (fromUniTags) return fromUniTags;

            return legendaryCompSuffixFromUniTagsOrKey(invCompItem);
        }

        /**
         * Friendly name for legendary inv_comp cards (weapon barrel, class mod body, augment, etc.).
         * Cached on data._legendaryInvCompDisplayCache by root id + comp key (class mod comp keys repeat per class).
         */
        function resolveLegendaryInvCompDisplaySuffix(invCompItem, data, root) {
            if (!invCompItem?.key || !/^comp_05_legendary/i.test(invCompItem.key)) return null;
            if (!data) return null;
            invalidateLegitInvCompLabelCachesIfStale(data);
            if (!data._legendaryInvCompDisplayCache) data._legendaryInvCompDisplayCache = Object.create(null);
            const ck = invCompItem.key;
            const rid = legendaryInvCompRootCacheId(root);
            const cacheKey = rid + '\x1f' + ck;
            if (Object.prototype.hasOwnProperty.call(data._legendaryInvCompDisplayCache, cacheKey)) {
                const hit = data._legendaryInvCompDisplayCache[cacheKey];
                return hit || null;
            }
            const effective = getEffectiveInvCompItemForLegendary(invCompItem, data);
            const computed = computeLegendaryInvCompDisplaySuffix(effective, data, root);
            data._legendaryInvCompDisplayCache[cacheKey] = computed || '';
            return computed;
        }

        /** Pearlescent inv_comp: resolve np_* name from parttypeselectionrules barrel (etc.), same as legendary. */
        function resolvePearlescentInvCompDisplaySuffix(invCompItem, data, root) {
            if (!invCompItem?.key || !/^comp_06_pearl/i.test(String(invCompItem.key))) return null;
            if (!data) return null;
            invalidateLegitInvCompLabelCachesIfStale(data);
            if (!data._pearlescentInvCompDisplayCache) data._pearlescentInvCompDisplayCache = Object.create(null);
            const ck = invCompItem.key;
            const rid = legendaryInvCompRootCacheId(root);
            const cacheKey = rid + '\x1f' + ck;
            if (Object.prototype.hasOwnProperty.call(data._pearlescentInvCompDisplayCache, cacheKey)) {
                const hit = data._pearlescentInvCompDisplayCache[cacheKey];
                return hit || null;
            }
            const effective = getEffectiveInvCompItemForLegendary(invCompItem, data);
            const computed = computeLegendaryInvCompDisplaySuffix(effective, data, root);
            data._pearlescentInvCompDisplayCache[cacheKey] = computed || '';
            return computed;
        }

        /**
         * Legit-parity label for validateSerialCore validatedParts.displaySerial (merge class_mod_body from subItems + naming aspects).
         * Used by save editor item lines, getValidatorPartLabelByIndex, and inventory filter corpus.
         */
        function computeValidatorPartDisplaySerial(part, partTokenFallback, data) {
            const fb = (partTokenFallback && String(partTokenFallback).trim()) || '';
            if (!part || !data) return fb;
            const fp = part.foundPart;
            const invMap =
                data.invNamePartDisplayNames && typeof data.invNamePartDisplayNames === 'object'
                    ? data.invNamePartDisplayNames
                    : {};
            const pt = String(part.partType || '').toLowerCase();
            let label = null;

            if (fp && (pt === 'class_mod_body' || pt.indexOf('leg_body') === 0)) {
                let body = fp;
                if (part.partKey) {
                    const sub = findSubItemByTableAndPartKey(data, 'class_mod_body', part.partKey);
                    if (sub) body = Object.assign({}, sub, fp);
                }
                label = getDisplayNameForPartOrFallback(body, invMap);
            } else if (
                fp &&
                pt === 'inv_comp' &&
                fp.key &&
                (/^comp_05_legendary/i.test(fp.key) || /^comp_06_pearl/i.test(fp.key))
            ) {
                const rootDef =
                    part.foundInRoot && Array.isArray(data.rootDefinitions)
                        ? data.rootDefinitions.find(function (r) {
                              return (
                                  r &&
                                  String(r.key).toLowerCase() === String(part.foundInRoot).toLowerCase()
                              );
                          })
                        : null;
                if (rootDef) {
                    const eff = getEffectiveInvCompItemForLegendary(fp, data);
                    const suffix = computeLegendaryInvCompDisplaySuffix(eff, data, rootDef);
                    const base =
                        getDisplayNameForPartOrFallback(eff, invMap) ||
                        getDisplayNameForPart(eff, invMap) ||
                        '';
                    if (suffix && base) label = `${String(base).trim()} (${suffix})`;
                    else if (suffix) label = suffix;
                    else label = base || null;
                }
            }

            if (!label && fp) {
                label = getDisplayNameForPartOrFallback(fp, invMap);
                if (!label) label = getDisplayNameForPart(fp, invMap);
            }

            const t = (label && String(label).trim()) || '';
            return t || fb || (part.partKey ? String(part.partKey) : '');
        }

        /**
         * Resolve an attribute reference (e.g. attribute'mal_sm_barrel_01_damage_OhmIGot') to a constant or datatable ref.
         * @param {string} attributeRef - String like "attribute'name'" or just the name
         * @param {{ attributeByKey?: Map<string, object> }} data - App data with attribute map
         * @returns {{ type: 'constant', value: number }|{ type: 'datatable', datatable: string, rowname?: string, columnname?: string }|{ type: 'unknown' }|null}
         */
        function resolveAttributeValue(attributeRef, data) {
            if (!attributeRef || typeof attributeRef !== 'string') return null;
            const match = attributeRef.match(/attribute'([^']*)'/i) || attributeRef.match(/'([^']*)'/);
            const key = (match ? match[1].trim() : attributeRef.trim()).toLowerCase();
            if (!key) return null;
            const entry = data?.attributeByKey?.get(key);
            if (!entry || !entry.value) return { type: 'unknown' };
            const v = entry.value;

            const parseNum = (x) => {
                if (x == null) return NaN;
                if (typeof x === 'number' && !Number.isNaN(x)) return x;
                const n = parseFloat(String(x));
                return Number.isNaN(n) ? NaN : n;
            };
            const getDt = (obj) => {
                if (!obj || typeof obj !== 'object') return null;
                const dtv = obj.datatablevalue;
                if (!dtv || typeof dtv !== 'object') return null;
                const datatable = dtv.datatable != null ? String(dtv.datatable) : '';
                const rowname = dtv.rowname != null ? String(dtv.rowname) : undefined;
                const columnname = dtv.columnname != null ? String(dtv.columnname) : undefined;
                return { datatable, rowname, columnname };
            };
            const lookupDatatableValue = (dt) => {
                if (!dt || !data?.datatableValues) return null;
                const tableName = normalizeDatatableName(dt.datatable || '');
                if (!tableName) return null;
                const columnKey = (dt.columnname || '').toLowerCase();
                if (!columnKey) return null;
                const val = getDatatableCell(data, tableName, dt.rowname, columnKey);
                if (typeof val === 'number' && !Number.isNaN(val)) return val;
                if (val != null) { const n = parseNum(val); if (!Number.isNaN(n)) return n; }
                return null;
            };

            if (v.constant != null) {
                const num = parseNum(v.constant);
                if (!Number.isNaN(num)) return { type: 'constant', value: num };
            }
            const directDt = getDt(v);
            if (directDt) {
                const numVal = lookupDatatableValue(directDt);
                if (numVal != null) return { type: 'constant', value: numVal };
                return { type: 'datatable', ...directDt };
            }
            const levelDt = getDt(v.level);
            if (levelDt) {
                const numVal = lookupDatatableValue(levelDt);
                if (numVal != null) return { type: 'constant', value: numVal };
                return { type: 'datatable', ...levelDt };
            }
            const multDt = getDt(v.multiplier);
            if (multDt) {
                const numVal = lookupDatatableValue(multDt);
                if (numVal != null) return { type: 'constant', value: numVal };
                return { type: 'datatable', ...multDt };
            }
            const scalarDt = getDt(v.scalar);
            if (scalarDt) {
                const numVal = lookupDatatableValue(scalarDt);
                if (numVal != null) return { type: 'constant', value: numVal };
                return { type: 'datatable', ...scalarDt };
            }
            if (v.level?.constant != null) {
                const num = parseNum(v.level.constant);
                if (!Number.isNaN(num)) return { type: 'constant', value: num };
            }
            if (v.multiplier?.constant != null) {
                const num = parseNum(v.multiplier.constant);
                if (!Number.isNaN(num)) return { type: 'constant', value: num };
            }
            if (v.scalar?.constant != null) {
                const num = parseNum(v.scalar.constant);
                if (!Number.isNaN(num)) return { type: 'constant', value: num };
            }
            return { type: 'unknown' };
        }

        /**
         * Extract damage type(s), shot ammo cost(s), and part stats (trigger, fire rate, charge/discharge, gestalt, damage/accuracy/spread refs, constants, direct dt refs) from a part entry.
         * @param {Object} partEntry - Part/dep entry with aspects array
         * @returns {{
         *   damageTypes: string[], shotAmmoCosts: string[],
         *   triggerEveryShots: number[], fireRateConstants: number[], chargeTimes: number[], dischargeTimes: number[], cancelChargeTimes: number[],
         *   gestaltNames: string[], damageRefs: string[], accuracyRefs: string[], spreadRefs: string[],
         *   damageConstants: number[], accuracyConstants: number[], spreadConstants: number[],
         *   damageDirectDtRefs: Array<{datatable:string,rowname?:string,columnname?:string}>, accuracyDirectDtRefs: Array<{}>, spreadDirectDtRefs: Array<{}>
         * }}
         */
        function getPartDamageTypeAndAmmo(partEntry) {
            const damageTypes = [];
            const shotAmmoCosts = [];
            const triggerEveryShots = [];
            const fireRateConstants = [];
            const chargeTimes = [];
            const dischargeTimes = [];
            const cancelChargeTimes = [];
            const gestaltNames = [];
            const damageRefs = [];
            const accuracyRefs = [];
            const spreadRefs = [];
            const damageConstants = [];
            const accuracyConstants = [];
            const spreadConstants = [];
            const damageDirectDtRefs = [];
            const accuracyDirectDtRefs = [];
            const spreadDirectDtRefs = [];
            const seenDamage = new Set();
            const seenAmmo = new Set();
            const seenTrigger = new Set();
            const seenFireRate = new Set();
            const seenCharge = new Set();
            const seenDischarge = new Set();
            const seenCancelCharge = new Set();
            const seenGestalt = new Set();
            const seenDamageRef = new Set();
            const seenAccuracyRef = new Set();
            const seenSpreadRef = new Set();
            const seenDamageConst = new Set();
            const seenAccuracyConst = new Set();
            const seenSpreadConst = new Set();
            const parseDamagetype = (val) => {
                if (val == null || typeof val !== 'string') return null;
                const m = val.match(/damagetype'([^']*)'/i) || val.match(/'([^']*)'/);
                return m ? m[1].trim() : (val.trim() || null);
            };
            const addAttrRef = (ref, list, seen) => {
                if (!ref || typeof ref !== 'string') return;
                const r = ref.trim();
                if (r && !seen.has(r)) { seen.add(r); list.push(r); }
            };
            const parseNum = (x) => {
                if (x == null) return NaN;
                if (typeof x === 'number' && !Number.isNaN(x)) return x;
                return parseFloat(String(x));
            };

            if (partEntry?.gestaltpartnames && Array.isArray(partEntry.gestaltpartnames) && partEntry.gestaltpartnames[0]) {
                const g = String(partEntry.gestaltpartnames[0]).trim();
                if (g && !seenGestalt.has(g)) { seenGestalt.add(g); gestaltNames.push(g); }
            }

            if (!partEntry?.aspects || !Array.isArray(partEntry.aspects)) {
                return { damageTypes, shotAmmoCosts, triggerEveryShots, fireRateConstants, chargeTimes, dischargeTimes, cancelChargeTimes, gestaltNames, damageRefs, accuracyRefs, spreadRefs, damageConstants, accuracyConstants, spreadConstants, damageDirectDtRefs, accuracyDirectDtRefs, spreadDirectDtRefs };
            }
            partEntry.aspects.forEach((aspect) => {
                if (!aspect) return;
                let dt = parseDamagetype(aspect.damagetype);
                if (dt && !seenDamage.has(dt)) { seenDamage.add(dt); damageTypes.push(dt); }
                if (aspect.behavior) {
                    const bh = aspect.behavior;
                    dt = parseDamagetype(bh.damagetype);
                    if (dt && !seenDamage.has(dt)) { seenDamage.add(dt); damageTypes.push(dt); }
                    const ammo = bh.shotammocost;
                    if (ammo !== undefined && ammo !== null) {
                        const a = String(ammo).trim();
                        if (a && !seenAmmo.has(a)) { seenAmmo.add(a); shotAmmoCosts.push(a); }
                    }
                    if (bh.firerate && bh.firerate.constant != null && bh.firerate.datatablevalue == null) {
                        const fr = parseNum(bh.firerate.constant);
                        if (!Number.isNaN(fr) && !seenFireRate.has(fr)) { seenFireRate.add(fr); fireRateConstants.push(fr); }
                    }
                    if (bh.chargetime?.constant != null) {
                        const c = parseNum(bh.chargetime.constant);
                        if (!Number.isNaN(c) && !seenCharge.has(c)) { seenCharge.add(c); chargeTimes.push(c); }
                    }
                    if (bh.dischargetime?.constant != null) {
                        const d = parseNum(bh.dischargetime.constant);
                        if (!Number.isNaN(d) && !seenDischarge.has(d)) { seenDischarge.add(d); dischargeTimes.push(d); }
                    }
                    if (bh.cancelchargetime?.constant != null) {
                        const cc = parseNum(bh.cancelchargetime.constant);
                        if (!Number.isNaN(cc) && !seenCancelCharge.has(cc)) { seenCancelCharge.add(cc); cancelChargeTimes.push(cc); }
                    }
                    if (bh.damage) {
                        if (bh.damage.attribute) addAttrRef(bh.damage.attribute, damageRefs, seenDamageRef);
                        else if (bh.damage.constant != null) {
                            const n = parseNum(bh.damage.constant);
                            if (!Number.isNaN(n) && !seenDamageConst.has(n)) { seenDamageConst.add(n); damageConstants.push(n); }
                        } else if (bh.damage.datatablevalue && typeof bh.damage.datatablevalue === 'object') {
                            const dtv = bh.damage.datatablevalue;
                            damageDirectDtRefs.push({
                                datatable: dtv.datatable != null ? String(dtv.datatable) : '',
                                rowname: dtv.rowname != null ? String(dtv.rowname) : undefined,
                                columnname: dtv.columnname != null ? String(dtv.columnname) : undefined
                            });
                        }
                    }
                    if (bh.accuracy) {
                        if (bh.accuracy.attribute) addAttrRef(bh.accuracy.attribute, accuracyRefs, seenAccuracyRef);
                        else if (bh.accuracy.constant != null) {
                            const n = parseNum(bh.accuracy.constant);
                            if (!Number.isNaN(n) && !seenAccuracyConst.has(n)) { seenAccuracyConst.add(n); accuracyConstants.push(n); }
                        } else if (bh.accuracy.datatablevalue && typeof bh.accuracy.datatablevalue === 'object') {
                            const dtv = bh.accuracy.datatablevalue;
                            accuracyDirectDtRefs.push({
                                datatable: dtv.datatable != null ? String(dtv.datatable) : '',
                                rowname: dtv.rowname != null ? String(dtv.rowname) : undefined,
                                columnname: dtv.columnname != null ? String(dtv.columnname) : undefined
                            });
                        }
                    }
                    if (bh.spread) {
                        if (bh.spread.attribute) addAttrRef(bh.spread.attribute, spreadRefs, seenSpreadRef);
                        else if (bh.spread.constant != null) {
                            const n = parseNum(bh.spread.constant);
                            if (!Number.isNaN(n) && !seenSpreadConst.has(n)) { seenSpreadConst.add(n); spreadConstants.push(n); }
                        } else if (bh.spread.datatablevalue && typeof bh.spread.datatablevalue === 'object') {
                            const dtv = bh.spread.datatablevalue;
                            spreadDirectDtRefs.push({
                                datatable: dtv.datatable != null ? String(dtv.datatable) : '',
                                rowname: dtv.rowname != null ? String(dtv.rowname) : undefined,
                                columnname: dtv.columnname != null ? String(dtv.columnname) : undefined
                            });
                        }
                    }
                }
                const sm = aspect.augment?.shotmodifier;
                if (sm) {
                    if (sm.damagetype) {
                        dt = parseDamagetype(sm.damagetype);
                        if (dt && !seenDamage.has(dt)) { seenDamage.add(dt); damageTypes.push(dt); }
                    }
                    if (sm.btriggereveryxshots === true || sm.triggereveryxshots != null) {
                        const x = sm.triggereveryxshots?.constant != null ? parseNum(sm.triggereveryxshots.constant) : (sm.triggereveryxshots != null ? parseNum(sm.triggereveryxshots) : NaN);
                        if (!Number.isNaN(x) && x >= 1 && !seenTrigger.has(x)) { seenTrigger.add(x); triggerEveryShots.push(x); }
                    }
                }
            });
            return { damageTypes, shotAmmoCosts, triggerEveryShots, fireRateConstants, chargeTimes, dischargeTimes, cancelChargeTimes, gestaltNames, damageRefs, accuracyRefs, spreadRefs, damageConstants, accuracyConstants, spreadConstants, damageDirectDtRefs, accuracyDirectDtRefs, spreadDirectDtRefs };
        }

        /** Return CSS color for a damage type name (element-themed). */
        function getDamageTypeColor(damageTypeName) {
            if (!damageTypeName || typeof damageTypeName !== 'string') return 'var(--text-secondary)';
            const key = damageTypeName.toLowerCase();
            const colors = {
                fire: '#e87830',
                shock: '#60a8ff',
                cryo: '#70d8e8',
                corrosive: '#90d030',
                radiation: '#b8c830',
                normal: '#a8a8a8'
            };
            return colors[key] || 'var(--text-secondary)';
        }

        /** Build the damage/ammo line HTML with elemental and ammo-count coloring. */
        function buildDamageAmmoLineHTML(damageInfo) {
            if (!damageInfo || (!damageInfo.damageTypes?.length && !damageInfo.shotAmmoCosts?.length)) return '';
            const parts = [];
            if (damageInfo.damageTypes && damageInfo.damageTypes.length > 0) {
                const damageSpans = damageInfo.damageTypes.map(dt =>
                    `<span style="color: ${getDamageTypeColor(dt)}; font-weight: 600;">${dt}</span>`
                ).join(', ');
                parts.push(`<span style="color: var(--text-secondary);">Damage: </span>${damageSpans}`);
            }
            if (damageInfo.shotAmmoCosts && damageInfo.shotAmmoCosts.length > 0) {
                const ammoNum = damageInfo.shotAmmoCosts[0];
                parts.push(`<span style="color: var(--text-secondary);">Ammo/shot: </span><span style="color: #c8b040; font-weight: 600;">${ammoNum}</span>`);
            }
            if (parts.length === 0) return '';
            return `<div style="font-size: 10px; margin-top: 4px;">${parts.join(' <span style="color: var(--text-secondary);">|</span> ')}</div>`;
        }

        const sep = ' <span style="color: var(--text-secondary);">|</span> ';
        /** Resolve a list of direct datatable refs (no attribute) via data.datatableValues; return display strings (number or "Table→Row"). */
        function resolveDirectDtRefsForDisplay(dtRefs, data) {
            if (!dtRefs?.length) return [];
            const out = [];
            dtRefs.forEach((dt) => {
                const tableName = normalizeDatatableName(dt.datatable || '');
                if (!tableName) return;
                const columnKey = (dt.columnname || '').toLowerCase();
                if (data?.datatableValues && columnKey) {
                    const val = getDatatableCell(data, tableName, dt.rowname, columnKey);
                    if (typeof val === 'number' && !Number.isNaN(val)) { out.push(String(val)); return; }
                    const n = val != null ? parseFloat(String(val)) : NaN;
                    if (!Number.isNaN(n)) { out.push(String(n)); return; }
                }
                const tbl = tableName || 'Table';
                const row = dt.rowname || '';
                const col = dt.columnname || '';
                if (row && col) out.push(`(${tbl}→${row})`);
                else if (row) out.push(`(${tbl}→${row})`);
                else if (col) out.push(`(${tbl}→${col})`);
                else out.push(`(${tbl})`);
            });
            return out;
        }
        /** Resolve a list of attribute refs and return display strings (constant value or "Table→Row" style). */
        function resolveRefsForDisplay(refs, data) {
            if (!refs?.length) return [];
            const out = [];
            refs.forEach((ref) => {
                const r = resolveAttributeValue(ref, data);
                if (!r) return;
                if (r.type === 'constant') out.push(String(r.value));
                else if (r.type === 'datatable') {
                    const row = r.rowname || '';
                    const col = r.columnname || '';
                    const tbl = normalizeDatatableName(r.datatable || '');
                    if (row && col) out.push(`(${tbl || 'Table'}→${row})`);
                    else if (col) out.push(`(${tbl || 'Table'}→${col})`);
                    else out.push(`(${tbl || 'Table'})`);
                } else out.push('—');
            });
            return out;
        }

        /** Where to find datatable/attribute refs in part aspects. Add entries for new JSON paths (e.g. aspects[].attributeeffecttemplate.modifiervalue, aspects[].attributeeffects[].modifiervalue). */
        const ASPECT_REF_PATHS = [
            { type: 'template', template: 'attributeeffecttemplate', valueKey: 'modifiervalue', getContext: function (a) { return a.parent ? String(a.parent).replace(/inv_aspect'|'/g, '') : ''; } },
            { type: 'array', array: 'attributeeffects', valueKey: 'modifiervalue', getLabel: function (e) { const m = (e.attributetomodify || '').match(/attribute'([^']*)'/i); return m ? m[1].trim() : (e.attributetomodify || '').replace(/attribute'|'/g, '').trim() || undefined; } },
            { type: 'array', array: 'equippedattributeeffects', valueKey: 'modifiervalue', getLabel: function (e) { const m = (e.attributetomodify || '').match(/attribute'([^']*)'/i); return m ? m[1].trim() : (e.attributetomodify || '').replace(/attribute'|'/g, '').trim() || undefined; } },
            { type: 'array', array: 'armorsegmentattributeeffects', valueKey: 'modifiervalue', getLabel: function (e) { const m = (e.attributetomodify || '').match(/attribute'([^']*)'/i); return m ? m[1].trim() : (e.attributetomodify || '').replace(/attribute'|'/g, '').trim() || undefined; } },
            { type: 'array', array: 'conditionalattributeeffects', valueKey: 'modifiervalue', getLabel: function (e) { const m = (e.attributetomodify || '').match(/attribute'([^']*)'/i); return m ? m[1].trim() : (e.attributetomodify || '').replace(/attribute'|'/g, '').trim() || undefined; } }
        ];

        /** Collect all aspect refs (from ASPECT_REF_PATHS) for resolution. Returns list of { type, value?, ref?, datatable?, rowname?, columnname?, label?, context? }. Deduped. */
        function collectAllAspectRefs(aspects) {
            if (!aspects || !Array.isArray(aspects)) return [];
            const out = [];
            const seen = new Set();
            const key = (r) => {
                if (r.type === 'constant') return `c:${r.label}:${r.value}`;
                if (r.type === 'attribute') return `a:${(r.label || '')}:${(r.ref || '')}`;
                if (r.type === 'datatable') return `d:${(r.label || '')}:${r.datatable}:${r.rowname}:${r.columnname}`;
                return `u:${Math.random()}`;
            };
            const push = (r) => {
                const k = key(r);
                if (seen.has(k)) return;
                seen.add(k);
                out.push(r);
            };
            const parseNum = (x) => {
                if (x == null) return NaN;
                if (typeof x === 'number' && !Number.isNaN(x)) return x;
                return parseFloat(String(x));
            };
            const pushFromModifierValue = (mv, opts) => {
                if (!mv) return;
                if (mv.constant != null) {
                    const n = parseNum(mv.constant);
                    if (!Number.isNaN(n)) push({ type: 'constant', value: n, label: opts.label, context: opts.context });
                }
                if (mv.datatablevalue && typeof mv.datatablevalue === 'object') {
                    const dt = mv.datatablevalue.datatable;
                    if (dt && String(dt).toLowerCase().indexOf("'none'") === -1) {
                        push({ type: 'datatable', datatable: String(mv.datatablevalue.datatable || ''), rowname: mv.datatablevalue.rowname != null ? String(mv.datatablevalue.rowname) : undefined, columnname: mv.datatablevalue.columnname != null ? String(mv.datatablevalue.columnname) : undefined, label: opts.label, context: opts.context });
                    }
                }
                if (mv.attribute && String(mv.attribute).toLowerCase() !== "attribute'none'") {
                    push({ type: 'attribute', ref: mv.attribute, label: opts.label, context: opts.context });
                }
            };
            aspects.forEach((aspect) => {
                if (!aspect) return;
                ASPECT_REF_PATHS.forEach((path) => {
                    if (path.type === 'template') {
                        const template = aspect[path.template];
                        const mv = template && template[path.valueKey];
                        if (mv) pushFromModifierValue(mv, { context: path.getContext(aspect) });
                        return;
                    }
                    if (path.type === 'array') {
                        const arr = aspect[path.array];
                        if (!Array.isArray(arr)) return;
                        arr.forEach((effect) => {
                            if (!effect || !effect[path.valueKey]) return;
                            pushFromModifierValue(effect[path.valueKey], { label: path.getLabel(effect) });
                        });
                    }
                });
            });
            return out;
        }

        /** Resolve one ref (for aspect panel). Returns display string or empty if none/skip. */
        function resolveOneRefForDisplay(ref, data) {
            if (!ref) return '';
            if (ref.type === 'constant') return String(ref.value);
            if (ref.type === 'attribute') {
                const r = resolveAttributeValue(ref.ref, data);
                if (!r) return '—';
                if (r.type === 'constant') return String(r.value);
                if (r.type === 'datatable') {
                    const tbl = normalizeDatatableName(r.datatable || '') || 'Table';
                    const row = r.rowname || '';
                    const col = r.columnname || '';
                    if (row && col) return `(${tbl}→${row})`;
                    if (row) return `(${tbl}→${row})`;
                    if (col) return `(${tbl}→${col})`;
                    return `(${tbl})`;
                }
                return '—';
            }
            if (ref.type === 'datatable') {
                const resolved = resolveDatatableRef(data, ref);
                if (resolved != null && typeof resolved === 'object' && !Array.isArray(resolved)) {
                    const tableName = normalizeDatatableName(ref.datatable || '');
                    const defaultCols = tableName ? DATATABLE_DISPLAY_DEFAULTS[tableName] : null;
                    if (defaultCols && defaultCols.length > 0) {
                        const parts = defaultCols.map(col => {
                            const ck = col.toLowerCase();
                            const v = resolved[ck];
                            if (v == null) return null;
                            const label = labelForDatatableColumn(col);
                            return label ? `${label}: ${v}` : String(v);
                        }).filter(Boolean);
                        if (parts.length > 0) return parts.join(', ');
                    }
                    return `(${tableName || 'Table'}→${ref.rowname || ''})`;
                }
                if (resolved != null && (typeof resolved === 'number' || typeof resolved === 'string')) {
                    if (typeof resolved === 'number' && !Number.isNaN(resolved)) return String(resolved);
                    const n = parseFloat(String(resolved));
                    if (!Number.isNaN(n)) return String(n);
                    return String(resolved);
                }
                const tbl = normalizeDatatableName(ref.datatable || '') || 'Table';
                const row = ref.rowname || '';
                const col = ref.columnname || '';
                if (row && col) return `(${tbl}→${row})`;
                if (row) return `(${tbl}→${row})`;
                if (col) return `(${tbl}→${col})`;
                return `(${tbl})`;
            }
            return '';
        }

        /** Resolve list of aspect refs; returns [{ label?, context?, displayValue }]. */
        function resolveAspectRefs(refs, data) {
            if (!refs || !refs.length) return [];
            return refs.map((r) => {
                const displayValue = resolveOneRefForDisplay(r, data);
                return { label: r.label, context: r.context, displayValue };
            }).filter((x) => x.displayValue !== '');
        }

        /** Build "Other stats" line HTML from resolved aspect refs (limit entries). When simpleMode is true, only includes stats with determined values. */
        function buildOtherStatsLineHTML(resolvedList, limit, simpleMode) {
            if (!resolvedList || !resolvedList.length) return '';
            const list = simpleMode ? resolvedList.filter((r) => isDisplayValueDetermined(r.displayValue)) : resolvedList;
            if (!list.length) return '';
            const cap = Math.min(limit || 5, list.length);
            const parts = [];
            for (let i = 0; i < cap; i++) {
                const r = list[i];
                const label = (r.label || r.context || 'Stat').replace(/_/g, ' ');
                parts.push(`<span style="color: var(--text-secondary);">${label}: </span><span style="font-weight: 600;">${r.displayValue}</span>`);
            }
            if (parts.length === 0) return '';
            return `<div style="font-size: var(--legit-chip-stat, 9px); margin-top: 1px;">${parts.join(sep)}</div>`;
        }

        /** Format a resolved numeric attribute value for ui_stat placeholders. */
        function formatUiStatNumericForDisplay(num, asPercentage) {
            if (typeof num !== 'number' || Number.isNaN(num)) return '—';
            if (asPercentage) {
                const p = num * 100;
                const rounded = Math.round(p);
                if (Math.abs(p - rounded) < 1e-4) return `${rounded}%`;
                const s = p.toFixed(1);
                return (s.endsWith('.0') ? s.slice(0, -2) : s) + '%';
            }
            if (Number.isInteger(num) || Math.abs(num - Math.round(num)) < 1e-6) return String(Math.round(num));
            return String(num);
        }

        /** Replace `{key}` placeholders in Primary ui_stat text using argsmap + resolveAttributeValue. */
        function resolveUiStatPrimaryPlaceholders(text, argSpecs, data) {
            if (text == null || typeof text !== 'string') return '';
            if (!argSpecs || typeof argSpecs !== 'object') return text;
            let out = text;
            Object.keys(argSpecs).forEach((placeholderKey) => {
                const spec = argSpecs[placeholderKey];
                if (!spec || !spec.attributedef) return;
                const re = new RegExp('\\{' + placeholderKey.replace(/[.*+?^${}()|[\]\\]/g, '\\$&') + '\\}', 'g');
                const resolved = resolveAttributeValue(spec.attributedef, data);
                let repl = '—';
                if (resolved && resolved.type === 'constant') {
                    repl = formatUiStatNumericForDisplay(resolved.value, spec.bdisplayaspercentage);
                    if (spec.bdisplayplussign && typeof resolved.value === 'number' && !Number.isNaN(resolved.value) && resolved.value > 0 && repl !== '—') {
                        repl = '+' + repl;
                    }
                }
                out = out.replace(re, repl);
            });
            return out;
        }

        /** Collect description, red text, and Primary effect text from ui_stat refs in UIStatAspect uistatstoinclude. */
        function getUiStatDescriptionAndRedtext(aspects, data) {
            const descriptionTexts = [];
            const redtexts = [];
            const primaryTexts = [];
            if (!aspects || !Array.isArray(aspects) || !data?.uiStatByKey) return { descriptionTexts, redtexts, primaryTexts };
            const seenDesc = new Set();
            const seenRed = new Set();
            const seenPrimary = new Set();
            aspects.forEach((aspect) => {
                if (!aspect || String(aspect.structtype || '').indexOf('UIStatAspect') === -1) return;
                const list = aspect.uistatstoinclude;
                if (!Array.isArray(list)) return;
                list.forEach((ref) => {
                    if (typeof ref !== 'string') return;
                    const m = ref.match(/ui_stat'([^']*)'/i) || ref.match(/'([^']*)'/);
                    const id = (m ? m[1].trim() : ref.trim()).toLowerCase();
                    if (!id) return;
                    const entry = data.uiStatByKey.get(id);
                    if (!entry) return;
                    if (entry.descriptionText && !seenDesc.has(entry.descriptionText)) {
                        seenDesc.add(entry.descriptionText);
                        descriptionTexts.push(entry.descriptionText);
                    }
                    if (entry.redtext && !seenRed.has(entry.redtext)) {
                        seenRed.add(entry.redtext);
                        redtexts.push(entry.redtext);
                    }
                    if (entry.primaryText) {
                        const resolved = resolveUiStatPrimaryPlaceholders(entry.primaryText, entry.primaryArgSpecs, data);
                        if (resolved && !seenPrimary.has(resolved)) {
                            seenPrimary.add(resolved);
                            primaryTexts.push(resolved);
                        }
                    }
                });
            });
            return { descriptionTexts, redtexts, primaryTexts };
        }

        /** Map ui_stat tag names to CSS colors for formatted description/red text. */
        const UI_STAT_TAG_COLORS = {
            rarity_legendary: '#f57c00',
            rarity_pearl: '#00e5ff',
            secondary: '#64b5f6',
            primary: '#ffb74d',
            kinetic: '#ffffff',
            rd_color: '#ef5350'
        };

        /**
         * Private-use placeholders survive esc() and become HTML or text at the end.
         */
        const FMT_PH = {
            nl: '\u200BNL\u200B',
            frtn: '\uE700',
            wfll: '\uE701',
            tediore: '\uE702',
            nowrapS: '\uE703',
            nowrapE: '\uE704',
            roll: '\uE705'
        };

        /** passive_points uitooltip description for serial validator (skilltrees + uitooltipdata). */
        function getPassivePointsTooltipFormatText(validationPart, dataRef) {
            if (!validationPart || !validationPart.found || !validationPart.foundPart || !dataRef) return '';
            if (normalizeDepTableName(validationPart.partType) !== 'passive_points') return '';
            const nx = getSkillInfoForPassive(validationPart.foundPart, dataRef);
            const t = nx && String(nx.description || '').trim();
            return t || '';
        }

        /** Convert raw display text with [tag]...[/tag] to HTML. [newline], [nowrap], [rd_color], frtn/wfll/tediore/roll icons, paired element/secondary tags. */
        function formatUiStatTextToHtml(rawText) {
            if (rawText == null || typeof rawText !== 'string') return '';
            const elementTagNames = ['fire', 'cryo', 'shock', 'corrosive', 'radiation'];
            let str = String(rawText)
                .replace(/\[newline\]/gi, FMT_PH.nl)
                .replace(/\[glyph\]([^\]]*)\[\/glyph\]/gi, function (_m, inner) {
                    return '[' + String(inner || '').replace(/_/g, ' ') + ']';
                });
            str = str
                .replace(/\[frtn_icon\]/gi, FMT_PH.frtn)
                .replace(/\[wfll_icon\]/gi, FMT_PH.wfll)
                .replace(/\[(tediore_icon|ted_icon)\]/gi, FMT_PH.tediore)
                .replace(/\[roll_icon\]/gi, FMT_PH.roll);
            let nwGuard = 0;
            while (/\[nowrap\]/i.test(str) && nwGuard < 200) {
                nwGuard++;
                const next = str.replace(/\[nowrap\]([\s\S]*?)\[\/nowrap\]/i, FMT_PH.nowrapS + '$1' + FMT_PH.nowrapE);
                if (next === str) break;
                str = next;
            }
            str = str
                .replace(/\[flavor\]/gi, '')
                .replace(/\[\/flavor\]/gi, '')
                .replace(/\[(fire|cryo|shock|corrosive|radiation|kinetic)_icon\]/gi, '')
                .replace(/\[elemental_icon\]/gi, '');
            const esc = (s) => String(s).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;');
            const tagRe =
                /\[(rarity_legendary|rarity_pearl|secondary|primary|rd_color|fire|cryo|shock|corrosive|radiation|kinetic)\]([\s\S]*?)\[\/\1\]/gi;
            let result = '';
            let lastIndex = 0;
            let match;
            tagRe.lastIndex = 0;
            while ((match = tagRe.exec(str)) !== null) {
                result += esc(str.slice(lastIndex, match.index));
                const tagLower = match[1].toLowerCase();
                const color = elementTagNames.includes(tagLower) ? getDamageTypeColor(tagLower) : (UI_STAT_TAG_COLORS[match[1]] || 'inherit');
                result += '<span style="color:' + color + '">' + esc(match[2]) + '</span>';
                lastIndex = match.index + match[0].length;
            }
            result += esc(str.slice(lastIndex));
            result = result.split(FMT_PH.nl).join('<br/>');
            const clover =
                '<span class="legit-fmt-icon legit-fmt-icon--frtn" title="Fortune" aria-label="Fortune" style="display:inline-block;color:#43a047;font-size:1.05em;line-height:1;vertical-align:-0.12em;margin:0 1px;">🍀</span>';
            const windfall =
                '<span class="legit-fmt-icon legit-fmt-icon--wfll" title="Windfall" aria-label="Windfall" style="display:inline-block;color:#ff8a65;font-size:1em;line-height:1;vertical-align:-0.1em;margin:0 1px;">🎲</span>';
            const rollIc =
                '<span class="legit-fmt-icon legit-fmt-icon--roll" title="Roll" aria-hidden="true" style="display:inline-block;margin:0 1px;">🎲</span>';
            result = result
                .split(FMT_PH.frtn).join(clover)
                .split(FMT_PH.wfll).join(windfall)
                .split(FMT_PH.tediore).join('Tediore')
                .split(FMT_PH.roll).join(rollIc)
                .split(FMT_PH.nowrapS).join('<span style="white-space:nowrap">')
                .split(FMT_PH.nowrapE).join('</span>');
            return result;
        }

        /** Build Description, Effect (Primary ui_stat), and Red text lines HTML for part card. */
        function buildUiStatDescriptionRedtextHTML(uiStatInfo) {
            if (!uiStatInfo || (!uiStatInfo.descriptionTexts?.length && !uiStatInfo.redtexts?.length && !uiStatInfo.primaryTexts?.length)) return '';
            const parts = [];
            if (uiStatInfo.descriptionTexts && uiStatInfo.descriptionTexts.length > 0) {
                const text = uiStatInfo.descriptionTexts[0];
                parts.push(`<div style="font-size: var(--legit-chip-stat, 9px); margin-top: 1px; color: var(--text-secondary);"><span style="color: var(--text-secondary);">Description:</span> <span style="font-weight: 500;">${formatUiStatTextToHtml(text)}</span></div>`);
            }
            if (uiStatInfo.primaryTexts && uiStatInfo.primaryTexts.length > 0) {
                for (let pi = 0; pi < uiStatInfo.primaryTexts.length; pi++) {
                    const text = uiStatInfo.primaryTexts[pi];
                    parts.push(`<div style="font-size: var(--legit-chip-stat, 9px); margin-top: 1px; color: var(--text-secondary);"><span style="color: var(--text-secondary);">Effect:</span> <span style="font-weight: 500;">${formatUiStatTextToHtml(text)}</span></div>`);
                }
            }
            if (uiStatInfo.redtexts && uiStatInfo.redtexts.length > 0) {
                const text = uiStatInfo.redtexts[0];
                parts.push(`<div style="font-size: var(--legit-chip-stat, 9px); margin-top: 1px;"><span style="color: var(--text-secondary);">Red text:</span> <span style="font-style: italic; color: #ef5350;">${formatUiStatTextToHtml(text)}</span></div>`);
            }
            return parts.join('');
        }

        /** True if the displayed value is a concrete number/text, not an unresolved ref like (Table→Row). In simple mode we hide undetermined values. */
        function isDisplayValueDetermined(val) {
            if (val == null) return false;
            const s = String(val).trim();
            if (s === '') return false;
            return !/→|->/.test(s);
        }

        /** Build full part stats line: damage type, ammo, trigger, fire rate, charge/discharge, resolved damage/accuracy/spread, model. When simpleMode is true, only includes stats with determined values. */
        function buildPartStatsLineHTML(damageInfo, data, simpleMode) {
            if (!damageInfo) return '';
            const parts = [];
            if (damageInfo.damageTypes?.length) {
                const damageSpans = damageInfo.damageTypes.map(dt =>
                    `<span style="color: ${getDamageTypeColor(dt)}; font-weight: 600;">${dt}</span>`
                ).join(', ');
                parts.push(`<span style="color: var(--text-secondary);">Damage: </span>${damageSpans}`);
            }
            if (damageInfo.shotAmmoCosts?.length) {
                parts.push(`<span style="color: var(--text-secondary);">Ammo/shot: </span><span style="color: #c8b040; font-weight: 600;">${damageInfo.shotAmmoCosts[0]}</span>`);
            }
            if (damageInfo.triggerEveryShots?.length) {
                const n = damageInfo.triggerEveryShots[0];
                parts.push(`<span style="color: var(--text-secondary);">Every </span><span style="font-weight: 600;">${n}</span><span style="color: var(--text-secondary);"> shots</span>`);
            }
            if (damageInfo.fireRateConstants?.length) {
                parts.push(`<span style="color: var(--text-secondary);">Fire rate: </span><span style="font-weight: 600;">${damageInfo.fireRateConstants[0]}/s</span>`);
            }
            if (damageInfo.chargeTimes?.length) {
                parts.push(`<span style="color: var(--text-secondary);">Charge: </span><span style="font-weight: 600;">${damageInfo.chargeTimes[0]}s</span>`);
            }
            if (damageInfo.dischargeTimes?.length) {
                parts.push(`<span style="color: var(--text-secondary);">Discharge: </span><span style="font-weight: 600;">${damageInfo.dischargeTimes[0]}s</span>`);
            }
            const damageResolved = resolveRefsForDisplay(damageInfo.damageRefs, data);
            const damageVal = damageResolved.length ? damageResolved[0] : (damageInfo.damageConstants?.length ? String(damageInfo.damageConstants[0]) : (resolveDirectDtRefsForDisplay(damageInfo.damageDirectDtRefs, data)[0]));
            if (damageVal != null && (!simpleMode || isDisplayValueDetermined(damageVal))) parts.push(`<span style="color: var(--text-secondary);">Damage: </span><span style="font-weight: 600;">${damageVal}</span>`);
            const accuracyResolved = resolveRefsForDisplay(damageInfo.accuracyRefs, data);
            const accuracyVal = accuracyResolved.length ? accuracyResolved[0] : (damageInfo.accuracyConstants?.length ? String(damageInfo.accuracyConstants[0]) : (resolveDirectDtRefsForDisplay(damageInfo.accuracyDirectDtRefs, data)[0]));
            if (accuracyVal != null && (!simpleMode || isDisplayValueDetermined(accuracyVal))) parts.push(`<span style="color: var(--text-secondary);">Accuracy: </span><span style="font-weight: 600;">${accuracyVal}</span>`);
            const spreadResolved = resolveRefsForDisplay(damageInfo.spreadRefs, data);
            const spreadVal = spreadResolved.length ? spreadResolved[0] : (damageInfo.spreadConstants?.length ? String(damageInfo.spreadConstants[0]) : (resolveDirectDtRefsForDisplay(damageInfo.spreadDirectDtRefs, data)[0]));
            if (spreadVal != null && (!simpleMode || isDisplayValueDetermined(spreadVal))) parts.push(`<span style="color: var(--text-secondary);">Spread: </span><span style="font-weight: 600;">${spreadVal}</span>`);
            if (damageInfo.gestaltNames?.length) {
                parts.push(`<span style="color: var(--text-secondary);">Model: </span><span style="font-weight: 600;">${damageInfo.gestaltNames[0]}</span>`);
            }
            if (parts.length === 0) return '';
            const line1 = parts.slice(0, 6).join(sep);
            const line2 = parts.length > 6 ? parts.slice(6).join(sep) : '';
            const fs = 'var(--legit-chip-stat, 9px)';
            const html = line2 ? `<div style="font-size: ${fs}; margin-top: 2px;">${line1}</div><div style="font-size: ${fs}; margin-top: 1px;">${line2}</div>` : `<div style="font-size: ${fs}; margin-top: 2px;">${line1}</div>`;
            return html;
        }

        function decodeAndValidateSerial() {
            if (typeof window.syncLegitBuilderCtxToActiveMainTab === 'function') {
                window.syncLegitBuilderCtxToActiveMainTab();
            }
            const serialInputEl = lbSerialInputEl();
            const serialInput = serialInputEl ? serialInputEl.value.trim() : '';
            const resultDiv = lbEl('serialValidationResult');
            
            if (!serialInput) {
                showError('Please paste a serial string');
                if (resultDiv) resultDiv.style.display = 'none';
                if (window.__lbCtx && window.__lbCtx.relaxedPicker) {
                    clearModdedDecodedSummaryAndSkinUi();
                }
                return;
            }
            
            // Check if it's Base85 and needs deserialization first (but only if not already deserialized)
            // Skip this check if we're being called from deserializeFromBase85 to avoid infinite loop
            if (isBase85Format(serialInput) && !window.skipBase85Check) {
                // It's Base85 - must deserialize first
                window.skipBase85Check = false;
                deserializeFromBase85();
                return;
            }
            window.skipBase85Check = false;
            
            // If we get here and it still looks like Base85 (starts with @ but failed detection),
            // it might be a malformed Base85 string - show helpful error
            if (serialInput.startsWith('@') && serialInput.length > 10) {
                showError('Input appears to be Base85 format but deserialization may have failed. Please try clicking "Deserialize Base85" button first.');
                if (resultDiv) {
                    resultDiv.style.display = 'block';
                    resultDiv.innerHTML = '<div style="color: #ffaa44; padding: 10px; background: #3a2a1a; border-radius: 4px;">Base85 detected but not deserialized. Please use the "Deserialize Base85" button first.</div>';
                }
                ensureMiModdedDecodeAccordionCollapsedForModdedResult();
                return;
            }
            
            if (!data || !data.rootDefinitions) {
                showError('No data loaded. Please load JSON files first.');
                if (resultDiv) resultDiv.style.display = 'none';
                return;
            }

            if (textMightReferencePack4ClassMod(serialInput)) {
                void ensurePack4NexusLoaded();
            }

            const spacingNorm = normalizeDeserializedSerialInterPartSpacing(serialInput);
            if (spacingNorm !== serialInput && serialInputEl) {
                serialInputEl.value = spacingNorm;
            }
            let serialForValidate = spacingNorm;
            if (window.__lbCtx && window.__lbCtx.relaxedPicker) {
                const normPairs = normalizeConsecutiveRootPairTokensInModdedHumanLine(spacingNorm);
                if (normPairs !== spacingNorm && serialInputEl) {
                    serialInputEl.value = normPairs;
                }
                serialForValidate = normPairs;
            }

            // Shared validator path: keep single and bulk behavior in sync.
            try {
                const validation = validateSerialCore(serialForValidate, 1, {
                    includePartDetails: true,
                    moddedRelaxedValidation: true
                });
                window.__legitBackpackContext = window.__legitBackpackContext || {};
                window.__legitBackpackContext.lastDeserialized = serialForValidate;
                if (!window.__legitBackpackContext.pendingFromB85) {
                    window.__legitBackpackContext.lastBase85 = null;
                } else {
                    window.__legitBackpackContext.pendingFromB85 = false;
                }
                if (typeof window.updateLegitValidatorBackpackButton === 'function') {
                    window.updateLegitValidatorBackpackButton();
                }
                window.__legitLastSingleValidation = validation;
                if (window.__lbCtx && window.__lbCtx.relaxedPicker) {
                    const flagsPb = parseModdedFirmwareBuybackFlagsFromDeserialized(serialForValidate);
                    const fwPb = document.getElementById('mi_firmwareLock');
                    const bbPb = document.getElementById('mi_buyback');
                    if (fwPb) {
                        fwPb.checked = flagsPb.firmwareLock;
                    }
                    if (bbPb) {
                        bbPb.checked = flagsPb.buyback;
                    }
                    syncModdedLevelAndSeedFromDecode(serialForValidate, validation);
                }
                applyMiSkinUIFromDeserializedTail(serialForValidate);
                resultDiv.innerHTML = renderSerialValidationDetailsHtml(validation, {
                    data: data,
                    translationOnly: !!(window.__lbCtx && window.__lbCtx.relaxedPicker),
                    suppressSinglePushButton: !!(window.__lbCtx && window.__lbCtx.relaxedPicker)
                });
                resultDiv.style.display = 'block';
                if (window.__lbCtx && window.__lbCtx.relaxedPicker) {
                    applyModdedParseValidationToBuilder(validation);
                }
                ensureMiModdedDecodeAccordionCollapsedForModdedResult();
                let humanForB85 =
                    validation.details && validation.details.inputSerial != null
                        ? String(validation.details.inputSerial).trim()
                        : serialForValidate;
                if (window.__lbCtx && window.__lbCtx.relaxedPicker) {
                    humanForB85 = normalizeConsecutiveRootPairTokensInModdedHumanLine(humanForB85);
                }
                if (window.__lbCtx && window.__lbCtx.relaxedPicker) {
                    const genEl = lbGeneratedHumanEl();
                    if (genEl) {
                        genEl.value = humanForB85;
                    }
                }
                maybeSerializeModdedHumanToBase85(humanForB85);
                return;
            } catch (sharedError) {
                console.warn('Shared validator path failed, falling back to legacy decode path:', sharedError);
            }
            
            try {
                // Parse serial string format: rootSerial, 0, 1, level| seed1, seed2|| {serial1} {serial2} ...|
                // Handle multiple | separators
                const parts = serialForValidate.split('|').filter(p => p.trim().length > 0);
                if (parts.length < 2) {
                    // Show the actual input value for debugging
                    const debugInfo = serialForValidate.length > 100 ? serialForValidate.substring(0, 100) + '...' : serialForValidate;
                    throw new Error(`Invalid serial format. Expected: rootSerial, 0, 1, level| seed|| {serials}|\nGot: ${debugInfo}`);
                }
                
                // Parse header: "rootSerial, 0, 1, level" or '"rootSerial", 0, 1, level'
                // Handle quoted root serials like "mal_sm"
                let headerStr = parts[0].trim();
                const header = [];
                let i = 0;
                
                // Parse header with support for quoted strings
                while (i < headerStr.length) {
                    if (headerStr[i] === '"') {
                        // Quoted string
                        let start = i;
                        let j = i + 1;
                        while (j < headerStr.length && headerStr[j] !== '"') {
                            j++;
                        }
                        if (j < headerStr.length) {
                            header.push(headerStr.substring(start + 1, j)); // Remove quotes
                            i = j + 1;
                        } else {
                            // Unmatched quote, treat as regular
                            const nextComma = headerStr.indexOf(',', i);
                            if (nextComma > 0) {
                                header.push(headerStr.substring(i, nextComma).trim());
                                i = nextComma + 1;
                            } else {
                                header.push(headerStr.substring(i).trim());
                                break;
                            }
                        }
                    } else {
                        // Regular value
                        const nextComma = headerStr.indexOf(',', i);
                        if (nextComma > 0) {
                            header.push(headerStr.substring(i, nextComma).trim());
                            i = nextComma + 1;
                        } else {
                            header.push(headerStr.substring(i).trim());
                            break;
                        }
                    }
                    // Skip whitespace and commas
                    while (i < headerStr.length && (headerStr[i] === ',' || /\s/.test(headerStr[i]))) {
                        i++;
                    }
                }
                
                if (header.length < 4) {
                    throw new Error('Invalid header format. Expected: rootSerial, 0, 1, level (rootSerial can be number or quoted string)');
                }
                
                // Parse root serial - can be number or quoted string (e.g., "mal_sm")
                let rootSerial = null;
                let rootSerialKey = null;
                const rootSerialStr = header[0];
                if (rootSerialStr.startsWith('"') && rootSerialStr.endsWith('"')) {
                    // Quoted string - extract key
                    rootSerialKey = rootSerialStr.slice(1, -1);
                } else {
                    // Try to parse as number
                    const parsed = parseInt(rootSerialStr);
                    if (!isNaN(parsed)) {
                        rootSerial = parsed;
                    } else {
                        // Treat as key if not a valid number
                        rootSerialKey = rootSerialStr;
                    }
                }
                
                const level = parseInt(header[3]);
                
                // Seed and serial list: optional flags (e.g. "9, 1", "10, 1") may appear between header and seed
                const { seedPart, serialListStr: rawSerialListStr } = splitSerialHeaderSeedAndList(parts);
                if (!rawSerialListStr) {
                    const debugInfo = serialForValidate.length > 100 ? serialForValidate.substring(0, 100) + '...' : serialForValidate;
                    throw new Error(`Invalid serial format. Expected: rootSerial, 0, 1, level| seed|| {serials}| (optional flags like 9, 1| allowed before seed).\nGot: ${debugInfo}`);
                }
                let seed = null;
                let seed2 = null;
                if (seedPart) {
                    const parsedSeed = parseNexusSeedSegment(seedPart);
                    seed = parsedSeed.seed;
                    seed2 = parsedSeed.seed2;
                }
                let serialListStr = stripCosmeticSelectionSegments(rawSerialListStr);
                
                // Extract all serial patterns: {serial}, {root:serial}, {root:[serial1 serial2 ...]}, or quoted strings like "MAL_SM.comp_05_legendary_firework"
                const serialMatches = parseSerialPatterns(serialListStr);
                
                const decodedLevel = isNaN(level) ? null : level;
                const decodedSeed = isNaN(seed) ? null : seed;
                const decodedSeed2 = seed2 !== null && !isNaN(seed2) ? seed2 : null;
                
                // Find root by serial (if numeric) or by key (if string)
                let rootDef = null;
                if (rootSerialKey) {
                    // Find by key (case-insensitive)
                    rootDef = data.rootDefinitions.find(r => {
                        const rKey = (r.key || '').toLowerCase();
                        return rKey === rootSerialKey.toLowerCase();
                    });
                } else if (rootSerial !== null) {
                    // Find by serial number
                    rootDef = data.rootDefinitions.find(r => {
                        const rSerial = r.serialIndex;
                        if (rSerial === null || rSerial === undefined) return false;
                        const normalizedRSerial = typeof rSerial === 'number' ? rSerial : parseInt(rSerial);
                        return normalizedRSerial === rootSerial;
                    });
                }
                
                const rootKey = rootDef ? rootDef.key : null;
                const rootName = rootDef ? (rootDef.itembasename || rootDef.key) : null;
                const decodedSerial = rootDef ? (rootDef.serialIndex || rootSerial) : rootSerial;
                
                // Parse and validate each part serial
                const validatedParts = [];
                const errors = [];
                
                serialMatches.forEach((match, idx) => {
                    // Check if this is a quoted string (e.g., "MAL_SM.comp_05_legendary_firework")
                    if (match.startsWith('"') && match.endsWith('"')) {
                        // Quoted string - lookup by key
                        const partKey = match.slice(1, -1); // Remove quotes
                        // Extract root key and part key from format like "MAL_SM.comp_05_legendary_firework"
                        const keyParts = partKey.split('.');
                        const partKeyOnly = keyParts.length > 1 ? keyParts.slice(1).join('.') : partKey;
                        
                        // Try to find the part by key in any root
                        let foundPart = null;
                        let foundInRoot = null;
                        let partType = normalizeDepTableName(null);
                        
                        if (data && data.rootDefinitions) {
                            for (const r of data.rootDefinitions) {
                                if (r.depEntries) {
                                    const partEntry = r.depEntries.find(dep => {
                                        const depKey = dep.key || dep.inv || '';
                                        return depKey.toLowerCase() === partKeyOnly.toLowerCase();
                                    });
                                    if (partEntry) {
                                        foundPart = partEntry;
                                        foundInRoot = r;
                                        partType = normalizeDepTableName(partEntry.depTableName);
                                        break;
                                    }
                                }
                            }
                        }
                        
                        validatedParts.push({
                            index: idx + 1,
                            serial: match,
                            serials: [partKey],
                            rootSerial: null,
                            isBaseComp: false,
                            isArray: false,
                            partKey: foundPart ? foundPart.key : partKeyOnly,
                            partType: partType,
                            found: !!foundPart,
                            foundInRoot: foundInRoot ? foundInRoot.key : null,
                            foundPart: foundPart,
                            rootMatches: true, // String-based parts always match root
                            isQuotedKeyPart: true,
                            rawPatternText: match,
                            displaySerial: computeValidatorPartDisplaySerial(
                                {
                                    foundPart,
                                    partKey: foundPart ? foundPart.key : partKeyOnly,
                                    partType,
                                    foundInRoot: foundInRoot ? foundInRoot.key : null,
                                    isBaseComp: false,
                                    rootSerial: null,
                                    serial: partKey,
                                },
                                match,
                                data
                            ),
                            isValid: !!foundPart,
                            validationIssue: foundPart ? null : `Part key "${partKeyOnly}" not found in loaded data`
                        });
                        return;
                    }
                    
                    // Handle brace patterns: {serial}, {root:serial}, {root:[serial1 serial2 ...]}
                    const serialStr = match.slice(1, -1); // Remove {}
                    let serials = [];
                    let rootSerialPart = null;
                    let isBaseCompPart = false;
                    let isArrayFormat = false;
                    
                    // Check if it's array format: {rootSerial:[serial1 serial2 ...]}
                    if (serialStr.includes('[') && serialStr.includes(']')) {
                        isArrayFormat = true;
                        const colonIndex = serialStr.indexOf(':');
                        if (colonIndex > 0) {
                            rootSerialPart = parseInt(serialStr.substring(0, colonIndex).trim());
                            isBaseCompPart = true;
                            const arrayPart = serialStr.substring(colonIndex + 1).trim();
                            const arrayContent = arrayPart.slice(1, -1); // Remove []
                            serials = arrayContent.split(/\s+/).map(s => parseInt(s.trim())).filter(n => !isNaN(n));
                        } else {
                            const arrayContent = serialStr.slice(1, -1); // Remove []
                            serials = arrayContent.split(/\s+/).map(s => parseInt(s.trim())).filter(n => !isNaN(n));
                        }
                    }
                    // Check if it's base comp format: {rootSerial:subSerial}
                    // If this format is used, it MUST be a valid rootSerial:subSerial combination
                    else if (serialStr.includes(':')) {
                        const [rs, ss] = serialStr.split(':').map(s => parseInt(s.trim()));
                        const potentialRootSerial = rs;
                        const potentialSubSerial = ss;
                        
                        // Verify this is actually a valid rootSerial:subSerial combination
                        // Find if a root with this serial exists
                        const targetRoot = data.rootDefinitions && data.rootDefinitions.find(r => {
                            const rSerial = r.serialIndex;
                            if (rSerial === null || rSerial === undefined) return false;
                            const normalizedRSerial = typeof rSerial === 'number' ? rSerial : parseInt(rSerial);
                            return normalizedRSerial === potentialRootSerial;
                        });
                        
                        if (!targetRoot) {
                            // Root doesn't exist - this is an invalid lookup
                            errors.push(`Part ${idx + 1}: Invalid base comp format {${potentialRootSerial}:${potentialSubSerial}} - root serial ${potentialRootSerial} does not exist`);
                            return; // Skip this part entirely
                        }
                        
                        // If root exists, check if it has a sub-item with the specified subserial
                        if (targetRoot.depEntries) {
                            const hasSubSerial = targetRoot.depEntries.some(dep => {
                                // Try multiple formats: serialIndex (extracted), serialindex.index (nested object), or direct value
                                let depSerial = dep.serialIndex;
                                if (depSerial === null || depSerial === undefined) {
                                    depSerial = dep.serialindex?.index;
                                }
                                if (depSerial === null || depSerial === undefined && dep.serialindex) {
                                    // Handle case where serialindex might be the value directly or in nested structure
                                    depSerial = typeof dep.serialindex === 'number' ? dep.serialindex : 
                                               (typeof dep.serialindex === 'object' && dep.serialindex !== null ? dep.serialindex.index : null);
                                }
                                if (depSerial === null || depSerial === undefined) return false;
                                const normalizedDepSerial = typeof depSerial === 'number' ? depSerial : parseInt(depSerial);
                                if (isNaN(normalizedDepSerial)) return false;
                                return normalizedDepSerial === potentialSubSerial;
                            });
                            
                            if (!hasSubSerial) {
                                // Root exists but doesn't have this subserial - invalid lookup
                                errors.push(`Part ${idx + 1}: Invalid base comp format {${potentialRootSerial}:${potentialSubSerial}} - root serial ${potentialRootSerial} does not have subserial ${potentialSubSerial}`);
                                return; // Skip this part entirely
                            }
                        } else {
                            // Root exists but has no depEntries - invalid lookup
                            errors.push(`Part ${idx + 1}: Invalid base comp format {${potentialRootSerial}:${potentialSubSerial}} - root serial ${potentialRootSerial} has no sub-items`);
                            return; // Skip this part entirely
                        }
                        
                        // Valid rootSerial:subSerial combination - treat as base comp part
                        rootSerialPart = potentialRootSerial;
                        serials = [potentialSubSerial];
                        isBaseCompPart = true;
                    } else {
                        // Regular format: {serial}
                        serials = [parseInt(serialStr)];
                    }
                    
                    // Process each serial in the array (or single serial)
                    serials.forEach((serial, serialIdx) => {
                        if (isNaN(serial)) {
                            errors.push(`Part ${idx + 1}${isArrayFormat ? `[${serialIdx}]` : ''}: Invalid serial number "${serialStr}"`);
                            return;
                        }
                    
                        // Try to find the part in the data
                        let foundPart = null;
                        let foundInRoot = null;
                        let partType = normalizeDepTableName(null);
                        
                        // Normalize serial for comparison (handle both string and number)
                        const normalizedSerial = typeof serial === 'number' ? serial : parseInt(serial);
                        
                        // If this is a base comp part (has rootSerialPart), first find that root
                        if (isBaseCompPart && rootSerialPart !== null) {
                            // Find the root with the specified root serial
                            const targetRoot = data.rootDefinitions.find(r => {
                                const rSerial = r.serialIndex;
                                if (rSerial === null || rSerial === undefined) return false;
                                const normalizedRSerial = typeof rSerial === 'number' ? rSerial : parseInt(rSerial);
                                return normalizedRSerial === rootSerialPart;
                            });
                            
                            if (targetRoot && targetRoot.depEntries) {
                                // Look for the part within this specific root
                                const partEntry = targetRoot.depEntries.find(dep => {
                                    // Try multiple formats: serialIndex (extracted), serialindex.index (nested object), or direct value
                                    let depSerial = dep.serialIndex;
                                    if (depSerial === null || depSerial === undefined) {
                                        depSerial = dep.serialindex?.index;
                                    }
                                    if (depSerial === null || depSerial === undefined && dep.serialindex) {
                                        // Handle case where serialindex might be the value directly or in nested structure
                                        depSerial = typeof dep.serialindex === 'number' ? dep.serialindex : 
                                                   (typeof dep.serialindex === 'object' && dep.serialindex !== null ? dep.serialindex.index : null);
                                    }
                                    if (depSerial === null || depSerial === undefined) return false;
                                    const normalizedDepSerial = typeof depSerial === 'number' ? depSerial : parseInt(depSerial);
                                    if (isNaN(normalizedDepSerial)) return false;
                                    return normalizedDepSerial === normalizedSerial;
                                });
                                
                                if (partEntry) {
                                    foundPart = partEntry;
                                    foundInRoot = targetRoot;
                                    partType = normalizeDepTableName(partEntry.depTableName);
                                }
                            }
                        } else {
                            // Regular part (single serial {#}) - search in the local root first, then generic root
                            // If we have a decoded root, search in that root first
                            if (rootDef && rootDef.depEntries) {
                                // Debug: Log all serials in root's depEntries when looking for serials 14 or 15
                                if (normalizedSerial === 14 || normalizedSerial === 15) {
                                    const allSerials = rootDef.depEntries.map(dep => {
                                        const depSerial = dep.serialIndex || dep.serialindex?.index;
                                        const normalized = depSerial !== null && depSerial !== undefined 
                                            ? (typeof depSerial === 'number' ? depSerial : parseInt(depSerial))
                                            : null;
                                        return { key: dep.key, serialIndex: depSerial, normalized: normalized, depTableName: dep.depTableName };
                                    }).filter(d => d.normalized !== null);
                                    console.log(`[Part Lookup] Looking for serial ${normalizedSerial} in root ${rootDef.key} (serial ${rootDef.serialIndex})`);
                                    console.log(`[Part Lookup] Available serials in root ${rootDef.key}:`, allSerials);
                                }
                                
                                const partEntry = rootDef.depEntries.find(dep => {
                                    // Try multiple possible serial index formats
                                    const depSerial = dep.serialIndex || 
                                                     dep.serialindex?.index || 
                                                     dep.serialindex?.index || 
                                                     (dep.serialindex && typeof dep.serialindex === 'object' ? dep.serialindex.index : dep.serialindex);
                                    if (depSerial === null || depSerial === undefined) return false;
                                    const normalizedDepSerial = typeof depSerial === 'number' ? depSerial : parseInt(depSerial);
                                    if (isNaN(normalizedDepSerial)) return false;
                                    return normalizedDepSerial === normalizedSerial;
                                });
                                
                                if (partEntry) {
                                    foundPart = partEntry;
                                    foundInRoot = rootDef;
                                    partType = normalizeDepTableName(partEntry.depTableName);
                                }
                            }
                            
                            // If not found in local root's depEntries, try subItemsByTable (but only for local root)
                            if (!foundPart && data.subItemsByTable && rootDef) {
                                // Debug: Log when searching subItemsByTable for serials 14 or 15
                                if (normalizedSerial === 14 || normalizedSerial === 15) {
                                    console.log(`[Part Lookup] Searching subItemsByTable for serial ${normalizedSerial} in root ${rootDef.key}`);
                                }
                                
                                for (const [tableName, tableData] of Object.entries(data.subItemsByTable)) {
                                    if (tableData && tableData.items && Array.isArray(tableData.items)) {
                                        const partEntry = tableData.items.find(item => {
                                            const itemSerial = item.serialIndex || item.serialindex?.index;
                                            if (itemSerial === null || itemSerial === undefined) return false;
                                            const normalizedItemSerial = typeof itemSerial === 'number' ? itemSerial : parseInt(itemSerial);
                                            if (normalizedItemSerial !== normalizedSerial) return false;
                                            
                                            // Only match if this part belongs to the local root
                                            const itemRootKey = item.rootKey || item.root;
                                            const matches = itemRootKey === rootDef.key;
                                            
                                            if (matches && (normalizedSerial === 14 || normalizedSerial === 15)) {
                                                console.log(`[Part Lookup] Found serial ${normalizedSerial} in subItemsByTable[${tableName}] for root ${rootDef.key}`);
                                            }
                                            
                                            return matches;
                                        });
                                        
                                        if (partEntry) {
                                            foundPart = partEntry;
                                            foundInRoot = rootDef;
                                            partType = normalizeDepTableName(partEntry.depTableName || tableName);
                                            break;
                                        }
                                    }
                                }
                            }
                            
                            // Also check dependency tables (but only for local root)
                            if (!foundPart && data.dependencyTablesObj && rootDef) {
                                // Debug: Log when searching dependencyTablesObj for serials 14 or 15
                                if (normalizedSerial === 14 || normalizedSerial === 15) {
                                    console.log(`[Part Lookup] Searching dependencyTablesObj for serial ${normalizedSerial} in root ${rootDef.key}`);
                                }
                                
                                for (const [tableName, table] of Object.entries(data.dependencyTablesObj)) {
                                    if (table && table.items && Array.isArray(table.items)) {
                                        const partEntry = table.items.find(item => {
                                            const itemSerial = item.serialIndex || item.serialindex?.index;
                                            if (itemSerial === null || itemSerial === undefined) return false;
                                            const normalizedItemSerial = typeof itemSerial === 'number' ? itemSerial : parseInt(itemSerial);
                                            if (normalizedItemSerial !== normalizedSerial) return false;
                                            
                                            // Only match if this part belongs to the local root
                                            const itemRootKey = item.rootKey || item.root;
                                            const matches = itemRootKey === rootDef.key;
                                            
                                            if (matches && (normalizedSerial === 14 || normalizedSerial === 15)) {
                                                console.log(`[Part Lookup] Found serial ${normalizedSerial} in dependencyTablesObj[${tableName}] for root ${rootDef.key}`);
                                            }
                                            
                                            return matches;
                                        });
                                        
                                        if (partEntry) {
                                            foundPart = partEntry;
                                            foundInRoot = rootDef;
                                            partType = tableName || 'Unknown';
                                            break;
                                        }
                                    }
                                }
                            }
                            
                            // For simple format parts {#}, ONLY search in the local root
                            // Parts do NOT traverse types or manufacturers - only basecomp format does
                            // If not found in local root, do a more thorough search of the local root only
                            if (!foundPart && rootDef && rootDef.depEntries) {
                                // Enhanced search in local root with better serial extraction
                                const partEntry = rootDef.depEntries.find(dep => {
                                    // Try all possible serial index formats
                                    let depSerial = dep.serialIndex;
                                    if (depSerial === null || depSerial === undefined) {
                                        depSerial = dep.serialindex?.index;
                                    }
                                    if (depSerial === null || depSerial === undefined && dep.serialindex) {
                                        // Handle case where serialindex might be the value directly
                                        depSerial = typeof dep.serialindex === 'number' ? dep.serialindex : 
                                                   (typeof dep.serialindex === 'object' ? dep.serialindex.index : null);
                                    }
                                    if (depSerial === null || depSerial === undefined) return false;
                                    const normalizedDepSerial = typeof depSerial === 'number' ? depSerial : parseInt(depSerial);
                                    if (isNaN(normalizedDepSerial)) return false;
                                    
                                    if (normalizedDepSerial === normalizedSerial) {
                                        // Debug log when found
                                        if (normalizedSerial === 14 || normalizedSerial === 15) {
                                            console.log(`[Part Lookup] Found serial ${normalizedSerial} in local root ${rootDef.key} with format:`, {
                                                serialIndex: dep.serialIndex,
                                                serialindex: dep.serialindex,
                                                normalized: normalizedDepSerial,
                                                key: dep.key,
                                                depTableName: dep.depTableName
                                            });
                                        }
                                        return true;
                                    }
                                    return false;
                                });
                                
                                if (partEntry) {
                                    foundPart = partEntry;
                                    foundInRoot = rootDef;
                                    partType = normalizeDepTableName(partEntry.depTableName);
                                }
                            }
                            
                            // For shield roots only: if part not found in local root, try type-specific / generic shield roots (dual base composition)
                            // So parts from energy_shield (248) or armor_shield (237) are resolved when validating a shield item (root 246)
                            if (!foundPart && rootDef && data && data.rootDefinitions) {
                                const rootKeyLower = (rootDef.key || '').toLowerCase();
                                const isShieldRoot = rootKeyLower === 'shield' || rootKeyLower.includes('energy_shield') || rootKeyLower.includes('armor_shield');
                                if (isShieldRoot) {
                                    const shieldRoots = data.rootDefinitions.filter(r => {
                                        const rKey = (r.key || '').toLowerCase();
                                        return rKey === 'shield' || rKey === 'energy_shield' || rKey === 'armor_shield';
                                    });
                                    for (const otherRoot of shieldRoots) {
                                        if (otherRoot === rootDef || !otherRoot.depEntries) continue;
                                        const partEntry = otherRoot.depEntries.find(dep => {
                                            const depSerial = dep.serialIndex || dep.serialindex?.index
                                                || (dep.serialindex && typeof dep.serialindex === 'object' ? dep.serialindex.index : dep.serialindex);
                                            if (depSerial === null || depSerial === undefined) return false;
                                            const normalizedDepSerial = typeof depSerial === 'number' ? depSerial : parseInt(depSerial);
                                            return !isNaN(normalizedDepSerial) && normalizedDepSerial === normalizedSerial;
                                        });
                                        if (partEntry) {
                                            foundPart = partEntry;
                                            foundInRoot = otherRoot;
                                            partType = normalizeDepTableName(partEntry.depTableName);
                                            break;
                                        }
                                    }
                                }
                            }
                            
                            // DO NOT search other roots for simple format parts (except shield type-specific above)
                            // Simple format {#} parts MUST be in the local root only
                            // Only base comp format {rootSerial:subSerial} can reference other roots
                        }
                        
                        const partDisplayIndex = isArrayFormat ? `${idx + 1}[${serialIdx}]` : `${idx + 1}`;
                        const partDisplaySerial = isArrayFormat 
                            ? (isBaseCompPart ? `{${rootSerialPart}:[${serials.join(' ')}]}` : `{[${serials.join(' ')}]}`)
                            : (isBaseCompPart ? `{${rootSerialPart}:${serial}}` : `{${serial}}`);
                        
                        // For base comp parts, rootMatches is true if:
                        // 1. We found the part in the specified root, AND
                        // 2. The rootSerialPart matches either the weapon's root or an allowed generic root
                        // For regular parts, rootMatches is always true (no root serial to match)
                        let rootMatches = true;
                        if (isBaseCompPart && rootSerialPart !== null) {
                            // First check: did we find the part in the specified root?
                            const foundInSpecifiedRoot = foundInRoot && (foundInRoot.serialIndex === rootSerialPart || 
                                (typeof foundInRoot.serialIndex === 'number' ? foundInRoot.serialIndex : parseInt(foundInRoot.serialIndex)) === rootSerialPart);
                            
                            if (foundInSpecifiedRoot) {
                                // Second check: is this root serial allowed for this weapon?
                                // We'll validate this later when we have rootDef context, but for now mark as true if found
                                // The actual validation will happen in the validation section below
                                rootMatches = true; // Will be re-validated later
                            } else {
                                rootMatches = false;
                            }
                        }
                        
                        validatedParts.push({
                            index: partDisplayIndex,
                            serial: serial,
                            serials: serials,
                            rootSerial: rootSerialPart,
                            isBaseComp: isBaseCompPart,
                            isArray: isArrayFormat,
                            partKey: foundPart ? foundPart.key : null,
                            partType: partType,
                            found: !!foundPart,
                            foundInRoot: foundInRoot ? foundInRoot.key : null,
                            foundPart: foundPart,
                            rootMatches: rootMatches,
                            displaySerial: computeValidatorPartDisplaySerial(
                                {
                                    foundPart,
                                    partKey: foundPart ? foundPart.key : null,
                                    partType,
                                    foundInRoot: foundInRoot ? foundInRoot.key : null,
                                    isBaseComp: isBaseCompPart,
                                    rootSerial: rootSerialPart,
                                    serial: serial,
                                },
                                partDisplaySerial,
                                data
                            ),
                            validationIssue: null
                        });
                    });
                });
                
                // Validate parts using comp rules (same logic as part selection)
                let isLegit = true;
                let validationIssues = [];
                let slotConstraintDiagnostics = [];
                
                if (rootDef) {
                    // Get comp rules for this root (find selected comp from parts)
                    const selectedCompPart = validatedParts.find(p => p.partType === 'inv_comp' && p.found);
                    let selectedComp = null;
                    if (selectedCompPart && selectedCompPart.partKey) {
                        selectedComp = selectedCompPart.partKey;
                    }
                    
                    // Check for duplicate parts (same part key in same part type)
                    const partKeyCounts = new Map(); // partType -> Map(partKey -> count)
                    validatedParts.forEach(part => {
                        if (part.found && part.partKey && part.partType) {
                            if (!partKeyCounts.has(part.partType)) {
                                partKeyCounts.set(part.partType, new Map());
                            }
                            const typeMap = partKeyCounts.get(part.partType);
                            const currentCount = typeMap.get(part.partKey) || 0;
                            typeMap.set(part.partKey, currentCount + 1);
                        }
                    });
                    
                    // Mark duplicates as invalid
                    partKeyCounts.forEach((typeMap, partType) => {
                        typeMap.forEach((count, partKey) => {
                            if (count > 1) {
                                // Find all parts with this duplicate
                                const duplicateParts = validatedParts.filter(p => 
                                    p.partType === partType && p.partKey === partKey && p.found
                                );
                                duplicateParts.forEach(part => {
                                    part.isValid = false;
                                    part.validationIssue = `Duplicate part: "${partKey}" appears ${count} times in ${partType} (duplicates not allowed)`;
                                    isLegit = false;
                                });
                                validationIssues.push(`Duplicate part "${partKey}" in ${partType}: found ${count} times (only 1 allowed)`);
                            }
                        });
                    });
                    
                    // Check for firmware max count violation (firmware always has max 1)
                    const firmwareParts = validatedParts.filter(p => p.partType === 'firmware' && p.found);
                    if (firmwareParts.length > 1) {
                        // All firmware parts are invalid when more than 1 exists
                        firmwareParts.forEach(part => {
                            part.isValid = false;
                            part.validationIssue = `Multiple firmware parts selected: found ${firmwareParts.length} firmware parts (maximum 1 allowed)`;
                            isLegit = false;
                        });
                        validationIssues.push(`Multiple firmware parts: found ${firmwareParts.length} firmware parts (maximum 1 allowed)`);
                    }
                    
                    // Check for multiple comps (inv_comp is Min1 Max1 - exactly one per item)
                    const compParts = validatedParts.filter(p => p.partType === 'inv_comp' && p.found);
                    if (compParts.length > 1) {
                        compParts.forEach(part => {
                            part.isValid = false;
                            part.validationIssue = `Multiple components (inv_comp): found ${compParts.length} (exactly 1 allowed - comp is Min1 Max1)`;
                            isLegit = false;
                        });
                        validationIssues.push(`Multiple components (inv_comp): found ${compParts.length} (exactly 1 allowed - comp is Min1 Max1)`);
                    }
                    
                    // Check if all parts are found and from correct roots first
                    const allPartsFound = validatedParts.every(p => p.found && p.rootMatches);
                    
                    // If no comp, item is MODDED - all items require a component
                    if (!selectedComp) {
                        isLegit = false;
                        validationIssues.push('Missing component (inv_comp) - all items require a component');
                    } else {
                        // Check if parts come from the correct composition/root
                        // Parts should come from the selected comp's root or allowed generic roots
                        const compRoot = rootDef;
                        const compRootSerial = compRoot.serialIndex ? (typeof compRoot.serialIndex === 'number' ? compRoot.serialIndex : parseInt(compRoot.serialIndex)) : null;
                        
                        // Find allowed generic roots (like 'weapon', 'enhancement', etc.)
                        const compRootKeyLower = (compRoot.key || '').toLowerCase();
                        const isManufacturerHeavyWeaponRoot = compRootKeyLower.endsWith('_hw');
                        const allowsGenericParts = isManufacturerHeavyWeaponRoot || ['enhancement', 'classmod', 'repair_kit', 'grenade_gadget', 'heavy_weapon_gadget', 'turret_gadget', 'terminal_gadget'].some(gr => 
                            compRootKeyLower.includes(gr) || compRootKeyLower.startsWith(gr + '_')
                        );
                        
                        // Check if this is a shield - shields have dual base compositions
                        const isShield = compRootKeyLower.includes('shield') || compRootKeyLower.includes('energy_shield') || compRootKeyLower.includes('armor_shield');
                        
                        // Get allowed generic root serials if applicable
                        const allowedGenericRootSerials = new Set();
                        if (allowsGenericParts && data && data.rootDefinitions) {
                            const genericRootKeys = ['weapon', 'shield', 'enhancement', 'classmod', 'grenade', 'grenade_gadget', 'heavy_weapon', 'heavy_weapon_gadget', 'turret_gadget', 'terminal_gadget', 'repair_kit'];
                            data.rootDefinitions.forEach(root => {
                                const rootKeyLower = (root.key || '').toLowerCase();
                                // Check if this root matches any generic root pattern (exact match OR starts with pattern_)
                                if (genericRootKeys.some(gr => {
                                    const grLower = gr.toLowerCase();
                                    return rootKeyLower === grLower || rootKeyLower.startsWith(grLower + '_');
                                })) {
                                    const rootSerial = root.serialIndex ? (typeof root.serialIndex === 'number' ? root.serialIndex : parseInt(root.serialIndex)) : null;
                                    if (rootSerial !== null) {
                                        allowedGenericRootSerials.add(rootSerial);
                                    }
                                }
                            });
                        }
                        
                        // For class mods and enhancements, allow base-comp roots (substat/skill/perk roots 234, 237, 243, 244, 245, 246, 247, 248)
                        const isClassModOrEnhancement = compRootKeyLower.includes('classmod') || compRootKeyLower.includes('enhancement');
                        if (isClassModOrEnhancement) {
                            [234, 237, 243, 244, 245, 246, 247, 248].forEach(serial => allowedGenericRootSerials.add(serial));
                        }
                        
                        // Check if this is a heavy weapon - manufacturer roots are * _hw (key does not contain "heavy_weapon")
                        const isHeavyWeapon = isManufacturerHeavyWeaponRoot || compRootKeyLower.includes('heavy_weapon') || compRootKeyLower.includes('heavy_weapon_gadget');
                        
                        // For shields, also allow energy_shield, armor_shield, and generic shield roots
                        // Shields have dual base compositions: one in energy/armor shield and one in shield
                        if (isShield && data && data.rootDefinitions) {
                            data.rootDefinitions.forEach(root => {
                                const rootKeyLower = (root.key || '').toLowerCase();
                                // Allow generic shield root (e.g., "shield" with serial 246)
                                // Allow energy_shield roots
                                // Allow armor_shield roots
                                if (rootKeyLower === 'shield' || 
                                    rootKeyLower.includes('energy_shield') || 
                                    rootKeyLower.includes('armor_shield')) {
                                    const rootSerial = root.serialIndex ? (typeof root.serialIndex === 'number' ? root.serialIndex : parseInt(root.serialIndex)) : null;
                                    if (rootSerial !== null) {
                                        allowedGenericRootSerials.add(rootSerial);
                                    }
                                }
                            });
                        }
                        
                        // For heavy weapons, also allow generic heavy_weapon root (e.g., serial 244)
                        // Heavy weapons have dual base compositions similar to shields
                        if (isHeavyWeapon && data && data.rootDefinitions) {
                            data.rootDefinitions.forEach(root => {
                                const rootKeyLower = (root.key || '').toLowerCase();
                                const rootSerial = root.serialIndex ? (typeof root.serialIndex === 'number' ? root.serialIndex : parseInt(root.serialIndex)) : null;
                                
                                // Allow generic heavy_weapon root (e.g., "heavy_weapon" with serial 244)
                                // Match both "heavy_weapon" and "heavy_weapon_gadget" roots
                                // Also check if this is specifically the root with serial 244 (firmware root)
                                if (rootKeyLower === 'heavy_weapon' || 
                                    rootKeyLower === 'heavy_weapon_gadget' ||
                                    rootKeyLower.includes('heavy_weapon') ||
                                    rootSerial === 244) { // Explicitly allow root serial 244 for heavy weapons
                                    if (rootSerial !== null) {
                                        allowedGenericRootSerials.add(rootSerial);
                                    }
                                }
                            });
                        }
                        
                        // Debug: Log allowed generic root serials for grenade gadgets
                        if (compRootKeyLower.includes('grenade_gadget')) {
                            console.log(`[Validation] Root ${compRoot.key} (serial ${compRootSerial}) - allowsGenericParts: ${allowsGenericParts}`);
                            console.log(`[Validation] Allowed generic root serials:`, Array.from(allowedGenericRootSerials));
                        }
                        
                        validatedParts.forEach(part => {
                            // First, check base comp parts - validate root serial matches allowed roots
                            if (part.found && part.isBaseComp && part.rootSerial !== null && part.partType !== 'inv_comp') {
                                const partRootSerial = part.rootSerial;
                                const isCompRootSerial = compRootSerial !== null && partRootSerial === compRootSerial;
                                const isAllowedGenericRoot = allowedGenericRootSerials.has(partRootSerial);
                                
                                // Special case: For heavy weapon firmware parts, allow root serial 244
                                let isAllowedForHeavyWeaponFirmware = false;
                                if (isHeavyWeapon && part.partType === 'firmware' && partRootSerial === 244) {
                                    isAllowedForHeavyWeaponFirmware = true;
                                }
                                
                                // Weapon-shared parts can legitimately come from generic weapon root.
                                // This includes elemental and pearlescent slots used by manufacturer variants.
                                const isWeaponSharedRootPart = ['body_ele', 'secondary_ele', 'pearl_elem', 'pearl_stat'].includes(part.partType);
                                let isAllowedForWeaponSharedRootPart = false;
                                if (isWeaponSharedRootPart && data && data.rootDefinitions) {
                                    // Check if root serial 1 (or other generic weapon root) exists
                                    const genericWeaponRoot = data.rootDefinitions.find(r => {
                                        const rSerial = r.serialIndex ? (typeof r.serialIndex === 'number' ? r.serialIndex : parseInt(r.serialIndex)) : null;
                                        const rKey = (r.key || '').toLowerCase();
                                        return rSerial === partRootSerial && (rKey === 'weapon' || rKey.startsWith('weapon_'));
                                    });
                                    isAllowedForWeaponSharedRootPart = genericWeaponRoot !== undefined;
                                }
                                
                                if (!isCompRootSerial && !isAllowedGenericRoot && !isAllowedForWeaponSharedRootPart && !isAllowedForHeavyWeaponFirmware) {
                                    // Root serial doesn't match weapon's root or allowed generic root
                                    part.rootMatches = false;
                                    part.isValid = false;
                                    const expectedSerials = [compRootSerial, ...Array.from(allowedGenericRootSerials)].filter(s => s !== null).join(' or ');
                                    part.validationIssue = `Base comp part uses root serial ${partRootSerial} which doesn't match weapon root serial (${compRootSerial}) or allowed generic root serials (${Array.from(allowedGenericRootSerials).join(', ') || 'none'})`;
                                    isLegit = false;
                                    validationIssues.push(`Part "${part.partKey}" uses root serial ${partRootSerial} (wrong root - expected ${expectedSerials || compRootSerial})`);
                                } else if (isAllowedForWeaponSharedRootPart || isAllowedForHeavyWeaponFirmware) {
                                    // Shared weapon-root part from generic root is valid, or heavy weapon firmware part is valid
                                    part.rootMatches = true;
                                }
                            }
                            
                            // Then check regular parts - validate root key matches
                            if (part.found && !part.isBaseComp && part.partType !== 'inv_comp' && part.foundInRoot) {
                                // Check if part comes from the correct root
                                // Part should come from compRoot or an allowed generic root
                                const partRootKey = part.foundInRoot;
                                const compRootKey = compRoot.key;
                                
                                // Allow parts from:
                                // 1. The comp's own root
                                // 2. Generic roots (weapon, shield, enhancement, etc.) if applicable
                                const isFromCompRoot = partRootKey === compRootKey;
                                const isFromGenericRoot = ['weapon', 'shield', 'enhancement', 'classmod', 'grenade', 'grenade_gadget', 'heavy_weapon', 'heavy_weapon_gadget', 'repair_kit'].some(gr => 
                                    partRootKey === gr || partRootKey.startsWith(gr + '_')
                                );
                                
                                // Check if the comp root allows generic root parts
                                const compRootKeyLower = (compRootKey || '').toLowerCase();
                                const allowsGenericParts = compRootKeyLower.endsWith('_hw') || ['enhancement', 'classmod', 'repair_kit', 'grenade_gadget', 'heavy_weapon_gadget'].some(gr => 
                                    compRootKeyLower.includes(gr) || compRootKeyLower.startsWith(gr + '_')
                                );
                                
                                // For shields, also allow energy_shield and armor_shield roots (dual base compositions)
                                const isShield = compRootKeyLower.includes('shield') || compRootKeyLower.includes('energy_shield') || compRootKeyLower.includes('armor_shield');
                                let isFromShieldRoot = false;
                                if (isShield) {
                                    const partRootKeyLower = partRootKey.toLowerCase();
                                    isFromShieldRoot = partRootKeyLower.includes('energy_shield') || partRootKeyLower.includes('armor_shield') || partRootKeyLower === 'shield';
                                }
                                
                                // For heavy weapons, also allow generic heavy_weapon root (dual base compositions)
                                const isHeavyWeapon = compRootKeyLower.endsWith('_hw') || compRootKeyLower.includes('heavy_weapon') || compRootKeyLower.includes('heavy_weapon_gadget');
                                let isFromHeavyWeaponRoot = false;
                                if (isHeavyWeapon) {
                                    const partRootKeyLower = partRootKey.toLowerCase();
                                    isFromHeavyWeaponRoot = partRootKeyLower === 'heavy_weapon' || partRootKeyLower === 'heavy_weapon_gadget';
                                }
                                
                                if (!isFromCompRoot && (!isFromGenericRoot || !allowsGenericParts) && !isFromShieldRoot && !isFromHeavyWeaponRoot) {
                                    // Part comes from wrong root
                                    if (!part.validationIssue) {
                                        part.isValid = false;
                                        part.validationIssue = `Part from wrong root: found in "${partRootKey}" but expected from "${compRootKey}" or allowed generic root`;
                                        isLegit = false;
                                        validationIssues.push(`Part "${part.partKey}" comes from wrong root "${partRootKey}" (expected "${compRootKey}")`);
                                    }
                                }
                            }
                        });
                        
                        // We have a comp - do full validation without mutating builder selections.
                        const validationSelectionKeys = validatedParts
                            .filter(part => part.found && part.partKey)
                            .map(part => part.partKey);
                        
                        // Get comp rules for validation (build tag pool from validated parts only)
                        const compRules = getSelectedCompRules(rootDef, validationSelectionKeys);

                        if (compRules) {
                        slotConstraintDiagnostics = buildSlotConstraintDiagnostics(rootDef, compRules);
                        // Validate each part - build incremental tag pool as we go
                        const formatTags = (tags) => {
                            if (!tags || !Array.isArray(tags)) return [];
                            return tags.map(tag => {
                                if (typeof tag === 'object' && tag !== null) {
                                    const values = Object.values(tag);
                                    return values[0] || Object.keys(tag)[0] || '';
                                }
                                return tag;
                            }).filter(Boolean);
                        };
                        
                                // Build tag pool incrementally - start empty, add tags only as parts are processed
                                // This matches the game's incremental validation behavior
                                const validationTagPool = new Set();
                                
                                // Sort parts by selection order from Nexus (root parttypes; for variant roots use generic root e.g. classmod from inv0)
                        const partSelectionOrderArray = getPartSelectionOrderFromRoot(rootDef, data?.rootDefinitions);
                        const selectionOrder = {};
                        partSelectionOrderArray.forEach((partType, idx) => {
                            const normalized = normalizeDepTableName(partType);
                            if (normalized && normalized !== 'unknown') selectionOrder[normalized] = idx + 1;
                        });
                        
                        // Sort by selection order, preserving original order for parts with same selection order
                        // Create a map of original indices to preserve relative order
                        const originalIndexMap = new Map();
                        validatedParts.forEach((part, idx) => {
                            originalIndexMap.set(part, idx);
                        });
                        
                        const sortedParts = [...validatedParts].sort((a, b) => {
                            const orderA = selectionOrder[normalizeDepTableName(a.partType)] || 999; // Unknown types go last
                            const orderB = selectionOrder[normalizeDepTableName(b.partType)] || 999;
                            if (orderA !== orderB) {
                                return orderA - orderB;
                            }
                            // If same selection order, preserve original order
                            const idxA = originalIndexMap.get(a) || 0;
                            const idxB = originalIndexMap.get(b) || 0;
                            return idxA - idxB;
                        });
                        
                        const classModTierOrder = detectClassModPassiveTierOrderIssues(sortedParts, rootDef, formatTags);
                        
                        // Validate each part in order, building tag pool as we go
                        sortedParts.forEach((part, partIdx) => {
                            if (!part.found || !part.rootMatches) {
                                part.isValid = false;
                                part.validationIssue = part.found ? 'Root serial mismatch' : 'Part not found';
                                isLegit = false;
                                return;
                            }
                            
                                    // Comp is always valid (it establishes the base tag pool)
                                    // Add component's tags from the component part itself (not from compRules.tagPool)
                                    if (part.partType === 'inv_comp') {
                                        part.isValid = true;
                                        // Add component part's own tags to pool (basetags and addtags)
                                        if (part.foundPart) {
                                            // Add basetags first (base tags from component)
                                            const compBaseTags = formatTags(part.foundPart.basetags || []).map(t => t.toLowerCase());
                                            compBaseTags.forEach(tag => validationTagPool.add(tag));
                                            
                                            // Then add addtags (additional tags from component)
                                            const compAddTags = formatTags(part.foundPart.addtags || []).map(t => t.toLowerCase());
                                            compAddTags.forEach(tag => validationTagPool.add(tag));
                                        }
                                        return;
                                    }
                            
                            // Validate part against current tag pool
                            const partAddTags = new Set(formatTags(part.foundPart.addtags || []).map(t => t.toLowerCase()));
                            const partDepTags = new Set(formatTags(part.foundPart.dependencytags || []).map(t => t.toLowerCase()));
                            const partExclTags = new Set(formatTags(part.foundPart.exclusiontags || []).map(t => t.toLowerCase()));
                            
                            const conflictingExclusionTagsLegacy = Array.from(partExclTags).filter(tag =>
                                validationTagPool.has(tag)
                            );
                            const hasExclusionConflict = conflictingExclusionTagsLegacy.length > 0;
                            
                            // IMPORTANT: Add part's tags to pool BEFORE checking dependencies
                            // This matches game behavior where parts add tags when selected, regardless of dependencies
                            // For example: body_ele adds mal_ele, which then satisfies secondary_ele's dependency
                            // Only skip adding tags if there's an exclusion conflict (part can't be selected)
                            if (!hasExclusionConflict) {
                                // Add part's tags to pool (tags are added when part is selected, even if it has unmet deps)
                                partAddTags.forEach(tag => validationTagPool.add(tag));
                            }
                            
                            // Check dependencies (all must be in pool) - check AFTER adding tags
                            const missingDeps = Array.from(partDepTags).filter(tag => !validationTagPool.has(tag));
                            
                            // Detect class-mod passive tier ordering violations (tier_1 -> tier_5 only).
                            const tierOrderIssue = classModTierOrder.issueByPart.get(part);
                            const effectiveTierIssueLegacy = tierOrderIssue && !hasExclusionConflict;
                            
                            // Check if part is in allowed parts
                            const partItemForAllowed = { ...(part.foundPart || {}), key: part.partKey || part.foundPart?.key };
                            const isAllowed = isPartAllowedForTable(partItemForAllowed, compRules, part.partType);
                            
                            // Part is valid if no conflicts, deps met, allowed, and not out-of-order in classmod tier chain.
                            const isValid = !hasExclusionConflict && missingDeps.length === 0 && isAllowed && !effectiveTierIssueLegacy;
                            part.isValid = isValid;
                            
                            if (!isValid) {
                                isLegit = false;
                                
                                // Build issue message
                                let issues = [];
                                if (hasExclusionConflict) {
                                    const conflictMsg = `Excludes tags in pool: ${conflictingExclusionTagsLegacy.join(', ')}`;
                                    issues.push(conflictMsg);
                                    validationIssues.push(`Part "${part.partKey}" (${part.partType}): ${conflictMsg}`);
                                }
                                if (missingDeps.length > 0) {
                                    let depsMsg = `Missing dependencies: ${missingDeps.join(', ')}`;
                                    if (effectiveTierIssueLegacy) {
                                        const orderExpected = tierOrderIssue.expectedTag;
                                        const orderObserved = tierOrderIssue.observedTag;
                                        if (missingDeps.includes(orderExpected)) {
                                            depsMsg = `Dependency blocked by tier order: expected ${orderExpected} before ${orderObserved}`;
                                        }
                                    }
                                    issues.push(depsMsg);
                                    validationIssues.push(`Part "${part.partKey}" (${part.partType}): ${depsMsg}`);
                                }
                                if (!isAllowed) {
                                    const notAllowedMsg = `Not in allowed parts for ${part.partType}`;
                                    issues.push(notAllowedMsg);
                                    validationIssues.push(`Part "${part.partKey}" (${part.partType}): ${notAllowedMsg}`);
                                }
                                if (effectiveTierIssueLegacy) {
                                    issues.push(tierOrderIssue.message);
                                    validationIssues.push(`Part "${part.partKey}" (${part.partType}): ${tierOrderIssue.message}`);
                                }
                                
                                part.validationIssue = issues.join('; ') || 'Validation failed';
                            }
                        });

                        // Enforce comp-level tag max rules (parttagselectionrules.max).
                        const legacyTagMaxViolations = enforcePartTagMaxRules(validatedParts, compRules, formatTags);
                        legacyTagMaxViolations.forEach(violation => {
                            isLegit = false;
                            const issue = `Too many parts with tag "${violation.tag}": found ${violation.found}, maximum ${violation.max} allowed`;
                            validationIssues.push(issue);
                            (violation.overflowParts || []).forEach(part => {
                                part.isValid = false;
                                part.validationIssue = part.validationIssue ? `${part.validationIssue}; ${issue}` : issue;
                            });
                        });
                        
                        // Check for missing required parts (slots with min > 0)
                        // CRITICAL: Only validate slots that actually exist in this weapon's root
                        // Don't validate manufacturer-specific or weapon-type-specific slots from other roots
                        if (compRules.partCounts) {
                            // Count parts by type in the validated serial FIRST (before using it)
                            // Normalize part types (e.g. "Element" -> "element") so they match normalized slot names from partCounts
                            const partsByType = new Map();
                            validatedParts.forEach(part => {
                                if (part.partType !== 'inv_comp' && part.found && part.isValid !== false) {
                                    const normalizedPartType = normalizeDepTableName(part.partType);
                                    const count = partsByType.get(normalizedPartType) || 0;
                                    partsByType.set(normalizedPartType, count + 1);
                                }
                            });
                            
                            // Get valid slots for this root - ONLY from depEntries (actual dependency tables)
                            // CRITICAL: Do NOT use parttypes - it may include slots from generic weapon root
                            // Only use depEntries which are the actual dependency tables for THIS specific root
                            const validSlots = new Set();
                            
                            // Add slots ONLY from depEntries (actual dependency tables for this root)
                            // This ensures we only validate slots that actually exist in this weapon, not from generic roots
                            if (rootDef.depEntries) {
                                rootDef.depEntries.forEach(dep => {
                                    if (dep.depTableName) {
                                        const normalizedSlot = normalizeDepTableName(dep.depTableName);
                                        if (normalizedSlot !== 'unknown' && normalizedSlot !== 'inv_comp') {
                                            validSlots.add(normalizedSlot);
                                        }
                                    }
                                });
                            }
                            // Include body_ele/secondary_ele when in comp rules (e.g. from base weapon root) so we validate them for weapons
                            const rootKeyLowerForSlots = (rootDef.key || '').toLowerCase();
                            const isWeaponRoot = rootKeyLowerForSlots.includes('weapon') || ['vla', 'dah', 'jak', 'mal', 'hyperion', 'atl', 'ted', 'ord', 'bor', 'grenade', 'heavy', 'turret', 'terminal'].some(m => rootKeyLowerForSlots.includes(m));
                            if (isWeaponRoot && compRules.partCounts) {
                                ['body_ele', 'secondary_ele'].forEach(slot => {
                                    if (compRules.partCounts.has(slot)) validSlots.add(slot);
                                });
                            }
                            // Also include all slots from compRules.partCounts (e.g. stat_group1 for enhancements - defined in base composition but not in depEntries)
                            if (compRules.partCounts) {
                                compRules.partCounts.forEach((countInfo, slotName) => {
                                    const normalizedSlot = normalizeDepTableName(slotName);
                                    if (normalizedSlot !== 'unknown' && normalizedSlot !== 'inv_comp') {
                                        validSlots.add(normalizedSlot);
                                    }
                                });
                            }
                            
                            // Debug: log valid slots for troubleshooting
                            console.log(`[Validation] Valid slots for ${rootDef.key}:`, Array.from(validSlots).sort());
                            console.log(`[Validation] PartCounts to check:`, Array.from(compRules.partCounts.keys()).sort());
                            console.log(`[Validation] Parts in serial:`, Array.from(partsByType.keys()).sort());
                            
                            // Check each required slot - ONLY if it's valid for this root
                            compRules.partCounts.forEach((countInfo, slotName) => {
                                // CRITICAL: Only validate slots that exist in this weapon's root
                                // Skip manufacturer-specific slots (e.g., hyperion_secondary_acc for Maliwan)
                                // Skip weapon-type-specific slots (e.g., magazine_ted_thrown for non-Tediore)
                                
                                // Check for manufacturer-specific or weapon-type-specific slot names first
                                const slotNameLower = slotName.toLowerCase();
                                const normalizedSlotName = normalizeDepTableName(slotName);
                                const rootKeyLower = (rootDef.key || '').toLowerCase();
                                
                                // Determine if slot is manufacturer-specific
                                const isManufacturerSpecific = slotNameLower.includes('hyperion') || 
                                                               slotNameLower.includes('tediore') || 
                                                               slotNameLower.includes('atlas') ||
                                                               slotNameLower.includes('jakobs') ||
                                                               slotNameLower.includes('maliwan') ||
                                                               slotNameLower.includes('vladof') ||
                                                               slotNameLower.includes('dahl') ||
                                                               slotNameLower.includes('order') ||
                                                               slotNameLower.includes('borg');
                                
                                // If slot is manufacturer-specific, check if it matches this weapon's manufacturer
                                if (isManufacturerSpecific) {
                                    const slotManufacturer = slotNameLower.includes('hyperion') ? 'hyperion' :
                                                             slotNameLower.includes('tediore') ? 'tediore' :
                                                             slotNameLower.includes('atlas') ? 'atlas' :
                                                             slotNameLower.includes('jakobs') ? 'jakobs' :
                                                             slotNameLower.includes('maliwan') ? 'maliwan' :
                                                             slotNameLower.includes('vladof') ? 'vladof' :
                                                             slotNameLower.includes('dahl') ? 'dahl' :
                                                             slotNameLower.includes('order') ? 'order' :
                                                             slotNameLower.includes('borg') ? 'borg' : null;
                                    
                                    // Check if this weapon's root matches the slot's manufacturer
                                    const weaponManufacturer = rootKeyLower.includes('hyperion') ? 'hyperion' :
                                                               rootKeyLower.includes('ted') ? 'tediore' :
                                                               rootKeyLower.includes('atl') ? 'atlas' :
                                                               rootKeyLower.includes('jak') ? 'jakobs' :
                                                               rootKeyLower.includes('mal') ? 'maliwan' :
                                                               rootKeyLower.includes('vla') ? 'vladof' :
                                                               rootKeyLower.includes('dah') ? 'dahl' :
                                                               rootKeyLower.includes('ord') ? 'order' :
                                                               rootKeyLower.includes('bor') ? 'borg' : null;
                                    
                                    // If slot is for a different manufacturer, skip it
                                    if (slotManufacturer && weaponManufacturer && slotManufacturer !== weaponManufacturer) {
                                        return; // Skip - wrong manufacturer
                                    }
                                }
                                
                                const isWeaponTypeSpecific = slotNameLower.includes('ted_thrown') ||
                                                              slotNameLower.includes('secondary_ammo');
                                
                                const hasPartsInSerial = partsByType.has(normalizedSlotName) && partsByType.get(normalizedSlotName) > 0;
                                
                                // ALWAYS skip weapon-type-specific slots if they don't have parts in serial
                                // This check happens BEFORE validSlots to catch slots that might be incorrectly in validSlots
                                // (e.g., magazine_ted_thrown only applies to Tediore weapons, secondary_ammo is weapon-type-specific)
                                if (isWeaponTypeSpecific && !hasPartsInSerial) {
                                    return; // Skip - doesn't apply to this weapon type
                                }
                                
                                // If we have valid slots defined, only check those
                                if (validSlots.size > 0) {
                                    // We have valid slots - only validate if slot exists in root
                                    if (!validSlots.has(normalizedSlotName)) {
                                        return; // Skip this slot - it doesn't apply to this weapon
                                    }
                                } else {
                                    // No valid slots found - use conservative filtering
                                    // Skip manufacturer/weapon-type specific slots unless they have parts
                                    if ((isManufacturerSpecific || isWeaponTypeSpecific) && !hasPartsInSerial) {
                                        return; // Skip - doesn't apply to this weapon
                                    }
                                    
                                    // If no parts in serial and we can't verify it exists in root, skip to avoid false positives
                                    // This is conservative: only validate slots that we know exist (have parts)
                                    if (!hasPartsInSerial) {
                                        return; // Skip - can't confirm this slot applies to this weapon
                                    }
                                }
                                
                                const qualifiedDepsForSlotLegacy = getDependencyQualifiedDepEntriesForSlot(
                                    rootDef,
                                    compRules,
                                    slotName,
                                    validationTagPool
                                );
                                const qualifiedFillableDepsLegacy = filterDepsFillableUnderPartTagMaxRules(
                                    qualifiedDepsForSlotLegacy,
                                    compRules,
                                    validatedParts,
                                    formatTags
                                );
                                const dependencyQualifiedOptions = qualifiedFillableDepsLegacy.length;
                                const requiredMin = resolveRequiredMinForSlot(
                                    countInfo,
                                    rootDef,
                                    compRules,
                                    slotName,
                                    { availableOptions: dependencyQualifiedOptions }
                                );
                                const currentCount = partsByType.get(normalizedSlotName) || 0;
                                const debugCountResolution = typeof window !== 'undefined' && !!window.debugCountResolutionSources;
                                if (debugCountResolution && (normalizedSlotName.endsWith('_acc') || normalizedSlotName === 'barrel_licensed')) {
                                    console.log('[Count Resolution Legacy]', {
                                        slotName,
                                        normalizedSlotName,
                                        countSource: countInfo?.countSource || 'unknown',
                                        configuredMin: countInfo?.min,
                                        configuredMax: countInfo?.max,
                                        dependencyQualifiedOptions,
                                        requiredMin,
                                        currentCount
                                    });
                                }
                                // Enforce any resolved max shown in the rule model (including inferred 0-1).
                                const maxCount =
                                    countInfo && countInfo.max !== undefined && countInfo.max !== null
                                        ? countInfo.max
                                        : null;
                                // When body_acc_ele is in the tag pool, body_ele selection is required (no non-elemental wombo combos).
                                // Comps that disable body_ele (min=0 max=0) must not be required to select an element.
                                let effectiveRequiredMin = requiredMin;
                                if (normalizedSlotName === 'body_ele' && validationTagPool.has('body_acc_ele')) {
                                    const slotDisabled = (countInfo && countInfo.min === 0 && countInfo.max === 0);
                                    if (!slotDisabled) effectiveRequiredMin = Math.max(requiredMin, 1);
                                }
                                // Check for missing required parts (min count)
                                if (effectiveRequiredMin > 0 && currentCount < effectiveRequiredMin) {
                                    isLegit = false;
                                    const missing = effectiveRequiredMin - currentCount;
                                    const partWord = missing === 1 ? 'part' : 'parts';
                                    validationIssues.push(`Missing required ${slotName}: need ${effectiveRequiredMin}, found ${currentCount} (missing ${missing} ${partWord})`);
                                }
                                
                                // Check for exceeding maximum parts (max count)
                                // Skip when max is 0 and slot has 0 options for this root (slot not applicable, e.g. stat_group1 on enhancement)
                                if (maxCount !== null && currentCount > maxCount && !(maxCount === 0 && dependencyQualifiedOptions === 0)) {
                                    isLegit = false;
                                    const excess = currentCount - maxCount;
                                    const partWord = excess === 1 ? 'part' : 'parts';
                                    
                                    // Mark all parts of this type as invalid when exceeding max
                                    validatedParts.forEach(part => {
                                        if (part.partType === normalizedSlotName && part.found) {
                                            const maxViolationMsg = `Exceeds maximum count for ${slotName}: found ${currentCount}, maximum ${maxCount} allowed`;
                                            if (!part.validationIssue) {
                                                part.validationIssue = maxViolationMsg;
                                            } else if (!part.validationIssue.includes('exceeds maximum')) {
                                                part.validationIssue = `${part.validationIssue}; ${maxViolationMsg}`;
                                            }
                                            part.isValid = false;
                                        }
                                    });
                                    
                                    validationIssues.push(`Too many ${slotName} parts: found ${currentCount}, maximum ${maxCount} allowed (${excess} ${partWord} too many)`);
                                }
                            });
                        }
                        } else {
                            // Comp found but couldn't get rules
                            isLegit = false;
                            validationIssues.push('Could not get component rules - validation failed');
                        }
                    }
                } else {
                    isLegit = false;
                    validationIssues.push('Root not found - cannot validate');
                }
                
                // Serials/parts that show "NEW!" badge in the editor
                const newSerialIds = new Set([
                    '1:51','1:52','1:53','1:54','1:55','1:56','1:57','1:58','1:59','1:60',
                    '13:90','13:89','22:90','21:79','22:89','287:12','298:11',
                    '6:81','17:81','6:80','17:80','6:79','17:79',
                    '11:81','14:78','13:82','18:99','23:22','6:77','4:84','12:78','25:81','21:80','9:99','17:82','22:91','3:82','7:55',
                    '18:100','9:100','11:82','287:11','13:83','4:85','3:83','12:79','25:82','7:56','298:12','17:83','6:78','14:79','22:92','21:81','23:56'
                ]);
                const newPartKeys = new Set([
                    'pearl_damage','pearl_reload','pearl_firerate','pearl_handling','pearl_normal','pearl_shock','pearl_radiation','pearl_corrosive','pearl_cryo','pearl_fire',
                    'part_underbarrel_04_atlas_mercredi','part_underbarrel_04_atlas_ball_mercredi','part_mag_torgue_normal_mercury','part_mag_torgue_normal_songbird','part_mag_torgue_sticky_mercury',
                    'body_flare','part_body_hopscotch',
                    'part_foregrip_01_handcannon','part_foregrip_01_fleabag','part_foregrip_02_handcannon','part_foregrip_02_fleabag','part_foregrip_03_handcannon','part_foregrip_03_fleabag',
                    'part_barrel_01_eigenburst','part_barrel_01_laserdisc','part_barrel_01_mercredi','part_barrel_01_bubbles','part_barrel_01_tankbuster','part_barrel_02_handcannon','part_barrel_02_roulette','part_barrel_02_arctic','part_barrel_02_conflux','part_barrel_02_songbird','part_barrel_doeshot','part_barrel_fleabag','part_barrel_mercury','part_barrel_shalashaska','part_unique_barrel_02_demo',
                    'comp_05_legendary_bubbles','comp_05_legendary_doeshot','comp_05_legendary_eigenburst','comp_05_legendary_hopscotch','comp_05_legendary_mercredi','comp_05_legendary_roulette','comp_05_legendary_shalashaska','comp_05_legendary_arctic','comp_05_legendary_conflux','comp_05_legendary_demo','comp_05_legendary_flare','comp_05_legendary_fleabag','comp_05_legendary_handcannon','comp_05_legendary_laserdisc','comp_05_legendary_mercury','comp_05_legendary_songbird','comp_05_legendary_tankbuster'
                ].map(k => k.toLowerCase()));
                
                // Build result HTML
                const legitimacyColor = isLegit ? '#4CAF50' : '#ff4444';
                const legitimacyIcon = isLegit ? '✓' : '✗';
                const legitimacyLabel = isLegit ? 'LEGIT' : 'MODDED';
                const slotConstraintDiagnosticsHtml = renderSlotConstraintDiagnosticsHtml(slotConstraintDiagnostics);
                const namePartOrderLegacy = ['inv_comp', 'barrel', 'body', 'body_acc', 'body_ele', 'magazine', 'scope', 'grip', 'foregrip', 'secondary_ele'];
                const namePartsLegacy = [];
                namePartOrderLegacy.forEach(tableName => {
                    const part = validatedParts.find(p => (p.partType || '').toLowerCase() === tableName.toLowerCase() && p.found);
                    if (!part) return;
                    const d = (part.displaySerial || '').trim();
                    const name = (!d || /^\{\d+(:\d+)?\}$/.test(d)) ? getDisplayNameFromPartKey(part.partKey) : d;
                    if (!name) return;
                    namePartsLegacy.push({ type: tableName, name });
                });
                const nameRoundupHtmlLegacy = namePartsLegacy.length > 0
                    ? `<div style="margin-top: 6px; padding: 6px 8px; background: rgba(0,0,0,0.2); border-radius: 4px;"><strong>Name:</strong> ${namePartsLegacy.map(p => `<span style="color: var(--text-primary);">${p.name}</span>`).join(' <span style="color: var(--text-muted);">/</span> ')}</div>`
                    : '';
                const elementTypesLegacy = [];
                validatedParts.forEach(p => {
                    if (!p.found || !p.foundPart) return;
                    getElementalDamageTypesForValidationPart(p).forEach(function (elem) {
                        if (elem && !elementTypesLegacy.includes(elem)) elementTypesLegacy.push(elem);
                    });
                });
                elementTypesLegacy.sort(
                    (a, b) => VALIDATION_ELEMENT_ORDER.indexOf(a) - VALIDATION_ELEMENT_ORDER.indexOf(b)
                );
                const elementsRoundupHtmlLegacy = elementTypesLegacy.length > 0
                    ? `<div style="margin-top: 4px;"><strong>Elements:</strong> ${elementTypesLegacy.map(e => `<span style="color: ${getDamageTypeColor(e)}; font-weight: 600;">${e.charAt(0).toUpperCase() + e.slice(1)}</span>`).join(', ')}</div>`
                    : '';

                let html = `
                    <div style="padding: 15px; background: #1a1a1a; border-radius: 6px; border-left: 3px solid ${legitimacyColor};">
                        <h4 style="color: ${legitimacyColor}; margin-bottom: 10px;">
                            ${legitimacyIcon} ${legitimacyLabel} - Decoded Serial
                        </h4>
                        <div style="font-size: 12px; color: #aaa; margin-bottom: 15px;">
                            ${decodedSerial ? `<div style="margin-bottom: 4px;"><strong>Decoded Serial:</strong> <code style="background: rgba(0,0,0,0.3); padding: 2px 6px; border-radius: 3px; font-size: 11px; word-break: break-all;">${decodedSerial}</code></div>` : ''}
                            <div><strong>Root Serial:</strong> ${decodedSerial} ${rootDef ? `(${rootKey}${rootName ? ' - ' + rootName : ''})` : '<span style="color: #ff4444;">(Not Found)</span>'}</div>
                            <div><strong>Level:</strong> ${decodedLevel !== null ? decodedLevel : 'N/A'}</div>
                            <div><strong>Seed:</strong> ${decodedSeed !== null ? decodedSeed : 'N/A'}${decodedSeed2 !== null ? `, ${decodedSeed2}` : ''}</div>
                            <div><strong>Parts:</strong> ${validatedParts.length}</div>
                            ${!isLegit ? `<div style="color: #ff4444; margin-top: 5px;"><strong>Status:</strong> This item appears to be MODDED - contains invalid part combinations</div>` : `<div style="color: #4CAF50; margin-top: 5px;"><strong>Status:</strong> This item appears to be LEGIT - all parts are valid</div>`}
                            ${nameRoundupHtmlLegacy}
                            ${elementsRoundupHtmlLegacy}
                        </div>
                        
                        <h5 style="color: #FFA500; margin-bottom: 8px; font-size: 13px;">Part Validation:</h5>
                        <div style="max-height: 400px; overflow-y: auto;">
                `;
                
                    validatedParts.forEach(part => {
                    // Check if part is valid (exists, root matches, and passes tag validation)
                    const partFoundAndRootMatch = part.found && (part.isBaseComp ? part.rootMatches : true);
                    const partIsValid = part.isValid !== undefined ? part.isValid : partFoundAndRootMatch;
                    const statusColor = partIsValid ? '#4CAF50' : '#ff4444';
                    const statusIcon = partIsValid ? '✓' : '✗';
                    const partIdStr = part.rootSerial != null ? `${part.rootSerial}:${part.serial}` : `${part.serial}`;
                    const isNewPart = newSerialIds.has(partIdStr) || (part.partKey && newPartKeys.has(String(part.partKey).toLowerCase()));
                    const newBadge = isNewPart ? '<span style="color: #FF8C42; font-weight: 700;">NEW!</span> ' : '';
                    const addTags = part.foundPart ? formatTags(part.foundPart.addtags || []) : [];
                    const depTags = part.foundPart ? formatTags(part.foundPart.dependencytags || []) : [];
                    const exclTags = part.foundPart ? formatTags(part.foundPart.exclusiontags || []) : [];
                    const baseTags = part.foundPart ? formatTags(part.foundPart.basetags || []) : [];
                    const addTagsText = addTags.length > 0 ? addTags.join(', ') : 'None';
                    const baseTagsText = baseTags.length > 0 ? baseTags.join(', ') : 'None';
                    const depTagsText = depTags.length > 0 ? depTags.join(', ') : 'None';
                    const exclTagsText = exclTags.length > 0 ? exclTags.join(', ') : 'None';
                    const elementalTypesLegacyRow =
                        part.found && part.foundPart ? getElementalDamageTypesForValidationPart(part) : [];
                    const damageLineHtml = buildValidatorDamageLineHtml(elementalTypesLegacyRow);
                    let descRedHtml = '';
                    if (part.found && part.foundPart && data) {
                        const aspects = part.foundPart.aspects || part.foundPart.item?.aspects || [];
                        const uiStatInfo = getUiStatDescriptionAndRedtext(aspects, data);
                        if (uiStatInfo.descriptionTexts?.length || uiStatInfo.redtexts?.length || uiStatInfo.primaryTexts?.length) {
                            descRedHtml = (uiStatInfo.descriptionTexts?.length ? `<div style="margin-top: 2px;"><span style="color: var(--text-secondary);">Description:</span> <span style="font-weight: 500;">${formatUiStatTextToHtml(uiStatInfo.descriptionTexts[0])}</span></div>` : '') +
                                (uiStatInfo.primaryTexts?.length ? uiStatInfo.primaryTexts.map((t) => `<div style="margin-top: 2px;"><span style="color: var(--text-secondary);">Effect:</span> <span style="font-weight: 500;">${formatUiStatTextToHtml(t)}</span></div>`).join('') : '') +
                                (uiStatInfo.redtexts?.length ? `<div style="margin-top: 2px;"><span style="color: var(--text-secondary);">Red text:</span> <span style="font-style: italic; color: #ef5350;">${formatUiStatTextToHtml(uiStatInfo.redtexts[0])}</span></div>` : '');
                        }
                    }
                    const partDebugDescValLegacy = part.found && part.foundPart ? getPartDebugDisplayDescription(part.foundPart) : '';
                    const partDebugLineHtmlLegacy = partDebugDescValLegacy
                        ? `<div style="margin-top: 4px;"><span style="color: ${LEGIT_DEBUG_LABEL_FG}; font-weight: 700;">Debug:</span> <span style="color: ${LEGIT_DEBUG_DESC_FG}; font-weight: 600; text-shadow: 0 1px 2px rgba(0,0,0,0.75);">${escapeLegitHtml(partDebugDescValLegacy)}</span></div>`
                        : '';
                    const passiveFmtLegacy = part.found && part.foundPart && data ? getPassivePointsTooltipFormatText(part, data) : '';
                    const passiveFmtHtmlLegacy =
                        passiveFmtLegacy
                            ? `<div style="margin-top: 6px;"><strong style="color: #9e9e9e;">Format text:</strong> <span style="display:block;margin-top:3px;font-weight:500;color:#d0d0d0;line-height:1.45;word-break:break-word;">${formatUiStatTextToHtml(passiveFmtLegacy)}</span></div>`
                            : '';
                    html += `
                        <div style="margin-bottom: 8px; padding: 8px; background: #2a2a2a; border-radius: 4px; border-left: 2px solid ${statusColor};">
                            <div style="font-size: 11px; color: ${statusColor}; font-weight: bold; margin-bottom: 4px;">
                                ${statusIcon} Part ${part.index}: ${newBadge}${part.displaySerial || formatValidatorPartTokenFallback(part)}
                            </div>
                            ${part.found ? `
                                <div style="font-size: 10px; color: #aaa;">
                                    <div><strong>Part Key:</strong> ${part.partKey || 'N/A'}</div>
                                    <div><strong>Type:</strong> ${part.partType}</div>
                                    <div><strong>Found In:</strong> ${part.foundInRoot || 'N/A'}</div>
                                    ${partDebugLineHtmlLegacy}
                                    ${damageLineHtml}
                                    <div><strong>Addtags:</strong> ${addTags.length > 0 ? `<span style="color: var(--accent-secondary);">+</span> <span style="color: var(--accent-secondary);">${addTagsText}</span>` : addTagsText}</div>
                                    <div><strong>Basetags:</strong> ${baseTags.length > 0 ? `<span style="color: var(--accent-primary);">◆</span> <span style="color: var(--accent-primary);">${baseTagsText}</span>` : baseTagsText}</div>
                                    <div><strong>Dependencytags:</strong> ${depTags.length > 0 ? `<span style="color: var(--accent-warning);">→</span> <span style="color: var(--accent-warning);">${depTagsText}</span>` : depTagsText}</div>
                                    <div><strong>Exclusiontags:</strong> ${exclTags.length > 0 ? `<span style="color: var(--accent-danger);">✗</span> <span style="color: var(--accent-danger);">${exclTagsText}</span>` : exclTagsText}</div>
                                    ${passiveFmtHtmlLegacy}
                                    ${descRedHtml}
                                    ${part.isBaseComp && !part.rootMatches ? '<div style="color: #ffaa44;"><strong>Warning:</strong> Root serial mismatch</div>' : ''}
                                    ${part.validationIssue ? `<div style="color: #ff4444; margin-top: 4px;"><strong>Validation Issue:</strong> ${part.validationIssue}</div>` : ''}
                                </div>
                            ` : `
                                <div style="font-size: 10px; color: #ff4444;">
                                    <strong>Not Found:</strong> Serial ${part.serial} not found in loaded data
                                </div>
                            `}
                        </div>
                    `;
                });
                
                html += `
                        </div>
                        ${slotConstraintDiagnosticsHtml}
                        ${errors.length > 0 ? `
                            <div style="margin-top: 10px; padding: 8px; background: #3a1a1a; border-radius: 4px; border-left: 2px solid #ff4444;">
                                <div style="font-size: 11px; color: #ff4444; font-weight: bold; margin-bottom: 4px;">Errors:</div>
                                ${errors.map(e => `<div style="font-size: 10px; color: #ffaa44;">• ${e}</div>`).join('')}
                            </div>
                        ` : ''}
                        ${validationIssues.length > 0 ? `
                            <div style="margin-top: 10px; padding: 8px; background: #3a1a1a; border-radius: 4px; border-left: 2px solid #ff4444;">
                                <div style="font-size: 11px; color: #ff4444; font-weight: bold; margin-bottom: 4px;">Validation Issues:</div>
                                ${validationIssues.map(issue => `<div style="font-size: 10px; color: #ffaa44;">• ${issue}</div>`).join('')}
                            </div>
                        ` : ''}
                        ${!isLegit && validatedParts.every(p => p.isValid !== false) ? `
                            <div style="margin-top: 10px; padding: 8px; background: #3a1a1a; border-radius: 4px; border-left: 2px solid #ffaa44;">
                                <div style="font-size: 11px; color: #ffaa44; font-weight: bold; margin-bottom: 4px;">Debug Info:</div>
                                <div style="font-size: 10px; color: #aaa;">
                                    <div>All parts found and validated, but item marked as MODDED.</div>
                                    <div>This may indicate a missing component or validation logic issue.</div>
                                </div>
                            </div>
                        ` : ''}
                    </div>
                `;
                
                resultDiv.innerHTML = html;

                window.__legitBackpackContext = window.__legitBackpackContext || {};
                window.__legitBackpackContext.lastDeserialized = serialForValidate;
                if (!window.__legitBackpackContext.pendingFromB85) {
                    window.__legitBackpackContext.lastBase85 = null;
                } else {
                    window.__legitBackpackContext.pendingFromB85 = false;
                }
                if (typeof window.updateLegitValidatorBackpackButton === 'function') {
                    window.updateLegitValidatorBackpackButton();
                }

                if (window.__lbCtx && window.__lbCtx.relaxedPicker) {
                    try {
                        const flagsL = parseModdedFirmwareBuybackFlagsFromDeserialized(serialForValidate);
                        const fwL = document.getElementById('mi_firmwareLock');
                        const bbL = document.getElementById('mi_buyback');
                        if (fwL) {
                            fwL.checked = flagsL.firmwareLock;
                        }
                        if (bbL) {
                            bbL.checked = flagsL.buyback;
                        }
                        applyMiSkinUIFromDeserializedTail(serialForValidate);
                        const vLeg = validateSerialCore(serialForValidate, 1, {
                            includePartDetails: true,
                            moddedRelaxedValidation: true
                        });
                        window.__legitLastSingleValidation = vLeg;
                        syncModdedLevelAndSeedFromDecode(serialForValidate, vLeg);
                        applyModdedParseValidationToBuilder(vLeg);
                    } catch (eMiLeg) {
                        applyModdedParseValidationToBuilder(null);
                    }
                }

                const bulkDetailHtmlCache = new Map();
                let openBulkDetailRowId = null;

                const hideDetailRow = (rowId) => {
                    const detailRow = resultDiv.querySelector(`tr[data-bulk-detail-row-id="${rowId}"]`);
                    if (detailRow) {
                        detailRow.style.display = 'none';
                    }
                };

                const buildBulkDetailHtml = (resultIdx) => {
                    const result = validationResults[resultIdx];
                    if (!result) {
                        return `<div style="padding: 10px; color: #ffaa44;">No details available for this row.</div>`;
                    }

                    let detailedValidation = result;
                    const hasPartDetails = Array.isArray(result?.details?.validatedParts);
                    if (!hasPartDetails) {
                        const sourceSerial = result.deserialized || result.original;
                        if (!sourceSerial) {
                            return `<div style="padding: 10px; color: #ffaa44;">No serial payload available for detailed view.</div>`;
                        }
                        const bulkMiRelaxed = !!(
                            resultDiv &&
                            resultDiv.closest &&
                            resultDiv.closest('#item-editor-tab')
                        );
                        detailedValidation = validateSerialCore(sourceSerial, result.index || (resultIdx + 1), {
                            includePartDetails: true,
                            moddedRelaxedValidation: bulkMiRelaxed
                        });
                        detailedValidation.lineNumber = result.lineNumber;
                        detailedValidation.original = result.original;
                        detailedValidation.deserialized = result.deserialized || sourceSerial;
                    }

                    return renderSerialValidationDetailsHtml(detailedValidation, {
                        source: 'bulk',
                        data: data,
                        translationOnly: !!(resultDiv && resultDiv.closest && resultDiv.closest('#item-editor-tab'))
                    });
                };

                resultDiv.querySelectorAll('tr[data-bulk-row-id]').forEach(row => {
                    row.addEventListener('click', () => {
                        const rowId = row.getAttribute('data-bulk-row-id');
                        const resultIdxRaw = row.getAttribute('data-result-idx');
                        const resultIdx = Number.parseInt(resultIdxRaw, 10);
                        if (!rowId || Number.isNaN(resultIdx)) return;

                        const detailRow = resultDiv.querySelector(`tr[data-bulk-detail-row-id="${rowId}"]`);
                        const detailContent = resultDiv.querySelector(`[data-bulk-detail-content="${rowId}"]`);
                        if (!detailRow || !detailContent) return;

                        const isOpen = detailRow.style.display !== 'none';
                        if (isOpen) {
                            hideDetailRow(rowId);
                            if (openBulkDetailRowId === rowId) openBulkDetailRowId = null;
                            return;
                        }

                        if (openBulkDetailRowId && openBulkDetailRowId !== rowId) {
                            hideDetailRow(openBulkDetailRowId);
                        }

                        if (!bulkDetailHtmlCache.has(rowId)) {
                            bulkDetailHtmlCache.set(rowId, buildBulkDetailHtml(resultIdx));
                        }
                        detailContent.innerHTML = bulkDetailHtmlCache.get(rowId);
                        detailRow.style.display = '';
                        openBulkDetailRowId = rowId;
                    });
                });

                resultDiv.style.display = 'block';
                ensureMiModdedDecodeAccordionCollapsedForModdedResult();
                maybeSerializeModdedHumanToBase85(serialForValidate);
                
            } catch (error) {
                resultDiv.innerHTML = `
                    <div style="padding: 15px; background: #3a1a1a; border-radius: 6px; border-left: 3px solid #ff4444;">
                        <h4 style="color: #ff4444; margin-bottom: 10px;">✗ Decode Error</h4>
                        <div style="font-size: 12px; color: #ffaa44;">${error.message}</div>
                    </div>
                `;
                resultDiv.style.display = 'block';
                ensureMiModdedDecodeAccordionCollapsedForModdedResult();
                if (window.__lbCtx && window.__lbCtx.relaxedPicker) {
                    try {
                        const flagsE = parseModdedFirmwareBuybackFlagsFromDeserialized(serialForValidate);
                        const fwE = document.getElementById('mi_firmwareLock');
                        const bbE = document.getElementById('mi_buyback');
                        if (fwE) {
                            fwE.checked = flagsE.firmwareLock;
                        }
                        if (bbE) {
                            bbE.checked = flagsE.buyback;
                        }
                        applyMiSkinUIFromDeserializedTail(serialForValidate);
                    } catch (eFl) {}
                    updateModdedDecodedSummaryFromValidation(null);
                }
            }
        }

        // Extract serials from YAML-like format or one-per-line format
        function extractSerialsFromInput(inputText) {
            const serials = [];
            const lines = inputText.split('\n');
            
            // Try YAML-like format first (looks for "serial:" or "serial =")
            const yamlPattern = /serial\s*[:=]\s*['"]?([^'"]+)['"]?/i;
            
            for (let i = 0; i < lines.length; i++) {
                const line = lines[i].trim();
                if (!line) continue;
                
                // Check for YAML format
                const yamlMatch = line.match(yamlPattern);
                if (yamlMatch) {
                    const serial = yamlMatch[1].trim();
                    if (serial) {
                        const isBase85 = (serial.startsWith('@Ug') && serial.length >= 10) || isBase85Format(serial);
                        serials.push({
                            serial: serial,
                            lineNumber: i + 1,
                            format: isBase85 ? 'base85' : 'deserialized'
                        });
                    }
                } else {
                    // Support mixed input: YAML-like lines and plain serial lines can coexist.
                    const trimmed = line.trim();
                    if (trimmed && (trimmed.startsWith('@') || trimmed.includes('|') || trimmed.includes('{') || /^\d/.test(trimmed))) {
                        // Treat @Ug... as Base85 (same as item-editor) so bulk API is used; else use isBase85Format
                        const isBase85 = trimmed.startsWith('@Ug') && trimmed.length >= 10 || isBase85Format(trimmed);
                        serials.push({
                            serial: trimmed,
                            lineNumber: i + 1,
                            format: isBase85 ? 'base85' : 'deserialized'
                        });
                    }
                }
            }
            
            return serials;
        }

        function sortBulkFailsToEnd() {
            const bulkInputEl = lbEl('bulkSerialInput');
            const resultDiv = lbEl('bulkValidationResult');
            if (!bulkInputEl) return;

            const currentInput = String(bulkInputEl.value || '');
            if (!currentInput.trim()) {
                showError('Please paste serials before sorting fails.');
                return;
            }

            const snapshot = window.lastBulkValidationSnapshot;
            if (!snapshot || !Array.isArray(snapshot.results) || snapshot.results.length === 0) {
                showError('Run "Validate All Serials" first, then sort fails.');
                return;
            }

            if (snapshot.inputText !== currentInput) {
                showError('Bulk input changed since last validation. Re-run validation before sorting fails.');
                return;
            }

            const lines = currentInput.split('\n');
            const sortableEntries = snapshot.results
                .filter(entry => Number.isFinite(entry.lineNumber))
                .map(entry => ({
                    lineNumber: Number(entry.lineNumber),
                    valid: !!entry.valid
                }))
                .filter(entry => entry.lineNumber >= 1 && entry.lineNumber <= lines.length)
                .sort((a, b) => a.lineNumber - b.lineNumber);

            if (sortableEntries.length === 0) {
                showError('No sortable serial lines were found in the current input.');
                return;
            }

            const invalidCount = sortableEntries.filter(entry => !entry.valid).length;
            if (invalidCount === 0) {
                showError('No failed serials to sort.');
                return;
            }

            const validLineValues = [];
            const invalidLineValues = [];
            sortableEntries.forEach(entry => {
                const lineValue = lines[entry.lineNumber - 1] || '';
                if (entry.valid) validLineValues.push(lineValue);
                else invalidLineValues.push(lineValue);
            });

            const reorderedValues = [...validLineValues, ...invalidLineValues];
            sortableEntries.forEach((entry, idx) => {
                lines[entry.lineNumber - 1] = reorderedValues[idx];
            });

            bulkInputEl.value = lines.join('\n');
            hideError();

            // Re-run validation so the table/order reflects the newly sorted input.
            bulkValidateSerials();
            if (resultDiv && typeof resultDiv.scrollIntoView === 'function') {
                resultDiv.scrollIntoView({ behavior: 'smooth', block: 'start' });
            }
        }

        // Deserialize a Base85 serial
        async function deserializeSingleSerial(base85Serial) {
            try {
                const response = await fetch(SERIAL_API_URL, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ serial_b85: base85Serial })
                });
                
                if (!response.ok) {
                    throw new Error(`API error: ${response.status}`);
                }
                
                const result = await response.json();
                
                if (result.error) {
                    throw new Error(result.error);
                }
                
                return result.deserialized || null;
            } catch (error) {
                throw new Error(`Deserialization failed: ${error.message}`);
            }
        }

        // Helper to delay execution
        function delay(ms) {
            return new Promise(resolve => setTimeout(resolve, ms));
        }

        // Deserialize with retry logic for rate limiting
        async function deserializeSingleSerialWithRetry(base85Serial, maxRetries = 3) {
            let lastError = null;
            for (let attempt = 0; attempt < maxRetries; attempt++) {
                try {
                    const response = await fetch(SERIAL_API_URL, {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({ serial_b85: base85Serial })
                    });
                    
                    if (response.status === 429) {
                        // Rate limited - wait longer and retry
                        const waitTime = Math.min(1000 * Math.pow(2, attempt), 5000); // Exponential backoff, max 5s
                        await delay(waitTime);
                        continue;
                    }
                    
                    if (!response.ok) {
                        throw new Error(`API error: ${response.status}`);
                    }
                    
                    const result = await response.json();
                    
                    if (result.error) {
                        throw new Error(result.error);
                    }
                    
                    return result.deserialized || null;
                } catch (error) {
                    lastError = error;
                    // If it's a network/CORS error, don't retry (it won't help)
                    if (error.message.includes('CORS') || error.message.includes('Failed to fetch')) {
                        throw error;
                    }
                    // For rate limiting or other errors, wait and retry
                    if (attempt < maxRetries - 1) {
                        await delay(500 * (attempt + 1)); // Exponential backoff
                    }
                }
            }
            throw lastError || new Error('Deserialization failed after retries');
        }

        // Bulk deserialize Base85 serials — mirrors save-editor item-editor-11-bulk-init.js (same API, same response handling).
        // Named uniquely so item-editor-11-bulk-init.js does not overwrite it (that script sets window.bulkDeserializeSerials for the save-editor UI).
        async function legitBulkDeserializeSerials(base85Serials, progressCallback = null) {
            const failAll = (message) => base85Serials.map((item) => ({
                original: item.serial,
                deserialized: null,
                lineNumber: item.lineNumber,
                format: 'base85',
                error: message
            }));

            if (!Array.isArray(base85Serials) || base85Serials.length === 0) return [];

            let out;
            try {
                if (progressCallback) {
                    progressCallback({ current: 0, total: base85Serials.length, message: 'Sending to bulk API...' });
                }

                const normalizedBase85Serials = base85Serials.map(item => {
                    const s = item.serial && String(item.serial).trim();
                    if (!s) return null;
                    return s.startsWith('@') ? s : `@${s}`;
                }).filter(Boolean);
                if (normalizedBase85Serials.length !== base85Serials.length) {
                    out = failAll('One or more serials were empty or invalid.');
                    return out;
                }

                const apiUrl = typeof SERIAL_API_URL === 'string' && SERIAL_API_URL ? SERIAL_API_URL : 'https://save-editor.be/nicnl/api.php';
                let base85Results = {};
                const controller = new AbortController();
                const timeoutId = setTimeout(() => controller.abort(), 60000);
                if (BULK_VALIDATOR_DEBUG && typeof console !== 'undefined' && console.log) {
                    console.log('[Bulk validator] POST bulk deserialize:', apiUrl, 'serials:', normalizedBase85Serials.length);
                }
                const response = await fetch(apiUrl, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ serials: normalizedBase85Serials }),
                    signal: controller.signal
                });
                clearTimeout(timeoutId);

                if (!response.ok) {
                    if (response.status === 401) {
                        out = failAll('API requires authentication. Use a web server instead of file://.');
                        return out;
                    }
                    const errorText = await response.text();
                    out = failAll(`API responded with ${response.status}: ${errorText}`);
                    return out;
                }

                const data = await response.json();
                if (data === undefined || data === null) {
                    out = failAll('API returned no data.');
                    return out;
                }
                if (data.results != null && typeof data.results === 'object') {
                    base85Results = data.results;
                } else if (data && typeof data === 'object') {
                    base85Results = data;
                }

                if (progressCallback) {
                    progressCallback({ current: base85Serials.length, total: base85Serials.length, message: 'Bulk deserialization complete!' });
                }

                const results = [];
                const isArray = Array.isArray(base85Results);
                const hasIndexKeys = !isArray && base85Serials.length > 0 && base85Results['index_0'] != null && typeof base85Results['index_0'] === 'object';

                const yieldMapChunk = function () {
                    return new Promise(function (resolve) {
                        if (typeof requestAnimationFrame === 'function') {
                            requestAnimationFrame(function () { setTimeout(resolve, 0); });
                        } else {
                            setTimeout(resolve, 0);
                        }
                    });
                };
                const mapHint = function (serial) {
                    if (serial == null || serial === '') return '';
                    var s = String(serial).trim();
                    if (s.length <= 28) return s;
                    if (s.charAt(0) === '@') return s.slice(0, 16) + '…';
                    return s.slice(0, 22) + '…';
                };

                for (let i = 0; i < base85Serials.length; i++) {
                    if (i > 0 && i % 800 === 0) {
                        if (typeof window.showLegitActivityPill === 'function') {
                            var mapItem = base85Serials[i];
                            window.showLegitActivityPill({
                                phase: 'Mapping API results',
                                current: i + 1,
                                total: base85Serials.length,
                                hint: mapItem && mapItem.serial ? mapHint(mapItem.serial) : ''
                            });
                        }
                        await yieldMapChunk();
                    }
                    const item = base85Serials[i];
                    const originalSerial = item.serial;
                    const normalizedSerial = originalSerial.startsWith('@') ? originalSerial : `@${originalSerial}`;
                    let result = isArray ? base85Results[i] : (hasIndexKeys ? base85Results['index_' + i] : (base85Results[normalizedSerial] || base85Results[originalSerial] || base85Results[originalSerial.replace('@', '')]));
                    // API may return plain decoded string per index
                    if (typeof result === 'string') result = { deserialized: result, success: true };
                    const deserialized = result && (result.deserialized != null) ? result.deserialized : null;
                    if (result && (result.success !== false) && deserialized) {
                        results.push({ original: originalSerial, deserialized: deserialized, lineNumber: item.lineNumber, format: 'base85', error: null });
                    } else if (result && result.error) {
                        results.push({ original: originalSerial, deserialized: null, lineNumber: item.lineNumber, format: 'base85', error: result.error });
                    } else {
                        results.push({ original: originalSerial, deserialized: null, lineNumber: item.lineNumber, format: 'base85', error: result ? 'No result from API' : 'Serial not found in response' });
                    }
                }
                out = results;
                return out;
            } catch (error) {
                const msg = error?.message || String(error || 'Unknown error');
                out = failAll(`Bulk deserialization failed: ${msg}`);
                return out;
            }
            return out != null ? out : failAll('Unexpected error (no response).');
        }

        const SERIAL_VALIDATION_CLASS = Object.freeze({
            PASS_ALL: 'Pass All Validation Checks',
            FAIL_ORDER: 'Fail Order check pass tag dep checks',
            FAIL_TAG_DEPS: 'Fail Tag Dep Checks',
            HARD_FAIL: 'Balance Fail - Modded',
            /** Modded tab: decoded OK; cross-root parts are expected, not "missing". */
            MODDED_BUILD: 'Modded build'
        });

        function getSerialValidationClassPresentation(validationClass) {
            switch (validationClass) {
                case SERIAL_VALIDATION_CLASS.PASS_ALL:
                    return { label: 'Pass', color: '#4CAF50', icon: '✓', isPass: true };
                case SERIAL_VALIDATION_CLASS.MODDED_BUILD:
                    return { label: 'Modded build', color: '#b39ddb', icon: '◆', isPass: true };
                case SERIAL_VALIDATION_CLASS.FAIL_ORDER:
                    return { label: 'Order Issue', color: '#ff9800', icon: '⚠', isPass: false };
                case SERIAL_VALIDATION_CLASS.FAIL_TAG_DEPS:
                    return { label: 'Prerequisite Fail', color: '#ff4444', icon: '✗', isPass: false };
                case SERIAL_VALIDATION_CLASS.HARD_FAIL:
                default:
                    return { label: 'Balance Fail (Modded)', color: '#ff4444', icon: '✗', isPass: false };
            }
        }

        /** Split decode results for modded UI (unknown vs cross-root vs legit-missing). */
        function partitionValidatedPartsForModdedDecode(validatedParts, moddedRelaxedValidation) {
            const list = Array.isArray(validatedParts) ? validatedParts : [];
            const unknownParts = list.filter((p) => p && !p.found);
            const foreignRootParts = list.filter((p) => p && p.found && p.rootMatches === false);
            const missingParts = moddedRelaxedValidation
                ? unknownParts
                : list.filter((p) => p && (!p.found || p.rootMatches === false));
            return { unknownParts, foreignRootParts, missingParts };
        }

        function buildModdedDecodeSummaryNote(unknownCount, foreignRootCount, compRootKey) {
            const bits = [];
            if (foreignRootCount > 0) {
                bits.push(
                    `<strong style="color:#ffb74d;">${foreignRootCount}</strong> cross-root part(s) — normal for modded / cross-manufacturer picks (would fail legit balance on <code style="font-size:10px;">${escapeLegitHtml(compRootKey || 'this item')}</code>)`
                );
            }
            if (unknownCount > 0) {
                bits.push(
                    `<strong style="color:#ef5350;">${unknownCount}</strong> token(s) not found in loaded Nexus data for the resolved root — check serial format or load inv0/inv4`
                );
            }
            if (!bits.length) {
                return '<span style="color:#b39ddb;">All tokens resolved in Nexus. Legit slot/tag rules still apply on the Legit tab.</span>';
            }
            return bits.join('<br>');
        }

        function renderValidationChecksHtml(details = {}, validationClass = null, options = {}) {
            const compact = !!options.compact;
            const orderFail = !!details.orderFail;
            const tagDepFail = !!details.tagDepFail;
            const exclusionFail = !!details.exclusionFail || (Array.isArray(details.exclusionErrors) && details.exclusionErrors.length > 0);
            const prereqFail = details.prereqFail != null ? !!details.prereqFail : (!!details.tagDepFail && !exclusionFail);
            const hardFail = validationClass === SERIAL_VALIDATION_CLASS.HARD_FAIL || (Array.isArray(details.hardFailReasons) && details.hardFailReasons.length > 0);
            const criticalRulesFail = hardFail || tagDepFail;

            const row = (label, failed, failColor) => {
                const color = failed ? failColor : '#4CAF50';
                const icon = failed ? '✗' : '✓';
                const value = failed ? 'Fail' : 'Pass';
                if (compact) {
                    return `<span style="display: inline-flex; align-items: center; gap: 4px; color: ${color}; font-weight: 600; font-size: 10px;">${icon} ${label}: ${value}</span>`;
                }
                return `<div style="color: ${color};"><strong>${label}:</strong> ${icon} ${value}</div>`;
            };

            if (compact) {
                return `
                    <div style="display: flex; flex-direction: column; gap: 2px;">
                        ${row('Part Order', orderFail, '#ff9800')}
                        ${row('Tag Exclusion', exclusionFail, '#ff4444')}
                        ${row('Part Prerequisites', prereqFail, '#ff4444')}
                        ${row('Critical Rules', criticalRulesFail, '#ff4444')}
                    </div>
                `;
            }

            return `
                <div style="margin-top: 6px; padding: 8px; background: rgba(255,255,255,0.03); border-radius: 4px; border: 1px solid rgba(255,255,255,0.08);">
                    ${row('Part Order', orderFail, '#ff9800')}
                    ${row('Tag Exclusion', exclusionFail, '#ff4444')}
                    ${row('Part Prerequisites', prereqFail, '#ff4444')}
                    ${row('Critical Rules', criticalRulesFail, '#ff4444')}
                </div>
            `;
        }

        function resolveSerialValidationOutcome(flags = {}) {
            const hardFailReasons = Array.isArray(flags.hardFailReasons)
                ? flags.hardFailReasons.filter(Boolean)
                : [];
            const orderFail = !!flags.orderFail;
            const tagDepFail = !!flags.tagDepFail;
            const moddedRelaxed = !!flags.moddedRelaxedValidation;
            const unknownCount = Number(flags.unknownPartsCount) || 0;
            const foreignRootCount = Number(flags.foreignRootPartsCount) || 0;

            if (hardFailReasons.length > 0) {
                if (moddedRelaxed && unknownCount === 0 && foreignRootCount > 0) {
                    const reasons = hardFailReasons.slice();
                    if (foreignRootCount > 0) {
                        reasons.unshift(
                            `${foreignRootCount} cross-root part(s) (expected for modded; listed under other issues)`
                        );
                    }
                    return {
                        validationClass: SERIAL_VALIDATION_CLASS.MODDED_BUILD,
                        valid: false,
                        classificationReasons: reasons
                    };
                }
                return {
                    validationClass: SERIAL_VALIDATION_CLASS.HARD_FAIL,
                    valid: false,
                    classificationReasons: hardFailReasons
                };
            }
            if (moddedRelaxed) {
                const moddedReasons = [];
                if (foreignRootCount > 0) {
                    moddedReasons.push(`${foreignRootCount} cross-root part(s) (OK for modded)`);
                }
                if (unknownCount > 0) {
                    moddedReasons.push(`${unknownCount} token(s) not in Nexus data`);
                }
                if (moddedReasons.length > 0 || tagDepFail || orderFail) {
                    return {
                        validationClass: SERIAL_VALIDATION_CLASS.MODDED_BUILD,
                        valid: unknownCount === 0 && !tagDepFail && !orderFail,
                        classificationReasons: moddedReasons
                    };
                }
            }
            if (orderFail && !tagDepFail) {
                return {
                    validationClass: SERIAL_VALIDATION_CLASS.FAIL_ORDER,
                    valid: false,
                    classificationReasons: ['Canonical part order mismatch']
                };
            }
            if (tagDepFail) {
                return {
                    validationClass: SERIAL_VALIDATION_CLASS.FAIL_TAG_DEPS,
                    valid: false,
                    classificationReasons: ['Part prerequisite checks failed']
                };
            }
            return {
                validationClass: SERIAL_VALIDATION_CLASS.PASS_ALL,
                valid: true,
                classificationReasons: []
            };
        }

        function classifyHardFailReasons(
            validatedParts,
            validationIssues,
            exclusionErrors,
            missingParts,
            parseErrorMessage,
            moddedRelaxedValidation
        ) {
            const reasons = [];
            const pushReason = (reason) => {
                if (!reason) return;
                if (!reasons.includes(reason)) reasons.push(reason);
            };

            if (Array.isArray(missingParts) && missingParts.length > 0) {
                pushReason(
                    moddedRelaxedValidation
                        ? `Unknown tokens (${missingParts.length})`
                        : `Missing or root-mismatched parts (${missingParts.length})`
                );
            }

            if (parseErrorMessage) {
                pushReason(parseErrorMessage);
            }

            const hardFailPatterns = [
                /^Root not found/i,
                /^Missing component \(inv_comp\)/i,
                /^Multiple components? \(inv_comp\)/i,
                /^Duplicate part /i,
                /^Multiple firmware parts/i,
                ...(moddedRelaxedValidation
                    ? []
                    : [/^Part ".*" uses root serial /i, /^Part ".*" comes from wrong root /i]),
                /^Missing required .*:/i,
                /^Too many .* parts:/i,
                /^Could not get component rules/i,
                /^Validation error:/i
            ];

            (Array.isArray(validationIssues) ? validationIssues : []).forEach(issue => {
                if (!issue) return;

                const isCompatibilityIssue = /^Compatibility:\s/i.test(issue);
                const normalizedIssue = isCompatibilityIssue
                    ? issue.replace(/^Compatibility:\s*/i, '')
                    : issue;

                // Keep compatibility notes informational by default, but still hard-fail
                // when they represent structural slot count violations.
                const compatibilityHardFail = /^Too many .* parts:/i.test(normalizedIssue) ||
                    /^Missing required .*:/i.test(normalizedIssue);
                if (isCompatibilityIssue && !compatibilityHardFail) {
                    return;
                }

                if (hardFailPatterns.some(pattern => pattern.test(normalizedIssue))) {
                    pushReason(issue);
                }
            });

            if (Array.isArray(exclusionErrors) && exclusionErrors.length > 0) {
                pushReason(`Exclusion conflicts detected (${exclusionErrors.length})`);
            }

            if (Array.isArray(validatedParts)) {
                const invalidStructuralParts = validatedParts.filter(part => {
                    if (!part) return false;
                    if (!part.found) return true;
                    if (!moddedRelaxedValidation && part.rootMatches === false) return true;
                    const msg = String(part.validationIssue || '').toLowerCase();
                    return msg.includes('duplicate part') ||
                        (!moddedRelaxedValidation && msg.includes('wrong root')) ||
                        msg.includes('exceeds maximum count');
                });
                if (invalidStructuralParts.length > 0) {
                    pushReason(`Structural part validation failures (${invalidStructuralParts.length})`);
                }
            }

            return reasons;
        }

        /**
         * When true, omit Fix missing / Extra slot pickers so users cannot "repair" serials that are
         * structurally invalid (duplicates, wrong-root parts, over-max counts, unknown parts).
         */
        function serialHasStructuralSlotPickerBlockers(
            validatedParts,
            validationIssues,
            missingParts,
            moddedRelaxedValidation
        ) {
            if (Array.isArray(missingParts) && missingParts.length > 0) {
                return true;
            }
            const issues = Array.isArray(validationIssues) ? validationIssues : [];
            for (let i = 0; i < issues.length; i++) {
                const issue = issues[i];
                if (!issue || typeof issue !== 'string') continue;
                const n = issue.replace(/^Compatibility:\s*/i, '');
                if (/^Duplicate part /i.test(n)) return true;
                if (/^Too many .* parts:/i.test(n)) return true;
                if (/^Part "/i.test(n) && /uses root serial/i.test(n)) return true;
                if (/^Part "/i.test(n) && /comes from wrong root/i.test(n)) return true;
                if (/^Multiple firmware parts/i.test(n)) return true;
                if (/^Multiple components? \(inv_comp\)/i.test(n)) return true;
            }
            if (Array.isArray(validatedParts)) {
                for (let j = 0; j < validatedParts.length; j++) {
                    const p = validatedParts[j];
                    if (!p || !p.found) continue;
                    const msg = String(p.validationIssue || '').toLowerCase();
                    if (
                        msg.includes('duplicate part') ||
                        msg.includes('wrong root') ||
                        msg.includes('exceeds maximum count')
                    ) {
                        return true;
                    }
                }
            }
            return false;
        }

        // Shared serial validation core used by single and bulk paths.
        function validateSerialCore(deserializedSerial, index, options = {}) {
            const includePartDetails = !!options.includePartDetails;
            const enforceCanonicalOrder = options.enforceCanonicalOrder === true;
            const skipReorderTry = !!options.skipReorderTry;
            const moddedRelaxedValidation =
                options.moddedRelaxedValidation === true ||
                !!(options.moddedRelaxedValidation !== false &&
                    window.__lbCtx &&
                    window.__lbCtx.relaxedPicker);
            if (!data || !data.rootDefinitions) {
                return {
                    index: index,
                    valid: false,
                    error: 'No data loaded',
                    details: null
                };
            }
            
            try {
                // Use the same parsing logic as decodeAndValidateSerial
                const parts = deserializedSerial.split('|').filter(p => p.trim().length > 0);
                if (parts.length < 2) {
                    const hint = !deserializedSerial || !deserializedSerial.includes('|')
                        ? ' Decoded format should be: root, 0, 1, level| seed|| {parts}|'
                        : '';
                    return {
                        index: index,
                        valid: false,
                        error: 'Invalid serial format.' + hint,
                        details: null
                    };
                }
                
                // Parse header with support for quoted strings
                let headerStr = parts[0].trim();
                const header = [];
                let i = 0;
                
                // Parse header with support for quoted strings (same logic as decodeAndValidateSerial)
                while (i < headerStr.length) {
                    if (headerStr[i] === '"') {
                        // Quoted string
                        let start = i;
                        let j = i + 1;
                        while (j < headerStr.length && headerStr[j] !== '"') {
                            j++;
                        }
                        if (j < headerStr.length) {
                            header.push(headerStr.substring(start + 1, j)); // Remove quotes
                            i = j + 1;
                        } else {
                            // Unmatched quote, treat as regular
                            const nextComma = headerStr.indexOf(',', i);
                            if (nextComma > 0) {
                                header.push(headerStr.substring(i, nextComma).trim());
                                i = nextComma + 1;
                            } else {
                                header.push(headerStr.substring(i).trim());
                                break;
                            }
                        }
                    } else {
                        // Regular value
                        const nextComma = headerStr.indexOf(',', i);
                        if (nextComma > 0) {
                            header.push(headerStr.substring(i, nextComma).trim());
                            i = nextComma + 1;
                        } else {
                            header.push(headerStr.substring(i).trim());
                            break;
                        }
                    }
                    // Skip whitespace and commas
                    while (i < headerStr.length && (headerStr[i] === ',' || /\s/.test(headerStr[i]))) {
                        i++;
                    }
                }
                
                if (header.length < 4) {
                    return {
                        index: index,
                        valid: false,
                        error: 'Invalid header format. Expected: rootSerial, 0, 1, level (rootSerial can be number or quoted string)',
                        details: null
                    };
                }
                
                // Parse root serial - can be number or quoted string (e.g., "mal_sm")
                let rootSerial = null;
                let rootSerialKey = null;
                const rootSerialStr = header[0];
                if (rootSerialStr.startsWith('"') && rootSerialStr.endsWith('"')) {
                    // Quoted string - extract key
                    rootSerialKey = rootSerialStr.slice(1, -1);
                } else {
                    // Try to parse as number
                    const parsed = parseInt(rootSerialStr);
                    if (!isNaN(parsed)) {
                        rootSerial = parsed;
                    } else {
                        // Treat as key if not a valid number
                        rootSerialKey = rootSerialStr;
                    }
                }
                
                const level = parseInt(header[3]);
                const { seedPart, serialListStr: rawSerialListStr, rawPipeBeforePartList } = splitSerialHeaderSeedAndList(parts);
                if (!rawSerialListStr) {
                    return {
                        index: index,
                        valid: false,
                        error: 'Invalid serial format: no part list found. Expected ...| seed|| {parts}| (optional flags like 9, 1| allowed before seed).',
                        details: null
                    };
                }
                let seed = null;
                let seed2 = null;
                if (seedPart) {
                    const parsedSeed = parseNexusSeedSegment(seedPart);
                    seed = parsedSeed.seed;
                    seed2 = parsedSeed.seed2;
                }
                
                // Find root by serial (if numeric) or by key (if string)
                let rootDef = null;
                if (rootSerialKey) {
                    // Find by key (case-insensitive)
                    rootDef = data.rootDefinitions.find(r => {
                        const rKey = (r.key || '').toLowerCase();
                        return rKey === rootSerialKey.toLowerCase();
                    });
                } else if (rootSerial !== null) {
                    // Find by serial number
                    rootDef = data.rootDefinitions.find(r => {
                        const rSerial = r.serialIndex;
                        if (rSerial === null || rSerial === undefined) return false;
                        const normalizedRSerial = typeof rSerial === 'number' ? rSerial : parseInt(rSerial);
                        return normalizedRSerial === rootSerial;
                    });
                }
                
                if (!rootDef) {
                    return {
                        index: index,
                        valid: false,
                        error: rootSerialKey ? `Root key "${rootSerialKey}" not found` : `Root serial ${rootSerial} not found`,
                        details: {
                            rootSerial: rootSerial,
                            rootSerialKey: rootSerialKey,
                            level: level
                        }
                    };
                }
                
                const serialListStr = stripCosmeticSelectionSegments(rawSerialListStr);
                const serialMatches = parseSerialPatterns(serialListStr);
                
                const decodedSerial = rootDef ? (rootDef.serialIndex || rootSerial) : rootSerial;
                
                // Basic validation: check if parts exist
                const partsFound = [];
                const partsNotFound = [];
                let partIndex = 0;
                
                serialMatches.forEach((match, matchIndex) => {
                    // Check if this is a quoted string (e.g., "MAL_SM.comp_05_legendary_firework")
                    if (match.startsWith('"') && match.endsWith('"')) {
                        // Quoted string - lookup by key
                        const partKey = match.slice(1, -1); // Remove quotes
                        // Extract root key and part key from format like "MAL_SM.comp_05_legendary_firework"
                        const keyParts = partKey.split('.');
                        const partKeyOnly = keyParts.length > 1 ? keyParts.slice(1).join('.') : partKey;
                        
                        // Try to find the part by key in any root
                        let found = false;
                        let foundPart = null;
                        let foundInRoot = null;
                        let partType = normalizeDepTableName(null);
                        let resolvedPartKey = partKeyOnly;
                        
                        if (data && data.rootDefinitions) {
                            for (const r of data.rootDefinitions) {
                                if (r.depEntries) {
                                    const partEntry = r.depEntries.find(dep => {
                                        const depKey = dep.key || dep.inv || '';
                                        return depKey.toLowerCase() === partKeyOnly.toLowerCase();
                                    });
                                    if (partEntry) {
                                        found = true;
                                        foundPart = partEntry;
                                        foundInRoot = r;
                                        partType = normalizeDepTableName(partEntry.depTableName);
                                        resolvedPartKey = partEntry.key || partKeyOnly;
                                        break;
                                    }
                                }
                            }
                        }
                        
                        partIndex++;
                        if (found) {
                            partsFound.push({
                                index: partIndex,
                                serial: match,
                                partKey: resolvedPartKey,
                                partType: partType,
                                rootKey: foundInRoot ? foundInRoot.key : null,
                                foundPart: foundPart,
                                foundInRoot: foundInRoot ? foundInRoot.key : null,
                                patternTokenIndex: matchIndex,
                                withinArrayIndex: null,
                                rawPatternText: match,
                                isQuotedKeyPart: true,
                                isArrayBlock: false
                            });
                        } else {
                            partsNotFound.push({
                                index: partIndex,
                                serial: match,
                                reason: `Part key "${partKeyOnly}" not found in loaded data`,
                                patternTokenIndex: matchIndex,
                                withinArrayIndex: null,
                                rawPatternText: match,
                                isQuotedKeyPart: true,
                                isArrayBlock: false,
                                partKeyOnly: partKeyOnly
                            });
                        }
                        return;
                    }
                    
                    // Handle brace patterns: {serial}, {root:serial}, {root:[serial1 serial2 ...]}
                    const serialStr = match.slice(1, -1); // Remove {}
                    let rootSerialPart = null;
                    let serials = [];
                    
                    // Check if it's array format: {rootSerial:[serial1 serial2 ...]}
                    if (serialStr.includes('[') && serialStr.includes(']')) {
                        const colonIndex = serialStr.indexOf(':');
                        if (colonIndex > 0) {
                            rootSerialPart = parseInt(serialStr.substring(0, colonIndex).trim());
                            const arrayPart = serialStr.substring(colonIndex + 1).trim();
                            const arrayContent = arrayPart.slice(1, -1); // Remove []
                            serials = arrayContent.split(/\s+/).map(s => parseInt(s.trim())).filter(n => !isNaN(n));
                        } else {
                            // Array without rootSerial: {[serial1 serial2 ...]}
                            const arrayContent = serialStr.slice(1, -1); // Remove []
                            serials = arrayContent.split(/\s+/).map(s => parseInt(s.trim())).filter(n => !isNaN(n));
                        }
                    }
                    // Check if it's base comp format: {rootSerial:subSerial}
                    else if (serialStr.includes(':')) {
                        const [rs, ss] = serialStr.split(':').map(s => parseInt(s.trim()));
                        rootSerialPart = rs;
                        serials = [ss];
                    } else {
                        // Regular format: {serial}
                        serials = [parseInt(serialStr)];
                    }
                    
                    const isArrayBlock = serials.length > 1;
                    const arrayBlockKey = isArrayBlock ? (matchIndex + '_' + (rootSerialPart !== null ? String(rootSerialPart) : 'main')) : null;
                    
                    // Process each serial in the pattern (handles arrays)
                    serials.forEach((serial, serialIdx) => {
                        partIndex++;
                        
                        if (isNaN(serial)) {
                            partsNotFound.push({ 
                                index: partIndex, 
                                serial: serialStr, 
                                reason: 'Invalid serial number',
                                arrayBlockKey: arrayBlockKey,
                                isArrayBlock: isArrayBlock,
                                patternTokenIndex: matchIndex,
                                withinArrayIndex: isArrayBlock ? serialIdx : null,
                                rawPatternText: match,
                                isQuotedKeyPart: false
                            });
                            return;
                        }
                        
                        let found = false;
                        
                        if (rootSerialPart !== null) {
                            // Base comp part - search in specified root
                            const targetRoot = data.rootDefinitions.find(r => {
                                const rSerial = r.serialIndex;
                                if (rSerial === null || rSerial === undefined) return false;
                                const normalizedRSerial = typeof rSerial === 'number' ? rSerial : parseInt(rSerial);
                                return normalizedRSerial === rootSerialPart;
                            });
                            
                            if (targetRoot && targetRoot.depEntries) {
                                const partEntry = targetRoot.depEntries.find(dep => {
                                    const depSerial = dep.serialIndex || dep.serialindex?.index;
                                    if (depSerial === null || depSerial === undefined) return false;
                                    const normalizedDepSerial = typeof depSerial === 'number' ? depSerial : parseInt(depSerial);
                                    return normalizedDepSerial === serial;
                                });
                                found = !!partEntry;
                            }
                        } else {
                            // Regular part - search in root's depEntries
                            if (rootDef && rootDef.depEntries) {
                                const partEntry = rootDef.depEntries.find(dep => {
                                    const depSerial = dep.serialIndex || dep.serialindex?.index;
                                    if (depSerial === null || depSerial === undefined) return false;
                                    const normalizedDepSerial = typeof depSerial === 'number' ? depSerial : parseInt(depSerial);
                                    return normalizedDepSerial === serial;
                                });
                                found = !!partEntry;
                            }
                        }
                        
                        // Find the full part entry (same logic as decodeAndValidateSerial)
                        let foundPart = null;
                        let foundInRoot = null;
                        let partType = 'Unknown';
                        let partKey = null;
                        
                        if (rootSerialPart !== null) {
                            // Base comp part - search in specified root
                            const targetRoot = data.rootDefinitions.find(r => {
                                const rSerial = r.serialIndex;
                                if (rSerial === null || rSerial === undefined) return false;
                                const normalizedRSerial = typeof rSerial === 'number' ? rSerial : parseInt(rSerial);
                                return normalizedRSerial === rootSerialPart;
                            });
                            
                            if (targetRoot && targetRoot.depEntries) {
                                const partEntry = targetRoot.depEntries.find(dep => {
                                    const depSerial = dep.serialIndex || dep.serialindex?.index;
                                    if (depSerial === null || depSerial === undefined) return false;
                                    const normalizedDepSerial = typeof depSerial === 'number' ? depSerial : parseInt(depSerial);
                                    return normalizedDepSerial === serial;
                                });
                                if (partEntry) {
                                    foundPart = partEntry;
                                    foundInRoot = targetRoot;
                                    partType = normalizeDepTableName(partEntry.depTableName);
                                    partKey = partEntry.key || null;
                                }
                            }
                        } else {
                            // Regular part - search in root's depEntries
                            if (rootDef && rootDef.depEntries) {
                                const partEntry = rootDef.depEntries.find(dep => {
                                    const depSerial = dep.serialIndex || dep.serialindex?.index;
                                    if (depSerial === null || depSerial === undefined) return false;
                                    const normalizedDepSerial = typeof depSerial === 'number' ? depSerial : parseInt(depSerial);
                                    return normalizedDepSerial === serial;
                                });
                                if (partEntry) {
                                    foundPart = partEntry;
                                    foundInRoot = rootDef;
                                    partType = normalizeDepTableName(partEntry.depTableName);
                                    partKey = partEntry.key || null;
                                }
                            }
                            // Shield roots: if not found in local root, try type-specific / generic shield roots
                            if (!foundPart && rootDef && data && data.rootDefinitions) {
                                const rootKeyLower = (rootDef.key || '').toLowerCase();
                                const isShieldRoot = rootKeyLower === 'shield' || rootKeyLower.includes('energy_shield') || rootKeyLower.includes('armor_shield');
                                if (isShieldRoot) {
                                    const shieldRoots = data.rootDefinitions.filter(r => {
                                        const rKey = (r.key || '').toLowerCase();
                                        return rKey === 'shield' || rKey === 'energy_shield' || rKey === 'armor_shield';
                                    });
                                    for (const otherRoot of shieldRoots) {
                                        if (otherRoot === rootDef || !otherRoot.depEntries) continue;
                                        const partEntry = otherRoot.depEntries.find(dep => {
                                            const depSerial = dep.serialIndex || dep.serialindex?.index;
                                            if (depSerial === null || depSerial === undefined) return false;
                                            const normalizedDepSerial = typeof depSerial === 'number' ? depSerial : parseInt(depSerial);
                                            return normalizedDepSerial === serial;
                                        });
                                        if (partEntry) {
                                            foundPart = partEntry;
                                            foundInRoot = otherRoot;
                                            partType = normalizeDepTableName(partEntry.depTableName);
                                            partKey = partEntry.key || null;
                                            break;
                                        }
                                    }
                                }
                            }
                        }
                        
                        if (foundPart) {
                            partsFound.push({ 
                                index: partIndex, 
                                serial: serial,
                                rootSerial: rootSerialPart,
                                foundPart: foundPart,
                                foundInRoot: foundInRoot ? foundInRoot.key : null,
                                partType: partType,
                                partKey: partKey,
                                isBaseComp: rootSerialPart !== null,
                                arrayBlockKey: arrayBlockKey,
                                isArrayBlock: isArrayBlock,
                                patternTokenIndex: matchIndex,
                                withinArrayIndex: isArrayBlock ? serialIdx : null,
                                rawPatternText: match,
                                isQuotedKeyPart: false
                            });
                        } else {
                            partsNotFound.push({ 
                                index: partIndex, 
                                serial: serial, 
                                rootSerial: rootSerialPart,
                                serialStr: serialStr,
                                reason: 'Part not found',
                                arrayBlockKey: arrayBlockKey,
                                isArrayBlock: isArrayBlock,
                                patternTokenIndex: matchIndex,
                                withinArrayIndex: isArrayBlock ? serialIdx : null,
                                rawPatternText: match,
                                isQuotedKeyPart: false
                            });
                        }
                    });
                });
                
                // Calculate total parts (arrays expand to multiple parts)
                const totalParts = partsFound.length + partsNotFound.length;
                
                // Tag validation and outcome class flags
                let isModded = false;
                let orderFail = false;
                let tagDepFail = false;
                let exclusionFail = false;
                let prereqFail = false;
                const tagErrors = [];
                const exclusionErrors = [];
                const validationIssues = [];
                const missingRequiredSlots = [];
                const extraOptionalSlots = [];
                let slotConstraintDiagnostics = [];
                
                // Build validatedParts array similar to decodeAndValidateSerial
                const validatedParts = [];
                partsFound.forEach(part => {
                    validatedParts.push({
                        index: part.index,
                        serial: part.serial,
                        rootSerial: part.rootSerial,
                        isBaseComp: part.isBaseComp,
                        partKey: part.partKey,
                        partType: part.partType,
                        found: true,
                        foundInRoot: part.foundInRoot,
                        foundPart: part.foundPart,
                        rootMatches: true, // Will be validated later
                        isValid: true, // Will be validated
                        arrayBlockKey: part.arrayBlockKey,
                        isArrayBlock: part.isArrayBlock,
                        patternTokenIndex: part.patternTokenIndex,
                        withinArrayIndex: part.withinArrayIndex,
                        rawPatternText: part.rawPatternText,
                        isQuotedKeyPart: part.isQuotedKeyPart
                    });
                });
                validatedParts.forEach(function (vp) {
                    const tok = formatValidatorPartTokenFallback(vp);
                    vp.displaySerial = computeValidatorPartDisplaySerial(vp, tok, data);
                });
                partsNotFound.forEach(part => {
                    const qpk =
                        part.partKeyOnly != null
                            ? String(part.partKeyOnly).trim()
                            : '';
                    const quotedComp =
                        !!part.isQuotedKeyPart && qpk && /^comp_/i.test(qpk);
                    validatedParts.push({
                        index: part.index,
                        serial: part.serial || part.serialStr,
                        rootSerial: part.rootSerial,
                        isBaseComp: part.rootSerial !== null,
                        partKey: qpk || null,
                        partType: quotedComp
                            ? 'inv_comp'
                            : normalizeDepTableName(null),
                        found: false,
                        foundInRoot: null,
                        foundPart: null,
                        rootMatches: false,
                        isValid: false,
                        arrayBlockKey: part.arrayBlockKey,
                        isArrayBlock: part.isArrayBlock,
                        patternTokenIndex: part.patternTokenIndex,
                        withinArrayIndex: part.withinArrayIndex,
                        rawPatternText: part.rawPatternText,
                        isQuotedKeyPart: part.isQuotedKeyPart
                    });
                });
                
                if (partsNotFound.length === 0 && totalParts > 0) {
                    // All parts found - now do comprehensive validation
                    // Find component (inv_comp part)
                    const compPart = validatedParts.find(p => p.partType === 'inv_comp' && p.found);
                    
                    if (!compPart) {
                        // No component found - item is modded
                        isModded = true;
                        validationIssues.push('Missing component (inv_comp) - all items require a component');
                    } else {
                        // We have a component - perform comprehensive validation
                        try {
                            // Check for duplicate parts (same partKey in same partType)
                            const partKeyCounts = new Map(); // partType -> Map(partKey -> count)
                            validatedParts.forEach(part => {
                                if (part.found && part.partKey && part.partType) {
                                    if (!partKeyCounts.has(part.partType)) {
                                        partKeyCounts.set(part.partType, new Map());
                                    }
                                    const typeMap = partKeyCounts.get(part.partType);
                                    const currentCount = typeMap.get(part.partKey) || 0;
                                    typeMap.set(part.partKey, currentCount + 1);
                                }
                            });
                            
                            // Mark duplicates as invalid
                            partKeyCounts.forEach((typeMap, partType) => {
                                typeMap.forEach((count, partKey) => {
                                    if (count > 1) {
                                        // Find all parts with this duplicate
                                        const duplicateParts = validatedParts.filter(p => 
                                            p.partType === partType && p.partKey === partKey && p.found
                                        );
                                        duplicateParts.forEach(part => {
                                            part.isValid = false;
                                            part.validationIssue = `Duplicate part: "${partKey}" appears ${count} times in ${partType} (duplicates not allowed)`;
                                            isModded = true;
                                        });
                                        validationIssues.push(`Duplicate part "${partKey}" in ${partType}: found ${count} times (only 1 allowed)`);
                                    }
                                });
                            });
                            
                            // Check for firmware max count violation (firmware always has max 1)
                            const firmwareParts = validatedParts.filter(p => p.partType === 'firmware' && p.found);
                            if (firmwareParts.length > 1) {
                                firmwareParts.forEach(part => {
                                    part.isValid = false;
                                    part.validationIssue = `Multiple firmware parts selected: found ${firmwareParts.length} firmware parts (maximum 1 allowed)`;
                                    isModded = true;
                                });
                                validationIssues.push(`Multiple firmware parts: found ${firmwareParts.length} firmware parts (maximum 1 allowed)`);
                            }
                            
                            // Check for multiple comps (inv_comp is Min1 Max1 - exactly one per item)
                            const compParts = validatedParts.filter(p => p.partType === 'inv_comp' && p.found);
                            if (compParts.length > 1) {
                                compParts.forEach(part => {
                                    part.isValid = false;
                                    part.validationIssue = `Multiple components (inv_comp): found ${compParts.length} (exactly 1 allowed - comp is Min1 Max1)`;
                                    isModded = true;
                                });
                                validationIssues.push(`Multiple components (inv_comp): found ${compParts.length} (exactly 1 allowed - comp is Min1 Max1)`);
                            }
                            
                            // Validate root serial matching for base comp parts and root key matching for regular parts
                            const compRoot = rootDef;
                            const compRootSerial = compRoot.serialIndex ? (typeof compRoot.serialIndex === 'number' ? compRoot.serialIndex : parseInt(compRoot.serialIndex)) : null;
                            
                            // Find allowed generic roots
                            const compRootKeyLower = (compRoot.key || '').toLowerCase();
                            const isManufacturerHeavyWeaponRootCore = compRootKeyLower.endsWith('_hw');
                            const allowsGenericParts = isManufacturerHeavyWeaponRootCore || ['enhancement', 'classmod', 'repair_kit', 'grenade_gadget', 'heavy_weapon_gadget'].some(gr => 
                                compRootKeyLower.includes(gr) || compRootKeyLower.startsWith(gr + '_')
                            );
                            
                            // Check if this is a shield - shields have dual base compositions
                            const isShield = compRootKeyLower.includes('shield') || compRootKeyLower.includes('energy_shield') || compRootKeyLower.includes('armor_shield');
                            
                            const allowedGenericRootSerials = new Set();
                            if (allowsGenericParts && data && data.rootDefinitions) {
                                const genericRootKeys = ['weapon', 'shield', 'enhancement', 'classmod', 'grenade', 'grenade_gadget', 'heavy_weapon', 'heavy_weapon_gadget', 'turret_gadget', 'terminal_gadget', 'repair_kit'];
                                data.rootDefinitions.forEach(root => {
                                    const rootKeyLower = (root.key || '').toLowerCase();
                                    if (genericRootKeys.some(gr => {
                                        const grLower = gr.toLowerCase();
                                        return rootKeyLower === grLower || rootKeyLower.startsWith(grLower + '_');
                                    })) {
                                        const rootSerial = root.serialIndex ? (typeof root.serialIndex === 'number' ? root.serialIndex : parseInt(root.serialIndex)) : null;
                                        if (rootSerial !== null) {
                                            allowedGenericRootSerials.add(rootSerial);
                                        }
                                    }
                                });
                            }
                            
                            // For class mods and enhancements, allow base-comp roots (substat/skill/perk roots 234, 237, 243, 244, 245, 246, 247, 248)
                            const isClassModOrEnhancement = compRootKeyLower.includes('classmod') || compRootKeyLower.includes('enhancement');
                            if (isClassModOrEnhancement) {
                                [234, 237, 243, 244, 245, 246, 247, 248].forEach(serial => allowedGenericRootSerials.add(serial));
                            }
                            
                            // Check if this is a heavy weapon - manufacturer roots use *_hw (key may omit "heavy_weapon")
                            const isHeavyWeapon = isManufacturerHeavyWeaponRootCore || compRootKeyLower.includes('heavy_weapon') || compRootKeyLower.includes('heavy_weapon_gadget');
                            
                            // For shields, also allow energy_shield, armor_shield, and generic shield roots
                            // Shields have dual base compositions: one in energy/armor shield and one in shield
                            if (isShield && data && data.rootDefinitions) {
                                data.rootDefinitions.forEach(root => {
                                    const rootKeyLower = (root.key || '').toLowerCase();
                                    // Allow generic shield root (e.g., "shield" with serial 246)
                                    // Allow energy_shield roots
                                    // Allow armor_shield roots
                                    if (rootKeyLower === 'shield' || 
                                        rootKeyLower.includes('energy_shield') || 
                                        rootKeyLower.includes('armor_shield')) {
                                        const rootSerial = root.serialIndex ? (typeof root.serialIndex === 'number' ? root.serialIndex : parseInt(root.serialIndex)) : null;
                                        if (rootSerial !== null) {
                                            allowedGenericRootSerials.add(rootSerial);
                                        }
                                    }
                                });
                            }
                            
                            // For heavy weapons, also allow generic heavy_weapon root (e.g., serial 244)
                            // Heavy weapons have dual base compositions similar to shields
                            if (isHeavyWeapon && data && data.rootDefinitions) {
                                data.rootDefinitions.forEach(root => {
                                    const rootKeyLower = (root.key || '').toLowerCase();
                                    const rootSerial = root.serialIndex ? (typeof root.serialIndex === 'number' ? root.serialIndex : parseInt(root.serialIndex)) : null;
                                    
                                    // Allow generic heavy_weapon root (e.g., "heavy_weapon" with serial 244)
                                    // Match both "heavy_weapon" and "heavy_weapon_gadget" roots
                                    // Also check if this is specifically the root with serial 244 (firmware root)
                                    if (rootKeyLower === 'heavy_weapon' || 
                                        rootKeyLower === 'heavy_weapon_gadget' ||
                                        rootKeyLower.includes('heavy_weapon') ||
                                        rootSerial === 244) { // Explicitly allow root serial 244 for heavy weapons
                                        if (rootSerial !== null) {
                                            allowedGenericRootSerials.add(rootSerial);
                                        }
                                    }
                                });
                            }
                            
                            validatedParts.forEach(part => {
                                // Check base comp parts - validate root serial matches allowed roots
                                if (part.found && part.isBaseComp && part.rootSerial !== null && part.partType !== 'inv_comp') {
                                    const partRootSerial = part.rootSerial;
                                    const isCompRootSerial = compRootSerial !== null && partRootSerial === compRootSerial;
                                    const isAllowedGenericRoot = allowedGenericRootSerials.has(partRootSerial);
                                    
                                    // Special case: For heavy weapon firmware parts, allow root serial 244
                                    let isAllowedForHeavyWeaponFirmware = false;
                                    if (isHeavyWeapon && part.partType === 'firmware' && partRootSerial === 244) {
                                        isAllowedForHeavyWeaponFirmware = true;
                                    }

                                    // Compatibility: legacy legit serials may encode firmware under shared root 244.
                                    const isAllowedCompatibilityFirmwareRoot = part.partType === 'firmware' && partRootSerial === 244;
                                    
                                    const isWeaponSharedRootPart = ['body_ele', 'secondary_ele', 'pearl_elem', 'pearl_stat'].includes(part.partType);
                                    let isAllowedForWeaponSharedRootPart = false;
                                    if (isWeaponSharedRootPart && data && data.rootDefinitions) {
                                        const genericWeaponRoot = data.rootDefinitions.find(r => {
                                            const rSerial = r.serialIndex ? (typeof r.serialIndex === 'number' ? r.serialIndex : parseInt(r.serialIndex)) : null;
                                            const rKey = (r.key || '').toLowerCase();
                                            return rSerial === partRootSerial && (rKey === 'weapon' || rKey.startsWith('weapon_'));
                                        });
                                        isAllowedForWeaponSharedRootPart = genericWeaponRoot !== undefined;
                                    }
                                    
                                    if (!isCompRootSerial && !isAllowedGenericRoot && !isAllowedForWeaponSharedRootPart && !isAllowedForHeavyWeaponFirmware && !isAllowedCompatibilityFirmwareRoot) {
                                        part.rootMatches = false;
                                        part.isValid = false;
                                        isModded = true;
                                        validationIssues.push(`Part "${part.partKey}" uses root serial ${partRootSerial} (wrong root)`);
                                    }
                                }
                                
                                // Check regular parts - validate root key matches
                                if (part.found && !part.isBaseComp && part.partType !== 'inv_comp' && part.foundInRoot) {
                                    const partRootKey = part.foundInRoot;
                                    const compRootKey = compRoot.key;
                                    const isFromCompRoot = partRootKey === compRootKey;
                                    const isFromGenericRoot = ['weapon', 'shield', 'enhancement', 'classmod', 'grenade', 'grenade_gadget', 'heavy_weapon', 'heavy_weapon_gadget', 'repair_kit'].some(gr => 
                                        partRootKey === gr || partRootKey.startsWith(gr + '_')
                                    );
                                    
                                    // For shields, also allow energy_shield and armor_shield roots (dual base compositions)
                                    let isFromShieldRoot = false;
                                    if (isShield) {
                                        const partRootKeyLower = partRootKey.toLowerCase();
                                        isFromShieldRoot = partRootKeyLower.includes('energy_shield') || partRootKeyLower.includes('armor_shield') || partRootKeyLower === 'shield';
                                    }
                                    
                                    // For heavy weapons, also allow generic heavy_weapon root (dual base compositions)
                                    let isFromHeavyWeaponRoot = false;
                                    if (isHeavyWeapon) {
                                        const partRootKeyLower = partRootKey.toLowerCase();
                                        isFromHeavyWeaponRoot = partRootKeyLower === 'heavy_weapon' || partRootKeyLower === 'heavy_weapon_gadget';
                                    }
                                    
                                    if (!isFromCompRoot && (!isFromGenericRoot || !allowsGenericParts) && !isFromShieldRoot && !isFromHeavyWeaponRoot) {
                                        part.isValid = false;
                                        isModded = true;
                                        validationIssues.push(`Part "${part.partKey}" comes from wrong root "${partRootKey}" (expected "${compRootKey}")`);
                                    }
                                }
                            });
                            
                            // Now perform tag validation using local validated selections only.
                            const validationSelectionKeys = validatedParts
                                .filter(part => part.found && part.partKey)
                                .map(part => part.partKey);
                            
                            // Get comp rules for validation
                            const compRules = getSelectedCompRules(rootDef, validationSelectionKeys);
                            
                            if (compRules) {
                                slotConstraintDiagnostics = buildSlotConstraintDiagnostics(rootDef, compRules);
                                const formatTags = (tags) => {
                                    if (!tags || !Array.isArray(tags)) return [];
                                    return tags.map(tag => {
                                        if (typeof tag === 'object' && tag !== null) {
                                            const values = Object.values(tag);
                                            return values[0] || Object.keys(tag)[0] || '';
                                        }
                                        return tag;
                                    }).filter(Boolean);
                                };
                                
                                const orderViolation = detectCanonicalOrderViolation(validatedParts, rootDef);
                                if (orderViolation.violated) {
                                    orderFail = true;
                                    const orderIssue = `Out-of-order parts: expected ${orderViolation.expected.join(' -> ')}, observed ${orderViolation.observed.join(' -> ')}`;
                                    validationIssues.push(orderIssue);
                                    const foundPartsInSequence = validatedParts.filter(p => p && p.found && p.partType);
                                    foundPartsInSequence.forEach((part, idx) => {
                                        if (orderViolation.mismatchedPartIndexes.has(idx)) {
                                            part.orderIssue = true;
                                            const issue = 'Part is out of canonical order for this root';
                                            if (!String(part.validationIssue || '').includes(issue)) {
                                                part.validationIssue = part.validationIssue ? `${part.validationIssue}; ${issue}` : issue;
                                            }
                                        }
                                    });

                                    // Compatibility mode: known-legit serials may not follow canonical parttypes order.
                                    // Keep diagnostics, but do not fail unless strict enforcement is explicitly requested.
                                    if (enforceCanonicalOrder) {
                                        isModded = true;
                                        foundPartsInSequence.forEach((part, idx) => {
                                            if (orderViolation.mismatchedPartIndexes.has(idx)) {
                                                part.isValid = false;
                                                const issue = 'Part is out of canonical order for this root';
                                                part.validationIssue = part.validationIssue ? `${part.validationIssue}; ${issue}` : issue;
                                            }
                                        });
                                    }
                                }

                                const sortedParts = sortPartsByCanonicalAndDependencyOrder(validatedParts, rootDef, formatTags);
                                const classModTierOrder = detectClassModPassiveTierOrderIssues(sortedParts, rootDef, formatTags);
                                
                                // Find the component part first
                                const compPart = sortedParts.find(p => p.partType === 'inv_comp' && p.found);
                                
                                // Build tag pool incrementally in serial/canonical order.
                                // IMPORTANT: Do NOT preload compRules.tagPool here because it can contain tags
                                // from later parts, which causes forward-looking false positives.
                                const validationTagPool = new Set();
                                
                                // Validate each part in order, building tag pool as we go
                                sortedParts.forEach((part, partIdx) => {
                                    if (!part.found || !part.rootMatches) {
                                        part.isValid = false;
                                        isModded = true;
                                        return;
                                    }
                                    
                                    // Comp is always valid; seed the rolling pool from the comp part only.
                                    if (part.partType === 'inv_comp') {
                                        part.isValid = true;
                                        if (part.foundPart) {
                                            const compBaseTags = formatTags(part.foundPart.basetags || []).map(t => t.toLowerCase());
                                            compBaseTags.forEach(tag => validationTagPool.add(tag));
                                            const compAddTags = formatTags(part.foundPart.addtags || []).map(t => t.toLowerCase());
                                            compAddTags.forEach(tag => validationTagPool.add(tag));
                                        }
                                        return;
                                    }
                                    
                                    // Skip parts that are already invalid
                                    if (part.isValid === false) {
                                        return;
                                    }
                                    
                                    // Validate part against current tag pool
                                    const partEntry = part.foundPart;
                                    if (partEntry) {
                                        const partAddTags = new Set(formatTags(partEntry.addtags || []).map(t => t.toLowerCase()));
                                        const partDepTags = new Set(formatTags(partEntry.dependencytags || []).map(t => t.toLowerCase()));
                                        const partExclTags = new Set(formatTags(partEntry.exclusiontags || []).map(t => t.toLowerCase()));
                                        
                                        // Exclusion: tag must not already be in pool from earlier parts (duplicate tier / mutual exclusion).
                                        // Self-exclusion in data (part adds T and excludes T) only clears conflict when T is not yet in pool.
                                        const conflictingExclusionTags = Array.from(partExclTags).filter(tag =>
                                            validationTagPool.has(tag)
                                        );
                                        const hasExclusionConflict = conflictingExclusionTags.length > 0;
                                        
                                        // Check dependencies (all must be in pool)
                                        const missingDeps = Array.from(partDepTags).filter(tag => !validationTagPool.has(tag));
                                        
                                        // Detect class-mod passive tier ordering violations (tier_1 -> tier_5 only).
                                        const tierOrderIssue = classModTierOrder.issueByPart.get(part);
                                        const effectiveTierIssue = tierOrderIssue && !hasExclusionConflict;
                                        
                                        // Check if part is in allowed parts
                                        const partItemForAllowed = { ...(partEntry || {}), key: part.partKey || partEntry?.key };
                                        const isAllowed = isPartAllowedForTable(partItemForAllowed, compRules, part.partType);
                                        
                                        // IMPORTANT: Add part's tags to pool BEFORE validation checks
                                        // This matches game behavior where parts add tags when selected, regardless of dependencies
                                        // For example: body_ele adds mal_ele, which then satisfies secondary_ele's dependency
                                        // Only skip adding tags if there's an exclusion conflict (part can't be selected)
                                        // Always add part tags in compatibility mode, even when exclusions conflict.
                                        partAddTags.forEach(tag => validationTagPool.add(tag));
                                        
                                        // Part is valid if no conflicts, deps met, allowed, and not out-of-order in classmod tier chain.
                                        const isValid = !hasExclusionConflict && missingDeps.length === 0 && isAllowed && !effectiveTierIssue;
                                        part.isValid = isValid;
                                        
                                        if (!isValid) {
                                            isModded = true;
                                            if (effectiveTierIssue) {
                                                orderFail = true;
                                            }
                                            if (hasExclusionConflict) {
                                                exclusionFail = true;
                                            }
                                            if (missingDeps.length > 0 || !isAllowed) {
                                                prereqFail = true;
                                            }
                                            
                                            // Build issue message
                                            let issues = [];
                                            if (hasExclusionConflict) {
                                                const conflictMsg = `Excludes tags in pool: ${conflictingExclusionTags.join(', ')}`;
                                                issues.push(conflictMsg);
                                                exclusionErrors.push(`Part ${part.index} (Serial ${part.serial}): ${conflictMsg}`);
                                            }
                                            if (missingDeps.length > 0) {
                                                let depsMsg = `Missing dependencies: ${missingDeps.join(', ')}`;
                                                if (effectiveTierIssue) {
                                                    const orderExpected = tierOrderIssue.expectedTag;
                                                    const orderObserved = tierOrderIssue.observedTag;
                                                    if (missingDeps.includes(orderExpected)) {
                                                        depsMsg = `Dependency blocked by tier order: expected ${orderExpected} before ${orderObserved}`;
                                                    }
                                                }
                                                issues.push(depsMsg);
                                                tagErrors.push(`Part ${part.index} (Serial ${part.serial}): ${depsMsg}`);
                                            }
                                            if (!isAllowed) {
                                                const notAllowedMsg = `Not in allowed parts for ${part.partType}`;
                                                issues.push(notAllowedMsg);
                                                tagErrors.push(`Part ${part.index} (Serial ${part.serial}): ${notAllowedMsg}`);
                                            }
                                            if (effectiveTierIssue) {
                                                issues.push(tierOrderIssue.message);
                                                validationIssues.push(`Part "${part.partKey}" (${part.partType}): ${tierOrderIssue.message}`);
                                            }
                                            
                                            part.validationIssue = issues.join('; ') || 'Validation failed';
                                        }
                                    }
                                });

                                // Enforce comp-level tag max rules (parttagselectionrules.max).
                                const coreTagMaxViolations = enforcePartTagMaxRules(validatedParts, compRules, formatTags);
                                coreTagMaxViolations.forEach(violation => {
                                    isModded = true;
                                    prereqFail = true;
                                    const issue = `Too many parts with tag "${violation.tag}": found ${violation.found}, maximum ${violation.max} allowed`;
                                    validationIssues.push(issue);
                                    tagErrors.push(issue);
                                    (violation.overflowParts || []).forEach(part => {
                                        part.isValid = false;
                                        part.validationIssue = part.validationIssue ? `${part.validationIssue}; ${issue}` : issue;
                                    });
                                });
                                
                                // Check part counts (min/max per slot) - use sophisticated slot filtering
                                if (compRules.partCounts) {
                                        // Count parts by type in the validated serial FIRST (before using it)
                                        // Normalize part types (e.g. "Element" -> "element") so they match normalized slot names from partCounts
                                        const partsByType = new Map();
                                        validatedParts.forEach(part => {
                                            if (part.partType !== 'inv_comp' && part.found && part.isValid !== false) {
                                                const normalizedPartType = normalizeDepTableName(part.partType);
                                                const count = partsByType.get(normalizedPartType) || 0;
                                                partsByType.set(normalizedPartType, count + 1);
                                            }
                                        });
                                    // Get valid slots for this root - ONLY from depEntries (actual dependency tables)
                                    const validSlots = new Set();
                                    if (rootDef.depEntries) {
                                            rootDef.depEntries.forEach(dep => {
                                                if (dep.depTableName) {
                                                    const normalizedSlot = normalizeDepTableName(dep.depTableName);
                                                    if (normalizedSlot !== 'unknown' && normalizedSlot !== 'inv_comp') {
                                                        validSlots.add(normalizedSlot);
                                                    }
                                                }
                                            });
                                    }
                                    // Include body_ele/secondary_ele when in comp rules (e.g. from base weapon root) so we validate them for weapons
                                    const rootKeyLowerForSlots = (rootDef.key || '').toLowerCase();
                                    const isWeaponRoot = rootKeyLowerForSlots.includes('weapon') || ['vla', 'dah', 'jak', 'mal', 'hyperion', 'atl', 'ted', 'ord', 'bor', 'grenade', 'heavy', 'turret', 'terminal'].some(m => rootKeyLowerForSlots.includes(m));
                                    if (isWeaponRoot && compRules.partCounts) {
                                        ['body_ele', 'secondary_ele'].forEach(slot => {
                                            if (compRules.partCounts.has(slot)) validSlots.add(slot);
                                        });
                                    }
                                    // Also include all slots from compRules.partCounts (e.g. stat_group1 for enhancements - defined in base composition but not in depEntries)
                                    if (compRules.partCounts) {
                                        compRules.partCounts.forEach((countInfo, slotName) => {
                                            const normalizedSlot = normalizeDepTableName(slotName);
                                            if (normalizedSlot !== 'unknown' && normalizedSlot !== 'inv_comp') {
                                                validSlots.add(normalizedSlot);
                                            }
                                        });
                                    }
                                    
                                    // Check each required slot - ONLY if it's valid for this root
                                    compRules.partCounts.forEach((countInfo, slotName) => {
                                        const slotNameLower = slotName.toLowerCase();
                                        const normalizedSlotName = normalizeDepTableName(slotName);
                                        const rootKeyLower = (rootDef.key || '').toLowerCase();
                                        
                                        // Determine if slot is manufacturer-specific
                                        const isManufacturerSpecific = slotNameLower.includes('hyperion') || 
                                                                       slotNameLower.includes('tediore') || 
                                                                       slotNameLower.includes('atlas') ||
                                                                       slotNameLower.includes('jakobs') ||
                                                                       slotNameLower.includes('maliwan') ||
                                                                       slotNameLower.includes('vladof') ||
                                                                       slotNameLower.includes('dahl') ||
                                                                       slotNameLower.includes('order') ||
                                                                       slotNameLower.includes('borg');
                                        
                                        // If slot is manufacturer-specific, check if it matches this weapon's manufacturer
                                        if (isManufacturerSpecific) {
                                            const slotManufacturer = slotNameLower.includes('hyperion') ? 'hyperion' :
                                                                     slotNameLower.includes('tediore') ? 'tediore' :
                                                                     slotNameLower.includes('atlas') ? 'atlas' :
                                                                     slotNameLower.includes('jakobs') ? 'jakobs' :
                                                                     slotNameLower.includes('maliwan') ? 'maliwan' :
                                                                     slotNameLower.includes('vladof') ? 'vladof' :
                                                                     slotNameLower.includes('dahl') ? 'dahl' :
                                                                     slotNameLower.includes('order') ? 'order' :
                                                                     slotNameLower.includes('borg') ? 'borg' : null;
                                            
                                            const weaponManufacturer = rootKeyLower.includes('hyperion') ? 'hyperion' :
                                                                       rootKeyLower.includes('ted') ? 'tediore' :
                                                                       rootKeyLower.includes('atl') ? 'atlas' :
                                                                       rootKeyLower.includes('jak') ? 'jakobs' :
                                                                       rootKeyLower.includes('mal') ? 'maliwan' :
                                                                       rootKeyLower.includes('vla') ? 'vladof' :
                                                                       rootKeyLower.includes('dah') ? 'dahl' :
                                                                       rootKeyLower.includes('ord') ? 'order' :
                                                                       rootKeyLower.includes('bor') ? 'borg' : null;
                                            
                                            if (slotManufacturer && weaponManufacturer && slotManufacturer !== weaponManufacturer) {
                                                return; // Skip - wrong manufacturer
                                            }
                                        }
                                        
                                        const isWeaponTypeSpecific = slotNameLower.includes('ted_thrown') ||
                                                                      slotNameLower.includes('secondary_ammo');
                                        
                                        const hasPartsInSerial = partsByType.has(normalizedSlotName) && partsByType.get(normalizedSlotName) > 0;
                                        
                                        if (isWeaponTypeSpecific && !hasPartsInSerial) {
                                            return; // Skip - doesn't apply to this weapon type
                                        }
                                        
                                        if (validSlots.size > 0 && !validSlots.has(normalizedSlotName)) {
                                            return; // Skip this slot - it doesn't apply to this weapon
                                        }
                                        
                                        const qualifiedDepsForSlot = getDependencyQualifiedDepEntriesForSlot(
                                            rootDef,
                                            compRules,
                                            slotName,
                                            validationTagPool
                                        );
                                        const qualifiedFillableDeps = filterDepsFillableUnderPartTagMaxRules(
                                            qualifiedDepsForSlot,
                                            compRules,
                                            validatedParts,
                                            formatTags
                                        );
                                        const dependencyQualifiedOptions = qualifiedFillableDeps.length;
                                        const selectedKeysForSlot = collectPartKeysInNormalizedSlot(
                                            validatedParts,
                                            normalizedSlotName
                                        );
                                        const requiredMin = resolveRequiredMinForSlot(
                                            countInfo,
                                            rootDef,
                                            compRules,
                                            slotName,
                                            { availableOptions: dependencyQualifiedOptions }
                                        );
                                        const currentCount = partsByType.get(normalizedSlotName) || 0;
                                        const debugCountResolution = typeof window !== 'undefined' && !!window.debugCountResolutionSources;
                                        if (debugCountResolution && (normalizedSlotName.endsWith('_acc') || normalizedSlotName === 'barrel_licensed')) {
                                            console.log('[Count Resolution Core]', {
                                                slotName,
                                                normalizedSlotName,
                                                countSource: countInfo?.countSource || 'unknown',
                                                configuredMin: countInfo?.min,
                                                configuredMax: countInfo?.max,
                                                dependencyQualifiedOptions,
                                                requiredMin,
                                                currentCount
                                            });
                                        }
                                        // Enforce any resolved max shown in the rule model (including inferred 0-1).
                                        const maxCount =
                                            countInfo && countInfo.max !== undefined && countInfo.max !== null
                                                ? countInfo.max
                                                : null;
                                        // When body_acc_ele is in the tag pool, body_ele selection is required (no non-elemental wombo combos).
                                        // Comps that disable body_ele (min=0 max=0) must not be required to select an element.
                                        let effectiveRequiredMin = requiredMin;
                                        if (normalizedSlotName === 'body_ele' && validationTagPool.has('body_acc_ele')) {
                                            const slotDisabled = (countInfo && countInfo.min === 0 && countInfo.max === 0);
                                            if (!slotDisabled) effectiveRequiredMin = Math.max(requiredMin, 1);
                                        }
                                        const isAccessoryCompatibilitySlot = normalizedSlotName.endsWith('_acc');
                                        
                                        // Check for missing required parts (min count)
                                        if (effectiveRequiredMin > 0 && currentCount < effectiveRequiredMin) {
                                            const missing = effectiveRequiredMin - currentCount;
                                            const partWord = missing === 1 ? 'part' : 'parts';
                                            const issue = `Missing required ${slotName}: need ${effectiveRequiredMin}, found ${currentCount} (missing ${missing} ${partWord})`;
                                            const candidates = filterPickerCandidates(
                                                qualifiedFillableDeps,
                                                selectedKeysForSlot
                                            );
                                            missingRequiredSlots.push({
                                                slotName,
                                                normalizedSlot: normalizedSlotName,
                                                required: effectiveRequiredMin,
                                                current: currentCount,
                                                missing,
                                                compatibility: isAccessoryCompatibilitySlot,
                                                candidates
                                            });
                                            if (isAccessoryCompatibilitySlot) {
                                                validationIssues.push(`Compatibility: ${issue}`);
                                            } else {
                                                isModded = true;
                                                prereqFail = true;
                                                validationIssues.push(issue);
                                            }
                                        }

                                        if (
                                            currentCount >= effectiveRequiredMin &&
                                            maxCount !== null &&
                                            currentCount < maxCount &&
                                            !(maxCount === 0 && dependencyQualifiedOptions === 0)
                                        ) {
                                            const extraCandidates = filterPickerCandidates(
                                                qualifiedFillableDeps,
                                                selectedKeysForSlot
                                            );
                                            if (extraCandidates.length > 0) {
                                                extraOptionalSlots.push({
                                                    slotName,
                                                    normalizedSlot: normalizedSlotName,
                                                    current: currentCount,
                                                    max: maxCount,
                                                    compatibility: isAccessoryCompatibilitySlot,
                                                    candidates: extraCandidates
                                                });
                                            }
                                        }
                                        
                                        // Check for exceeding maximum parts (max count)
                                        // Skip when max is 0 and slot has 0 options for this root (slot not applicable, e.g. stat_group1 on enhancement)
                                        if (maxCount !== null && currentCount > maxCount && !(maxCount === 0 && dependencyQualifiedOptions === 0)) {
                                            const excess = currentCount - maxCount;
                                            const partWord = excess === 1 ? 'part' : 'parts';
                                            const issue = `Too many ${slotName} parts: found ${currentCount}, maximum ${maxCount} allowed (${excess} ${partWord} too many)`;
                                            if (isAccessoryCompatibilitySlot) {
                                                validationIssues.push(`Compatibility: ${issue}`);
                                            } else {
                                                isModded = true;
                                                prereqFail = true;
                                                validationIssues.push(issue);
                                            }
                                        }
                                    });
                                }
                            } else {
                                isModded = true;
                                prereqFail = true;
                                validationIssues.push('Could not get component rules - validation failed');
                            }
                        } catch (error) {
                            // Validation error - don't fail, just note it
                            console.warn('Validation error:', error);
                            validationIssues.push(`Validation error: ${error.message}`);
                            isModded = true;
                            prereqFail = true;
                        }
                    }
                }
                
                // Combine all errors
                const allErrors = [];
                const { unknownParts, foreignRootParts, missingParts } = partitionValidatedPartsForModdedDecode(
                    validatedParts,
                    moddedRelaxedValidation
                );
                if (moddedRelaxedValidation) {
                    if (unknownParts.length > 0) {
                        allErrors.push(
                            `${unknownParts.length} token(s) not in loaded Nexus data`
                        );
                    }
                } else if (missingParts.length > 0) {
                    allErrors.push(`${missingParts.length} part(s) not found`);
                }
                exclusionFail = exclusionFail || exclusionErrors.length > 0;
                prereqFail = prereqFail || tagErrors.length > 0;
                tagDepFail = exclusionFail || prereqFail;

                const hardFailReasons = classifyHardFailReasons(
                    validatedParts,
                    validationIssues,
                    exclusionErrors,
                    missingParts,
                    null,
                    moddedRelaxedValidation
                );
                if (totalParts === 0) {
                    hardFailReasons.push('No parts found in serial');
                }
                let reorderedSerial = null;
                if ((orderFail || tagDepFail) && hardFailReasons.length === 0 && rootDef && !skipReorderTry) {
                    const formatTagsForReorder = (tags) => {
                        if (!tags || !Array.isArray(tags)) return [];
                        return tags.map(tag => {
                            if (typeof tag === 'object' && tag !== null) {
                                const values = Object.values(tag);
                                return values[0] || Object.keys(tag)[0] || '';
                            }
                            return tag;
                        }).filter(Boolean);
                    };
                    let orderedFoundParts = sortPartsByCanonicalAndDependencyOrder(
                        validatedParts.filter(p => p && p.found && p.partType),
                        rootDef,
                        formatTagsForReorder
                    );
                    orderedFoundParts = sortArrayBlockRunsByDependency(orderedFoundParts, formatTagsForReorder);
                    const reorderedSerialCandidate = buildReorderedSerialWithArrayBlocks(
                        rootSerial,
                        rootSerialKey,
                        level,
                        seed,
                        seed2,
                        orderedFoundParts,
                        rawPipeBeforePartList
                    );
                    const revalidateResult = validateSerialCore(reorderedSerialCandidate, index, { includePartDetails: false, skipReorderTry: true });
                    if (revalidateResult && revalidateResult.valid === true) {
                        orderFail = true;
                        tagDepFail = false;
                        reorderedSerial = reorderedSerialCandidate;
                    }
                }
                const outcome = resolveSerialValidationOutcome({
                    hardFailReasons,
                    orderFail,
                    tagDepFail,
                    moddedRelaxedValidation,
                    unknownPartsCount: unknownParts.length,
                    foreignRootPartsCount: foreignRootParts.length
                });
                if (reorderedSerial == null && outcome.validationClass === SERIAL_VALIDATION_CLASS.FAIL_ORDER && rootDef) {
                    const formatTagsForReorder = (tags) => {
                        if (!tags || !Array.isArray(tags)) return [];
                        return tags.map(tag => {
                            if (typeof tag === 'object' && tag !== null) {
                                const values = Object.values(tag);
                                return values[0] || Object.keys(tag)[0] || '';
                            }
                            return tag;
                        }).filter(Boolean);
                    };
                    let orderedFoundParts = sortPartsByCanonicalAndDependencyOrder(
                        validatedParts.filter(p => p && p.found && p.partType),
                        rootDef,
                        formatTagsForReorder
                    );
                    orderedFoundParts = sortArrayBlockRunsByDependency(orderedFoundParts, formatTagsForReorder);
                    reorderedSerial = buildReorderedSerialWithArrayBlocks(
                        rootSerial,
                        rootSerialKey,
                        level,
                        seed,
                        seed2,
                        orderedFoundParts,
                        rawPipeBeforePartList
                    );
                }
                isModded = !outcome.valid;
                if (
                    !outcome.valid &&
                    !(
                        moddedRelaxedValidation &&
                        outcome.validationClass === SERIAL_VALIDATION_CLASS.MODDED_BUILD
                    )
                ) {
                    allErrors.push(outcome.validationClass);
                } else if (
                    moddedRelaxedValidation &&
                    outcome.validationClass === SERIAL_VALIDATION_CLASS.MODDED_BUILD &&
                    (orderFail || tagDepFail) &&
                    hardFailReasons.length === 0
                ) {
                    if (orderFail) {
                        allErrors.push('Part order differs from legit canonical order');
                    }
                    if (tagDepFail) {
                        allErrors.push('Legit tag / slot rules not met (informational on modded tab)');
                    }
                }
                const suppressSlotRepairPickers = serialHasStructuralSlotPickerBlockers(
                    validatedParts,
                    validationIssues,
                    missingParts,
                    moddedRelaxedValidation
                );
                const moddedDecodeNote = moddedRelaxedValidation
                    ? buildModdedDecodeSummaryNote(
                          unknownParts.length,
                          foreignRootParts.length,
                          rootDef ? rootDef.key : null
                      )
                    : null;
                const missingSlotsForDetails = suppressSlotRepairPickers ? [] : missingRequiredSlots;
                const extraSlotsForDetails = suppressSlotRepairPickers ? [] : extraOptionalSlots;

                let lootSourceNote = null;
                let genericLegendaryDropNote = null;
                const invCompForLoot = validatedParts.find(
                    p => (p.partType || '').toLowerCase() === 'inv_comp' && p.found && p.foundPart
                );
                if (invCompForLoot) {
                    const lootCtxRoot =
                        (invCompForLoot.foundInRoot && String(invCompForLoot.foundInRoot).trim()) ||
                        rootDef.key;
                    const lootCtx = { rootKey: lootCtxRoot };
                    if (isBareGenericLegendaryInvalidDrop(invCompForLoot.foundPart, lootCtx)) {
                        const qk = getInvCompQualifiedEntryKey(invCompForLoot.foundPart, lootCtx);
                        genericLegendaryDropNote = {
                            qualifiedKey: qk,
                            message:
                                'GENERIC Legendary (bare comp_05_legendary): treated as an invalid drop for this item type — use a unique composition (comp_05_legendary_*). Enhancement items are excluded from this rule. Indexed dedicated pools are ignored when this applies.'
                        };
                    } else if (isInvCompMissingIndexedLootSource(invCompForLoot.foundPart, data, lootCtx)) {
                        const qk = getInvCompQualifiedEntryKey(invCompForLoot.foundPart, lootCtx);
                        lootSourceNote = {
                            qualifiedKey: qk,
                            message:
                                'Excluded from the global pool and no dedicated pool in loaded item pool data — likely no normal drop source (exceptions: SHiFT, Twitch, promos, gifts, hotfixes, incomplete exports).'
                        };
                    }
                }

                let npcWeaponNote = null;
                if (invCompForLoot && entryHasNpcWeaponBasetag(invCompForLoot.foundPart)) {
                    npcWeaponNote = {
                        message:
                            'Composition includes the npc_weapon basetag — NPC-only item (not normal player-ground loot).'
                    };
                }

                const resolvedValid = moddedRelaxedValidation
                    ? totalParts > 0 && unknownParts.length === 0 && hardFailReasons.length === 0
                    : totalParts > 0 && outcome.valid;

                return {
                    index: index,
                    valid: resolvedValid,
                    error: allErrors.length > 0 ? allErrors.join('; ') : null,
                    details: {
                        inputSerial: deserializedSerial,
                        rootSerial: rootSerial,
                        rootKey: rootDef.key,
                        rootName: rootDef.itembasename || rootDef.key,
                        level: level,
                        seed: seed !== null && !isNaN(seed) ? seed : null,
                        seed2: seed2 !== null && !isNaN(seed2) ? seed2 : null,
                        totalParts: totalParts,
                        partsFound: moddedRelaxedValidation
                            ? validatedParts.filter((p) => p.found).length
                            : validatedParts.filter((p) => p.found && p.isValid).length,
                        partsNotFound: moddedRelaxedValidation ? unknownParts.length : missingParts.length,
                        unknownPartsCount: unknownParts.length,
                        foreignRootPartsCount: foreignRootParts.length,
                        moddedRelaxedValidation: moddedRelaxedValidation,
                        moddedDecodeNote: moddedDecodeNote,
                        missingParts: missingParts.map((p) => ({
                            index: p.index,
                            serial: p.serial,
                            rootSerial: p.rootSerial,
                            reason: p.found ? 'Root serial mismatch' : 'Part not found'
                        })),
                        foreignRootParts: moddedRelaxedValidation
                            ? foreignRootParts.map((p) => ({
                                  index: p.index,
                                  serial: p.serial,
                                  rootSerial: p.rootSerial,
                                  partKey: p.partKey
                              }))
                            : undefined,
                        isModded: isModded,
                        validationClass: outcome.validationClass,
                        classificationReasons: outcome.classificationReasons,
                        orderFail: orderFail,
                        tagDepFail: tagDepFail,
                        exclusionFail: exclusionFail,
                        prereqFail: prereqFail,
                        hardFailReasons: hardFailReasons,
                        reorderedSerial: reorderedSerial,
                        tagErrors: tagErrors,
                        exclusionErrors: exclusionErrors,
                        validationIssues: validationIssues,
                        slotConstraintDiagnostics: slotConstraintDiagnostics,
                        suppressSlotRepairPickers: suppressSlotRepairPickers,
                        missingRequiredSlots: missingSlotsForDetails,
                        extraOptionalSlots: extraSlotsForDetails,
                        validatedParts: includePartDetails ? validatedParts : undefined,
                        lootSourceNote: lootSourceNote,
                        genericLegendaryDropNote: genericLegendaryDropNote,
                        npcWeaponNote: npcWeaponNote
                    }
                };
            } catch (error) {
                return {
                    index: index,
                    valid: false,
                    error: error.message,
                    details: null
                };
            }
        }

        function validateSingleSerial(deserializedSerial, index) {
            return validateSerialCore(deserializedSerial, index, { includePartDetails: false });
        }

        function getValidationClassFromResult(result) {
            const fromDetails = result?.details?.validationClass;
            if (fromDetails) return fromDetails;
            if (result?.valid) return SERIAL_VALIDATION_CLASS.PASS_ALL;
            return SERIAL_VALIDATION_CLASS.HARD_FAIL;
        }

        const __validatorResultCache = new Map();
        const __validatorResultCacheDefaultMax = 100;

        /** During save-editor bulk decode/render, allow a larger LRU cap so unique serials stay cached. */
        function getValidatorResultCacheMax() {
            if (typeof window !== 'undefined' && window.__legitValidatorBulkSession === true) {
                var m = window.__legitValidatorBulkCacheMax | 0;
                if (m > 0) return m;
            }
            return __validatorResultCacheDefaultMax;
        }

        function normalizeValidatorCacheKey(decodedSerial) {
            return decodedSerial == null ? '' : String(decodedSerial).trim();
        }

        window.beginLegitValidatorBulkSession = function (maxEntries) {
            window.__legitValidatorBulkSession = true;
            window.__legitValidatorBulkCacheMax =
                (maxEntries | 0) > 0 ? maxEntries | 0 : 20000;
        };
        window.endLegitValidatorBulkSession = function () {
            window.__legitValidatorBulkSession = false;
            window.__legitValidatorBulkCacheMax = 0;
        };

        function stripNexusPrefix(str) {
            if (str == null || typeof str !== 'string') return str;
            return str.replace(/^NexusSerialized,\s*[0-9A-Fa-f]{32},\s*/i, '').trim() || str;
        }

        /** Returns label for hover/tooltip: display name plus string name in parentheses when both exist, e.g. "Zipper (part_barrel_01_zipgun)". */
        function getPartLabelFromValidatorData(decodedSerial, partIndex) {
            if (!data || !data.rootDefinitions) return '';
            if (!decodedSerial || typeof decodedSerial !== 'string') return '';
            const cacheKey = normalizeValidatorCacheKey(decodedSerial);
            if (!cacheKey) return '';
            let result = __validatorResultCache.get(cacheKey);
            if (!result) {
                result = validateSerialCore(cacheKey, 1, { includePartDetails: true });
                if (__validatorResultCache.size >= getValidatorResultCacheMax()) {
                    const firstKey = __validatorResultCache.keys().next().value;
                    __validatorResultCache.delete(firstKey);
                }
                __validatorResultCache.set(cacheKey, result);
            }
            const details = result?.details;
            if (!details) return '';
            if (partIndex === -1 || partIndex === '-1') {
                const rootName = stripNexusPrefix(details.rootName || '');
                const rootKey = (details.rootKey || '').trim();
                if (rootName && rootKey && rootName !== rootKey) return rootName + ' (' + rootKey + ')';
                return rootName || rootKey || '';
            }
            const validatedParts = Array.isArray(details.validatedParts) ? details.validatedParts : [];
            const idx = typeof partIndex === 'number' ? partIndex : parseInt(partIndex, 10);
            if (Number.isNaN(idx) || idx < 0) return '';
            // partIndex is the 0-based brace match index in the decoded serial; validator part.index is 1-based (e.g. "6" or "4[0]"). Look up by match index so we don't use array position (which can be wrong when a brace expands to multiple parts, e.g. {246:[9 16]}).
            const matchNum = idx + 1;
            const matchStr = String(matchNum);
            let part = validatedParts[idx] || null;
            if (part && (part.index === matchNum || part.index === matchStr)) { /* use it */ }
            else {
                part = validatedParts.find(function (p) {
                    if (!p) return false;
                    const pi = p.index;
                    if (pi === matchNum || pi === matchStr) return true;
                    if (typeof pi === 'string' && pi.replace(/\[\d+\]$/, '') === matchStr) return true;
                    return false;
                }) || null;
            }
            if (!part) return '';
            const displayName = (part.displaySerial || '').trim();
            const partKey = (part.partKey || '').trim();
            if (displayName && partKey && displayName !== partKey) return displayName + ' (' + partKey + ')';
            return displayName || partKey || '';
        }

        function getValidatorDerivedString(decodedSerial) {
            if (!decodedSerial || typeof decodedSerial !== 'string') return '';
            if (!data || !data.rootDefinitions) return decodedSerial.substring(0, 80) || '';
            const cacheKey = normalizeValidatorCacheKey(decodedSerial);
            if (!cacheKey) return decodedSerial.substring(0, 80) || '';
            let result = __validatorResultCache.get(cacheKey);
            if (!result) {
                result = validateSerialCore(cacheKey, 1, { includePartDetails: true });
                if (__validatorResultCache.size >= getValidatorResultCacheMax()) {
                    const firstKey = __validatorResultCache.keys().next().value;
                    __validatorResultCache.delete(firstKey);
                }
                __validatorResultCache.set(cacheKey, result);
            }
            const details = result?.details;
            if (!details) return decodedSerial.substring(0, 80) || '';
            const parts = [];
            parts.push(stripNexusPrefix(details.rootName || details.rootKey || ''));
            const validatedParts = Array.isArray(details.validatedParts) ? details.validatedParts : [];
            validatedParts.forEach(function (p) {
                parts.push((p && (p.displaySerial || p.partKey)) || '');
            });
            return parts.filter(Boolean).join('  ');
        }

        /**
         * Extra searchable tokens for save-inventory filter: part types, keys, UI labels,
         * inv_comp browse names, and underscore-split segments (e.g. slugger from part_barrel_…_slugger).
         */
        function getValidatorInventoryFilterCorpus(decodedSerial) {
            if (!decodedSerial || typeof decodedSerial !== 'string') return '';
            if (!data || !data.rootDefinitions) return '';
            var cacheKey = normalizeValidatorCacheKey(decodedSerial);
            if (!cacheKey) return '';
            var result = __validatorResultCache.get(cacheKey);
            if (!result) {
                result = validateSerialCore(cacheKey, 1, { includePartDetails: true });
                if (__validatorResultCache.size >= getValidatorResultCacheMax()) {
                    var fk = __validatorResultCache.keys().next().value;
                    __validatorResultCache.delete(fk);
                }
                __validatorResultCache.set(cacheKey, result);
            }
            var details = result && result.details;
            if (!details) return '';
            var pieces = [];
            function add(s) {
                var t = s == null ? '' : String(s).trim();
                if (t) pieces.push(t);
            }
            add(stripNexusPrefix(details.rootName || ''));
            if (details.rootKey) add(details.rootKey);
            var validatedParts = Array.isArray(details.validatedParts) ? details.validatedParts : [];
            var invLab =
                typeof window !== 'undefined' && typeof window.getInvCompBrowseLabelFromKey === 'function'
                    ? window.getInvCompBrowseLabelFromKey
                    : null;
            for (var i = 0; i < validatedParts.length; i++) {
                var p = validatedParts[i];
                if (!p) continue;
                add(p.partType);
                add(p.partKey);
                add(p.displaySerial);
                if (typeof getPartLabelFromValidatorData === 'function') {
                    add(getPartLabelFromValidatorData(decodedSerial, i));
                }
                var pk = (p.partKey && String(p.partKey).trim()) || '';
                if (invLab && pk) {
                    var ptLower = String(p.partType || '').toLowerCase();
                    if (ptLower === 'inv_comp' || /^comp_/i.test(pk)) {
                        try {
                            var lb = invLab(pk);
                            if (lb && String(lb).trim() && String(lb).trim() !== pk) add(lb);
                        } catch (e) {
                            /* ignore */
                        }
                    }
                }
                function pushSegs(str) {
                    if (!str || typeof str !== 'string') return;
                    str.split('_').forEach(function (seg) {
                        var u = seg.trim();
                        if (u.length >= 2) add(u);
                    });
                }
                pushSegs(pk);
                pushSegs((p.displaySerial && String(p.displaySerial).trim()) || '');
            }
            var seen = {};
            var out = [];
            for (var j = 0; j < pieces.length; j++) {
                var k = String(pieces[j]).toLowerCase();
                if (!seen[k]) {
                    seen[k] = true;
                    out.push(pieces[j]);
                }
            }
            return out.join(' ');
        }

        /** Save-inventory filter: human derived line plus rich per-part corpus (types, keys, labels, tokens). */
        function getValidatorSearchText(decodedSerial) {
            var derived = typeof getValidatorDerivedString === 'function' ? getValidatorDerivedString(decodedSerial) : '';
            var corpus =
                typeof getValidatorInventoryFilterCorpus === 'function'
                    ? getValidatorInventoryFilterCorpus(decodedSerial)
                    : '';
            var merged = (derived + ' ' + corpus).trim();
            return (merged || decodedSerial || '').toLowerCase();
        }

        function getValidatorCompPartIndex(decodedSerial) {
            if (!data || !data.rootDefinitions) return -1;
            if (!decodedSerial || typeof decodedSerial !== 'string') return -1;
            var cacheKey = normalizeValidatorCacheKey(decodedSerial);
            if (!cacheKey) return -1;
            var result = __validatorResultCache.get(cacheKey);
            if (!result) {
                result = validateSerialCore(cacheKey, 1, { includePartDetails: true });
                if (__validatorResultCache.size >= getValidatorResultCacheMax()) {
                    var firstKey = __validatorResultCache.keys().next().value;
                    __validatorResultCache.delete(firstKey);
                }
                __validatorResultCache.set(cacheKey, result);
            }
            var validatedParts = result && result.details && Array.isArray(result.details.validatedParts) ? result.details.validatedParts : [];
            for (var i = 0; i < validatedParts.length; i++) {
                if (validatedParts[i] && String(validatedParts[i].partType || '').toLowerCase() === 'inv_comp') return i;
            }
            return -1;
        }

        function getValidatorValidStatus(decodedSerial) {
            if (!decodedSerial || typeof decodedSerial !== 'string') return 'Invalid';
            if (!data || !data.rootDefinitions) return 'Invalid';
            var cacheKey = normalizeValidatorCacheKey(decodedSerial);
            if (!cacheKey) return 'Invalid';
            var result = __validatorResultCache.get(cacheKey);
            var cacheHit = !!result;
            if (!result) {
                result = validateSerialCore(cacheKey, 1, { includePartDetails: true });
                if (__validatorResultCache.size >= getValidatorResultCacheMax()) {
                    var firstKey = __validatorResultCache.keys().next().value;
                    __validatorResultCache.delete(firstKey);
                }
                __validatorResultCache.set(cacheKey, result);
            }
            var label = 'Invalid';
            if (result && result.valid === true) label = 'Valid';
            else if (result && result.details && result.details.validationClass === SERIAL_VALIDATION_CLASS.FAIL_ORDER) label = 'Reorder to make legit';
            return label;
        }

        window.getFreshSerialValidationResult = function (decodedSerial, options = {}) {
            if (!decodedSerial || typeof decodedSerial !== 'string') {
                return { valid: false, error: 'Empty serial', details: null };
            }
            const cacheKey = normalizeValidatorCacheKey(decodedSerial);
            if (!cacheKey) {
                return { valid: false, error: 'Empty serial', details: null };
            }
            const result = validateSerialCore(cacheKey, 1, { includePartDetails: true });
            if (options && options.syncCache === false) {
                return result;
            }
            if (__validatorResultCache.size >= getValidatorResultCacheMax()) {
                const firstKey = __validatorResultCache.keys().next().value;
                __validatorResultCache.delete(firstKey);
            }
            __validatorResultCache.set(cacheKey, result);
            return result;
        };

        /** Same labels as getValidatorValidStatus but from an existing validateSerialCore result (no second validation). */
        window.getValidatorStatusLabelFromResult = function (result) {
            if (!result) return 'Invalid';
            if (result.valid === true) return 'Valid';
            if (result.details && result.details.validationClass === SERIAL_VALIDATION_CLASS.FAIL_ORDER) return 'Reorder to make legit';
            return 'Invalid';
        };

        /** For enhancements: return the first validator part that is part_core (name + partIndex), so "Unique part" can show it. */
        function getValidatorEnhancementCorePart(decodedSerial) {
            if (!decodedSerial || typeof decodedSerial !== 'string') return { name: '', partIndex: -1 };
            if (!data || !data.rootDefinitions) return { name: '', partIndex: -1 };
            var cacheKey = normalizeValidatorCacheKey(decodedSerial);
            if (!cacheKey) return { name: '', partIndex: -1 };
            var result = __validatorResultCache.get(cacheKey);
            if (!result) {
                result = validateSerialCore(cacheKey, 1, { includePartDetails: true });
                if (__validatorResultCache.size >= getValidatorResultCacheMax()) {
                    var firstKey = __validatorResultCache.keys().next().value;
                    __validatorResultCache.delete(firstKey);
                }
                __validatorResultCache.set(cacheKey, result);
            }
            var parts = result && result.details && Array.isArray(result.details.validatedParts) ? result.details.validatedParts : [];
            for (var i = 0; i < parts.length; i++) {
                var p = parts[i];
                if (!p || !p.found) continue;
                var pt = (p.partType || '').toLowerCase();
                var pk = (p.partKey || '').toLowerCase();
                if (pt.indexOf('part_core') !== -1 || pk.indexOf('part_core') !== -1) {
                    var name = getPartLabelFromValidatorData(decodedSerial, i);
                    return { name: name || '', partIndex: i };
                }
            }
            return { name: '', partIndex: -1 };
        }

        /** For shields: return the first validator part that is part_body or part_unique (name + partIndex), so "Unique part" can show it when not in simple parts. */
        function getValidatorShieldBodyPart(decodedSerial) {
            if (!decodedSerial || typeof decodedSerial !== 'string') return { name: '', partIndex: -1 };
            if (!data || !data.rootDefinitions) return { name: '', partIndex: -1 };
            var cacheKey = normalizeValidatorCacheKey(decodedSerial);
            if (!cacheKey) return { name: '', partIndex: -1 };
            var result = __validatorResultCache.get(cacheKey);
            if (!result) {
                result = validateSerialCore(cacheKey, 1, { includePartDetails: true });
                if (__validatorResultCache.size >= getValidatorResultCacheMax()) {
                    var firstKey = __validatorResultCache.keys().next().value;
                    __validatorResultCache.delete(firstKey);
                }
                __validatorResultCache.set(cacheKey, result);
            }
            var parts = result && result.details && Array.isArray(result.details.validatedParts) ? result.details.validatedParts : [];
            var partUnique = { name: '', partIndex: -1 };
            var partBody = { name: '', partIndex: -1 };
            for (var i = 0; i < parts.length; i++) {
                var p = parts[i];
                if (!p || !p.found) continue;
                var pt = (p.partType || '').toLowerCase();
                var pk = (p.partKey || '').toLowerCase();
                var isUnique = pt.indexOf('part_unique') !== -1 || pk.indexOf('part_unique') !== -1;
                var isBody = pt.indexOf('part_body') !== -1 || pk.indexOf('part_body') !== -1;
                if (!isUnique && !isBody) continue;
                var name = getPartLabelFromValidatorData(decodedSerial, i);
                if (isUnique && partUnique.partIndex < 0) partUnique = { name: name || '', partIndex: i };
                if (isBody && partBody.partIndex < 0) partBody = { name: name || '', partIndex: i };
            }
            if (partUnique.partIndex >= 0) return partUnique;
            if (partBody.partIndex >= 0) return partBody;
            return { name: '', partIndex: -1 };
        }

        /** For weapons: return the best barrel part from validator (part_barrel, prefer part_unique_barrel over generic, non-licensed), so "Unique part" matches the decoded highlight including comp parts like {1:13}. */
        function getValidatorWeaponBarrelPart(decodedSerial) {
            if (!decodedSerial || typeof decodedSerial !== 'string') return { name: '', partIndex: -1 };
            if (!data || !data.rootDefinitions) return { name: '', partIndex: -1 };
            var cacheKey = normalizeValidatorCacheKey(decodedSerial);
            if (!cacheKey) return { name: '', partIndex: -1 };
            var result = __validatorResultCache.get(cacheKey);
            if (!result) {
                result = validateSerialCore(cacheKey, 1, { includePartDetails: true });
                if (__validatorResultCache.size >= getValidatorResultCacheMax()) {
                    var firstKey = __validatorResultCache.keys().next().value;
                    __validatorResultCache.delete(firstKey);
                }
                __validatorResultCache.set(cacheKey, result);
            }
            var parts = result && result.details && Array.isArray(result.details.validatedParts) ? result.details.validatedParts : [];
            var bestGeneric = { name: '', partIndex: -1, keyLen: -1 };
            var bestUniqueBarrel = { name: '', partIndex: -1, keyLen: -1 };
            for (var i = 0; i < parts.length; i++) {
                var p = parts[i];
                if (!p || !p.found) continue;
                var pt = (p.partType || '').toLowerCase();
                var pk = (p.partKey || '').toLowerCase();
                if (pt.indexOf('part_barrel') === -1 && pk.indexOf('part_barrel') === -1) continue;
                var label = getPartLabelFromValidatorData(decodedSerial, i);
                if (!label) continue;
                var labelLower = label.toLowerCase();
                if (/licensed|visual\s*accessory|no\s*stat\s*changes|cosmetic/i.test(labelLower)) continue;
                var keyLen = (pk || pt).length;
                var isUniqueBarrel = pt.indexOf('part_unique') !== -1 || pk.indexOf('part_unique') !== -1;
                if (isUniqueBarrel) {
                    if (keyLen > bestUniqueBarrel.keyLen) bestUniqueBarrel = { name: label, partIndex: i, keyLen: keyLen };
                } else {
                    if (keyLen > bestGeneric.keyLen) bestGeneric = { name: label, partIndex: i, keyLen: keyLen };
                }
            }
            if (bestUniqueBarrel.partIndex >= 0) return { name: bestUniqueBarrel.name, partIndex: bestUniqueBarrel.partIndex };
            if (bestGeneric.partIndex >= 0) return { name: bestGeneric.name, partIndex: bestGeneric.partIndex };
            return { name: '', partIndex: -1 };
        }

        function getRootDisplayName(rootSerial) {
            if (!data || !data.rootDefinitions) return '';
            var r = typeof rootSerial === 'number' ? rootSerial : parseInt(rootSerial, 10);
            if (Number.isNaN(r)) return '';
            var root = data.rootDefinitions.find(function (x) {
                var sx = x.serialIndex;
                if (sx === null || sx === undefined) return false;
                return (typeof sx === 'number' ? sx : parseInt(sx, 10)) === r;
            });
            if (!root) return '';
            return stripNexusPrefix(root.itembasename || root.key || '');
        }

        function getSubPartDisplayName(rootSerial, subSerial) {
            if (!data || !data.rootDefinitions) return '';
            var r = typeof rootSerial === 'number' ? rootSerial : parseInt(rootSerial, 10);
            var s = typeof subSerial === 'number' ? subSerial : parseInt(subSerial, 10);
            if (Number.isNaN(r) || Number.isNaN(s)) return '';
            var root = data.rootDefinitions.find(function (x) {
                var sx = x.serialIndex;
                if (sx === null || sx === undefined) return false;
                return (typeof sx === 'number' ? sx : parseInt(sx, 10)) === r;
            });
            if (!root || !root.depEntries) return '';
            var dep = root.depEntries.find(function (d) {
                var ds = d.serialIndex;
                if (ds === null || ds === undefined) ds = (d.serialindex && d.serialindex.index) != null ? d.serialindex.index : null;
                if (ds === null || ds === undefined) return false;
                return (typeof ds === 'number' ? ds : parseInt(ds, 10)) === s;
            });
            if (!dep) return '';
            return getDisplayNameForPart(dep, data.invNamePartDisplayNames) || dep.key || '';
        }

        window.getPartLabelFromValidatorData = getPartLabelFromValidatorData;
        window.getRootDisplayName = getRootDisplayName;
        window.getSubPartDisplayName = getSubPartDisplayName;
        window.getValidatorDerivedString = getValidatorDerivedString;
        window.getValidatorInventoryFilterCorpus = getValidatorInventoryFilterCorpus;
        window.getValidatorSearchText = getValidatorSearchText;
        window.getValidatorCompPartIndex = getValidatorCompPartIndex;
        window.getValidatorValidStatus = getValidatorValidStatus;
        /** Save editor / backpack bulk UI: full validateSerialCore result for a human-readable serial. */
        window.getOrValidateSerialResult = function (decodedSerial) {
            const s = decodedSerial == null ? '' : String(decodedSerial).trim();
            if (!s) {
                return { valid: false, error: 'Empty serial', details: null };
            }
            let result = __validatorResultCache.get(s);
            const cacheHit = !!result;
            if (!result) {
                result = validateSerialCore(s, 1, { includePartDetails: true });
                if (__validatorResultCache.size >= getValidatorResultCacheMax()) {
                    const firstKey = __validatorResultCache.keys().next().value;
                    __validatorResultCache.delete(firstKey);
                }
                __validatorResultCache.set(s, result);
            }
            return result;
        };
        /** Save editor / cards: friendly inv_comp label from key when dep entry is missing from Nexus. */
        window.getInvCompBrowseLabelFromKey = function (compKey) {
            if (compKey == null || compKey === '') return '';
            const k = String(compKey).trim();
            if (!k) return '';
            if (!data || !data.invNamePartDisplayNames) return k;
            return (
                getModdedInvCompBrowseDisplayName(
                    { key: k },
                    data.invNamePartDisplayNames
                ) || k
            );
        };
        window.renderSerialValidationDetailsHtml = renderSerialValidationDetailsHtml;
        window.getValidatorEnhancementCorePart = getValidatorEnhancementCorePart;
        window.getValidatorShieldBodyPart = getValidatorShieldBodyPart;
        window.getValidatorWeaponBarrelPart = getValidatorWeaponBarrelPart;

        /** Switch to Legit Builder tab, paste decoded serial into single-line validator, run decode/validate, scroll to results. */
        function openDecodedSerialInLegitValidator(decodedSerial, sourceContext) {
            if (!decodedSerial || typeof decodedSerial !== 'string' || !decodedSerial.trim()) {
                if (typeof alert === 'function') alert('No decoded serial available. Decode inventory first.');
                return;
            }
            if (typeof window.applyInventoryEditSessionFromSourceContext === 'function') {
                window.applyInventoryEditSessionFromSourceContext(sourceContext);
            } else if (sourceContext === null) {
                if (typeof window.clearInventoryEditSession === 'function') {
                    window.clearInventoryEditSession();
                }
            } else if (sourceContext && typeof window.setInventoryEditSession === 'function') {
                window.setInventoryEditSession(
                    sourceContext.slotNum,
                    sourceContext.isEquippedSlot === true,
                    sourceContext.isBankSlot === true
                );
                if (typeof window.syncItemEditorStateFlagForSession === 'function') {
                    window.syncItemEditorStateFlagForSession(
                        sourceContext.slotNum,
                        sourceContext.isEquippedSlot === true,
                        sourceContext.stateFlag
                    );
                }
            }
            if (typeof window.switchTab === 'function') {
                window.switchTab('legit-builder-tab');
            } else if (typeof switchTab === 'function') {
                switchTab('legit-builder-tab');
            }
            window.legitBuilderSetCtx(0);
            if (typeof window.syncLegitBuilderCtxToActiveMainTab === 'function') {
                window.syncLegitBuilderCtxToActiveMainTab();
            }
            const serialInput = lbEl('serialInput');
            if (!serialInput) {
                if (typeof alert === 'function') alert('Validator input not found.');
                return;
            }
            serialInput.value = decodedSerial.trim();
            if (typeof decodeAndValidateSerial === 'function') {
                decodeAndValidateSerial();
            }
            if (typeof window.updateLegitValidatorBackpackButton === 'function') {
                window.updateLegitValidatorBackpackButton();
            }
            function scrollToLegitBuilderAfterSavePush() {
                const tab = document.getElementById('legit-builder-tab');
                if (tab) {
                    try {
                        tab.scrollIntoView({ behavior: 'smooth', block: 'start' });
                    } catch (e) { /* ignore */ }
                }
                const inp = document.getElementById('serialInput');
                if (inp) {
                    try {
                        inp.scrollIntoView({ behavior: 'smooth', block: 'center' });
                    } catch (e) { /* ignore */ }
                }
                const res = document.getElementById('serialValidationResult');
                if (res && res.offsetParent !== null) {
                    try {
                        res.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
                    } catch (e) { /* ignore */ }
                }
            }
            requestAnimationFrame(function () {
                requestAnimationFrame(scrollToLegitBuilderAfterSavePush);
            });
            setTimeout(scrollToLegitBuilderAfterSavePush, 220);
            setTimeout(scrollToLegitBuilderAfterSavePush, 480);
        }

        /** When validation class is FAIL_ORDER, return suggested reordered deserialized serial (else null). */
        function getReorderedSerialForFailOrder(decodedSerial) {
            if (!decodedSerial || typeof decodedSerial !== 'string') return null;
            const trimmed = decodedSerial.trim();
            if (!trimmed || !trimmed.includes('|')) return null;
            try {
                const result = validateSerialCore(trimmed, 1, { includePartDetails: true });
                if (!result || !result.details) return null;
                if (result.details.validationClass !== SERIAL_VALIDATION_CLASS.FAIL_ORDER) return null;
                const rs = result.details.reorderedSerial;
                if (rs && typeof rs === 'string' && rs.trim().length > 0) return rs.trim();
            } catch (e) {
                console.warn('getReorderedSerialForFailOrder:', e);
            }
            return null;
        }

        window.openDecodedSerialInLegitValidator = openDecodedSerialInLegitValidator;
        window.getReorderedSerialForFailOrder = getReorderedSerialForFailOrder;

        /**
         * Item Editor guidelines: slot order matches getPartSelectionOrderFromRoot (same as builder UI).
         * Resolves by composition serialIndex (typeId); falls back to synthetic root from category when needed.
         * When Nexus is not loaded (no rootDefinitions), returns category-specific slot lists aligned with builder fallbacks.
         */
        function getLegitBuilderSlotsForGuidelines(typeId, categoryLower) {
            function filterInvComp(slots) {
                return (slots || []).filter(function (s) {
                    return s && s !== 'inv_comp';
                });
            }
            var n = typeId != null ? Number(typeId) : NaN;
            var roots = data && data.rootDefinitions ? data.rootDefinitions : [];
            if (!Number.isNaN(n) && roots.length) {
                var root = roots.find(function (r) {
                    return Number(r.serialIndex) === n;
                });
                if (root) {
                    return {
                        ok: true,
                        source: 'root',
                        rootKey: root.key,
                        slots: filterInvComp(getPartSelectionOrderFromRoot(root, roots))
                    };
                }
            }
            var cat = String(categoryLower || '').toLowerCase();
            var syn = { key: 'weapon', basetype: '' };
            if (cat.indexOf('heavy') !== -1) {
                syn = { key: 'bor_hw', basetype: 'heavy_weapon' };
            } else if (cat.indexOf('repkit') !== -1 || cat.indexOf('rep kit') !== -1) {
                syn = { key: 'repair_kit', basetype: '' };
            } else if (cat.indexOf('grenade') !== -1 || cat.indexOf('ordnance') !== -1) {
                syn = { key: 'grenade_gadget', basetype: '' };
            } else if (cat.indexOf('class mod') !== -1) {
                syn = { key: 'classmod', basetype: '' };
            } else if (cat.indexOf('shield') !== -1) {
                syn = { key: 'shield', basetype: 'energy_shield' };
            } else if (cat.indexOf('enhancement') !== -1) {
                syn = { key: 'enhancement', basetype: '' };
            }
            if (!roots.length) {
                if (syn.key === 'classmod') {
                    return {
                        ok: true,
                        source: 'synthetic_nodata',
                        rootKey: 'classmod',
                        slots: ['body', 'skills', 'stat234', 'stat2_234', 'statspecial_234', 'firmware234', 'element']
                    };
                }
                if (syn.key === 'enhancement') {
                    return {
                        ok: true,
                        source: 'synthetic_nodata',
                        rootKey: 'enhancement',
                        slots: ['baseBody247', 'legendaryPerks', 'stat_247', 'stat2_247', 'stat3_247', 'firmware247', 'element']
                    };
                }
                if (syn.key === 'grenade_gadget') {
                    return {
                        ok: true,
                        source: 'synthetic_nodata',
                        rootKey: syn.key,
                        slots: ['body', 'parts245', 'payload245', 'stats245', 'augment245', 'firmware245', 'element']
                    };
                }
                if (syn.key === 'repair_kit' || syn.key === 'shield') {
                    return {
                        ok: true,
                        source: 'synthetic_nodata',
                        rootKey: syn.key,
                        slots: filterInvComp(getPartSelectionOrderFromRoot(syn, []))
                    };
                }
                if (syn.key === 'bor_hw' || syn.basetype === 'heavy_weapon') {
                    return {
                        ok: true,
                        source: 'synthetic_nodata',
                        rootKey: syn.key,
                        slots: filterInvComp(getPartSelectionOrderFromRoot(syn, []))
                    };
                }
                return {
                    ok: true,
                    source: 'synthetic_nodata',
                    rootKey: 'weapon',
                    slots: filterInvComp(getPartSelectionOrderFromRoot({ key: 'weapon', basetype: '' }, []))
                };
            }
            return {
                ok: true,
                source: 'synthetic',
                rootKey: syn.key,
                slots: filterInvComp(getPartSelectionOrderFromRoot(syn, roots))
            };
        }

        window.getLegitBuilderSlotsForGuidelines = getLegitBuilderSlotsForGuidelines;

        window.getLegitBuilderSlotOrderForTypeId = function (typeId) {
            if (!data || !data.rootDefinitions || !data.rootDefinitions.length) {
                return { ok: false, reason: 'no_data' };
            }
            var n = Number(typeId);
            if (Number.isNaN(n)) {
                return { ok: false, reason: 'bad_type_id' };
            }
            var root = data.rootDefinitions.find(function (r) {
                return Number(r.serialIndex) === n;
            });
            if (!root) {
                return { ok: false, reason: 'no_root' };
            }
            var slots = getPartSelectionOrderFromRoot(root, data.rootDefinitions).filter(function (s) {
                return s && s !== 'inv_comp';
            });
            return { ok: true, rootKey: root.key, slots: slots };
        };

        // Dev helper: evaluate a corpus of known-legit serials and group failure reasons.
        function runLegitBaselineDiagnostics(inputText, options = {}) {
            if (!data || !data.rootDefinitions) {
                return { ok: false, error: 'No data loaded' };
            }

            const lines = String(inputText || '')
                .split('\n')
                .map(line => line.trim())
                .filter(Boolean);

            const failed = [];
            const groupedFailures = new Map();

            lines.forEach((serial, idx) => {
                const result = validateSerialCore(serial, idx + 1, {
                    includePartDetails: false,
                    enforceCanonicalOrder: !!options.enforceCanonicalOrder
                });
                if (!result.valid) {
                    const issueList = result.details?.validationIssues || [];
                    const tagList = result.details?.tagErrors || [];
                    const exclList = result.details?.exclusionErrors || [];
                    const validationClass = getValidationClassFromResult(result);
                    failed.push({
                        line: idx + 1,
                        serial,
                        error: result.error || 'Invalid',
                        validationClass,
                        issues: issueList,
                        tagErrors: tagList,
                        exclusionErrors: exclList
                    });
                    const signature = `${validationClass}|${result.error || 'Invalid'}|${issueList.slice(0, 2).join(' | ')}|${tagList.slice(0, 2).join(' | ')}|${exclList.slice(0, 2).join(' | ')}`;
                    groupedFailures.set(signature, (groupedFailures.get(signature) || 0) + 1);
                }
            });

            return {
                ok: true,
                total: lines.length,
                passed: lines.length - failed.length,
                failed: failed.length,
                groupedFailures: Array.from(groupedFailures.entries()).map(([signature, count]) => ({ signature, count })),
                samples: failed.slice(0, 20),
                allFailures: failed
            };
        }

        window.runLegitBaselineDiagnostics = runLegitBaselineDiagnostics;

        // Main bulk validation function
        async function bulkValidateSerials() {
            const bulkInputEl = lbEl('bulkSerialInput');
            const bulkInput = bulkInputEl ? bulkInputEl.value.trim() : '';
            const resultDiv = lbEl('bulkValidationResult');
            window.lastBulkValidationSnapshot = null;
            
            if (!bulkInput) {
                showError('Please paste serials to validate');
                if (resultDiv) resultDiv.style.display = 'none';
                return;
            }
            
            if (!data || !data.rootDefinitions) {
                showError('No data loaded. Please load JSON files first.');
                if (resultDiv) resultDiv.style.display = 'none';
                return;
            }

            if (textMightReferencePack4ClassMod(bulkInput)) {
                await ensurePack4NexusLoaded();
            }
            
            // Show loading state
            if (resultDiv) {
                resultDiv.innerHTML = '<div style="padding: 15px; background: #3a3a3a; border-radius: 6px; color: #aaa;">Processing serials... Please wait.</div>';
                resultDiv.style.display = 'block';
            }
            
            try {
                // Extract serials from input
                const extractedSerials = extractSerialsFromInput(bulkInput);
                if (extractedSerials.length === 0) {
                    throw new Error('No valid serials found in input. Please check the format.');
                }
                
                // Separate Base85 and already-deserialized serials
                const base85Serials = extractedSerials.filter(s => s.format === 'base85');
                const deserializedSerials = extractedSerials.filter(s => s.format !== 'base85').map(item => ({
                    original: item.serial,
                    deserialized: item.serial,
                    lineNumber: item.lineNumber,
                    format: 'deserialized',
                    error: null
                }));
                
                // Update loading message (make it clear when API is or isn't called)
                const base85Count = base85Serials.length;
                const deserializedCount = deserializedSerials.length;
                if (resultDiv) {
                    resultDiv.innerHTML = `<div style="padding: 15px; background: #3a3a3a; border-radius: 6px; color: #aaa;">
                        Processing ${extractedSerials.length} serials...<br>
                        ${base85Count > 0
                            ? `Sending ${base85Count} Base85 serial${base85Count > 1 ? 's' : ''} to API (POST request)...`
                            : `<span style="color: #888;">No API request: all ${extractedSerials.length} line${extractedSerials.length !== 1 ? 's' : ''} treated as decoded format.</span>`}
                        ${deserializedCount > 0 && base85Count > 0 ? ` ${deserializedCount} already decoded.` : ''}
                        <div id="bulkProgress" style="margin-top: 10px; font-size: 11px; color: #888;"></div>
                    </div>`;
                }
                
                // Progress callback for deserialization
                const progressCallback = (progress) => {
                    const progressDiv = lbEl('bulkProgress');
                    if (progressDiv && progress) {
                        if (progress.message) {
                            progressDiv.textContent = progress.message;
                        } else if (progress.batch && progress.totalBatches) {
                            progressDiv.textContent = `Processing batch ${progress.batch}/${progress.totalBatches} (${progress.current + (progress.batch.length || 0)}/${progress.total} serials)...`;
                        } else {
                            progressDiv.textContent = `Processing ${progress.current || 0}/${progress.total} serials...`;
                        }
                    }
                    if (typeof window.showLegitActivityPill === 'function' && progress && progress.message) {
                        window.showLegitActivityPill({
                            phase: 'Deserialize',
                            hint: progress.message
                        });
                    }
                };
                
                // Bulk deserialize all Base85 serials (always uses bulk API only)
                if (base85Serials.length > 0) {
                    const bulkDeserializedRaw = await legitBulkDeserializeSerials(base85Serials, progressCallback);
                    if (Array.isArray(bulkDeserializedRaw) && bulkDeserializedRaw.length > 0) {
                        deserializedSerials.push(...bulkDeserializedRaw);
                    } else if (Array.isArray(bulkDeserializedRaw)) {
                        // Empty array from API (e.g. no results object) – treat as one error per serial
                        const errMsg = 'Bulk API returned no results for this serial.';
                        base85Serials.forEach((item) => {
                            deserializedSerials.push({
                                original: item.serial,
                                deserialized: null,
                                lineNumber: item.lineNumber,
                                format: 'base85',
                                error: errMsg
                            });
                        });
                    } else {
                        const typeGot = bulkDeserializedRaw === null ? 'null' : typeof bulkDeserializedRaw;
                        console.warn('[Bulk validator] legitBulkDeserializeSerials did not return an array. type:', typeGot, 'value:', bulkDeserializedRaw);
                        const hint = (typeof bulkDeserializedRaw === 'undefined')
                            ? ' No request may have been sent — try hard-refresh (Ctrl+Shift+R), load the page over HTTP/HTTPS (not file://), and check Network tab for a POST to api.php.'
                            : '';
                        const errMsg = 'Bulk deserialization failed (unexpected response: ' + typeGot + '). Check console for details.' + hint;
                        base85Serials.forEach((item) => {
                            deserializedSerials.push({
                                original: item.serial,
                                deserialized: null,
                                lineNumber: item.lineNumber,
                                format: 'base85',
                                error: errMsg
                            });
                        });
                    }
                }
                
                // Clear progress message
                const progressDiv = lbEl('bulkProgress');
                if (progressDiv) {
                    progressDiv.textContent = 'Validating serials...';
                }
                
                // Sort by line number to maintain original order
                deserializedSerials.sort((a, b) => a.lineNumber - b.lineNumber);

                // Replace bulk textarea with deserialized content so we validate decoded strings and user sees what was decoded
                if (bulkInputEl) {
                    const lines = deserializedSerials.map((item) => {
                        if (item.deserialized) return item.deserialized.trim();
                        if (item.error) return `# Line ${item.lineNumber}: ${item.error}`;
                        return item.original || '';
                    });
                    bulkInputEl.value = lines.join('\n');
                }
                
                // Validate each serial (using deserialized strings from memory)
                const validationResults = [];
                const yieldBulkValidateUi = function () {
                    return new Promise(function (resolve) {
                        if (typeof requestAnimationFrame === 'function') {
                            requestAnimationFrame(function () { setTimeout(resolve, 0); });
                        } else {
                            setTimeout(resolve, 0);
                        }
                    });
                };
                const bulkRowHint = function (row) {
                    if (!row) return '';
                    var o = row.original != null ? String(row.original).trim() : '';
                    if (o) {
                        if (o.length <= 24) return 'L' + row.lineNumber + ' ' + o;
                        return 'L' + row.lineNumber + ' ' + (o.charAt(0) === '@' ? o.slice(0, 14) + '…' : o.slice(0, 18) + '…');
                    }
                    var d = row.deserialized != null ? String(row.deserialized).trim() : '';
                    if (!d) return 'L' + row.lineNumber;
                    return 'L' + row.lineNumber + ' ' + (d.length <= 20 ? d : d.slice(0, 18) + '…');
                };
                const nVal = deserializedSerials.length;
                const yieldEveryBulk = nVal > 8000 ? 25 : 50;
                for (let i = 0; i < nVal; i++) {
                    const item = deserializedSerials[i];
                    if (item.error) {
                        validationResults.push({
                            index: i + 1,
                            lineNumber: item.lineNumber,
                            original: item.original,
                            deserialized: item.deserialized || null,
                            valid: false,
                            error: item.error,
                            details: null
                        });
                    } else if (item.deserialized) {
                        const validation = validateSingleSerial(item.deserialized, i + 1);
                        validation.lineNumber = item.lineNumber;
                        validation.original = item.original;
                        validation.deserialized = item.deserialized;
                        validationResults.push(validation);
                    } else {
                        validationResults.push({
                            index: i + 1,
                            lineNumber: item.lineNumber,
                            original: item.original,
                            deserialized: null,
                            valid: false,
                            error: 'Could not deserialize or validate serial',
                            details: null
                        });
                    }
                    const isLastVal = i + 1 === nVal;
                    if ((i + 1) % yieldEveryBulk === 0 || isLastVal) {
                        if (typeof window.showLegitActivityPill === 'function') {
                            window.showLegitActivityPill({
                                phase: 'Validating',
                                current: i + 1,
                                total: nVal,
                                hint: bulkRowHint(item)
                            });
                        }
                    }
                    if ((i + 1) % yieldEveryBulk === 0 && !isLastVal) {
                        await yieldBulkValidateUi();
                    }
                }

                if (typeof window.hideLegitActivityPill === 'function') {
                    window.hideLegitActivityPill();
                }

                // Generate results HTML
                const validCount = validationResults.filter(r => r.valid).length;
                const invalidCount = validationResults.length - validCount;
                window.lastBulkValidationSnapshot = {
                    inputText: bulkInputEl ? String(bulkInputEl.value || '') : '',
                    results: validationResults.map(result => ({
                        lineNumber: result.lineNumber,
                        valid: !!result.valid
                    }))
                };
                const validationClassOrder = [
                    SERIAL_VALIDATION_CLASS.PASS_ALL,
                    SERIAL_VALIDATION_CLASS.FAIL_ORDER,
                    SERIAL_VALIDATION_CLASS.FAIL_TAG_DEPS,
                    SERIAL_VALIDATION_CLASS.HARD_FAIL
                ];
                const classCounts = new Map(validationClassOrder.map(label => [label, 0]));
                validationResults.forEach(result => {
                    const resultClass = getValidationClassFromResult(result);
                    classCounts.set(resultClass, (classCounts.get(resultClass) || 0) + 1);
                });
                
                let html = `
                    <div style="padding: 15px; background: #2a2a2a; border-radius: 6px; border-left: 3px solid ${validCount === validationResults.length ? '#4CAF50' : '#ff4444'};">
                        <h4 style="color: ${validCount === validationResults.length ? '#4CAF50' : '#ffaa44'}; margin-bottom: 15px;">
                            ${validCount === validationResults.length ? '✓' : '✗'} Validation Complete
                        </h4>
                        <div style="margin-bottom: 15px; padding: 10px; background: #3a3a3a; border-radius: 4px;">
                            <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(150px, 1fr)); gap: 10px;">
                                <div style="text-align: center;">
                                    <div style="font-size: 24px; font-weight: bold; color: #4a9eff;">${validationResults.length}</div>
                                    <div style="font-size: 12px; color: #aaa;">Total Serials</div>
                                </div>
                                <div style="text-align: center;">
                                    <div style="font-size: 24px; font-weight: bold; color: #4CAF50;">${validCount}</div>
                                    <div style="font-size: 12px; color: #aaa;">Valid</div>
                                </div>
                                <div style="text-align: center;">
                                    <div style="font-size: 24px; font-weight: bold; color: #ff4444;">${invalidCount}</div>
                                    <div style="font-size: 12px; color: #aaa;">Invalid</div>
                                </div>
                                ${validationClassOrder.map(classLabel => {
                                    const classMeta = getSerialValidationClassPresentation(classLabel);
                                    const count = classCounts.get(classLabel) || 0;
                                    return `
                                        <div style="text-align: center;">
                                            <div style="font-size: 18px; font-weight: bold; color: ${classMeta.color};">${count}</div>
                                            <div style="font-size: 11px; color: #aaa;">${classMeta.label}</div>
                                        </div>
                                    `;
                                }).join('')}
                            </div>
                        </div>
                        <div style="max-height: 500px; overflow-y: auto; border: 1px solid #404040; border-radius: 4px;">
                            <table style="width: 100%; border-collapse: collapse; font-size: 12px;">
                                <thead>
                                    <tr style="background: #3a3a3a; position: sticky; top: 0;">
                                        <th style="padding: 8px; text-align: left; border-bottom: 1px solid #404040; color: #4a9eff;">#</th>
                                        <th style="padding: 8px; text-align: left; border-bottom: 1px solid #404040; color: #4a9eff;">Line</th>
                                        <th style="padding: 8px; text-align: left; border-bottom: 1px solid #404040; color: #4a9eff;">Status</th>
                                        <th style="padding: 8px; text-align: left; border-bottom: 1px solid #404040; color: #4a9eff;">Checks</th>
                                        <th style="padding: 8px; text-align: left; border-bottom: 1px solid #404040; color: #4a9eff;">Root</th>
                                        <th style="padding: 8px; text-align: left; border-bottom: 1px solid #404040; color: #4a9eff;">Parts</th>
                                        <th style="padding: 8px; text-align: left; border-bottom: 1px solid #404040; color: #4a9eff;">Error</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    ${validationResults.map((result, idx) => {
                                        const resultClass = getValidationClassFromResult(result);
                                        const classMeta = getSerialValidationClassPresentation(resultClass);
                                        const statusColor = classMeta.color;
                                        const statusIcon = classMeta.icon;
                                        const rootName = result.details?.rootName || 'N/A';
                                        const partsInfo = result.details ? `${result.details.partsFound}/${result.details.totalParts}` : 'N/A';
                                        
                                        // Build error text with tag/exclusion errors
                                        let errorParts = [];
                                        if (result.details?.moddedRelaxedValidation) {
                                            const u = result.details.unknownPartsCount || 0;
                                            const f = result.details.foreignRootPartsCount || 0;
                                            if (u > 0) {
                                                errorParts.push(`${u} unknown token(s)`);
                                            }
                                            if (f > 0 && u > 0) {
                                                errorParts.push(`${f} cross-root`);
                                            }
                                        } else if (result.details?.missingParts?.length > 0) {
                                            errorParts.push(`${result.details.missingParts.length} part(s) not found`);
                                        }
                                        if (result.details?.isModded) {
                                            if (result.details.exclusionErrors?.length > 0) {
                                                errorParts.push(`Exclusion: ${result.details.exclusionErrors.join('; ')}`);
                                            }
                                            if (result.details.tagErrors?.length > 0) {
                                                errorParts.push(`Tags: ${result.details.tagErrors.join('; ')}`);
                                            }
                                            if (result.details.validationIssues?.length > 0) {
                                                errorParts.push(...result.details.validationIssues);
                                            }
                                        }
                                        const errorText = errorParts.length > 0 ? errorParts.join(' | ') : '';
                                        
                                        const rowId = `bulk-row-${idx}`;
                                        return `
                                            <tr data-bulk-row-id="${rowId}" data-result-idx="${idx}" title="Click to open modal details" style="border-bottom: 1px solid #333; cursor: pointer; ${idx % 2 === 0 ? 'background: #2a2a2a;' : ''}">
                                                <td style="padding: 8px; color: #aaa;">${result.index}</td>
                                                <td style="padding: 8px; color: #aaa;">
                                                    <span data-expand-icon="${rowId}" style="display: inline-block; width: 14px; color: #4a9eff; font-weight: bold;">↗</span>
                                                    ${result.lineNumber}
                                                    <div style="font-size: 9px; color: #7f96a9; margin-top: 2px;">Click-to-open modal</div>
                                                </td>
                                                <td style="padding: 8px;">
                                                    <span style="color: ${statusColor}; font-weight: bold;">${statusIcon}</span>
                                                    <span style="color: ${statusColor}; font-size: 10px; margin-left: 4px;">${classMeta.label}</span>
                                                </td>
                                                <td style="padding: 8px;">
                                                    ${renderValidationChecksHtml(result.details || {}, resultClass, { compact: true })}
                                                </td>
                                                <td style="padding: 8px; color: #ccc;">${rootName}</td>
                                                <td style="padding: 8px; color: #ccc;">${partsInfo}</td>
                                                <td style="padding: 8px; color: #ffaa44; font-size: 11px;">${errorText}</td>
                                            </tr>
                                        `;
                                    }).join('')}
                                </tbody>
                            </table>
                        </div>
                        ${(() => {
                            // Generate missing parts summary
                            const itemsWithMissingParts = validationResults.filter(r => 
                                r.details && r.details.missingParts && r.details.missingParts.length > 0
                            );
                            
                            if (itemsWithMissingParts.length === 0) {
                                return '';
                            }
                            
                            // Group missing parts by serial for easy decoding
                            const missingPartsBySerial = new Map();
                            let totalMissingParts = 0;
                            
                            itemsWithMissingParts.forEach(result => {
                                result.details.missingParts.forEach(part => {
                                    // Handle both regular parts and base comp parts (rootSerial:serial format)
                                    let serialKey;
                                    if (part.rootSerial !== null && part.rootSerial !== undefined) {
                                        serialKey = `${part.rootSerial}:${part.serial}`;
                                    } else {
                                        serialKey = part.serial.toString();
                                    }
                                    
                                    if (!missingPartsBySerial.has(serialKey)) {
                                        missingPartsBySerial.set(serialKey, []);
                                    }
                                    missingPartsBySerial.get(serialKey).push({
                                        line: result.lineNumber,
                                        root: result.details.rootName,
                                        rootSerial: result.details.rootSerial,
                                        index: part.index,
                                        reason: part.reason
                                    });
                                    totalMissingParts++;
                                });
                            });
                            
                            // Generate unique missing serials list for decoding (one per line format)
                            const uniqueMissingSerials = Array.from(missingPartsBySerial.keys()).sort((a, b) => {
                                // Sort numerically if both are numbers, otherwise alphabetically
                                const aNum = parseInt(a.split(':')[0]);
                                const bNum = parseInt(b.split(':')[0]);
                                if (!isNaN(aNum) && !isNaN(bNum)) {
                                    return aNum - bNum;
                                }
                                return a.localeCompare(b);
                            });
                            
                            return `
                                <div style="margin-top: 20px; padding: 15px; background: #2a2a2a; border-radius: 6px; border-left: 3px solid #ffaa44;">
                                    <h4 style="color: #ffaa44; margin-bottom: 15px;">
                                        ⚠ Missing Parts Summary (${itemsWithMissingParts.length} items, ${totalMissingParts} missing parts)
                                    </h4>
                                    
                                    <div style="margin-bottom: 15px;">
                                        <div style="font-size: 12px; color: #aaa; margin-bottom: 8px;">
                                            <strong>Missing Part Serials (${uniqueMissingSerials.length} unique):</strong>
                                        </div>
                                        <div style="padding: 10px; background: #1a1a1a; border-radius: 4px; font-family: 'Consolas', 'Monaco', monospace; font-size: 11px; color: #4a9eff; word-break: break-all; max-height: 150px; overflow-y: auto;">
                                            ${uniqueMissingSerials.join(', ')}
                                        </div>
                                        <button onclick="navigator.clipboard.writeText('${uniqueMissingSerials.join('\\n')}')" 
                                                style="margin-top: 8px; padding: 4px 8px; background: #3a3a3a; border: 1px solid #555; color: #4a9eff; cursor: pointer; font-size: 11px; border-radius: 3px;">
                                            Copy All Missing Serials
                                        </button>
                                    </div>
                                    
                                    <div style="max-height: 400px; overflow-y: auto; border: 1px solid #404040; border-radius: 4px;">
                                        <table style="width: 100%; border-collapse: collapse; font-size: 11px;">
                                            <thead>
                                                <tr style="background: #3a3a3a; position: sticky; top: 0;">
                                                    <th style="padding: 6px; text-align: left; border-bottom: 1px solid #404040; color: #4a9eff;">Line</th>
                                                    <th style="padding: 6px; text-align: left; border-bottom: 1px solid #404040; color: #4a9eff;">Root</th>
                                                    <th style="padding: 6px; text-align: left; border-bottom: 1px solid #404040; color: #4a9eff;">Root Serial</th>
                                                    <th style="padding: 6px; text-align: left; border-bottom: 1px solid #404040; color: #4a9eff;">Parts Found</th>
                                                    <th style="padding: 6px; text-align: left; border-bottom: 1px solid #404040; color: #4a9eff;">Missing Parts</th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                ${itemsWithMissingParts.map((result, idx) => {
                                                    const missingPartsList = result.details.missingParts.map(p => {
                                                        if (p.rootSerial !== null && p.rootSerial !== undefined) {
                                                            return `Part #${p.index}: Root ${p.rootSerial}:${p.serial}`;
                                                        }
                                                        return `Part #${p.index}: Serial ${p.serial}`;
                                                    }).join('<br>');
                                                    
                                                    return `
                                                        <tr style="border-bottom: 1px solid #333; ${idx % 2 === 0 ? 'background: #2a2a2a;' : ''}">
                                                            <td style="padding: 6px; color: #aaa;">${result.lineNumber}</td>
                                                            <td style="padding: 6px; color: #ccc;">${result.details.rootName}</td>
                                                            <td style="padding: 6px; color: #888; font-family: monospace;">${result.details.rootSerial}</td>
                                                            <td style="padding: 6px; color: #4CAF50;">${result.details.partsFound}/${result.details.totalParts}</td>
                                                            <td style="padding: 6px; color: #ffaa44; font-size: 10px;">${missingPartsList}</td>
                                                        </tr>
                                                    `;
                                                }).join('')}
                                            </tbody>
                                        </table>
                                    </div>
                                </div>
                            `;
                        })()}
                    </div>
                `;
                
                resultDiv.innerHTML = html;

                const bulkDetailHtmlCache = new Map();
                const bulkInspectPayloadByRowId = new Map();
                const bulkInspectValidationByRowId = new Map();

                const buildBulkDetailHtml = (resultIdx) => {
                    const rowKey = `bulk-row-${resultIdx}`;
                    const result = validationResults[resultIdx];
                    if (!result) {
                        bulkInspectPayloadByRowId.delete(rowKey);
                        bulkInspectValidationByRowId.delete(rowKey);
                        return `<div style="padding: 10px; color: #ffaa44;">No details available for this row.</div>`;
                    }

                    let detailedValidation = result;
                    const hasPartDetails = Array.isArray(result?.details?.validatedParts);
                    if (!hasPartDetails) {
                        const sourceSerial = result.deserialized || result.original;
                        if (!sourceSerial) {
                            bulkInspectPayloadByRowId.delete(rowKey);
                            bulkInspectValidationByRowId.delete(rowKey);
                            return `<div style="padding: 10px; color: #ffaa44;">No serial payload available for detailed view.</div>`;
                        }
                        const bulkMiRelaxedInsp = !!(
                            resultDiv &&
                            resultDiv.closest &&
                            resultDiv.closest('#item-editor-tab')
                        );
                        detailedValidation = validateSerialCore(sourceSerial, result.index || (resultIdx + 1), {
                            includePartDetails: true,
                            moddedRelaxedValidation: bulkMiRelaxedInsp
                        });
                        detailedValidation.lineNumber = result.lineNumber;
                        detailedValidation.original = result.original;
                        detailedValidation.deserialized = result.deserialized || sourceSerial;
                    }

                    const det = detailedValidation.details || {};
                    const dSerial = det.inputSerial != null && String(det.inputSerial).trim() !== ''
                        ? String(det.inputSerial).trim()
                        : String(detailedValidation.deserialized || result.deserialized || '').trim();
                    const orig = (result.original != null ? String(result.original) : '').trim();
                    const base85ForPayload = orig && isBase85Format(orig) ? (orig.startsWith('@') ? orig : '@' + orig) : null;
                    const payload = { deserialized: dSerial, base85: base85ForPayload };
                    bulkInspectPayloadByRowId.set(rowKey, payload);
                    bulkInspectValidationByRowId.set(rowKey, detailedValidation);

                    return renderSerialValidationDetailsHtml(detailedValidation, {
                        source: 'bulk',
                        data: data,
                        translationOnly: !!(resultDiv && resultDiv.closest && resultDiv.closest('#item-editor-tab'))
                    });
                };

                resultDiv.querySelectorAll('tr[data-bulk-row-id]').forEach(row => {
                    row.addEventListener('click', () => {
                        const rowId = row.getAttribute('data-bulk-row-id');
                        const resultIdxRaw = row.getAttribute('data-result-idx');
                        const resultIdx = Number.parseInt(resultIdxRaw, 10);
                        if (!rowId || Number.isNaN(resultIdx)) return;

                        if (!bulkDetailHtmlCache.has(rowId)) {
                            bulkDetailHtmlCache.set(rowId, buildBulkDetailHtml(resultIdx));
                        }
                        const pl = bulkInspectPayloadByRowId.get(rowId);
                        window.__legitBackpackInspectPayload = pl
                            ? { deserialized: pl.deserialized, base85: pl.base85 }
                            : { deserialized: '', base85: null };
                        window.__legitInspectValidation = bulkInspectValidationByRowId.get(rowId) || null;
                        openValidationDetailsModal(bulkDetailHtmlCache.get(rowId));
                    });
                });
                resultDiv.style.display = 'block';
                
            } catch (error) {
                if (typeof window.hideLegitActivityPill === 'function') {
                    window.hideLegitActivityPill();
                }
                resultDiv.innerHTML = `
                    <div style="padding: 15px; background: #3a1a1a; border-radius: 6px; border-left: 3px solid #ff4444;">
                        <h4 style="color: #ff4444; margin-bottom: 10px;">✗ Validation Error</h4>
                        <div style="font-size: 12px; color: #ffaa44;">${error.message}</div>
                    </div>
                `;
                resultDiv.style.display = 'block';
            }
        }

        function searchRoot() {
            const si = lbEl('searchInput');
            if (!si) return;
            const searchTerm = si.value.trim();
            if (!searchTerm) {
                showError('Please enter a search term');
                return;
            }

            if (!data || !data.rootDefinitions) {
                showError('No data loaded');
                return;
            }

            const results = data.rootDefinitions.filter(root => {
                const key = (root.key || '').toLowerCase();
                const serial = (root.serialIndex || '').toString();
                const inv = (root.inv || '').toLowerCase();
                const search = searchTerm.toLowerCase();
                return key.includes(search) || serial === search || inv.includes(search);
            });

            const container = lbEl('searchContent');
            if (!container) return;
            const searchUseCompact = !!(container.closest && container.closest('#item-editor-tab'));
            if (results.length === 0) {
                container.innerHTML = '<div class="loading">No results found</div>';
            } else {
                let html = `<div style="margin-bottom: 15px; color: #aaa;">Found ${results.length} result(s)</div>`;
                results.forEach((root, index) => {
                    html += createRootItemHTML(
                        root,
                        index,
                        searchUseCompact ? { useCompactDepTables: true } : undefined
                    );
                });
                container.innerHTML = html;
                if (searchUseCompact) {
                    miInitOpenModdedPickers(container);
                }

                // Add click handlers for expand/collapse
                container.querySelectorAll('.root-header-clickable').forEach(header => {
                    header.addEventListener('click', function() {
                        const rootItem = this.closest('.root-item');
                        const content = rootItem.querySelector('.root-content');
                        rootItem.classList.toggle('expanded');
                        content.classList.toggle('collapsed');
                    });
                });
            }

            switchLegitTab('search');
        }

        function clearSearch() {
            const si = lbEl('searchInput');
            if (si) si.value = '';
            const tf = lbEl('typeFilter');
            if (tf) tf.value = '';
            if (data) {
                displayRootDefinitions();
            }
        }

        function findPartEntryForBuilderSelection(root, partKey) {
            if (!partKey) return null;
            const baseGenericRoot = getBaseGenericRootForPartLookup(root, data);
            return findPartDepEntryForRootContext(root, partKey, data, baseGenericRoot);
        }

        function buildBuilderValidatedParts(root, selectionKeys) {
            const validatedParts = [];
            const selectedKeys = Array.from(selectionKeys || []);

            selectedKeys.forEach((selectedKey, idx) => {
                const foundPart = findPartEntryForBuilderSelection(root, selectedKey);
                validatedParts.push({
                    index: idx + 1,
                    selectionOrder: idx,
                    partKey: selectedKey,
                    partType: normalizeDepTableName(foundPart?.depTableName),
                    found: !!foundPart,
                    foundPart: foundPart || null,
                    isValid: !!foundPart
                });
            });

            return validatedParts;
        }

        function togglePartSelection(rootKey, partKey, partId, event) {
            // Prevent dropdowns from closing - stop all event propagation
            if (event) {
                event.stopPropagation();
                event.preventDefault();
                event.stopImmediatePropagation();
                const hit = window.legitBuilderInstances.find(function (i) {
                    return i.mount && event.target && i.mount.contains(event.target);
                });
                if (hit) window.__lbCtx = hit;
            }

            if (data && data.rootDefinitions) {
                const _rkRoot = data.rootDefinitions.find(r => r.key === rootKey);
                if (rootDefNeedsPack4Nexus(_rkRoot)) {
                    void ensurePack4NexusLoaded();
                }
            }

            if (!_sp().has(rootKey)) {
                _sp().set(rootKey, new Set());
            }
            
            const rootSelections = _sp().get(rootKey);
            const isComp = partKey.toLowerCase().startsWith('comp');
            const wasCompSelected = Array.from(rootSelections).some(key => key.toLowerCase().startsWith('comp'));
            
            // If selecting a comp, clear other comp selections (only one comp at a time)
            if (isComp && !rootSelections.has(partKey)) {
                const otherComps = Array.from(rootSelections).filter(key => 
                    key.toLowerCase().startsWith('comp')
                );
                otherComps.forEach(key => rootSelections.delete(key));
            }
            
            // Check max count enforcement BEFORE toggling selection (only for non-comp parts; skipped in Modded tab)
            if (!isComp && !(window.__lbCtx && window.__lbCtx.relaxedPicker)) {
                // Get table name from the clicked element (try multiple methods)
                let partElement = null;
                let tableName = null;
                
                // Get root first for fallback lookup
                let root = null;
                if (data && data.rootDefinitions) {
                    root = data.rootDefinitions.find(r => r.key === rootKey);
                }
                
                if (event && event.target) {
                    // Try closest first (handles clicks on child elements)
                    partElement = event.target.closest('[data-table-name]');
                    tableName = partElement?.getAttribute('data-table-name');
                    
                    // Fallback: if closest didn't work, try direct parent or current target
                    if (!tableName) {
                        const depItem = event.target.closest('.dep-item[data-table-name]');
                        tableName = depItem?.getAttribute('data-table-name');
                    }
                }
                
                // Final fallback: look up the part in root's depEntries to find its table name
                // This MUST happen before the enforcement check so tableName is always found
                if (!tableName && root && root.depEntries) {
                    const dep = root.depEntries.find(d => d.key === partKey);
                    if (dep) {
                        tableName = dep.depTableName;
                    }
                }
                
                const compRules = (root && tableName && tableName !== 'inv_comp') ? getSelectedCompRules(root) : null;
                const selectedComp = compRules ? compRules.compKey : null;
                const basePartCounts = (root && tableName && tableName !== 'inv_comp')
                    ? getBaseGenericRootPartCounts(root, selectedComp)
                    : null;

                // Block selecting parts that are not allowed by normalized slot constraints.
                if (!rootSelections.has(partKey) && root && tableName && tableName !== 'inv_comp' && compRules) {
                    const partEntryForConstraint =
                        root.depEntries?.find(dep => dep.key === partKey) ||
                        (data && data.rootDefinitions
                            ? data.rootDefinitions.flatMap(r => r.depEntries || []).find(dep => dep.key === partKey)
                            : null);
                    const partItemForConstraint = {
                        ...(partEntryForConstraint || {}),
                        key: partKey
                    };
                    if (!isPartAllowedForTable(partItemForConstraint, compRules, tableName)) {
                        alert(`Cannot select "${partKey}" in ${tableName}: this part is not allowed by the selected component rules.`);
                        return false;
                    }
                }

                // Prevent deselecting a singleton-required constrained slot below its required minimum.
                if (rootSelections.has(partKey) && root && tableName && tableName !== 'inv_comp' && compRules) {
                    const allowedForSlot = getAllowedPartsForTable(compRules, tableName);
                    if (allowedForSlot && allowedForSlot.size === 1 && allowedForSlot.has(partKey)) {
                        let partCountInfo = null;
                        if (compRules.partCounts && compRules.partCounts.has(tableName)) {
                            partCountInfo = compRules.partCounts.get(tableName);
                        } else if (basePartCounts && basePartCounts.has(tableName)) {
                            partCountInfo = basePartCounts.get(tableName);
                        }
                        const requiredMin = resolveRequiredMinForSlot(partCountInfo || {}, root, compRules, tableName);
                        if (requiredMin > 0) {
                            alert(`Cannot deselect "${partKey}" from ${tableName}: this slot is forced by component selection rules.`);
                            return false;
                        }
                    }
                }
                
                // Always check max count enforcement when trying to select a new part
                if (!rootSelections.has(partKey)) {
                    // Only enforce for actual part types (not inv_comp)
                    // CRITICAL: Must have root and tableName for enforcement to work
                    if (root && tableName && tableName !== 'inv_comp') {
                                let partCountInfo = null;
                                
                                if (compRules && compRules.partCounts && compRules.partCounts.has(tableName)) {
                                    partCountInfo = compRules.partCounts.get(tableName);
                                } else if (basePartCounts && basePartCounts.has(tableName)) {
                                    partCountInfo = basePartCounts.get(tableName);
                                }
                                
                                // Always enforce max count, even if it's an assumed/default value
                                // effectiveMax is always set (defaults to 1 if not specified)
                                let maxCountToEnforce = null;
                                
                                if (partCountInfo) {
                                    // Use effectiveMax (which includes assumed values)
                                    // effectiveMax is always set, even when displayMax is null
                                    maxCountToEnforce = partCountInfo.max;
                                    
                                    // Safety check: if max is somehow undefined/null, use effectiveMin or default to 1
                                    if (maxCountToEnforce === null || maxCountToEnforce === undefined) {
                                        // If we have a min, use that as fallback, otherwise default to 1
                                        maxCountToEnforce = (partCountInfo.min !== null && partCountInfo.min !== undefined) ? partCountInfo.min : 1;
                                    }
                                } else {
                                    // No partCountInfo - default to max 1 (standard assumption)
                                    maxCountToEnforce = 1;
                                }
                                
                                // Always enforce max count - we should always have a value at this point
                                // This ensures assumed counts (like min 0, max assumed 1) are enforced
                                // Final safety: if somehow maxCountToEnforce is still null/undefined, default to 1
                                if (maxCountToEnforce === null || maxCountToEnforce === undefined) {
                                    maxCountToEnforce = 1;
                                }
                                
                                // ALWAYS enforce max count - no exceptions
                                // Count currently selected parts in this table for this root
                                // NOTE: Parts may be from the current root OR from the base generic root (for universal parts)
                                let selectedInTable = 0;
                                
                                // Build a map of table names for selected keys
                                // Check ALL roots' depEntries to find table names (parts can be from any root)
                                const keyToTableMap = new Map();
                                
                                // First, check current root
                                if (root.depEntries) {
                                    root.depEntries.forEach(dep => {
                                        if (dep.key && dep.depTableName) {
                                            keyToTableMap.set(dep.key, dep.depTableName);
                                        }
                                    });
                                }
                                
                                // Then check ALL other roots (for universal parts like firmware, stat groups, etc.)
                                if (data && data.rootDefinitions) {
                                    data.rootDefinitions.forEach(r => {
                                        if (r.depEntries && r !== root) {
                                            r.depEntries.forEach(dep => {
                                                if (dep.key && dep.depTableName && !keyToTableMap.has(dep.key)) {
                                                    keyToTableMap.set(dep.key, dep.depTableName);
                                                }
                                            });
                                        }
                                    });
                                }
                                
                                // Count selected keys that match the current tableName
                                rootSelections.forEach(selectedKey => {
                                    // Skip comp keys (they're in inv_comp table, not in part type tables)
                                    if (selectedKey.toLowerCase().startsWith('comp')) {
                                        return;
                                    }
                                    
                                    // Check if this key belongs to the current table
                                    const keyTable = keyToTableMap.get(selectedKey);
                                    if (keyTable === tableName) {
                                        selectedInTable++;
                                    } else if (!keyTable) {
                                        // Key not found in any depEntries - use pattern matching as fallback
                                        // This handles cases where keys might not be in depEntries but match table patterns
                                        const keyLower = selectedKey.toLowerCase();
                                        
                                        // Pattern matching for common table types
                                        if (tableName === 'firmware' && keyLower.includes('firmware')) {
                                            selectedInTable++;
                                        } else if ((tableName === 'class_mod_body' || tableName === 'body') && 
                                                   (keyLower.includes('body') || selectedKey.startsWith('body_'))) {
                                            selectedInTable++;
                                        } else if (tableName === 'passive_points' && 
                                                   (keyLower.includes('passive') || selectedKey.startsWith('passive_'))) {
                                            selectedInTable++;
                                        } else if ((tableName === 'stat_group1' || tableName === 'stat_group2' || tableName === 'stat_group3') && 
                                                   selectedKey.startsWith('stat_')) {
                                            // For stat groups, count any key starting with stat_
                                            // Note: This is a fallback - ideally keys should be in depEntries
                                            selectedInTable++;
                                        } else if (tableName === 'core_augment' && 
                                                   (keyLower.includes('core') || selectedKey.startsWith('core_'))) {
                                            selectedInTable++;
                                        } else if (tableName === 'payload' && 
                                                   (keyLower.includes('payload') || selectedKey.startsWith('payload_'))) {
                                            selectedInTable++;
                                        } else if (tableName === 'action_skill_mod' && 
                                                   (keyLower.includes('action') || selectedKey.startsWith('action_'))) {
                                            selectedInTable++;
                                        } else if (tableName === 'special_passive' && 
                                                   (keyLower.includes('special') || selectedKey.startsWith('special_'))) {
                                            selectedInTable++;
                                        } else if ((tableName === 'Element' || tableName === 'element') && 
                                                   (keyLower.includes('element') || selectedKey.startsWith('element_'))) {
                                            selectedInTable++;
                                        }
                                        // Add more patterns as needed for other part types
                                    }
                                });
                                
                                // If we're at or above max count, prevent selection
                                // CRITICAL: This check must happen BEFORE toggling the selection
                                if (selectedInTable >= maxCountToEnforce) {
                                    // Show message to user
                                    const displayMax = partCountInfo?.displayMax;
                                    const effectiveMax = partCountInfo?.max ?? maxCountToEnforce;
                                    
                                    // Show appropriate message based on whether it's explicit or assumed
                                    let message = `Cannot select more than ${effectiveMax} part(s) in ${tableName}. `;
                                    if (displayMax === null || displayMax === undefined) {
                                        message += `(Maximum count: ${effectiveMax} - assumed from base composition rules)`;
                                    } else {
                                        message += `Maximum count reached.`;
                                    }
                                    // Blocking selection - max count reached
                                    alert(message);
                                    return false; // Block selection
                                }
                            }

                            if (compRules) {
                                const nextSelection = new Set(rootSelections);
                                nextSelection.add(partKey);
                                const nextValidatedParts = buildBuilderValidatedParts(root, nextSelection);
                                const nextViolations = enforcePartTagMaxRules(nextValidatedParts, compRules, formatValidationTags);
                                if (nextViolations.length > 0) {
                                    const candidateOrder = nextValidatedParts.length - 1;
                                    const triggeredViolation = nextViolations.find(violation =>
                                        Array.isArray(violation?.overflowParts) &&
                                        violation.overflowParts.some(part => part.selectionOrder === candidateOrder)
                                    );

                                    if (triggeredViolation) {
                                        alert(
                                            `Cannot select "${partKey}": tag "${triggeredViolation.tag}" exceeds maximum ${triggeredViolation.max} (found ${triggeredViolation.found}).`
                                        );
                                        return false;
                                    }
                                }
                            }
                        }
            }
            
            // Toggle selection
            if (rootSelections.has(partKey)) {
                rootSelections.delete(partKey);
            } else {
                rootSelections.add(partKey);
            }
            
            const isNowCompSelected = Array.from(rootSelections).some(key => key.toLowerCase().startsWith('comp'));
            
            // If no selections left for this root, remove the entry
            if (rootSelections.size === 0) {
                _sp().delete(rootKey);
            }

            // No pinned signature: new random seed (1–4096) when selecting a component (inv_comp)
            if (isComp && rootSelections.has(partKey)) {
                try {
                    if (!localStorage.getItem('permanentSeed')) {
                        const _rand = Math.floor(Math.random() * 4096) + 1;
                        const _outSeed = lbEl('outputSeed');
                        const _itemSeed = document.getElementById('floating-tab-loot-signature') || document.getElementById('seed');
                        if (_outSeed) _outSeed.value = String(_rand);
                        if (_itemSeed) _itemSeed.value = String(_rand);
                    }
                } catch (_e) {}
            }
            
            // Update visual state
            const _rootsHost = lbEl('rootsContent');
            const partElement = _rootsHost
                ? Array.from(_rootsHost.querySelectorAll('[data-part-id]')).find(el => el.getAttribute('data-part-id') === partId)
                : null;
            if (partElement) {
                if (rootSelections.has(partKey)) {
                    partElement.classList.add('selected');
                } else {
                    partElement.classList.remove('selected');
                }
            }
            
            // Update the output panel
            updateSelectedPartsOutput();
            
            // Re-render if comp selection changed OR if any part was selected (tags affect filtering)
            // We need to update filtering when tags change, but preserve expanded state
            if (data && data.rootDefinitions) {
                const root = data.rootDefinitions.find(r => r.key === rootKey);
                if (root) {
                    const _rc = lbEl('rootsContent');
                    // Find the root item in DOM (scoped to this builder pane)
                    const rootItem = _rc ? Array.from(_rc.querySelectorAll('.root-item')).find(item => {
                        const itemRootKey = item.querySelector('[data-root-key]')?.getAttribute('data-root-key');
                        return itemRootKey === rootKey;
                    }) : null;
                    
                    if (rootItem) {
                        // Save expanded state
                        const wasExpanded = rootItem.classList.contains('expanded');
                        const depSearchState = captureDepTableSearchState(rootItem);
                        
                        // Get parent for replacement
                        const parent = rootItem.parentElement;
                        
                        // Find the index for this root
                        const rootIndex = data.rootDefinitions.findIndex(r => r.key === rootKey);
                        
                        // Create new HTML
                        const refreshUseCompact = !!(_rc && _rc.closest && _rc.closest('#item-editor-tab'));
                        const newHTML = createRootItemHTML(
                            root,
                            rootIndex,
                            refreshUseCompact ? { useCompactDepTables: true } : undefined
                        );
                        const tempDiv = document.createElement('div');
                        tempDiv.innerHTML = newHTML;
                        const newElement = tempDiv.firstElementChild;
                        
                        // Replace old element
                        parent.replaceChild(newElement, rootItem);
                        
                        // Restore expanded state
                        if (wasExpanded) {
                            newElement.classList.add('expanded');
                            const content = newElement.querySelector('.root-content');
                            if (content) {
                                content.classList.remove('collapsed');
                            }
                        }

                        restoreDepTableSearchState(newElement, depSearchState);

                        if (refreshUseCompact) {
                            miInitOpenModdedPickers(newElement);
                        }
                        
                        // Re-attach click handler for root header
                        const header = newElement.querySelector('.root-header-clickable');
                        if (header) {
                            header.addEventListener('click', function(e) {
                                e.stopPropagation();
                                const rootItem = this.closest('.root-item');
                                const content = rootItem.querySelector('.root-content');
                                rootItem.classList.toggle('expanded');
                                content.classList.toggle('collapsed');
                            });
                        }
                        
                        // Re-attach click handlers for all selectable parts
                        newElement.querySelectorAll('.dep-item.selectable').forEach(partEl => {
                            const partRootKey = partEl.getAttribute('data-root-key');
                            const partKey = partEl.getAttribute('data-part-key');
                            const partId = partEl.getAttribute('data-part-id');
                            partEl.addEventListener('click', function(e) {
                                togglePartSelection(partRootKey, partKey, partId, e);
                            });
                        });
                    }
                }
            }
        }

        function updateSelectedPartsOutput() {
            if (window.__lbCtx && window.__lbCtx.relaxedPicker) {
                const spOut = lbEl('selectedPartsOutput');
                if (spOut) spOut.innerHTML = '';
                updateFinalOutput();
                if (typeof updateMiItemPartsBreakdown === 'function') {
                    updateMiItemPartsBreakdown();
                }
                return;
            }
            const outputDiv = lbEl('selectedPartsOutput');
            if (!outputDiv) return;

            if (_sp().size === 0) {
                outputDiv.innerHTML = `
                    <div style="padding: 10px; background: #1a1a1a; border-radius: 4px; margin-bottom: 10px;">
                        No parts selected. Click on parts in root definitions to select them.
                    </div>
                `;
                return;
            }

            let html = '';
            
            // Helper function to format tags
            const formatTags = (tags) => {
                if (!tags || !Array.isArray(tags)) return [];
                return tags.map(tag => {
                    if (typeof tag === 'object' && tag !== null) {
                        const values = Object.values(tag);
                        return values[0] || Object.keys(tag)[0] || '';
                    }
                    return tag;
                }).filter(Boolean);
            };
            
            // Get all root definitions for context
            const rootDefs = data && data.rootDefinitions ? data.rootDefinitions : [];
            const orderedRoots = getRootsInBuilderDisplayOrder().filter(r => _sp().has(r.key));
            
            orderedRoots.forEach(root => {
                const partKeys = _sp().get(root.key);
                const rootKey = root.key;
                const rootName = root ? (root.itembasename || root.key) : rootKey;
                const compRules = root ? getSelectedCompRules(root) : null;
                
                // Find base generic root (e.g., "Enhancement" for "cov_enhancement", or "Weapon" for "bor_sm")
                let baseGenericRoot = null;
                if (root && data && data.rootDefinitions) {
                    const baseItemTypePatterns = ['weapon', 'enhancement', 'shield', 'repair_kit', 'grenade_gadget', 'heavy_weapon_gadget', 'turret_gadget', 'terminal_gadget', 'grenade', 'artifact', 'classmod'];
                    let baseItemType = '';
                    const rootKeyLower = (root.key || '').toLowerCase();
                    
                    // Check if this is a weapon by checking for weapon-specific part types
                    // Manufacturer weapons like "bor_sm" don't have "weapon" in their key, but they have weapon parts
                    const weaponPartTypes = ['body', 'barrel', 'magazine', 'scope', 'grip', 'foregrip', 'body_ele', 'secondary_ele'];
                    const hasWeaponParts = root.depEntries && root.depEntries.some(dep => 
                        dep.depTableName && weaponPartTypes.includes(dep.depTableName.toLowerCase())
                    );
                    
                    // Special handling: terminal_combat, terminal_*, turret_* should map to terminal_gadget/turret_gadget
                    if (rootKeyLower.includes('terminal_') && !rootKeyLower.includes('terminal_gadget')) {
                        // Map terminal_combat, terminal_*, etc. to terminal_gadget
                        if (rootKeyLower.includes('terminal_combat') || rootKeyLower.includes('terminal_')) {
                            baseItemType = 'terminal_gadget';
                        }
                    } else if (rootKeyLower.includes('turret_') && !rootKeyLower.includes('turret_gadget')) {
                        // Map turret_combat, turret_*, etc. to turret_gadget
                        if (rootKeyLower.includes('turret_combat') || rootKeyLower.includes('turret_')) {
                            baseItemType = 'turret_gadget';
                        }
                    } else {
                        for (const pattern of baseItemTypePatterns) {
                            if (rootKeyLower.includes(pattern) && rootKeyLower !== pattern) {
                                const match = rootKeyLower.match(new RegExp(`(?:\\w+_)?(${pattern.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')})(?:\\w+)?$`, 'i'));
                                if (match && match[1]) {
                                    baseItemType = match[1].toLowerCase();
                                    break;
                                }
                            } else if (rootKeyLower === pattern) {
                                baseItemType = pattern;
                                break;
                            }
                        }
                    }
                    
                    // If no pattern matched but this root has weapon parts, it's likely a manufacturer weapon
                    // Find the generic weapon root (key "weapon" or serial 1)
                    if (!baseItemType && hasWeaponParts) {
                        baseItemType = 'weapon';
                    }
                    
                    if (baseItemType) {
                        baseGenericRoot = data.rootDefinitions.find(r => {
                            const rKey = (r.key || '').toLowerCase();
                            return rKey === baseItemType;
                        });
                        
                        // If not found by key, try to find by serial (weapon root is usually serial 1)
                        if (!baseGenericRoot && baseItemType === 'weapon') {
                            baseGenericRoot = data.rootDefinitions.find(r => {
                                const rSerial = r.serialIndex;
                                return rSerial === 1 || rSerial === '1';
                            });
                        }
                    }
                }

                const resolvePartEntryForSelectedKey = (selectedPartKey) => {
                    const crossP = parseMiCrossPartRef(selectedPartKey);
                    if (crossP && data && data.rootDefinitions) {
                        const srRoot = data.rootDefinitions.find(r => r.key === crossP.sourceRootKey);
                        const hit = srRoot && srRoot.depEntries && srRoot.depEntries.find(dep => dep.key === crossP.partKey);
                        if (hit) return hit;
                    }
                    if (root && root.depEntries) {
                        const localEntry = root.depEntries.find(dep => dep.key === selectedPartKey);
                        if (localEntry) return localEntry;
                    }
                    if (baseGenericRoot && baseGenericRoot.depEntries) {
                        const baseEntry = baseGenericRoot.depEntries.find(dep => dep.key === selectedPartKey);
                        if (baseEntry) return baseEntry;
                    }
                    return findPartEntryForBuilderSelection(root, selectedPartKey);
                };

                const miInstOut = window.legitBuilderInstances && window.legitBuilderInstances[1];
                const useMiLinesExpanded = miPartLinesHasSyncedRoot(miInstOut, rootKey);
                const miLines = useMiLinesExpanded ? miInstOut.miPartLines.get(rootKey) : null;
                const expandedPartKeys = useMiLinesExpanded
                    ? (() => {
                        const out = [];
                        (miLines || []).forEach(({ partKey, qty }) => {
                            if (!partKey) return;
                            expandModdedMiLineToPartKeys(partKey, qty).forEach(k => out.push(k));
                        });
                        return out;
                    })()
                    : Array.from(partKeys);

                const selectedTagCounts = new Map();
                expandedPartKeys.forEach(selectedPartKey => {
                    const partEntry = resolvePartEntryForSelectedKey(selectedPartKey);
                    const partType = normalizeDepTableName(partEntry?.depTableName);
                    if (!partEntry || partType === 'inv_comp') return;
                    const addTags = formatTags(partEntry.addtags || []).map(tag => String(tag).toLowerCase());
                    addTags.forEach(tag => {
                        const currentCount = selectedTagCounts.get(tag) || 0;
                        selectedTagCounts.set(tag, currentCount + 1);
                    });
                });
                
                // Find selected comp
                const selectedComps = Array.from(partKeys).filter(key => 
                    key.toLowerCase().startsWith('comp') || 
                    (root && root.depEntries?.some(dep => dep.key === key && dep.depTableName === 'inv_comp'))
                );
                
                // Get root serial index for display
                const rootSerialIndex = root ? root.serialIndex : null;
                
                html += `
                    <div style="margin-bottom: 15px; padding: 12px; background: #1a1a1a; border-radius: 6px; border-left: 3px solid #4CAF50;">
                        <div style="font-weight: bold; color: #4CAF50; margin-bottom: 8px; font-size: 14px;">${rootKey}</div>
                        ${rootSerialIndex !== null ? `<div style="font-size: 11px; color: #888; margin-bottom: 4px;">${rootSerialIndex}</div>` : ''}
                        <div style="font-size: 11px; color: #888; margin-bottom: 8px;">${rootName}</div>
                `;
                
                // Show current tag pool
                if (compRules && compRules.tagPool.size > 0) {
                    html += `
                        <div style="margin-bottom: 10px; padding: 8px; background: #1a2a1a; border-radius: 4px; border-left: 2px solid #4CAF50;">
                            <div style="font-size: 11px; color: #4CAF50; font-weight: bold; margin-bottom: 6px;">Current Tag Pool:</div>
                            <div style="display: flex; flex-wrap: wrap; gap: 4px; margin-top: 4px;">
                                ${Array.from(compRules.tagPool).sort().map(tag => `
                                    <span style="padding: 2px 5px; background: #2a4a2a; border-radius: 3px; font-size: 9px; color: #4CAF50; font-weight: bold;">${tag}</span>
                                `).join('')}
                            </div>
                        </div>
                    `;
                }

                // Live slot requirements: compRules.partCounts vs current selection (same source as builder slot rules)
                if (compRules && compRules.partCounts && compRules.partCounts.size > 0) {
                    const selectedCountBySlot = new Map();
                    expandedPartKeys.forEach(selectedPartKey => {
                        const partEntry = resolvePartEntryForSelectedKey(selectedPartKey);
                        const tn = normalizeDepTableName(partEntry?.depTableName);
                        if (!partEntry || !tn || tn === 'unknown') return;
                        selectedCountBySlot.set(tn, (selectedCountBySlot.get(tn) || 0) + 1);
                    });

                    const partSelectionOrderSlots = getPartSelectionOrderFromRoot(root, data?.rootDefinitions);
                    const partSelectionOrderLowerSlots = partSelectionOrderSlots.map(s => (s || '').toLowerCase());
                    const normalizeSlotKeyForPartCounts = (slotName) => {
                        const n = normalizeDepTableName(slotName);
                        if (n && n !== 'unknown') return n;
                        return String(slotName || '').toLowerCase();
                    };

                    const sortedPartCountEntries = Array.from(compRules.partCounts.entries())
                        .filter(([, info]) => {
                            const minC = info.min != null ? info.min : 0;
                            const maxC = info.max != null ? info.max : 0;
                            return !(minC === 0 && maxC === 0);
                        })
                        .sort((a, b) => {
                            const orderA = partSelectionOrderLowerSlots.indexOf(normalizeSlotKeyForPartCounts(a[0]));
                            const orderB = partSelectionOrderLowerSlots.indexOf(normalizeSlotKeyForPartCounts(b[0]));
                            if (orderA !== -1 && orderB !== -1) return orderA - orderB;
                            if (orderA !== -1) return -1;
                            if (orderB !== -1) return 1;
                            return a[0].localeCompare(b[0]);
                        });

                    if (sortedPartCountEntries.length > 0) {
                        let requiredSlotsTotal = 0;
                        let requiredSlotsOk = 0;
                        let anyOverfilled = false;
                        const slotRowsHtml = sortedPartCountEntries.map(([slotName, countInfo]) => {
                            const minR = countInfo.min != null ? countInfo.min : 0;
                            const maxR = countInfo.max != null ? countInfo.max : minR;
                            const slotNorm = normalizeSlotKeyForPartCounts(slotName);
                            const n = selectedCountBySlot.get(slotNorm) || 0;
                            const ok = n >= minR && n <= maxR;
                            if (minR > 0) {
                                requiredSlotsTotal++;
                                if (ok) requiredSlotsOk++;
                            }
                            if (n > maxR) anyOverfilled = true;
                            const rangeLabel = minR === maxR ? String(minR) : `${minR}–${maxR}`;
                            let statusColor = '#4CAF50';
                            let statusBg = '#2a4a2a';
                            if (n < minR) {
                                statusColor = '#FFA500';
                                statusBg = '#4a3a1a';
                            } else if (n > maxR) {
                                statusColor = '#e57373';
                                statusBg = '#4a2a2a';
                            }
                            return `
                            <div style="display: flex; align-items: center; justify-content: space-between; gap: 8px; margin-bottom: 4px; padding: 4px 6px; background: ${statusBg}; border-radius: 3px;">
                                <span style="font-size: 10px; color: #ccc;">${slotName}</span>
                                <span style="font-size: 10px; color: ${statusColor}; font-weight: bold;">${n} / ${rangeLabel}</span>
                            </div>`;
                        }).join('');

                        const summaryOkColor = requiredSlotsTotal > 0 && requiredSlotsOk === requiredSlotsTotal ? '#4CAF50' : '#FFA500';
                        const summaryExtra = anyOverfilled ? ' <span style="color:#e57373;">(some slots overfilled)</span>' : '';
                        const summaryLine = requiredSlotsTotal > 0
                            ? `Required slots satisfied: <span style="color: ${summaryOkColor}; font-weight: bold;">${requiredSlotsOk}/${requiredSlotsTotal}</span>${summaryExtra}`
                            : `<span style="color:#888;">No required slots (min &gt; 0) in part rules.</span>${summaryExtra}`;

                        html += `
                        <div style="margin-bottom: 10px; padding: 8px; background: #1a2a2a; border-radius: 4px; border-left: 2px solid #6bcf7f;">
                            <div style="font-size: 11px; color: #6bcf7f; font-weight: bold; margin-bottom: 4px;">Slot requirements (live)</div>
                            <div style="font-size: 10px; color: #aaa; margin-bottom: 6px;">${summaryLine}</div>
                            ${slotRowsHtml}
                        </div>`;
                    }
                }

                if (selectedTagCounts.size > 0) {
                    html += `
                        <div style="margin-bottom: 10px; padding: 8px; background: #1d2433; border-radius: 4px; border-left: 2px solid #4a9eff;">
                            <div style="font-size: 11px; color: #4a9eff; font-weight: bold; margin-bottom: 6px;">Selected Tag Counts:</div>
                            <div style="display: flex; flex-wrap: wrap; gap: 4px; margin-top: 4px;">
                                ${Array.from(selectedTagCounts.entries())
                                    .sort((a, b) => a[0].localeCompare(b[0]))
                                    .map(([tag, count]) => `
                                        <span style="padding: 2px 6px; background: #2a3550; border-radius: 3px; font-size: 9px; color: #9ec8ff; font-weight: bold;">${tag} x${count}</span>
                                    `).join('')}
                            </div>
                        </div>
                    `;
                }
                
                // Show forced parts from comp
                if (compRules && compRules.forcedParts.size > 0) {
                    html += `
                        <div style="margin-bottom: 10px; padding: 8px; background: #2a1a1a; border-radius: 4px; border-left: 2px solid #FFA500;">
                            <div style="font-size: 11px; color: #FFA500; font-weight: bold; margin-bottom: 6px;">⚠ Forced Parts (from comp rules):</div>
                    `;
                    
                    // Use root's part order from parttypes / ncs (matches builder and game)
                    const partSelectionOrderForced = getPartSelectionOrderFromRoot(root, data?.rootDefinitions);
                    const partSelectionOrderLowerForced = partSelectionOrderForced.map(s => (s || '').toLowerCase());
                    
                    // Sort forced parts by root's part selection order
                    const sortedForcedParts = Array.from(compRules.forcedParts.entries()).sort((a, b) => {
                        const orderA = partSelectionOrderLowerForced.indexOf(normalizeDepTableName(a[0]));
                        const orderB = partSelectionOrderLowerForced.indexOf(normalizeDepTableName(b[0]));
                        
                        // If both are in the order list, sort by their position
                        if (orderA !== -1 && orderB !== -1) {
                            return orderA - orderB;
                        }
                        // If only A is in the order list, it comes first
                        if (orderA !== -1) {
                            return -1;
                        }
                        // If only B is in the order list, it comes first
                        if (orderB !== -1) {
                            return 1;
                        }
                        // If neither is in the order list, maintain alphabetical order
                        return a[0].localeCompare(b[0]);
                    });
                    
                    sortedForcedParts.forEach(([tableName, forcedPartKeys]) => {
                        html += `
                            <div style="margin-bottom: 4px;">
                                <div style="font-size: 10px; color: #FFA500; margin-bottom: 2px;">${tableName}:</div>
                                <div style="display: flex; flex-wrap: wrap; gap: 4px; margin-left: 10px;">
                                    ${Array.from(forcedPartKeys).map(partKey => {
                                        const isSelected = partKeys.has(partKey) ||
                                            Array.from(partKeys).some(sk => sk && partKey && sk.toLowerCase() === partKey.toLowerCase()) ||
                                            Array.from(partKeys).some(sk => {
                                                const e = resolvePartEntryForSelectedKey(sk);
                                                return e && partKey && (e.key || '').toLowerCase() === partKey.toLowerCase();
                                            });
                                        return `<span style="padding: 2px 5px; background: ${isSelected ? '#4CAF50' : '#5a3a1a'}; border-radius: 3px; font-size: 9px; color: #fff; ${isSelected ? 'font-weight: bold;' : ''}">${partKey}${isSelected ? ' ✓' : ''}</span>`;
                                    }).join('')}
                                </div>
                            </div>
                        `;
                    });
                    
                    html += `</div>`;
                }

                if (miLines && miLines.length > 0) {
                    const rkEsc = String(rootKey).replace(/&/g, '&amp;').replace(/"/g, '&quot;');
                    html += `
                        <div style="margin-bottom: 12px; padding: 10px; background: #121820; border-radius: 6px; border-left: 3px solid #9ccc65;">
                            <div style="font-size: 11px; color: #9ccc65; font-weight: bold; margin-bottom: 6px;">Serial order (Modded)</div>
                            <div style="font-size: 10px; color: #888; margin-bottom: 8px;">Drag rows to reorder tokens in the output string. Quantity repeats that part; for passive_point rows whose key ends in <code style="color:#9ec8ff;">_tier_1</code>, quantity walks tiers 1→5 then repeats tier 5.</div>
                            <div class="mi-serial-order-list" data-root-key="${rkEsc}" style="display: flex; flex-direction: column; gap: 4px;">`;
                    miLines.forEach(line => {
                        if (!line || !line.partKey) return;
                        const partEntry = resolvePartEntryForSelectedKey(line.partKey);
                        const disp =
                            (partEntry &&
                                (getDisplayNameForPart(partEntry, data && data.invNamePartDisplayNames) || line.partKey)) ||
                            line.partKey;
                        const tn = line.tableName || partEntry?.depTableName || '?';
                        const q = line.qty != null ? line.qty : 1;
                        const b64 = miEncodeSerialOrderLineB64(line);
                        if (!b64) return;
                        const qtyLab = q > 1 ? ` <span style="font-size:9px;color:#9ec8ff;">×${q}</span>` : '';
                        html += `
                            <div class="mi-serial-order-item" draggable="true" data-line-b64="${b64}" style="display:flex;align-items:center;gap:8px;padding:6px 8px;background:#1e1e1e;border:1px solid #333;border-radius:4px;cursor:grab;user-select:none;">
                                <span style="color:#666;font-size:12px;letter-spacing:-2px;" title="Drag">⠿</span>
                                <span style="font-size:9px;color:#888;min-width:72px;">${escapeLegitHtml(String(tn))}</span>
                                <span style="font-size:11px;color:#eee;font-weight:600;flex:1;">${escapeLegitHtml(String(disp))}</span>
                                ${qtyLab}
                            </div>`;
                    });
                    html += `</div></div>`;
                }
                
                // Show selected parts grouped by table (modded: respect line qty)
                const partsByTable = {};
                if (miLines && miLines.length > 0) {
                    miLines.forEach(({ tableName: tn, partKey, qty }) => {
                        if (!partKey) return;
                        const partEntry = resolvePartEntryForSelectedKey(partKey);
                        const tableName = (partEntry && partEntry.depTableName) || tn || 'Unknown';
                        if (!partsByTable[tableName]) {
                            partsByTable[tableName] = [];
                        }
                        const q = Math.min(99, Math.max(1, parseInt(qty, 10) || 1));
                        partsByTable[tableName].push({ key: partKey, entry: partEntry, qty: q });
                    });
                } else {
                    Array.from(partKeys).forEach(partKey => {
                        let tableName = 'Unknown';
                        const partEntry = resolvePartEntryForSelectedKey(partKey);
                        if (partEntry) {
                            tableName = partEntry.depTableName || 'Unknown';
                        }
                        if (!partsByTable[tableName]) {
                            partsByTable[tableName] = [];
                        }
                        partsByTable[tableName].push({ key: partKey, entry: partEntry, qty: 1 });
                    });
                }
                
                // Use root's part order from parttypes / ncs (matches builder and order defined in ncs files)
                const partSelectionOrder = getPartSelectionOrderFromRoot(root, data?.rootDefinitions);
                const partSelectionOrderLower = partSelectionOrder.map(s => (s || '').toLowerCase());
                
                // Sort partsByTable entries according to root's part selection order
                const sortedPartsByTable = Object.entries(partsByTable).sort((a, b) => {
                    const normalizedA = normalizeDepTableName(a[0]);
                    const normalizedB = normalizeDepTableName(b[0]);
                    const orderA = partSelectionOrderLower.indexOf(normalizedA);
                    const orderB = partSelectionOrderLower.indexOf(normalizedB);
                    
                    // If both are in the order list, sort by their position
                    if (orderA !== -1 && orderB !== -1) {
                        return orderA - orderB;
                    }
                    // If only A is in the order list, it comes first
                    if (orderA !== -1) {
                        return -1;
                    }
                    // If only B is in the order list, it comes first
                    if (orderB !== -1) {
                        return 1;
                    }
                    // If neither is in the order list, maintain alphabetical order
                    return a[0].localeCompare(b[0]);
                });
                
                sortedPartsByTable.forEach(([tableName, parts]) => {
                    html += `
                        <div style="margin-bottom: 6px; padding: 6px; background: #0f0f0f; border-radius: 3px;">
                            <div style="font-size: 10px; color: #FFA500; font-weight: bold; margin-bottom: 4px;">${tableName}</div>
                            ${parts.map(({ key: partKey, entry: partEntry, qty: lineQty = 1 }) => {
                                const qtyLabel = lineQty > 1 ? ` <span style="font-size:9px;color:#9ec8ff;font-weight:600;">×${lineQty}</span>` : '';
                                const addTags = formatTags(partEntry?.addtags || []);
                                const depTags = formatTags(partEntry?.dependencytags || []);
                                const exclTags = formatTags(partEntry?.exclusiontags || []);
                                const invCompLootCtxMi = String(tableName || '').toLowerCase() === 'inv_comp'
                                    ? { rootKey: (partEntry?.rootKey && String(partEntry.rootKey).trim()) || rootKey }
                                    : {};
                                const invCompBadges = String(tableName || '').toLowerCase() === 'inv_comp'
                                    ? getInvCompBadges(partEntry, { compact: true }, data, invCompLootCtxMi)
                                    : [];
                                
                                // Get serial numbers
                                const serialIndex = partEntry?.serialIndex || partEntry?.serialindex?.index || null;
                                const rootSerialIndex = partEntry?.rootSerialIndex || null;
                                
                                // Check if part comes from base generic root (not current root)
                                const isFromBaseComp = baseGenericRoot && 
                                                      partEntry?.rootKey && 
                                                      partEntry.rootKey !== rootKey &&
                                                      partEntry.rootKey === baseGenericRoot.key;
                                
                                // Format serial display: {rootSerial:subSerial} for base comp parts, (Serial: subSerial) for others
                                let serialDisplay = '';
                                if (isFromBaseComp && rootSerialIndex !== null && serialIndex !== null) {
                                    serialDisplay = ` <span style="font-size: 8px; color: #888; font-weight: normal;">{${rootSerialIndex}:${serialIndex}}</span>`;
                                } else if (serialIndex !== null) {
                                    serialDisplay = ` <span style="font-size: 8px; color: #888; font-weight: normal;">(Serial: ${serialIndex})</span>`;
                                }
                                
                                // Show multiplierLinks and multipliers if available
                                let multiplierDebug = '';
                                if (partEntry?.multiplierLinks && partEntry.multiplierLinks.length > 0) {
                                    multiplierDebug += `<div style="margin-top: 4px; padding: 4px; background: #1a1a1a; border-radius: 3px; border-left: 2px solid #9C27B0;">`;
                                    multiplierDebug += `<div style="font-size: 8px; color: #9C27B0; font-weight: bold; margin-bottom: 2px;">Multiplier Links:</div>`;
                                    partEntry.multiplierLinks.forEach((link, idx) => {
                                        multiplierDebug += `<div style="font-size: 7px; color: #aaa; margin-left: 6px; margin-bottom: 1px;">`;
                                        multiplierDebug += `${idx + 1}. ${link.structCandidate || 'N/A'} | ${link.statKey || 'N/A'} | Row: ${link.rowname || 'N/A'}`;
                                        multiplierDebug += `</div>`;
                                    });
                                    multiplierDebug += `</div>`;
                                }
                                
                                if (partEntry?.multipliers) {
                                    const multipliers = partEntry.multipliers;
                                    const hasMultipliers = (multipliers.Value && Object.keys(multipliers.Value).length > 0) ||
                                                          (multipliers.Scale && Object.keys(multipliers.Scale).length > 0) ||
                                                          (multipliers.Add && Object.keys(multipliers.Add).length > 0);
                                    if (hasMultipliers) {
                                        multiplierDebug += `<div style="margin-top: 4px; padding: 4px; background: #1a1a1a; border-radius: 3px; border-left: 2px solid #00BCD4;">`;
                                        multiplierDebug += `<div style="font-size: 8px; color: #00BCD4; font-weight: bold; margin-bottom: 2px;">Multipliers:</div>`;
                                        
                                        if (multipliers.Value && Object.keys(multipliers.Value).length > 0) {
                                            multiplierDebug += `<div style="font-size: 7px; color: #aaa; margin-left: 6px; margin-bottom: 1px;">Value: ${Object.keys(multipliers.Value).join(', ')}</div>`;
                                        }
                                        if (multipliers.Scale && Object.keys(multipliers.Scale).length > 0) {
                                            multiplierDebug += `<div style="font-size: 7px; color: #aaa; margin-left: 6px; margin-bottom: 1px;">Scale: ${Object.keys(multipliers.Scale).join(', ')}</div>`;
                                        }
                                        if (multipliers.Add && Object.keys(multipliers.Add).length > 0) {
                                            multiplierDebug += `<div style="font-size: 7px; color: #aaa; margin-left: 6px; margin-bottom: 1px;">Add: ${Object.keys(multipliers.Add).join(', ')}</div>`;
                                        }
                                        multiplierDebug += `</div>`;
                                    }
                                }
                                
                                return `
                                    <div style="margin-bottom: 4px; padding: 4px; background: #2a2a2a; border-radius: 3px;">
                                        <div style="font-size: 10px; color: #fff; font-weight: bold; margin-bottom: 2px;">${partKey}${qtyLabel}${serialDisplay}</div>
                                        ${renderBadgePills(invCompBadges, { marginTop: 0, marginBottom: 2, fontSize: 8 })}
                                        ${addTags.length > 0 ? `<div style="font-size: 8px; color: #4CAF50;">+ ${addTags.join(', ')}</div>` : ''}
                                        ${depTags.length > 0 ? `<div style="font-size: 8px; color: #FFA500;">→ ${depTags.join(', ')}</div>` : ''}
                                        ${exclTags.length > 0 ? `<div style="font-size: 8px; color: #ff4444;">✗ ${exclTags.join(', ')}</div>` : ''}
                                        ${multiplierDebug}
                                    </div>
                                `;
                            }).join('')}
                        </div>
                    `;
                });
                
                // Show build-level combined multipliers for this root
                const selectedPartItems = getSelectedPartItems(rootKey);
                const buildMultipliers = combineBuildMultipliers(selectedPartItems);
                
                if (buildMultipliers && (Object.keys(buildMultipliers.Value || {}).length > 0 || 
                                         Object.keys(buildMultipliers.Scale || {}).length > 0 || 
                                         Object.keys(buildMultipliers.Add || {}).length > 0)) {
                    html += `
                        <div style="margin-top: var(--section-gap); padding: var(--input-pad-y) var(--input-pad-x); background: #1a2a2a; border-radius: 6px; border-left: 3px solid #00BCD4;">
                            <div style="font-size: 12px; color: #00BCD4; font-weight: bold; margin-bottom: 8px;">🔧 Build-Level Multipliers:</div>
                    `;
                    
                    // Show Value bucket
                    if (buildMultipliers.Value && Object.keys(buildMultipliers.Value).length > 0) {
                        html += `<div style="margin-bottom: 8px;">`;
                        html += `<div style="font-size: 10px; color: #00BCD4; font-weight: bold; margin-bottom: 4px;">Value (Sum):</div>`;
                        Object.entries(buildMultipliers.Value).forEach(([key, data]) => {
                            html += `<div style="font-size: 9px; color: #aaa; margin-left: 10px; margin-bottom: 2px;">`;
                            html += `${key}: ${data.sum}`;
                            if (data.parts && data.parts.length > 0) {
                                html += ` <span style="color: #666;">(${data.parts.length} source${data.parts.length > 1 ? 's' : ''})</span>`;
                            }
                            html += `</div>`;
                        });
                        html += `</div>`;
                    }
                    
                    // Show Scale bucket
                    if (buildMultipliers.Scale && Object.keys(buildMultipliers.Scale).length > 0) {
                        html += `<div style="margin-bottom: 8px;">`;
                        html += `<div style="font-size: 10px; color: #00BCD4; font-weight: bold; margin-bottom: 4px;">Scale (Product):</div>`;
                        Object.entries(buildMultipliers.Scale).forEach(([key, data]) => {
                            html += `<div style="font-size: 9px; color: #aaa; margin-left: 10px; margin-bottom: 2px;">`;
                            html += `${key}: ${data.product}`;
                            if (data.parts && data.parts.length > 0) {
                                html += ` <span style="color: #666;">(${data.parts.length} source${data.parts.length > 1 ? 's' : ''})</span>`;
                            }
                            html += `</div>`;
                        });
                        html += `</div>`;
                    }
                    
                    // Show Add bucket
                    if (buildMultipliers.Add && Object.keys(buildMultipliers.Add).length > 0) {
                        html += `<div style="margin-bottom: 8px;">`;
                        html += `<div style="font-size: 10px; color: #00BCD4; font-weight: bold; margin-bottom: 4px;">Add (Sum):</div>`;
                        Object.entries(buildMultipliers.Add).forEach(([key, data]) => {
                            html += `<div style="font-size: 9px; color: #aaa; margin-left: 10px; margin-bottom: 2px;">`;
                            html += `${key}: ${data.sum}`;
                            if (data.parts && data.parts.length > 0) {
                                html += ` <span style="color: #666;">(${data.parts.length} source${data.parts.length > 1 ? 's' : ''})</span>`;
                            }
                            html += `</div>`;
                        });
                        html += `</div>`;
                    }
                    
                    html += `</div>`;
                }
                
                html += `</div>`;
            });

            outputDiv.innerHTML = html;
            
            // Update final output string
            updateFinalOutput();
        }

        function resolvePartEntryForSerialOutput(root, partKey, baseGenericRoot, dataRef) {
            if (!dataRef || !partKey) return null;
            const crossResolved = parseMiCrossPartRef(partKey);
            if (crossResolved && dataRef.rootDefinitions) {
                const srRoot = dataRef.rootDefinitions.find(r => r.key === crossResolved.sourceRootKey);
                if (srRoot && srRoot.depEntries) {
                    const dep = srRoot.depEntries.find(d => d.key === crossResolved.partKey);
                    if (dep) return dep;
                }
                const inner = crossResolved.partKey;
                if (inner) {
                    for (let i = 0; i < dataRef.rootDefinitions.length; i++) {
                        const r = dataRef.rootDefinitions[i];
                        if (!r.depEntries) continue;
                        const dep = r.depEntries.find(d => d.key === inner);
                        if (dep) return dep;
                    }
                    const supplementalDep = miResolveSupplementalPartInfo(inner);
                    if (supplementalDep) return supplementalDep;
                }
            }
            if (root && root.depEntries) {
                const dep = root.depEntries.find(d => d.key === partKey);
                if (dep) return dep;
            }
            if (baseGenericRoot && baseGenericRoot.depEntries) {
                const dep = baseGenericRoot.depEntries.find(d => d.key === partKey);
                if (dep) return dep;
            }
            if (dataRef.rootDefinitions) {
                for (let i = 0; i < dataRef.rootDefinitions.length; i++) {
                    const r = dataRef.rootDefinitions[i];
                    if (!r.depEntries) continue;
                    const dep = r.depEntries.find(d => d.key === partKey);
                    if (dep) return dep;
                }
            }
            return null;
        }

        /** Same token string as deserialized output: `{sub}`, `{root:sub}`, or quoted spawn code. */
        function formatDeserializedPartToken(compositionRootSerial, partKey, partEntry, dataRef) {
            if (!partEntry || !dataRef) {
                return formatQuotedPartSerialToken(partKey, partEntry);
            }
            let serialIndex = partEntry.serialIndex;
            if (!isValidNumericPartSerial(serialIndex)) {
                serialIndex = partEntry.serialindex?.index;
            }
            if (!isValidNumericPartSerial(serialIndex) && partEntry.serialindex) {
                serialIndex =
                    typeof partEntry.serialindex === 'number'
                        ? partEntry.serialindex
                        : typeof partEntry.serialindex === 'object' && partEntry.serialindex !== null
                          ? partEntry.serialindex.index
                          : null;
            }
            const subSer = normalizeNumericPartSerial(serialIndex);

            const crossP = parseMiCrossPartRef(partKey);
            if (crossP && dataRef.rootDefinitions) {
                const srcRoot = dataRef.rootDefinitions.find(r => r.key === crossP.sourceRootKey);
                const srcRs =
                    srcRoot != null && srcRoot.serialIndex != null && srcRoot.serialIndex !== undefined
                        ? Number(srcRoot.serialIndex)
                        : NaN;
                if (subSer !== null && !isNaN(srcRs)) {
                    const compRootNum = Number(compositionRootSerial);
                    if (srcRs !== compRootNum) {
                        return `{${srcRs}:${subSer}}`;
                    }
                    return `{${subSer}}`;
                }
            }

            const partRootSerialIndex =
                partEntry.rootSerialIndex !== undefined
                    ? partEntry.rootSerialIndex
                    : partEntry.rootKey && dataRef.rootDefinitions
                      ? dataRef.rootDefinitions.find(r => r.key === partEntry.rootKey)?.serialIndex
                      : null;
            const compRootNum = Number(compositionRootSerial);
            const partRootNum = normalizeNumericPartSerial(partRootSerialIndex);
            const isFromDifferentRoot =
                partRootNum !== null &&
                !Number.isNaN(compRootNum) &&
                partRootNum !== compRootNum;
            if (isFromDifferentRoot) {
                if (subSer !== null) {
                    return `{${partRootNum}:${subSer}}`;
                }
                return formatQuotedPartSerialToken(partKey, partEntry);
            }
            if (subSer !== null) {
                return `{${subSer}}`;
            }
            return formatQuotedPartSerialToken(partKey, partEntry);
        }

        function appendSerialTokenForPart(serialList, compositionRootSerial, partKey, partEntry, dataRef) {
            const t = formatDeserializedPartToken(compositionRootSerial, partKey, partEntry, dataRef);
            if (t) serialList.push(t);
        }

        function initMiItemPartsBreakdownCollapse() {
            const details = document.getElementById('mi_itemPartsBreakdownCollapse');
            if (!details || details.dataset.miBreakdownCollapseWired) {
                return;
            }
            details.dataset.miBreakdownCollapseWired = '1';
            function applyCollapseState() {
                const mobile = window.matchMedia('(max-width: 767px)').matches;
                if (mobile) {
                    if (!details.hasAttribute('data-mi-user-toggled')) {
                        details.open = false;
                    }
                } else {
                    details.open = true;
                    details.removeAttribute('data-mi-user-toggled');
                }
            }
            details.addEventListener('toggle', function () {
                if (window.matchMedia('(max-width: 767px)').matches) {
                    details.setAttribute('data-mi-user-toggled', '1');
                }
            });
            applyCollapseState();
            if (!window.__miBreakdownResizeBound) {
                window.__miBreakdownResizeBound = true;
                window.addEventListener('resize', applyCollapseState);
            }
        }

        /** Right-panel live breakdown for modded simple builder (grouped parts, slot status, compact serial). */
        function updateMiItemPartsBreakdown() {
            const panel = document.getElementById('mi_itemPartsBreakdownContent');
            if (!panel) {
                return;
            }
            const inst = window.legitBuilderInstances && window.legitBuilderInstances[1];
            if (!inst || !inst.relaxedPicker) {
                return;
            }
            initMiItemPartsBreakdownCollapse();

            const typeSel = document.getElementById('mi_simpleTypeSelect');
            const manSel = document.getElementById('mi_simpleManufacturerSelect');
            const levelEl = document.getElementById('mi_outputLevel');
            const seedEl = document.getElementById('mi_outputSeed');
            const typeLabel =
                typeSel && typeSel.selectedIndex >= 0
                    ? typeSel.options[typeSel.selectedIndex].text
                    : '—';
            const manLabel =
                manSel && manSel.selectedIndex >= 0 && manSel.value
                    ? manSel.options[manSel.selectedIndex].text
                    : '—';
            const levelVal = levelEl && levelEl.value != null ? String(levelEl.value) : '—';
            const seedVal = seedEl && seedEl.value != null ? String(seedEl.value) : '—';

            const metaHtml =
                '<div class="mi-breakdown-meta">' +
                '<div>Manufacturer: ' +
                escapeLegitHtml(manLabel) +
                '</div>' +
                '<div>Type: ' +
                escapeLegitHtml(typeLabel) +
                '</div>' +
                '<div>Level: ' +
                escapeLegitHtml(levelVal) +
                '</div>' +
                '<div>Seed: ' +
                escapeLegitHtml(seedVal) +
                '</div></div>';

            if (typeof syncAllMiPartLinesFromModdedDom === 'function') {
                syncAllMiPartLinesFromModdedDom();
            }

            const host = document.getElementById('mi_simpleBuilderHost');
            const rootItem =
                host && host.style.display !== 'none' ? host.querySelector('.root-item[data-root-key]') : null;
            const rootKey = rootItem ? String(rootItem.getAttribute('data-root-key') || '').trim() : '';
            if (rootItem && rootKey && typeof syncMiPartLinesFromRootItem === 'function') {
                syncMiPartLinesFromRootItem(rootItem);
            }
            const root =
                rootKey && data && data.rootDefinitions
                    ? data.rootDefinitions.find(function (r) {
                          return r && r.key === rootKey;
                      })
                    : null;

            const lines =
                rootKey && inst.miPartLines && miPartLinesHasSyncedRoot(inst, rootKey)
                    ? inst.miPartLines.get(rootKey) || []
                    : [];
            const pickedLines = lines.filter(function (l) {
                return l && l.partKey;
            });

            const serialWrap = document.getElementById('mi_itemPartsBreakdownSerialWrap');
            const serialPre = document.getElementById('mi_itemPartsBreakdownSerialPreview');
            const advEl = document.getElementById('mi_itemPartsBreakdownAdvanced');
            const genTa = lbGeneratedHumanEl();
            const compactSerial = genTa && genTa.value ? String(genTa.value).trim() : '';
            if (serialWrap && serialPre) {
                if (compactSerial && compactSerial.indexOf('|') !== -1) {
                    serialWrap.hidden = false;
                    serialPre.textContent = compactSerial;
                } else {
                    serialWrap.hidden = true;
                    serialPre.textContent = '';
                }
            }
            if (advEl) {
                const valDiv = document.getElementById('mi_serialValidationResult');
                if (valDiv && valDiv.style.display !== 'none' && valDiv.innerHTML.trim()) {
                    advEl.innerHTML = valDiv.innerHTML;
                } else {
                    advEl.innerHTML =
                        '<p class="mi-breakdown-empty">Parse a serial or build parts to see decode details.</p>';
                }
            }

            const itemName =
                root && typeof stripNexusPrefix === 'function'
                    ? stripNexusPrefix(root.itembasename || root.key || '')
                    : manLabel && typeLabel && manLabel !== '—'
                      ? manLabel + ' ' + typeLabel
                      : typeLabel;

            if (!rootKey) {
                panel.innerHTML =
                    metaHtml + '<p class="mi-breakdown-empty">Select type and manufacturer to begin.</p>';
                return;
            }

            if (pickedLines.length === 0) {
                let emptyHtml = metaHtml;
                if (itemName) {
                    emptyHtml += '<div class="mi-breakdown-item-name">' + escapeLegitHtml(itemName) + '</div>';
                }
                emptyHtml += '<p class="mi-breakdown-empty">No parts selected yet.</p>';
                panel.innerHTML = emptyHtml;
                return;
            }

            const groups = new Map();
            const groupOrder = [];
            pickedLines.forEach(function (line) {
                const tn = normalizeDepTableName(line.tableName || '');
                if (!tn || tn === 'unknown') {
                    return;
                }
                if (miShouldHideEndgameForModded() && tn === 'endgame') {
                    return;
                }
                if (!groups.has(tn)) {
                    groups.set(tn, []);
                    groupOrder.push(tn);
                }
                groups.get(tn).push(line);
            });

            if (root && data) {
                const slotOrder = getPartSelectionOrderFromRoot(root, data.rootDefinitions).map(function (s) {
                    return normalizeDepTableName(s);
                });
                groupOrder.sort(function (a, b) {
                    const ia = slotOrder.indexOf(a);
                    const ib = slotOrder.indexOf(b);
                    if (ia !== -1 && ib !== -1) {
                        return ia - ib;
                    }
                    if (ia !== -1) {
                        return -1;
                    }
                    if (ib !== -1) {
                        return 1;
                    }
                    return a.localeCompare(b);
                });
            }

            const compRoot = root;
            const compSer =
                compRoot != null && compRoot.serialIndex != null && compRoot.serialIndex !== undefined
                    ? compRoot.serialIndex
                    : null;
            const baseGenericRoot = compRoot ? getBaseGenericRootForPartLookup(compRoot, data) : null;

            let groupsHtml = '';
            groupOrder.forEach(function (tn) {
                const slotLines = groups.get(tn) || [];
                if (!slotLines.length) {
                    return;
                }
                let rowsHtml = '';
                slotLines.forEach(function (line) {
                    const partKey = line.partKey;
                    const pr = parseMiCrossPartRef(partKey);
                    const pk = pr ? pr.partKey : partKey;
                    const partEntry = resolvePartEntryForSerialOutput(
                        compRoot,
                        partKey,
                        baseGenericRoot,
                        data
                    );
                    const token =
                        formatModdedBreakdownTokensForLine(compRoot, line, baseGenericRoot, data) ||
                        formatDeserializedPartToken(compSer, partKey, partEntry, data) ||
                        '';
                    let label = pk;
                    if (partEntry && data) {
                        if (tn === 'inv_comp' && typeof getModdedInvCompBrowseDisplayName === 'function') {
                            label =
                                getModdedInvCompBrowseDisplayName(
                                    partEntry,
                                    data.invNamePartDisplayNames,
                                    data,
                                    compRoot
                                ) || label;
                        } else {
                            label =
                                getDisplayNameForPart(partEntry, data.invNamePartDisplayNames) ||
                                getDisplayNameForPartOrFallback(partEntry, data.invNamePartDisplayNames) ||
                                pk;
                        }
                    }
                    const qty = line.qty != null ? line.qty : 1;
                    const qtySuffix = qty > 1 ? ' ×' + qty : '';
                    rowsHtml +=
                        '<div class="breakdown-part-row">' +
                        '<span class="breakdown-part-label">✓ ' +
                        escapeLegitHtml(String(label || pk)) +
                        qtySuffix +
                        '</span>' +
                        '<span class="breakdown-part-token">' +
                        escapeLegitHtml(token) +
                        '</span></div>';
                });
                groupsHtml +=
                    '<div class="breakdown-group"><div class="breakdown-group-title">' +
                    escapeLegitHtml(getMiPartTypeDisplayLabel(tn)) +
                    '</div>' +
                    rowsHtml +
                    '</div>';
            });

            let html = '';
            if (itemName) {
                html += '<div class="mi-breakdown-item-name">' + escapeLegitHtml(itemName) + '</div>';
            }
            html += metaHtml;
            html += groupsHtml || '<p class="mi-breakdown-empty">No parts selected yet.</p>';
            panel.innerHTML = html;
        }

        /** Live preview: global #currentBuildHumanSerial (legit tab; id has no mi_ prefix) + per-root .lb-current-build-human-serial-in-root inside expanded roots. */
        function syncCurrentBuildHumanSerialPreview(joinedHuman, perRootHumanLines) {
            const placeholder = '— Select parts to see the human-readable serial —';
            const t = joinedHuman != null ? String(joinedHuman).trim() : '';
            const globalPre =
                document.getElementById('currentBuildHumanSerial') ||
                document.getElementById('mi_currentBuildHumanSerial');
            if (globalPre) {
                if (globalPre.tagName === 'TEXTAREA') {
                    if (document.activeElement !== globalPre) {
                        globalPre.value = t || placeholder;
                    }
                } else {
                    globalPre.textContent = t || placeholder;
                }
            }
            const map = Object.create(null);
            if (Array.isArray(perRootHumanLines)) {
                perRootHumanLines.forEach((row) => {
                    if (row && row.rootKey != null && row.text != null) {
                        map[String(row.rootKey)] = String(row.text).trim();
                    }
                });
            }
            if (typeof document !== 'undefined' && document.querySelectorAll) {
                document.querySelectorAll('.lb-current-build-human-serial-in-root').forEach((el) => {
                    const rk = el.getAttribute('data-root-key');
                    const line = rk != null && Object.prototype.hasOwnProperty.call(map, rk) ? map[rk] : '';
                    const next = (line && line.length) ? line : placeholder;
                    if (el.tagName === 'TEXTAREA') {
                        if (document.activeElement === el) return;
                        el.value = next;
                    } else {
                        el.textContent = next;
                    }
                });
            }
        }
        
        function updateFinalOutput() {
            const useMiMergedHuman = !!(window.__lbCtx && window.__lbCtx.relaxedPicker);
            const outputTextarea = useMiMergedHuman ? lbGeneratedHumanEl() : lbEl('finalOutputString');
            if (!outputTextarea) return;
            
            if (_sp().size === 0) {
                if (useMiMergedHuman) {
                    const taGen = lbGeneratedHumanEl();
                    const base = (taGen && taGen.value) ? taGen.value.trim() : '';
                    if (
                        base &&
                        base.indexOf('|') !== -1 &&
                        !(typeof isBase85Format === 'function' && isBase85Format(base))
                    ) {
                        const merged = mergeModdedDecodedViewUiIntoHumanSerialText(base);
                        if (taGen) {
                            taGen.value = merged;
                        }
                        syncCurrentBuildHumanSerialPreview(merged, []);
                        serializeToBase85(merged);
                        if (typeof updateMiItemPartsBreakdown === 'function') {
                            updateMiItemPartsBreakdown();
                        }
                        return;
                    }
                    if (taGen) {
                        taGen.value = '';
                    }
                    const base85Empty = lbEl('finalOutputBase85');
                    if (base85Empty) {
                        base85Empty.value = '';
                    }
                    syncCurrentBuildHumanSerialPreview('', []);
                    if (typeof updateMiItemPartsBreakdown === 'function') {
                        updateMiItemPartsBreakdown();
                    }
                    return;
                }
                outputTextarea.value = '';
                const base85TextareaEmpty = lbEl('finalOutputBase85');
                if (base85TextareaEmpty) {
                    base85TextareaEmpty.value = '';
                }
                syncCurrentBuildHumanSerialPreview('', []);
                return;
            }

            // Modded merged serial must reflect picked rows: flush DOM → miPartLines before read (× handler sync can miss a card).
            if (useMiMergedHuman) {
                syncAllMiPartLinesFromModdedDom();
            }
            
            const rootDefs = data && data.rootDefinitions ? data.rootDefinitions : [];
            const level = lbEl('outputLevel')?.value || '60';
            const seed = lbEl('outputSeed')?.value || '1';
            
            // Build output string for each root
            let outputStrings = [];
            /** @type {{ rootKey: string, text: string }[]} */
            const perRootHumanLines = [];
            const displayOrderRoots = getRootsInBuilderDisplayOrder().filter(r => _sp().has(r.key));
            const selectedKeys = Array.from(_sp().keys());
            const seenRootKey = new Set(displayOrderRoots.map(r => r.key));
            const orderedRoots = displayOrderRoots.slice();
            if (data && data.rootDefinitions && selectedKeys.length) {
                const byKey = new Map(data.rootDefinitions.map(r => [r.key, r]));
                selectedKeys.forEach(k => {
                    if (seenRootKey.has(k)) return;
                    const r = byKey.get(k);
                    if (r) {
                        orderedRoots.push(r);
                        seenRootKey.add(k);
                    }
                });
            }

            orderedRoots.forEach(root => {
                const partKeys = _sp().get(root.key);
                const rootKey = root.key;

                let rootSerialIndex = null;
                if (root.serialIndex !== undefined && root.serialIndex !== null && root.serialIndex !== '') {
                    const n = Number(root.serialIndex);
                    if (!Number.isNaN(n)) rootSerialIndex = n;
                }
                if (rootSerialIndex === null) return;
                
                // Find base generic root to check if parts come from it
                let baseGenericRoot = null;
                if (root && data && data.rootDefinitions) {
                    const baseItemTypePatterns = ['weapon', 'enhancement', 'shield', 'repair_kit', 'grenade_gadget', 'heavy_weapon_gadget', 'turret_gadget', 'terminal_gadget', 'grenade', 'artifact', 'classmod'];
                    let baseItemType = '';
                    const rootKeyLower = (root.key || '').toLowerCase();
                    
                    // Check if this is a weapon by checking for weapon-specific part types
                    // Manufacturer weapons like "bor_sm" don't have "weapon" in their key, but they have weapon parts
                    const weaponPartTypes = ['body', 'barrel', 'magazine', 'scope', 'grip', 'foregrip', 'body_ele', 'secondary_ele'];
                    const hasWeaponParts = root.depEntries && root.depEntries.some(dep => 
                        dep.depTableName && weaponPartTypes.includes(dep.depTableName.toLowerCase())
                    );
                    
                    // Special handling: terminal_combat, terminal_*, turret_* should map to terminal_gadget/turret_gadget
                    if (rootKeyLower.includes('terminal_') && !rootKeyLower.includes('terminal_gadget')) {
                        // Map terminal_combat, terminal_*, etc. to terminal_gadget
                        if (rootKeyLower.includes('terminal_combat') || rootKeyLower.includes('terminal_')) {
                            baseItemType = 'terminal_gadget';
                        }
                    } else if (rootKeyLower.includes('turret_') && !rootKeyLower.includes('turret_gadget')) {
                        // Map turret_combat, turret_*, etc. to turret_gadget
                        if (rootKeyLower.includes('turret_combat') || rootKeyLower.includes('turret_')) {
                            baseItemType = 'turret_gadget';
                        }
                    } else {
                        for (const pattern of baseItemTypePatterns) {
                            if (rootKeyLower.includes(pattern) && rootKeyLower !== pattern) {
                                const match = rootKeyLower.match(new RegExp(`(?:\\w+_)?(${pattern.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')})(?:\\w+)?$`, 'i'));
                                if (match && match[1]) {
                                    baseItemType = match[1].toLowerCase();
                                    break;
                                }
                            } else if (rootKeyLower === pattern) {
                                baseItemType = pattern;
                                break;
                            }
                        }
                    }
                    
                    // If no pattern matched but this root has weapon parts, it's likely a manufacturer weapon
                    // Find the generic weapon root (key "weapon" or serial 1)
                    if (!baseItemType && hasWeaponParts) {
                        baseItemType = 'weapon';
                    }
                    
                    if (baseItemType) {
                        baseGenericRoot = data.rootDefinitions.find(r => {
                            const rKey = (r.key || '').toLowerCase();
                            return rKey === baseItemType;
                        });
                        
                        // If not found by key, try to find by serial (weapon root is usually serial 1)
                        if (!baseGenericRoot && baseItemType === 'weapon') {
                            baseGenericRoot = data.rootDefinitions.find(r => {
                                const rSerial = r.serialIndex;
                                return rSerial === 1 || rSerial === '1';
                            });
                        }
                    }
                }
                
                // Collect all part serials in order. Modded: expand line qty (passive *_tier_1 walks tiers 1→5 then repeats tier 5).
                const miInstFo = window.legitBuilderInstances && window.legitBuilderInstances[1];
                const useMiLinesForSerial = miPartLinesHasSyncedRoot(miInstFo, root.key);
                const miLinesFo = useMiLinesForSerial ? miInstFo.miPartLines.get(root.key) : null;
                const partKeySequence = useMiLinesForSerial
                    ? (() => {
                        const rows = miLinesFo || [];
                        const hasDecompressOrder = rows.some((r) => r && r._miOrder != null);
                        if (!hasDecompressOrder) {
                            const seq = [];
                            rows.forEach(({ partKey, qty }) => {
                                if (!partKey) return;
                                expandModdedMiLineToPartKeys(
                                    partKey,
                                    qty != null ? qty : 1
                                ).forEach((k) => seq.push(k));
                            });
                            return seq;
                        }
                        const orderedRows = rows.slice().sort((a, b) => {
                            const ao = a._miOrder != null ? a._miOrder : 1e15;
                            const bo = b._miOrder != null ? b._miOrder : 1e15;
                            return ao - bo;
                        });
                        const keyed = [];
                        orderedRows.forEach((line, lineIdx) => {
                            if (!line || !line.partKey) return;
                            const sub = expandModdedMiLineToPartKeys(
                                line.partKey,
                                line.qty != null ? line.qty : 1
                            );
                            const base =
                                line._miOrder != null ? line._miOrder : 1e15 + lineIdx;
                            sub.forEach((k, subIdx) => {
                                keyed.push({ partKey: k, ord: base + subIdx * 1e-3 });
                            });
                        });
                        keyed.sort((a, b) => a.ord - b.ord);
                        return keyed.map((x) => x.partKey);
                    })()
                    : Array.from(partKeys);

                const serialList = [];

                if (useMiLinesForSerial) {
                    partKeySequence.forEach(partKey => {
                        const miss = parseMiMissingPartRef(partKey);
                        if (miss && miss.rawToken) {
                            const rt = String(miss.rawToken).trim();
                            // Cosmetic belongs in | "c", …|; not as space-separated part tokens (would yield {61} "c" "Skin"|).
                            if (/^"c"$/i.test(rt) || /^"Cosmetics_/i.test(rt)) {
                                return;
                            }
                            serialList.push(rt);
                            return;
                        }
                        const partEntry = resolvePartEntryForSerialOutput(root, partKey, baseGenericRoot, data);
                        if (partEntry) {
                            appendSerialTokenForPart(serialList, rootSerialIndex, partKey, partEntry, data);
                        }
                    });
                } else {
                    const partsByTable = {};
                    partKeySequence.forEach(partKey => {
                        let tableName = 'Unknown';
                        let partEntry = null;
                        const crossResolved = parseMiCrossPartRef(partKey);
                        if (crossResolved && data && data.rootDefinitions) {
                            const srRoot = data.rootDefinitions.find(r => r.key === crossResolved.sourceRootKey);
                            if (srRoot && srRoot.depEntries) {
                                partEntry = srRoot.depEntries.find(dep => dep.key === crossResolved.partKey);
                                if (partEntry) {
                                    tableName = partEntry.depTableName || 'Unknown';
                                }
                            }
                        }
                        if (!partEntry && root && root.depEntries) {
                            partEntry = root.depEntries.find(dep => dep.key === partKey);
                            if (partEntry) {
                                tableName = partEntry.depTableName || 'Unknown';
                            }
                        }
                        if (!partEntry && baseGenericRoot && baseGenericRoot.depEntries) {
                            partEntry = baseGenericRoot.depEntries.find(dep => dep.key === partKey);
                            if (partEntry) {
                                tableName = partEntry.depTableName || 'Unknown';
                            }
                        }
                        if (!partEntry && data && data.rootDefinitions) {
                            for (const r of data.rootDefinitions) {
                                if (r.depEntries) {
                                    partEntry = r.depEntries.find(dep => dep.key === partKey);
                                    if (partEntry) {
                                        tableName = partEntry.depTableName || 'Unknown';
                                        break;
                                    }
                                }
                            }
                        }

                        if (!partsByTable[tableName]) {
                            partsByTable[tableName] = [];
                        }
                        partsByTable[tableName].push({ key: partKey, entry: partEntry });
                    });

                    const partSelectionOrder = getPartSelectionOrderFromRoot(root, data?.rootDefinitions);
                    const partSelectionOrderLower = partSelectionOrder.map(s => (s || '').toLowerCase());

                    const sortedTables = Object.entries(partsByTable).sort((a, b) => {
                        const normalizedA = normalizeDepTableName(a[0]);
                        const normalizedB = normalizeDepTableName(b[0]);
                        const orderA = partSelectionOrderLower.indexOf(normalizedA);
                        const orderB = partSelectionOrderLower.indexOf(normalizedB);
                        if (orderA !== -1 && orderB !== -1) return orderA - orderB;
                        if (orderA !== -1) return -1;
                        if (orderB !== -1) return 1;
                        return a[0].localeCompare(b[0]);
                    });

                    sortedTables.forEach(([tableName, parts]) => {
                        parts.forEach(({ key: partKey, entry: partEntry }) => {
                            if (!partEntry) return;
                            appendSerialTokenForPart(serialList, rootSerialIndex, partKey, partEntry, data);
                        });
                    });
                }

                if (useMiMergedHuman) {
                    try {
                        serialList = mergeConsecutiveRootPairTokensToArrayTokens(serialList);
                    } catch (e) {
                        console.warn('updateFinalOutput mergeConsecutiveRootPairTokensToArrayTokens', e);
                    }
                }

                // Format: rootSerial, 0, 1, level| 2, seed|| {serial1} {serial2} ...|
                // Modded: optional 9,1 (firmware lock) and 10,1 (buyback) before seed segment.
                if (serialList.length > 0) {
                    let outputString;
                    if (useMiMergedHuman) {
                        const seg = [];
                        const fwEl = document.getElementById('mi_firmwareLock');
                        const bbEl = document.getElementById('mi_buyback');
                        if (fwEl && fwEl.checked) {
                            seg.push('9, 1');
                        }
                        if (bbEl && bbEl.checked) {
                            seg.push('10, 1');
                        }
                        seg.push(`2, ${seed}`);
                        outputString = `${rootSerialIndex}, 0, 1, ${level}| ${seg.join('| ')}|| ${serialList.join(' ')}|`;
                        outputString = appendMiSkinTailToModdedHumanLine(outputString);
                        outputString = normalizeConsecutiveRootPairTokensInModdedHumanLine(outputString);
                    } else {
                        outputString = `${rootSerialIndex}, 0, 1, ${level}| 2, ${seed}|| ${serialList.join(' ')}|`;
                        outputString = appendMiSkinTailToModdedHumanLine(outputString);
                    }
                    outputStrings.push(outputString);
                    perRootHumanLines.push({ rootKey: root.key, text: outputString });
                }
            });
            
            const joinedHuman = outputStrings.join('\n');
            outputTextarea.value = joinedHuman;
            syncCurrentBuildHumanSerialPreview(joinedHuman, perRootHumanLines);
            
            // Also serialize to Base85 if we have output
            if (outputStrings.length > 0) {
                serializeToBase85(joinedHuman);
            } else {
                const base85Textarea = lbEl('finalOutputBase85');
                if (base85Textarea) {
                    base85Textarea.value = '';
                }
            }
            if (useMiMergedHuman && typeof updateMiItemPartsBreakdown === 'function') {
                updateMiItemPartsBreakdown();
            }
        }
        
        // Serialize human-readable string to Base85
        async function serializeToBase85(deserializedString) {
            const base85Textarea = lbEl('finalOutputBase85');
            if (!base85Textarea || !deserializedString) return;
            
            try {
                // Show loading state
                base85Textarea.value = 'Serializing...';
                
                const response = await fetch(SERIAL_API_URL, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ deserialized: deserializedString })
                });
                
                if (!response.ok) {
                    throw new Error(`API error: ${response.status}`);
                }
                
                const result = await response.json();
                
                if (result.error) {
                    throw new Error(result.error);
                }
                
                if (result.serial_b85) {
                    base85Textarea.value = result.serial_b85;
                } else {
                    base85Textarea.value = 'Error: No Base85 result from API';
                }
            } catch (error) {
                base85Textarea.value = `Error: ${error.message}`;
            }
        }

        /** After parse on Modded tab: fill human + Base85 (parse-only skips updateFinalOutput, which normally sets both textareas). */
        function maybeSerializeModdedHumanToBase85(humanSerial) {
            if (!(window.__lbCtx && window.__lbCtx.relaxedPicker)) {
                return;
            }
            const s = humanSerial != null ? String(humanSerial).trim() : '';
            if (!s || s.indexOf('|') === -1) {
                return;
            }
            if (typeof isBase85Format === 'function' && isBase85Format(s)) {
                return;
            }
            const withSkin =
                typeof mergeModdedSkinUiIntoHumanSerialText === 'function'
                    ? mergeModdedSkinUiIntoHumanSerialText(s)
                    : s;
            const taGen = lbGeneratedHumanEl();
            if (taGen) {
                taGen.value = withSkin;
            }
            serializeToBase85(withSkin);
        }

        /** Copy plain text via Clipboard API; fallback uses a temporary textarea (works when source is inside a closed details panel). */
        function copyTextUsingClipboardOrFallback(text, btn) {
            text = text != null ? String(text) : '';
            if (!text.trim()) {
                flashCopyButton(btn, false);
                return;
            }
            function fallbackExecCopy() {
                const ta = document.createElement('textarea');
                ta.value = text;
                ta.setAttribute('readonly', '');
                ta.style.position = 'fixed';
                ta.style.left = '-9999px';
                ta.style.top = '0';
                document.body.appendChild(ta);
                ta.focus();
                ta.select();
                ta.setSelectionRange(0, text.length);
                var ok = false;
                try {
                    ok = document.execCommand('copy');
                } catch (e) {
                    ok = false;
                }
                document.body.removeChild(ta);
                flashCopyButton(btn, ok);
            }
            if (navigator.clipboard && navigator.clipboard.writeText) {
                navigator.clipboard.writeText(text).then(
                    function () {
                        flashCopyButton(btn, true);
                    },
                    function () {
                        fallbackExecCopy();
                    }
                );
            } else {
                fallbackExecCopy();
            }
        }

        function copyBase85Output(btn) {
            const base85Textarea = lbEl('finalOutputBase85');
            if (!base85Textarea) return;
            const text = base85Textarea.value != null ? String(base85Textarea.value).trim() : '';
            if (!text || text === 'Serializing...' || text.indexOf('Error:') === 0) {
                flashCopyButton(btn, false);
                return;
            }
            copyTextUsingClipboardOrFallback(text, btn);
        }

        function flashCopyButton(btn, ok) {
            if (!btn || !btn.textContent) {
                return;
            }
            const orig = btn.textContent;
            const bg = btn.style.background;
            btn.textContent = ok ? 'Copied!' : 'Failed';
            btn.style.background = ok ? '#4CAF50' : '#c62828';
            setTimeout(function () {
                btn.textContent = orig;
                btn.style.background = bg;
            }, 1600);
        }

        window.copyModdedDeserializedOutput = function copyModdedDeserializedOutput(btn) {
            window.legitBuilderSetCtx(1);
            const ta = lbGeneratedHumanEl();
            const text = ta && ta.value != null ? String(ta.value).trim() : '';
            if (!text) {
                flashCopyButton(btn, false);
                return;
            }
            if (navigator.clipboard && navigator.clipboard.writeText) {
                navigator.clipboard.writeText(text).then(
                    function () {
                        flashCopyButton(btn, true);
                    },
                    function () {
                        try {
                            ta.select();
                            ta.setSelectionRange(0, 99999);
                            document.execCommand('copy');
                            flashCopyButton(btn, true);
                        } catch (e) {
                            flashCopyButton(btn, false);
                        }
                    }
                );
            } else {
                try {
                    ta.select();
                    ta.setSelectionRange(0, 99999);
                    document.execCommand('copy');
                    flashCopyButton(btn, true);
                } catch (e2) {
                    flashCopyButton(btn, false);
                }
            }
        };

        window.copyModdedSerializedOutput = function copyModdedSerializedOutput(btn) {
            window.legitBuilderSetCtx(1);
            const ta = document.getElementById('mi_finalOutputBase85');
            const text = ta && ta.value != null ? String(ta.value).trim() : '';
            if (!text || text === 'Serializing...' || text.indexOf('Error:') === 0) {
                flashCopyButton(btn, false);
                return;
            }
            if (navigator.clipboard && navigator.clipboard.writeText) {
                navigator.clipboard.writeText(text).then(
                    function () {
                        flashCopyButton(btn, true);
                    },
                    function () {
                        try {
                            ta.select();
                            ta.setSelectionRange(0, 99999);
                            document.execCommand('copy');
                            flashCopyButton(btn, true);
                        } catch (e) {
                            flashCopyButton(btn, false);
                        }
                    }
                );
            } else {
                try {
                    ta.select();
                    ta.setSelectionRange(0, 99999);
                    document.execCommand('copy');
                    flashCopyButton(btn, true);
                } catch (e2) {
                    flashCopyButton(btn, false);
                }
            }
        };
        
        function clearAllSelectedParts(opt) {
            opt = opt || {};
            // Clear all selected parts
            _sp().clear();
            if (window.__lbCtx && window.__lbCtx.miPartLines) {
                window.__lbCtx.miPartLines.clear();
            }
            if (window.__lbCtx && window.__lbCtx.relaxedPicker) {
                const si = lbGeneratedHumanEl();
                if (si) {
                    si.value = '';
                }
                const b85 = lbEl('finalOutputBase85');
                if (b85) {
                    b85.value = '';
                }
            }
            
            // Update the display
            updateSelectedPartsOutput();
            updateFinalOutput();
            
            // Re-render root definitions to update visual highlights
            if (data && data.rootDefinitions && !opt.skipDisplayRootDefinitions) {
                displayRootDefinitions();
            }
        }

        function formatTagsForBuilderPush(tags) {
            if (!tags || !Array.isArray(tags)) return [];
            return tags.map(function (tag) {
                if (typeof tag === 'object' && tag !== null) {
                    const values = Object.values(tag);
                    return values[0] || Object.keys(tag)[0] || '';
                }
                return tag;
            }).filter(Boolean);
        }

        /**
         * Add one validator picker part (missing or extra optional slot) to builder, regenerate serial, re-validate.
         * @param {'missing'|'extra'} pool
         * @param {number} slotIndex
         * @param {HTMLElement} btn
         */
        function legitApplyValidatorSlotPick(pool, slotIndex, btn) {
            const notify =
                typeof window.showNotification === 'function' ? window.showNotification : null;
            const err = function (msg) {
                if (typeof showError === 'function') {
                    showError(msg);
                } else {
                    alert(msg);
                }
            };

            const saveEditorOverlay =
                btn && typeof btn.closest === 'function'
                    ? btn.closest('#save-editor-validation-modal-overlay')
                    : null;
            const useInspect =
                btn &&
                typeof btn.closest === 'function' &&
                document.getElementById('validationDetailsModalContent') &&
                btn.closest('#validationDetailsModalContent');
            const validation = useInspect
                ? window.__legitInspectValidation
                : saveEditorOverlay
                  ? window.__legitSaveEditorModalValidation || window.__legitLastSingleValidation
                  : window.__legitLastSingleValidation;

            if (!validation || !validation.details) {
                err('No validation data.');
                return false;
            }
            const details = validation.details;
            if (details.suppressSlotRepairPickers) {
                err('Slot repair is disabled until structural issues are fixed (duplicates, wrong-root parts, unknown parts, or over-max slot counts).');
                return false;
            }
            const slots =
                pool === 'extra'
                    ? details.extraOptionalSlots
                    : details.missingRequiredSlots;
            if (!Array.isArray(slots) || slotIndex < 0 || slotIndex >= slots.length) {
                err('Slot entry not found.');
                return false;
            }
            const rowAttr = pool === 'extra' ? 'data-extra-slot-row' : 'data-missing-slot-row';
            const selectSel =
                pool === 'extra' ? 'select[data-extra-slot-select]' : 'select[data-missing-slot-select]';
            const row =
                btn && btn.closest
                    ? btn.closest(`[${rowAttr}]`)
                    : null;
            const sel = row ? row.querySelector(selectSel) : null;
            const partKey = sel && sel.value ? String(sel.value).trim() : '';
            if (!partKey) {
                err('Choose a part first.');
                return false;
            }

            if (!data || !data.rootDefinitions) {
                err('No data loaded.');
                return false;
            }
            const rootKey = details.rootKey;
            if (!rootKey) {
                err('Missing root key on validation result.');
                return false;
            }
            const rootDef = data.rootDefinitions.find(function (r) {
                return r.key === rootKey;
            });
            if (!rootDef) {
                err('Root definition not found.');
                return false;
            }

            const keySet = new Set();
            (details.validatedParts || []).forEach(function (p) {
                if (p && p.found && p.partKey && p.rootMatches !== false) {
                    keySet.add(p.partKey);
                }
            });
            keySet.add(partKey);

            clearAllSelectedParts();
            _sp().set(rootKey, keySet);

            const levelEl = lbEl('outputLevel');
            const seedEl = lbEl('outputSeed');
            if (levelEl && details.level != null && !isNaN(Number(details.level))) {
                levelEl.value = String(details.level);
            }
            if (seedEl && details.seed != null && !isNaN(Number(details.seed))) {
                seedEl.value = String(details.seed);
            }

            if (data && data.rootDefinitions) {
                displayRootDefinitions();
            }
            updateSelectedPartsOutput();
            updateFinalOutput();

            const outEl = lbGeneratedHumanEl();
            const serialInputEl = lbSerialInputEl();
            const newSerial = outEl && outEl.value ? outEl.value.trim() : '';
            if (!newSerial) {
                err('Could not build serial from selections.');
                return false;
            }
            if (saveEditorOverlay) {
                const base85 = saveEditorOverlay.getAttribute('data-inv-base85-serial');
                if (!base85 || typeof window.applySaveEditorModalDecodedChange !== 'function') {
                    err('Save editor modal is missing item context.');
                    return false;
                }
                if (
                    typeof __validatorResultCache !== 'undefined' &&
                    __validatorResultCache &&
                    __validatorResultCache.clear
                ) {
                    __validatorResultCache.clear();
                }
                void window
                    .applySaveEditorModalDecodedChange(base85, newSerial)
                    .then(function () {
                        if (notify) {
                            notify('Updated serial with selected part', 'success', { durationMs: 3500 });
                        }
                    })
                    .catch(function (e) {
                        console.warn('applySaveEditorModalDecodedChange failed:', e);
                        err(e && e.message ? e.message : String(e));
                    });
                return true;
            }

            if (serialInputEl) {
                serialInputEl.value = newSerial;
            }
            if (window.__lbCtx && window.__lbCtx.relaxedPicker) {
                const genEl = lbGeneratedHumanEl();
                if (genEl) {
                    genEl.value = newSerial;
                }
            }

            __validatorResultCache.clear();

            if (typeof decodeAndValidateSerial === 'function') {
                decodeAndValidateSerial();
            }
            const resultDiv = lbEl('serialValidationResult');
            if (resultDiv && typeof resultDiv.scrollIntoView === 'function') {
                resultDiv.scrollIntoView({ behavior: 'smooth', block: 'start' });
            }

            const modalOverlay = document.getElementById('validationDetailsModalOverlay');
            if (
                useInspect &&
                modalOverlay &&
                window.getComputedStyle(modalOverlay).display !== 'none'
            ) {
                modalOverlay.style.display = 'none';
            }

            if (notify) {
                notify('Updated serial with selected part', 'success', { durationMs: 3500 });
            }
            return true;
        }

        window.legitApplyValidatorSlotPick = legitApplyValidatorSlotPick;
        window.legitApplyMissingSlotCandidateFromValidator = function (slotIndex, btn) {
            return legitApplyValidatorSlotPick('missing', slotIndex, btn);
        };

        /** After Push to Modded from Save Editor inventory: show Root Definitions and expand/scrolled to the decoded item’s root. */
        window.focusModdedBuilderRootsAfterInventoryPush = function focusModdedBuilderRootsAfterInventoryPush() {
            window.legitBuilderSetCtx(1);
            const miTab = document.getElementById('item-editor-tab');
            const alreadyActive = miTab && miTab.classList.contains('active');
            if (!alreadyActive && typeof window.switchTab === 'function') {
                window.__suppressScrollMainViewportToTopOnce = true;
                window.switchTab('item-editor-tab');
            }
            window.legitBuilderSetCtx(1);
            const v = window.__legitLastSingleValidation;
            const rk = v && v.details && v.details.rootKey;
            if (!rk || typeof switchLegitTab !== 'function') {
                return;
            }
            switchLegitTab('roots');
            const miRoots = document.getElementById('mi_rootsContent');
            requestAnimationFrame(function () {
                requestAnimationFrame(function () {
                    window.legitBuilderSetCtx(1);
                    expandLegitBuilderRootsForRootKey(rk, miRoots);
                    const pushedRoot =
                        miRoots &&
                        Array.from(miRoots.querySelectorAll('.root-item')).find(function (item) {
                            const el = item.querySelector('[data-root-key]');
                            return el && el.getAttribute('data-root-key') === rk;
                        });
                    const scrollTarget = pushedRoot || miRoots;
                    if (scrollTarget) {
                        miScrollModdedPushTargetIntoView(scrollTarget);
                    }
                });
            });
        };

        /**
         * Apply validator result to Legit Builder: clears selections, sets parts under composition root in canonical order.
         * Only PASS_ALL or FAIL_ORDER (order-only); blocks HARD_FAIL and FAIL_TAG_DEPS.
         * @param {object} validation - validateSerialCore result with details
         * @param {{ reorderTextareaRoot?: Element }} [options] - optional root to read live reordered serial textarea
         * @returns {boolean}
         */
        function pushValidationToLegitBuilder(validation, options) {
            options = options || {};
            const moddedRelaxed = !!options.moddedRelaxed;
            const inventoryPushSilent = !!options.inventoryPushSilent;
            const skipModdedAutoScroll = moddedRelaxed && inventoryPushSilent;
            let miScrollTokenPassed = options._miScrollToken;
            const notify =
                typeof window.showNotification === 'function' ? window.showNotification : null;
            const err = function (msg) {
                if (inventoryPushSilent) {
                    return;
                }
                if (typeof showError === 'function') {
                    showError(msg);
                } else {
                    alert(msg);
                }
            };

            if (!validation || !validation.details) {
                err('No validation data.');
                return false;
            }
            const details = validation.details;
            const vc =
                details.validationClass ||
                (validation.valid ? SERIAL_VALIDATION_CLASS.PASS_ALL : SERIAL_VALIDATION_CLASS.HARD_FAIL);

            if (!moddedRelaxed) {
                if (
                    vc === SERIAL_VALIDATION_CLASS.HARD_FAIL ||
                    vc === SERIAL_VALIDATION_CLASS.FAIL_TAG_DEPS
                ) {
                    err('Cannot push: balance fail or prerequisite failures.');
                    return false;
                }
                if (vc !== SERIAL_VALIDATION_CLASS.PASS_ALL && vc !== SERIAL_VALIDATION_CLASS.FAIL_ORDER) {
                    err('Cannot push this validation result.');
                    return false;
                }
            }
            if (!data || !data.rootDefinitions) {
                err('No data loaded.');
                return false;
            }

            let workSerial = '';
            if (moddedRelaxed) {
                workSerial =
                    details.inputSerial != null && String(details.inputSerial).trim()
                        ? String(details.inputSerial).trim()
                        : '';
                if (!workSerial) {
                    const siMi = document.getElementById('mi_finalOutputString');
                    const siParse = document.getElementById('mi_parseSerialInput');
                    const siLeg = document.getElementById('serialInput');
                    workSerial =
                        (siMi && siMi.value.trim()) ||
                        (siParse && siParse.value.trim()) ||
                        (siLeg && siLeg.value.trim()) ||
                        '';
                }
            } else if (vc === SERIAL_VALIDATION_CLASS.PASS_ALL) {
                workSerial =
                    details.inputSerial != null && String(details.inputSerial).trim()
                        ? String(details.inputSerial).trim()
                        : '';
                if (!workSerial) {
                    const si =
                        document.getElementById('serialInput') ||
                        document.getElementById('mi_finalOutputString');
                    workSerial = si ? si.value.trim() : '';
                }
            } else {
                workSerial =
                    details.reorderedSerial != null && String(details.reorderedSerial).trim()
                        ? String(details.reorderedSerial).trim()
                        : '';
                if (options.reorderTextareaRoot) {
                    const ta = options.reorderTextareaRoot.querySelector(
                        'textarea[data-reordered-serial-text]'
                    );
                    if (ta && ta.value.trim()) {
                        workSerial = ta.value.trim();
                    }
                }
            }
            if (!workSerial) {
                err(
                    !moddedRelaxed && vc === SERIAL_VALIDATION_CLASS.FAIL_ORDER
                        ? 'Add or generate a reordered serial first, then push.'
                        : 'No deserialized serial to push.'
                );
                return false;
            }

            let fd;
            if (moddedRelaxed) {
                fd = details;
                if (!fd.rootKey || !Array.isArray(fd.validatedParts)) {
                    const parsed = validateSerialCore(workSerial, 1, { includePartDetails: true });
                    fd = parsed.details || {};
                }
            } else {
                const fresh = validateSerialCore(workSerial, 1, { includePartDetails: true });
                fd = fresh.details || {};
                const fvc =
                    fd.validationClass ||
                    (fresh.valid ? SERIAL_VALIDATION_CLASS.PASS_ALL : SERIAL_VALIDATION_CLASS.HARD_FAIL);
                if (fvc !== SERIAL_VALIDATION_CLASS.PASS_ALL && fvc !== SERIAL_VALIDATION_CLASS.FAIL_ORDER) {
                    err('Re-validation failed — cannot push to builder.');
                    return false;
                }
            }
            if (!fd.rootKey || !Array.isArray(fd.validatedParts)) {
                err(moddedRelaxed ? 'Missing root or parts in decode result.' : 'Missing root or parts after re-validation.');
                return false;
            }
            const rootKeyPush = String(fd.rootKey || '').trim();
            const rootDef = data.rootDefinitions.find(function (r) {
                return r && String(r.key || '').trim() === rootKeyPush;
            });
            if (!rootDef) {
                err('Root definition not found for this item.');
                return false;
            }

            let rawParts = fd.validatedParts.filter(function (p) {
                if (!p) return false;
                if (moddedRelaxed) {
                    if (p.found && p.partKey) return true;
                    return !p.found && !!formatValidatorPartRawToken(p);
                }
                if (!p.found || !p.partKey) return false;
                return p.rootMatches !== false && p.isValid !== false;
            });
            if (rawParts.length === 0) {
                err('No valid parts to apply to the builder.');
                return false;
            }
            if (moddedRelaxed) {
                miEnsureItemEditorMainTabActive({ skipViewportScroll: skipModdedAutoScroll });
            } else {
                window.legitBuilderSetCtx(0);
                window.__suppressScrollMainViewportToTopOnce = true;
                if (typeof window.switchTab === 'function') {
                    window.switchTab('legit-builder-tab');
                }
                window.legitBuilderSetCtx(0);
            }
            let ordered;
            if (moddedRelaxed) {
                // Modded Items: preserve validator / deserialized token order. Canonical dependency
                // sorting is for Legit builds only; reordering long modded serials regroups passive
                // chains and breaks tier-collapse, which dropped or reshaped repeated tokens when
                // rebuilding the human-readable serial.
                ordered = rawParts.slice();
            } else {
                ordered = sortPartsByCanonicalAndDependencyOrder(
                    rawParts,
                    rootDef,
                    formatTagsForBuilderPush
                );
                ordered = sortArrayBlockRunsByDependency(ordered, formatTagsForBuilderPush);
            }
            const keySet = new Set();
            ordered.forEach(function (p) {
                if (moddedRelaxed && (!p.found || !p.partKey)) {
                    const raw = formatValidatorPartRawToken(p);
                    const ref = makeMiMissingPartRef(raw);
                    if (ref) keySet.add(ref);
                    return;
                }
                if (p.partKey) {
                    keySet.add(p.partKey);
                }
            });
            if (keySet.size === 0) {
                err('No part keys to apply.');
                return false;
            }

            if (moddedRelaxed && !skipModdedAutoScroll) {
                window.__miPushScrollLatest = (window.__miPushScrollLatest || 0) + 1;
                miScrollTokenPassed = window.__miPushScrollLatest;
            }

            clearAllSelectedParts({ skipDisplayRootDefinitions: true });
            _sp().set(rootKeyPush, keySet);
            if (moddedRelaxed) {
                const miInstPush = window.legitBuilderInstances && window.legitBuilderInstances[1];
                if (miInstPush && miInstPush.miPartLines) {
                    let pushLines = ordered.map(function (p) {
                        if (!p) return null;
                        if (!p.found || !p.partKey) {
                            const raw = formatValidatorPartRawToken(p);
                            const mref = makeMiMissingPartRef(raw);
                            if (!mref) return null;
                            return {
                                tableName: MI_DOES_NOT_EXIST_TABLE,
                                partKey: mref,
                                qty: 1,
                                _miOrder: miPartLineEmitOrderFromValidatorPart(p)
                            };
                        }
                        const dep = (p.foundPart && p.foundPart.depTableName)
                            ? p.foundPart
                            : findPartEntryForBuilderSelection(rootDef, p.partKey);
                        let tn = (dep && dep.depTableName) || p.partType || 'Unknown';
                        const rk =
                            dep && dep.rootKey != null
                                ? String(dep.rootKey)
                                : p.foundInRoot
                                  ? String(p.foundInRoot)
                                  : fd.rootKey;
                        const partRootForCross =
                            p.foundInRoot != null && String(p.foundInRoot).trim()
                                ? String(p.foundInRoot)
                                : rk;
                        tn = moddedTableNameNativeOrCross(tn, partRootForCross, rootDef);
                        const pk = String(p.partKey);
                        const ref =
                            rk && pk ? makeMiCrossPartRef(rk, pk) || pk : pk;
                        tn = moddedResolveMiTableNameAfterCrossCheck(rootDef, tn, ref, data);
                        return {
                            tableName: tn,
                            partKey: ref,
                            qty: 1,
                            _miOrder: miPartLineEmitOrderFromValidatorPart(p)
                        };
                    }).filter(Boolean);
                    pushLines = collapsePassivePointsTierRunsForMiLines(pushLines);
                    pushLines = mergeConsecutiveMiPartLines(pushLines);
                    pushLines = coalesceMiModdedLinesByFirstAppearance(pushLines);
                    pushLines = mergeConsecutiveMiPartLines(pushLines);
                    pushLines = reorderMiPartLinesForCompactDom(rootDef, pushLines);
                    pushLines = mergeConsecutiveMiPartLines(pushLines);
                    pushLines = dedupeModdedCrossLinesRedundantWithNative(rootDef, pushLines, data);
                    pushLines = mergeConsecutiveMiPartLines(pushLines);
                    miInstPush.miPartLines.set(rootKeyPush, pushLines);
                }
            }

            const levelEl = lbEl('outputLevel');
            const seedEl = lbEl('outputSeed');
            if (levelEl && fd.level != null && !isNaN(Number(fd.level))) {
                levelEl.value = String(fd.level);
            }
            if (seedEl && fd.seed != null && !isNaN(Number(fd.seed))) {
                seedEl.value = String(fd.seed);
            }

            window.legitBuilderSetCtx(moddedRelaxed ? 1 : 0);
            applyMiSkinUIFromDeserializedTail(workSerial);

            if (data && data.rootDefinitions) {
                displayRootDefinitions();
            }
            // Modded: expand + paint compact slots BEFORE updateFinalOutput — it calls
            // syncAllMiPartLinesFromModdedDom(), which would overwrite miPartLines from still-empty DOM.
            if (moddedRelaxed) {
                window.legitBuilderSetCtx(1);
                const miRootsBeforeOutputs = document.getElementById('mi_rootsContent');
                expandLegitBuilderRootsForRootKey(rootKeyPush, miRootsBeforeOutputs);
            }
            updateSelectedPartsOutput();
            updateFinalOutput();

            window.legitBuilderSetCtx(moddedRelaxed ? 1 : 0);
            switchLegitTab('roots');

            const modalOverlay = document.getElementById('validationDetailsModalOverlay');
            if (
                modalOverlay &&
                window.getComputedStyle(modalOverlay).display !== 'none'
            ) {
                modalOverlay.style.display = 'none';
            }

            if (moddedRelaxed && !skipModdedAutoScroll) {
                window.legitBuilderSetCtx(1);
                const miRootsSync = document.getElementById('mi_rootsContent');
                expandLegitBuilderRootsForRootKey(rootKeyPush, miRootsSync);
                const pushedSync =
                    miRootsSync && fd.rootKey
                        ? Array.from(miRootsSync.querySelectorAll('.root-item')).find(function (item) {
                              const el = item.querySelector('[data-root-key]');
                              const a = el && el.getAttribute('data-root-key');
                              return (
                                  a != null &&
                                  String(a).trim() === String(fd.rootKey || '').trim()
                              );
                          })
                        : null;
                if (miRootsSync) {
                    miScrollModdedPushTargetIntoView(miRootsSync);
                }
                if (pushedSync) {
                    miScrollModdedPushTargetIntoView(pushedSync);
                }
                const rkMicro = rootKeyPush;
                const tokMicro = miScrollTokenPassed;
                queueMicrotask(function () {
                    if (tokMicro != null && tokMicro !== window.__miPushScrollLatest) {
                        return;
                    }
                    window.legitBuilderSetCtx(1);
                    const miRm = document.getElementById('mi_rootsContent');
                    expandLegitBuilderRootsForRootKey(rkMicro, miRm);
                    const pzm =
                        miRm && rkMicro
                            ? Array.from(miRm.querySelectorAll('.root-item')).find(function (item) {
                                  const el = item.querySelector('[data-root-key]');
                                  const a = el && el.getAttribute('data-root-key');
                                  return (
                                      a != null &&
                                      String(a).trim() === String(rkMicro || '').trim()
                                  );
                              })
                            : null;
                    if (miRm) {
                        miScrollModdedPushTargetIntoView(miRm);
                    }
                    if (pzm) {
                        miScrollModdedPushTargetIntoView(pzm);
                    }
                });
            }

            if (skipModdedAutoScroll) {
                if (moddedRelaxed) {
                    updateModdedDecodedSummaryFromValidation(validation);
                }
                return true;
            }

            (function scrollLegitBuilderRootsIntoView() {
                const rootsContainerOverride = moddedRelaxed
                    ? document.getElementById('mi_rootsContent')
                    : null;
                const tabEl = document.getElementById(
                    moddedRelaxed ? 'item-editor-tab' : 'legit-builder-tab'
                );
                const maxAttempts = moddedRelaxed ? 20 : 1;
                function scrollAttempt(attempt) {
                    if (
                        miScrollTokenPassed != null &&
                        miScrollTokenPassed !== window.__miPushScrollLatest
                    ) {
                        return;
                    }
                    expandLegitBuilderRootsForRootKey(fd.rootKey, rootsContainerOverride);
                    window.legitBuilderSetCtx(moddedRelaxed ? 1 : 0);
                    updateFinalOutput();
                    const rootsEl = lbEl('rootsContent');
                    if (rootsEl && typeof rootsEl.getBoundingClientRect === 'function') {
                        try {
                            void rootsEl.getBoundingClientRect();
                        } catch (eBr) { /* ignore */ }
                    }
                    const rkScroll = String(fd.rootKey || '').trim();
                    const pushedRoot =
                        rootsEl && rkScroll
                            ? Array.from(rootsEl.querySelectorAll('.root-item')).find(function (item) {
                                  const el = item.querySelector('[data-root-key]');
                                  const a = el && el.getAttribute('data-root-key');
                                  return a != null && String(a).trim() === rkScroll;
                              })
                            : null;
                    const scrollTarget =
                        pushedRoot ||
                        (rootsEl && !rootsEl.classList.contains('hidden') ? rootsEl : tabEl);
                    const scrollBehavior = moddedRelaxed ? 'auto' : 'smooth';
                    if (moddedRelaxed) {
                        if (rootsEl) {
                            miScrollModdedPushTargetIntoView(rootsEl);
                        }
                        if (pushedRoot) {
                            miScrollModdedPushTargetIntoView(pushedRoot);
                        }
                    } else if (scrollTarget && typeof scrollTarget.scrollIntoView === 'function') {
                        try {
                            scrollTarget.scrollIntoView({
                                behavior: scrollBehavior,
                                block: pushedRoot ? 'nearest' : 'start'
                            });
                        } catch (eSi) {
                            try {
                                scrollTarget.scrollIntoView(true);
                            } catch (e2) { /* ignore */ }
                        }
                        const winScrollEl = pushedRoot || scrollTarget;
                        if (
                            winScrollEl &&
                            typeof winScrollEl.getBoundingClientRect === 'function'
                        ) {
                            try {
                                const docEl = document.documentElement;
                                const rect = winScrollEl.getBoundingClientRect();
                                const vpH =
                                    window.innerHeight ||
                                    (docEl && docEl.clientHeight) ||
                                    0;
                                if (
                                    vpH > 0 &&
                                    (rect.top < 72 || rect.bottom > vpH - 24)
                                ) {
                                    const y =
                                        window.scrollY || window.pageYOffset || 0;
                                    const want =
                                        y +
                                        rect.top -
                                        Math.max(48, vpH * 0.12);
                                    try {
                                        window.scrollTo({
                                            top: Math.max(0, want),
                                            left: 0,
                                            behavior: 'auto'
                                        });
                                    } catch (eW) {
                                        try {
                                            window.scrollTo(0, Math.max(0, want));
                                        } catch (eW2) { /* ignore */ }
                                    }
                                }
                            } catch (eRect) { /* ignore */ }
                        }
                    }
                    if (moddedRelaxed && !pushedRoot && attempt + 1 < maxAttempts) {
                        requestAnimationFrame(function () {
                            requestAnimationFrame(function () {
                                scrollAttempt(attempt + 1);
                            });
                        });
                    }
                }
                requestAnimationFrame(function () {
                    requestAnimationFrame(function () {
                        requestAnimationFrame(function () {
                            scrollAttempt(0);
                        });
                    });
                });
            })();

            if (moddedRelaxed) {
                updateModdedDecodedSummaryFromValidation({
                    valid: true,
                    details: fd
                });
            }

            if (notify) {
                notify(
                    moddedRelaxed ? 'Applied parts to Modded Items builder' : 'Pushed serial to Legit Builder',
                    'success',
                    { durationMs: 3500 }
                );
            }
            return true;
        }

        window.pushValidationToLegitBuilder = pushValidationToLegitBuilder;
        window.pushValidationToLegitBuilderFromUi = function pushValidationToLegitBuilderFromUi() {
            if (typeof window.syncLegitBuilderCtxToActiveMainTab === 'function') {
                window.syncLegitBuilderCtxToActiveMainTab();
            }
            const panel = lbEl('serialValidationResult');
            return pushValidationToLegitBuilder(window.__legitLastSingleValidation, {
                reorderTextareaRoot: panel,
                moddedRelaxed: !!(window.__lbCtx && window.__lbCtx.relaxedPicker)
            });
        };
        window.pushValidationToLegitBuilderFromSaveEditorModal =
            function pushValidationToLegitBuilderFromSaveEditorModal() {
                if (typeof window.syncLegitBuilderCtxToActiveMainTab === 'function') {
                    window.syncLegitBuilderCtxToActiveMainTab();
                }
                const body = document.querySelector(
                    '#save-editor-validation-modal-overlay .save-editor-validation-modal-body'
                );
                const v =
                    window.__legitSaveEditorModalValidation || window.__legitLastSingleValidation;
                return pushValidationToLegitBuilder(v, {
                    reorderTextareaRoot: body,
                    moddedRelaxed: false
                });
            };
        window.pushValidationToLegitBuilderFromInspectModal =
            function pushValidationToLegitBuilderFromInspectModal() {
                if (typeof window.syncLegitBuilderCtxToActiveMainTab === 'function') {
                    window.syncLegitBuilderCtxToActiveMainTab();
                }
                const panel = document.getElementById('validationDetailsModalContent');
                return pushValidationToLegitBuilder(window.__legitInspectValidation, {
                    reorderTextareaRoot: panel,
                    moddedRelaxed: !!(window.__lbCtx && window.__lbCtx.relaxedPicker)
                });
            };
        
        function pushSerialToValidator() {
            const useMerge = !!(window.__lbCtx && window.__lbCtx.relaxedPicker);
            const human = useMerge
                ? (lbGeneratedHumanEl()?.value?.trim() || '')
                : (lbEl('finalOutputString')?.value?.trim() ||
                      lbSerialInputEl()?.value?.trim() ||
                      '');
            const serial = (lbEl('finalOutputBase85')?.value?.trim()) || human;
            if (!serial) {
                alert('No serial available. Select parts and build a serial first.');
                return;
            }
            const serialInput = lbSerialInputEl();
            if (!serialInput) return;
            serialInput.value = serial;
            decodeAndValidateSerial();
            const resultDiv = lbEl('serialValidationResult');
            if (resultDiv && typeof resultDiv.scrollIntoView === 'function') {
                resultDiv.scrollIntoView({ behavior: 'smooth', block: 'start' });
            }
        }

        /** Copy live human-readable serial into parse field and run decode/validate. Modded tab scrolls #mi_parseSerialPanel into view. */
        function pushCurrentBuildHumanSerialToParser(anchorEl) {
            const fromModded = !!(anchorEl && anchorEl.closest && anchorEl.closest('#item-editor-tab'));
            let pre = null;
            if (anchorEl && anchorEl.closest) {
                const panel = anchorEl.closest('.current-build-human-serial-panel--in-root');
                if (panel) {
                    pre = panel.querySelector('.lb-current-build-human-serial-in-root');
                }
            }
            if (!pre) {
                pre = document.getElementById('currentBuildHumanSerial');
            }
            const placeholder = '— Select parts to see the human-readable serial —';
            const raw = pre
                ? String(pre.tagName === 'TEXTAREA' ? pre.value : pre.textContent || '').trim()
                : '';
            if (!raw || raw === placeholder) {
                if (typeof showError === 'function') {
                    showError('No human-readable build yet. Select parts first.');
                } else {
                    alert('No human-readable build yet. Select parts first.');
                }
                return;
            }
            window.legitBuilderSetCtx(fromModded ? 1 : 0);
            if (fromModded && typeof window.switchTab === 'function') {
                window.switchTab('item-editor-tab');
            }
            const serialInputEl = fromModded ? lbSerialInputEl() : lbSerialInputEl();
            if (!serialInputEl) return;
            if (fromModded) {
                const panelEl =
                    document.getElementById('mi_parseSerialPanel') ||
                    document.getElementById('mi_modeSelector') ||
                    document.getElementById('item-editor-tab');
                const scrollModdedTop = function () {
                    if (panelEl && typeof panelEl.scrollIntoView === 'function') {
                        panelEl.scrollIntoView({ behavior: 'smooth', block: 'start' });
                    }
                };
                if (typeof requestAnimationFrame === 'function') {
                    requestAnimationFrame(function () {
                        requestAnimationFrame(scrollModdedTop);
                    });
                } else {
                    setTimeout(scrollModdedTop, 0);
                }
            }
            serialInputEl.value = raw;
            if (!fromModded && typeof serialInputEl.scrollIntoView === 'function') {
                serialInputEl.scrollIntoView({ behavior: 'smooth', block: 'center' });
            }
            decodeAndValidateSerial();
            const resultDiv = lbEl('serialValidationResult');
            if (resultDiv && typeof resultDiv.scrollIntoView === 'function') {
                resultDiv.scrollIntoView({ behavior: 'smooth', block: 'start' });
            }
        }
        window.pushCurrentBuildHumanSerialToParser = pushCurrentBuildHumanSerialToParser;

        window.pushModdedSerialToBuilderFromPanel = async function pushModdedSerialToBuilderFromPanel() {
            if (window.__miPushModdedSerialInFlight) {
                return;
            }
            window.__miPushModdedSerialInFlight = true;
            try {
                miEnsureItemEditorMainTabActive();
                const genEl = lbGeneratedHumanEl();
                const paste = genEl ? genEl.value.trim() : '';
                const b85 = (lbEl('finalOutputBase85') && lbEl('finalOutputBase85').value.trim()) || '';
                const work = paste || b85;
                if (!work) {
                    alert('No serial available. Paste a serial, parse it, or build one from parts first.');
                    return;
                }
                if (!data || !data.rootDefinitions) {
                    if (typeof showError === 'function') {
                        showError('No data loaded. Please load JSON files first.');
                    } else {
                        alert('No data loaded.');
                    }
                    return;
                }
                let deserialized = work;
                if (typeof isBase85Format === 'function' && isBase85Format(work)) {
                    try {
                        const response = await fetch(SERIAL_API_URL, {
                            method: 'POST',
                            headers: { 'Content-Type': 'application/json' },
                            body: JSON.stringify({ serial_b85: work.trim() })
                        });
                        if (!response.ok) {
                            throw new Error('API error: ' + response.status);
                        }
                        const result = await response.json();
                        if (result.error) {
                            throw new Error(result.error);
                        }
                        if (!result.deserialized || !String(result.deserialized).trim()) {
                            throw new Error('No deserialized result from API.');
                        }
                        deserialized = String(result.deserialized).trim();
                    } catch (e) {
                        const m = e && e.message ? e.message : String(e);
                        if (typeof showError === 'function') {
                            showError('Could not deserialize Base85: ' + m);
                        } else {
                            alert(m);
                        }
                        return;
                    }
                }
                const flagsPush = parseModdedFirmwareBuybackFlagsFromDeserialized(deserialized);
                const fwP = document.getElementById('mi_firmwareLock');
                const bbP = document.getElementById('mi_buyback');
                if (fwP) {
                    fwP.checked = flagsPush.firmwareLock;
                }
                if (bbP) {
                    bbP.checked = flagsPush.buyback;
                }
                deserialized = mergeModdedSkinUiIntoHumanSerialText(deserialized);
                applyMiSkinUIFromDeserializedTail(deserialized);
                const validation = validateSerialCore(deserialized, 1, { includePartDetails: true });
                window.__legitLastSingleValidation = validation;
                pushValidationToLegitBuilder(validation, { moddedRelaxed: true });
            } finally {
                window.__miPushModdedSerialInFlight = false;
            }
        };

        (function bindMiPushModdedBuilderFromParser() {
            function attach() {
                var btn = document.getElementById('mi_pushModdedBuilderBtn');
                if (!btn || btn.getAttribute('data-mi-push-modded-bound') === '1') {
                    return;
                }
                btn.setAttribute('data-mi-push-modded-bound', '1');
                btn.addEventListener(
                    'click',
                    function miPushModdedBuilderFromParserClick() {
                        if (typeof window.legitBuilderSetCtx === 'function') {
                            window.legitBuilderSetCtx(1);
                        }
                        if (typeof window.pushModdedSerialToBuilderFromPanel === 'function') {
                            void window.pushModdedSerialToBuilderFromPanel();
                        }
                    },
                    false
                );
            }
            attach();
            if (document.readyState === 'loading') {
                document.addEventListener('DOMContentLoaded', attach);
            }
        })();
        var pushModdedSerialToBuilderFromPanel = window.pushModdedSerialToBuilderFromPanel;

        window.clearModdedParserOutputAndSelection = function clearModdedParserOutputAndSelection() {
            window.legitBuilderSetCtx(1);
            try {
                const fw = document.getElementById('mi_firmwareLock');
                const bb = document.getElementById('mi_buyback');
                if (fw) {
                    fw.checked = false;
                }
                if (bb) {
                    bb.checked = false;
                }
            } catch (eFw) {}
            if (typeof window.clearSerialDecoderValidator === 'function') {
                window.clearSerialDecoderValidator();
            }
            clearModdedDecodedSummaryAndSkinUi();
            clearAllSelectedParts();
            const genEl = document.getElementById('mi_finalOutputString');
            const b85El = document.getElementById('mi_finalOutputBase85');
            if (genEl) {
                genEl.value = '';
            }
            if (b85El) {
                b85El.value = '';
            }
        };

        window.clearSerialDecoderValidator = function clearSerialDecoderValidator() {
            const serialInputEl = lbSerialInputEl();
            if (serialInputEl) {
                serialInputEl.value = '';
            }
            const resultDiv = lbEl('serialValidationResult');
            if (resultDiv) {
                resultDiv.innerHTML = '';
                resultDiv.style.display = 'none';
                if (
                    resultDiv.id === 'mi_serialValidationResult' &&
                    typeof window.setMiModdedDecodeExpanded === 'function'
                ) {
                    window.setMiModdedDecodeExpanded(false);
                }
            }
            try {
                window.skipBase85Check = false;
            } catch (e) {}
            try {
                window.__legitLastSingleValidation = null;
            } catch (e2) {}
            try {
                window.__legitBackpackContext = window.__legitBackpackContext || {};
                window.__legitBackpackContext.lastDeserialized = '';
                window.__legitBackpackContext.lastBase85 = null;
                window.__legitBackpackContext.pendingFromB85 = false;
            } catch (e3) {}
            const modalOverlay = document.getElementById('validationDetailsModalOverlay');
            if (modalOverlay) {
                modalOverlay.style.display = 'none';
            }
            if (typeof window.updateLegitValidatorBackpackButton === 'function') {
                window.updateLegitValidatorBackpackButton();
            }
            if (serialInputEl && typeof serialInputEl.scrollIntoView === 'function') {
                try {
                    serialInputEl.scrollIntoView({ behavior: 'smooth', block: 'center' });
                } catch (e4) {}
            }
        };

        window.clearBulkSerialValidator = function clearBulkSerialValidator() {
            const bulkInputEl = lbEl('bulkSerialInput');
            if (bulkInputEl) {
                bulkInputEl.value = '';
            }
            const resultDiv = lbEl('bulkValidationResult');
            if (resultDiv) {
                resultDiv.innerHTML = '';
                resultDiv.style.display = 'none';
            }
            try {
                window.__legitInspectValidation = null;
            } catch (e) {}
            const modalOverlay = document.getElementById('validationDetailsModalOverlay');
            if (modalOverlay) {
                modalOverlay.style.display = 'none';
            }
            if (typeof window.updateLegitValidatorBackpackButton === 'function') {
                window.updateLegitValidatorBackpackButton();
            }
            if (bulkInputEl && typeof bulkInputEl.scrollIntoView === 'function') {
                try {
                    bulkInputEl.scrollIntoView({ behavior: 'smooth', block: 'center' });
                } catch (e2) {}
            }
        };

        window.clearLegitBuilderTab = function clearLegitBuilderTab() {
            window.legitBuilderSetCtx(0);
            if (typeof window.clearSerialDecoderValidator === 'function') {
                window.clearSerialDecoderValidator();
            }
            if (typeof window.clearBulkSerialValidator === 'function') {
                window.clearBulkSerialValidator();
            }
            const levelEl = lbEl('outputLevel');
            const seedEl = lbEl('outputSeed');
            if (levelEl) levelEl.value = '60';
            if (seedEl) seedEl.value = '1';
            clearAllSelectedParts();
        };

        window.clearModdedItemsBuilderTab = function clearModdedItemsBuilderTab() {
            window.legitBuilderSetCtx(1);
            if (typeof window.clearSerialDecoderValidator === 'function') {
                window.clearSerialDecoderValidator();
            }
            if (typeof window.clearBulkSerialValidator === 'function') {
                window.clearBulkSerialValidator();
            }
            clearModdedDecodedSummaryAndSkinUi();
            const levelEl = lbEl('outputLevel');
            const seedEl = lbEl('outputSeed');
            if (levelEl) levelEl.value = '60';
            if (seedEl) seedEl.value = '1';
            clearAllSelectedParts();
        };
        
        function copyFinalOutput(btn) {
            const outputTextarea = lbEl('finalOutputString');
            if (!outputTextarea) {
                return;
            }
            const text = outputTextarea.value != null ? String(outputTextarea.value).trim() : '';
            copyTextUsingClipboardOrFallback(text, btn);
        }

        function exportParsedData() {
            if (!data || !data.rootDefinitions || data.rootDefinitions.length === 0) {
                showError('No data to export. Please load JSON files first.');
                return;
            }

            try {
                // Prepare export data in a clean format
                const exportData = {
                    metadata: {
                        exportedAt: new Date().toISOString(),
                        version: '1.0',
                        totalRootDefinitions: data.rootDefinitions ? data.rootDefinitions.length : 0,
                        totalSubItems: data.totalSubItems || 0,
                        multiplierStructs: data.multiplierStructs ? Object.keys(data.multiplierStructs).length : 0,
                        sourceFiles: loadedFileNames.length > 0 ? loadedFileNames : ['unknown']
                    },
                    rootDefinitions: data.rootDefinitions || [],
                    dependencyTables: data.dependencyTablesObj || {},
                    subItemsByTable: data.subItemsByTable || {},
                    multiplierStructs: data.multiplierStructs || {},
                    compositionSummary: data.compositionSummary || null,
                    statistics: {
                        totalRootDefinitions: data.rootDefinitions ? data.rootDefinitions.length : 0,
                        totalSubItems: data.totalSubItems || 0,
                        dependencyTables: Object.keys(data.dependencyTablesObj || {}).length,
                        multiplierStructs: data.multiplierStructs ? Object.keys(data.multiplierStructs).length : 0
                    }
                };

                // Convert to JSON string with pretty formatting
                const jsonString = JSON.stringify(exportData, null, 2);
                
                // Create a blob and download link
                const blob = new Blob([jsonString], { type: 'application/json' });
                const url = URL.createObjectURL(blob);
                const link = document.createElement('a');
                link.href = url;
                
                // Generate filename with timestamp
                const timestamp = new Date().toISOString().replace(/[:.]/g, '-').split('T')[0] + '_' + 
                                  new Date().toISOString().split('T')[1].split('.')[0].replace(/:/g, '-');
                link.download = `inventory_data_parsed_${timestamp}.json`;
                
                // Trigger download
                document.body.appendChild(link);
                link.click();
                document.body.removeChild(link);
                
                // Clean up
                URL.revokeObjectURL(url);
                
                // Show success message
                const errorDiv = lbEl('errorMessage');
                errorDiv.textContent = `✅ Data exported successfully! (${data.rootDefinitions.length} root definitions)`;
                errorDiv.className = 'error success';
                errorDiv.classList.remove('hidden');
                setTimeout(() => {
                    errorDiv.classList.add('hidden');
                }, 3000);
            } catch (error) {
                showError('Error exporting data: ' + error.message);
                console.error('Export error:', error);
            }
        }

        // Build-level multiplier combiner - aggregates multipliers from selected parts
        function combineBuildMultipliers(selectedPartItems) {
            if (!selectedPartItems || selectedPartItems.length === 0) {
                return { Value: {}, Scale: {}, Add: {} };
            }

            const buildMultipliers = {
                Value: {},
                Scale: {},
                Add: {}
            };

            selectedPartItems.forEach(subItem => {
                if (!subItem.multipliers) return;

                // Process Scale bucket - product, start at 1
                if (subItem.multipliers.Scale) {
                    Object.entries(subItem.multipliers.Scale).forEach(([key, sources]) => {
                        if (!Array.isArray(sources)) return;
                        if (!buildMultipliers.Scale[key]) {
                            buildMultipliers.Scale[key] = { product: 1, parts: [] };
                        }
                        sources.forEach(source => {
                            buildMultipliers.Scale[key].product *= (source.value || 1);
                            buildMultipliers.Scale[key].parts.push({
                                partKey: subItem.key,
                                rowname: source.rowname,
                                struct: source.struct,
                                value: source.value
                            });
                        });
                    });
                }

                // Process Add bucket - sum, start at 0
                if (subItem.multipliers.Add) {
                    Object.entries(subItem.multipliers.Add).forEach(([key, sources]) => {
                        if (!Array.isArray(sources)) return;
                        if (!buildMultipliers.Add[key]) {
                            buildMultipliers.Add[key] = { sum: 0, parts: [] };
                        }
                        sources.forEach(source => {
                            buildMultipliers.Add[key].sum += (source.value || 0);
                            buildMultipliers.Add[key].parts.push({
                                partKey: subItem.key,
                                rowname: source.rowname,
                                struct: source.struct,
                                value: source.value
                            });
                        });
                    });
                }

                // Process Value bucket - sum by default (start 0), but preserve raw values per part for debugging
                if (subItem.multipliers.Value) {
                    Object.entries(subItem.multipliers.Value).forEach(([key, sources]) => {
                        if (!Array.isArray(sources)) return;
                        if (!buildMultipliers.Value[key]) {
                            buildMultipliers.Value[key] = { sum: 0, parts: [] };
                        }
                        sources.forEach(source => {
                            buildMultipliers.Value[key].sum += (source.value || 0);
                            buildMultipliers.Value[key].parts.push({
                                partKey: subItem.key,
                                rowname: source.rowname,
                                struct: source.struct,
                                value: source.value
                            });
                        });
                    });
                }
            });

            // Clean up empty buckets
            Object.keys(buildMultipliers).forEach(bucket => {
                if (Object.keys(buildMultipliers[bucket]).length === 0) {
                    delete buildMultipliers[bucket];
                }
            });

            // If all buckets are empty, return undefined
            if (Object.keys(buildMultipliers).length === 0) {
                return undefined;
            }

            return buildMultipliers;
        }

        // Get selected parts as subItems for a given root
        function getSelectedPartItems(rootKey) {
            if (!data || !data.rootDefinitions) return [];
            
            const root = data.rootDefinitions.find(r => r.key === rootKey);
            if (!root) return [];

            const inst = window.legitBuilderInstances && window.legitBuilderInstances[1];
            if (inst && inst.relaxedPicker && inst.miPartLines && inst.miPartLines.has(rootKey)) {
                const miLines = inst.miPartLines.get(rootKey) || [];
                if (miLines.length === 0) return [];
                const selectedItems = [];
                miLines.forEach(({ partKey, qty }) => {
                    if (!partKey) return;
                    const keys = expandModdedMiLineToPartKeys(partKey, qty);
                    keys.forEach(pk => {
                        let dep = null;
                        const crossP = parseMiCrossPartRef(pk);
                        if (crossP && data && data.rootDefinitions) {
                            const srRoot = data.rootDefinitions.find(r => r.key === crossP.sourceRootKey);
                            dep = srRoot && srRoot.depEntries && srRoot.depEntries.find(d => d.key === crossP.partKey);
                        }
                        if (!dep) {
                            dep =
                                (root.depEntries || []).find(d => d.key === pk) ||
                                findPartEntryForBuilderSelection(root, pk);
                        }
                        if (dep) selectedItems.push(dep);
                    });
                });
                return selectedItems;
            }

            if (!root.depEntries) return [];
            const selectedPartKeys = _sp().has(rootKey) ? _sp().get(rootKey) : new Set();
            const selectedItems = [];
            root.depEntries.forEach(dep => {
                if (selectedPartKeys.has(dep.key)) {
                    selectedItems.push(dep);
                }
            });
            return selectedItems;
        }

        function displayMultiplierStructs() {
            const container = lbEl('multipliersContent');
            if (!container) return;

            if (!data || !data.multiplierStructs || Object.keys(data.multiplierStructs).length === 0) {
                container.innerHTML = '<div class="loading">No multiplier structs loaded</div>';
                return;
            }

            const structs = data.multiplierStructs;
            const structCount = Object.keys(structs).length;
            const mid = window.__lbCtx && window.__lbCtx.idPrefix ? window.__lbCtx.idPrefix : '';
            const lbIdx = window.__lbCtx ? window.__lbCtx.index : 0;

            let html = `
                <div style="margin-bottom: 20px;">
                    <div style="font-size: 14px; color: #aaa; margin-bottom: 15px;">Showing ${structCount} multiplier struct${structCount !== 1 ? 's' : ''}</div>
                    <input type="text" id="${mid}multiplierSearchInput" placeholder="Search struct name or stat..." 
                           style="width: 100%; max-width: 500px; padding: 8px 12px; background: #3a3a3a; border: 1px solid #4a4a4a; border-radius: 4px; color: #e0e0e0; font-size: 14px;"
                           onkeyup="legitBuilderSetCtx(${lbIdx}); filterMultiplierStructs()">
                </div>
                <div id="${mid}multiplierStructsList">
            `;

            // Sort struct names alphabetically
            const sortedStructNames = Object.keys(structs).sort((a, b) => a.localeCompare(b));

            sortedStructNames.forEach(structName => {
                const struct = structs[structName];
                const normalized = struct.normalized || {};
                const normalizedKeys = Object.keys(normalized);
                const keyCount = normalizedKeys.length;

                // Sort normalized keys alphabetically
                const sortedKeys = normalizedKeys.sort((a, b) => a.localeCompare(b));

                html += `
                    <div class="struct-card" data-struct-name="${structName}" style="margin-bottom: 15px; background: #3a3a3a; border-radius: 8px; border-left: 4px solid #4CAF50; overflow: hidden;">
                        <div class="struct-header-clickable" style="padding: 12px 15px; cursor: pointer; user-select: none; display: flex; align-items: center; transition: background 0.3s;" 
                             onclick="this.classList.toggle('expanded'); this.nextElementSibling.classList.toggle('collapsed');">
                            <span class="struct-expand-icon" style="display: inline-block; width: 20px; text-align: center; margin-right: 10px; transition: transform 0.3s;">▶</span>
                            <div style="flex: 1;">
                                <div style="font-size: 16px; font-weight: bold; color: #4CAF50; margin-bottom: 4px;">${structName}</div>
                                <div style="font-size: 12px; color: #aaa;">${keyCount} normalized propert${keyCount !== 1 ? 'ies' : 'y'}</div>
                            </div>
                        </div>
                        <div class="struct-content collapsed" style="padding: 15px; background: #2a2a2a;">
                            <div style="margin-bottom: 15px;">
                                <div style="font-size: 12px; color: #4CAF50; font-weight: bold; margin-bottom: 8px;">Normalized Properties:</div>
                                <div style="background: #1a1a1a; border-radius: 4px; padding: 10px; overflow-x: auto;">
                                    <table style="width: 100%; border-collapse: collapse; font-size: 12px;">
                                        <thead>
                                            <tr style="border-bottom: 1px solid #444;">
                                                <th style="text-align: left; padding: 8px; color: #4CAF50; font-weight: bold;">Property</th>
                                                <th style="text-align: left; padding: 8px; color: #4CAF50; font-weight: bold;">Value</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            ${sortedKeys.map(key => {
                                                const value = normalized[key];
                                                const valueStr = typeof value === 'number' ? value.toString() : JSON.stringify(value);
                                                return `
                                                    <tr style="border-bottom: 1px solid #333;">
                                                        <td style="padding: 8px; color: #fff; font-weight: bold;">${key}</td>
                                                        <td style="padding: 8px; color: #aaa; font-family: monospace;">${valueStr}</td>
                                                    </tr>
                                                `;
                                            }).join('')}
                                        </tbody>
                                    </table>
                                </div>
                                <button onclick="copyStructJSON('${structName}', this)" 
                                        style="margin-top: 10px; padding: 6px 12px; background: #4CAF50; border: none; border-radius: 4px; color: #e0e0e0; font-size: 12px; cursor: pointer; font-weight: bold; transition: background 0.3s;"
                                        onmouseover="this.style.background='#45a049'" 
                                        onmouseout="this.style.background='#4CAF50'">
                                    📋 Copy normalized JSON
                                </button>
                            </div>
                            <div style="margin-top: 15px; padding-top: 15px; border-top: 1px solid #444;">
                                <div style="font-size: 12px; color: #FFA500; font-weight: bold; margin-bottom: 8px; cursor: pointer; user-select: none; display: flex; align-items: center;"
                                     onclick="this.classList.toggle('expanded'); this.nextElementSibling.classList.toggle('collapsed');">
                                    <span class="raw-expand-icon" style="display: inline-block; width: 20px; text-align: center; margin-right: 10px; transition: transform 0.3s;">▶</span>
                                    Raw DefaultProperties
                                </div>
                                <div class="raw-content collapsed" style="margin-top: 8px; background: #1a1a1a; border-radius: 4px; padding: 10px; max-height: 400px; overflow-y: auto;">
                                    <pre style="margin: 0; color: #aaa; font-size: 11px; white-space: pre-wrap; word-wrap: break-word;">${JSON.stringify(struct.rawDefaultProperties, null, 2)}</pre>
                                </div>
                            </div>
                        </div>
                    </div>
                `;
            });

            html += '</div>';

            container.innerHTML = html;

            // Set up search filter event listener
            const searchInput = lbEl('multiplierSearchInput');
            if (searchInput) {
                searchInput.addEventListener('input', filterMultiplierStructs);
            }

            // Set up expand icon rotation
            setupStructExpandIcons();
        }

        function filterMultiplierStructs() {
            const searchInput = lbEl('multiplierSearchInput');
            if (!searchInput) return;

            const searchTerm = searchInput.value.toLowerCase().trim();
            const host = lbEl('multipliersContent');
            const structCards = host ? host.querySelectorAll('.struct-card') : document.querySelectorAll('.struct-card');

            if (!searchTerm) {
                // Show all structs
                structCards.forEach(card => {
                    card.style.display = 'block';
                });
                return;
            }

            structCards.forEach(card => {
                const structName = (card.getAttribute('data-struct-name') || '').toLowerCase();
                
                // Get normalized keys from the struct data
                const struct = data && data.multiplierStructs ? data.multiplierStructs[card.getAttribute('data-struct-name')] : null;
                const normalized = struct?.normalized || {};
                const normalizedKeys = Object.keys(normalized).map(k => k.toLowerCase());

                // Check if search term matches struct name or any normalized key
                const matches = structName.includes(searchTerm) || 
                               normalizedKeys.some(key => key.includes(searchTerm));

                card.style.display = matches ? 'block' : 'none';
            });
        }

        function copyStructJSON(structName, buttonElement) {
            if (!data || !data.multiplierStructs || !data.multiplierStructs[structName]) {
                return;
            }

            const normalized = data.multiplierStructs[structName].normalized;
            const jsonStr = JSON.stringify(normalized, null, 2);

            // Copy to clipboard
            navigator.clipboard.writeText(jsonStr).then(() => {
                // Show feedback
                const button = buttonElement || window.event?.target;
                if (button) {
                    const originalText = button.textContent;
                    button.textContent = '✓ Copied!';
                    button.style.background = '#45a049';
                    setTimeout(() => {
                        button.textContent = originalText;
                        button.style.background = '#4CAF50';
                    }, 2000);
                }
            }).catch(err => {
                console.error('Failed to copy to clipboard:', err);
                showError('Failed to copy to clipboard');
            });
        }

        function setupStructExpandIcons() {
            // Set up expand icon rotation for struct headers
            document.querySelectorAll('.struct-header-clickable').forEach(header => {
                const icon = header.querySelector('.struct-expand-icon');
                if (icon) {
                    const updateIcon = () => {
                        if (header.classList.contains('expanded')) {
                            icon.style.transform = 'rotate(90deg)';
                        } else {
                            icon.style.transform = 'rotate(0deg)';
                        }
                    };
                    // Update immediately
                    updateIcon();
                    // Also update on click (already handled by onclick, but ensure icon rotates)
                    header.addEventListener('click', () => {
                        setTimeout(updateIcon, 0);
                    });
                }
            });

            // Set up expand icon rotation for raw content headers
            document.querySelectorAll('.raw-content').forEach(rawContent => {
                const header = rawContent.previousElementSibling;
                if (header) {
                    const icon = header.querySelector('.raw-expand-icon');
                    if (icon) {
                        const updateIcon = () => {
                            if (header.classList.contains('expanded')) {
                                icon.style.transform = 'rotate(90deg)';
                            } else {
                                icon.style.transform = 'rotate(0deg)';
                            }
                        };
                        // Update immediately
                        updateIcon();
                        // Also update on click (already handled by onclick, but ensure icon rotates)
                        header.addEventListener('click', () => {
                            setTimeout(updateIcon, 0);
                        });
                    }
                }
            });
        }

        // Top-level application mode (Simple/Advanced). Always starts simple on full page load (see initializeApplicationMode).
        let applicationMode = 'simple';

        /** Five plain left-clicks on #dataStatusIndicator: unlock Simple/Advanced toggles, switch Legit Builder to Advanced, open multi-file picker for Nexus JSON. Sequence resets if idle longer than CLICK_RESET_MS. */
        window.__legitBuilderAdvancedUnlocked = false;

        function updateModeSelectorUnlockUI() {
            document.querySelectorAll('.mode-selector-toggles').forEach(function (wrap) {
                wrap.style.display = window.__legitBuilderAdvancedUnlocked ? 'flex' : 'none';
            });
        }

        function wireDataStatusIndicatorAdvancedUnlock() {
            const el = document.getElementById('dataStatusIndicator');
            if (!el || el.getAttribute('data-adv-unlock-wired') === '1') return;
            el.setAttribute('data-adv-unlock-wired', '1');
            const CLICK_RESET_MS = 4000;
            let lastTs = 0;
            let seq = 0;
            el.addEventListener(
                'click',
                function (e) {
                    if (e.button !== 0) return;
                    if (e.ctrlKey || e.metaKey) {
                        return;
                    }
                    const now = Date.now();
                    if (lastTs && now - lastTs > CLICK_RESET_MS) {
                        seq = 0;
                    }
                    lastTs = now;
                    seq++;
                    if (seq >= 5) {
                        seq = 0;
                        e.preventDefault();
                        e.stopPropagation();
                        window.__legitBuilderAdvancedUnlocked = true;
                        if (typeof window.legitBuilderSetCtx === 'function') {
                            window.legitBuilderSetCtx(0);
                        }
                        setApplicationMode('advanced');
                        const nexusInput = document.getElementById('fileInput');
                        if (nexusInput) nexusInput.click();
                        return false;
                    }
                },
                false
            );
        }
        
        function setApplicationMode(mode) {
            if (mode === 'advanced' && !window.__legitBuilderAdvancedUnlocked) {
                mode = 'simple';
            }
            applicationMode = mode;
            localStorage.setItem('applicationMode', mode);
            localStorage.setItem('rootDefinitionsViewMode', mode); // Sync with root definitions view
            
            document.querySelectorAll('.legit-inline-app').forEach(function (el) {
                el.classList.remove('simple-mode', 'advanced-mode');
                el.classList.add(mode + '-mode');
            });

            forEachLegitInstance(function () {
                const simpleBtn = lbEl('simpleModeBtn');
                const advancedBtn = lbEl('advancedModeBtn');
                if (simpleBtn && advancedBtn) {
                    if (mode === 'simple') {
                        simpleBtn.classList.add('active');
                        simpleBtn.style.background = 'var(--accent-primary)';
                        simpleBtn.style.color = '#fff';
                        simpleBtn.style.border = 'none';
                        advancedBtn.classList.remove('active');
                        advancedBtn.style.background = 'var(--bg-tertiary)';
                        advancedBtn.style.color = 'var(--text-secondary)';
                        advancedBtn.style.border = '1px solid var(--border-color)';
                    } else {
                        advancedBtn.classList.add('active');
                        advancedBtn.style.background = 'var(--accent-primary)';
                        advancedBtn.style.color = '#fff';
                        advancedBtn.style.border = 'none';
                        simpleBtn.classList.remove('active');
                        simpleBtn.style.background = 'var(--bg-tertiary)';
                        simpleBtn.style.color = 'var(--text-secondary)';
                        simpleBtn.style.border = '1px solid var(--border-color)';
                    }
                }
                const simpleModeHelp = lbEl('simpleModeHelp');
                if (simpleModeHelp) {
                    if (data && data.rootDefinitions && data.rootDefinitions.length > 0) {
                        if (mode === 'simple') {
                            simpleModeHelp.style.display = 'block';
                        } else {
                            simpleModeHelp.style.display = 'none';
                        }
                    } else {
                        simpleModeHelp.style.display = 'none';
                    }
                }
            });

            if (data) {
                forEachLegitInstance(function () {
                    displayRootDefinitions();
                });
                const miTab = document.getElementById('item-editor-tab');
                if (miTab && typeof miInitCrossManufacturerPickers === 'function') {
                    miTab.querySelectorAll('.mi-cross-picker-host').forEach(function (host) {
                        delete host.dataset.miCatalogInit;
                    });
                    miInitCrossManufacturerPickers(miTab);
                }
            }
            updateModeSelectorUnlockUI();
        }

        function setRootDefinitionsViewMode(mode) {
            // This is now just for internal use - sync with application mode
            localStorage.setItem('rootDefinitionsViewMode', mode);
            if (data) {
                displayRootDefinitions();
            }
        }
        
        // Initialize mode on page load — always simple; Item Builder advanced must be unlocked each session (data indicator).
        function initializeApplicationMode() {
            window.__legitBuilderAdvancedUnlocked = false;
            applicationMode = 'simple';
            try {
                localStorage.setItem('applicationMode', 'simple');
                localStorage.setItem('rootDefinitionsViewMode', 'simple');
            } catch (e) {
                /* ignore */
            }
            updateModeSelectorUnlockUI();
            setApplicationMode('simple');
            wireDataStatusIndicatorAdvancedUnlock();
        }

        function switchLegitTab(tab) {
            if (window.__lbCtx) window.__lbCtx.innerTab = tab;

            const legitPane = document.getElementById(window.__lbCtx && window.__lbCtx.tabId ? window.__lbCtx.tabId : 'legit-builder-tab');
            if (legitPane) {
                legitPane.querySelectorAll('.tab').forEach(btn => {
                    btn.classList.remove('active');
                    const oc = btn.getAttribute('onclick') || '';
                    if (
                        oc.includes("switchLegitTab('" + tab + "')") ||
                        oc.includes('switchLegitTab("' + tab + '")')
                    ) {
                        btn.classList.add('active');
                    }
                });
            } else {
                document.querySelectorAll('.tab').forEach(btn => {
                    btn.classList.remove('active');
                });
                event?.target?.classList.add('active');
            }

            // Show/hide content
            lbEl('rootsContent').classList.toggle('hidden', tab !== 'roots');
            lbEl('tablesContent').classList.toggle('hidden', tab !== 'tables');
            lbEl('searchContent').classList.toggle('hidden', tab !== 'search');
            lbEl('multipliersContent').classList.toggle('hidden', tab !== 'multipliers');
            if (typeof window.scrollMainViewportToTop === 'function') {
                window.scrollMainViewportToTop();
            }
        }

        function showError(message) {
            forEachLegitInstance(function () {
                const errorDiv = lbEl('errorMessage');
                if (errorDiv) {
                    errorDiv.textContent = message;
                    errorDiv.classList.remove('hidden');
                }
            });
        }

        function hideError() {
            forEachLegitInstance(function () {
                const errorDiv = lbEl('errorMessage');
                if (errorDiv) errorDiv.classList.add('hidden');
            });
        }

        window.legitBuilderInstances.forEach(function (inst) {
            const si = document.getElementById((inst.idPrefix || '') + 'searchInput');
            if (si) {
                si.addEventListener('keypress', function (e) {
                    if (e.key === 'Enter') {
                        window.__lbCtx = inst;
                        searchRoot();
                    }
                });
            }
            const tf = document.getElementById((inst.idPrefix || '') + 'typeFilter');
            if (tf) {
                tf.addEventListener('change', function () {
                    window.__lbCtx = inst;
                    if (data) displayRootDefinitions();
                });
            }
        });
        
        function wireLegitBuilderOutputListeners() {
            window.legitBuilderInstances.forEach(function (inst) {
                const p = inst.idPrefix || '';
                const levelInput = document.getElementById(p + 'outputLevel');
                const seedInput = document.getElementById(p + 'outputSeed');
                const serialInputEl = inst.relaxedPicker
                    ? document.getElementById('mi_finalOutputString')
                    : document.getElementById(p + 'serialInput');
                if (levelInput && !levelInput.hasAttribute('data-listener-added')) {
                    levelInput.addEventListener('input', function () {
                        window.__lbCtx = inst;
                        updateFinalOutput();
                    });
                    levelInput.addEventListener('change', function () {
                        window.__lbCtx = inst;
                        updateFinalOutput();
                    });
                    levelInput.setAttribute('data-listener-added', 'true');
                }
                if (seedInput && !seedInput.hasAttribute('data-listener-added')) {
                    seedInput.addEventListener('input', function () {
                        window.__lbCtx = inst;
                        updateFinalOutput();
                    });
                    seedInput.addEventListener('change', function () {
                        window.__lbCtx = inst;
                        updateFinalOutput();
                    });
                    seedInput.setAttribute('data-listener-added', 'true');
                }
                if (serialInputEl && !serialInputEl.hasAttribute('data-legit-backpack-btn-sync')) {
                    const syncLegitBackpackBtn = function () {
                        if (typeof window.updateLegitValidatorBackpackButton === 'function') {
                            window.updateLegitValidatorBackpackButton();
                        }
                    };
                    serialInputEl.addEventListener('input', syncLegitBackpackBtn);
                    serialInputEl.addEventListener('change', syncLegitBackpackBtn);
                    serialInputEl.setAttribute('data-legit-backpack-btn-sync', 'true');
                }
                if (inst.relaxedPicker) {
                    const genEl = document.getElementById('mi_finalOutputString');
                    if (genEl && !genEl.hasAttribute('data-mi-generated-output')) {
                        const onGenEdit = function () {
                            window.__lbCtx = inst;
                            const base = genEl.value ? genEl.value.trim() : '';
                            if (
                                base &&
                                base.indexOf('|') !== -1 &&
                                !(typeof isBase85Format === 'function' && isBase85Format(base))
                            ) {
                                const merged = mergeModdedDecodedViewUiIntoHumanSerialText(base);
                                if (merged !== base) {
                                    genEl.value = merged;
                                }
                                serializeToBase85(merged);
                            }
                        };
                        genEl.addEventListener('input', onGenEdit);
                        genEl.addEventListener('change', onGenEdit);
                        genEl.setAttribute('data-mi-generated-output', 'true');
                    }
                }
                if (inst.relaxedPicker) {
                    const miFw = document.getElementById('mi_firmwareLock');
                    const miBb = document.getElementById('mi_buyback');
                    if (miFw && !miFw.hasAttribute('data-mi-flag-output')) {
                        const onMiFlag = function () {
                            window.__lbCtx = inst;
                            updateFinalOutput();
                            if (
                                window.__legitLastSingleValidation &&
                                typeof updateModdedDecodedSummaryFromValidation === 'function'
                            ) {
                                updateModdedDecodedSummaryFromValidation(window.__legitLastSingleValidation);
                            }
                        };
                        miFw.addEventListener('change', onMiFlag);
                        if (miBb) {
                            miBb.addEventListener('change', onMiFlag);
                        }
                        miFw.setAttribute('data-mi-flag-output', 'true');
                        if (miBb) {
                            miBb.setAttribute('data-mi-flag-output', 'true');
                        }
                    }
                }
                // Skin customization controls exist on both builders (legit + modded); wire for every
                // instance so selecting a skin applies to the serial immediately, not on the next update.
                // Resolve elements from inst.idPrefix (NOT lbEl(), which reads the global __lbCtx and would
                // wire the same dropdown twice while leaving the other instance's control unbound).
                {
                    const skinPrefix = inst.idPrefix || '';
                    const skinDd = document.getElementById(skinPrefix + 'skinCustomizationDropdown');
                    const skinNv = document.getElementById(skinPrefix + 'skinCustomizationValue');
                    if (skinDd && !skinDd.hasAttribute('data-skin-output-bound')) {
                        const refreshMiSummaryBadges = function () {
                            if (
                                window.__legitLastSingleValidation &&
                                typeof updateModdedDecodedSummaryFromValidation === 'function'
                            ) {
                                updateModdedDecodedSummaryFromValidation(window.__legitLastSingleValidation);
                            }
                        };
                        const onSkinDd = function () {
                            window.__lbCtx = inst;
                            if (skinNv && skinDd.value) {
                                skinNv.value = '';
                            }
                            updateFinalOutput();
                            refreshMiSummaryBadges();
                        };
                        const onSkinNv = function () {
                            window.__lbCtx = inst;
                            if (skinDd && skinNv && skinNv.value.trim()) {
                                skinDd.value = '';
                            }
                            updateFinalOutput();
                            refreshMiSummaryBadges();
                        };
                        skinDd.addEventListener('change', onSkinDd);
                        if (skinNv) {
                            skinNv.addEventListener('input', onSkinNv);
                        }
                        skinDd.setAttribute('data-skin-output-bound', 'true');
                    }
                }
            });
        }

        /** Modded: validation HTML lives inside #mi_moddedDecodeBody; keep collapsed after decode so the summary stays tidy until the user expands. */
        function ensureMiModdedDecodeAccordionCollapsedForModdedResult() {
            if (!(window.__lbCtx && window.__lbCtx.relaxedPicker)) {
                return;
            }
            if (typeof window.setMiModdedDecodeExpanded !== 'function') {
                return;
            }
            window.setMiModdedDecodeExpanded(false);
        }

        function wireModdedDecodeAccordionOnce() {
            if (window.__miModdedDecodeAccordionWired) {
                return;
            }
            const toggle = document.getElementById('mi_moddedDecodeToggle');
            const body = document.getElementById('mi_moddedDecodeBody');
            const chevron = document.getElementById('mi_moddedDecodeChevron');
            const expandHint = document.getElementById('mi_moddedDecodeExpandHint');
            if (!toggle || !body) {
                return;
            }
            window.__miModdedDecodeAccordionWired = true;
            function setMiModdedDecodeExpanded(expanded) {
                body.hidden = !expanded;
                toggle.setAttribute('aria-expanded', expanded ? 'true' : 'false');
                if (chevron) {
                    chevron.textContent = expanded ? '▼' : '▶';
                }
                if (expandHint) {
                    expandHint.textContent = expanded ? ' (Click to collapse)' : ' (Click to expand)';
                }
            }
            window.setMiModdedDecodeExpanded = setMiModdedDecodeExpanded;
            setMiModdedDecodeExpanded(false);
            toggle.addEventListener('click', function () {
                setMiModdedDecodeExpanded(!!body.hidden);
            });
        }

        document.addEventListener('DOMContentLoaded', function () {
            initializeApplicationMode();
            wireLegitBuilderOutputListeners();
            wireModdedDecodeAccordionOnce();
            if (typeof initUniqueQuickAccessPanels === 'function') {
                initUniqueQuickAccessPanels();
            }
            if (typeof window.refreshMiSkinCustomizationDropdown === 'function') {
                window.refreshMiSkinCustomizationDropdown();
            }
            const pack4Chk = document.getElementById('lbAlwaysLoadPack4');
            if (pack4Chk) {
                if (typeof localStorage !== 'undefined' && localStorage.getItem('lbAlwaysLoadPack4') == null) {
                    localStorage.setItem('lbAlwaysLoadPack4', '1');
                }
                pack4Chk.checked = typeof localStorage !== 'undefined' && localStorage.getItem('lbAlwaysLoadPack4') === '1';
                pack4Chk.addEventListener('change', function () {
                    if (typeof localStorage !== 'undefined') {
                        localStorage.setItem('lbAlwaysLoadPack4', pack4Chk.checked ? '1' : '0');
                    }
                });
            }
            const pack4Btn = document.getElementById('lbLoadPack4NowBtn');
            if (pack4Btn) {
                pack4Btn.addEventListener('click', function () {
                    const st = document.getElementById('lbPack4Status');
                    if (st) st.textContent = 'Loading…';
                    ensurePack4NexusLoaded().then(function (ok) {
                        if (!ok && st) {
                            st.textContent = 'Could not load (need network Nexus proxy, or already failed).';
                        }
                    });
                });
            }
        });

        setTimeout(wireLegitBuilderOutputListeners, 100);
        
        // Auto-load Nexus-Data files from PHP proxy on page load
        async function autoLoadNexusData() {
            // Determine the base URL for the PHP proxy.
            // Must support both legacy inv files and new itempool/itempoollist files.
            let proxyBaseUrl = null;

            const seen = new Set();
            const proxyCandidates = [];
            const addCandidate = (url) => {
                if (!url) return;
                if (seen.has(url)) return;
                seen.add(url);
                proxyCandidates.push(url);
            };

            // Electron desktop: local-api-server emulates nexus_data_proxy (fetch shim must already be installed).
            if (typeof window.ELECTRON_API_URL === 'string' && window.ELECTRON_API_URL) {
                addCandidate(`${window.ELECTRON_API_URL}/LegitItems/nexus_data_proxy.php`);
            }

            // Absolute host candidates for known deployments (most likely first).
            const origin = window.location.origin;
            const pathSegments = window.location.pathname.split('/').filter(Boolean);
            const firstSegment = pathSegments.length > 0 ? pathSegments[0] : '';
            if (firstSegment) {
                addCandidate(`${origin}/${firstSegment}/LegitItems/nexus_data_proxy.php`);
                addCandidate(`${origin}/${firstSegment}/legititems/nexus_data_proxy.php`);
            }
            addCandidate(`${origin}/LegitItems/nexus_data_proxy.php`);
            addCandidate(`${origin}/legititems/nexus_data_proxy.php`);

            // Current-directory and common relative layouts (case variants included).
            addCandidate('LegitItems/nexus_data_proxy.php');
            addCandidate('legititems/nexus_data_proxy.php');
            addCandidate('nexus_data_proxy.php');
            addCandidate('../LegitItems/nexus_data_proxy.php');
            addCandidate('../legititems/nexus_data_proxy.php');

            const probeProxyCandidate = async (baseUrl) => {
                try {
                    const [invResp, poolResp] = await Promise.all([
                        fetch(`${baseUrl}?file=inv0`, { cache: 'default' }),
                        fetch(`${baseUrl}?file=itempool0`, { cache: 'default' })
                    ]);
                    if (!invResp.ok || !poolResp.ok) return false;

                    const poolJson = await poolResp.json();
                    if (poolJson && typeof poolJson === 'object' && poolJson.error) {
                        // A valid proxy that does not support the new dataset key.
                        return false;
                    }
                    return true;
                } catch (error) {
                    return false;
                }
            };

            const probeStaticLegitItemsFolder = async (folderUrl) => {
                try {
                    const base = folderUrl.endsWith('/') ? folderUrl : folderUrl + '/';
                    const [invResp, poolResp] = await Promise.all([
                        fetch(base + 'Nexus-Data-inv0.json', { cache: 'default' }),
                        fetch(base + 'Nexus-Data-itempool0.json', { cache: 'default' })
                    ]);
                    if (!invResp.ok || !poolResp.ok) return null;
                    return base;
                } catch (_err) {
                    return null;
                }
            };

            for (const candidate of proxyCandidates) {
                // eslint-disable-next-line no-await-in-loop
                const isValid = await probeProxyCandidate(candidate);
                if (isValid) {
                    proxyBaseUrl = candidate;
                    break;
                }
            }

            let nexusLoadUsesStaticFiles = false;
            let resolvedNexusBase = proxyBaseUrl;

            if (
                !resolvedNexusBase &&
                (window.location.protocol === 'http:' || window.location.protocol === 'https:')
            ) {
                const pathForStatic = window.location.pathname;
                const basePathForStatic = pathForStatic.endsWith('/')
                    ? pathForStatic
                    : pathForStatic.replace(/\/[^/]*$/, '/');
                const staticFolderCandidates = [];
                staticFolderCandidates.push(`${origin}${basePathForStatic}LegitItems/`);
                staticFolderCandidates.push(`${origin}${basePathForStatic}legititems/`);
                if (firstSegment) {
                    staticFolderCandidates.push(`${origin}/${firstSegment}/LegitItems/`);
                    staticFolderCandidates.push(`${origin}/${firstSegment}/legititems/`);
                }
                try {
                    staticFolderCandidates.push(new URL('LegitItems/', window.location.href).href);
                    staticFolderCandidates.push(new URL('legititems/', window.location.href).href);
                } catch (_e) {
                    /* ignore */
                }
                const staticSeen = new Set();
                for (let si = 0; si < staticFolderCandidates.length; si++) {
                    const cand = staticFolderCandidates[si];
                    if (!cand || staticSeen.has(cand)) continue;
                    staticSeen.add(cand);
                    // eslint-disable-next-line no-await-in-loop
                    const staticBaseResolved = await probeStaticLegitItemsFolder(cand);
                    if (staticBaseResolved) {
                        resolvedNexusBase = staticBaseResolved;
                        nexusLoadUsesStaticFiles = true;
                        break;
                    }
                }
            }

            const REMOTE_NEXUS_PROXY_FALLBACK =
                'https://save-editor.be/LegitItems/nexus_data_proxy.php';
            if (!resolvedNexusBase) {
                if (window.ALLOW_REMOTE_NEXUS_FALLBACK === false) {
                    console.error(
                        '[Legit Builder] No local Nexus proxy or LegitItems/ JSON found, and ALLOW_REMOTE_NEXUS_FALLBACK is false. Load Nexus via folder picker or serve LegitItems next to the app.'
                    );
                    forEachLegitInstance(function () {
                        const container = lbEl('rootsContent');
                        if (container) {
                            container.innerHTML =
                                '<div class="loading" style="color:#ffb74d;">Nexus data not loaded: no bundled LegitItems/ and remote fallback disabled.</div>';
                        }
                    });
                    if (typeof window.updateDataStatusIndicator === 'function') {
                        window.updateDataStatusIndicator('Nexus JSON not loaded (no local source)', 'error');
                    }
                    return;
                }
                console.warn(
                    '[Legit Builder] Using remote Nexus proxy (save-editor.be). For bundled Application/LegitItems data, serve the app over HTTP(S) so static JSON loads, or use a local PHP proxy.'
                );
                resolvedNexusBase = REMOTE_NEXUS_PROXY_FALLBACK;
                nexusLoadUsesStaticFiles = false;
            }

            nexusProxyBaseUrlResolved = resolvedNexusBase;

            const nexusManifest = [
                ['inv0', 'Nexus-Data-inv0.json'],
                ['inv4', 'Nexus-Data-inv4.json'],
                ['inv6', 'Nexus-Data-inv6.json'],
                ['inv_name_part0', 'Nexus-Data-inv_name_part0.json'],
                ['inv_name_part4', 'Nexus-Data-inv_name_part4.json'],
                ['inv_name_part6', 'Nexus-Data-inv_name_part6.json'],
                ['inv_stat0', 'Nexus-Data-inv_stat0.json'],
                ['inv_stat4', 'Nexus-Data-inv_stat4.json'],
                ['inv_custom0', 'Nexus-Data-inv_custom0.json'],
                ['inv_custom4', 'Nexus-Data-inv_custom4.json'],
                ['ui_stat0', 'Nexus-Data-ui_stat0.json'],
                ['ui_stat4', 'Nexus-Data-ui_stat4.json'],
                ['ui_stat6', 'Nexus-Data-ui_stat6.json'],
                ['attribute0', 'Nexus-Data-attribute0.json'],
                ['attribute4', 'Nexus-Data-attribute4.json'],
                ['attribute6', 'Nexus-Data-attribute6.json'],
                ['gbx_ue_data_table0', 'Nexus-Data-gbx_ue_data_table0.json'],
                ['gbx_ue_data_table4', 'Nexus-Data-gbx_ue_data_table4.json'],
                ['gbx_ue_data_table6', 'Nexus-Data-gbx_ue_data_table6.json'],
                ['itempool0', 'Nexus-Data-itempool0.json'],
                ['itempool4', 'Nexus-Data-itempool4.json'],
                ['itempool6', 'Nexus-Data-itempool6.json'],
                ['itempoollist0', 'Nexus-Data-ItemPoolList0.json'],
                ['itempoollist4', 'Nexus-Data-ItemPoolList4.json'],
                ['itempoollist6', 'Nexus-Data-ItemPoolList6.json'],
                ['skilltrees_data0', 'Nexus-Data-skilltrees_data0.json'],
                ['skilltrees_data4', 'Nexus-Data-skilltrees_data4.json'],
                ['skilltrees_data6', 'Nexus-Data-skilltrees_data6.json'],
                ['uitooltipdata0', 'Nexus-Data-uitooltipdata0.json'],
                ['uitooltipdata4', 'Nexus-Data-uitooltipdata4.json'],
                ['uitooltipdata6', 'Nexus-Data-uitooltipdata6.json'],
                ['resident0', 'Nexus-Data-Resident0.json'],
                ['resident4', 'Nexus-Data-Resident4.json'],
                ['resident6', 'Nexus-Data-Resident6.json'],
                ['gbxactorpart0', 'Nexus-Data-GbxActorPart0.json'],
                ['gbxactorpart4', 'Nexus-Data-GbxActorPart4.json'],
                ['gbxactorpart6', 'Nexus-Data-GbxActorPart6.json'],
                ['challenge0', 'Nexus-Data-challenge0.json'],
                ['challenge4', 'Nexus-Data-challenge4.json'],
                ['challenge6', 'Nexus-Data-challenge6.json'],
                ['challenge_list4', 'Nexus-Data-challenge_list4.json'],
                ['challenge_list6', 'Nexus-Data-challenge_list6.json'],
                ['gbx_discovery_location_meta_data4', 'Nexus-Data-gbx_discovery_location_meta_data4.json'],
                ['gbx_discovery_location_meta_data6', 'Nexus-Data-gbx_discovery_location_meta_data6.json'],
                ['mission0', 'Nexus-Data-Mission0.json'],
                ['mission4', 'Nexus-Data-Mission4.json'],
                ['mission6', 'Nexus-Data-Mission6.json'],
                ['missionset0', 'Nexus-Data-missionset0.json'],
                ['missionset4', 'Nexus-Data-missionset4.json'],
                ['missionset6', 'Nexus-Data-missionset6.json'],
                ['game_region0', 'Nexus-Data-game_region0.json'],
                ['game_region4', 'Nexus-Data-game_region4.json'],
                ['game_region6', 'Nexus-Data-game_region6.json'],
                ['progress_graph_group0', 'Nexus-Data-progress_graph_group0.json'],
                ['progress_graph_group4', 'Nexus-Data-progress_graph_group4.json'],
                ['progress_graph0', 'Nexus-Data-progress_graph0.json'],
                ['progress_graph4', 'Nexus-Data-progress_graph4.json'],
                ['progress_graph6', 'Nexus-Data-progress_graph6.json']
            ];

            const nexusFilesAll = nexusManifest.map(function (row) {
                const q = row[0];
                const f = row[1];
                return {
                    url: nexusLoadUsesStaticFiles
                        ? resolvedNexusBase + f
                        : resolvedNexusBase + '?file=' + q,
                    filename: f
                };
            });

            const nexusFiles = nexusFilesAll;

            try {
                const nexusLoadBannerMsg = nexusLoadUsesStaticFiles
                    ? 'Auto-loading Nexus-Data from LegitItems/ (bundled static JSON)...'
                    : String(resolvedNexusBase || '').indexOf('save-editor.be') >= 0
                      ? 'Auto-loading Nexus-Data from remote server (save-editor.be)...'
                      : 'Auto-loading Nexus-Data files from server...';
                forEachLegitInstance(function () {
                    const container = lbEl('rootsContent');
                    if (container) {
                        container.innerHTML =
                            '<div class="loading">' + nexusLoadBannerMsg + '</div>';
                    }
                });

                await loadJsonSources(nexusFiles);

                if (loadedFileNames.length > 0) {
                    forEachLegitInstance(function () {
                        const fileListDiv = lbEl('fileList');
                        if (fileListDiv) fileListDiv.textContent = loadedFileNames.join(', ');
                        const loadedFilesDiv = lbEl('loadedFiles');
                        if (loadedFilesDiv) loadedFilesDiv.style.display = 'block';
                    });
                }

                // Same-origin fallback: fetch supplemental value files from the resolved
                // Nexus data location first, then try common bundled folders.
                if (window.location.protocol === 'http:' || window.location.protocol === 'https:') {
                    const path = window.location.pathname;
                    const basePath = path.endsWith('/') ? path : path.replace(/\/[^/]*$/, '/');
                    const supplementalBaseCandidates = [];
                    const supplementalSeen = new Set();
                    const addSupplementalBase = (url) => {
                        if (!url) return;
                        const normalized = url.endsWith('/') ? url : url + '/';
                        if (supplementalSeen.has(normalized)) return;
                        supplementalSeen.add(normalized);
                        supplementalBaseCandidates.push(normalized);
                    };
                    if (nexusLoadUsesStaticFiles) {
                        addSupplementalBase(resolvedNexusBase);
                    } else if (
                        typeof resolvedNexusBase === 'string' &&
                        /nexus_data_proxy\.php(?:\?.*)?$/i.test(resolvedNexusBase)
                    ) {
                        addSupplementalBase(
                            resolvedNexusBase.replace(/nexus_data_proxy\.php(?:\?.*)?$/i, '')
                        );
                    }
                    addSupplementalBase(window.location.origin + basePath + 'LegitItems/');
                    addSupplementalBase(window.location.origin + basePath + 'legititems/');
                    addSupplementalBase(window.location.origin + basePath + 'json/');
                    const fetchFirstAvailableSupplementalJson = async (name) => {
                        for (const base of supplementalBaseCandidates) {
                            try {
                                const response = await fetch(base + name, { cache: 'default' });
                                if (!response.ok) continue;
                                const parsed = await response.json();
                                if (parsed && typeof parsed === 'object') {
                                    return parsed;
                                }
                            } catch (_) {
                                /* ignore and try next candidate */
                            }
                        }
                        return null;
                    };
                    const mergeSupplementalFiles = async (fileNames, mergeFn) => {
                        const parsedFiles = await Promise.all(
                            fileNames.map(name => fetchFirstAvailableSupplementalJson(name))
                        );
                        for (let i = 0; i < parsedFiles.length; i++) {
                            const parsed = parsedFiles[i];
                            if (parsed && typeof parsed === 'object') {
                                mergeFn(data, parsed);
                            }
                        }
                    };
                    const valueFilesToFetch = VALUE_FILES;
                    const uiStatFiles = [
                        'Nexus-Data-ui_stat0.json',
                        'Nexus-Data-ui_stat4.json',
                        'Nexus-Data-ui_stat6.json'
                    ];
                    await mergeSupplementalFiles(valueFilesToFetch, mergeValueFilesIntoData);
                    await mergeSupplementalFiles(uiStatFiles, mergeUiStatIntoData);
                    const skilltreesFiles = [
                        'Nexus-Data-skilltrees_data0.json',
                        'Nexus-Data-skilltrees_data4.json',
                        'Nexus-Data-skilltrees_data6.json'
                    ];
                    const uitooltipdataFiles = [
                        'Nexus-Data-uitooltipdata0.json',
                        'Nexus-Data-uitooltipdata4.json',
                        'Nexus-Data-uitooltipdata6.json'
                    ];
                    await mergeSupplementalFiles(skilltreesFiles, mergeSkilltreesIntoData);
                    await mergeSupplementalFiles(uitooltipdataFiles, mergeUitooltipdataIntoData);
                    requestAnimationFrame(() => {
                        loadData();
                        if (typeof window.refreshCombinedDataStatus === 'function') {
                            window.refreshCombinedDataStatus();
                        }
                    });
                }
                if (typeof window.refreshCombinedDataStatus === 'function') {
                    window.refreshCombinedDataStatus();
                }
            } catch (error) {
                console.error('Auto-load failed:', error);
                showError('Auto-load failed: ' + error.message + '. Please load files manually.');
                if (typeof window.refreshCombinedDataStatus === 'function') {
                    if (!window.refreshCombinedDataStatus()) {
                        if (typeof window.updateDataStatusIndicator === 'function') {
                            window.updateDataStatusIndicator('Nexus data auto-load failed', 'error');
                        }
                    }
                } else if (typeof window.updateDataStatusIndicator === 'function') {
                    window.updateDataStatusIndicator('Nexus data auto-load failed', 'error');
                }
            }
        }

        /** Right-click / header: load Nexus JSON via the same network path as startup auto-load (not legacy game_data_export). */
        window.runLegitNexusAutoLoadFromNetwork = function () {
            return autoLoadNexusData();
        };
        
        // Auto-load on page load
        function setFinalOutputDetailsOpen() {
            forEachLegitInstance(function () {
                const details = lbEl('finalOutputDetails');
                if (!details) return;
                details.setAttribute('open', '');
            });
        }
        function scheduleLegitBuilderNexusAutoLoad() {
            function runWhenDomReady() {
                if (window.location.protocol === 'file:') {
                    let tries = 0;
                    const maxTries = 200;
                    const intervalId = setInterval(function () {
                        tries++;
                        if (window.IS_ELECTRON_APP) {
                            clearInterval(intervalId);
                            autoLoadNexusData();
                        } else if (tries >= maxTries) {
                            clearInterval(intervalId);
                            console.warn(
                                'Auto-load disabled: file:// protocol detected. Please use a web server or manually load files.'
                            );
                        }
                    }, 50);
                    return;
                }
                autoLoadNexusData();
            }
            function onReady() {
                runWhenDomReady();
                setFinalOutputDetailsOpen();
                window.addEventListener('resize', setFinalOutputDetailsOpen);
            }
            if (document.readyState === 'loading') {
                document.addEventListener('DOMContentLoaded', onReady);
            } else {
                onReady();
            }
        }
        scheduleLegitBuilderNexusAutoLoad();

        // Theme particle configurations
        const themeParticles = {
            'theme-default': ['⚙️', '🔧', '✨', '⭐', '🌟'],
            'theme-frozen': ['❄', '❅', '❆', '✦', '✧'],
            'theme-hellforge': ['🔥', '✨', '💫', '⭐', '🌟'],
            'theme-shadowforge': ['⚡', '💠', '🔮', '🌌', '✨'],
            'theme-blastforge': ['💣', '✨', '💥', '⭐', '🌟'],
            'theme-medforge': ['🏥', '✨', '💚', '⭐', '🌟'],
            'theme-barrforge': ['🛡️', '✨', '💙', '⭐', '🌟'],
            'theme-master': ['⚡', '✨', '⭐', '🌟', '💫'],
            'theme-save': ['💾', '🔐', '🔒', '✨', '💫', '⭐', '🌟'],
            'theme-scooters': ['🛴', '🧰', '✨', '💫', '⭐', '🌟', '💎'],
            'theme-midnight': ['🌙', '⭐', '🌟', '✨', '💫', '🌠', '💎'],
            'theme-mattmab': ['⚡', '🌌', '✨', '💫', '⭐', '🌟', '💎', '🔥', '💜', '💙'],
            'theme-venom': ['🕷️', '🐍', '☠️', '💀', '🦠', '🧪', '⚡', '💚', '🌑', '⚫'],
            'theme-skippy': ['🔥', '💀', '☠️', '⚡', '💥', '🌋', '🔥', '💀', '⚰️', '🔥'],
            'theme-dunkie': ['🦄', '✨', '💫', '⭐', '🌟', '🔮']
        };

        // Create header particles effect
        function createHeaderParticles(particleSymbols) {
            const container = document.getElementById('headerParticles');
            if (!container) return;
            container.innerHTML = '';

            // Create 12 floating particles at various positions
            for (let i = 0; i < 12; i++) {
                const particle = document.createElement('div');
                particle.className = 'header-snowflake';
                particle.textContent = particleSymbols[Math.floor(Math.random() * particleSymbols.length)];

                const left = 5 + Math.random() * 90; // 5-95%
                const top = 5 + Math.random() * 90; // 5-95%
                const delay = Math.random() * 6;
                const duration = 4 + Math.random() * 4; // 4-8 seconds

                particle.style.left = left + '%';
                particle.style.top = top + '%';
                particle.style.animationDelay = delay + 's';
                particle.style.animationDuration = duration + 's';

                container.appendChild(particle);
            }
        }

        // Create backdrop particle effect
        function createBackdropEffect(particleSymbols) {
            const backdrop = document.getElementById('backdropEffect');
            if (!backdrop) return;
            backdrop.innerHTML = '';

            const totalParticles = 100;

            for (let i = 0; i < totalParticles; i++) {
                const particle = document.createElement('div');
                particle.className = 'particle';
                particle.textContent = particleSymbols[Math.floor(Math.random() * particleSymbols.length)];

                let startLeft;
                if (Math.random() < 0.6) {
                    if (Math.random() < 0.5) {
                        startLeft = Math.random() * 20;
                    } else {
                        startLeft = 80 + Math.random() * 20;
                    }
                } else {
                    startLeft = 20 + Math.random() * 60;
                }

                const startTop = -50 + Math.random() * (window.innerHeight + 100);
                const duration = 5 + Math.random() * 10;
                const delay = Math.random() * 5;
                const size = 0.8 + Math.random() * 0.8;
                const opacity = 0.4 + Math.random() * 0.4;

                particle.style.left = startLeft + '%';
                particle.style.top = startTop + 'px';
                particle.style.fontSize = size + 'em';
                particle.style.opacity = opacity;
                particle.style.animation = `fall ${duration}s linear infinite`;
                particle.style.animationDelay = delay + 's';

                backdrop.appendChild(particle);
            }
        }
