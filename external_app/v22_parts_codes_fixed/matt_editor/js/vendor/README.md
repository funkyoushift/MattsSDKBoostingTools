# Vendor scripts

Third-party libraries, no build step. Load order is set in `index.html`.

- **js-yaml-4.3.2.js** – js-yaml 4.3.2 (MIT) and pako 2.1.0 (decompression). Exposes globals `jsyaml` and `pako` (used by item-editor-10-yaml-save and elsewhere).
