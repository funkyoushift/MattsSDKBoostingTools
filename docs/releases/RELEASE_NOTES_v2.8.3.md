### What's new

**v2.8.3**

- **F7 Quick Menu clicks work again** after travel / world load (the menu opened but buttons did not respond)
- **Human serials** encode locally (comma or space heads) so Boosting / BL4 Codes delivery still works when the Lootlemon API is down
- **Ground Loot** now has a distance slider and Local / Named / All / Other player scope for Pull, Hide, and Delete
- Hide Ground Loot is a one-way clear (Restore Hidden Loot was removed)

### Download: pick ONE

**Recommended for almost everyone**

Download and run:
- `MSBT-Installer-v2.8.3.exe`

**Manual install / portable Electron app**

Download and extract:
- `MSBT-Portable-v2.8.3-win-x64.zip`

**Do not manually download these unless you know why**

- `latest.json`, `latest.yml`, `*.blockmap`

### Upgrade notes

1. Open Updates → **Install / Update SDK Mod**, or copy the packaged `.sdkmod` into `sdk_mods`.
2. **Fully restart Borderlands 4** after the SDK install.
3. Do not overwrite `MattsSDKBoostingTools.sdkmod` while the game is open — that breaks inventory reads (`ZipImportError`).
4. Requires [oak2-mod-manager v0.3](https://github.com/bl-sdk/oak2-mod-manager/releases/tag/v0.3).
