[project]
name = "NearbyDump"
version = "0.4.0"
authors = [{ name = "MSBT" }]
description = "Dump nearby or map-wide live objects for Dev Spawner path hunting."

[tool.sdkmod]
name = "NearbyDump"
license = { name = "MIT" }
version = "0.4.0"
coop_support = "ClientSide"
supported_games = ["BL4"]
